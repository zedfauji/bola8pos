# Tauri signing keys — NEVER commit private keys
*.key
*.key.pub
*.pem

# AI / IDE tool configs and runtimes
.agents/
.claude/
.claude-flow/
.cursor/
.kiro/
.paperclip/
.swarm/
.vscode/*
!.vscode/extensions.json
.storybook/
.husky/

# Agent runtime files
agentdb.rvf*
skills-lock.json

# Binary / archive files
*.pdf
*.zip

# Design exports and one-off project folders
bar_pos_design_extracted/
design_handoff_pool_table_card/
installer/
tickets/

# Test / CI outputs (keep clones lean)
coverage/
artifacts/
e2e-results/
e2e-blob-reports/
playwright-report/
verification-artifacts/

# Storybook
*storybook.log
storybook-static

# Local lint/type dumps (regenerate anytime)
eslint-report.json
lint-results*.json
lint_report*.json
lint-results-clean*.json
lint_output*.txt
tsc_errors.txt

# Dev scratch / one-off files
fix_lint.cjs
docs/
.planning/

# Markdown docs (CLAUDE.md is the exception — it's project instructions)
*.md
!CLAUDE.md
