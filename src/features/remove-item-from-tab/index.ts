/**
 * Remove line items from the in-memory cart before an order is placed.
 * Cart state lives on {@link useCartStore} in the tab entity.
 */
export { useCartStore } from '@entities/tab/model/cartStore';
