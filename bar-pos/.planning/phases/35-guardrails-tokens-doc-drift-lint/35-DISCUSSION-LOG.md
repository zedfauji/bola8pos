# Phase 35: Guardrails — Tokens Doc & Drift Lint - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-15
**Phase:** 35-guardrails-tokens-doc-drift-lint
**Areas discussed:** Token doc scope & location, Lint mechanism & categories, Lint severity & exemptions, CI integration, Custom rule implementation location, Pre-merge violation sweep, Cross-referencing the lint rule from the token doc

---

## Token doc scope & location

| Option | Description | Selected |
|--------|-------------|----------|
| Tailwind + component conventions | Cover tailwind.config.ts/index.css tokens AND touch-target/focus-ring tiers | ✓ |
| Strictly tailwind.config.ts/index.css only | Matches DOCS-01's literal wording exactly | |

| Option | Description | Selected |
|--------|-------------|----------|
| bar-pos/DESIGN-TOKENS.md | Matches existing root-level doc pattern | ✓ |
| docs/design-tokens.md | New docs/ subfolder | |
| Inline in CLAUDE.md | Add section to CLAUDE.md | |

| Option | Description | Selected |
|--------|-------------|----------|
| Tokens + do/don't examples | Actionable use-this-not-that pairing | ✓ |
| Tokens list only | Plain reference table | |

| Option | Description | Selected |
|--------|-------------|----------|
| Hand-written | No generator tooling | |
| Generated by script | Zero drift between doc and config | ✓ |

| Option | Description | Selected |
|--------|-------------|----------|
| One-line dark mode note | Colors are CSS vars auto-resolving per theme | ✓ |
| Skip dark mode mention | Out of scope for this doc | |

| Option | Description | Selected |
|--------|-------------|----------|
| scripts/generate-design-tokens.ts, manual npm script | Matches scripts/ convention, manual trigger | ✓ |
| Auto-run in CI, fail if stale | New CI gate, guarantees zero drift | |

| Option | Description | Selected |
|--------|-------------|----------|
| Generated-block markers | Script only touches marked sections; hand-written examples survive regeneration | ✓ |
| Fully regenerated, examples baked into script | Whole file regenerated every run | |

| Option | Description | Selected |
|--------|-------------|----------|
| docs:tokens | Short, verb:noun pattern | ✓ |
| generate:design-tokens | More explicit/verbose | |

**User's choice:** DESIGN-TOKENS.md at repo root, covering tailwind config + component conventions, generated (marker-based hybrid) via `scripts/generate-design-tokens.ts` / `npm run docs:tokens`, with hand-written do/don't examples and a one-line dark-mode note.
**Notes:** User picked "Generated by script" against the initial recommendation (hand-written) — reconciled with the do/don't-examples requirement via generated-block markers so both coexist in one file.

---

## Lint mechanism & categories

| Option | Description | Selected |
|--------|-------------|----------|
| Custom no-restricted-syntax | Zero new dependency, reuses Phase 29's proven regex approach | |
| eslint-plugin-tailwindcss | New dependency, purpose-built for Tailwind hygiene | ✓ (initial) |
| Both | Plugin for general hygiene + custom rules for specific patterns | ✓ (final, after reconciliation) |

| Option | Description | Selected |
|--------|-------------|----------|
| All 4 categories | Matches DRIFT-AUDIT.md's full scope | ✓ |
| Button/input only | Narrower, skip hex-color/spacing | |

**Reconciliation follow-up:** eslint-plugin-tailwindcss alone only covers arbitrary-spacing (category 4); flagged that it can't reach raw button/input/hex-color (categories 1-3). User confirmed "Both" (plugin + custom rules) to cover all 4 categories as originally requested.

**User's choice:** eslint-plugin-tailwindcss + custom no-restricted-syntax rules, covering all 4 Phase 29 drift categories.
**Notes:** Initial "eslint-plugin-tailwindcss" answer alone would have under-delivered on "all 4 categories" — surfaced the mismatch and got explicit confirmation before locking.

---

## Lint severity & exemptions

| Option | Description | Selected |
|--------|-------------|----------|
| Error | Blocks npm run lint/CI, matches max-warnings:0 | ✓ |
| Warn first, error later | Softer rollout | |

| Option | Description | Selected |
|--------|-------------|----------|
| shared/ui/* only | Primitive-definition layer is the only legitimate exception | ✓ |
| shared/ui/* + Storybook stories | Also exempt *.stories.tsx | |

**User's choice:** `error` severity, `shared/ui/*` is the only exemption.

---

## CI integration

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into existing eslint config | No new CI step, already gated by npm run lint | ✓ |
| Separate advisory script | New standalone check, not CI-gated | |

**User's choice:** Fold into existing `eslint.config.js` / `npm run lint`.

---

## Custom rule implementation location

| Option | Description | Selected |
|--------|-------------|----------|
| Inline in eslint.config.js | Single source for all custom rule config | |
| Separate eslint-rules/no-ui-drift.js, imported | Groups UI-drift-specific rules together | ✓ |

**User's choice:** Separate `eslint-rules/no-ui-drift.js`, imported into `eslint.config.js`.

---

## Pre-merge violation sweep

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, verify zero violations first | Cheap safety check before flipping to error | ✓ |
| Skip — trust prior phases' verification | Redundant given 29-33/33.1 already proved conformance | |

**User's choice:** Run `npm run lint` as a verification task before flipping severity to `error`.

---

## Cross-referencing the lint rule from the token doc

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, one-line note | Tells readers conventions are actively checked | ✓ |
| No — keep doc and lint fully separate | Pure reference doc | |

**User's choice:** One-line note in DESIGN-TOKENS.md referencing `eslint-rules/no-ui-drift.js`.

---

## Claude's Discretion

- Exact wording/table formatting inside DESIGN-TOKENS.md's generated-block markers.
- Exact regex patterns for the 3 custom no-restricted-syntax rules (informed by `scripts/audit-ui-drift.ts`).
- Ordering of tasks within the phase's wave(s).

## Deferred Ideas

None — discussion stayed within phase scope.
