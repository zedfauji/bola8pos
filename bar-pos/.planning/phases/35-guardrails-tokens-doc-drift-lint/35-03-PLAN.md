---
phase: 35-guardrails-tokens-doc-drift-lint
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - eslint-rules/no-ui-drift.js
  - eslint.config.js
  - src/features/manage-products/ui/CategoryForm.tsx
  - src/features/manage-categories/ui/CategoryTreeEditor.tsx
  - src/widgets/InventoryPagePanel.tsx
autonomous: true
requirements: [LINT-01]

must_haves:
  truths:
    - "A raw button, a non-exempt raw input, an exact hex-color literal, an rgb()/rgba() literal, or an arbitrary-value spacing class introduced in src/pages|widgets|features fails `npm run lint` at error severity"
    - "Native type=color/time/date/file inputs are NOT flagged (structural :not(:has(...)) exemption)"
    - "The pre-existing barrel-export (export *) ban still fires in pages/widgets/features (not silently replaced by the merge hazard)"
    - "`npm run lint` passes clean against the full codebase (D-14) — zero violations, zero warnings"
    - "The 4 pre-negotiated Phase-31 exception sites carry real eslint-disable-next-line directives, not prose-only comments"
  artifacts:
    - "eslint-rules/no-ui-drift.js exporting uiDriftSelectors (5 no-restricted-syntax selectors)"
    - "eslint.config.js: a pages/widgets/features-scoped config object with the tailwindcss plugin + the merged no-restricted-syntax array"
  key_links:
    - "uiDriftSelectors is spread into a no-restricted-syntax array that ALSO restates the ExportAllDeclaration selector, inside a config object scoped to src/pages|widgets|features (test/stories ignored), placed AFTER the existing repo-wide block so it wins the last-config-object merge for those files"
    - "eslint-plugin-tailwindcss settings.tailwindcss.callees includes 'cn'; whitelist covers tw-animate-css utilities; no-arbitrary-value is intentionally NOT enabled (D-15 — spacing drift is covered by the custom selector)"
---

<objective>
Wire the drift-detection lint rules that prevent regression on all 4 of Phase 29's categories (raw button, raw input, hardcoded hex/rgb, arbitrary-value spacing) at error severity (D-08..D-15), fold them into the existing eslint.config.js (D-13), convert the 3 pre-negotiated Phase-31 exception categories from prose comments to real suppressions, and prove the whole thing with a positive disposable-fixture smoke test AND the D-14 zero-violation `npm run lint` gate. This is the last plan of the milestone — it locks the conformance achieved by Phases 30-33/33.1 into an enforced guardrail.

Purpose: Make it impossible to silently reintroduce the drift the earlier phases fixed. The custom project-specific selectors (button/input/hex → route through shared/ui primitives) live in their own file (D-10); the Tailwind-class rules come from eslint-plugin-tailwindcss.
Output: eslint-rules/no-ui-drift.js, an extended eslint.config.js, and real eslint-disable directives at the 4 documented exception sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-CONTEXT.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 03 slice)

- **New file:** `eslint-rules/no-ui-drift.js` exporting `uiDriftSelectors` (5 selectors: raw button, raw input with type-exemption, exact hex literal, rgb()/rgba() literal, arbitrary-value spacing class)
- **Modified:** `eslint.config.js` — imports the plugin + selectors; adds one pages/widgets/features-scoped config object (test/stories ignored) carrying the tailwindcss plugin rules and the merged `no-restricted-syntax` array
- **New ESLint rule config blocks:** `tailwindcss/no-custom-classname` + `tailwindcss/enforces-shorthand` (scoped), and the 5 custom `no-restricted-syntax` selectors, all at `error`
- **Modified:** `CategoryForm.tsx`, `CategoryTreeEditor.tsx`, `InventoryPagePanel.tsx` — 4 prose-only exception comments converted to real `eslint-disable-next-line no-restricted-syntax` directives

<tasks>

<task type="auto">
  <name>Task 1: Create eslint-rules/no-ui-drift.js and prove all 5 selectors fire (disposable fixture, Wave-0 positive proof)</name>
  <files>eslint-rules/no-ui-drift.js</files>
  <read_first>
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md ("eslint-rules/no-ui-drift.js (config, transform)" section — the exact 5 selector strings, including the input type-exemption `:not(:has(...))` clause and the D-15 narrow arbitrary-spacing selector)
    - scripts/audit-ui-drift.ts (CATEGORY_PATTERNS lines 25-40 — the regexes the selectors are the AST-equivalent of; the arbitrary-spacing prefix list to mirror exactly)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-RESEARCH.md (Code Examples for the input/hex selectors; Pitfall 2 for the type-based exemption rationale)
    - eslint.config.js (existing no-restricted-syntax block at lines 125-131 — the shape to be compatible with; note package.json has "type":"module" so the new .js file is ESM)
  </read_first>
  <action>
Create eslint-rules/no-ui-drift.js as an ESM module exporting a single named const `uiDriftSelectors` — a plain array of 5 selector objects (NOT a full rule config, NOT a plugin). Use the exact selector strings from 35-PATTERNS.md's "eslint-rules/no-ui-drift.js" section: (1) raw button via `JSXOpeningElement[name.name='button']`; (2) raw input with the type-based permanent exemption via `JSXOpeningElement[name.name='input']:not(:has(JSXAttribute[name.name='type'][value.value=/^(color|time|date|file)$/]))`; (3) exact hex-color literal via `Literal` matching an anchored 3-or-6-digit hex; (4) rgb()/rgba() literal via `Literal` matching `rgba?\(`; (5) the D-15 narrow arbitrary-spacing selector — a `Literal` matching the exact Phase-29 spacing-prefix list (p|px|py|pt|pb|pl|pr|m|mx|my|mt|mb|ml|mr|gap|gap-x|gap-y|space-x|space-y) followed by a bracketed arbitrary value. Each selector's `message` must match the developer-facing copy in 35-UI-SPEC.md's Copywriting Contract (point to DESIGN-TOKENS.md and name the correct shared primitive). Do NOT enable eslint-plugin-tailwindcss's no-arbitrary-value here — spacing is covered by selector 5 (D-15).

Then PROVE the selectors actually match (Nyquist positive proof — a rule with 0 matches is ambiguous between "clean" and "selector never matches"). Create a disposable fixture `.tsx` file (NOT under src/, NOT committed) containing exactly one violation of each category — a raw button element, a raw text input, an exact hex string literal, an rgba string literal, and an arbitrary-spacing className — PLUS one `type="date"` input that must NOT fire (proving the exemption). Create a disposable minimal flat-config that applies only a `no-restricted-syntax` rule built from `uiDriftSelectors` to that fixture. Run eslint against the fixture with the disposable config and `--no-ignore`; confirm it exits non-zero, reports the button/input/hex/rgb/spacing messages, and does NOT report the date input. Then DELETE both disposable files — do not commit them.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./eslint-rules/no-ui-drift.js').then(m=>{const a=m.uiDriftSelectors; if(!Array.isArray(a)||a.length!==5||!a.every(s=>typeof s.selector==='string'&&typeof s.message==='string')){process.exit(1)} process.exit(0)}).catch(()=>process.exit(1))"</automated>
  </verify>
  <acceptance_criteria>
    - eslint-rules/no-ui-drift.js exports `uiDriftSelectors` as an array of exactly 5 objects, each with a string `selector` and a string `message`
    - The input selector contains the `:not(:has(...type...color|time|date|file...))` exemption clause; the arbitrary-spacing selector mirrors scripts/audit-ui-drift.ts's spacing-prefix list (NOT the plugin's no-arbitrary-value)
    - The disposable-fixture run exited NON-ZERO, reporting one violation each for the raw button, raw text input, hex literal, rgba literal, and arbitrary-spacing class — and reported NOTHING for the type=date input (exemption proven)
    - The disposable fixture and disposable config were deleted and are not committed (git status shows only eslint-rules/no-ui-drift.js as new)
    - Selector messages match 35-UI-SPEC.md Copywriting Contract and reference DESIGN-TOKENS.md
  </acceptance_criteria>
  <done>eslint-rules/no-ui-drift.js is committed with 5 proven-firing selectors; the throwaway fixture demonstrated each fires and the type-exemption holds, then was removed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the rules into eslint.config.js, convert the 4 exception sites, and pass the D-14 clean-lint gate</name>
  <files>eslint.config.js, src/features/manage-products/ui/CategoryForm.tsx, src/features/manage-categories/ui/CategoryTreeEditor.tsx, src/widgets/InventoryPagePanel.tsx</files>
  <read_first>
    - eslint.config.js (full file — the existing repo-wide `files: ['**/*.{ts,tsx}']` block at lines 33-133 with its `no-restricted-syntax: ['error', {ExportAllDeclaration ...}]`; the test-override block at 134-147 which does NOT touch no-restricted-syntax; the ignores list at the top)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md ("eslint.config.js (modified)" section AND the "Shared Patterns" section — the flat-config merge hazard, the shared/ui exemption, the pre-negotiated suppression sites)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-RESEARCH.md (Pattern 2 for the plugin block; Pitfalls 4 & 5 for the whitelist + cn callee; Pitfall 2 for the exact exception sites and decision ids)
    - src/features/manage-products/ui/CategoryForm.tsx (hex literals at lines 36 and 138 — the useState default `'#6B7280'` and the placeholder; the type="color" input at line 120 is structurally exempt and needs NO change)
    - src/features/manage-categories/ui/CategoryTreeEditor.tsx (hex literal at line 472 — the default category-color seed; the type="color" input at line 88 is structurally exempt)
    - src/widgets/InventoryPagePanel.tsx (the signed-delta type="number" input at lines 351-358 — NOT type-exempt; the prose exception note references 31-CONTEXT.md D-06)
  </read_first>
  <action>
Wire two things into eslint.config.js, then converge on a clean lint.

(A) Import the plugin and the selectors at the top: the default import of eslint-plugin-tailwindcss and the named `uiDriftSelectors` from ./eslint-rules/no-ui-drift.js.

(B) Add ONE new flat-config object, placed AFTER the existing repo-wide `files: ['**/*.{ts,tsx}']` block (so it wins the last-config-object merge for the files it matches), scoped to `files: ['src/pages/**/*.tsx', 'src/widgets/**/*.tsx', 'src/features/**/*.tsx']` with `ignores: ['**/*.test.tsx', '**/*.stories.tsx']` (test/stories carry raw elements in mock components and must stay out of scope, matching Phase 29's audit exclusions — the existing test-override block does NOT disable no-restricted-syntax, so without this ignore the rule would flag test fixtures). This object:
  - registers `plugins: { tailwindcss: <plugin> }` with `settings.tailwindcss` = `{ config: './tailwind.config.ts', callees: ['cn','clsx','classnames','ctl','cva','tv'], whitelist: [<tw-animate-css regex>] }` — `cn` MUST be added (this repo's helper, absent from plugin defaults — Pitfall 5); the whitelist MUST cover tw-animate-css utilities (Pitfall 4).
  - enables `tailwindcss/no-custom-classname: 'error'` and `tailwindcss/enforces-shorthand: 'error'` (D-08). Do NOT enable `tailwindcss/no-arbitrary-value` (D-15 — it would flag ~70 pre-existing non-spacing bracket values; arbitrary-spacing is covered by uiDriftSelectors instead).
  - sets `no-restricted-syntax: ['error', <the ExportAllDeclaration selector copied VERBATIM from the existing block>, ...uiDriftSelectors]`. Restating ExportAllDeclaration here is MANDATORY: flat config replaces (does not merge) a rule key across matching config objects, so this later, more-specific object fully overrides `no-restricted-syntax` for pages/widgets/features files — omitting the barrel-export selector would silently kill that ban for those files. Leave the existing repo-wide block untouched so the barrel ban still applies everywhere else. Keep severity at `error` (D-11) with no warn-first period.

(C) Convert the 4 pre-negotiated Phase-31 exception sites from prose-only comments to REAL suppressions (they will otherwise become genuine errors the instant the rules go live). At each site add an `eslint-disable-next-line no-restricted-syntax -- <reason citing the decision id>` directive on the line immediately preceding the flagged node — using a `//` comment in JS/attribute contexts and a `{/* ... */}` comment in JSX-children contexts:
  - CategoryForm.tsx line 36 — the `'#6B7280'` useState default (category.color is per-row user data, 31-CONTEXT.md D-08).
  - CategoryForm.tsx line 138 — the `placeholder="#6B7280"` attribute value (D-08).
  - CategoryTreeEditor.tsx line 472 — the `'#6366f1'` default category-color seed (D-08).
  - InventoryPagePanel.tsx line 351 — the signed-delta `type="number"` input, which is NOT covered by the type-based exemption because MoneyInput clamps negatives (31-CONTEXT.md D-06); this is a JSX child of FormField, so use the `{/* eslint-disable-next-line no-restricted-syntax -- ... */}` form.
These directives MUST land in the SAME commit as (A)/(B): committed earlier they would be flagged as unused-disable-directive warnings (ESLint 9 reports unused directives by default) and fail `npm run lint --max-warnings 0`; committed later the exception sites would be hard errors.

(D) Run `npm run lint`. Resolve to a clean exit-0 (D-14):
  - Real drift the rules catch that is NOT a documented exception → that is an unplanned regression; STOP and surface it (do not blanket-suppress).
  - `tailwindcss/enforces-shorthand` findings are behavior-neutral and auto-fixable → apply via `npm run lint:fix` and re-run.
  - `tailwindcss/no-custom-classname` false positives on legitimately-imported utility classes (tw-animate-css or other third-party `@import`ed classes) → extend the `whitelist` regex to cover them and re-run; note each addition in the SUMMARY. Do NOT whitelist a class that is actually a typo/drift.
Repeat until `npm run lint` exits 0 with zero warnings.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with zero warnings against the full codebase (D-14 — the negative proof: no false positives on the now-conformant tree)
    - eslint.config.js contains one pages/widgets/features-scoped config object (test/stories ignored) that registers the tailwindcss plugin, enables no-custom-classname + enforces-shorthand at error, and does NOT enable no-arbitrary-value
    - That object's `no-restricted-syntax` array restates the ExportAllDeclaration selector AND spreads uiDriftSelectors; the barrel-export ban still fires in a features/ file (verifiable by a temporary `export *` reproducing an error, then reverting)
    - settings.tailwindcss.callees includes 'cn'; the whitelist covers the tw-animate-css utilities actually used
    - The 4 exception sites (CategoryForm ×2, CategoryTreeEditor ×1, InventoryPagePanel ×1) carry real `eslint-disable-next-line no-restricted-syntax` directives with `-- <decision id>` justifications; there are no unused-disable-directive warnings
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The drift rules are live at error severity, folded into the existing eslint.config.js; the 4 documented exceptions are really suppressed; the barrel ban is preserved; and `npm run lint` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced at runtime. This plan changes lint configuration and adds compile-time suppressions only. The one third-party dependency (eslint-plugin-tailwindcss) was installed and legitimacy-checked in plan 35-02; here it merely runs at lint time. No authentication, session, access-control, input-validation-at-a-trust-boundary, cryptography, network, or user-facing runtime surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03 | — | eslint.config.js / no-ui-drift.js / exception suppressions | low | accept | Out of ASVS scope per 35-RESEARCH.md Security Domain — dev-tooling (static analysis at lint time), no runtime attack surface. The plugin's supply-chain threat is T-35-SC, mitigated in plan 35-02. Documented, accepted. |
</threat_model>

<verification>
- `npm run lint` — exits 0, zero warnings (D-14, LINT-01)
- `npm run typecheck` — exits 0
- Positive proof (Task 1 disposable fixture): each of the 5 selectors fires; the type=date input is exempt
- Regression proof: a temporary `export *` in a features/ file still errors (barrel ban preserved despite the scoped-block merge)
</verification>

<success_criteria>
LINT-01 satisfied: a drift-detection lint rule set (eslint-plugin-tailwindcss's no-custom-classname/enforces-shorthand + the 5 custom no-restricted-syntax selectors) is folded into the existing eslint.config.js at error severity, scoped to the Phase-29 audit zone, covering all 4 drift categories, with the codebase passing `npm run lint` clean — added only after conformance, and proven to actually fire.
</success_criteria>

<output>
Create `.planning/phases/35-guardrails-tokens-doc-drift-lint/35-03-SUMMARY.md` when done.
</output>
