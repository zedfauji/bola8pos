---
phase: 35-guardrails-tokens-doc-drift-lint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN-TOKENS.md
  - scripts/generate-design-tokens.ts
  - package.json
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "DESIGN-TOKENS.md exists at the bar-pos repo root documenting color, border-radius, spacing, and typography tokens PLUS the touch-target (44/56/72px) and focusEmphasis conventions"
    - "Running `npm run docs:tokens` a second time on unchanged source produces no git diff (idempotent generation)"
    - "The generated token tables reflect the real values in tailwind.config.ts and src/app/globals.css (no invented tokens)"
  artifacts:
    - "DESIGN-TOKENS.md"
    - "scripts/generate-design-tokens.ts"
    - "package.json docs:tokens script"
  key_links:
    - "scripts/generate-design-tokens.ts default-imports ../tailwind.config and regex-reads src/app/globals.css, rewriting ONLY the text between the GENERATED:START / GENERATED:END markers"
    - "DESIGN-TOKENS.md hand-written sections (Touch Targets, Focus Emphasis, Dark Mode, do/don't, lint cross-reference) live OUTSIDE the markers and survive regeneration"
---

<objective>
Ship DESIGN-TOKENS.md (the design-tokens reference doc) and its generator, satisfying DOCS-01. The doc documents tokens that ALREADY EXIST in tailwind.config.ts / src/app/globals.css / POSButton.tsx / button.tsx — no new tokens are invented, no visual change is made.

Purpose: Give developers one authoritative reference for the color/spacing/typography/border-radius tokens and the touch-target/focus conventions established across Phases 30-33, and the rationale the Phase-35 lint rules enforce.
Output: DESIGN-TOKENS.md (root doc), scripts/generate-design-tokens.ts (generator), package.json `docs:tokens` script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-CONTEXT.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 slice)

- **New file:** `DESIGN-TOKENS.md` (repo root, sibling to FSD-STRUCTURE.md/DOMAIN-CONTRACTS.md/SUPABASE-CONTRACTS.md)
- **New file:** `scripts/generate-design-tokens.ts`
- **New npm script:** `docs:tokens` → `npx tsx scripts/generate-design-tokens.ts`

<tasks>

<task type="auto">
  <name>Task 1: Hand-author the DESIGN-TOKENS.md skeleton (markers + fixed sections)</name>
  <files>DESIGN-TOKENS.md</files>
  <read_first>
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md (the "DESIGN-TOKENS.md (doc, file-I/O)" section — generated-block skeleton D-07, token source values to document, do/don't convention D-02)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-UI-SPEC.md (Spacing Scale / Typography / Color sections — the exact current values the doc must reproduce; note "no custom spacing/type scale — Tailwind defaults")
    - tailwind.config.ts (color + borderRadius token names)
    - src/app/globals.css (the :root oklch values and --radius)
    - src/shared/ui/POSButton.tsx (touchSize tiers: default 44px / large 56px / xl 72px)
    - src/shared/ui/button.tsx (focusEmphasis CVA variant: default '' / high 'focus-visible:ring-4 focus-visible:ring-ring')
    - FSD-STRUCTURE.md (root-doc header/formatting convention to mirror)
  </read_first>
  <action>
Create DESIGN-TOKENS.md at the bar-pos repo root (D-05). Give it: a `# Design Tokens` H1; a one-line purpose sentence; the D-04 cross-reference line stating that these conventions are enforced by `eslint-rules/no-ui-drift.js` (forward reference — that file lands in plan 35-03, same phase); an empty generated block delimited by the exact HTML-comment markers `<!-- GENERATED:START -->` and `<!-- GENERATED:END -->` (D-07) with a one-line placeholder inside noting the block is machine-generated via `npm run docs:tokens`; then the hand-written sections that live OUTSIDE the markers and are never touched by the generator: a "Touch Targets" section documenting the 44/56/72px `touchSize` tiers from POSButton.tsx (D-01), a "Focus Emphasis" section documenting the `focusEmphasis` default/high variant from button.tsx (D-01), a "Dark Mode" one-liner stating colors are `var(--x)` CSS vars that resolve per-theme automatically because darkMode is `media`/auto (D-03), and short do/don't usage examples per category (D-02, e.g. prefer the `bg-primary` token class over a hardcoded blue). Use the exact token names and values from the read_first source files — do not fabricate a spacing or typography scale that the config does not define; state plainly that spacing/typography use Tailwind defaults (no project-specific extension exists), per 35-UI-SPEC.md. Mirror the markdown formatting style of FSD-STRUCTURE.md. Do NOT populate the generated block with real tables yet — Task 2's generator does that.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('DESIGN-TOKENS.md','utf8'); const ok=s.includes('<!-- GENERATED:START -->')&&s.includes('<!-- GENERATED:END -->')&&/##\s*Touch Targets/i.test(s)&&/##\s*Focus Emphasis/i.test(s)&&/##\s*Dark Mode/i.test(s)&&/no-ui-drift\.js/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN-TOKENS.md exists at the bar-pos repo root
    - Both `<!-- GENERATED:START -->` and `<!-- GENERATED:END -->` markers are present, in that order
    - Hand-written "Touch Targets", "Focus Emphasis", and "Dark Mode" sections exist OUTSIDE the markers, documenting the exact 44/56/72px tiers, the focusEmphasis high variant, and the media-dark-mode note respectively
    - The doc contains the D-04 cross-reference naming `eslint-rules/no-ui-drift.js`
    - The doc states spacing/typography are Tailwind defaults (no invented scale) per 35-UI-SPEC.md
  </acceptance_criteria>
  <done>DESIGN-TOKENS.md skeleton is committed with markers and all hand-written sections; the generated block is present but not yet populated.</done>
</task>

<task type="auto">
  <name>Task 2: Build the generator script + docs:tokens npm script + populate the generated block</name>
  <files>scripts/generate-design-tokens.ts, package.json, DESIGN-TOKENS.md</files>
  <read_first>
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md ("scripts/generate-design-tokens.ts (utility, transform)" section — header/doc-comment pattern, OUTPUT_PATH + marker constants, structured-read-then-render-then-write, marker-replace write pattern)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-RESEARCH.md (Code Examples: "Reading tailwind.config.ts structurally" and "Reading globals.css CSS custom properties")
    - scripts/audit-ui-drift.ts (the exact standalone-script conventions to mirror: node:fs/node:path imports, OUTPUT_PATH constant, main() read→render→write→console-summary shape)
    - tailwind.config.ts (theme.extend.colors and borderRadius — the structured source)
    - src/app/globals.css (:root oklch values to resolve each var against)
    - package.json (scripts block — insert docs:tokens alongside setup:dev-users/seed:dev/index-codebase using the same `npx tsx scripts/<name>.ts` style)
  </read_first>
  <action>
Create scripts/generate-design-tokens.ts following the audit-ui-drift.ts conventions (standalone Node script, `import * as fs from 'node:fs'`, top-of-file doc comment stating it is not imported by src/ and is run via `npm run docs:tokens`). Define an `OUTPUT_PATH = 'DESIGN-TOKENS.md'` constant and the marker-constant pair `GENERATED_START = '<!-- GENERATED:START -->'` / `GENERATED_END = '<!-- GENERATED:END -->'`. Source the token values structurally: default-import `../tailwind.config` (tsx resolves the .ts module natively) to read `config.theme.extend.colors` and `config.theme.extend.borderRadius`; regex-read `src/app/globals.css` for the `:root` custom-property values (the `--x: oklch(...)` lines). Build a markdown string containing: a Colors table (one row per entry in theme.extend.colors — token name, its Tailwind class form e.g. `bg-primary`, the `var(--x)` reference, and the resolved light-mode oklch value from globals.css); a Border Radius table (lg/md/sm → their `var(--radius)`-based values, plus `--radius` itself); and a short Spacing + Typography note stating these use Tailwind defaults (no project extension exists) rather than fabricating a scale. Write by reading the existing DESIGN-TOKENS.md and replacing everything between GENERATED_START and GENERATED_END with the freshly-built markdown (marker-replace regex per PATTERNS.md), preserving all hand-written content outside the markers. End with a console summary of counts written (audit-ui-drift.ts style). Add the `docs:tokens` script to package.json's scripts block: `npx tsx scripts/generate-design-tokens.ts`. Then run `npm run docs:tokens` once to populate the generated block, and commit the populated DESIGN-TOKENS.md. Do NOT auto-run this in CI or a git hook (D-06) — it is a manual regeneration command.
  </action>
  <verify>
    <automated>npm run docs:tokens && git diff --exit-code DESIGN-TOKENS.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run docs:tokens` exits 0 and writes real color + border-radius tables into the generated block, sourced from tailwind.config.ts + globals.css
    - Re-running `npm run docs:tokens` produces NO git diff on DESIGN-TOKENS.md (idempotent), proving the generator is deterministic and only touches the marker region
    - Hand-written sections (Touch Targets / Focus Emphasis / Dark Mode / do-don't / lint cross-reference) are unchanged by the generator run
    - package.json scripts block contains `docs:tokens` invoking the generator via npx tsx, matching the existing script style
    - `npm run typecheck` exits 0 (the generator's imports resolve; note the script is not under src/ so it is not linted by `npm run lint`)
  </acceptance_criteria>
  <done>scripts/generate-design-tokens.ts, the docs:tokens npm script, and a populated DESIGN-TOKENS.md are committed; the generator is idempotent against unchanged source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None introduced. This plan is dev-tooling only — a static markdown doc plus a build-time generator script that reads two source files and writes one doc. No authentication, session, access-control, input-validation-at-a-trust-boundary, cryptography, network, or user-facing runtime surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | — | DESIGN-TOKENS.md / generate-design-tokens.ts | low | accept | Out of ASVS scope per 35-RESEARCH.md Security Domain — dev-tooling with no runtime attack surface. No new dependency, no runtime code, no data flow. Documented, accepted. |
</threat_model>

<verification>
- `npm run docs:tokens && git diff --exit-code DESIGN-TOKENS.md` — generator is idempotent (DOCS-01)
- `npm run typecheck` — exits 0
- DESIGN-TOKENS.md contains both the generated color/border-radius tables and the hand-written Touch Targets / Focus Emphasis / Dark Mode sections + lint cross-reference
</verification>

<success_criteria>
DOCS-01 satisfied: a design-tokens reference doc exists at the repo root, its token tables are generated from and match tailwind.config.ts/globals.css exactly, and it additionally documents the touch-target and focus conventions from Phases 30-33. No token is invented; no visual change is made.
</success_criteria>

<output>
Create `.planning/phases/35-guardrails-tokens-doc-drift-lint/35-01-SUMMARY.md` when done.
</output>
