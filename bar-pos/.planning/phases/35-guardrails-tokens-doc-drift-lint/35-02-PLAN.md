---
phase: 35-guardrails-tokens-doc-drift-lint
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx
  - package.json
  - package-lock.json
autonomous: false
requirements: [LINT-01]
user_setup: []

must_haves:
  truths:
    - "The day-of-week toggle in PromotionAvailabilityEditor.tsx renders the shared Button component, not a native button element"
    - "eslint-plugin-tailwindcss@3.18.3 is installed as an EXACT-pinned devDependency (not ^ or latest)"
    - "npm run lint still passes clean after the promotion fix — no regression, the new drift rules are not yet wired (that is plan 35-03)"
  artifacts:
    - "package.json devDependency: eslint-plugin-tailwindcss pinned to 3.18.3"
  key_links:
    - "PromotionAvailabilityEditor day-toggle uses Button from @shared/ui/button (already imported at line 20 — no new import), mirroring the already-fixed ComboAvailabilityEditor sibling"
---

<objective>
Make the pages/widgets/features zone genuinely conformant on the raw-button category BEFORE the drift lint rules flip to error (D-16), and install the pinned eslint-plugin-tailwindcss dependency behind the mandated package-legitimacy checkpoint. This unblocks plan 35-03's zero-violation `npm run lint` gate (D-14).

Purpose: RESEARCH.md found one live, previously-undocumented raw button (a day-picker chip in manage-promotions) that landed after Phase 29's audit and was never fixed. If it is not fixed, D-14's "lint passes clean" checkpoint cannot honestly pass. This plan removes it and stages the plugin.
Output: PromotionAvailabilityEditor.tsx conformant; eslint-plugin-tailwindcss@3.18.3 in devDependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-CONTEXT.md
@.planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02 slice)

- **Modified:** `src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx` — raw day-toggle button swapped for the shared `Button` (D-16)
- **New devDependency:** `eslint-plugin-tailwindcss@3.18.3` (exact pin) in package.json

<tasks>

<task type="auto">
  <name>Task 1: Fix the undocumented raw day-toggle button in PromotionAvailabilityEditor (D-16)</name>
  <files>src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx</files>
  <read_first>
    - src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx (the file being modified — the raw day-toggle element is around lines 91-105; `Button` is already imported at line 20)
    - src/features/manage-combos/ui/ComboAvailabilityEditor.tsx (the already-fixed sibling — its day-toggle at lines 89-106 uses `<Button variant="outline" ...>` with the identical props/className; copy that exact shape)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-PATTERNS.md ("PromotionAvailabilityEditor.tsx (D-16 remediation fix)" section — target shape, straight element-tag swap, no new import)
  </read_first>
  <action>
In PromotionAvailabilityEditor.tsx, replace the native day-of-week toggle element (the day-picker chip in the WindowRow sub-component) with the shared `Button` component from @shared/ui/button, exactly mirroring the already-fixed ComboAvailabilityEditor.tsx sibling: add `variant="outline"` and keep every existing prop (`key`, `type="button"`, `aria-pressed`, `onClick`) and the existing conditional `className` template literal unchanged. `Button` is already imported (line 20) so no import change is needed — this is a straight opening/closing element-tag swap. Do NOT touch the two native `type="time"` inputs in the same file — those are a documented, permanent type-based exception (matching the sibling's own still-raw time inputs) and will be structurally exempt from the plan-35-03 input rule. Make no behavior, prop, handler, or styling change beyond the tag swap.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The day-of-week toggle in PromotionAvailabilityEditor.tsx renders the shared `Button` (with `variant="outline"`), not a native button element, matching ComboAvailabilityEditor.tsx exactly
    - No new import was added (Button was already imported); the two `type="time"` inputs are untouched
    - `npm run typecheck` exits 0 and `npm run lint` exits 0 (no regression; the pre-existing behavior is unchanged)
    - No prop, handler, or className value changed other than the element tag and the added `variant="outline"`
  </acceptance_criteria>
  <done>PromotionAvailabilityEditor.tsx no longer contains a raw day-toggle button; typecheck and lint are clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Package legitimacy gate for eslint-plugin-tailwindcss@3.18.3</name>
  <action>Blocking-human gate — do NOT install the package or proceed to Task 2 until the human confirms the package legitimacy. This checkpoint is never auto-approvable (SUS-flagged package). Present the verification points below, wait for explicit "approved".</action>
  <what-built>Nothing yet — this is a package-legitimacy gate BEFORE installing a third-party dev dependency. The next task installs `eslint-plugin-tailwindcss@3.18.3`.</what-built>
  <how-to-verify>
Per the package-legitimacy protocol, confirm the package is legitimate before install. Context from 35-RESEARCH.md's Package Legitimacy Audit:
1. The automated legitimacy verdict is **SUS**, reason `too-new` — but that flag inspects the package's LATEST tag (`4.2.0`, published ~2 days before research), NOT the version this phase installs.
2. The pinned version being installed is **3.18.3** — published ~3 months before research, ~1.44M weekly downloads, backed by a 5-year-old real repo `github.com/francoismassart/eslint-plugin-tailwindcss`.
3. Verify at https://www.npmjs.com/package/eslint-plugin-tailwindcss that version 3.18.3 exists, the weekly download count is in the low millions, and the repository link resolves to the francoismassart repo.
4. Confirm the pin is exact (`3.18.3`, NOT `^3.18.3` or `latest`) — the 4.x line requires Tailwind v4 and would break this Tailwind-v3 repo.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the pinned 3.18.3 install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install eslint-plugin-tailwindcss@3.18.3 (exact pin)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (devDependencies block — the plugin lands here; note all other eslint-plugin-* entries use `^` but this one must be an EXACT pin)
    - .planning/phases/35-guardrails-tokens-doc-drift-lint/35-RESEARCH.md (Standard Stack + Installation — the exact `npm install -D eslint-plugin-tailwindcss@3.18.3` command and the peer-dependency compatibility rationale)
  </read_first>
  <action>
Run `npm install -D eslint-plugin-tailwindcss@3.18.3` from the bar-pos directory. Confirm package.json records the dependency as the EXACT string `3.18.3` (no caret, no range) — if npm writes a caret, edit package.json to pin it exactly and re-run `npm install` to update the lockfile. Do NOT wire the plugin into eslint.config.js yet — that is plan 35-03. This task only stages the dependency so plan 35-03's config import resolves.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const v=p.devDependencies['eslint-plugin-tailwindcss']; if(v!=='3.18.3'){console.error('expected exact 3.18.3, got',v);process.exit(1)}" && node -e "require.resolve('eslint-plugin-tailwindcss')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains `"eslint-plugin-tailwindcss": "3.18.3"` — the exact version string, not a range
    - The package resolves from node_modules (`require.resolve('eslint-plugin-tailwindcss')` succeeds)
    - package-lock.json is updated to reflect the install
    - eslint.config.js is UNCHANGED (wiring is deferred to plan 35-03)
    - `npm run lint` still exits 0 (installing a plugin without wiring it changes no lint behavior)
  </acceptance_criteria>
  <done>eslint-plugin-tailwindcss@3.18.3 is installed and exact-pinned; the config is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev + CI toolchain | A new third-party dev dependency (`eslint-plugin-tailwindcss`) is pulled from npm and executes at lint time on developer/CI machines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-SC | Tampering | `npm install eslint-plugin-tailwindcss@3.18.3` | high | mitigate | Blocking-human `checkpoint:human-verify` before install (never auto-approvable, per SUS protocol); pin to exact 3.18.3 (~3 months old, ~1.44M weekly downloads, 5-year real repo); lockfile records the resolved tree; no postinstall script per 35-RESEARCH.md verification. |
| T-35-02 | — | PromotionAvailabilityEditor.tsx tag swap | low | accept | Behavior-neutral element swap mirroring an already-shipped sibling; guarded by typecheck + lint. Out of ASVS scope (no auth/data/trust-boundary surface). |
</threat_model>

<verification>
- `npm run typecheck` — exits 0
- `npm run lint` — exits 0 (no regression from the promotion fix; plugin installed but unwired)
- package.json pins eslint-plugin-tailwindcss to exact 3.18.3
</verification>

<success_criteria>
The last live raw button in the pages/widgets/features zone is removed (D-16), and eslint-plugin-tailwindcss@3.18.3 is installed behind the legitimacy checkpoint — leaving the codebase conformant on the raw-button category and the plugin ready to wire in plan 35-03.
</success_criteria>

<output>
Create `.planning/phases/35-guardrails-tokens-doc-drift-lint/35-02-SUMMARY.md` when done.
</output>
