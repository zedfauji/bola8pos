---
phase: 16-kitchen-bar-split-routing
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [KBR-01]
must_haves:
  truths:
    - "The categories.routing column exists in the live database and categories.is_food no longer exists"
    - "supabase.types.ts matches the live schema (routing present, is_food absent)"
  artifacts:
    - path: "src/shared/lib/supabase.types.ts"
      provides: "generated types confirmed against pushed schema"
      contains: "routing"
  key_links:
    - from: "supabase/migrations/20260706000001_categories_routing.sql"
      to: "live categories table"
      via: "supabase db push"
      pattern: "routing"
---

<objective>
Apply the Phase 16 schema change to the live database. Run `supabase db push` so the `20260706000001_categories_routing.sql` migration executes against the remote project, then confirm the generated types reflect the pushed schema.

Purpose: **[BLOCKING]** — without this push, the `routing` column does not exist in the live DB. Build and typecheck pass without it (types come from `supabase.types.ts`, not the live DB), which would create a false-positive verification state where E2E and runtime silently break. This gate must complete before the Phase 16 verification/E2E plan (16-07) runs.
Output: Migration applied to the remote database; `supabase.types.ts` confirmed accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 16-02 slice)

- Applied migration state: `categories.routing` column live, `categories.is_food` dropped
- Confirmed `src/shared/lib/supabase.types.ts` (regenerated or manually-verified to match)

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push categories_routing migration to the live database</name>
  <files>supabase/migrations/20260706000001_categories_routing.sql, src/shared/lib/supabase.types.ts</files>
  <read_first>
    - supabase/migrations/20260706000001_categories_routing.sql (the migration to apply)
    - CLAUDE.md (Supabase section — "Missing generated types workaround" + migration DOWN-script note; remote pushes are user-confirmed in this project)
    - .planning/STATE.md (prior "supabase db push confirmed" decisions — remote project, migration-history drift caveats)
  </read_first>
  <action>
    From `bar-pos/`, run `supabase db push` to apply `20260706000001_categories_routing.sql` to the remote project (export `SUPABASE_ACCESS_TOKEN` first if the non-TTY shell requires it; confirm the `[Y/n]` prompt). If migration-history drift is reported for older files (a known pre-existing condition per STATE.md), use `--include-all` — do NOT edit older migrations. After the push succeeds, regenerate types if a local stack is available (`npx supabase gen types typescript --local > src/shared/lib/supabase.types.ts`); if Docker/local stack is unavailable, keep the manual 16-01 edit (it already matches). This is a human-gated step because the remote push is interactive and destructive (drops is_food) — do not auto-advance past it.
  </action>
  <what-built>
    Plan 16-01 authored `supabase/migrations/20260706000001_categories_routing.sql` (creates the `category_routing` enum, adds `categories.routing`, backfills from `is_food` per D-03, drops `is_food`) and manually updated `supabase.types.ts`. Nothing has been applied to the live database yet.
  </what-built>
  <how-to-verify>
    1. From `bar-pos/`, run `supabase db push`. If the CLI requires an access token in this non-TTY shell, export `SUPABASE_ACCESS_TOKEN` first. Confirm the `[Y/n]` prompt to push `20260706000001_categories_routing.sql` to the remote project.
    2. Confirm the push output reports the migration as applied with no error. (If migration-history drift is reported for older files — a known pre-existing condition per STATE.md — `--include-all` may be needed; do NOT edit older migrations.)
    3. Regenerate types if a local stack is available: `npx supabase gen types typescript --local > src/shared/lib/supabase.types.ts`. If Docker/local stack is unavailable (documented recurring condition in this project), KEEP the manual 16-01 edit — it already matches: verify `grep routing src/shared/lib/supabase.types.ts` shows the categories column and `grep is_food src/shared/lib/supabase.types.ts` returns nothing.
    4. Sanity-check the live column via any available Supabase client/SQL: `SELECT routing FROM categories LIMIT 1;` succeeds and `SELECT is_food FROM categories LIMIT 1;` errors (column dropped).
  </how-to-verify>
  <verify>
    <automated>cd bar-pos && grep -q "routing" src/shared/lib/supabase.types.ts && ! grep -q "is_food" src/shared/lib/supabase.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` reports the migration applied against the remote project
    - `SELECT routing FROM categories` succeeds and `SELECT is_food FROM categories` errors on the live DB
    - `supabase.types.ts` references `routing` and not `is_food`
  </acceptance_criteria>
  <resume-signal>Type "pushed" once `supabase db push` has applied the migration and the routing column is confirmed live, or describe the blocker (auth/drift) encountered.</resume-signal>
  <done>Migration applied to the remote database (routing column live, is_food dropped); supabase.types.ts confirmed accurate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → remote Postgres | `supabase db push` applies DDL to production data; an interrupted or partial push could leave categories in an inconsistent column state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Denial (data loss) | `supabase db push` of the is_food→routing swap | mitigate | Migration wraps the backfill + drop in a single `BEGIN/COMMIT` (16-01), so a failed push rolls back atomically — categories are never left with routing populated but is_food dropped mid-way. |
| T-16-05 | Tampering | false-positive verification (types pass, DB missing column) | mitigate | This blocking gate exists specifically to force the push before 16-07; the plan is `autonomous: false` so it cannot be skipped by auto-advance. |
| T-16-SC | Tampering | package installs | accept | No package installs — CLI push only. |
</threat_model>

<verification>
- `supabase db push` reports the migration applied
- `SELECT routing FROM categories` succeeds against the live DB; `SELECT is_food FROM categories` errors
- `supabase.types.ts` references `routing` and not `is_food`
</verification>

<success_criteria>
KBR-01 (complete): `category.routing` enum column is live in the database, migrated with the D-03 backfill default; existing categories now carry a routing value derived from their former is_food flag.
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-02-SUMMARY.md` when done.
</output>
