---
phase: 16-kitchen-bar-split-routing
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-03]
files_modified:
  - src/entities/kds/model/types.ts
  - src/entities/kds/model/queries.ts
  - src/widgets/KdsBoard/index.tsx
  - src/pages/kds/index.tsx
  - src/app/kds-bar-route.tsx
  - src/pages/kds-bar/index.tsx
  - src/app/router.tsx
autonomous: true
requirements: [KBR-02, KBR-03, KBR-04]
must_haves:
  truths:
    - "useKdsItems returns only order items whose category routing equals the requested station"
    - "The /kds board shows KITCHEN items; a new /kds-bar board shows BAR items using the same widget"
    - "/kds-bar is reachable only through the KdsBarRoute guard (view_kds_bar)"
    - "KDS cards render a RoutingBadge for the item's routing"
    - "Empty/error copy on each board names its own station (kitchen vs bar)"
  artifacts:
    - path: "src/entities/kds/model/queries.ts"
      provides: "useKdsItems(routing) + parameterized kdsKeys.items(routing)"
      contains: "routing"
    - path: "src/widgets/KdsBoard/index.tsx"
      provides: "KdsBoard({ routing }) + RoutingBadge on cards + station-derived copy"
      contains: "RoutingBadge"
    - path: "src/app/kds-bar-route.tsx"
      provides: "KdsBarRoute guard (view_kds_bar)"
      exports: ["KdsBarRoute"]
    - path: "src/pages/kds-bar/index.tsx"
      provides: "Bar Display page rendering KdsBoard routing=BAR"
      exports: ["default"]
  key_links:
    - from: "src/widgets/KdsBoard/index.tsx"
      to: "useKdsItems"
      via: "useKdsItems(routing) call"
      pattern: "useKdsItems\\(routing\\)"
    - from: "src/app/router.tsx"
      to: "KdsBarRoute + KdsBarPage"
      via: "/kds-bar Route registration"
      pattern: "kds-bar"
---

<objective>
Ship the bar KDS board end to end by parameterizing the existing KDS stack with a routing filter (D-06/D-07) and cloning the route/page (D-05). The `useKdsItems` query, the `KdsBoard` widget, the `/kds` page, a new `KdsBarRoute` guard, a new `/kds-bar` page, and the router registration all live in this one plan so the interface change and its only consumers land together (no cross-wave broken signature).

Purpose: Reuse `KdsBoard`/`KdsCard`/`useKdsItems` for both stations instead of duplicating the board (D-06), gate `/kds-bar` to bartender+ via the `KdsRoute` pattern (D-05), and surface `RoutingBadge` on cards (D-08) — while leaving the existing kitchen board behavior identical (KBR-04).
Output: A working `/kds-bar` (Bar Display) page showing BAR-routed items, an unchanged `/kds` showing KITCHEN items, and RoutingBadge-annotated cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md
@.planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 16-05 slice)

- `KdsOrderItem.routing` — `routing` field on `KdsOrderItemSchema` (replaces `isFood`)
- `useKdsItems(routing: 'KITCHEN' | 'BAR')` — parameterized query
- `kdsKeys.items(routing)` — routing-scoped query key
- `KdsBoard({ routing: 'KITCHEN' | 'BAR' })` — parameterized widget with station-derived copy + RoutingBadge on cards
- `KdsBarRoute` — `src/app/kds-bar-route.tsx` route guard (`view_kds_bar`)
- `KdsBarPage` (default export) — `src/pages/kds-bar/index.tsx`
- `/kds-bar` route registration in `src/app/router.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Parameterize useKdsItems + KdsOrderItem by routing</name>
  <files>src/entities/kds/model/types.ts, src/entities/kds/model/queries.ts</files>
  <read_first>
    - src/entities/kds/model/types.ts (full file — KdsOrderItemSchema, isFood at line 10)
    - src/entities/kds/model/queries.ts (full file — kdsKeys 10-13, useKdsItems signature 15, select is_food at 44-47, filter 72-73, push isFood:true at 87, realtime bridge invalidate at 119)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (sections "KdsOrderItemSchema.isFood → routing" and "useKdsItems() routing param" — exact select/filter/key/push/realtime edits)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (section B — KdsOrderItem needs a routing field for the card badge)
    - src/shared/lib/domain.ts (CategoryRoutingSchema, produced by 16-01)
  </read_first>
  <action>
    Per D-07: In types.ts, replace `isFood: z.boolean(),` with `routing: CategoryRoutingSchema,` and import `CategoryRoutingSchema` from `@shared/lib/domain` (alongside the existing KdsStatusSchema/UuidSchema/TimestampSchema import).
    In queries.ts: (1) change `useKdsItems()` to `useKdsItems(routing: 'KITCHEN' | 'BAR')`; (2) parameterize the query key so the two boards do not share a cache entry — change `items: () => [...kdsKeys.all, 'items'] as const,` to `items: (routing: 'KITCHEN' | 'BAR') => [...kdsKeys.all, 'items', routing] as const,` and pass `kdsKeys.items(routing)` as the `queryKey`; (3) in the Supabase select, change `categories!inner(id, is_food)` to `categories!inner(id, routing)`; (4) replace the filter `const isFood = row.products?.categories?.is_food ...; if (!isFood) continue;` with `const categoryRouting = row.products?.categories?.routing as string | undefined; if (categoryRouting !== routing) continue;`; (5) in the pushed item object, replace `isFood: true,` with `routing: categoryRouting as 'KITCHEN' | 'BAR',`; (6) in `useKdsRealtimeBridge`, change the invalidate target from `kdsKeys.items()` to `kdsKeys.all` (broad invalidate — the bridge does not know which board(s) are mounted, so invalidate both boards' caches).
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "useKdsItems(routing" src/entities/kds/model/queries.ts && grep -q "categories!inner(id, routing)" src/entities/kds/model/queries.ts && grep -q "categoryRouting !== routing" src/entities/kds/model/queries.ts && grep -q "queryKey: kdsKeys.all" src/entities/kds/model/queries.ts && ! grep -q "is_food" src/entities/kds/model/queries.ts && ! grep -q "isFood" src/entities/kds/model/queries.ts && ! grep -q "isFood" src/entities/kds/model/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useKdsItems` signature is `useKdsItems(routing: 'KITCHEN' | 'BAR')` and its queryKey is `kdsKeys.items(routing)`
    - `kdsKeys.items` takes a routing argument and appends it to the key tuple
    - The Supabase select joins `categories!inner(id, routing)` and the loop filter is `categoryRouting !== routing`
    - The pushed KdsOrderItem carries `routing`, not `isFood`
    - `useKdsRealtimeBridge` invalidates `kdsKeys.all`
    - No `is_food`/`isFood` token remains in types.ts or queries.ts
  </acceptance_criteria>
  <done>useKdsItems filters by the requested station and shapes items with a routing field; both KDS caches are keyed and invalidated correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Thread routing prop through KdsBoard (copy + RoutingBadge) + update /kds page</name>
  <files>src/widgets/KdsBoard/index.tsx, src/pages/kds/index.tsx</files>
  <read_first>
    - src/widgets/KdsBoard/index.tsx (full file — KdsCard name block 42-59, ComboKdsCard name+ComboBadge block 101-113, KdsBoard signature + useKdsItems() at 154-156, loading copy 166, error copy 172, empty copy 200-206)
    - src/pages/kds/index.tsx (full file — passes <KdsBoard /> with no prop)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (sections B, D — RoutingBadge placement in cards + parameterized empty/error copy via stationLabel)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (section "KdsBoard/index.tsx — routing prop threading")
    - src/shared/ui/RoutingBadge.tsx (produced by 16-03 — imported here)
  </read_first>
  <action>
    Per D-06/D-08: change the `KdsBoard()` signature to `KdsBoard({ routing }: { routing: 'KITCHEN' | 'BAR' })` and call `useKdsItems(routing)`. Add `import { RoutingBadge } from '@shared/ui/RoutingBadge';` alongside the existing `ComboBadge` import (line 6). Derive `const stationLabel = routing === 'KITCHEN' ? 'kitchen' : 'bar';` inside the component and use it in the three hardcoded copy strings per UI-SPEC section D: error becomes `Could not load ${stationLabel} queue.` (line ~172) and empty becomes `No active ${stationLabel} orders` (line ~200-206). (The `Loading kitchen queue...` string at line 166 is not required by UI-SPEC to change — parameterize it too for consistency using stationLabel, since it is a trivial matching edit.)
    Per UI-SPEC section B: in `KdsCard`, wrap the product-name `<p>` in a `<div className="flex items-center gap-2">` and add `<RoutingBadge routing={item.routing} />` immediately after the name. In `ComboKdsCard`, the name is already inside a `flex items-center gap-2` div with `<ComboBadge />`; insert `<RoutingBadge routing={item.routing} />` between the name `<p>` and `<ComboBadge />`.
    Then update `src/pages/kds/index.tsx` to pass `routing="KITCHEN"` explicitly to `<KdsBoard />` (no implicit default — explicit at both call sites per UI-SPEC section C). Leave the "Kitchen Display" title unchanged.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "routing }: { routing:" src/widgets/KdsBoard/index.tsx && grep -q "useKdsItems(routing)" src/widgets/KdsBoard/index.tsx && grep -q "RoutingBadge routing={item.routing}" src/widgets/KdsBoard/index.tsx && grep -q "stationLabel" src/widgets/KdsBoard/index.tsx && grep -q 'routing="KITCHEN"' src/pages/kds/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `KdsBoard` takes a `routing: 'KITCHEN' | 'BAR'` prop and passes it to `useKdsItems(routing)`
    - Both `KdsCard` and `ComboKdsCard` render `<RoutingBadge routing={item.routing} />` next to the product name (RoutingBadge before ComboBadge in the combo card)
    - Empty and error copy derive from `stationLabel` (no bare "food"/"kitchen" literal remains in the empty/error strings)
    - `src/pages/kds/index.tsx` renders `<KdsBoard routing="KITCHEN" />`
  </acceptance_criteria>
  <done>KdsBoard is station-parameterized with RoutingBadge on cards and station-specific copy; the kitchen page passes routing explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Add KdsBarRoute guard + /kds-bar page + router registration</name>
  <files>src/app/kds-bar-route.tsx, src/pages/kds-bar/index.tsx, src/app/router.tsx</files>
  <read_first>
    - src/app/kds-route.tsx (full file — the guard to clone; single string swap to view_kds_bar)
    - src/pages/kds/index.tsx (full file — the page to clone; title + prop swap)
    - src/app/router.tsx (full file — guard imports 6-10, lazy imports 12-27, /kds Route block 129-138)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (sections C, E — exact /kds-bar page + KdsBarRoute code, silent-redirect precedent)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (sections "kds-bar-route.tsx (new)", "pages/kds-bar/index.tsx (new)", "router.tsx — register /kds-bar")
  </read_first>
  <action>
    Per D-05: create `src/app/kds-bar-route.tsx` as an exact clone of `kds-route.tsx` with the permission check swapped to `can('view_kds_bar')` and the component/type renamed `KdsBarRoute`/`KdsBarRouteProps`. Keep the silent `<Navigate to="/home" replace />` denial (no toast — matches KdsRoute, NOT AuditRoute's toast variant).
    Create `src/pages/kds-bar/index.tsx` as an exact clone of `pages/kds/index.tsx` with `export default function KdsBarPage()`, the `PageContainer title="Bar Display"`, and `<KdsBoard routing="BAR" />` (verbatim from UI-SPEC section C).
    In `router.tsx`: add `import { KdsBarRoute } from './kds-bar-route';` alongside the other route-guard imports; add `const KdsBarPage = lazy(() => import('../pages/kds-bar'));` alongside `KdsPage`; add a `<Route path="/kds-bar">` block immediately after the existing `/kds` block, wrapping `<KdsBarPage />` in `<ProtectedRoute><KdsBarRoute>…</KdsBarRoute></ProtectedRoute>` (same shape as the /kds block).
  </action>
  <verify>
    <automated>cd bar-pos && test -f src/app/kds-bar-route.tsx && test -f src/pages/kds-bar/index.tsx && grep -q "can('view_kds_bar')" src/app/kds-bar-route.tsx && grep -q 'KdsBoard routing="BAR"' src/pages/kds-bar/index.tsx && grep -q 'title="Bar Display"' src/pages/kds-bar/index.tsx && grep -q 'path="/kds-bar"' src/app/router.tsx && grep -q "KdsBarRoute" src/app/router.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `KdsBarRoute` checks `can('view_kds_bar')` and silently redirects to `/home` on denial (no toast)
    - `KdsBarPage` default-exports a page with title "Bar Display" rendering `<KdsBoard routing="BAR" />`
    - `router.tsx` registers `/kds-bar` inside `<ProtectedRoute><KdsBarRoute>…` with a lazy `KdsBarPage` import
  </acceptance_criteria>
  <done>/kds-bar is registered, guarded by view_kds_bar, and renders the bar-routed board.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → /kds-bar route | route access must be limited to view_kds_bar holders (bartender+); kitchen role must be redirected |
| category routing → board query | the query filter decides which items each board shows; a wrong equality could leak kitchen items onto the bar board or vice versa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Elevation of privilege | `/kds-bar` route | mitigate | `KdsBarRoute` enforces `can('view_kds_bar')` and redirects to `/home` otherwise, wrapped inside `ProtectedRoute` (auth) — identical to the vetted KdsRoute pattern (D-05). |
| T-16-11 | Information disclosure | cross-station item leak | mitigate | `useKdsItems(routing)` filters on strict routing equality (`categoryRouting !== routing → continue`); `/kds` passes KITCHEN, `/kds-bar` passes BAR — no shared query, keys separated by routing so caches never bleed. |
| T-16-12 | Tampering | existing kitchen board regressed | mitigate | `/kds` now passes `routing="KITCHEN"` and `useKdsItems('KITCHEN')` reproduces the prior `is_food=true` set exactly (KITCHEN backfilled from is_food=true per D-03); `view_kds` unchanged. Verified by 28-kds regression in 16-07 (KBR-04). |
| T-16-SC | Tampering | package installs | accept | No package installs — clone + parameterize existing files. |
</threat_model>

<verification>
- `useKdsItems('KITCHEN')` and `useKdsItems('BAR')` filter by station; caches are keyed separately
- KdsBoard renders RoutingBadge on both card variants and station-specific empty/error copy
- `/kds` renders KITCHEN, `/kds-bar` renders BAR, guarded by view_kds_bar
- No `isFood`/`is_food` remains across the five KDS files
- NOTE: full-project typecheck/lint gate runs in 16-07 after the whole sweep (incl. 16-06) lands.
</verification>

<success_criteria>
KBR-02: `/kds-bar` renders bar-routed order items, gated to bartender+.
KBR-03: RoutingBadge is displayed on KDS cards.
KBR-04 (supporting): the kitchen board and `view_kds` are untouched; only the routing param and copy changed.
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-05-SUMMARY.md` when done.
</output>
