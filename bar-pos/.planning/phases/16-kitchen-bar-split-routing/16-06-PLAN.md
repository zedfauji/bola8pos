---
phase: 16-kitchen-bar-split-routing
plan: 06
type: execute
wave: 3
depends_on: [16-03, 16-04]
files_modified:
  - src/features/manage-categories/ui/CategoryTreeEditor.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
autonomous: true
requirements: [KBR-02, KBR-03]
must_haves:
  truths:
    - "An admin can pick a routing station (Kitchen/Bar/None) when creating OR editing a category"
    - "The category tree shows each category's routing as a badge, or 'Not routed' for NONE"
    - "The home dashboard shows a 'Bar Display' tile gated to view_kds_bar for bartender+"
  artifacts:
    - path: "src/features/manage-categories/ui/CategoryTreeEditor.tsx"
      provides: "routing Select in CategoryForm + RoutingBadge/Not-routed in NodeRow + routing persisted on create AND update"
      contains: "Routing station"
    - path: "src/widgets/HomeDashboard/ui/HomeDashboard.tsx"
      provides: "/kds-bar tile (requiredAction view_kds_bar)"
      contains: "kds-bar"
  key_links:
    - from: "src/features/manage-categories/ui/CategoryTreeEditor.tsx"
      to: "useMutationCreateCategory / useMutationUpdateCategory"
      via: "routing: data.routing in create + update payloads"
      pattern: "routing: data.routing"
---

<objective>
Make routing editable and visible in the category admin UI (D-08) and add the `/kds-bar` navigation tile. The `CategoryTreeEditor` gains a "Routing station" selector (a field that does not exist today — `isFood` was hardcoded `false` on create with no edit control), a routing badge in each tree row, and — closing a real pre-existing gap — persistence of routing on both create AND update. The `HomeDashboard` gains a bartender-gated Bar Display tile.

Purpose: Admins need to route categories to KITCHEN/BAR/NONE (D-08); staff need a home-screen entry point to the new bar board (UI-SPEC section F). Depends on RoutingBadge (16-03) and the category-queries routing persistence (16-04).
Output: A working routing selector + tree badges in category admin, and a `/kds-bar` tile using the standard `requiredAction` pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md
@.planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 16-06 slice)

- `CategoryFormData.routing` — `routing: CategoryRouting` form field (default `'NONE'`)
- "Routing station" `Select` field in `CategoryForm` (Kitchen/Bar/None)
- RoutingBadge / "Not routed" text in `NodeRow`
- `handleCreate` writes `routing: data.routing`; `handleUpdate` now includes `routing` in its `CategoryUpdate` payload
- `/kds-bar` `DashboardItem` tile (`Beer` icon, `requiredAction: 'view_kds_bar'`, `managerLabel: 'Bartender'`) in `HomeDashboard` `ITEMS`

<tasks>

<task type="auto">
  <name>Task 1: Add routing selector + tree badge + create/update persistence to CategoryTreeEditor</name>
  <files>src/features/manage-categories/ui/CategoryTreeEditor.tsx</files>
  <read_first>
    - src/features/manage-categories/ui/CategoryTreeEditor.tsx (full file — CategoryFormData 35-38, CategoryForm 47-96, NodeRow name span at 160 + depth badge 163, handleCreate isFood:false at 311, handleUpdate payload at 325, Dialog initial props at 408-415)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (sections G, H — exact Select JSX, helper text copy, default 'NONE', NodeRow badge/Not-routed placement, handleUpdate gap note)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (section "CategoryTreeEditor.tsx — routing selector + NodeRow badge")
    - src/shared/ui/select.tsx (Select/SelectTrigger/SelectValue/SelectContent/SelectItem exports)
    - src/shared/ui/RoutingBadge.tsx (produced by 16-03)
  </read_first>
  <action>
    Per D-08 and UI-SPEC sections G/H:
    (1) Add `routing: CategoryRouting` to the `CategoryFormData` interface; import `CategoryRouting` from `@shared/lib/domain` and `RoutingBadge` from `@shared/ui/RoutingBadge`, plus `Select, SelectTrigger, SelectValue, SelectContent, SelectItem` from `@shared/ui/select` and `Beer, UtensilsCrossed` from `lucide-react`.
    (2) In `CategoryForm`, add a `const [routing, setRouting] = useState<CategoryRouting>(initial.routing);` state and include `routing` in the `onSubmit({ name: trimmed, color, routing })` payload. Insert the new "Routing station" `FormField` (exact JSX from UI-SPEC section G — a `Select value={routing} onValueChange={setRouting}` with Kitchen/Bar/None `SelectItem`s carrying the UtensilsCrossed/Beer icons, plus the muted helper text) directly after the "Color" FormField and before the Cancel/Save button row.
    (3) In `NodeRow`, insert immediately after the category-name span and before the `L{depth+1}` depth badge: when `category.routing === 'NONE'` render `<span className="text-xs text-muted-foreground">Not routed</span>`, else render `<RoutingBadge routing={category.routing} />` (exact JSX from UI-SPEC section H — preserve the existing `gap-2`/`py-1.5` row rhythm, no second line).
    (4) In `handleCreate`, replace `isFood: false,` in the `CategoryCreate` payload with `routing: data.routing,`.
    (5) In `handleUpdate`, add `routing: data.routing` to the `CategoryUpdate` payload object (currently only name+color) — this closes the pre-existing gap where routing/isFood was never editable post-creation.
    (6) Update the two `CategoryForm` `initial={...}` props in the Dialog: the edit branch passes `{ name, color, routing: dialog.category.routing }`; the create branch passes `{ name: '', color: '#6366f1', routing: 'NONE' }` (default per UI-SPEC section G).
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "Routing station" src/features/manage-categories/ui/CategoryTreeEditor.tsx && grep -q "routing: data.routing" src/features/manage-categories/ui/CategoryTreeEditor.tsx && grep -q "RoutingBadge routing={category.routing}" src/features/manage-categories/ui/CategoryTreeEditor.tsx && grep -q "Not routed" src/features/manage-categories/ui/CategoryTreeEditor.tsx && ! grep -q "isFood" src/features/manage-categories/ui/CategoryTreeEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryFormData` has a `routing: CategoryRouting` field; `CategoryForm` renders a "Routing station" Select with Kitchen/Bar/None options and the helper text
    - `NodeRow` shows a `RoutingBadge` for KITCHEN/BAR and "Not routed" muted text for NONE, between the name and the depth badge
    - `handleCreate` sends `routing: data.routing`; `handleUpdate`'s `CategoryUpdate` payload includes `routing: data.routing`
    - Edit dialog seeds the form with the category's current routing; create dialog defaults to `'NONE'`
    - No `isFood` token remains in the file
  </acceptance_criteria>
  <done>Routing is selectable on create and edit, persisted through both mutations, and shown per-row in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add the /kds-bar Bar Display tile to HomeDashboard</name>
  <files>src/widgets/HomeDashboard/ui/HomeDashboard.tsx</files>
  <read_first>
    - src/widgets/HomeDashboard/ui/HomeDashboard.tsx (full file — icon imports 1-17, DashboardItem type 29-37, ITEMS array 39-100, requiredAction tiles e.g. Reports 45-51, legacy /kds visibleToRoles tile 94-99, gating render 140-185)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (section F — exact tile object, Beer icon, requiredAction view_kds_bar, managerLabel 'Bartender', and the managerLabel enum-verification note)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (section "HomeDashboard.tsx — new /kds-bar tile" — follow the requiredAction pattern, NOT the legacy visibleToRoles /kds tile)
  </read_first>
  <action>
    Per UI-SPEC section F: add `Beer` to the `lucide-react` icon import block. Add a new `DashboardItem` object to the `ITEMS` array (place it near the `/kds` tile) using the `requiredAction` pattern (NOT the legacy `visibleToRoles` pattern the `/kds` tile uses): `{ path: '/kds-bar', label: 'Bar Display', icon: Beer, requiredAction: 'view_kds_bar', managerLabel: 'Bartender' }`. The existing generic gating render (`can(item.requiredAction)` → navigate or ManagerPinGate; `managerLabel` badge) already handles this object with no further code. Verify while implementing that the `managerLabel` badge render (lines ~178-182) does not hardcode a 'Manager'/'Admin' enum — it renders `{item.managerLabel}` as free text, so 'Bartender' is safe; if any hardcoded enum is found, extend it rather than reusing 'Manager'.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "path: '/kds-bar'" src/widgets/HomeDashboard/ui/HomeDashboard.tsx && grep -q "requiredAction: 'view_kds_bar'" src/widgets/HomeDashboard/ui/HomeDashboard.tsx && grep -q "Beer" src/widgets/HomeDashboard/ui/HomeDashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ITEMS` contains a `/kds-bar` tile with `label: 'Bar Display'`, `icon: Beer`, `requiredAction: 'view_kds_bar'`, `managerLabel: 'Bartender'`
    - `Beer` is imported from lucide-react
    - The tile uses the requiredAction pattern (gated + PIN-gate on click for non-holders), not visibleToRoles
  </acceptance_criteria>
  <done>The home dashboard exposes a Bar Display tile gated to view_kds_bar holders (bartender+).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form → category routing | routing selection must persist on both create and update; a dropped update write silently mis-routes a category |
| home tile → gated navigation | the /kds-bar tile must be gated by view_kds_bar so non-holders hit the PIN gate rather than free-navigating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | routing not persisted on category edit | mitigate | `handleUpdate` now includes `routing` in the `CategoryUpdate` payload (closes the pre-existing isFood-never-persisted gap); create path already writes it. |
| T-16-14 | Elevation of privilege | /kds-bar tile free-navigation | mitigate | Tile uses `requiredAction: 'view_kds_bar'`; the generic dashboard gate routes non-holders to `ManagerPinGate` instead of navigating, and `KdsBarRoute` (16-05) is the authoritative guard on the route itself (defense in depth). |
| T-16-15 | Information disclosure | routing config edit UX | accept | Switching a category KITCHEN↔BAR↔NONE silently moves already-pending items between boards on next query refresh (UI-SPEC flags this as a known edge case; no confirmation UI was requested in CONTEXT.md — accepted, config edit parity with name/color). |
| T-16-SC | Tampering | package installs | accept | No package installs — Select/Badge/RoutingBadge and Beer icon already available. |
</threat_model>

<verification>
- Category create and edit both persist routing; tree rows show badge/Not-routed
- HomeDashboard shows a view_kds_bar-gated Bar Display tile
- No `isFood` remains in CategoryTreeEditor
- NOTE: full-project typecheck/lint gate runs in 16-07.
</verification>

<success_criteria>
KBR-02 (supporting): the Bar Display entry point exists on the home dashboard, gated to bartender+.
KBR-03: RoutingBadge is displayed on category admin UI; routing is configurable per category.
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-06-SUMMARY.md` when done.
</output>
