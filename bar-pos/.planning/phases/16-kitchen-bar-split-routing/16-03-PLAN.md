---
phase: 16-kitchen-bar-split-routing
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/shared/lib/rbac.ts
  - src/shared/lib/rbac.test.ts
  - src/shared/ui/RoutingBadge.tsx
  - src/shared/ui/RoutingBadge.stories.tsx
  - src/shared/ui/index.ts
autonomous: true
requirements: [KBR-02, KBR-03]
must_haves:
  truths:
    - "view_kds_bar is a valid StaffAction granted to bartender, manager, and admin but NOT kitchen"
    - "RoutingBadge renders an icon+label for KITCHEN and BAR and renders nothing for NONE"
  artifacts:
    - path: "src/shared/lib/rbac.ts"
      provides: "view_kds_bar action in STAFF_ACTIONS + BARTENDER_ACTIONS"
      contains: "view_kds_bar"
    - path: "src/shared/ui/RoutingBadge.tsx"
      provides: "RoutingBadge component"
      exports: ["RoutingBadge"]
    - path: "src/shared/ui/RoutingBadge.stories.tsx"
      provides: "Storybook coverage for RoutingBadge"
      contains: "RoutingBadge"
  key_links:
    - from: "src/shared/lib/rbac.ts"
      to: "BARTENDER_ACTIONS → MANAGER_ACTIONS → ADMIN_ACTIONS"
      via: "additive Set spread"
      pattern: "view_kds_bar"
---

<objective>
Add the `view_kds_bar` RBAC action and the `RoutingBadge` UI primitive. These are the two independent foundations consumed by the bar-KDS route/page (16-06) and by the KDS cards + category editor (16-05, 16-06). Neither touches the live DB, so this plan runs in parallel with the schema push.

Purpose: D-04 gates `/kds-bar` to bartender+ without disturbing the existing `view_kds` (kitchen+admin) food board; D-08 provides a reusable badge so staff can visually confirm an item's routing station.
Output: `view_kds_bar` in the RBAC action sets (bartender/manager/admin, not kitchen), a mirror update to the rbac test matrix, and `RoutingBadge` + its Storybook story exported from shared/ui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md
@.planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 16-03 slice)

- `view_kds_bar` — new `StaffAction` literal in `STAFF_ACTIONS` and member of `BARTENDER_ACTIONS`
- `RoutingBadge` — `src/shared/ui/RoutingBadge.tsx` component (`{ routing: CategoryRouting }` prop)
- `RoutingBadge` Storybook story — `src/shared/ui/RoutingBadge.stories.tsx`
- `RoutingBadge` barrel export in `src/shared/ui/index.ts`

<tasks>

<task type="auto">
  <name>Task 1: Add view_kds_bar action to rbac.ts + rbac.test.ts matrix</name>
  <files>src/shared/lib/rbac.ts, src/shared/lib/rbac.test.ts</files>
  <read_first>
    - src/shared/lib/rbac.ts (full file — STAFF_ACTIONS at 13-37, BARTENDER_ACTIONS 41-51, MANAGER_EXTRA 53-64, KITCHEN_ACTIONS 66-71, ADMIN_EXTRA 73-79, composition 81-83, rbacDenialMessage 105-110)
    - src/shared/lib/rbac.test.ts (full file — the ALLOWED mirror matrix at 12-46 must be kept in sync)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (section "src/shared/lib/rbac.ts — add view_kds_bar" — exact placement + the "do NOT add to KITCHEN_ACTIONS" rule)
  </read_first>
  <action>
    Per D-04: add `'view_kds_bar',` to the `STAFF_ACTIONS` union (immediately after `'view_kds',` at line ~32) and add `'view_kds_bar',` to the `BARTENDER_ACTIONS` Set. Because `MANAGER_ACTIONS` spreads `BARTENDER_ACTIONS` and `ADMIN_ACTIONS` spreads `MANAGER_ACTIONS`, bartender/manager/admin all inherit it automatically — do NOT add it to `KITCHEN_ACTIONS` (the `kitchen` role must not gain access to the bar board) and do NOT add it to `MANAGER_EXTRA`/`ADMIN_EXTRA`. Leaving it out of `ADMIN_EXTRA` means `rbacDenialMessage('view_kds_bar')` correctly falls through to "Manager access required" (no change needed there).
    Then update the test mirror in rbac.test.ts: add `'view_kds_bar'` to the `bartender` and `manager` sets in the `ALLOWED` matrix (admin already uses `new Set(STAFF_ACTIONS)` so it covers automatically; kitchen's explicit set must NOT include it, which asserts D-04's exclusion).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `canAccess('bartender','view_kds_bar')`, `canAccess('manager','view_kds_bar')`, `canAccess('admin','view_kds_bar')` all return true (asserted by the rbac test matrix)
    - `canAccess('kitchen','view_kds_bar')` returns false (asserted by the kitchen set NOT containing it)
    - `isStaffAction('view_kds_bar')` is true (member of STAFF_ACTIONS)
    - `npx vitest run src/shared/lib/rbac.test.ts` passes
  </acceptance_criteria>
  <done>`view_kds_bar` is a StaffAction granted to bartender/manager/admin only; the rbac test matrix passes and asserts the kitchen-role exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Create RoutingBadge component + Storybook story + barrel export</name>
  <files>src/shared/ui/RoutingBadge.tsx, src/shared/ui/RoutingBadge.stories.tsx, src/shared/ui/index.ts</files>
  <read_first>
    - src/shared/ui/ChefHatBadge.tsx (the analog — icon + Badge variant="secondary" gap-1 shape)
    - src/shared/ui/ChefHatBadge.stories.tsx (the story analog — Default + InlineWithText)
    - src/shared/ui/index.ts (barrel — ChefHatBadge exported at line 10; add RoutingBadge in the Display group)
    - .planning/phases/16-kitchen-bar-split-routing/16-UI-SPEC.md (section A — the exact, locked RoutingBadge implementation; Color section — neutral variant only, icon+text differentiation, no accent color)
  </read_first>
  <action>
    Create `src/shared/ui/RoutingBadge.tsx` exactly as specified in UI-SPEC section A (this is a locked design contract): a `RoutingBadge({ routing }: { routing: CategoryRouting })` component that returns `null` when `routing === 'NONE'`, uses `UtensilsCrossed` (lucide) + label "Kitchen" for KITCHEN and `Beer` (lucide) + label "Bar" for BAR, wrapped in `<Badge variant="secondary" className="flex items-center gap-1">` with the icon at `className="h-3 w-3" aria-hidden`. Import `CategoryRouting` type from `@shared/lib/domain` and `Badge` from `./badge` (matching ChefHatBadge's relative import). Do NOT color-code KITCHEN vs BAR — neutral `secondary` variant for both, per the UI-SPEC Color constraint (differentiate by icon+text only).
    Create `src/shared/ui/RoutingBadge.stories.tsx` following ChefHatBadge.stories.tsx conventions (import from `@storybook/react-vite` per the storybook/no-renderer-packages ESLint rule): a `Kitchen` story (`args: { routing: 'KITCHEN' }`), a `Bar` story (`args: { routing: 'BAR' }`), and a `None` story (`args: { routing: 'NONE' }`, demonstrating the null render).
    Add `export { RoutingBadge } from './RoutingBadge';` to the Display section of `src/shared/ui/index.ts` (next to the ChefHatBadge export), so consumers may import via the barrel; note that KdsBoard/CategoryTreeEditor in later plans import the direct path `@shared/ui/RoutingBadge` per PATTERNS — both resolve to the same component.
  </action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RoutingBadge" ; test $? -eq 1 && echo "no RoutingBadge type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/ui/RoutingBadge.tsx` exports `RoutingBadge`, returns `null` for `routing === 'NONE'`, and renders `UtensilsCrossed`/"Kitchen" and `Beer`/"Bar" for the other two values
    - No color/accent class is applied to differentiate KITCHEN vs BAR (only the icon + label differ)
    - `src/shared/ui/RoutingBadge.stories.tsx` exists with Kitchen, Bar, and None stories importing from `@storybook/react-vite`
    - `src/shared/ui/index.ts` exports `RoutingBadge`
    - No RoutingBadge-related TypeScript errors
  </acceptance_criteria>
  <done>RoutingBadge is implemented per the locked UI-SPEC, story-covered, and exported from the shared/ui barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user role → RBAC action set | `view_kds_bar` membership decides who can reach the bar board; a mis-placement could grant kitchen staff bar access or deny bartenders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Elevation of privilege | `view_kds_bar` role membership | mitigate | Added only to `BARTENDER_ACTIONS` (inherited by manager/admin via Set spread) and explicitly NOT to `KITCHEN_ACTIONS`; the rbac test matrix asserts `canAccess('kitchen','view_kds_bar') === false` (D-04). |
| T-16-07 | Spoofing | shared permission with food board | mitigate | `view_kds`(kitchen board) left untouched — bartenders do not gain food-board access, kitchen does not gain bar-board access; the two actions are independent. |
| T-16-SC | Tampering | package installs | accept | No new packages — lucide-react icons (Beer, UtensilsCrossed) and shadcn Badge already installed. |
</threat_model>

<verification>
- `npx vitest run src/shared/lib/rbac.test.ts` passes (asserts D-04 grant/deny matrix)
- RoutingBadge renders per UI-SPEC section A (null for NONE, icon+text for KITCHEN/BAR, neutral variant)
- RoutingBadge exported from shared/ui barrel and story-covered
</verification>

<success_criteria>
KBR-02 (partial): the `view_kds_bar` permission that gates `/kds-bar` to bartender+ exists and excludes kitchen.
KBR-03 (partial): the `RoutingBadge` widget exists and is ready for placement on KDS cards (16-05) and the category editor (16-06).
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-03-SUMMARY.md` when done.
</output>
