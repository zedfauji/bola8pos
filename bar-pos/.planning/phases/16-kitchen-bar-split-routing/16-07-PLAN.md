---
phase: 16-kitchen-bar-split-routing
plan: 07
type: execute
wave: 4
depends_on: [16-02, 16-05, 16-06]
files_modified:
  - e2e/helpers/supabase.ts
  - e2e/40-kds-bar.spec.ts
  - CLAUDE.md
autonomous: false
requirements: [KBR-01, KBR-02, KBR-03, KBR-04]
must_haves:
  truths:
    - "The full unit suite, typecheck, and lint pass after the routing sweep"
    - "bartender/manager/admin can reach /kds-bar; kitchen is redirected"
    - "A BAR-routed order item renders as a card on /kds-bar"
    - "The existing kitchen KDS (28-kds) still passes — kitchen flow unaffected"
  artifacts:
    - path: "e2e/40-kds-bar.spec.ts"
      provides: "bar-KDS role access + rendering + dashboard tile E2E"
      contains: "/kds-bar"
    - path: "e2e/helpers/supabase.ts"
      provides: "routing-parameterized KDS order seed helper"
      contains: "routing"
  key_links:
    - from: "e2e/40-kds-bar.spec.ts"
      to: "/kds-bar route + view_kds_bar"
      via: "loginAs(role) + page.goto('/kds-bar')"
      pattern: "kds-bar"
---

<objective>
Prove the phase goal end to end and gate on regressions. Migrate the last `is_food` call site (the E2E seed helper), add a `/kds-bar` E2E spec covering role access, bar-card rendering, and the dashboard tile, and run the full typecheck/lint/unit regression gate. This is the plan that turns "code compiles" into "the feature works and the kitchen board is unharmed."

Purpose: Complete the D-02 sweep (the e2e seed helper still queries `is_food`), verify KBR-02/03 behaviorally, and assert KBR-04 (kitchen KDS unaffected) via the existing 28-kds suite.
Output: A green regression gate plus a passing `40-kds-bar.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces (Plan 16-07 slice)

- `seedKdsFoodOrder(tabId, routing?)` — routing-parameterized seed helper in `e2e/helpers/supabase.ts` (queries `categories.routing` instead of `is_food`)
- `e2e/40-kds-bar.spec.ts` — new Playwright spec (role access, bar card rendering, dashboard tile)
- CLAUDE.md updates: `/kds-bar` route row + `40-kds-bar` in the E2E spec list

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the KDS seed helper by routing + write 40-kds-bar.spec.ts</name>
  <files>e2e/helpers/supabase.ts, e2e/40-kds-bar.spec.ts</files>
  <read_first>
    - e2e/helpers/supabase.ts (lines 574-632 — seedKdsFoodOrder; the `.eq('is_food', true)` category lookup at line 589 is the last isFood call site)
    - e2e/28-kds.spec.ts (full file — the pattern to mirror: role access T1-T4, seeded card T5, dashboard tile T8-T10; uses loginAs/logout/openCaja/resetTestState/seedKdsFoodOrder + local seedOpenTabWithFoodOrder)
    - e2e/helpers/auth.ts (loginAs supports 'bartender' | 'manager' | 'admin' | 'kitchen' via E2E_* env)
    - e2e/32-combos.spec.ts (the test.info().annotations.push conditional-skip pattern to use when no BAR seed data exists)
    - .planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md (D-04 — bartender/manager/admin CAN access /kds-bar, kitchen CANNOT)
  </read_first>
  <action>
    Per D-02, migrate the seed helper: change `seedKdsFoodOrder(tabId)` to accept an optional `routing: 'KITCHEN' | 'BAR' = 'KITCHEN'` parameter and change its category lookup from `.eq('is_food', true)` to `.eq('routing', routing)`. Update the helper's doc comment and its error strings to reference routing. The default `'KITCHEN'` keeps the existing 28-kds callers working unchanged.
    Create `e2e/40-kds-bar.spec.ts` mirroring 28-kds.spec.ts structure, with a `test.beforeEach` that runs `requireIntegrationEnv()`, `resetTestState()`, `openCaja(500)`, `page.goto('/')`:
    - T1: bartender can access /kds-bar — `loginAs(page,'bartender')`, goto `/kds-bar`, expect heading "Bar Display" visible.
    - T2: manager can access /kds-bar (heading visible).
    - T3: admin can access /kds-bar (heading visible).
    - T4: kitchen is redirected away from /kds-bar — expect URL `/home` (asserts D-04 exclusion).
    - T5: a seeded BAR order renders — seed an open tab with a BAR order (clone 28-kds's local `seedOpenTabWithFoodOrder`, but call `seedKdsFoodOrder(tabId, 'BAR')`; if no BAR-routed category/product exists in the seed DB, annotate via `test.info().annotations.push` and skip the assertion rather than hard-failing — follow the 32-combos conditional pattern), then `loginAs(page,'admin')`, goto `/kds-bar`, expect `getByTestId('kds-card').first()` visible.
    - T6: bartender sees the "Bar Display" dashboard tile — goto `/home`, expect a button named /Bar Display/i visible.
    - T7: kitchen does NOT see the "Bar Display" tile — goto `/home`, expect count 0 for /Bar Display/i (asserts D-04 at the tile level).
    Placing the spec in `e2e/` is sufficient for Playwright auto-discovery — no config change needed.
  </action>
  <verify>
    <automated>cd bar-pos && test -f e2e/40-kds-bar.spec.ts && grep -q "routing" e2e/helpers/supabase.ts && ! grep -q "is_food" e2e/helpers/supabase.ts && grep -q "/kds-bar" e2e/40-kds-bar.spec.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "40-kds-bar|helpers/supabase" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `seedKdsFoodOrder` accepts a routing arg (default 'KITCHEN') and queries `categories.routing`; no `is_food` remains in e2e/helpers/supabase.ts
    - `e2e/40-kds-bar.spec.ts` exists with role-access tests (bartender/manager/admin allowed, kitchen redirected), a bar-card rendering test, and dashboard-tile visibility tests
    - The spec typechecks (no TS errors in the new spec or the edited helper)
  </acceptance_criteria>
  <done>The last isFood call site is migrated and a /kds-bar E2E spec exists covering access, rendering, and the tile per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression gate (typecheck + lint + unit) and CLAUDE.md docs</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Routes table — add /kds-bar row; "E2E Test Suite" spec list — add 40-kds-bar; "Implemented Features" — optionally note bar-routing)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (full — cross-check every isFood call site listed is addressed)
  </read_first>
  <action>
    Run the full regression gate from `bar-pos/`: `npm run typecheck` (must exit 0 — this is the first point where the entire routing sweep is validated together), `npm run lint` (max-warnings 0), and `npm run test` (unit suite; the one pre-existing `useCloseTab.test.ts:95` failure documented since Phase 15 is the only acceptable failure — everything else must pass). Fix any typecheck/lint errors introduced by the sweep (import ordering, exactOptionalPropertyTypes, unused isFood-era imports). Then confirm via grep that NO `isFood`/`is_food` token remains anywhere in `src/` or `e2e/` except inside the migration SQL DOWN comment and the migration's backfill CASE.
    Update CLAUDE.md: add a `/kds-bar | KdsBarPage | Bar KDS board (bartender+)` row to the Routes table; add `40-kds-bar` to the E2E spec list (and bump the spec count).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint && npm run test && ! grep -rn "isFood" src/ e2e/ && ! grep -rn "is_food" src/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run test` passes (only the documented pre-existing useCloseTab failure permitted)
    - No `isFood` token anywhere in `src/` or `e2e/`; no `is_food` in `src/` (only in migration SQL)
    - CLAUDE.md lists the /kds-bar route and the 40-kds-bar spec
  </acceptance_criteria>
  <done>The whole routing sweep passes typecheck/lint/unit with zero lingering isFood references, and docs reflect the new route + spec.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify live /kds-bar E2E run + kitchen-board regression</name>
  <files>e2e/40-kds-bar.spec.ts, e2e/28-kds.spec.ts</files>
  <read_first>
    - e2e/40-kds-bar.spec.ts (the spec to run)
    - e2e/28-kds.spec.ts (the kitchen regression to run)
    - CLAUDE.md (E2E section — specs require `.env.local` E2E credentials + dev server)
  </read_first>
  <action>
    Run the two Playwright specs against a live dev server with `.env.local` E2E credentials and confirm the results, since these specs cannot run in the offline gate (they need a running app + seeded DB). Execute `npx playwright test e2e/40-kds-bar.spec.ts` and `npx playwright test e2e/28-kds.spec.ts`, then report pass/fail. This checkpoint is blocking and human-gated because it depends on the live-DB push (16-02) and local dev environment; do not auto-advance.
  </action>
  <what-built>
    A new `/kds-bar` Bar Display board (bartender+), RoutingBadge on KDS + category UI, a routing selector in category admin, and a routing-parameterized KDS query. Tasks 1-2 authored `40-kds-bar.spec.ts` and passed the offline gate (typecheck/lint/unit). The live E2E run needs a dev server + E2E credentials, which this checkpoint covers.
  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`) with `.env.local` E2E credentials present (E2E_BARTENDER_*, E2E_MANAGER_*, E2E_KITCHEN_*, E2E_ADMIN_*).
    2. Run the new spec: `npx playwright test e2e/40-kds-bar.spec.ts`. Confirm bartender/manager/admin reach the "Bar Display" heading, kitchen is redirected to /home, the seeded BAR card renders (or is annotated-skipped if no BAR seed data), and the dashboard tile is visible to bartender but absent for kitchen.
    3. Run the kitchen regression: `npx playwright test e2e/28-kds.spec.ts`. Confirm it still passes — kitchen role reaches /kds, bartender/manager are still redirected from /kds, food cards render and bump (KBR-04: existing kitchen flow unaffected).
    4. Optionally sanity-check the category admin routing selector in the UI: open Settings → Products/Categories, edit a category, pick a routing station, save, reopen — the selection persists and the tree shows the badge.
  </how-to-verify>
  <verify>
    <human-check>Both 40-kds-bar and 28-kds Playwright specs pass against the live dev server; kitchen board behavior is unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - `40-kds-bar.spec.ts` passes: bartender/manager/admin admitted, kitchen redirected, bar card renders (or annotated-skip), tile visibility correct per D-04
    - `28-kds.spec.ts` passes unchanged (KBR-04 regression)
  </acceptance_criteria>
  <resume-signal>Type "approved" once both specs pass and the kitchen board is confirmed unaffected, or describe the failures observed.</resume-signal>
  <done>Live E2E confirms /kds-bar access control + rendering and that the kitchen KDS is unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E role fixtures → /kds-bar | the spec is the executable proof that access control (D-04) holds for all four roles |
| routing sweep → existing kitchen flow | the 28-kds regression is the proof that KBR-04 (kitchen unaffected) holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Elevation of privilege | /kds-bar access control unverified | mitigate | `40-kds-bar.spec.ts` asserts kitchen is redirected and bartender/manager/admin are admitted; the dashboard-tile tests assert kitchen cannot see the entry point (D-04). |
| T-16-17 | Tampering | silent kitchen-board regression | mitigate | `28-kds.spec.ts` is re-run unchanged as the KBR-04 regression gate; the routing-param KITCHEN path must reproduce the prior food board exactly. |
| T-16-18 | Denial | false-positive gate (types pass, DB wrong) | mitigate | This plan depends on 16-02 (schema push), so the live DB has the routing column before the E2E and seed helper query it. |
| T-16-SC | Tampering | package installs | accept | No package installs — spec + helper edits only. |
</threat_model>

<verification>
- `npm run typecheck` / `npm run lint` / `npm run test` all green (pre-existing useCloseTab failure excepted)
- `40-kds-bar.spec.ts` passes: role access (D-04), bar card rendering, dashboard tile visibility
- `28-kds.spec.ts` passes unchanged (KBR-04)
- No `isFood` anywhere in src/ or e2e/; no `is_food` in src/
</verification>

<success_criteria>
KBR-01: routing column live and queried by the E2E seed helper.
KBR-02: /kds-bar renders bar-routed items and is gated to bartender+ (E2E-proven).
KBR-03: RoutingBadge shown on cards + category UI (verified in-app).
KBR-04: existing kitchen KDS flow unaffected (28-kds regression green).
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-07-SUMMARY.md` when done.
</output>
