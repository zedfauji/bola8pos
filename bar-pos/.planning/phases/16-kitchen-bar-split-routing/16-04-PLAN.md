---
phase: 16-kitchen-bar-split-routing
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/entities/category/model/queries.ts
  - src/features/add-item-to-tab/ui/ModifierSheet.tsx
  - src/features/print-precheque/usePrintPreCheque.ts
autonomous: true
requirements: [KBR-01, KBR-04]
must_haves:
  truths:
    - "Category read/create/update round-trips the routing enum to and from the database"
    - "A product with no category resolves to a synthetic category routed 'NONE'"
    - "When KDS is enabled, only NONE-routed items print on the pre-cheque; KITCHEN and BAR items are excluded"
  artifacts:
    - path: "src/entities/category/model/queries.ts"
      provides: "routing in mapCategoryRow + create insert + update partial"
      contains: "routing"
    - path: "src/features/print-precheque/usePrintPreCheque.ts"
      provides: "routing-based pre-cheque filter"
      contains: "routing"
  key_links:
    - from: "src/entities/category/model/queries.ts"
      to: "categories.routing column"
      via: "insert row.routing / update row.routing / mapCategoryRow"
      pattern: "routing"
---

<objective>
Migrate the category persistence layer and two consumer call sites off `isFood` onto `routing` (D-02). This covers the data mapper/mutations that the category admin editor (16-06) writes through, the ModifierSheet pricing fallback, and the pre-cheque printer filter — the call sites that depend only on the domain type contract, not on RoutingBadge or the KDS query.

Purpose: Complete the D-02 call-site sweep for the non-KDS surfaces so the routing field persists correctly and the printer/pricing paths keep working after the column swap.
Output: category queries round-trip `routing`; ModifierSheet's synthetic-category fallback is `NONE`; the pre-cheque excludes both KITCHEN and BAR items when KDS is enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-kitchen-bar-split-routing/16-CONTEXT.md
@.planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 16-04 slice)

- `mapCategoryRow` reads `row.routing` (was `is_food`) → `Category.routing`
- `useMutationCreateCategory` insert row writes `routing` (was `is_food`)
- `useMutationUpdateCategory` partial update writes `routing` (was `is_food`)
- `categoryForPricing()` synthetic fallback uses `routing: 'NONE'`
- `usePrintPreCheque` filter keyed on `routing === 'NONE'`

<tasks>

<task type="auto">
  <name>Task 1: Migrate entities/category/model/queries.ts to routing</name>
  <files>src/entities/category/model/queries.ts</files>
  <read_first>
    - src/entities/category/model/queries.ts (full file — mapCategoryRow at 28-46, useMutationCreateCategory insert at 147-176, useMutationUpdateCategory partial at 178-210)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (section "src/entities/category/model/queries.ts — row mapper + mutations" — exact 3-site edits + the CategoryRouting import note)
    - src/shared/lib/domain.ts (CategoryRouting type, produced by 16-01)
  </read_first>
  <action>
    Per D-02, replace all three `isFood`/`is_food` sites: in `mapCategoryRow`, change `isFood: (row as { is_food?: boolean }).is_food ?? false,` to `routing: (row as { routing?: CategoryRouting }).routing ?? 'NONE',`. In `useMutationCreateCategory`'s `insertRow`, change `is_food: input.isFood,` to `routing: input.routing,`. In `useMutationUpdateCategory`, change `if (rest.isFood !== undefined) row.is_food = rest.isFood;` to `if (rest.routing !== undefined) row.routing = rest.routing;`. Add `CategoryRouting` to the `import type { ... } from '@shared/lib/domain'` on line 3 (it is used as the cast type in the mapper). The `Tables/TablesInsert/TablesUpdate<'categories'>` types now carry `routing` (from 16-01's supabase.types.ts edit), so no `supabase as any` cast is needed; if a stale-type error surfaces because the executor's environment has not picked up the 16-01 edit, fall back to the documented `const db = supabase as any` + file-level eslint-disable workaround.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "routing" src/entities/category/model/queries.ts && ! grep -q "is_food" src/entities/category/model/queries.ts && ! grep -q "isFood" src/entities/category/model/queries.ts</automated>
  </verify>
  <acceptance_criteria>
    - `mapCategoryRow` maps `row.routing` to `Category.routing` with a `'NONE'` fallback
    - `useMutationCreateCategory` insert row sets `routing: input.routing`
    - `useMutationUpdateCategory` conditionally sets `row.routing = rest.routing`
    - No `is_food` or `isFood` token remains in the file
  </acceptance_criteria>
  <done>Category read/create/update round-trip the routing enum with no lingering isFood references.</done>
</task>

<task type="auto">
  <name>Task 2: Update ModifierSheet fallback + usePrintPreCheque filter to routing</name>
  <files>src/features/add-item-to-tab/ui/ModifierSheet.tsx, src/features/print-precheque/usePrintPreCheque.ts</files>
  <read_first>
    - src/features/add-item-to-tab/ui/ModifierSheet.tsx (lines 17-29 — categoryForPricing synthetic fallback uses isFood: false at line 26; Category type imported from @entities/product/model/types which re-exports domain's Category)
    - src/features/print-precheque/usePrintPreCheque.ts (lines 33-50 — kdsEnabled filter at line 41 uses item.product?.category?.isFood)
    - .planning/phases/16-kitchen-bar-split-routing/16-PATTERNS.md (sections "ModifierSheet.tsx — categoryForPricing fallback" and "usePrintPreCheque.ts — kdsEnabled filter" — the routing==='NONE' inference and its planner-confirmation note)
  </read_first>
  <action>
    In ModifierSheet.tsx `categoryForPricing()`, replace `isFood: false,` with `routing: 'NONE',` in the synthetic fallback category (this branch only fires when `product.category` is missing entirely; `'NONE'` matches the schema default for a placeholder). Per D-02.
    In usePrintPreCheque.ts, replace the filter predicate `.filter(item => !kdsEnabled || item.product?.category?.isFood !== true)` with `.filter(item => !kdsEnabled || item.product?.category?.routing === 'NONE')`. **Confirmed pre-cheque behavior (resolving the PATTERNS inference):** when KDS is enabled, both KITCHEN and BAR items are now KDS-routed and therefore excluded from the printed pre-cheque; only `NONE`-routed items still print. This is the intended semantics for a phase that adds a bar KDS board — bar items go to `/kds-bar`, not the printer. Update the adjacent comment (line ~33) from "food items are handled by the kitchen display" to reflect that KITCHEN- and BAR-routed items are handled by their KDS boards.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "routing: 'NONE'" src/features/add-item-to-tab/ui/ModifierSheet.tsx && grep -q "routing === 'NONE'" src/features/print-precheque/usePrintPreCheque.ts && ! grep -q "isFood" src/features/add-item-to-tab/ui/ModifierSheet.tsx && ! grep -q "isFood" src/features/print-precheque/usePrintPreCheque.ts</automated>
  </verify>
  <acceptance_criteria>
    - ModifierSheet's `categoryForPricing` fallback object sets `routing: 'NONE'` and no longer sets `isFood`
    - usePrintPreCheque's item filter uses `item.product?.category?.routing === 'NONE'`
    - No `isFood` token remains in either file
    - The pre-cheque comment reflects that KITCHEN and BAR items are KDS-routed (excluded from print when KDS enabled)
  </acceptance_criteria>
  <done>Both consumer call sites read routing; the pre-cheque prints only NONE-routed items when KDS is enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| category admin form → categories table | routing values written on create/update must persist exactly; a dropped write reproduces the pre-existing "isFood never persisted on update" gap |
| order items → thermal printer | the KDS-enabled filter decides what physically prints; a wrong predicate could double-serve (print AND KDS) or drop items entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | routing lost on category update | mitigate | Update mutation now writes `row.routing` when provided (closes the pre-existing gap where isFood was never in the update payload). |
| T-16-09 | Information disclosure / Denial | pre-cheque prints items already sent to KDS | mitigate | Filter keyed on `routing === 'NONE'` — KITCHEN and BAR items are deterministically excluded when KDS is enabled; non-KDS mode (`!kdsEnabled`) prints everything, unchanged. |
| T-16-SC | Tampering | package installs | accept | No package installs — in-place edits only. |
</threat_model>

<verification>
- No `isFood`/`is_food` token in the three touched files
- category queries write and read `routing`
- pre-cheque filter uses `routing === 'NONE'`
- NOTE: full-project typecheck completes only after the whole sweep (16-05/16-06) lands — gate at 16-07, not here.
</verification>

<success_criteria>
KBR-01 (supporting): routing persists correctly through the category data layer.
KBR-04 (supporting): the pre-cheque and pricing paths keep functioning after the column swap, with KITCHEN routing preserving prior food behavior.
</success_criteria>

<output>
Create `.planning/phases/16-kitchen-bar-split-routing/16-04-SUMMARY.md` when done.
</output>
