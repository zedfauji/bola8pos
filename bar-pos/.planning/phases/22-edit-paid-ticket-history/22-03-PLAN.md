---
phase: 22-edit-paid-ticket-history
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - src/features/edit-paid-tab/model/useEditPaidTab.ts
  - src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx
  - src/features/edit-paid-tab/index.ts
  - src/widgets/PaymentPane/ui/PaymentPane.tsx
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/shared/lib/i18n/locales/en-US/featOrders.json
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "useEditPaidTab calls supabase.rpc('edit_paid_tab', ...) and maps NO_OPEN_CAJA→CAJA_CLOSED and AUTH_FORBIDDEN→AUTH_FORBIDDEN into typed Result errors, invalidating tab + audit query caches on success"
    - "EditPaidTabDialog gates the mutation behind ManagerPinDialog (requiredAction='edit_paid_tab'), requires a reason, and lets a manager edit item qty/price/notes, add/remove rows, and tab notes before saving"
    - "A manager can open the dialog for a paid tab from the Payments page and save a correction"
  artifacts:
    - src/features/edit-paid-tab/model/useEditPaidTab.ts
    - src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx
    - src/features/edit-paid-tab/index.ts
  key_links:
    - "EditPaidTabDialog → ManagerPinDialog(requiredAction='edit_paid_tab') → useEditPaidTab → edit_paid_tab RPC"
    - "PaymentPane trigger button → EditPaidTabDialog (tabId + expectedVersion from the selected paid tab)"
    - "useEditPaidTab onSuccess → invalidate tabKeys.lists() + auditKeys.all (refreshes /edit-history)"
---

<objective>
Build the `edit-paid-tab` feature slice — a `useEditPaidTab` mutation hook and an `EditPaidTabDialog` Sheet that mirrors `process-refund`'s manager-PIN-gated financial-correction pattern — and wire an "Edit paid ticket" trigger into `PaymentPane` so a manager can correct a paid tab. This delivers SC-3's flow: manager PIN gate → field edit → reason → confirm.

Purpose: The RPC (22-02) is the enforcement boundary; this plan is the operator-facing surface that composes the existing ManagerPinDialog + Sheet + QuantityControl primitives into the correction UX (Pattern 2). Free-text reason (not a fixed enum) per the UI-SPEC.
Output: A 3-file feature slice, PaymentPane wiring, and featOrders i18n keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-edit-paid-ticket-history/22-CONTEXT.md
@.planning/phases/22-edit-paid-ticket-history/22-RESEARCH.md
@.planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md
@.planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md
@src/features/process-refund/model/useProcessRefund.ts
@src/features/process-refund/ui/RefundSheet.tsx
@src/features/manager-pin-gate/ui/ManagerPinDialog.tsx
</context>

## Artifacts this phase produces (whole-phase reference)

- **RPC:** `edit_paid_tab` (22-02) · **New AuditAction:** `tab.edit_paid` (22-01) · **New StaffAction:** `edit_paid_tab` (22-01)
- **New files (this plan):** `src/features/edit-paid-tab/{model/useEditPaidTab.ts, ui/EditPaidTabDialog.tsx, index.ts}`
- **Modified files (this plan):** `src/widgets/PaymentPane/ui/PaymentPane.tsx`, `featOrders.json` (both locales)
- **Other phase files:** migration + integration test (22-02); /edit-history view (22-04); E2E spec (22-05)
- **AppErrorCode:** none new — `NO_OPEN_CAJA`→`CAJA_CLOSED`, `AUTH_FORBIDDEN` already exist

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useEditPaidTab mutation hook + feature barrel</name>
  <files>src/features/edit-paid-tab/model/useEditPaidTab.ts, src/features/edit-paid-tab/index.ts</files>
  <read_first>
    - src/features/process-refund/model/useProcessRefund.ts (full 61-line template — Result<T> wrapping, err()/ok(), i18n.t() message resolution, error.message.includes() branches, query invalidation)
    - src/entities/audit-log/index.ts (barrel exports auditKeys — note auditKeys has `all` + `list(filters)`, NO `lists()`; invalidate `auditKeys.all`)
    - src/entities/tab/model/queries.ts (tabKeys.lists() at line 51 exists — invalidate it)
    - src/shared/lib/result.ts (AppErrorCode union — confirm CAJA_CLOSED and AUTH_FORBIDDEN already exist; no new code needed)
    - .planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md (useEditPaidTab section — exact hook shape + the p_manager_pin placeholder note)
  </read_first>
  <behavior>
    - Given a valid EditPaidTabInput, mutationFn calls supabase.rpc('edit_paid_tab', { p_tab_id, p_expected_version, p_order_item_patches, p_notes, p_reason }) and returns ok(data) on success
    - When the RPC error message includes 'NO_OPEN_CAJA', returns err({ code: 'CAJA_CLOSED', message: i18n.t('featOrders:editPaidTab.noOpenCaja') })
    - When it includes 'AUTH_FORBIDDEN', returns err({ code: 'AUTH_FORBIDDEN', message: i18n.t('featOrders:editPaidTab.authForbidden') })
    - When it includes 'TAB_NOT_EDITABLE', returns a typed err with a localized message
    - On success, invalidates tabKeys.lists() and auditKeys.all
    - STALE_VERSION / NOT_FOUND_VERSIONED are left for the component's onError (handleVersionError), not mapped here
  </behavior>
  <action>
    Create `src/features/edit-paid-tab/model/useEditPaidTab.ts` mirroring `useProcessRefund.ts`. Export `interface EditPaidTabInput { tabId: string; expectedVersion: number; orderItemPatches: EditPaidTabPatch[]; notes: string | undefined; reason: string }` and an `EditPaidTabPatch` type with `{ op: 'update' | 'add' | 'delete'; id?: string; quantity?: number; unitPrice?: number; notes?: string; productId?: string }` (respect `exactOptionalPropertyTypes` — use `| undefined` on mutation-input optionals, not `?`). The `useEditPaidTab` hook returns `useMutation` whose `mutationFn` calls `supabase.rpc('edit_paid_tab', { p_tab_id: input.tabId, p_expected_version: input.expectedVersion, p_order_item_patches: input.orderItemPatches, p_notes: input.notes, p_reason: input.reason })`, maps the error branches per the behavior block, and on success invalidates `tabKeys.lists()` + `auditKeys.all`. Use the `const db = supabase as any` cast only if 22-02 did not regenerate `supabase.types.ts` with the `edit_paid_tab` function type (per CLAUDE.md). Create `src/features/edit-paid-tab/index.ts` exporting `EditPaidTabDialog`, `useEditPaidTab`, and the input/patch types (mirror `process-refund/index.ts`).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint -- src/features/edit-paid-tab/model/useEditPaidTab.ts src/features/edit-paid-tab/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useEditPaidTab` calls `edit_paid_tab` with the five `p_*` params and returns `Result<unknown>`
    - NO_OPEN_CAJA→CAJA_CLOSED and AUTH_FORBIDDEN→AUTH_FORBIDDEN branches present; onSuccess invalidates tab + audit caches
    - typecheck + lint pass; no new AppErrorCode added
  </acceptance_criteria>
  <done>The mutation hook and barrel exist and the RPC contract is typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditPaidTabDialog Sheet + featOrders i18n keys</name>
  <files>src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx, src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>
    - src/features/process-refund/ui/RefundSheet.tsx (full — Sheet shell, SELECTING→CONFIGURING→PIN_MODAL→SUBMITTING state machine, overrides Map pattern lines 129-168, ManagerPinDialog wiring lines ~186-194, SheetFooter POSButton with touchSize/focusEmphasis)
    - src/features/manager-pin-gate/ui/ManagerPinDialog.tsx (requiredAction prop + onSuccess contract; eligibleStaff filtered via canAccess)
    - src/entities/tab/model/queries.ts (useTab(id) at line 356 — single-tab detail with `version` + `notes` + order lines; use it as the source of tabId/version/notes and current order_items for the editable list)
    - src/shared/ui (POSButton, QuantityControl, MoneyDisplay barrel exports) and src/shared/ui/{sheet,select,checkbox,label} paths
    - .planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md (Copywriting Contract → EditPaidTabDialog section: every en-US string; touchSize=xl/large; free-text reason Textarea not a Select; "Edited" badge; add/remove item affordances; Remove-item ConfirmDialog copy)
  </read_first>
  <behavior>
    - Rendering with a paid tab loads its current order_items and notes; each row shows quantity/unit_price/notes editable controls
    - Editing a row's qty/price/notes marks that row with an "Edited" badge and recomputes the New total row live
    - The Save CTA is disabled until a reason is entered; clicking it opens ManagerPinDialog(requiredAction='edit_paid_tab')
    - On PIN success, useEditPaidTab is called with the diff as orderItemPatches (op: update/add/delete), notes, reason, and expectedVersion=tab.version
    - A NO_OPEN_CAJA / CAJA_CLOSED error surfaces the localized "no open caja" toast; success shows the success toast and closes the sheet
  </behavior>
  <action>
    Create `src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx` cloning `RefundSheet.tsx`'s structure. Props: `interface EditPaidTabDialogProps { open: boolean; tabId: string | null; onOpenChange: (open: boolean) => void }`. Load the tab via `useTab(tabId)` for `version`, `notes`, and order lines (fall back to loading order_items by tab if `useTab` does not nest them). Keep local state: an `overrides: Map<string, ItemOverride>` (quantity/unitPrice/notes per row, copy-and-set like RefundSheet), an `addedRows` list and a `removedIds` set (D-01 add/remove), a `tabNotes` string, a free-text `reason` string (Textarea/Input, NOT a fixed Select — this is an open correction), and `pinOpen`. Render the `Sheet`/`SheetContent side="right"`/`SheetHeader`/item-list/`SheetFooter` layout: per row show `QuantityControl` for qty, a numeric input for `unitPrice`, a notes input, an "Edited" `Badge variant="outline"` when the row differs from original, and a per-row "Remove" destructive affordance gated by a `ConfirmDialog` (use the existing `confirmClassName` destructive pattern; copy per UI-SPEC "Remove item?"). Add an "Add item" button per D-01. Show a tab-level "Ticket notes" field and a "New total" row (`flex items-center justify-between`, `text-base font-semibold` label + `MoneyDisplay size="lg"`) that recomputes live; when the new total differs from the original, show the total-delta hint. Footer: a primary `POSButton touchSize="xl" focusEmphasis="high"` labeled "Save correction", disabled until a reason exists and not pending, that opens `<ManagerPinDialog requiredAction="edit_paid_tab" onSuccess={() => { setPinOpen(false); void handleSubmit(); }} />`; and a secondary outline "Cancel" `POSButton touchSize="large"`. `handleSubmit` builds `orderItemPatches` from overrides/addedRows/removedIds (op update/add/delete), calls `mutation.mutateAsync({ tabId, expectedVersion: tab.version, orderItemPatches, notes: tabNotes, reason })`, and on `!result.ok` toasts the error else toasts success and closes. Wire STALE_VERSION/NOT_FOUND_VERSIONED to `handleVersionError()` in `onError`. Use `useTranslation('featOrders')` with the `editPaidTab.*` key group. Add ALL `featOrders.editPaidTab.*` keys from the UI-SPEC to both `es-MX/featOrders.json` and `en-US/featOrders.json` (identical key sets; natural es-MX copy per Phase 21 convention). Do NOT put fixed refund-style enum reasons — reason is free text.
  </action>
  <verify>
    <automated>cd bar-pos && python -m json.tool src/shared/lib/i18n/locales/en-US/featOrders.json > /dev/null && python -m json.tool src/shared/lib/i18n/locales/es-MX/featOrders.json > /dev/null && npm run typecheck && npm run lint -- src/features/edit-paid-tab</automated>
  </verify>
  <acceptance_criteria>
    - `EditPaidTabDialog` renders per-row qty/price/notes editors, add/remove item affordances, a free-text reason, and a live New-total row
    - The Save CTA opens `ManagerPinDialog(requiredAction='edit_paid_tab')` and only submits on PIN success
    - `featOrders.json` parses in both locales with identical key sets covering every `editPaidTab.*` string in the UI-SPEC; typecheck + lint (incl. no-literal-string) pass
  </acceptance_criteria>
  <done>The correction Sheet exists, is PIN-gated, and calls the RPC with the built patch set.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the "Edit paid ticket" trigger into PaymentPane</name>
  <files>src/widgets/PaymentPane/ui/PaymentPane.tsx</files>
  <read_first>
    - src/widgets/PaymentPane/ui/PaymentPane.tsx (RefundButton lines 18-42 + PaymentHistoryList lines 44-101 = per-payment-row action buttons; refundTarget state line 107 + RefundSheet render lines 229-235 = the exact "hoist a target id + render the sheet" pattern to mirror)
    - src/features/edit-paid-tab/index.ts (the barrel from Task 1 — import EditPaidTabDialog)
    - src/entities/payment (Payment schema exposes tabId via mapPaymentRow — the row's tab id to pass to the dialog)
    - .planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md (Component Reuse Map — EditPaidTabDialog trigger placement)
  </read_first>
  <action>
    In `src/widgets/PaymentPane/ui/PaymentPane.tsx`, mirror the RefundButton/refundTarget/RefundSheet wiring for editing. Add an "Edit ticket" `POSButton` alongside the existing Refund button in each `PaymentHistoryList` row (only for non-refund payment rows), calling a new `onEdit(payment.tabId)` callback. Hoist an `editTarget: string | null` state in the `PaymentPane` component (mirror `refundTarget`), pass `onEdit={setEditTarget}` down, and render `<EditPaidTabDialog open={editTarget !== null} tabId={editTarget} onOpenChange={(open) => { if (!open) setEditTarget(null); }} />` next to the existing `<RefundSheet>`. Label the button via `t('paymentPane.editTicket')` (add the key to both `wPanels.json` locales if PaymentPane uses `wPanels`, or the namespace PaymentPane already uses). Keep behavior otherwise unchanged — this is purely additive.
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint -- src/widgets/PaymentPane && npx vitest run src/widgets/PaymentPane</automated>
  </verify>
  <acceptance_criteria>
    - A manager viewing the Payments page sees an "Edit ticket" button on paid payment rows that opens `EditPaidTabDialog` for that tab
    - `editTarget` state + `EditPaidTabDialog` render mirror the `refundTarget`/`RefundSheet` pattern; existing refund behavior is unchanged
    - typecheck + lint + PaymentPane unit tests pass
  </acceptance_criteria>
  <done>The correction dialog is reachable from the Payments page for a paid tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → EditPaidTabDialog | Client-side UX gate; the RPC is the real enforcement boundary |
| ManagerPinDialog → mutation | PIN correctness checked client-side against profiles.pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-02 | Elevation of Privilege | EditPaidTabDialog / PIN gate | high | mitigate | Client `ManagerPinDialog(requiredAction='edit_paid_tab')` filters eligible staff via `canAccess`; the RPC independently re-checks `role IN ('manager','admin')` (22-02) — the UI gate is never trusted alone |
| T-22-03 | Tampering (data race) | stale tab edited from the dialog | medium | mitigate | Dialog passes `expectedVersion=tab.version`; STALE_VERSION/NOT_FOUND_VERSIONED routed to `handleVersionError()` (toast + refetch), not swallowed |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit N/A); no install task, no checkpoint required |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` green across the feature slice + PaymentPane
- `featOrders.json` valid in both locales with matching key sets
- PaymentPane unit tests green
- Full SC-3 click-through is verified by the E2E in 22-05
</verification>

<success_criteria>
- SC-3: `EditPaidTabDialog` delivers manager PIN gate → field edit (qty/price/notes, add/remove, tab notes) → reason → confirm, reachable from the Payments page and calling the `edit_paid_tab` RPC
</success_criteria>

<output>
Create `.planning/phases/22-edit-paid-ticket-history/22-03-SUMMARY.md` when done
</output>