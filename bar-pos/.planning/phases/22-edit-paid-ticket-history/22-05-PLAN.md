---
phase: 22-edit-paid-ticket-history
plan: 05
type: execute
wave: 3
depends_on: ["22-02", "22-03", "22-04"]
files_modified:
  - e2e/47-edit-paid-tab.spec.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "e2e/47-edit-paid-tab.spec.ts runs live: a manager opens EditPaidTabDialog from /payments, PIN-gates, edits an item with a reason, saves, and the edit then appears on /edit-history with an openable diff"
    - "The full phase gate (typecheck, lint, unit suite, spec 47) is green before verification"
  artifacts:
    - e2e/47-edit-paid-tab.spec.ts
  key_links:
    - "spec 47 SC-3 → EditPaidTabDialog (22-03) → edit_paid_tab RPC (22-02)"
    - "spec 47 SC-4 → /edit-history (22-04) shows the tab.edit_paid audit row + JsonDiffViewer"
---

<objective>
Activate the Wave-0 E2E scaffold `e2e/47-edit-paid-tab.spec.ts` into a live end-to-end proof of SC-3 (dialog correction flow) and SC-4 (/edit-history diff view), then run the full phase verification gate. This is the phase's outer acceptance loop: the RPC (22-02), dialog (22-03), and view (22-04) must compose into one working operator flow.

Purpose: SC-3 and SC-4 are UI-flow criteria that only a real browser run can prove; the integration test (22-02) proved the RPC in isolation. User standing instruction (Phase 21): testing is automated E2E, no manual UAT checkpoints.
Output: A green `e2e/47-edit-paid-tab.spec.ts` and an updated CLAUDE.md E2E spec list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-edit-paid-ticket-history/22-RESEARCH.md
@.planning/phases/22-edit-paid-ticket-history/22-VALIDATION.md
@.planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md
@e2e/38-audit-logs.spec.ts
</context>

## Artifacts this phase produces (whole-phase reference)

- **RPC:** `edit_paid_tab` (22-02) · **New AuditAction:** `tab.edit_paid` (22-01) · **New StaffAction:** `edit_paid_tab` (22-01)
- **New files:** feature slice (22-03), migration + integration test (22-02), /edit-history widget/page/route (22-04), `e2e/47-edit-paid-tab.spec.ts` (22-01 scaffold, activated here)
- **Modified files (this plan):** `e2e/47-edit-paid-tab.spec.ts`, `CLAUDE.md` (E2E spec list)

<tasks>

<task type="auto">
  <name>Task 1: Activate the SC-3 + SC-4 E2E spec</name>
  <files>e2e/47-edit-paid-tab.spec.ts</files>
  <read_first>
    - e2e/47-edit-paid-tab.spec.ts (the Wave-0 scaffold from 22-01 — convert test.fixme placeholders to live tests)
    - e2e/38-audit-logs.spec.ts (DOM/a11y contract for the audit table: how it asserts the action cell, the sr-only "View diff" trigger, and opening the detail sheet — /edit-history mirrors this)
    - e2e/helpers/auth.ts (loginAs(page, 'admin') — admin PIN 0000; manager helpers)
    - src/widgets/PaymentPane/ui/PaymentPane.tsx (final selectors for the "Edit ticket" trigger, added in 22-03)
    - src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx (final selectors for the dialog, PIN gate, reason field, Save CTA — from 22-03)
  </read_first>
  <action>
    Convert the `test.fixme` placeholders in `e2e/47-edit-paid-tab.spec.ts` into live Playwright tests. SC-3 block: log in as a manager/admin, seed or select a paid tab, navigate to /payments, click the "Edit ticket" trigger on that tab's payment row, assert `EditPaidTabDialog` opens, change an item quantity or unit price, enter a reason, click "Save correction", complete the `ManagerPinDialog`, and assert the success toast plus dialog close. SC-4 block (same spec, extra test): after the SC-3 edit, navigate to /edit-history, assert a row with the `tab.edit_paid` action and the entered reason is listed, click the row (or the sr-only "View diff" trigger), and assert the `JsonDiffViewer` opens showing the before/after change. Follow the existing spec conventions (reset test state, in-app SPA navigation not page.goto reloads, deterministic selectors) proven in 38-audit-logs and the Phase 21 i18n specs. Keep the run stable across two back-to-back executions.
  </action>
  <verify>
    <automated>cd bar-pos && npx playwright test e2e/47-edit-paid-tab.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - SC-3: the manager PIN-gated edit flow from /payments passes end-to-end against the live app
    - SC-4: the saved edit appears on /edit-history with its reason and an openable before/after diff
    - No `test.fixme`/`test.skip` remain for SC-3/SC-4; the spec passes twice in a row
  </acceptance_criteria>
  <done>SC-3 and SC-4 are proven by a green live E2E spec.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase gate + register the spec in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md ("E2E Test Suite" section — the numbered spec-file list to extend with 47-edit-paid-tab)
    - .planning/phases/22-edit-paid-ticket-history/22-VALIDATION.md (Sampling Rate → Phase gate: full unit suite + targeted spec 47 before verification)
  </read_first>
  <action>
    Add `47-edit-paid-tab` to the E2E spec list in CLAUDE.md's "E2E Test Suite" section and bump the count. Run the phase gate: `npm run typecheck`, `npm run lint`, `npm run test` (full unit suite), and the targeted `npx playwright test e2e/47-edit-paid-tab.spec.ts`. Confirm all green (accounting for the documented pre-existing unrelated typecheck/test items noted in prior STATE.md sessions — do not introduce new failures). Do NOT touch the legacy `audit_log` (singular) table cleanup — it is out of scope for this phase (RESEARCH.md State of the Art / A4).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md lists `47-edit-paid-tab` in the E2E suite with an updated count
    - `npm run typecheck`, `npm run lint`, and `npm run test` are green (only the documented pre-existing unrelated items remain)
    - No new failures vs the pre-phase baseline
  </acceptance_criteria>
  <done>The full phase gate is green and the new spec is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E harness → live app + DB | Test asserts the composed real flow crosses client → RPC → audit_logs correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Repudiation | end-to-end audit continuity | medium | mitigate | Spec 47 SC-4 asserts the edit is recorded and visible in /edit-history — proves the audit trail is not silently dropped between UI and DB |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit N/A); no install task, no checkpoint required |
</threat_model>

<verification>
- `npx playwright test e2e/47-edit-paid-tab.spec.ts` green twice in a row
- `npm run typecheck` + `npm run lint` + `npm run test` green
- CLAUDE.md E2E list updated
</verification>

<success_criteria>
- SC-3 + SC-4 proven end-to-end; phase gate green; spec documented
</success_criteria>

<output>
Create `.planning/phases/22-edit-paid-ticket-history/22-05-SUMMARY.md` when done
</output>
