---
phase: 22-edit-paid-ticket-history
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/lib/audit-actions.ts
  - src/shared/lib/rbac.ts
  - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts
  - e2e/47-edit-paid-tab.spec.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "'tab.edit_paid' is a valid AuditAction so a migration can call record_audit() with it without failing the CI enforcement test"
    - "'edit_paid_tab' is a valid StaffAction granted to manager+ so ManagerPinDialog can gate on it and the RPC can re-check it"
    - "Failing/pending test scaffolds exist for the RPC integration path and the E2E flow before any implementation lands"
  artifacts:
    - src/shared/lib/audit-actions.ts
    - src/shared/lib/rbac.ts
    - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts
    - e2e/47-edit-paid-tab.spec.ts
  key_links:
    - "audit-actions.ts enum ← record_audit('tab.edit_paid') call added in 22-02 (Pitfall 1: enum MUST land first)"
    - "rbac.ts STAFF_ACTIONS ← ManagerPinDialog requiredAction='edit_paid_tab' in 22-03 + RPC role check in 22-02"
---

<objective>
Land the two enum additions and the Wave-0 test scaffolds that every later plan in Phase 22 depends on. Per RESEARCH.md Pitfall 1, `'tab.edit_paid'` MUST exist in `AuditActionSchema` BEFORE any migration calls `record_audit('tab.edit_paid', ...)`, or `src/shared/lib/__tests__/audit-actions.test.ts` fails CI. Per 22-VALIDATION.md Wave 0 Requirements, the RPC integration spec and the E2E spec must exist as failing/pending scaffolds before implementation so feedback sampling has a target.

Purpose: Unblock 22-02 (RPC + audit call), 22-03 (PIN gate), and 22-04 (/edit-history filter) with the enum strings and test files they reference.
Output: Two modified config files (audit-actions.ts, rbac.ts) + two new test scaffold files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-edit-paid-ticket-history/22-CONTEXT.md
@.planning/phases/22-edit-paid-ticket-history/22-RESEARCH.md
@.planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md
@.planning/phases/22-edit-paid-ticket-history/22-VALIDATION.md
</context>

## Artifacts this phase produces (whole-phase reference — same block in every plan)

- **RPC:** `edit_paid_tab(p_tab_id uuid, p_expected_version int, p_order_item_patches jsonb, p_notes text, p_reason text)` (created in 22-02)
- **New AuditAction:** `tab.edit_paid` (const key `TAB_EDIT_PAID`) — this plan
- **New StaffAction:** `edit_paid_tab` (manager+; route `/edit-history` reuses existing `view_audit_log`) — this plan
- **New files:** `src/features/edit-paid-tab/{model/useEditPaidTab.ts, ui/EditPaidTabDialog.tsx, index.ts}` (22-03); `src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts` (this plan, activated 22-02); `src/widgets/EditHistoryTable/{EditHistoryTable.tsx, index.ts}` (22-04); `src/pages/edit-history/index.tsx` (22-04); `src/app/edit-history-route.tsx` (22-04); `supabase/migrations/20260719000001_edit_paid_tab_rpc.sql` (22-02); `e2e/47-edit-paid-tab.spec.ts` (this plan, activated 22-05)
- **Modified files:** `audit-actions.ts`, `rbac.ts` (this plan); `router.tsx`, `HomeDashboard.tsx`, `EditHistoryTable` i18n (22-04); `PaymentPane.tsx`, `featOrders.json` (22-03); `supabase.types.ts` (22-02)
- **AppErrorCode:** none new — reuse existing `CAJA_CLOSED` (for `NO_OPEN_CAJA`) and `AUTH_FORBIDDEN`

<tasks>

<task type="auto">
  <name>Task 1: Add 'tab.edit_paid' to the AuditAction enum</name>
  <files>src/shared/lib/audit-actions.ts</files>
  <read_first>
    - src/shared/lib/audit-actions.ts (the file being modified — enum array lines 17-55, const object lines 59-85)
    - src/shared/lib/__tests__/audit-actions.test.ts (the CI enforcement that greps migrations — confirm it only asserts migration→enum direction, so an unused enum entry is safe)
    - .planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md (section "src/shared/lib/audit-actions.ts" — exact insertion points)
  </read_first>
  <action>
    In `src/shared/lib/audit-actions.ts`, add the literal `'tab.edit_paid'` to the `AuditActionSchema` z.enum array in the existing `// Tabs` group (immediately after `'tab.split',`). Add the matching const-object entry `TAB_EDIT_PAID: 'tab.edit_paid',` to the `AuditAction` const object immediately after `TAB_SPLIT: 'tab.split',`. Both additions are required so the `satisfies Record<string, AuditAction>` constraint stays exhaustive. This is per RESEARCH.md Pitfall 1 — the enum entry must exist before 22-02's migration calls `record_audit('tab.edit_paid', ...)`.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib/__tests__/audit-actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AuditActionSchema.options` includes `'tab.edit_paid'` and `AuditAction.TAB_EDIT_PAID === 'tab.edit_paid'`
    - `npm run typecheck` passes (the const object still satisfies `Record<string, AuditAction>`)
    - The audit-actions enforcement test passes green
  </acceptance_criteria>
  <done>`'tab.edit_paid'` is a registered audit action in both the schema enum and the const map.</done>
</task>

<task type="auto">
  <name>Task 2: Add 'edit_paid_tab' StaffAction granted to manager+</name>
  <files>src/shared/lib/rbac.ts</files>
  <read_first>
    - src/shared/lib/rbac.ts (the file being modified — STAFF_ACTIONS lines 13-38, MANAGER_EXTRA lines 55-66)
    - .planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md (section "src/shared/lib/rbac.ts" — exact insertion points; note A3: the /edit-history route reuses view_audit_log, edit_paid_tab is a separate action only for the PIN gate + RPC role check)
  </read_first>
  <action>
    In `src/shared/lib/rbac.ts`, append `'edit_paid_tab'` to the `STAFF_ACTIONS` tuple (after `'view_audit_log',`). Add `'edit_paid_tab'` to the `MANAGER_EXTRA` set (after the `'view_audit_log'` entry) so managers and admins (admin inherits via `ADMIN_ACTIONS = MANAGER_ACTIONS ∪ ADMIN_EXTRA`) can access it, and bartenders cannot. Do NOT add it to `ADMIN_EXTRA` (that would make it admin-only). Do NOT add a new route action — `/edit-history` reuses `view_audit_log` per RESEARCH.md A3.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `canAccess('manager', 'edit_paid_tab') === true` and `canAccess('admin', 'edit_paid_tab') === true`
    - `canAccess('bartender', 'edit_paid_tab') === false` and `canAccess('kitchen', 'edit_paid_tab') === false`
    - `isStaffAction('edit_paid_tab') === true`; typecheck passes
  </acceptance_criteria>
  <done>`edit_paid_tab` is a manager+ StaffAction usable as ManagerPinDialog's `requiredAction`.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave-0 failing test scaffolds (RPC integration + E2E)</name>
  <files>src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts, e2e/47-edit-paid-tab.spec.ts</files>
  <read_first>
    - src/entities/payment/model/split-payment-rpc.integration.test.ts (live-Supabase integration test pattern to mirror — how it authenticates, seeds, and calls supabase.rpc)
    - e2e/38-audit-logs.spec.ts (E2E DOM/a11y contract for the audit table that /edit-history mirrors)
    - e2e/helpers/auth.ts (loginAs helper — admin PIN 0000)
    - .planning/phases/22-edit-paid-ticket-history/22-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map — the exact scenarios each scaffold must cover)
  </read_first>
  <action>
    Create `src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts` as a PENDING scaffold: use `describe('edit_paid_tab RPC', ...)` with `it.todo(...)` (or `it.skip`) placeholders naming the SC-1/SC-2 scenarios that 22-02 will activate — happy-path whitelist patch bumps `tabs.version` and returns the new total; `STALE_VERSION` on wrong `p_expected_version`; `AUTH_FORBIDDEN` when caller is not manager/admin; whitelist-violation rejection (a non-whitelisted key in a patch is ignored/rejected); and an `audit_logs` row with before/after diff is written on success (SC-2). Todos must leave the suite green (no live RPC call yet — the RPC does not exist until 22-02).
    Create `e2e/47-edit-paid-tab.spec.ts` as a scaffold using `test.fixme(...)` (or `test.describe.skip`) placeholders for SC-3 (open EditPaidTabDialog from /payments → manager PIN gate → edit an item / reason → save) and SC-4 (/edit-history lists the edit, row click opens the JsonDiffViewer). These must not execute real assertions yet (dialog + view land in 22-03/22-04); they exist so 22-05 activates them.
    No fenced production code — these are test scaffolds; write the `it.todo`/`test.fixme` skeletons directly.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts && npx playwright test e2e/47-edit-paid-tab.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` on the integration scaffold exits 0 with the SC-1/SC-2 scenarios reported as pending/todo (zero failures)
    - `npx playwright test e2e/47-edit-paid-tab.spec.ts --list` lists the SC-3/SC-4 test titles without running failing assertions
    - Both files sit at the exact paths 22-02/22-05 will edit
  </acceptance_criteria>
  <done>Failing/pending scaffolds for SC-1..SC-4 exist and keep the suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → downstream RPC/UI | The enum strings added here become the contract the RPC role-check and PIN gate enforce; a typo silently disables enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-EN | Repudiation | audit-actions.ts enum vs migration | medium | mitigate | Enum lands before any `record_audit('tab.edit_paid')` call (Pitfall 1); CI test `audit-actions.test.ts` fails if a migration references an unregistered action |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | Phase adds ZERO new packages (RESEARCH.md "Package Legitimacy Audit: Not applicable"); no install task exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `npx vitest run src/shared/lib/__tests__/audit-actions.test.ts` green
- `npx vitest run src/shared/lib && npm run typecheck` green
- Integration + E2E scaffolds present and non-failing
</verification>

<success_criteria>
- `tab.edit_paid` registered in AuditAction (enum + const)
- `edit_paid_tab` registered as manager+ StaffAction
- Wave-0 test scaffolds for SC-1..SC-4 exist and keep `npm run test` green
</success_criteria>

<output>
Create `.planning/phases/22-edit-paid-ticket-history/22-01-SUMMARY.md` when done
</output>
