---
phase: 22-edit-paid-ticket-history
plan: 04
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - src/widgets/EditHistoryTable/EditHistoryTable.tsx
  - src/widgets/EditHistoryTable/index.ts
  - src/pages/edit-history/index.tsx
  - src/app/edit-history-route.tsx
  - src/app/router.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/shared/lib/i18n/locales/es-MX/wPanels.json
  - src/shared/lib/i18n/locales/en-US/wPanels.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "/edit-history renders a manager+-gated table of only tab.edit_paid audit rows, with a Reason column reading after.reason and a Ticket column reading entityId"
    - "Clicking a row opens the existing AuditLogDetailSheet → JsonDiffViewer showing the before/after field diff, unchanged"
    - "The page is reachable from the Home dashboard via a manager+ tile"
  artifacts:
    - src/widgets/EditHistoryTable/EditHistoryTable.tsx
    - src/pages/edit-history/index.tsx
    - src/app/edit-history-route.tsx
  key_links:
    - "EditHistoryTable → useAuditLogs({ action: 'tab.edit_paid' }) (action registered 22-01)"
    - "router.tsx /edit-history → EditHistoryRoute (view_audit_log) → EditHistoryPage"
    - "HomeDashboard tile → /edit-history (reachability)"
---

<objective>
Build the read-only `/edit-history` view (D-04, SC-4) by cloning the existing `/audit` composition: a new `EditHistoryTable` widget that reuses `useAuditLogs`, `DataTable`, and the unchanged `AuditLogDetailSheet`/`JsonDiffViewer`, hardcoded to `action='tab.edit_paid'` with added Reason + Ticket columns; a thin page container; a route guard reusing `view_audit_log`; router registration; and a Home dashboard tile for reachability.

Purpose: Give managers a before/after diff audit of every paid-ticket correction (D-04) without inventing a new table — the existing `audit_logs.before`/`after` jsonb plus `JsonDiffViewer`'s client-side `diffJson()` already produce the "field changed, old value, new value" shape (RESEARCH.md Pattern 3).
Output: One widget, one page, one route guard, router + Home wiring, and wAdmin/wPanels/pages i18n keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-edit-paid-ticket-history/22-CONTEXT.md
@.planning/phases/22-edit-paid-ticket-history/22-RESEARCH.md
@.planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md
@.planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md
@src/widgets/AuditLogTable/AuditLogTable.tsx
@src/app/audit-route.tsx
</context>

## Artifacts this phase produces (whole-phase reference)

- **RPC:** `edit_paid_tab` (22-02) · **New AuditAction:** `tab.edit_paid` (22-01) · **New StaffAction:** `edit_paid_tab` (22-01; this route reuses `view_audit_log`)
- **New files (this plan):** `src/widgets/EditHistoryTable/{EditHistoryTable.tsx, index.ts}`, `src/pages/edit-history/index.tsx`, `src/app/edit-history-route.tsx`
- **Modified files (this plan):** `src/app/router.tsx`, `src/widgets/HomeDashboard/ui/HomeDashboard.tsx`, `wAdmin.json`/`wPanels.json`/`pages.json` (both locales)
- **Other phase files:** migration + integration test (22-02); feature slice + PaymentPane (22-03); E2E spec (22-05)

<tasks>

<task type="auto">
  <name>Task 1: Build the EditHistoryTable widget (D-04, filtered clone)</name>
  <files>src/widgets/EditHistoryTable/EditHistoryTable.tsx, src/widgets/EditHistoryTable/index.ts</files>
  <read_first>
    - src/widgets/AuditLogTable/AuditLogTable.tsx (the exact clone source — columns array lines 62-115, DataTable + Load-more + AuditLogDetailSheet wiring lines 149-181; note the sr-only "View diff" Button in the action column must be preserved for the e2e/38 DOM/a11y contract)
    - src/widgets/AuditLogTable/AuditLogDetailSheet.tsx (reused unchanged — import directly, do NOT clone)
    - src/entities/audit-log/model/queries.ts (useAuditLogs(filters) accepts { action }, and auditKeys has only `all` + `list(filters)` — there is NO `auditKeys.lists()`; the Reason column reads row.original.after)
    - .planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md (Copywriting Contract → /edit-history section: column labels, empty-state copy)
    - .planning/phases/22-edit-paid-ticket-history/22-PATTERNS.md (EditHistoryTable section — the exact Reason column cell shape)
  </read_first>
  <action>
    Create `src/widgets/EditHistoryTable/EditHistoryTable.tsx` as a clone of `AuditLogTable.tsx` with these deviations: drop the `AuditLogFilterBar` and all staged/applied filter state — call `useAuditLogs({ action: 'tab.edit_paid' })` with the hardcoded filter (no user-facing action filter, per RESEARCH.md Pattern 3). Keep the base `action`/`entityType`/`actor`/`createdAt`/`source` columns verbatim including the sr-only diff-trigger `Button` in the action column (preserves the e2e a11y contract). Add a `reason` column whose cell reads `const after = row.original.after as Record<string, unknown> | null; return typeof after?.['reason'] === 'string' ? after['reason'] : '—'` with header `t('editHistoryTable.columnReason')`. Add a `ticket` column reading `row.original.entityId` (short id) with header `t('editHistoryTable.columnTicket')`. Reuse `AuditLogDetailSheet` (imported from `@widgets/AuditLogTable/AuditLogDetailSheet`), `DataTable`, `EmptyState`, `useStaffList`, and the Load-more button unchanged. Use `useTranslation('wAdmin')` and the `editHistoryTable.*` key group. Use empty-state copy keys `editHistoryTable.noActivityTitle`/`noActivityDescription` per the UI-SPEC. Create `src/widgets/EditHistoryTable/index.ts` exporting `EditHistoryTable` (mirror AuditLogTable's barrel).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint -- src/widgets/EditHistoryTable</automated>
  </verify>
  <acceptance_criteria>
    - `EditHistoryTable` compiles, calls `useAuditLogs({ action: 'tab.edit_paid' })`, and renders Reason + Ticket columns plus the base columns
    - The sr-only "View diff" trigger and row-click both open `AuditLogDetailSheet`
    - `npm run lint` passes with no `i18next/no-literal-string` violation (all copy via `t('editHistoryTable.*')`)
  </acceptance_criteria>
  <done>A manager-facing widget lists only paid-ticket edits with a Reason column and opens the diff viewer.</done>
</task>

<task type="auto">
  <name>Task 2: Add the /edit-history page, route guard, and router registration</name>
  <files>src/app/edit-history-route.tsx, src/pages/edit-history/index.tsx, src/app/router.tsx</files>
  <read_first>
    - src/app/audit-route.tsx (17-line guard to clone — reuse `view_audit_log` per A3, only the redirect toast copy changes)
    - src/pages/audit/index.tsx (thin PageContainer container to mirror — title only)
    - src/app/router.tsx (lines 6, 29, 179-188 — AuditRoute import, AuditPage lazy import, and the `/audit` <Route> block to clone for `/edit-history`)
    - src/pages/reports/index.tsx OR src/pages/audit/index.tsx (whichever shows the PageContainer title/`pages` namespace usage)
  </read_first>
  <action>
    Create `src/app/edit-history-route.tsx` as an exact clone of `audit-route.tsx` named `EditHistoryRoute`, keeping the SAME `can('view_audit_log')` check (RESEARCH.md A3 — no new route action) and a manager/admin redirect toast (reuse the existing restricted-page copy). Create `src/pages/edit-history/index.tsx` as a thin container mirroring `pages/audit/index.tsx`: render `EditHistoryTable` inside the same `PageContainer` shell with a `pages`-namespace title key (e.g. `t('editHistory.title')` = "Edit History"). In `src/app/router.tsx`: add `import { EditHistoryRoute } from './edit-history-route';`, add `const EditHistoryPage = lazy(() => import('../pages/edit-history'));`, and add a `<Route path="/edit-history">` element wrapping `<ProtectedRoute><EditHistoryRoute><EditHistoryPage /></EditHistoryRoute></ProtectedRoute>` immediately after the existing `/audit` route block. Export a default from the page so the lazy import resolves.
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint -- src/app/edit-history-route.tsx src/pages/edit-history src/app/router.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/edit-history` is registered in `router.tsx`, wrapped in `ProtectedRoute` + `EditHistoryRoute`
    - `EditHistoryRoute` redirects non-`view_audit_log` users to `/home` with a toast
    - The page renders `EditHistoryTable` inside `PageContainer`; typecheck + lint pass
  </acceptance_criteria>
  <done>Navigating to `/edit-history` as a manager/admin renders the edit-history table; others are redirected.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Home dashboard tile + i18n keys (both locales)</name>
  <files>src/widgets/HomeDashboard/ui/HomeDashboard.tsx, src/shared/lib/i18n/locales/es-MX/wPanels.json, src/shared/lib/i18n/locales/en-US/wPanels.json, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json</files>
  <read_first>
    - src/widgets/HomeDashboard/ui/HomeDashboard.tsx (ITEMS array lines 41-108 — the `/audit` tile at lines 90-94 is the exact shape to clone; uses `useTranslation('wPanels')`)
    - src/shared/lib/i18n/locales/en-US/wPanels.json + es-MX/wPanels.json (homeDashboard.tiles.* group — add an editHistory tile label)
    - src/shared/lib/i18n/locales/en-US/wAdmin.json + es-MX/wAdmin.json (auditLogTable.* group — add the editHistoryTable.* sibling group)
    - src/shared/lib/i18n/locales/en-US/pages.json + es-MX/pages.json (add editHistory.title)
    - .planning/phases/22-edit-paid-ticket-history/22-UI-SPEC.md (all en-US copy strings; es-MX uses the byte-identical-to-source convention — write natural es-MX copy, not machine-literal)
  </read_first>
  <action>
    In `HomeDashboard.tsx`, add one `DashboardItem` to the `ITEMS` array cloning the `/audit` tile: `{ path: '/edit-history', labelKey: 'homeDashboard.tiles.editHistory', icon: History, requiredAction: 'view_audit_log', managerLabelKey: 'homeDashboard.managerLabels.manager' }` (import `History` from lucide-react; this makes the page reachable and gated). Add the i18n keys in BOTH locales: `wPanels.json` → `homeDashboard.tiles.editHistory` ("Edit History" en / natural es-MX); `wAdmin.json` → an `editHistoryTable` group with `columnReason` ("Reason"), `columnTicket` ("Ticket"), `noActivityTitle` ("No edits yet"), `noActivityDescription`, `loadMoreEntries`, and any load-error keys the widget references (reuse the auditLogTable copy shapes from the UI-SPEC); `pages.json` → `editHistory.title` ("Edit History"). Keep es-MX and en-US key sets identical (the committed i18next lint gate + Phase 21 catalog convention).
  </action>
  <verify>
    <automated>cd bar-pos && node -e "['es-MX','en-US'].forEach(l=>['wPanels','wAdmin','pages'].forEach(n=>JSON.parse(require('fs').readFileSync('src/shared/lib/i18n/locales/'+l+'/'+n+'.json','utf8'))));console.log('locale JSON valid')" && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - A `/edit-history` tile appears on Home for manager+ (gated by `view_audit_log`) and navigates to the page
    - `wPanels.json`, `wAdmin.json`, and `pages.json` parse as valid JSON in both locales with identical key sets across locales
    - `npm run lint` passes (no `i18next/no-literal-string` violation) and `npm run typecheck` passes
  </acceptance_criteria>
  <done>The edit-history page is reachable from Home and all copy is localized in both locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /edit-history route | A non-manager could navigate directly to the URL |
| audit_logs read → UI render | `after.reason` is attacker-influenced free text rendered into the table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Elevation of Privilege | /edit-history route access | high | mitigate | `EditHistoryRoute` gates on `view_audit_log` (manager+); `audit_logs` RLS `audit_logs_select_manager` also restricts the underlying query server-side (defense in depth) |
| T-22-06 | Cross-Site Scripting | rendering after.reason in a table cell | low | mitigate | React escapes text content by default; the Reason cell renders `after.reason` as a plain string (no `dangerouslySetInnerHTML`), same as AuditLogTable's existing cells |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit N/A); no install task, no checkpoint required |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` green across the new widget/page/route/router/Home changes
- Locale JSON valid in both locales with matching key sets
- `/edit-history` reachable (Home tile) and gated (EditHistoryRoute)
- Full SC-4 behavior (real edit appears + diff opens) is verified by the E2E in 22-05
</verification>

<success_criteria>
- SC-4: `/edit-history` lists `tab.edit_paid` edits with a Reason column and opens `JsonDiffViewer` per row (D-04), reusing the AuditLogTable shell and reachable from Home
</success_criteria>

<output>
Create `.planning/phases/22-edit-paid-ticket-history/22-04-SUMMARY.md` when done
</output>