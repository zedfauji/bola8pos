---
phase: 14-audit-logs-table
plan: 04
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - supabase/migrations/20260703000003_caja_open_prep_batch_rpcs.sql
  - src/entities/caja/model/queries.ts
  - src/entities/prep/model/queries.ts
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "Opening a caja session goes through a new caja_open SECURITY DEFINER RPC that records a 'caja.open' audit row atomically (D-02: no client-side post-insert audit)"
    - "Producing a prep batch goes through a new produce_prep_batch SECURITY DEFINER RPC that records a 'prep.produce' audit row atomically, with the existing prep_productions validation trigger still firing"
    - "Both client mutation hooks call the new RPCs instead of direct table INSERTs, preserving their existing Result mapping + query invalidation"
  artifacts:
    - path: "supabase/migrations/20260703000003_caja_open_prep_batch_rpcs.sql"
      provides: "caja_open + produce_prep_batch SECURITY DEFINER RPCs with record_audit"
      contains: "PERFORM record_audit"
  key_links:
    - from: "useMutationOpenCaja"
      to: "caja_open"
      via: "supabase.rpc('caja_open', ...)"
      pattern: "rpc\\('caja_open'"
    - from: "useMutationCreatePrepProduction"
      to: "produce_prep_batch"
      via: "supabase.rpc('produce_prep_batch', ...)"
      pattern: "rpc\\('produce_prep_batch'"
---

<objective>
Two of the ROADMAP's "remaining RPCs" (`caja_open`, `produce_prep_batch`) do not exist as RPCs today — they are direct client-side table INSERTs under RLS (RESEARCH.md Pitfall 6 + D-02). Per D-01a the planner must build them fully: create new thin SECURITY DEFINER wrapper RPCs that perform the same INSERT and call `record_audit` atomically, then swap the two client call sites from `.from(table).insert(...)` to `.rpc(...)`.

Purpose: SC3 coverage for caja opening and prep-batch production with atomic audit (D-02 explicitly forbids client-side post-insert audit for caja_open).
Output: One migration creating both RPCs; two entity mutation hooks rewired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@supabase/migrations/20260511000002_rpc_audit_wiring.sql
@src/entities/caja/model/queries.ts
@src/entities/prep/model/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — create caja_open ('caja.open') and produce_prep_batch ('prep.produce') SECURITY DEFINER RPCs</name>
  <files>supabase/migrations/20260703000003_caja_open_prep_batch_rpcs.sql</files>
  <read_first>
    - src/entities/caja/model/queries.ts (lines 147-178 useMutationOpenCaja — the current `db.from('caja_sessions').insert({ opening_cash, opened_by }).select().single()`; the RPC must return the inserted row shape mapCajaRow expects)
    - src/entities/prep/model/queries.ts (lines 70-114 useMutationCreatePrepProduction — current `db.from('prep_productions').insert({ prep_ingredient_id, qty_produced, notes, produced_by }).select().single()`; a DB trigger raises PREP_INGREDIENT_REQUIRED / INGREDIENT_NOT_FOUND / INVENTORY_NEGATIVE and performs ingredient depletion — the wrapper RPC must let that trigger fire)
    - supabase/migrations/20260511000002_rpc_audit_wiring.sql (close_caja_session + add_combo_to_tab — the analog INSERT/UPDATE-then-audit SECURITY DEFINER shape; record_audit called pre-RETURN on success only)
    - .planning/phases/14-audit-logs-table/14-PATTERNS.md (Pattern 2 — direct-INSERT→new-RPC precedent; NULL before for newly-created rows)
  </read_first>
  <action>
    New migration `BEGIN; ... COMMIT;` + commented DOWN. Create two functions:
    (A) `caja_open(p_opening_cash numeric, p_opened_by uuid, p_terminal_id text DEFAULT NULL) RETURNS caja_sessions LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body: `INSERT INTO caja_sessions (opening_cash, opened_by) VALUES (p_opening_cash, p_opened_by) RETURNING * INTO v_row;` then `PERFORM record_audit('caja.open', 'caja_session', v_row.id, NULL, to_jsonb(v_row), 'rpc', p_terminal_id);` then `RETURN v_row;`. Do NOT re-implement RLS role checks that the table already enforces via existing INSERT policy — SECURITY DEFINER bypasses RLS, so the RPC itself must gate: verify `auth.uid()` maps to a profile with role IN ('manager','admin') using the same `SELECT role INTO v_role FROM profiles WHERE id = auth.uid();` guard used by close_caja_session, returning/raising a PERMISSION_DENIED error before the INSERT if not. (This preserves the manage_caja gate that the direct-INSERT path relied on via RLS.) Respect the `caja_sessions_one_open` constraint — do not add extra open-session logic; let the unique constraint raise naturally (the EXCEPTION path is not audited).
    (B) `produce_prep_batch(p_prep_ingredient_id uuid, p_qty_produced numeric, p_notes text DEFAULT NULL, p_produced_by uuid DEFAULT NULL, p_terminal_id text DEFAULT NULL) RETURNS prep_productions LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body: `INSERT INTO prep_productions (prep_ingredient_id, qty_produced, notes, produced_by) VALUES (p_prep_ingredient_id, p_qty_produced, p_notes, p_produced_by) RETURNING * INTO v_row;` (the existing BEFORE/AFTER INSERT trigger fires here and performs depletion + raises PREP_INGREDIENT_REQUIRED/INGREDIENT_NOT_FOUND/INVENTORY_NEGATIVE — do NOT catch these; let them propagate so the client's existing error mapping keeps working). Then `PERFORM record_audit('prep.produce', 'prep_production', v_row.id, NULL, to_jsonb(v_row), 'rpc', p_terminal_id);` then `RETURN v_row;`. GRANT EXECUTE on both to `authenticated`. Use action labels exactly as in audit-actions.ts ('caja.open', 'prep.produce').
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-actions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: migration defines `CREATE FUNCTION caja_open(` and `CREATE FUNCTION produce_prep_batch(` each `SECURITY DEFINER`.
    - source: contains `PERFORM record_audit('caja.open', 'caja_session',` and `PERFORM record_audit('prep.produce', 'prep_production',`.
    - source: caja_open gates on `role IN ('manager','admin')` before the INSERT (manage_caja parity); produce_prep_batch does NOT catch the validation trigger's exceptions.
    - behavior: the audit-actions coverage test's caja_open + produce_prep_batch assertions now PASS.
  </acceptance_criteria>
  <done>Both wrapper RPCs exist with atomic audit; validation errors still propagate. Migration not pushed (14-14).</done>
</task>

<task type="auto">
  <name>Task 2: Rewire useMutationOpenCaja to call caja_open RPC</name>
  <files>src/entities/caja/model/queries.ts</files>
  <read_first>
    - src/entities/caja/model/queries.ts (lines 147-178 — the mutation to change; `mapCajaRow` maps a caja_sessions row to CajaSession; onSuccess sets store + invalidates cajaKeys.all)
    - src/shared/lib/logger-instance.ts (line 19 — TERMINAL_ID convention)
  </read_first>
  <action>
    Replace the `db.from('caja_sessions').insert({ opening_cash: openingCash, opened_by: openedBy }).select().single()` call inside `supabaseMutation(...)` with `db.rpc('caja_open', { p_opening_cash: openingCash, p_opened_by: openedBy, p_terminal_id: TERMINAL_ID })`. Add a module-level `const TERMINAL_ID = (import.meta.env.VITE_TERMINAL_ID as string | undefined) ?? 'POS-1';` if not already present in the file. The RPC returns the inserted caja_sessions row (single object), so `res.data` still feeds `mapCajaRow(res.data as Record<string, unknown>)` exactly as before. Keep `supabaseMutation` wrapping, the `res.data == null → err(unknownError('no_row'))` guard, the logger.error on failure, and the onSuccess `setCaja` + `invalidateQueries({ queryKey: cajaKeys.all })`. If the file uses `db = supabase as any`, the rpc call needs no new types.
  </action>
  <verify>
    <automated>npx vitest run src/entities/caja --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: useMutationOpenCaja calls `db.rpc('caja_open', { p_opening_cash, p_opened_by, p_terminal_id })` and no longer calls `.from('caja_sessions').insert`.
    - behavior: existing caja unit tests pass; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Caja opening is audited atomically via caja_open (D-02 satisfied — no client-side post-insert audit).</done>
</task>

<task type="auto">
  <name>Task 3: Rewire useMutationCreatePrepProduction to call produce_prep_batch RPC</name>
  <files>src/entities/prep/model/queries.ts</files>
  <read_first>
    - src/entities/prep/model/queries.ts (lines 70-114 — the mutation; note the error-message mapping for PREP_INGREDIENT_REQUIRED / INGREDIENT_NOT_FOUND / INVENTORY_NEGATIVE which must keep working, and the mapPrepProductionRow → PrepProductionSchema.safeParse path)
    - src/shared/lib/logger-instance.ts (line 19 — TERMINAL_ID convention)
  </read_first>
  <action>
    Replace the `db.from('prep_productions').insert({ prep_ingredient_id, qty_produced, notes, produced_by }).select().single()` with `db.rpc('produce_prep_batch', { p_prep_ingredient_id: input.prepIngredientId, p_qty_produced: input.qtyProduced, p_notes: input.notes ?? null, p_produced_by: input.producedBy ?? null, p_terminal_id: TERMINAL_ID })`. Add the module-level `TERMINAL_ID` const if absent. The RPC RAISEs the same PREP_INGREDIENT_REQUIRED/INGREDIENT_NOT_FOUND/INVENTORY_NEGATIVE messages (from the trigger, re-surfaced through the RPC error), so the existing `msg.includes(...)` branches keep mapping to the correct AppError codes — leave that block unchanged. The RPC returns the inserted prep_productions row, so `mapPrepProductionRow(data as Record<string, unknown>)` + `PrepProductionSchema.safeParse` still applies. Keep the onSuccess invalidations (prepKeys.lists(), prepKeys.byIngredient, ['ingredients']).
  </action>
  <verify>
    <automated>npx vitest run src/entities/prep --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: useMutationCreatePrepProduction calls `db.rpc('produce_prep_batch', {...})` and no longer calls `.from('prep_productions').insert`.
    - behavior: the PREP_INGREDIENT_REQUIRED / INGREDIENT_NOT_FOUND / INVENTORY_NEGATIVE error mapping still resolves; prep unit tests + `npm run typecheck` pass.
  </acceptance_criteria>
  <done>Prep-batch production is audited atomically via produce_prep_batch; validation + depletion trigger behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → caja_open / produce_prep_batch RPC | SECURITY DEFINER bypasses RLS; the caja_open RPC must re-assert the manage_caja (manager+) gate the direct-INSERT path got from RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Elevation of Privilege | caja_open bypassing RLS role gate | mitigate | caja_open re-checks `role IN ('manager','admin')` via profiles + auth.uid() before INSERT (same guard as close_caja_session) |
| T-14-05 | Repudiation | caja open + prep production | mitigate | atomic PERFORM record_audit on success path, terminal_id carried |
| T-14-04 | Information Disclosure | audit failure | accept | record_audit swallows its own exceptions; trigger validation errors still propagate to client by design |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-04)
- `supabase/migrations/20260703000003_caja_open_prep_batch_rpcs.sql`
- `caja_open(numeric, uuid, text)` SECURITY DEFINER RPC — audits `caja.open`
- `produce_prep_batch(uuid, numeric, text, uuid, text)` SECURITY DEFINER RPC — audits `prep.produce`
- Rewired `useMutationOpenCaja` (calls `caja_open`) + `useMutationCreatePrepProduction` (calls `produce_prep_batch`)
</artifacts_produced>

<verification>
- audit-actions coverage test advances (caja_open + produce_prep_batch green).
- caja + prep entity unit tests pass; `npm run typecheck` exits 0.
</verification>

<success_criteria>
- Both new SECURITY DEFINER RPCs perform the original mutation + atomic audit; caja_open enforces manager+; produce_prep_batch preserves trigger validation.
- Client hooks call the RPCs; Result mapping + invalidations unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-04-SUMMARY.md` when done.
</output>
