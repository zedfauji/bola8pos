---
phase: 14-audit-logs-table
plan: 07
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - supabase/functions/void-order/index.ts
autonomous: true
requirements: [SC4]
must_haves:
  truths:
    - "A /functions/v1/void-order Edge Function exists in the repo, satisfies VoidOrderRequestSchema/VoidOrderResponseSchema, and voids the order as the frontend expects"
    - "On successful void, the function calls recordAudit('order.void', ...) via _shared/audit.ts with actorId = staffId and source 'edge'"
    - "Audit failure never fails the void (fire-and-forget)"
  artifacts:
    - path: "supabase/functions/void-order/index.ts"
      provides: "void-order Edge Function with recordAudit('order.void')"
      contains: "order.void"
  key_links:
    - from: "supabase/functions/void-order/index.ts"
      to: "recordAudit"
      via: "import from ../_shared/audit.ts + call with action 'order.void'"
      pattern: "order.void"
---

<objective>
The `void-order` Edge Function that `callVoidOrder()` fetches (`/functions/v1/void-order`) does NOT exist in the repository (RESEARCH.md Pitfall 2). Per D-01a the planner must build it fully — with `recordAudit('order.void')` built in from day one. The 14-01 Wave-0 checkpoint determined whether it also exists live (recover-and-patch) or must be authored from scratch; follow the path recorded in `14-01-SUMMARY.md`.

Purpose: SC4 coverage for the void flow (and unblocks D-07's E2E `order.void` restoration in 14-14).
Output: `supabase/functions/void-order/index.ts` with request validation, void semantics, and an `order.void` audit call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-01-SUMMARY.md
@.planning/phases/14-audit-logs-table/14-RESEARCH.md
@src/shared/lib/edge-function-contracts.ts
@src/features/void-order/model/useVoidOrder.ts
@supabase/functions/process-payment/index.ts
@supabase/functions/_shared/audit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author (or recover) the void-order Edge Function satisfying the void contract</name>
  <files>supabase/functions/void-order/index.ts</files>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-01-SUMMARY.md (Task 2 result — "build fresh" vs "recover + patch"; if recover, pull the deployed source via `supabase functions download void-order` and commit it, then only add the audit call in Task 2)
    - src/shared/lib/edge-function-contracts.ts (lines 515-600 — VoidOrderRequestSchema { orderId, reason min1 max500, staffId, amount?, inventoryRestoreItems?[] } and VoidOrderResponseSchema { success, voidedAt?, error? }; callVoidOrder POSTs JSON and expects 200 + { success: true })
    - src/features/void-order/model/useVoidOrder.ts (the caller — note the CLIENT performs deplete_for_order_item(-1) inventory reversal AFTER callVoidOrder succeeds, so the edge function does NOT need to reverse inventory; it must void the order state the frontend then treats as voided)
    - supabase/functions/process-payment/index.ts (lines 1-90 — the Deno edge-function template: createClient from esm.sh, zod from deno.land, BodySchema.parse, corsHeaders, jsonResponse helper, OPTIONS preflight handling, service-role client construction)
    - the orders / order_items schema migration (grep supabase/migrations for `CREATE TABLE orders` and `is_voided` to learn the exact void representation — e.g. order_items.is_voided boolean, or orders.status = 'voided')
  </read_first>
  <action>
    If 14-01 recorded "recover + patch", download and commit the deployed function first, then proceed to Task 2. Otherwise author from scratch: create `supabase/functions/void-order/index.ts` mirroring process-payment's structure — import `createClient` from the esm.sh supabase-js pin, `z` from the deno.land zod pin; define a BodySchema matching VoidOrderRequestSchema field names (orderId, reason min(1) max(500), staffId, amount optional, inventoryRestoreItems optional array of {orderItemId, productId, quantity}); copy the `corsHeaders`, `jsonResponse`, and OPTIONS-preflight handling verbatim; construct a service-role client from `Deno.env.get('SUPABASE_URL')` + `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')`. Void semantics: after parsing, capture the current order state (before), then mark the order voided using the exact representation found in the schema read (e.g. `UPDATE order_items SET is_voided = true WHERE order_id = orderId` and/or set orders.status) — do NOT reverse inventory here (the client hook does that). On success return `jsonResponse({ success: true, voidedAt: new Date().toISOString() }, 200)`; on validation failure return `jsonResponse({ success: false, error }, 400)`; on internal error return `jsonResponse({ success: false, error: 'Void failed' }, 500)`. Leave the `recordAudit` call for Task 2 (add a TODO marker where it will go, on the success path just before the 200 response).
  </action>
  <verify>
    <automated>test -f supabase/functions/void-order/index.ts && grep -q "success: true" supabase/functions/void-order/index.ts && grep -qi "orderId\|order_id" supabase/functions/void-order/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: `supabase/functions/void-order/index.ts` exists, parses a body matching VoidOrderRequestSchema, and returns `{ success: true, voidedAt }` on the success path.
    - source: the function voids the order using the representation confirmed from the orders/order_items schema (documented in a code comment) and does NOT reverse inventory (client does that).
    - behavior: OPTIONS preflight + corsHeaders + service-role client construction mirror process-payment/index.ts.
  </acceptance_criteria>
  <done>void-order Edge Function exists and satisfies the callVoidOrder contract; audit call pending (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Wire recordAudit('order.void') into the void-order success path</name>
  <files>supabase/functions/void-order/index.ts</files>
  <read_first>
    - supabase/functions/_shared/audit.ts (recordAudit signature — AuditParams now includes actorId per 14-02; fire-and-forget, never throws; source defaults 'edge')
    - src/shared/lib/audit-actions.ts (confirm 'order.void' is the enumerated label, entity_type 'order' — Pitfall 7: use 'order.void', NOT 'tab.void')
  </read_first>
  <action>
    Add `import { recordAudit } from '../_shared/audit.ts';` at the top. On the success path, immediately before the `jsonResponse({ success: true, ... }, 200)` return, `await recordAudit(supabase, { action: 'order.void', entityType: 'order', entityId: orderId, before: <captured before-state or null>, after: { reason, voidedAt }, source: 'edge', actorId: staffId });`. Because recordAudit is fire-and-forget (try/catch swallows everything), no additional error handling is needed and it must NOT be placed inside a try that would alter the 200 response. Use action label 'order.void' and entity_type 'order' exactly (NOT 'tab.void' — that enum value is reserved for a future whole-tab void per RESEARCH.md Pitfall 7; do not use or remove it here).
  </action>
  <verify>
    <automated>grep -q "recordAudit" supabase/functions/void-order/index.ts && grep -q "'order.void'" supabase/functions/void-order/index.ts && grep -q "actorId: staffId" supabase/functions/void-order/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: void-order imports recordAudit from '../_shared/audit.ts' and calls it with action 'order.void', entityType 'order', entityId orderId, actorId staffId, source 'edge'.
    - source: the recordAudit call is on the success path only and does not gate the 200 response.
    - behavior: the label 'order.void' matches AuditActionSchema; 'tab.void' is not used.
  </acceptance_criteria>
  <done>Void flow emits an 'order.void' audit row with actor = staffId. Deployment happens in 14-14 (supabase functions deploy).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → void-order Edge Function | untrusted request body (orderId, reason, staffId) crosses into a service-role Deno context |
| Edge Function (service-role) → audit_logs | service-role bypasses RLS; actor_id supplied explicitly as staffId (auth.uid() not meaningful under service-role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Repudiation | order void with no compliance trail | mitigate | recordAudit('order.void') on success path with actorId = staffId |
| T-14-10 | Tampering | unvalidated void request body | mitigate | zod BodySchema.parse (matching VoidOrderRequestSchema) rejects malformed input with 400 before any mutation |
| T-14-04 | Information Disclosure | internal error leakage | mitigate | 500 path returns generic 'Void failed'; recordAudit swallows its own errors (never surfaced) |
| T-14-SC | Tampering | npm installs | accept | Zero new npm packages; Deno imports are pinned esm.sh/deno.land URLs matching the existing process-payment function |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-07)
- `supabase/functions/void-order/index.ts` — new (or recovered+patched) Edge Function
- `recordAudit('order.void', source:'edge', actorId: staffId)` call within it
</artifacts_produced>

<verification>
- File exists, satisfies the void contract, imports + calls recordAudit with 'order.void'.
- The 14-08 sensitive-edge coverage test (allowlist including void-order) will assert this; the 14-14 E2E restores the order.void filter test.
</verification>

<success_criteria>
- void-order Edge Function exists and satisfies callVoidOrder's contract.
- Successful voids emit an 'order.void' audit row with actor = staffId; audit failure never fails the void.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-07-SUMMARY.md` when done.
</output>
