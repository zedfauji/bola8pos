---
phase: 14-audit-logs-table
plan: 14
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03", "14-04", "14-05", "14-06", "14-07", "14-08", "14-09", "14-10", "14-11", "14-12", "14-13"]
files_modified:
  - e2e/38-audit-logs.spec.ts
  - CLAUDE.md
autonomous: false
requirements: [SC1, SC8]
must_haves:
  truths:
    - "All Phase-14 migrations (record_audit/8, transfer_tab + record_stock_movement patches, caja_open + produce_prep_batch, close_tab, force_pin_change + must_change_pin, role_permissions view_audit_log seed) plus 20260511000001/000002 if not already live are pushed to remote Supabase and verified"
    - "void-order, create-staff, settings-restore Edge Functions are deployed to remote"
    - "e2e/38-audit-logs.spec.ts restores the order.void filter test (D-07) and is added to CLAUDE.md's tracked spec list"
    - "The full verification gate is green: typecheck, lint, unit suite, audit-actions RPC-coverage (all 10 green), audit-edge-coverage, and E2E 38"
  artifacts:
    - path: "e2e/38-audit-logs.spec.ts"
      provides: "order.void coverage restored per D-07"
      contains: "order.void"
  key_links:
    - from: "remote Supabase"
      to: "Phase-14 migrations + edge functions"
      via: "supabase db push + supabase functions deploy"
      pattern: "record_audit"
---

<objective>
Land Phase 14 on remote and prove it. This is the BLOCKING schema-push + edge-deploy plan (per schema_push_requirement): all Phase-14 migrations and the three sensitive Edge Functions must be applied to remote Supabase, because build/type checks pass WITHOUT the push (types come from config, not the live DB) — a false-positive verification state. Then restore the D-07 `order.void` E2E coverage (now that void is wired), register the spec in CLAUDE.md, and run the full green gate.

Purpose: SC1 (live migration) + SC8 (E2E coverage incl. order.void + bartender redirect) + the phase verification gate.
Output: pushed schema + deployed functions + updated E2E spec + CLAUDE.md + a green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-01-SUMMARY.md
@.planning/phases/14-audit-logs-table/14-VALIDATION.md
@e2e/38-audit-logs.spec.ts
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Push all Phase-14 migrations + deploy Edge Functions to remote, then smoke-test</name>
  <files>none (remote push/deploy + verification — no repo files modified in this task)</files>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-01-SUMMARY.md (whether 20260511000001/000002 are already live — if NOT, include them in this push)
    - the six new migrations: 20260703000001_record_audit_terminal_id.sql, 20260703000002_wire_transfer_tab_stock_movement_audit.sql, 20260703000003_caja_open_prep_batch_rpcs.sql, 20260703000004_close_tab_rpc.sql, 20260703000005_force_pin_change.sql, 20260703000006_role_permissions_view_audit_log.sql
    - the three edge functions: supabase/functions/void-order, supabase/functions/create-staff, supabase/functions/settings-restore
  </read_first>
  <action>
    Automation-first: run `supabase db push` (SUPABASE_ACCESS_TOKEN set for non-TTY per CLAUDE.md; this is a REMOTE push, not a local Docker start) so all six new migrations apply (plus 000001/000002 if 14-01 found them pending). Then run `supabase functions deploy void-order`, `supabase functions deploy create-staff`, `supabase functions deploy settings-restore`. Then run the smoke probe via Supabase MCP execute_sql / psql: confirm record_audit/8 resolves (`SELECT record_audit('test.probe'::text,'test'::text) IS NOT NULL;` in a throwaway/rollback context) and `SELECT to_regclass('public.audit_logs') IS NOT NULL, EXISTS(SELECT 1 FROM pg_proc WHERE proname IN ('caja_open','close_tab','produce_prep_batch','force_pin_change','clear_must_change_pin'));` returns all true. Record all outputs in the SUMMARY, then present to the human for confirmation.
  </action>
  <verify>
    <automated>echo "supabase db push + functions deploy + smoke probe — outputs recorded in SUMMARY; migration list + functions list confirmed by human"</automated>
  </verify>
  <what-built>Claude runs the push + deploy first (automation-first), then a human confirms remote success.</what-built>
  <how-to-verify>
    1. `supabase db push` applies all six new migrations (plus 000001/000002 if pending).
    2. `supabase functions deploy void-order|create-staff|settings-restore`.
    3. Smoke probe: record_audit/8 resolves; audit_logs + the five new RPCs exist live.
    4. Human confirms `supabase migration list` (six new applied) + `supabase functions list` (three deployed).
  </how-to-verify>
  <acceptance_criteria>
    - behavior: `supabase migration list` shows 20260703000001–20260703000006 applied on remote (plus 000001/000002 if they were pending).
    - behavior: `supabase functions list` shows void-order, create-staff, settings-restore deployed.
    - source: SUMMARY records the smoke-probe output confirming record_audit/8 + the new RPCs exist live.
  </acceptance_criteria>
  <done>All Phase-14 schema + functions live on remote; smoke probe green; human-confirmed.</done>
  <resume-signal>Type "pushed + deployed + smoke green" or paste the migration-list / functions-list / smoke-probe output.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Restore the order.void E2E test (D-07) + add 38-audit-logs to CLAUDE.md spec list</name>
  <files>e2e/38-audit-logs.spec.ts, CLAUDE.md</files>
  <read_first>
    - e2e/38-audit-logs.spec.ts (full — the header NOTE documenting the combo.add_to_tab substitution (lines 12-17) and Test 2 "should display audit entry after adding a combo to tab" (lines 96-121) to restore to an order.void filter test now that void is wired in 14-07)
    - src/features/void-order/model/useVoidOrder.ts (the void flow — order.void action)
    - CLAUDE.md (the "## E2E Test Suite" section listing 17 specs `01-ci ... 39-concurrent-edits` — 38-audit-logs is MISSING and must be added per D-07)
  </read_first>
  <action>
    In e2e/38-audit-logs.spec.ts: remove the header NOTE about the combo.add_to_tab substitution (lines 12-17). Replace Test 2 with an `order.void` filter test: as manager on /audit, open the `#audit-filter-action` Select, choose the `order.void` option, click "Apply filters", and assert either `getByText(/no matches/i)` OR `getByRole('cell', { name: 'order.void' })` (same tolerant pattern as the existing refund test — the void flow itself is exercised elsewhere; here we assert the audit page can filter+surface it without page error). Keep the payment.process happy-path, refund filter, RBAC bartender-redirect, diff-sheet, and date-range tests. In CLAUDE.md's "## E2E Test Suite" section, add `38-audit-logs` to the tracked spec list and bump the "17 spec files" count.
  </action>
  <verify>
    <automated>grep -q "order.void" e2e/38-audit-logs.spec.ts && grep -q "38-audit-logs" CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: the combo.add_to_tab substitution NOTE + Test 2 are replaced by an `order.void` action-filter test using `#audit-filter-action` + "Apply filters".
    - source: CLAUDE.md's E2E Test Suite list includes `38-audit-logs` and the count is updated.
    - behavior: the grep verify passes.
  </acceptance_criteria>
  <done>D-07 satisfied: order.void coverage restored; spec tracked in CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase verification gate</name>
  <files>e2e/38-audit-logs.spec.ts</files>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-VALIDATION.md (Sampling Rate + phase gate: unit suite + lint per wave; E2E 38 green before verify)
    - src/shared/lib/__tests__/audit-actions.test.ts (the RPC-coverage test — all 10 targets must now be green post-wiring)
    - src/shared/lib/__tests__/audit-edge-coverage.test.ts (sensitive-edge allowlist coverage)
  </read_first>
  <action>
    Run the full gate and record results in the SUMMARY: (1) `npm run typecheck` exit 0; (2) `npm run lint` exit 0 (max-warnings 0); (3) `npm run test` — full unit suite green (the pre-existing useCloseTab.test.ts:95 failure documented since Phase 15 is the only accepted exception; note it explicitly if present); (4) `npx vitest run src/shared/lib/__tests__/audit-actions.test.ts` — ALL 10 target RPC coverage assertions now GREEN (the six previously-RED ones resolved by 14-03/04/05/09); (5) `npx vitest run src/shared/lib/__tests__/audit-edge-coverage.test.ts` green; (6) `npm run test:e2e -- e2e/38-audit-logs.spec.ts` green against live remote (requires `.env.local` E2E creds + the Task-1 push). If an E2E test surfaces a real app bug, fix the root cause rather than weakening the assertion; document each fix in the SUMMARY. Do NOT use `--no-verify` or watch-mode flags.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - behavior: typecheck + lint exit 0; full unit suite green (only the documented pre-existing useCloseTab failure allowed).
    - behavior: audit-actions RPC-coverage test has all 10 target RPCs GREEN; audit-edge-coverage green.
    - behavior: `npm run test:e2e -- e2e/38-audit-logs.spec.ts` passes against live remote (recorded in SUMMARY).
  </acceptance_criteria>
  <done>Phase 14 is provably complete: schema live, functions deployed, all audit tests + E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → remote Supabase | the push/deploy is where local intent becomes the live compliance system; unverified push = false-positive verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | audit_logs live append-only ledger | mitigate | push applies the RLS-bearing migrations; smoke probe + RLS denial test (14-13) confirm append-only live |
| T-14-16 | Repudiation | false-positive verification (wired locally, not live) | mitigate | BLOCKING push + smoke probe + live E2E gate — the phase cannot pass on local-only checks |
| T-14-SC | Tampering | supabase CLI push/deploy provenance | mitigate | push/deploy use the project's own supabase CLI + SUPABASE_ACCESS_TOKEN; no third-party artifacts; zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-14)
- Remote Supabase: all six 20260703* migrations (plus 000001/000002 if pending) applied; void-order/create-staff/settings-restore Edge Functions deployed
- `e2e/38-audit-logs.spec.ts` — order.void test restored (D-07)
- `CLAUDE.md` — `38-audit-logs` added to the tracked E2E spec list
- SUMMARY records the green verification gate (typecheck/lint/unit/coverage/E2E)
</artifacts_produced>

<verification>
- Migration list + functions list confirm remote state; smoke probe green.
- All audit unit/coverage tests + E2E 38 green; typecheck + lint clean.
</verification>

<success_criteria>
- Phase-14 schema + functions live on remote; order.void E2E restored + spec tracked; full gate green.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-14-SUMMARY.md` when done.
</output>
