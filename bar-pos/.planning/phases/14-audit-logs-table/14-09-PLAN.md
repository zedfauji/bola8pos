---
phase: 14-audit-logs-table
plan: 09
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - supabase/migrations/20260703000005_force_pin_change.sql
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "profiles.must_change_pin exists (boolean NOT NULL DEFAULT false), reconciling the drift where StaffSchema/mapStaffRow already read it but no migration created it"
    - "A force_pin_change SECURITY DEFINER RPC sets a target staff member's must_change_pin=true (manager+ gated) and records a 'permission.force_pin_change' audit row"
    - "A clear_must_change_pin SECURITY DEFINER RPC lets the affected staff member clear their own flag when setting a new PIN, and records an audit row"
  artifacts:
    - path: "supabase/migrations/20260703000005_force_pin_change.sql"
      provides: "must_change_pin column + force_pin_change RPC + clear_must_change_pin RPC"
      contains: "PERFORM record_audit('permission.force_pin_change'"
  key_links:
    - from: "force_pin_change"
      to: "record_audit"
      via: "PERFORM record_audit('permission.force_pin_change', ...)"
      pattern: "permission.force_pin_change"
---

<objective>
`force_pin_change` has ZERO implementation (RESEARCH.md Pitfall 5): no RPC, no UI, and the `must_change_pin` column it depends on has NO creating migration — yet `StaffSchema.mustChangePin` (non-nullable) and `mapStaffRow` already read it, meaning the column MUST exist live and this is drift to reconcile. Per D-01a, build the full backend: reconcile the column, add a manager+-gated `force_pin_change` RPC that flags a staff member and audits it, and a `clear_must_change_pin` RPC the staff member calls when setting a new PIN. (Frontend is 14-12.)

Purpose: SC3 coverage for the forced-PIN-change capability (net-new feature backend).
Output: One migration with the column + two SECURITY DEFINER RPCs.

Follow the 14-01 Task-3 outcome: "build fresh" vs "reconcile drift". Either way the migration MUST be idempotent (ADD COLUMN IF NOT EXISTS, CREATE OR REPLACE FUNCTION) so it is safe against a column/RPC that already exists live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-01-SUMMARY.md
@.planning/phases/14-audit-logs-table/14-RESEARCH.md
@src/shared/lib/domain.ts
@supabase/migrations/20260511000002_rpc_audit_wiring.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — reconcile profiles.must_change_pin + create force_pin_change RPC</name>
  <files>supabase/migrations/20260703000005_force_pin_change.sql</files>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-01-SUMMARY.md (Task 3 result — whether must_change_pin column / force_pin_change RPC already exist live; if a remote force_pin_change def was captured, this migration must supersede/match it to end the drift)
    - src/shared/lib/domain.ts (line ~272 — `mustChangePin: z.boolean()` non-nullable in StaffSchema, confirming the column must exist + default false)
    - src/entities/staff/model/queries.ts (line ~45 mapStaffRow reads row.must_change_pin → mustChangePin)
    - supabase/migrations/20260511000002_rpc_audit_wiring.sql (process_refund — the manager+ auth check `SELECT id INTO v_staff_id FROM profiles WHERE id = auth.uid() AND role IN ('manager','admin'); IF NOT FOUND THEN RAISE EXCEPTION 'AUTH_FORBIDDEN...'` — copy this guard shape; and the pre-RETURN PERFORM record_audit pattern)
  </read_first>
  <action>
    New migration `BEGIN; ... COMMIT;` + commented DOWN. First: `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS must_change_pin boolean NOT NULL DEFAULT false;` (idempotent — safe if the column already exists live). Then `CREATE OR REPLACE FUNCTION force_pin_change(p_staff_id uuid, p_terminal_id text DEFAULT NULL) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body: (1) manager+ gate — `SELECT id INTO v_caller FROM profiles WHERE id = auth.uid() AND role IN ('manager','admin'); IF NOT FOUND THEN RAISE EXCEPTION 'AUTH_FORBIDDEN: manager or admin role required';`. (2) capture `SELECT to_jsonb(p) INTO v_before FROM profiles p WHERE p.id = p_staff_id;` — `IF v_before IS NULL THEN RAISE EXCEPTION 'NOT_FOUND: staff % not found', p_staff_id;`. (3) `UPDATE profiles SET must_change_pin = true WHERE id = p_staff_id;`. (4) `SELECT to_jsonb(p) INTO v_after FROM profiles p WHERE p.id = p_staff_id;` then `PERFORM record_audit('permission.force_pin_change', 'staff', p_staff_id, v_before, v_after, 'rpc', p_terminal_id);` then `RETURN jsonb_build_object('ok', true);`. GRANT EXECUTE to authenticated. Use action label 'permission.force_pin_change' exactly (already enumerated). Note: this migration must be idempotent because 14-01 may have found the column/RPC already live (drift) — commit the authoritative definition here to end the drift.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-actions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: migration contains `ADD COLUMN IF NOT EXISTS must_change_pin boolean NOT NULL DEFAULT false`.
    - source: force_pin_change gates on `role IN ('manager','admin')`, raises NOT_FOUND for a missing staff, and calls `PERFORM record_audit('permission.force_pin_change', 'staff', p_staff_id, v_before, v_after, 'rpc', p_terminal_id)` on success only.
    - behavior: the audit-actions coverage test's force_pin_change assertion now PASSES.
  </acceptance_criteria>
  <done>Column reconciled + force_pin_change RPC flags staff and audits it. Migration not pushed (14-14).</done>
</task>

<task type="auto">
  <name>Task 2: Add clear_must_change_pin SECURITY DEFINER RPC (self-service PIN reset)</name>
  <files>supabase/migrations/20260703000005_force_pin_change.sql</files>
  <read_first>
    - the same migration file from Task 1 (append to it, single migration)
    - src/widgets/PINLoginForm/PINLoginForm.tsx (the login flow that will call this after a staff member sets a new PIN — the forced-PIN-change screen is built in 14-12; the RPC must clear the flag for the CURRENTLY-AUTHENTICATED staff, not an arbitrary id)
  </read_first>
  <action>
    Append to the same migration: `CREATE OR REPLACE FUNCTION clear_must_change_pin(p_terminal_id text DEFAULT NULL) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body: (1) `v_uid := auth.uid(); IF v_uid IS NULL THEN RAISE EXCEPTION 'AUTH_REQUIRED';`. (2) capture before via to_jsonb. (3) `UPDATE profiles SET must_change_pin = false WHERE id = v_uid;`. (4) `PERFORM record_audit('permission.force_pin_change', 'staff', v_uid, v_before, v_after, 'rpc', p_terminal_id);` (same action label — the diff before/after shows the flag clearing; the actor is the staff member themselves). RETURN jsonb_build_object('ok', true). GRANT EXECUTE to authenticated. IMPORTANT: this RPC only clears the flag for auth.uid() (self-service) — it must NOT accept a target staff id, to prevent one staff member clearing another's forced-change flag. The actual PIN value change (Supabase auth password update) is performed client-side in 14-12 via supabase.auth.updateUser; this RPC only clears the domain flag.
  </action>
  <verify>
    <automated>grep -q "clear_must_change_pin" supabase/migrations/20260703000005_force_pin_change.sql && grep -q "auth.uid()" supabase/migrations/20260703000005_force_pin_change.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: clear_must_change_pin updates `must_change_pin = false WHERE id = auth.uid()` only (no target-id param).
    - source: it records an audit row and RAISEs AUTH_REQUIRED when auth.uid() is null.
  </acceptance_criteria>
  <done>Staff can clear their own forced-PIN-change flag; the action is audited. Migration not pushed (14-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → force_pin_change RPC | untrusted p_staff_id — must enforce that only manager+ can flag another staff member |
| client → clear_must_change_pin RPC | self-service only — must clear the flag for auth.uid() and never a caller-supplied id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Elevation of Privilege | force_pin_change flagging arbitrary staff | mitigate | manager+ gate via profiles + auth.uid() before UPDATE |
| T-14-12 | Elevation of Privilege | clear_must_change_pin clearing another user's flag | mitigate | RPC clears WHERE id = auth.uid() only; no target-id parameter accepted |
| T-14-05 | Repudiation | forced-PIN-change with no trail | mitigate | PERFORM record_audit('permission.force_pin_change') on both set + clear paths |
| T-14-04 | Information Disclosure | audit failure | accept | record_audit swallows its own exceptions |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-09)
- `supabase/migrations/20260703000005_force_pin_change.sql`
- `profiles.must_change_pin` column (boolean NOT NULL DEFAULT false) — reconciled
- `force_pin_change(uuid, text)` SECURITY DEFINER RPC — manager+ gated, audits `permission.force_pin_change`
- `clear_must_change_pin(text)` SECURITY DEFINER RPC — self-service, audits `permission.force_pin_change`
</artifacts_produced>

<verification>
- audit-actions coverage test advances (force_pin_change green).
- Migration is idempotent (safe against pre-existing live column/RPC per 14-01).
</verification>

<success_criteria>
- must_change_pin column reconciled; force_pin_change (manager+) + clear_must_change_pin (self) RPCs exist and audit.
- Neither RPC allows cross-staff flag manipulation beyond its intended authority.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-09-SUMMARY.md` when done.
</output>
