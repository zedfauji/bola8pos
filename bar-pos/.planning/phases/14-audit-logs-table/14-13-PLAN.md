---
phase: 14-audit-logs-table
plan: 13
type: execute
wave: 3
depends_on: ["14-10", "14-11"]
files_modified:
  - src/pages/audit/index.tsx
  - src/app/router.tsx
  - src/entities/audit-log/model/rls-denial.integration.test.ts
autonomous: true
requirements: [SC2, SC6, SC7]
must_haves:
  truths:
    - "/audit is a registered route (manager+ gated via ProtectedRoute + AuditRoute) rendering a thin page that composes the AuditLogTable widget under an 'Audit Log' heading"
    - "The page heading matches the E2E's getByRole('heading', { name: 'Audit Log' })"
    - "An integration test proves UPDATE and DELETE on audit_logs are denied for an authenticated non-service-role user (append-only RLS, SC2)"
  artifacts:
    - path: "src/pages/audit/index.tsx"
      provides: "thin /audit page container with 'Audit Log' heading + AuditLogTable"
      contains: "Audit Log"
    - path: "src/app/router.tsx"
      provides: "/audit route wrapped in ProtectedRoute + AuditRoute"
      contains: "AuditRoute"
  key_links:
    - from: "src/app/router.tsx"
      to: "AuditRoute + AuditPage"
      via: "lazy import + ProtectedRoute>AuditRoute>AuditPage"
      pattern: "path=\"/audit\""
    - from: "pages/audit"
      to: "AuditLogTable"
      via: "widget composition"
      pattern: "AuditLogTable"
---

<objective>
Wire the `/audit` page: a thin FSD page container that renders the `AuditLogTable` widget (14-11) under an "Audit Log" heading, registered as a route guarded by `ProtectedRoute` + `AuditRoute` (14-10) — matching the exact `/rbac` route shape. Add the append-only RLS denial integration test (SC2) that the phase's compliance guarantee depends on.

Purpose: SC6/SC7 (page + route) + SC2 (append-only RLS proven).
Output: page + router entry + RLS integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-UI-SPEC.md
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@src/app/router.tsx
@src/pages/reports/index.tsx
@e2e/38-audit-logs.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: pages/audit/index.tsx — thin container with 'Audit Log' heading + AuditLogTable</name>
  <files>src/pages/audit/index.tsx</files>
  <read_first>
    - src/pages/reports/index.tsx (the page-shape analog — container wrapper classes, SectionHeader/BackToHomeButton usage, thin composition, no logic in the page)
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (section A — `mx-auto max-w-6xl space-y-8` container matching StaffDashboard; SectionHeader heading = "Audit Log" (24px semibold), then `<AuditLogTable />`; no logic in the page)
    - src/widgets/AuditLogTable/index.ts (the AuditLogTable export from 14-11)
    - e2e/38-audit-logs.spec.ts (line 75 etc. — getByRole('heading', { name: 'Audit Log' }) must resolve)
  </read_first>
  <action>
    Create `src/pages/audit/index.tsx` as a thin container (default export, matching the lazy-import convention of other pages). Render a container `<div className="mx-auto max-w-6xl space-y-8">` (matching StaffDashboard/UI-SPEC), a heading rendered as an actual heading element (so `getByRole('heading', { name: 'Audit Log' })` resolves) — reuse the same SectionHeader component the reports/staff pages use with title "Audit Log", plus BackToHomeButton if the sibling pages include one — then `<AuditLogTable />`. No data-fetching or filter logic in the page (all of that lives in the widget). Import AuditLogTable from `@widgets/AuditLogTable`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: pages/audit/index.tsx renders an "Audit Log" heading (heading role) and `<AuditLogTable />` with no page-level logic.
    - source: default export present (lazy-importable).
    - behavior: typecheck + lint (max-warnings 0) pass.
  </acceptance_criteria>
  <done>/audit page container exists and composes the widget under the E2E-required heading.</done>
</task>

<task type="auto">
  <name>Task 2: Register the /audit route (ProtectedRoute + AuditRoute + lazy AuditPage)</name>
  <files>src/app/router.tsx</files>
  <read_first>
    - src/app/router.tsx (full — the lazy page imports (lines 11-25), the route-guard imports (lines 6-9: RbacRoute/ReportsRoute/etc.), and the `/rbac` route block (lines 155-164) which is the exact shape to copy)
    - src/app/audit-route.tsx (the AuditRoute export from 14-10)
    - .planning/phases/14-audit-logs-table/14-PATTERNS.md (router.tsx MODIFY — copy the /rbac block: lazy import + ProtectedRoute wrapping the guard wrapping the page)
  </read_first>
  <action>
    Add `import { AuditRoute } from './audit-route';` alongside the other route-guard imports (lines 6-9). Add `const AuditPage = lazy(() => import('../pages/audit'));` alongside the other lazy page consts. Add a new `<Route path="/audit" element={<ProtectedRoute><AuditRoute><AuditPage /></AuditRoute></ProtectedRoute>} />` inside `<Routes>`, mirroring the `/rbac` block exactly. Do not touch any other route. Also update the routes table in CLAUDE.md is NOT required here (that is handled in 14-14 alongside the E2E spec-list update) — keep this task to router.tsx only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - source: router.tsx imports AuditRoute + lazily imports AuditPage and registers `<Route path="/audit" element={<ProtectedRoute><AuditRoute><AuditPage/></AuditRoute></ProtectedRoute>} />`.
    - behavior: `npm run build` (tsc + vite) succeeds — the lazy import resolves.
  </acceptance_criteria>
  <done>/audit is reachable, manager+ gated end to end (ProtectedRoute + AuditRoute).</done>
</task>

<task type="auto">
  <name>Task 3: Append-only RLS denial integration test (SC2)</name>
  <files>src/entities/audit-log/model/rls-denial.integration.test.ts</files>
  <read_first>
    - supabase/migrations/20260511000001_audit_logs_table.sql (RLS: SELECT manager+, INSERT authenticated, NO UPDATE/DELETE policy → append-only by omission)
    - src/entities/tab/model/depletion.integration.test.ts (the repo's integration-test convention: anon client via signInWithPassword, `describe.skipIf(!hasEnv)` graceful-skip when E2E creds absent, itInt/itAuth aliases)
    - .planning/phases/14-audit-logs-table/14-VALIDATION.md (Wave 0 item: RLS denial on UPDATE/DELETE against audit_logs as an authenticated non-service-role user)
  </read_first>
  <action>
    Create an integration test that, using an ANON client authenticated via signInWithPassword as a manager (so SELECT is permitted but UPDATE/DELETE are not), attempts (a) `supabase.from('audit_logs').update({ action: 'tampered' }).eq('id', <some id>)` and asserts it affects 0 rows / returns an RLS error, and (b) `supabase.from('audit_logs').delete().eq('id', <some id>)` and asserts the same. Guard the whole suite with `describe.skipIf(!hasEnv)` using the same env-detection helper the other integration tests use (SUPABASE_SERVICE_ROLE_KEY / E2E creds), so CI without live creds skips gracefully. Seed a row to target via the service-role client (or record_audit) inside the test setup so there is a real id to attempt tampering on; assert the row still exists + is unchanged afterward. This test requires the migration to be LIVE — it depends on 14-01 having confirmed audit_logs is deployed (or 14-14 having pushed it); note that dependency in a comment.
  </action>
  <verify>
    <automated>npx vitest run src/entities/audit-log/model/rls-denial.integration.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: test attempts UPDATE and DELETE on audit_logs as an authenticated manager and asserts both are denied (0 rows affected / RLS error) and the target row is unchanged.
    - behavior: suite uses `describe.skipIf(!hasEnv)` — skips (not fails) when live creds are absent; passes when creds are present and the migration is live.
  </acceptance_criteria>
  <done>Append-only RLS (SC2) is proven by an automated integration test (skippable without live creds).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → audit_logs write paths | append-only enforced by RLS (no UPDATE/DELETE policy); this plan proves it |
| client → /audit route | ProtectedRoute (auth) + AuditRoute (manager+) gate access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | audit_logs post-write modification | mitigate | append-only RLS (no UPDATE/DELETE policy); proven by the Task 3 integration test |
| T-14-03 | Elevation of Privilege | unauthorized /audit access | mitigate | ProtectedRoute + AuditRoute + DB SELECT RLS (manager+) — three layers |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-13)
- `src/pages/audit/index.tsx` — thin /audit page (default export)
- `/audit` `<Route>` in `src/app/router.tsx` (ProtectedRoute + AuditRoute + lazy AuditPage) + `AuditRoute`/`AuditPage` imports
- `src/entities/audit-log/model/rls-denial.integration.test.ts` — append-only RLS denial test
</artifacts_produced>

<verification>
- `npm run build` succeeds (route + page resolve); typecheck + lint clean.
- RLS denial integration test passes with live creds (skips gracefully without).
</verification>

<success_criteria>
- /audit reachable + manager+ gated, renders AuditLogTable under "Audit Log" heading.
- Append-only RLS proven by integration test.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-13-SUMMARY.md` when done.
</output>
