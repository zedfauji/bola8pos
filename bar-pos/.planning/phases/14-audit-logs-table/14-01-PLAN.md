---
phase: 14-audit-logs-table
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [SC1, SC2]
must_haves:
  truths:
    - "Remote Supabase confirmed to contain the audit_logs table + record_audit() function + the 4-RPC wiring, OR the gap is documented and the db-push plan (14-14) will apply migration 20260511000001/000002"
    - "The live existence (or absence) of the /functions/v1/void-order Edge Function is known and recorded"
    - "The live existence (or absence) of profiles.must_change_pin column + any remote force_pin_change RPC is known and recorded"
  artifacts: []
  key_links:
    - from: "record_audit()"
      to: "audit_logs"
      via: "SECURITY DEFINER INSERT"
      pattern: "record_audit"
---

<objective>
Resolve the three BLOCKING live-remote unknowns that gate all downstream Phase 14 work. Nothing in this phase can be verified as complete until we know the true state of the deployed Supabase project, because build/type checks pass without the schema being live (types come from `supabase.types.ts`, not the live DB — see RESEARCH.md Pitfall 1).

Purpose: Prevent a false-positive verification state where wiring "passes" locally but every `record_audit` call fails on remote with `function record_audit does not exist`, and `/audit` returns empty because `audit_logs` was never pushed.
Output: Three recorded verification results (documented in the plan SUMMARY) that set the execution path for 14-07 (void-order build-vs-recover) and 14-09 (force_pin_change build-vs-reconcile).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-RESEARCH.md
@.planning/phases/14-audit-logs-table/14-VALIDATION.md
@supabase/migrations/20260511000001_audit_logs_table.sql
@supabase/migrations/20260511000002_rpc_audit_wiring.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify audit_logs table + record_audit() + 4-RPC wiring are live on remote Supabase</name>
  <files>none (verification only — no files modified)</files>
  <read_first>
    - supabase/migrations/20260511000001_audit_logs_table.sql (the table + record_audit definition whose push status is unverified)
    - supabase/migrations/20260511000002_rpc_audit_wiring.sql (the 4 wired RPCs)
    - .planning/phases/14-audit-logs-table/14-RESEARCH.md (Pitfall 1 — why type presence ≠ schema presence)
  </read_first>
  <action>
    Claude automates the probe first (automation-first): run `supabase migration list` (with SUPABASE_ACCESS_TOKEN set for non-TTY) and capture whether 20260511000001 + 20260511000002 appear applied on remote; then run a live query via the Supabase MCP execute_sql tool or psql: `SELECT to_regclass('public.audit_logs') IS NOT NULL AS table_live, EXISTS(SELECT 1 FROM pg_proc WHERE proname='record_audit') AS fn_live;`. Record both outputs in the SUMMARY under "Remote state: audit_logs". If either is `no`, record "14-14 db push MUST apply 20260511000001 + 20260511000002". Then present to the human for confirmation.
  </action>
  <verify>
    <automated>node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" query state.load >/dev/null 2>&1; echo "manual/integration probe — see how-to-verify; live query result recorded in SUMMARY"</automated>
  </verify>
  <what-built>No code built yet — this is a live-state probe. Claude automates the probe first, then presents the result for human confirmation.</what-built>
  <how-to-verify>
    1. `supabase migration list` against remote — check whether 20260511000001/000002 are applied.
    2. Live probe: `SELECT to_regclass('public.audit_logs') IS NOT NULL, EXISTS(SELECT 1 FROM pg_proc WHERE proname='record_audit');`.
    3. Record both in the SUMMARY.
    4. Human confirms whether migrations 000001/000002 are live; if NOT, record that 14-14's push must include them.
  </how-to-verify>
  <acceptance_criteria>
    - source: SUMMARY.md contains "audit_logs table live: yes|no", "record_audit() live: yes|no", "migrations 000001/000002 applied: yes|no", each backed by captured command output.
    - behavior: If either is `no`, the SUMMARY records "14-14 db push MUST apply 20260511000001 + 20260511000002".
  </acceptance_criteria>
  <done>Live status of audit_logs, record_audit(), and migrations 000001/000002 recorded + human-confirmed.</done>
  <resume-signal>Type "confirmed live", "confirmed missing — push in 14-14", or paste the probe output.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify whether the /functions/v1/void-order Edge Function exists live (build vs recover decision)</name>
  <files>none (verification only — no files modified)</files>
  <read_first>
    - src/shared/lib/edge-function-contracts.ts (VoidOrderRequestSchema/VoidOrderResponseSchema + callVoidOrder — the contract; fetch of `/functions/v1/void-order`)
    - src/features/void-order/model/useVoidOrder.ts (the only caller of callVoidOrder)
    - .planning/phases/14-audit-logs-table/14-RESEARCH.md (Pitfall 2 + Open Question 2 — no void-order dir exists in supabase/functions/ and none was ever committed)
  </read_first>
  <action>
    Claude automates first: run `supabase functions list` against remote and check for `void-order`; if reachable, issue a probe request to `/functions/v1/void-order` with an invalid body and record the HTTP status (404 = not deployed; 400/401/422 = deployed). Record the result in SUMMARY under "Remote state: void-order edge fn" and the chosen 14-07 path (build fresh vs recover+patch). Present to the human for confirmation.
  </action>
  <verify>
    <automated>echo "supabase functions list + endpoint probe — result recorded in SUMMARY (build-vs-recover path for 14-07)"</automated>
  </verify>
  <what-built>No code — a deployed-artifact probe. The repo has NO `supabase/functions/void-order/` directory (only 11 other functions exist).</what-built>
  <how-to-verify>
    1. `supabase functions list` — does `void-order` appear?
    2. If reachable, probe `/functions/v1/void-order` with an invalid body; record HTTP status.
    3. Record in SUMMARY + choose the 14-07 path: (a) not deployed → author from scratch with recordAudit; (b) deployed → recover source, commit, add recordAudit.
  </how-to-verify>
  <acceptance_criteria>
    - source: SUMMARY.md contains "void-order edge fn deployed: yes|no" backed by functions-list output and/or probe HTTP status.
    - behavior: SUMMARY records the chosen 14-07 path ("build from scratch" | "recover + patch").
  </acceptance_criteria>
  <done>void-order deployment status recorded + 14-07 execution path chosen.</done>
  <resume-signal>Type "not deployed — build fresh", "deployed — recover + patch", or paste `supabase functions list` output.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify whether profiles.must_change_pin column + any remote force_pin_change RPC already exist live</name>
  <files>none (verification only — no files modified)</files>
  <read_first>
    - src/entities/staff/model/queries.ts (line ~45 mapStaffRow reads row.must_change_pin → mustChangePin)
    - src/shared/lib/domain.ts (StaffSchema.mustChangePin: z.boolean() — line ~272, non-nullable, so the column MUST exist + default false)
    - .planning/phases/14-audit-logs-table/14-RESEARCH.md (Pitfall 5 — grep of migrations for must_change_pin returns ZERO hits; STATE.md notes a remote force_pin_change migration in drift, absent from the repo)
  </read_first>
  <action>
    Claude automates first: run `SELECT EXISTS(SELECT 1 FROM information_schema.columns WHERE table_name='profiles' AND column_name='must_change_pin') AS col_live, EXISTS(SELECT 1 FROM pg_proc WHERE proname='force_pin_change') AS fn_live;`. If force_pin_change exists live, capture `SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname='force_pin_change';`. Record results + the chosen 14-09 path (build fresh vs reconcile drift) in the SUMMARY. Present to the human for confirmation.
  </action>
  <verify>
    <automated>echo "information_schema + pg_proc probe — column/RPC live status + captured def recorded in SUMMARY (14-09 path)"</automated>
  </verify>
  <what-built>No code — a live schema/function probe. must_change_pin + force_pin_change have NO local migration; a force_pin_change migration was reportedly applied to remote but its .sql is absent from the repo (drift).</what-built>
  <how-to-verify>
    1. Probe column + function existence via information_schema.columns + pg_proc.
    2. If force_pin_change exists live, capture its definition via pg_get_functiondef.
    3. Record in SUMMARY + choose 14-09 path: (a) neither exists → build fresh; (b) exists → idempotent reconcile + commit recovered def to end drift.
  </how-to-verify>
  <acceptance_criteria>
    - source: SUMMARY.md contains "profiles.must_change_pin live: yes|no" and "force_pin_change RPC live: yes|no (+ captured signature if yes)".
    - behavior: SUMMARY records the chosen 14-09 path ("build fresh" | "reconcile drift").
  </acceptance_criteria>
  <done>Live status of must_change_pin + force_pin_change recorded + 14-09 execution path chosen.</done>
  <resume-signal>Type "neither exists — build fresh", "exists — reconcile drift", or paste the probe output + captured function def.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo assumptions → live remote schema | The repo is NOT a reliable proxy for deployed schema in this project (manual `supabase.types.ts` edits + out-of-band deploys create drift) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | audit_logs append-only ledger | mitigate | This plan verifies the append-only RLS-bearing migration (000001) is actually live; if not, 14-14 pushes it before the ledger is relied upon |
| T-14-04 | Information Disclosure | record_audit() failure semantics | accept | record_audit catches its own exceptions + returns NULL (verified in migration 000001 body); no internal error leaks to callers — no change |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Phase installs ZERO new packages (RESEARCH.md Package Legitimacy Audit = N/A); no supply-chain surface added |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-01)
- No source artifacts. Produces three recorded remote-state facts in `14-01-SUMMARY.md`:
  - `audit_logs`/`record_audit` live status + migration-applied status
  - `void-order` edge function deployed status + chosen 14-07 path
  - `profiles.must_change_pin` + `force_pin_change` live status + chosen 14-09 path
</artifacts_produced>

<verification>
- All three checkpoints resolved with recorded command output in the SUMMARY.
- Downstream execution paths for 14-07 and 14-09 are unambiguous.
- If migrations 000001/000002 are NOT live, 14-14's push list is updated to include them.
</verification>

<success_criteria>
- SUMMARY.md documents live status for: audit_logs table, record_audit(), migrations 000001/000002, void-order edge fn, profiles.must_change_pin, force_pin_change RPC.
- No downstream plan proceeds on an unverified assumption about remote state.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-01-SUMMARY.md` when done.
</output>
