---
phase: 14-audit-logs-table
plan: 05
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - supabase/migrations/20260703000004_close_tab_rpc.sql
  - src/entities/tab/model/queries.ts
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "A tab status transition via useMutationUpdateTabStatus goes through a new close_tab SECURITY DEFINER RPC that records a 'tab.close' audit row and PRESERVES the Phase-15 optimistic-concurrency guard (p_expected_version → P0V01 STALE_VERSION on mismatch)"
    - "The client still surfaces STALE_VERSION via handleVersionError (invalidate + toast + audit) exactly as before — no Phase-15 regression"
  artifacts:
    - path: "supabase/migrations/20260703000004_close_tab_rpc.sql"
      provides: "close_tab SECURITY DEFINER RPC with version guard + record_audit"
      contains: "PERFORM record_audit('tab.close'"
  key_links:
    - from: "useMutationUpdateTabStatus"
      to: "close_tab"
      via: "supabase.rpc('close_tab', {p_expected_version})"
      pattern: "rpc\\('close_tab'"
---

<objective>
`close_tab` is not an RPC today — tab status transitions happen via `useMutationUpdateTabStatus`'s direct `.from('tabs').update({status, version}).eq('version', expected)` path, which Phase 15 hardened with optimistic concurrency (RESEARCH.md Pitfall 6 + the explicit Phase-15-regression warning). Per D-01a, build a real `close_tab` SECURITY DEFINER RPC that performs the version-guarded status UPDATE, raises the canonical `P0V01` STALE_VERSION on mismatch, and calls `record_audit('tab.close')` — then rewire the hook to call it WITHOUT dropping the concurrency contract.

Purpose: SC3 coverage for tab status transitions while preserving Phase 15's version guard (the single highest-risk item in this phase).
Output: One migration + one hook rewire.

NOTE TO USER (RESEARCH.md Open Question 1): this wires the manual `useMutationUpdateTabStatus` status-change path. The auto-close-on-full-payment path is already audited as `payment.process` by `process_payment_atomic`. This plan audits manual status transitions under `tab.close`. If you intended distinct labels per target status (e.g. `tab.void` for a void transition), flag during review — default here is a single `tab.close` label for the manual path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@supabase/migrations/20260512000002_rpc_versioned_group_a.sql
@supabase/migrations/20260512000001_versioned_rows.sql
@src/entities/tab/model/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — create close_tab SECURITY DEFINER RPC with p_expected_version guard + 'tab.close' audit</name>
  <files>supabase/migrations/20260703000004_close_tab_rpc.sql</files>
  <read_first>
    - src/entities/tab/model/queries.ts (lines 752-867 useMutationUpdateTabStatus — the exact behavior to replicate: reads cached version, writes `version = expected + 1`, `.eq('version', expected)`, PGRST116 → staleVersionError; there is also a no-cached-version fallback path with plain UPDATE)
    - supabase/migrations/20260512000002_rpc_versioned_group_a.sql (the canonical Group-A version guard: FOR UPDATE, raise P0V01 STALE_VERSION when the row's version <> p_expected_version, raise P0V02 NOT_FOUND_VERSIONED when the row is missing, bump version = version + 1 on success — copy this shape)
    - supabase/migrations/20260512000001_versioned_rows.sql (bump_version_on_update trigger raising P0V01 — understand how version is enforced app-wide so the RPC does not double-bump)
    - supabase/migrations/20260511000002_rpc_audit_wiring.sql (close_caja_session — before/after to_jsonb capture pattern)
  </read_first>
  <action>
    New migration `BEGIN; ... COMMIT;` + commented DOWN. Create `close_tab(p_tab_id uuid, p_status tab_status, p_expected_version int DEFAULT NULL, p_terminal_id text DEFAULT NULL) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body:
    1. `SELECT to_jsonb(t), t.version INTO v_before, v_current_version FROM tabs t WHERE t.id = p_tab_id FOR UPDATE;` — `IF NOT FOUND THEN RAISE EXCEPTION USING ERRCODE = 'P0V02', MESSAGE = 'NOT_FOUND_VERSIONED';` (matches Group-A P0V02).
    2. Version guard: `IF p_expected_version IS NOT NULL AND v_current_version IS DISTINCT FROM p_expected_version THEN RAISE EXCEPTION USING ERRCODE = 'P0V01', MESSAGE = 'STALE_VERSION';` (matches Group-A P0V01; NULL expected version skips the check, mirroring the hook's fallback path).
    3. IMPORTANT — do NOT manually set version in the UPDATE if the `bump_version_on_update` trigger already increments it on every UPDATE (confirm from versioned_rows migration). If the trigger auto-bumps, `UPDATE tabs SET status = p_status WHERE id = p_tab_id;` is sufficient and the trigger handles version+1. If there is NO auto-bump trigger for arbitrary updates (only trigger-on-specific-columns), set `version = v_current_version + 1` explicitly to match the hook's prior behavior. Read versioned_rows to decide and state the decision in a SQL comment.
    4. `SELECT to_jsonb(t) INTO v_after FROM tabs t WHERE t.id = p_tab_id;` then `PERFORM record_audit('tab.close', 'tab', p_tab_id, v_before, v_after, 'rpc', p_terminal_id);` then `RETURN jsonb_build_object('ok', true);`. Do NOT wrap P0V01/P0V02 in a WHEN OTHERS handler that swallows them — they must propagate to the client as SQLSTATE errors (parseSupabaseError maps P0V01→STALE_VERSION, P0V02→NOT_FOUND_VERSIONED per Phase 15-01). GRANT EXECUTE to authenticated. Use exact action label 'tab.close'.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-actions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: close_tab RAISEs `ERRCODE = 'P0V01'` on version mismatch and `ERRCODE = 'P0V02'` on missing row, and neither is caught/swallowed.
    - source: `PERFORM record_audit('tab.close', 'tab', p_tab_id, v_before, v_after, 'rpc', p_terminal_id)` present on the success path only.
    - source: a SQL comment states whether version is bumped by trigger or explicitly (based on reading versioned_rows), avoiding double-increment.
    - behavior: the audit-actions coverage test's close_tab assertion now PASSES.
  </acceptance_criteria>
  <done>close_tab RPC exists with Phase-15-parity version guard + atomic tab.close audit. Migration not pushed (14-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire useMutationUpdateTabStatus to call close_tab, preserving the version-conflict path</name>
  <files>src/entities/tab/model/queries.ts</files>
  <behavior>
    - When a cached version exists, the hook calls `rpc('close_tab', { p_tab_id, p_status: status, p_expected_version: expected, p_terminal_id })`; a P0V01 response is mapped to STALE_VERSION and handled by the existing handleVersionError path (invalidate + toast + record_audit), and optimistic state is rolled back — identical observable behavior to the pre-RPC path.
    - When no cached version exists, the hook calls close_tab with `p_expected_version: null` (RPC skips the guard).
    - onMutate optimistic update, onSuccess invalidations, and onError rollback are unchanged.
  </behavior>
  <read_first>
    - src/entities/tab/model/queries.ts (lines 745-867 — the full useMutationUpdateTabStatus incl. UpdateTabStatusContext, onMutate optimistic update, onSuccess handleVersionError call with entity 'tabs' + expectedVersion + terminalId: TERMINAL_ID, onError rollback)
    - src/shared/lib/version-error.ts (handleVersionError + staleVersionError — how STALE_VERSION is surfaced; the hook already imports these)
    - src/shared/lib/result.ts (parseSupabaseError mapping P0V01→STALE_VERSION — confirm the RPC error flows through the same mapping the hook already relies on)
  </read_first>
  <action>
    In the mutationFn, replace BOTH branches (the version-guarded `.from('tabs').update(...).eq('version', expected)` branch AND the no-version fallback `.from('tabs').update({status})` branch) with a single `supabase.rpc('close_tab', { p_tab_id: tabId, p_status: status, p_expected_version: expected ?? null, p_terminal_id: TERMINAL_ID })` call. `expected` is still read from `queryClient.getQueryData<Result<Tab>>(tabKeys.detail(tabId))` as today. The RPC returns `{ ok: true }` on success — on RPC error, map it with the existing `staleVersionError`/`parseSupabaseError` logic: if the error's code/SQLSTATE is P0V01 → `return err(staleVersionError(error))` (same as the old PGRST116 branch); other errors → `return err(unknownError(error))` with the existing logger.error. On success, re-fetch or map the tab: since close_tab returns only `{ok:true}` (not the row), fetch the updated row via the existing tab detail query invalidation OR change the RPC to also return the row — simplest: keep returning `{ok:true}` and, on success, construct the optimistic Tab from cached data + new status (the onSuccess already invalidates tabKeys.detail which refetches authoritative state). Keep TERMINAL_ID (already used by handleVersionError in this file). Leave onMutate/onSuccess/onError bodies unchanged — the STALE_VERSION handling in onSuccess already fires handleVersionError for `!result.ok`.
  </action>
  <verify>
    <automated>npx vitest run src/entities/tab/model/queries.concurrent.test.ts --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: mutationFn calls `supabase.rpc('close_tab', { p_tab_id, p_status, p_expected_version, p_terminal_id })` and no longer calls `.from('tabs').update(...)`.
    - behavior: a P0V01 RPC error maps to STALE_VERSION and flows through handleVersionError (existing concurrent tests remain green).
    - behavior: `npm run typecheck` exits 0; no Phase-15 concurrency test regresses.
  </acceptance_criteria>
  <done>Tab status transitions audited as tab.close with terminal_id; Phase-15 optimistic-concurrency contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → close_tab RPC | untrusted p_status + p_expected_version cross into SECURITY DEFINER; version guard prevents lost-update tampering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Repudiation | manual tab status transition | mitigate | PERFORM record_audit('tab.close') with before/after + terminal_id on success |
| T-14-08 | Tampering | concurrent lost-update on tab status (regressing Phase 15) | mitigate | close_tab enforces p_expected_version → P0V01; RPC never swallows P0V01/P0V02; client maps via existing parseSupabaseError |
| T-14-04 | Information Disclosure | audit failure | accept | record_audit swallows its own exceptions returning NULL |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-05)
- `supabase/migrations/20260703000004_close_tab_rpc.sql`
- `close_tab(uuid, tab_status, int, text)` SECURITY DEFINER RPC — version-guarded (P0V01/P0V02), audits `tab.close`
- Rewired `useMutationUpdateTabStatus` (calls `close_tab`, preserves STALE_VERSION handling)
</artifacts_produced>

<verification>
- audit-actions coverage test advances (close_tab green).
- Phase-15 concurrent-edit tests (queries.concurrent.test.ts) remain green; `npm run typecheck` exits 0.
</verification>

<success_criteria>
- close_tab performs version-guarded status UPDATE + atomic tab.close audit; P0V01/P0V02 propagate.
- Hook rewired with zero observable change to the version-conflict UX.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-05-SUMMARY.md` when done.
</output>
