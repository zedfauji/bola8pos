---
phase: 14-audit-logs-table
plan: 10
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/shared/lib/rbac.ts
  - src/app/audit-route.tsx
  - src/app/audit-route.test.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - supabase/migrations/20260703000006_role_permissions_view_audit_log.sql
autonomous: true
requirements: [SC7]
must_haves:
  truths:
    - "A 'view_audit_log' StaffAction exists, granted to manager + admin, seeded into role_permissions"
    - "AuditRoute redirects non-manager+ users to /home AND fires a toast 'This page is restricted to managers and admins.'"
    - "The existing HomeDashboard /audit tile uses requiredAction: 'view_audit_log' (consistent with every other gated tile) instead of the inconsistent visibleToRoles list"
  artifacts:
    - path: "src/app/audit-route.tsx"
      provides: "AuditRoute guard with toast-on-redirect"
      exports: ["AuditRoute"]
    - path: "src/shared/lib/rbac.ts"
      provides: "'view_audit_log' StaffAction in STAFF_ACTIONS + MANAGER_EXTRA"
      contains: "view_audit_log"
  key_links:
    - from: "AuditRoute"
      to: "usePermissions().can"
      via: "can('view_audit_log')"
      pattern: "view_audit_log"
    - from: "HomeDashboard /audit tile"
      to: "view_audit_log"
      via: "requiredAction"
      pattern: "requiredAction: 'view_audit_log'"
---

<objective>
Build the RBAC + route-guard foundation for `/audit`: add a `view_audit_log` StaffAction (manager+), an `AuditRoute` guard that (uniquely among the app's guards) shows a toast on redirect per the E2E contract, and fix the HomeDashboard `/audit` tile's inconsistent `visibleToRoles` gating to use `requiredAction` like every other manager+ tile (RESEARCH.md Pitfall 8). Seed the new permission into `role_permissions` (Phase-13 DB-parity).

Purpose: SC7 (AuditRoute + gating) — the enforcement + navigation shell the /audit page (14-13) plugs into.
Output: rbac action + guard + tile fix + seed migration + guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@.planning/phases/14-audit-logs-table/14-UI-SPEC.md
@src/app/reports-route.tsx
@src/shared/lib/rbac.ts
@src/widgets/HomeDashboard/ui/HomeDashboard.tsx
@e2e/38-audit-logs.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'view_audit_log' StaffAction + role_permissions seed migration</name>
  <files>src/shared/lib/rbac.ts, supabase/migrations/20260703000006_role_permissions_view_audit_log.sql</files>
  <read_first>
    - src/shared/lib/rbac.ts (full — STAFF_ACTIONS const array (lines 13-36), MANAGER_EXTRA set (lines 52-62), ADMIN inherits MANAGER via `ADMIN_ACTIONS = new Set([...MANAGER_ACTIONS, ...ADMIN_EXTRA])`)
    - supabase/migrations/20260510000001_rls_rewrite_phase13.sql (lines 283-320 — the `INSERT INTO role_permissions (role, action) VALUES (...)` seed shape for manager + admin rows; confirm the unique constraint columns used for ON CONFLICT)
    - .planning/phases/14-audit-logs-table/14-PATTERNS.md (rbac.ts MODIFY section — add to STAFF_ACTIONS + MANAGER_EXTRA)
  </read_first>
  <action>
    In rbac.ts: append `'view_audit_log',` to the `STAFF_ACTIONS` const array (end of the list). Add `'view_audit_log',` to the `MANAGER_EXTRA` set (admin inherits it automatically via ADMIN_ACTIONS spreading MANAGER_ACTIONS). Do NOT add it to BARTENDER_ACTIONS or KITCHEN_ACTIONS. Create the seed migration `20260703000006_role_permissions_view_audit_log.sql` wrapped in `BEGIN; ... COMMIT;` + commented DOWN: `INSERT INTO role_permissions (role, action) VALUES ('manager', 'view_audit_log'), ('admin', 'view_audit_log') ON CONFLICT (role, action) DO NOTHING;` (use the same conflict target as the Phase-13 seed). This keeps client RBAC (rbac.ts) and DB role_permissions in parity per Phase 13.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/rbac.test.ts --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: STAFF_ACTIONS includes 'view_audit_log'; MANAGER_EXTRA contains 'view_audit_log'; bartender + kitchen do NOT.
    - source: migration inserts ('manager','view_audit_log') + ('admin','view_audit_log') with ON CONFLICT DO NOTHING.
    - behavior: rbac.test.ts + `npm run typecheck` pass (canAccess('manager','view_audit_log') === true, canAccess('bartender','view_audit_log') === false).
  </acceptance_criteria>
  <done>view_audit_log action defined + granted manager+; DB seed migration written (pushed in 14-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuditRoute guard with toast-on-redirect + RTL test</name>
  <files>src/app/audit-route.tsx, src/app/audit-route.test.tsx</files>
  <behavior>
    - A staff member WITH view_audit_log renders children.
    - A staff member WITHOUT view_audit_log gets `<Navigate to="/home" replace />` AND a `toast.error('This page is restricted to managers and admins.')` is fired.
  </behavior>
  <read_first>
    - src/app/reports-route.tsx (the exact guard shape to copy: usePermissions().can(...) → Navigate to /home replace)
    - src/app/rbac-route.tsx (second identical analog)
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (section E — AuditRoute must fire `toast.error('This page is restricted to managers and admins.')` before Navigate; copy tone)
    - e2e/38-audit-logs.spec.ts (lines 178-187 — the RBAC test expects redirect to /home AND getByText(/restricted to managers and admins/i) visible for ≥8s)
    - src/features/process-refund/ui/RefundSheet.tsx (line 8 — `import { toast } from 'sonner';` convention)
  </read_first>
  <action>
    Create `src/app/audit-route.tsx` copying ReportsRoute's structure: `export function AuditRoute({ children }: { children: ReactNode })`, `const { can } = usePermissions();`, and `if (!can('view_audit_log')) { toast.error('This page is restricted to managers and admins.'); return <Navigate to="/home" replace />; }` then `return <>{children}</>`. Import `toast` from 'sonner'. This is the ONE guard that shows a toast (the copy string must match the E2E regex `/restricted to managers and admins/i` exactly). Create `src/app/audit-route.test.tsx` (RTL): render AuditRoute inside a MemoryRouter with children; mock usePermissions to return `can: () => true` (asserts children render) and `can: () => false` (asserts Navigate + that the mocked sonner `toast.error` was called with the exact string). Mock 'sonner' and '@entities/staff/model/usePermissions'.
  </action>
  <verify>
    <automated>npx vitest run src/app/audit-route.test.tsx --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: AuditRoute calls `toast.error('This page is restricted to managers and admins.')` then `<Navigate to="/home" replace />` when `!can('view_audit_log')`.
    - behavior: RTL test proves children render when permitted, and toast.error fires + redirect occurs when denied.
    - source: the toast string matches `/restricted to managers and admins/i`.
  </acceptance_criteria>
  <done>AuditRoute gates manager+ with the E2E-required toast; covered by an RTL test.</done>
</task>

<task type="auto">
  <name>Task 3: Switch the HomeDashboard /audit tile from visibleToRoles to requiredAction</name>
  <files>src/widgets/HomeDashboard/ui/HomeDashboard.tsx</files>
  <read_first>
    - src/widgets/HomeDashboard/ui/HomeDashboard.tsx (full — the ITEMS array; the /audit entry at lines 87-92 currently uses `visibleToRoles: ['manager', 'admin']`; every other gated tile uses `requiredAction: <StaffAction>` + `managerLabel`; the gating-check logic (lines 112-144) already handles requiredAction via can(); data-testid="home-tile-audit" set at line 155)
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (section F — swap visibleToRoles for requiredAction: 'view_audit_log'; add managerLabel: 'Manager'; preserve data-testid="home-tile-audit" and the ClipboardList icon)
  </read_first>
  <action>
    In the ITEMS array, change the `/audit` entry from `{ path: '/audit', label: 'Audit Log', icon: ClipboardList, visibleToRoles: ['manager', 'admin'] }` to `{ path: '/audit', label: 'Audit Log', icon: ClipboardList, requiredAction: 'view_audit_log', managerLabel: 'Manager' }`. Remove the `visibleToRoles` key from this tile only (leave the /kds tile's visibleToRoles unchanged). This gives the tile the same gray-lock-icon-when-denied treatment + ManagerPinDialog gate flow every other manager+ tile has. Do NOT change data-testid="home-tile-audit" or the icon. No other tile is affected.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/HomeDashboard --reporter=verbose && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: the /audit tile uses `requiredAction: 'view_audit_log'` + `managerLabel: 'Manager'` and no longer has `visibleToRoles`.
    - source: data-testid="home-tile-audit" and the ClipboardList icon are preserved.
    - behavior: HomeDashboard tests + typecheck + lint (max-warnings 0) pass.
  </acceptance_criteria>
  <done>Audit tile gating is consistent with the rest of the app (Pitfall 8 resolved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /audit route | AuditRoute client guard is defense-in-depth; the real enforcement is the audit_logs SELECT RLS policy (manager+ only, verified live in 14-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Elevation of Privilege | bartender reaching /audit | mitigate | AuditRoute redirects + toasts; audit_logs SELECT RLS (get_user_role() IN manager/admin) is the authoritative boundary — client guard is not relied upon alone |
| T-14-13 | Information Disclosure | audit rows visible to unauthorized roles | mitigate | DB RLS restricts SELECT to manager+ regardless of client guard; view_audit_log seeded only for manager+admin |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-10)
- `'view_audit_log'` StaffAction in `STAFF_ACTIONS` + `MANAGER_EXTRA` (`src/shared/lib/rbac.ts`)
- `AuditRoute` exported from `src/app/audit-route.tsx` (toast-on-redirect guard)
- `src/app/audit-route.test.tsx`
- `supabase/migrations/20260703000006_role_permissions_view_audit_log.sql` (seed)
- HomeDashboard `/audit` tile switched to `requiredAction: 'view_audit_log'`
</artifacts_produced>

<verification>
- rbac.test.ts, audit-route.test.tsx, HomeDashboard tests pass; typecheck + lint clean.
- AuditRoute toast copy matches the E2E regex.
</verification>

<success_criteria>
- view_audit_log granted manager+ (client + DB seed); AuditRoute enforces + toasts; tile gating consistent.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-10-SUMMARY.md` when done.
</output>
