---
phase: 14-audit-logs-table
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - supabase/migrations/20260703000002_wire_transfer_tab_stock_movement_audit.sql
  - src/features/transfer-tab/useTransferTab.ts
  - src/features/adjust-stock-movement/ui/AdjustStockMovementDialog.tsx
autonomous: true
requirements: [SC3]
must_haves:
  truths:
    - "transfer_tab RPC records a 'tab.transfer' audit row on the success path, carrying terminal_id"
    - "record_stock_movement RPC records an 'inventory.manual_adjust' audit row on the success path, carrying terminal_id"
    - "Both client call sites pass the caller's TERMINAL_ID so terminal_id is populated, without regressing existing behavior"
  artifacts:
    - path: "supabase/migrations/20260703000002_wire_transfer_tab_stock_movement_audit.sql"
      provides: "transfer_tab + record_stock_movement patched with PERFORM record_audit"
      contains: "PERFORM record_audit"
  key_links:
    - from: "transfer_tab"
      to: "record_audit"
      via: "PERFORM record_audit('tab.transfer', ...)"
      pattern: "record_audit\\('tab.transfer'"
    - from: "record_stock_movement"
      to: "record_audit"
      via: "PERFORM record_audit('inventory.manual_adjust', ...)"
      pattern: "record_audit\\('inventory.manual_adjust'"
---

<objective>
Wire the two remaining TRUE SECURITY DEFINER RPCs (`transfer_tab`, `record_stock_movement`) with `record_audit` using the exact established post-mutation pattern, and thread `p_terminal_id` through both so their audit rows carry terminal id. These are the only two of the "8 remaining RPCs" that are genuine `PERFORM record_audit(...)` wiring targets (RESEARCH.md) — the rest need new RPCs or client-side calls.

Purpose: SC3 coverage for tab transfers and manual stock adjustments.
Output: One migration patching both RPCs; two client call sites updated to pass TERMINAL_ID.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@supabase/migrations/20260511000002_rpc_audit_wiring.sql
@supabase/migrations/20260420000003_transfers.sql
@supabase/migrations/20260426000003_record_stock_movement_rpc.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — wire transfer_tab ('tab.transfer') and record_stock_movement ('inventory.manual_adjust')</name>
  <files>supabase/migrations/20260703000002_wire_transfer_tab_stock_movement_audit.sql</files>
  <read_first>
    - supabase/migrations/20260420000003_transfers.sql (lines 64-122 — current transfer_tab body: fetches tab, validates open, UPDATE tabs, INSERT tab_transfers RETURNING v_transfer_id, RETURN json ok:true)
    - supabase/migrations/20260426000003_record_stock_movement_rpc.sql (full — current record_stock_movement body: FOR UPDATE ingredient, insert stock_movements RETURNING v_row, UPDATE ingredients, RETURN v_row)
    - supabase/migrations/20260511000002_rpc_audit_wiring.sql (the canonical PERFORM record_audit pattern — pre-RETURN, success path only, `SELECT to_jsonb(x) INTO ... `; header comment style at lines 1-21)
    - .planning/phases/14-audit-logs-table/14-PATTERNS.md (Pattern 1 verbatim shape + rule: NULL before for newly-created entities, to_jsonb(before) for pre-existing)
  </read_first>
  <action>
    New migration `BEGIN; ... COMMIT;` with a commented DOWN block. Two `CREATE OR REPLACE FUNCTION` statements copying the CURRENT bodies verbatim, each with two edits:
    (A) transfer_tab — add a new trailing parameter `p_terminal_id text DEFAULT NULL` to the signature (keep all existing params + order). Capture before-state with `SELECT to_jsonb(t) INTO v_before FROM tabs t WHERE t.id = p_tab_id;` BEFORE the `UPDATE tabs` (declare `v_before jsonb; v_after jsonb;`). After the INSERT into tab_transfers and BEFORE the final `RETURN json_build_object('ok', true, ...)`, add `SELECT to_jsonb(t) INTO v_after FROM tabs t WHERE t.id = p_tab_id;` then `PERFORM record_audit('tab.transfer', 'tab', p_tab_id, v_before, v_after, 'rpc', p_terminal_id);`. Do NOT audit the NOT_FOUND / TAB_NOT_OPEN early-return branches. Preserve `SECURITY DEFINER`.
    (B) record_stock_movement — add trailing parameter `p_terminal_id text DEFAULT NULL`. After the `UPDATE ingredients ... SET quantity_on_hand = v_new` and BEFORE `RETURN v_row;`, add `PERFORM record_audit('inventory.manual_adjust', 'ingredient', p_ingredient_id, NULL, to_jsonb(v_row), 'rpc', p_terminal_id);`. Do NOT audit the INGREDIENT_NOT_FOUND / INVENTORY_NEGATIVE RAISE branches (those abort before mutation). Preserve `SET search_path = public` and `SECURITY DEFINER`.
    Re-issue the GRANTs for the NEW signatures: `GRANT EXECUTE ON FUNCTION transfer_tab(uuid, uuid, uuid, int, text, text, text) TO authenticated;` and `GRANT EXECUTE ON FUNCTION record_stock_movement(uuid, numeric, text, text, uuid, text, text) TO authenticated;` (note the added trailing text param in each). Because adding a defaulted trailing param to record_stock_movement creates an overload alongside the existing /6, first `DROP FUNCTION IF EXISTS record_stock_movement(uuid, numeric, text, text, uuid, text);` and for transfer_tab `DROP FUNCTION IF EXISTS transfer_tab(uuid, uuid, uuid, int, text, text);` before the CREATE, to avoid PGRST203 "not unique" ambiguity on the 6-arg call form.
    Use action labels exactly as enumerated in audit-actions.ts ('tab.transfer', 'inventory.manual_adjust').
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-actions.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: migration contains `PERFORM record_audit('tab.transfer', 'tab', p_tab_id,` and `PERFORM record_audit('inventory.manual_adjust', 'ingredient', p_ingredient_id,`.
    - source: both functions gain a trailing `p_terminal_id text DEFAULT NULL` param and a preceding `DROP FUNCTION IF EXISTS` for the old signature.
    - behavior: the audit-actions coverage test's transfer_tab + record_stock_movement assertions now PASS (previously RED).
    - behavior: no PERFORM record_audit appears inside a validation-error/RAISE branch.
  </acceptance_criteria>
  <done>Both RPCs audit on success with terminal_id; coverage test advances by 2. Migration not pushed (14-14).</done>
</task>

<task type="auto">
  <name>Task 2: Pass TERMINAL_ID from the transfer_tab client call site</name>
  <files>src/features/transfer-tab/useTransferTab.ts</files>
  <read_first>
    - src/features/transfer-tab/useTransferTab.ts (full — the `db.rpc('transfer_tab', {...})` call at lines ~31-36 with p_tab_id/p_to_staff_id/p_to_table/p_transferred_by)
    - src/shared/lib/logger-instance.ts (line 19 — `const TERMINAL_ID = (import.meta.env.VITE_TERMINAL_ID as string | undefined) || 'POS-1';`)
    - src/app/OfflineQueueProcessor.tsx (line 15 — the local `TERMINAL_ID` re-declaration convention to copy)
  </read_first>
  <action>
    Add a module-level `const TERMINAL_ID = (import.meta.env.VITE_TERMINAL_ID as string | undefined) ?? 'POS-1';` (matching OfflineQueueProcessor.tsx's convention). Add `p_terminal_id: TERMINAL_ID` to the `db.rpc('transfer_tab', {...})` params object. Change nothing else — the existing error handling, Result mapping, and onSuccess invalidation stay identical. The file already uses `supabase as any` (db), so no type regen needed.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `useTransferTab.ts` declares `TERMINAL_ID` and the rpc params include `p_terminal_id: TERMINAL_ID`.
    - behavior: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Transfer audit rows carry terminal_id.</done>
</task>

<task type="auto">
  <name>Task 3: Pass TERMINAL_ID from the manual stock-adjust call site</name>
  <files>src/features/adjust-stock-movement/ui/AdjustStockMovementDialog.tsx</files>
  <read_first>
    - src/features/adjust-stock-movement/ui/AdjustStockMovementDialog.tsx (full — the `(supabase as any).rpc('record_stock_movement', {...})` call at lines ~58-65 with p_ingredient_id/p_delta/p_reason/p_ref_type:'manual'/p_ref_id/p_notes)
    - src/shared/lib/logger-instance.ts (line 19 — TERMINAL_ID convention)
  </read_first>
  <action>
    Add a module-level `const TERMINAL_ID = (import.meta.env.VITE_TERMINAL_ID as string | undefined) ?? 'POS-1';`. Add `p_terminal_id: TERMINAL_ID` to the `record_stock_movement` rpc params object. Leave the INVENTORY_NEGATIVE error mapping, toasts, and query invalidations unchanged. Note this dialog only handles `p_ref_type: 'manual'` adjustments — those are exactly the manual adjustments 'inventory.manual_adjust' is meant to audit; depletion/void/refund-sourced stock movements come through other ref_types and are audited at their own RPCs.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: the `record_stock_movement` rpc params include `p_terminal_id: TERMINAL_ID`.
    - behavior: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Manual stock-adjust audit rows carry terminal_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → transfer_tab / record_stock_movement RPC | untrusted params (target staff/table, delta, reason, terminal_id) cross into SECURITY DEFINER context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Repudiation | tab transfer + manual stock adjustment | mitigate | PERFORM record_audit on success path with before/after JSON + terminal_id |
| T-14-04 | Information Disclosure | audit failure inside these RPCs | accept | record_audit swallows its own exceptions returning NULL; the primary mutation is unaffected and no internal error leaks |
| T-14-07 | Tampering | RPC overload ambiguity after adding p_terminal_id | mitigate | DROP the prior 6-arg signature before CREATE to avoid PGRST203 "function not unique" |
| T-14-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-03)
- `supabase/migrations/20260703000002_wire_transfer_tab_stock_movement_audit.sql`
- `transfer_tab(uuid, uuid, uuid, int, text, text, text)` — new signature (adds `p_terminal_id`), audits `tab.transfer`
- `record_stock_movement(uuid, numeric, text, text, uuid, text, text)` — new signature (adds `p_terminal_id`), audits `inventory.manual_adjust`
- `TERMINAL_ID` module constant added to `useTransferTab.ts` and `AdjustStockMovementDialog.tsx`
</artifacts_produced>

<verification>
- audit-actions coverage test advances (transfer_tab + record_stock_movement now green).
- `npm run typecheck` exits 0 after both call-site edits.
</verification>

<success_criteria>
- Both RPCs emit their audit action on success with terminal_id; validation-error branches unaudited.
- Client call sites pass TERMINAL_ID with no behavioral regression.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-03-SUMMARY.md` when done.
</output>
