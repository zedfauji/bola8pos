---
phase: 14-audit-logs-table
plan: 08
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/shared/lib/audit-actions.ts
  - supabase/functions/create-staff/index.ts
  - supabase/functions/settings-restore/index.ts
  - src/shared/lib/__tests__/audit-edge-coverage.test.ts
autonomous: true
requirements: [SC4, SC5]
must_haves:
  truths:
    - "The sensitive Edge Functions that perform DB mutations not already covered by an audited RPC (create-staff, settings-restore, void-order) each import and call recordAudit"
    - "A CI test asserts a defined sensitive-Edge-Function allowlist all import + call recordAudit"
    - "'staff.create' is an enumerated audit action (create-staff needs a label with no prior enum entry)"
  artifacts:
    - path: "src/shared/lib/__tests__/audit-edge-coverage.test.ts"
      provides: "SC4 allowlist coverage test over supabase/functions/*/index.ts"
      contains: "SENSITIVE_EDGE_FUNCTIONS"
    - path: "src/shared/lib/audit-actions.ts"
      provides: "'staff.create' added to AuditActionSchema"
      contains: "staff.create"
  key_links:
    - from: "supabase/functions/create-staff/index.ts"
      to: "recordAudit"
      via: "import + call action 'staff.create'"
      pattern: "staff.create"
    - from: "supabase/functions/settings-restore/index.ts"
      to: "recordAudit"
      via: "import + call action 'settings.update'"
      pattern: "settings.update"
---

<objective>
D-09: grep `supabase/functions/` for Edge Functions performing sensitive mutations NOT already covered by RPC-level `record_audit`, and wire `recordAudit` into each. Confirmed sensitive, uncovered mutators: `create-staff` (auth.admin.createUser + profiles INSERT — staff creation) and `settings-restore` (bulk upsert of categories/modifiers/products/settings + settings_backups). (`void-order` is handled in 14-07; `process-payment` delegates to the already-audited `process_payment_atomic` RPC.) Add the missing `'staff.create'` action label and a CI test enforcing an explicit sensitive-Edge-Function allowlist.

Purpose: SC4 (edge coverage) + SC5 (enum + CI enforcement).
Output: One enum addition, two edge functions wired, one new coverage test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-RESEARCH.md
@src/shared/lib/audit-actions.ts
@supabase/functions/_shared/audit.ts
@supabase/functions/create-staff/index.ts
@supabase/functions/settings-restore/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'staff.create' to AuditActionSchema</name>
  <files>src/shared/lib/audit-actions.ts</files>
  <read_first>
    - src/shared/lib/audit-actions.ts (full — the AuditActionSchema z.enum with the "Staff" comment section currently holding only 'staff.role_change'; and the parallel AuditAction const object `satisfies Record<string, AuditAction>`)
  </read_first>
  <action>
    Add `'staff.create',` under the `// Staff` comment section of the `AuditActionSchema` z.enum (adjacent to `'staff.role_change'`). Add the matching const entry `STAFF_CREATE: 'staff.create',` to the `AuditAction` const object so it stays `satisfies Record<string, AuditAction>`. Do not reorder or remove any existing entries. This raises the enum count to 21 (the "at least 20" test still passes).
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-actions.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: AuditActionSchema.options includes 'staff.create'; AuditAction.STAFF_CREATE === 'staff.create'.
    - behavior: audit-actions.test.ts passes; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>'staff.create' is an enumerated, type-safe audit action.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recordAudit into create-staff and settings-restore</name>
  <files>supabase/functions/create-staff/index.ts, supabase/functions/settings-restore/index.ts</files>
  <read_first>
    - supabase/functions/create-staff/index.ts (full — auth.admin.createUser then profiles INSERT { id: staffId, name, role, pin, email }; note whether the request establishes an authenticated caller/actor and how it rolls back via deleteUser on profile-insert failure)
    - supabase/functions/settings-restore/index.ts (full — the bulk upserts of categories/modifiers/products/product_modifiers/pool_tables/settings + settings_backups update; note `user.id` is available and used as updated_by around line 136)
    - supabase/functions/_shared/audit.ts (recordAudit signature incl. actorId per 14-02; fire-and-forget, never throws)
  </read_first>
  <action>
    In create-staff/index.ts: add `import { recordAudit } from '../_shared/audit.ts';`. After the profiles INSERT succeeds (and the deleteUser rollback branch is NOT taken), call recordAudit(supabaseAdmin, { action: 'staff.create', entityType: 'staff', entityId: staffId, before: null, after with name/role/email only, source: 'edge', actorId: caller id if the function establishes one else null }). Do NOT audit the rollback/failure branch. Do NOT include the PIN in the after payload.
    In settings-restore/index.ts: add the same import. After the full restore completes successfully (all upserts done, before the success response), call recordAudit(serviceClient, { action: 'settings.update', entityType: 'settings', entityId: null, before: null, after: a summary object of restored counts (categories.length, products.length, settingsRows.length) NOT the full payload, source: 'edge', actorId: user.id }). Use 'settings.update' (already enumerated). Both calls are fire-and-forget — do not gate the function's response on them, and exclude any secrets/PINs from audit payloads.
  </action>
  <verify>
    <automated>grep -q "recordAudit" supabase/functions/create-staff/index.ts && grep -q "'staff.create'" supabase/functions/create-staff/index.ts && grep -q "recordAudit" supabase/functions/settings-restore/index.ts && grep -q "'settings.update'" supabase/functions/settings-restore/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: create-staff imports recordAudit and calls it with action 'staff.create' on the success path (PIN excluded from payload).
    - source: settings-restore imports recordAudit and calls it with action 'settings.update' + a restored-counts summary (no full payload).
    - behavior: neither audit call gates the function's HTTP response.
  </acceptance_criteria>
  <done>create-staff + settings-restore emit audit rows for their sensitive mutations.</done>
</task>

<task type="auto">
  <name>Task 3: SC4 allowlist coverage test over sensitive Edge Functions</name>
  <files>src/shared/lib/__tests__/audit-edge-coverage.test.ts</files>
  <read_first>
    - src/shared/lib/__tests__/audit-actions.test.ts (the readdirSync/readFileSync + join(process.cwd(), 'supabase', ...) file-reading pattern to mirror; comment-stripping via replace before matching)
    - supabase/functions/_shared/audit.ts (the recordAudit import path convention `../_shared/audit.ts`)
  </read_first>
  <action>
    Create a new Vitest test file. Define `const SENSITIVE_EDGE_FUNCTIONS = ['void-order', 'create-staff', 'settings-restore'];` (the explicit allowlist of sensitive-mutation edge functions per D-09). For each name, read `supabase/functions/<name>/index.ts` via readFileSync, strip comment lines (`replace(/^\s*\/\/.*$/gm, '')`), and assert the source contains BOTH `from '../_shared/audit.ts'` (the recordAudit import) AND `recordAudit(` (an actual call). Fail with a clear message naming the function if either is missing. Add a second `it` asserting the allowlist is non-empty and that each named directory exists (`existsSync`), so a renamed/deleted function surfaces as a test failure rather than a silent skip.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/__tests__/audit-edge-coverage.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - source: test defines `SENSITIVE_EDGE_FUNCTIONS` = ['void-order', 'create-staff', 'settings-restore'].
    - behavior: test PASSES once 14-07 (void-order) + this plan's Task 2 land; asserts each imports `../_shared/audit.ts` and calls `recordAudit(`.
    - behavior: a missing directory or missing recordAudit call fails the test with the offending function name.
  </acceptance_criteria>
  <done>SC4 edge-coverage enforced by an automated allowlist test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → create-staff / settings-restore Edge Functions | untrusted request bodies mutate profiles + full settings under service-role; audit must record who/what |
| Edge Function (service-role) → audit_logs | actor supplied explicitly (caller id / user.id) since auth.uid() is not meaningful under service-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Repudiation | staff creation + settings restore | mitigate | recordAudit('staff.create'/'settings.update') on success with actor + summary payload |
| T-14-11 | Information Disclosure | secrets in audit payload | mitigate | PIN excluded from create-staff payload; settings-restore records only restored-counts summary, not full snapshot (also keeps payload < 64KB) |
| T-14-04 | Information Disclosure | audit failure | accept | recordAudit swallows its own errors; response not gated |
| T-14-SC | Tampering | npm installs | accept | Zero new packages; Deno imports unchanged |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-08)
- `'staff.create'` added to `AuditActionSchema` + `AuditAction.STAFF_CREATE` in `src/shared/lib/audit-actions.ts`
- `recordAudit('staff.create')` call in `supabase/functions/create-staff/index.ts`
- `recordAudit('settings.update')` call in `supabase/functions/settings-restore/index.ts`
- `src/shared/lib/__tests__/audit-edge-coverage.test.ts` + `SENSITIVE_EDGE_FUNCTIONS` allowlist
</artifacts_produced>

<verification>
- audit-actions.test.ts (enum) + audit-edge-coverage.test.ts pass; `npm run typecheck` exits 0.
- All three allowlisted edge functions import + call recordAudit.
</verification>

<success_criteria>
- create-staff + settings-restore emit audit rows; 'staff.create' enumerated.
- Sensitive-edge coverage enforced by CI test with an explicit allowlist.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-08-SUMMARY.md` when done.
</output>
