---
phase: 14-audit-logs-table
plan: 11
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/entities/audit-log/model/queries.ts
  - src/entities/audit-log/model/queries.test.ts
  - src/widgets/AuditLogTable/AuditLogFilterBar.tsx
  - src/widgets/AuditLogTable/AuditLogDetailSheet.tsx
  - src/widgets/AuditLogTable/AuditLogTable.tsx
  - src/widgets/AuditLogTable/index.ts
  - src/widgets/AuditLogTable/AuditLogTable.test.tsx
autonomous: true
requirements: [SC6, SC7, SC9]
must_haves:
  truths:
    - "The free-text search filter escapes PostgREST .or() metacharacters (commas/periods) before interpolation, closing the injection gap found in research"
    - "AuditLogTable renders audit rows (action/entity/actor/timestamp/source), filters via the 5 locked-id controls + 'Apply filters', paginates page-size-50 via 'Load more', and shows 'No matches' when empty"
    - "Clicking a row opens a right-side Sheet rendering JsonDiffViewer(before, after, truncated) with truncation detected from _truncated on before/after"
    - "The row exposes an accessible 'View diff for {action} on {date}' button and a cell whose accessible name is the raw action string (E2E DOM contract)"
  artifacts:
    - path: "src/widgets/AuditLogTable/AuditLogTable.tsx"
      provides: "filter bar + infinite-scroll table + row→Sheet"
      min_lines: 60
    - path: "src/widgets/AuditLogTable/AuditLogDetailSheet.tsx"
      provides: "Sheet wrapper around JsonDiffViewer with truncation detection"
    - path: "src/widgets/AuditLogTable/index.ts"
      provides: "AuditLogTable barrel export"
      exports: ["AuditLogTable"]
  key_links:
    - from: "AuditLogTable"
      to: "useAuditLogs"
      via: "useInfiniteQuery consumption + Load more"
      pattern: "useAuditLogs"
    - from: "AuditLogDetailSheet"
      to: "JsonDiffViewer"
      via: "before/after/truncated props"
      pattern: "JsonDiffViewer"
    - from: "useAuditLogs search filter"
      to: "PostgREST .or()"
      via: "escaped interpolation"
      pattern: "escape"
---

<objective>
Build the `widgets/AuditLogTable/` composite that surfaces the audit log on `/audit`: filter bar (5 filters with E2E-locked DOM ids + "Apply filters"), infinite-scroll table (page size 50, "Load more"), and a click-to-open right-side Sheet rendering the existing `JsonDiffViewer` with truncation detection. Reuse the complete `entities/audit-log` query hook and `shared/ui/JsonDiffViewer` as-is (RESEARCH.md: the frontend half is essentially built) — but FIRST fix the real PostgREST filter-injection gap in the search filter (V5 / 14-0X-10).

Purpose: SC6 (page filters + infinite scroll + diff viewer), SC7 (widget), SC9 (truncation banner surfaced), plus the security fix.
Output: 3 widget components + barrel + the search-escape fix + 2 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-logs-table/14-UI-SPEC.md
@.planning/phases/14-audit-logs-table/14-PATTERNS.md
@src/entities/audit-log/model/queries.ts
@src/shared/ui/JsonDiffViewer/JsonDiffViewer.tsx
@src/shared/ui/DataTable.tsx
@src/features/process-refund/ui/RefundSheet.tsx
@e2e/38-audit-logs.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escape PostgREST .or() metacharacters in the useAuditLogs search filter (+ unit test)</name>
  <files>src/entities/audit-log/model/queries.ts, src/entities/audit-log/model/queries.test.ts</files>
  <behavior>
    - A search string containing `,` or `.` (PostgREST .or() filter-string metacharacters) is sanitized before it is interpolated into the `.or('entity_id::text.ilike.%...%,action.ilike.%...%')` expression, so it cannot alter the filter structure.
    - A benign search string still produces the same two ilike sub-filters.
  </behavior>
  <read_first>
    - src/entities/audit-log/model/queries.ts (lines 65-69 — the `if (filters.search) { query = query.or(`entity_id::text.ilike.%${filters.search}%,action.ilike.%${filters.search}%`) }` raw interpolation; note the file-level `db = supabase as any` cast and the mapAuditRow parser)
    - .planning/phases/14-audit-logs-table/14-RESEARCH.md (Security Domain V5 — the exact injection gap: commas/periods can restructure the .or() filter)
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (section B security note — strip/escape `,` and `.`; the copy "Some characters aren't searchable and will be ignored" is a UX decision for the filter bar in Task 2)
  </read_first>
  <action>
    Add a small pure helper (e.g. `sanitizeSearch(raw: string): string`) that strips PostgREST filter metacharacters — at minimum remove/replace commas `,` and periods `.` (and parentheses `()` which also have meaning in `.or()` grouping) from the search term, returning the cleaned string. Apply it: `const term = sanitizeSearch(filters.search); if (term) { query = query.or(`entity_id::text.ilike.%${term}%,action.ilike.%${term}%`); }`. Export `sanitizeSearch` so it is unit-testable. Do NOT change the two-column ilike shape or the rest of the query. In queries.test.ts (create if absent), add tests: (a) `sanitizeSearch('a,b.c')` contains no `,` or `.`; (b) `sanitizeSearch('budweiser')` returns 'budweiser'; (c) a property-style or explicit test that no input produces a string containing `,`/`.`/`(`/`)`.
  </action>
  <verify>
    <automated>npx vitest run src/entities/audit-log/model/queries.test.ts --reporter=verbose && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: `sanitizeSearch` is exported and strips `,` `.` `(` `)` from the input; useAuditLogs calls it before building the `.or()` string.
    - behavior: tests prove metacharacters are removed and benign terms survive; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Search filter no longer allows PostgREST filter-string injection (14-0X-10 closed).</done>
</task>

<task type="auto">
  <name>Task 2: AuditLogFilterBar (5 locked-id filters + Apply) and AuditLogDetailSheet (JsonDiffViewer + truncation)</name>
  <files>src/widgets/AuditLogTable/AuditLogFilterBar.tsx, src/widgets/AuditLogTable/AuditLogDetailSheet.tsx</files>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (sections B + D — exact filter controls, locked ids `#audit-filter-action`/`#audit-filter-date-from`/`#audit-filter-date-to`, placeholders, "Apply filters" button, staged-then-committed filter state; the DetailSheet Sheet structure copied from RefundSheet + JsonDiffViewer render + isTruncated extraction)
    - src/features/process-refund/ui/RefundSheet.tsx (Sheet/SheetContent side="right"/SheetHeader/SheetTitle/SheetDescription import + structure to copy; read-only so NO footer/mutation)
    - src/shared/ui/JsonDiffViewer/JsonDiffViewer.tsx (props: before, after, truncated; the truncation banner is already implemented — do NOT restyle)
    - src/shared/lib/audit-actions.ts (AuditActionSchema.options — the Action select options)
    - src/entities/staff/model/queries.ts (useStaffList / active-profiles hook for the Actor select — display name, value = id)
  </read_first>
  <action>
    AuditLogFilterBar.tsx: a controlled component taking staged filter state + an onApply callback. Render five controls left-to-right per UI-SPEC: (1) Action `<Select>` with trigger `id="audit-filter-action"`, options from `AuditActionSchema.options`, placeholder "All actions"; (2) Entity type `<Select>` placeholder "All entities" with the hardcoded entity-type list from UI-SPEC (payment, tab, caja_session, order, combo, ingredient, prep_production, permission, staff, settings); (3) Actor `<Select>` populated from the active-staff hook (name label, id value), placeholder "All staff"; (4) two `<input type="date">` with ids `id="audit-filter-date-from"` + `id="audit-filter-date-to"` styled like DateRangePicker's inputs; (5) a search `<Input>` placeholder "Search by entity ID or action…". A primary "Apply filters" `<Button>` at the end commits staged state via onApply (filters are staged locally, NOT applied on every change — matches the E2E's explicit Apply step). Container `flex flex-wrap items-center gap-2`. Add the inline note "Some characters aren't searchable and will be ignored" near the search input (UI-SPEC copy). AuditLogDetailSheet.tsx: props `{ row: AuditLog | null; open: boolean; onOpenChange: (o:boolean)=>void }`. Copy RefundSheet's Sheet shell (`<Sheet open onOpenChange><SheetContent side="right" className="w-full sm:max-w-xl overflow-y-auto flex flex-col">`), SheetTitle = row.action, SheetDescription = `{entityType} · {formattedTimestamp} · {actorName ?? 'System'}`, body renders `<JsonDiffViewer before={row.before} after={row.after} truncated={isTruncated} />` where `isTruncated = (row.before as {_truncated?:boolean}|null)?._truncated === true || (row.after as {_truncated?:boolean}|null)?._truncated === true`. No footer/action buttons (read-only). Reference color/typography tokens per UI-SPEC (no hardcoded oklch/hex).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: AuditLogFilterBar renders the Action Select with `id="audit-filter-action"`, date inputs with `id="audit-filter-date-from"` + `id="audit-filter-date-to"`, and an "Apply filters" button; filters are staged and applied only on Apply.
    - source: AuditLogDetailSheet renders `<JsonDiffViewer ... truncated={isTruncated} />` with the exact `_truncated` extraction; no footer/mutation.
    - behavior: typecheck + lint (max-warnings 0) pass.
  </acceptance_criteria>
  <done>Filter bar (locked DOM ids) + read-only diff Sheet exist and reuse JsonDiffViewer verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AuditLogTable widget (DataTable + accessible row-diff button + Load more + empty states) + barrel + RTL test</name>
  <files>src/widgets/AuditLogTable/AuditLogTable.tsx, src/widgets/AuditLogTable/index.ts, src/widgets/AuditLogTable/AuditLogTable.test.tsx</files>
  <behavior>
    - Renders one row per audit log with columns Action / Entity type / Actor (staff name or "System" if actorId null) / Timestamp / Source badge.
    - The action cell's accessible name is the raw action string (reachable by getByRole('cell', { name: 'payment.process' })).
    - Each row exposes an sr-only `<button>` with accessible name `View diff for {action} on {formatted date}` whose onClick opens the DetailSheet for that row.
    - "Load more entries" button appears when hasNextPage; disabled + spinner while isFetchingNextPage.
    - Empty (filtered) → "No matches" + body copy; zero-rows-ever → the alternate empty copy; query error → the error copy (per UI-SPEC copywriting).
  </behavior>
  <read_first>
    - .planning/phases/14-audit-logs-table/14-UI-SPEC.md (section C — the row/cell DOM contract; the CORRECTED recommendation (option b): add an sr-only `<button>` inside a cell with the accessible name, plus onRowClick for mouse users — do NOT override the <tr> role; and the "Load more" default over IntersectionObserver; empty-state + error copy)
    - src/shared/ui/DataTable.tsx (props: columns, data, onRowClick, getRowClassName, toolbar, searchable — note there is NO getRowAriaLabel/role hook, hence the sr-only-button approach)
    - src/entities/audit-log/model/queries.ts (useAuditLogs → useInfiniteQuery; data.pages flattened; fetchNextPage/hasNextPage/isFetchingNextPage/status/error; auditKeys)
    - src/entities/staff/model/queries.ts (active-staff hook to resolve actorId → name; "System" when null)
    - e2e/38-audit-logs.spec.ts (the exact queries: getByRole('cell',{name:'payment.process'}), getByRole('button',{name:/view diff for .* on .*/i}), getByText(/no matches/i), row-click → getByRole('dialog') with before/after)
  </read_first>
  <action>
    Build AuditLogTable.tsx: local staged filter state; call `useAuditLogs(appliedFilters)`; flatten `data.pages` into `rows`. Render `<DataTable columns={columns} data={rows} toolbar={<AuditLogFilterBar staged=... onApply={setAppliedFilters} />} onRowClick={openSheet} getRowClassName=... />`. Columns: Action (cell renders the raw action string as plain text so its accessible name equals e.g. 'payment.process' — do NOT wrap it in an interactive element; ALSO render, within this cell, an `sr-only` `<button aria-label={`View diff for ${row.action} on ${new Date(row.createdAt).toLocaleString()}`} onClick={() => openSheet(row)}>` so the E2E button-role query resolves without breaking table semantics), Entity type, Actor (staff name via the active-staff map, "System" if actorId null), Timestamp (toLocaleString), Source (`<Badge>` with rpc/edge/client/trigger). Below the table render a "Load more entries" `<Button>` that calls `fetchNextPage()` when `hasNextPage`, disabled + spinner while `isFetchingNextPage`. Empty states: when status==='success' && rows.length===0 → if any filter is applied show heading "No matches" + the "No audit entries match your filters…" body; if no filters applied show "No audit activity yet" + its body. On status==='error' show "Couldn't load audit log — check your connection and try again." Manage the selected-row + sheet-open state and render `<AuditLogDetailSheet row={selected} open={sheetOpen} onOpenChange={setSheetOpen} />`. Create index.ts barrel `export { AuditLogTable } from './AuditLogTable';` (explicit named export — `export *` is banned by ESLint). AuditLogTable.test.tsx (RTL): mock useAuditLogs to return 2 rows; assert a cell with the action string is present, assert a button matching /view diff for .* on .*/i exists, assert clicking it opens the sheet (DetailSheet); mock to return 0 rows WITH a filter applied and assert "No matches" appears.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx --reporter=verbose && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: an action cell renders the raw action string as plain text (accessible name = action), and each row has an sr-only button with aria-label matching `View diff for {action} on {date}`.
    - source: a "Load more entries" button drives fetchNextPage when hasNextPage; disabled + spinner while isFetchingNextPage.
    - source: empty (filtered) renders "No matches"; error renders the connection-error copy.
    - source: index.ts uses an explicit named export (no `export *`).
    - behavior: RTL test proves cell, diff-button, sheet-open, and "No matches" behaviors; typecheck + lint pass.
  </acceptance_criteria>
  <done>AuditLogTable composes filter bar + table + Load more + Sheet, satisfying the E2E DOM contract; barrel exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text search → PostgREST .or() filter | user-controlled string interpolated into a filter expression (the ONLY direct interpolation in this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | useAuditLogs free-text search .or() injection | mitigate | sanitizeSearch strips `,` `.` `(` `)` before interpolation (real bug from RESEARCH.md V5, fixed + unit-tested) |
| T-14-13 | Information Disclosure | audit data rendered to unauthorized users | mitigate | audit_logs SELECT RLS is manager+ (DB); AuditRoute (14-10) gates the page; widget renders only what RLS returns |
| T-14-14 | Information Disclosure | oversized/sensitive payload in diff | accept | JsonDiffViewer + truncation banner (D-08) surface `_truncated`; server-side record_audit already caps payloads at 64KB |
| T-14-SC | Tampering | npm installs | accept | Zero new packages; all shadcn primitives already installed |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-11)
- `sanitizeSearch(raw: string): string` exported from `src/entities/audit-log/model/queries.ts` (+ applied in useAuditLogs)
- `AuditLogFilterBar` (`src/widgets/AuditLogTable/AuditLogFilterBar.tsx`)
- `AuditLogDetailSheet` (`src/widgets/AuditLogTable/AuditLogDetailSheet.tsx`)
- `AuditLogTable` (`src/widgets/AuditLogTable/AuditLogTable.tsx`) + `index.ts` barrel
- `src/widgets/AuditLogTable/AuditLogTable.test.tsx` + search-escape tests in `queries.test.ts`
</artifacts_produced>

<verification>
- queries.test.ts (escape) + AuditLogTable.test.tsx pass; typecheck + lint clean.
- DOM contract (cell name, diff button aria-label, "No matches") matches e2e/38-audit-logs.spec.ts expectations.
</verification>

<success_criteria>
- Search injection fixed + tested; widget renders/filter/paginate/diff-sheet per UI-SPEC and the E2E DOM contract.
</success_criteria>

<output>
Create `.planning/phases/14-audit-logs-table/14-11-SUMMARY.md` when done.
</output>
