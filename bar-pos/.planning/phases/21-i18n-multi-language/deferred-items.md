# Deferred Items — Phase 21 (i18n-multi-language)

## Plan 21-13

- **`e2e/visual/45-visual-baseline.spec.ts`'s `resetTestState()` never resets or masks `audit_logs` (Phase 34 spec gap, unrelated to Phase 21):** the `admin-audit` route screenshot is a live, ever-growing table — any audit-generating action taken between baking the baseline and re-verifying it (by this spec, other E2E specs, or manual use of the app against the same Supabase project) will drift the row count/content and produce a diff on the next `test:e2e:visual` run. This surfaced during 21-13's Task 2 zero-diff gate run as one of 4 diffs; traced to `45-visual-baseline.spec.ts` masking every other route's live/time-based content (`toastMask`, `live-time-display`, `elapsed-minutes`, `kds-board`, staff clock-in/duration columns, caja "Opened:" line — see that spec's `masksFor()`) but never masking or resetting `audit_logs` for the `/audit` route. Rebaselined for 21-13 (unblocks Phase 21 closure), but the gap itself is NOT fixed — out of Phase 21's scope (`45-visual-baseline.spec.ts` belongs to Phase 34). Future work: either mask the audit log table's row contents/timestamps in `masksFor()` for the `/audit` route (mirroring the `/staff` pattern), or add an `audit_logs` truncate/reset step to `resetTestState()` scoped to E2E-created rows only.
- **`admin-home` screenshot showed a uniform ~10-12px vertical shift of the entire tile grid across a baseline re-run, with zero code changes to `HomeDashboard.tsx`/`globals.css` since the prior baseline commit.** Investigated the hypothesis that a rendered current-date/day-name string (baseline captured 2026-07-14, re-verified 2026-07-18 — a different day-name length in the active locale could shift a flex/grid layout by this magnitude): grepped `src/widgets/HomeDashboard` for `toLocaleDateString`, `toLocaleTimeString`, `Intl.DateTimeFormat`, `dayjs`, `date-fns` — **no matches**. `HomeDashboard.tsx` renders no date/day-name string at all, so this hypothesis is not confirmed. Rebaselined anyway (two deterministic runs across this session showed no functional regression — same tiles, same labels, same route/role gating — only a pixel-level layout shift), but the root cause remains genuinely unexplained. Non-blocking; flagged here in case it recurs on a future baseline re-run.
