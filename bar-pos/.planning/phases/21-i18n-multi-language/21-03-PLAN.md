---
phase: 21-i18n-multi-language
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.tsx
  - src/widgets/SettingsTabsPanel/index.tsx
  - src/shared/lib/i18n/locales/es-MX/settings.json
  - src/shared/lib/i18n/locales/en-US/settings.json
  - src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.test.tsx
autonomous: true
requirements: [SC-2, SC-4]

must_haves:
  truths:
    - "Every authenticated role (incl. bartender) sees a role-agnostic 'Language' tab in Settings — it renders outside the manage_settings/manage_products gates"
    - "Selecting a locale + Save writes the current staff's profiles.locale, fires i18n.changeLanguage(locale), and the app re-renders in the new language without reload"
    - "The pre-existing settings tab labels (General, Hardware, ...) render via t() from the settings namespace with es-MX values byte-identical to today"
  artifacts:
    - src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.tsx
    - "settings namespace keys for the Language tab + existing tab labels"
  key_links:
    - "SettingsTabsPanel builds a third, unconditional tab-list entry so firstTab is non-empty for every role (resolves RESEARCH Pitfall 1)"
    - "LanguageSettingsTab is NOT wrapped in ProtectedAction (D-03 self-service)"
---

<objective>
Ship the self-service language switcher (new UI surface #1, D-03): a role-agnostic "Language" tab in `SettingsTabsPanel` that renders for every authenticated role, and a `LanguageSettingsTab` that writes the current staff's `profiles.locale` and switches the live UI language on Save. Also migrate SettingsTabsPanel's own pre-existing hardcoded strings (tab labels + the permission-fallback message) into the `settings` namespace.

Purpose: D-03 requires self-service locale switching in Settings, but `SettingsTabsPanel` today renders ZERO tabs for any role lacking `manage_settings`/`manage_products` — a bartender sees only "You do not have permission to view settings" (RESEARCH Pitfall 1). The UI-SPEC coordinator override locks bartenders IN scope for self-service, so the tab must sit OUTSIDE both role gates. This is a structural change to the tab-assembly logic, not an additive tab.

Output: `LanguageSettingsTab` (built from existing shared/ui only — Select, Label, POSButton, per UI-SPEC "zero new components"); restructured `SettingsTabsPanel`; `settings` catalog populated (es-MX values per UI-SPEC copy table + existing labels preserved byte-for-byte). Implements D-03, contributes SC-2/SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-CONTEXT.md
@.planning/phases/21-i18n-multi-language/21-UI-SPEC.md
@.planning/phases/21-i18n-multi-language/21-PATTERNS.md
@src/widgets/SettingsTabsPanel/index.tsx
@src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx
@src/features/edit-staff-role/ui/EditRoleDialog.tsx
@src/entities/staff/model/store.ts
@src/shared/lib/domain.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LanguageSettingsTab (self-service locale switcher)</name>
  <files>src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.tsx, src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.test.tsx, src/shared/lib/i18n/locales/es-MX/settings.json, src/shared/lib/i18n/locales/en-US/settings.json</files>
  <read_first>
    - src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx (full — the dirty-tracking save-state-machine analog: useState form + dirty flag, save() with toast.error/success, POSButton disabled until dirty)
    - src/features/edit-staff-role/ui/EditRoleDialog.tsx lines 34, 100-135 (enum-driven Select: `const ROLE_OPTIONS = UserRoleSchema.options` → adapt to LocaleSchema.options)
    - src/entities/staff/model/store.ts (read currentStaff for id + current locale; import the i18n singleton for changeLanguage)
    - 21-UI-SPEC.md Copywriting Contract (exact es-MX/en-US strings + keys) + Interaction Contract — Settings → Language tab (5 steps) + the typography note (heading uses text-lg font-medium, NOT font-semibold)
    - useMutationUpdateStaffLocale in src/entities/staff/model/queries.ts (created in 21-02)
  </read_first>
  <behavior>
    - Test: renders a Select pre-selected to the current staff's locale (from the staff store).
    - Test: changing the Select marks dirty; Save button enabled only when dirty.
    - Test: on successful mutation, i18n.changeLanguage is called with the chosen locale and a success toast fires (mock the mutation + i18n).
    - Test: on failed mutation, toast.error fires and the selection is not reverted (form stays dirty).
  </behavior>
  <action>
    Create `LanguageSettingsTab.tsx` cloning GeneralSettingsTab's dirty-tracking save form, swapping the multi-field form for a single locale Select. Source the current staff (`useStaffStore(s => s.currentStaff)`) for `staffId` and the pre-selected locale. Options come from `LocaleSchema.options`. On Save: call `useMutationUpdateStaffLocale().mutateAsync({ staffId: currentStaff.id, locale })`; on `!result.ok` → `toast.error(t('settings:language.saveError'))`, leave dirty; on success → `i18n.changeLanguage(locale)`, `toast.success(t('settings:language.saveSuccess'))`, `setDirty(false)` (UI-SPEC steps 2-5; do NOT call changeLanguage before Save — avoids a half-saved state). Use `useTranslation('settings')` for all copy. Heading: `text-lg font-medium` (UI-SPEC typography deviation — NOT font-semibold). Do NOT wrap in `ProtectedAction` (D-03 — must be usable by every role). Save button = POSButton `touchSize="large"`, label `t('settings:language.save')` / `t('settings:language.saving')` while pending.

    Populate `settings.json` (both locales) with the Language-tab keys from UI-SPEC's copy table: `tabs.language`, `language.heading`, `language.fieldLabel`, `language.option.esMX`, `language.option.enUS`, `language.helper`, `language.save`, `language.saving`, `language.saveSuccess`, `language.saveError`. Use the UI-SPEC es-MX (Spanish) values for es-MX and the en-US values for en-US — these are NEW elements not in the visual baseline, so proper Spanish here is safe. Add the four behavior tests.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.test.tsx && npm run typecheck</automated>
  </verify>
  <done>LanguageSettingsTab renders + saves + switches language; no ProtectedAction wrapper; settings.json seeded with UI-SPEC copy; 4 tests pass.</done>
  <acceptance_criteria>
    - `npx vitest run src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.test.tsx` exits 0.
    - `grep -n "ProtectedAction" src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.tsx` returns nothing.
    - settings.json (both locales) contains all 10 UI-SPEC Language keys.
    - `npm run lint:i18n -- src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.tsx` exits 0 (no hardcoded strings).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restructure SettingsTabsPanel — role-agnostic Language tab + migrate labels</name>
  <files>src/widgets/SettingsTabsPanel/index.tsx, src/shared/lib/i18n/locales/es-MX/settings.json, src/shared/lib/i18n/locales/en-US/settings.json</files>
  <read_first>
    - src/widgets/SettingsTabsPanel/index.tsx (full — the two role-gated if-blocks building `tabs`, the firstTab/fallback logic at lines 103-111)
    - 21-PATTERNS.md "SettingsTabsPanel — role-agnostic tab group" (required change: an unconditional out.push OUTSIDE both if-blocks)
    - 21-UI-SPEC.md Locked decision table (self-service = always visible once logged in)
  </read_first>
  <action>
    Add a third, unconditional tab entry to the `tabs` useMemo — pushed OUTSIDE both `if (canManageSettings)` and `if (canManageProducts)` blocks — `{ key: 'language', label: t('settings:tabs.language'), render: () => <LanguageSettingsTab /> }`. Put it first in the array so it is the default tab for a bartender (whose other blocks are empty), guaranteeing `firstTab` is non-empty for every authenticated role and the "no permission" fallback (lines 104-109) never blocks a logged-in bartender from the Language tab. Import `useTranslation` and `LanguageSettingsTab`.

    Migrate SettingsTabsPanel's OWN hardcoded strings to the `settings` namespace via `t()`: the existing tab labels ('General','Hardware','Rappi','Email Receipts','Backup','Tip Split','Products','Pool Tables','Billing','Combos','Promotions','Ingredients') → keys like `settings:tabs.general` etc., and the fallback message 'You do not have permission to view settings.' → `settings:noPermission`. es-MX values MUST be the exact current literals byte-for-byte (English preserved — zero visual regression per the phase catalog rule); en-US values are the English equivalents (identical here since all are already English). Add these keys to settings.json (both locales).

    Add the `settings` namespace to the `tabs` useMemo dependency array is not required (t is stable), but include `t` per react-hooks/exhaustive-deps if the linter flags it.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run lint:i18n -- src/widgets/SettingsTabsPanel/index.tsx</automated>
  </verify>
  <done>Language tab renders for all roles; existing labels + fallback migrated to settings ns with byte-identical es-MX; typecheck + lint + scoped i18n lint clean.</done>
  <acceptance_criteria>
    - `npm run lint:i18n -- src/widgets/SettingsTabsPanel/index.tsx` exits 0 (no hardcoded strings remain in this file).
    - The `language` tab entry is pushed outside both role-gated if-blocks (verify by reading the diff).
    - es-MX settings.json tab-label values equal the pre-migration literals exactly (e.g. `tabs.general` === 'General').
    - `npm run lint` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bartender/any role → own profiles.locale | Self-service write to the caller's own locale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03a | Elevation of Privilege | LanguageSettingsTab write | medium | mitigate | The self-service tab writes ONLY the current staff's own row (staffId = currentStaff.id from the authenticated store); it cannot target another staff member (that is 21-04's admin-gated path). No new RLS surface — reuses the 21-02 audited mutation |
| T-21-04b | Tampering | translated tab labels → JSX | low | mitigate | Rendered as JSX children (React-escaped); escapeValue:false rationale from 21-01 holds |
</threat_model>

<verification>
- `npx vitest run src/widgets/SettingsTabsPanel/tabs/LanguageSettingsTab.test.tsx` passes.
- `npm run lint:i18n -- src/widgets/SettingsTabsPanel` exits 0 (both index.tsx + LanguageSettingsTab.tsx clean).
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- Self-service Language tab visible to every authenticated role (D-03, Pitfall 1 resolved).
- Save persists locale (SC-2) and switches the live UI (SC-4 no-reload behavior).
- SettingsTabsPanel's own strings migrated with zero visual regression.
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-03-SUMMARY.md` when done. Note which settings-ns keys this plan owns (Language tab + panel labels) so the 21-11 admin sweep does not re-touch index.tsx/LanguageSettingsTab.
</output>
