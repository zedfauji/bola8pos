---
phase: 21-i18n-multi-language
plan: 08
type: execute
wave: 4
depends_on: ["21-01"]
files_modified:
  - src/features/add-waitlist-entry/**
  - src/features/adjust-inventory/**
  - src/features/adjust-stock-movement/**
  - src/features/agent-chat/**
  - src/features/clock-in-staff/**
  - src/features/clock-out-staff/**
  - src/features/edit-staff-role/**
  - src/features/export-report/**
  - src/features/force-pin-change/**
  - src/features/import-ingredients-csv/**
  - src/features/manage-categories/**
  - src/features/manage-combos/**
  - src/features/manage-ingredients/**
  - src/features/manage-modifier-groups/**
  - src/features/manage-modifier-inventory-rules/**
  - src/features/manage-products/**
  - src/features/manage-promotions/**
  - src/features/manage-recipe/**
  - src/features/mark-waitlist-entry-cancelled/**
  - src/features/mark-waitlist-no-show/**
  - src/features/notify-waitlist/**
  - src/features/override-negative-stock/**
  - src/features/physical-count/**
  - src/features/produce-prep-batch/**
  - src/features/seat-waitlist-party/**
  - src/features/toggle-permission/**
  - src/features/upload-logo/**
  - src/shared/lib/i18n/locales/es-MX/featMgmt.json
  - src/shared/lib/i18n/locales/en-US/featMgmt.json
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "npm run lint:i18n exits 0 across all 27 management/staff/inventory/waitlist feature folders in this plan"
    - "edit-staff-locale is NOT touched here (owned by 21-04)"
    - "every migrated es-MX featMgmt value equals the pre-migration literal byte-for-byte"
  artifacts:
    - "featMgmt namespace populated with admin/inventory/staff/waitlist feature strings"
  key_links:
    - "component UIs use useTranslation('featMgmt'); hook toasts use i18n.t('featMgmt:...')"
---

<objective>
Big-bang string sweep (D-04) of the management / inventory / staff-ops / waitlist / catalog-admin FEATURE cluster into the `featMgmt` namespace, driven by `lint:i18n`. One of six parallel Wave-4 sweeps; owns a disjoint feature-folder set + the `featMgmt` catalog exclusively. Explicitly excludes `features/edit-staff-locale` (owned by 21-04) and `features/edit-staff-role`'s dialog is in scope here (it is not owned elsewhere).

Catalog rule (from 21-01): es-MX = exact current literal byte-for-byte (SC-4); en-US = English rendering. Contributes SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@src/shared/lib/i18n/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep catalog-admin + inventory features → featMgmt</name>
  <files>src/features/manage-categories/**, src/features/manage-combos/**, src/features/manage-ingredients/**, src/features/manage-modifier-groups/**, src/features/manage-modifier-inventory-rules/**, src/features/manage-products/**, src/features/manage-promotions/**, src/features/manage-recipe/**, src/features/adjust-inventory/**, src/features/adjust-stock-movement/**, src/features/override-negative-stock/**, src/features/physical-count/**, src/features/import-ingredients-csv/**, src/features/produce-prep-batch/**, src/shared/lib/i18n/locales/es-MX/featMgmt.json, src/shared/lib/i18n/locales/en-US/featMgmt.json</files>
  <read_first>
    - src/shared/lib/i18n/index.ts (singleton for hook toasts)
    - 21-RESEARCH.md Pattern 3 + Pitfall 5 + Don't Hand-Roll (interpolation over concatenation)
  </read_first>
  <action>
    Run `npm run lint:i18n --` over the listed folders, fix each violation to `t('featMgmt:<key>')` (components, via useTranslation('featMgmt')) or `i18n.t('featMgmt:<key>')` (hook toasts). Keys grouped by feature. es-MX = exact current literal; en-US = English. Use interpolation for embedded values. SKIP className/data-testid/enum/symbol + test/story files.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Catalog-admin + inventory feature violations fixed; featMgmt.json seeded (byte-identical es-MX).</done>
  <acceptance_criteria>
    - `npm run typecheck` + `npm run lint` exit 0.
    - featMgmt.json es-MX/en-US top-level key parity holds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep staff-ops + waitlist + misc features → featMgmt</name>
  <files>src/features/clock-in-staff/**, src/features/clock-out-staff/**, src/features/edit-staff-role/**, src/features/force-pin-change/**, src/features/toggle-permission/**, src/features/add-waitlist-entry/**, src/features/mark-waitlist-entry-cancelled/**, src/features/mark-waitlist-no-show/**, src/features/notify-waitlist/**, src/features/seat-waitlist-party/**, src/features/export-report/**, src/features/upload-logo/**, src/features/agent-chat/**, src/shared/lib/i18n/locales/es-MX/featMgmt.json, src/shared/lib/i18n/locales/en-US/featMgmt.json</files>
  <read_first>
    - src/shared/lib/i18n/locales/es-MX/featMgmt.json (Task-1 keys — reuse before adding)
  </read_first>
  <action>
    Same fix loop on the listed folders. Do NOT touch `src/features/edit-staff-locale` (21-04). Reuse existing featMgmt keys where wording matches. es-MX byte-identical.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Staff-ops + waitlist + misc features migrated; featMgmt reconciled; edit-staff-locale untouched.</done>
  <acceptance_criteria>
    - `npm run typecheck` + `npm run lint` exit 0.
    - `git status --porcelain src/features/edit-staff-locale` shows no changes from this plan.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove zero violations across the mgmt cluster + reconcile catalog</name>
  <files>src/shared/lib/i18n/locales/es-MX/featMgmt.json, src/shared/lib/i18n/locales/en-US/featMgmt.json</files>
  <read_first>
    - both featMgmt.json files (1:1 key parity, no orphans)
  </read_first>
  <action>
    Run `npm run lint:i18n` over all 27 folders of this plan; fix stragglers. Reconcile featMgmt.json parity + byte-identical es-MX values.
  </action>
  <verify>
    <automated>npm run lint:i18n -- src/features/add-waitlist-entry src/features/adjust-inventory src/features/adjust-stock-movement src/features/agent-chat src/features/clock-in-staff src/features/clock-out-staff src/features/edit-staff-role src/features/export-report src/features/force-pin-change src/features/import-ingredients-csv src/features/manage-categories src/features/manage-combos src/features/manage-ingredients src/features/manage-modifier-groups src/features/manage-modifier-inventory-rules src/features/manage-products src/features/manage-promotions src/features/manage-recipe src/features/mark-waitlist-entry-cancelled src/features/mark-waitlist-no-show src/features/notify-waitlist src/features/override-negative-stock src/features/physical-count src/features/produce-prep-batch src/features/seat-waitlist-party src/features/toggle-permission src/features/upload-logo && npm run typecheck && npm run lint</automated>
  </verify>
  <done>lint:i18n exits 0 across all 27 folders; catalog parity holds.</done>
  <acceptance_criteria>
    - The Task-3 lint:i18n command exits 0.
    - featMgmt.json es-MX/en-US key parity verified.
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| featMgmt catalog → feature JSX/toasts | Translation strings become admin/inventory UI text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | featMgmt strings → JSX/toast | low | mitigate | React-escaped output; no HTML sink; migration is source-only, behavior unchanged |
</threat_model>

<verification>
- `npm run lint:i18n` exits 0 across all 27 folders.
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- Management/staff/inventory/waitlist feature cluster fully migrated to featMgmt (D-04, SC-4), zero visual regression.
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-08-SUMMARY.md` when done.
</output>
