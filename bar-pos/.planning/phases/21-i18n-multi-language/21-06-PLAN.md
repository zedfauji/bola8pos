---
phase: 21-i18n-multi-language
plan: 06
type: execute
wave: 4
depends_on: ["21-01"]
files_modified:
  - src/shared/ui/**/*.tsx
  - src/shared/lib/i18n/locales/es-MX/common.json
  - src/shared/lib/i18n/locales/en-US/common.json
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "npm run lint:i18n -- src/shared/ui exits 0 — no hardcoded user-facing string remains in any shared/ui component"
    - "Every migrated es-MX common value equals the pre-migration on-screen literal byte-for-byte"
  artifacts:
    - "common namespace populated with shared/ui primitive strings"
  key_links:
    - "shared/ui components call useTranslation('common'); the reactive re-render on changeLanguage flows through them into every consumer"
---

<objective>
Big-bang string sweep of the `shared/ui` layer (D-04) into the `common` namespace. Every hardcoded user-facing string in `src/shared/ui/**/*.tsx` becomes a `t('common:...')` lookup, driven by the `lint:i18n` violation report (the tool enumerates exactly what to fix and verifies completeness).

Purpose: shared/ui primitives (banners, dialogs, empty/error states, confirm dialogs, data-table chrome) render text seen across the whole app; migrating them to `common` gives every consumer a translated primitive with zero import changes. This is one of six parallel Wave-4 sweeps, each owning a DISJOINT file set + its OWN namespace catalog (no cross-sweep file or JSON conflict).

Catalog rule (phase-wide, from 21-01): es-MX value = the EXACT current literal byte-for-byte (English preserved — zero visual regression, SC-4); en-US value = the English rendering (identical where the current literal is already English). Contributes SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@src/shared/lib/i18n/index.ts
@src/widgets/SettingsTabsPanel/tabs/GeneralSettingsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep shared/ui primitives (feedback/dialog/state) → common</name>
  <files>src/shared/ui/**/*.tsx, src/shared/lib/i18n/locales/es-MX/common.json, src/shared/lib/i18n/locales/en-US/common.json</files>
  <read_first>
    - src/shared/lib/i18n/index.ts (the singleton — for non-component i18n.t; component files use useTranslation from 'react-i18next')
    - 21-RESEARCH.md Pitfall 5 (mode:'all' catches JSX text + attributes + call args) + the jsx-attributes/callees/words exclude lists in eslint.i18n.config.js (what the rule deliberately ignores)
    - Any GeneralSettingsTab.tsx usage of useTranslation is not yet present — follow the standard react-i18next hook form: `const { t } = useTranslation('common');`
  </read_first>
  <action>
    Run `npm run lint:i18n -- src/shared/ui` to get the exact list of `i18next/no-literal-string` violations (file:line + the offending literal). Fix each: in a component render, wrap the literal in `{t('common:<key>')}` (add `const { t } = useTranslation('common');`); for a literal passed to `toast.*`/`logger`/non-component code, use the imported singleton `i18n.t('common:<key>')`. Choose stable dot-path keys grouped by primitive (e.g. `offlineBanner.message`, `errorBoundary.title`, `confirmDialog.cancel`, `dataTable.empty`). REUSE the seeded `common:actions.{save,cancel,saving}` where a primitive shows exactly those words. For each new key add BOTH locales to common.json: es-MX = the exact current literal (byte-for-byte, even if English); en-US = the English rendering. SKIP: `className`, `data-testid`, `key`, `role`, `variant`, `type`, technical enum strings, and any symbol-only/numeric literal (the rule already excludes these; if a false positive slips through, add it to the eslint.i18n.config.js words/jsx-attributes exclude rather than translating it). SKIP test/story files (excluded by the config). Process roughly the first half of the violating files here; the second half is Task 2. Do NOT touch files owned by other plans — shared/ui has none owned elsewhere.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>All violations in the first batch of shared/ui files fixed; keys added to common.json (both locales) with byte-identical es-MX; typecheck + lint clean.</done>
  <acceptance_criteria>
    - `npm run typecheck` + `npm run lint` exit 0.
    - Every key added to es-MX common.json has an identical-or-English counterpart in en-US common.json (1:1 key parity).
    - No new key's es-MX value differs from the literal it replaced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Finish shared/ui sweep + prove zero violations</name>
  <files>src/shared/ui/**/*.tsx, src/shared/lib/i18n/locales/es-MX/common.json, src/shared/lib/i18n/locales/en-US/common.json</files>
  <read_first>
    - src/shared/lib/i18n/locales/es-MX/common.json (current keys from Task 1 — reuse before inventing new keys)
    - 21-RESEARCH.md Don't Hand-Roll (pluralization via t('key',{count}); interpolation via {{var}} — use these instead of string concatenation where a literal embeds a value)
  </read_first>
  <action>
    Repeat the Task-1 fix loop on the remaining shared/ui violations until `npm run lint:i18n -- src/shared/ui` exits 0. Where a literal embeds a dynamic value (e.g. `` `${n} items` ``), use i18next interpolation (`t('common:itemCount', { count: n })`) rather than concatenation (RESEARCH Don't Hand-Roll). Reconcile common.json: ensure es-MX and en-US have identical key sets (1:1 parity), no orphan keys, and every es-MX value equals its original literal.
  </action>
  <verify>
    <automated>npm run lint:i18n -- src/shared/ui && npm run typecheck && npm run lint</automated>
  </verify>
  <done>`npm run lint:i18n -- src/shared/ui` exits 0; common.json es-MX/en-US key parity holds; typecheck + lint clean.</done>
  <acceptance_criteria>
    - `npm run lint:i18n -- src/shared/ui` exits 0 (zero hardcoded strings in shared/ui).
    - `node -e "const a=require('./src/shared/lib/i18n/locales/es-MX/common.json'),b=require('./src/shared/lib/i18n/locales/en-US/common.json'); const ka=JSON.stringify(Object.keys(a).sort()),kb=JSON.stringify(Object.keys(b).sort()); if(ka!==kb) process.exit(1)"` exits 0 (top-level key parity; nested parity verified by review).
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| common catalog → shared/ui JSX | Translation strings become DOM text across every screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | common translation strings → JSX | low | mitigate | Strings render as React-escaped JSX children; no dangerouslySetInnerHTML introduced; escapeValue:false safe per 21-01 rationale. String migration introduces no new trust boundary |
</threat_model>

<verification>
- `npm run lint:i18n -- src/shared/ui` exits 0.
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- shared/ui fully migrated to common namespace (D-04, SC-4) with zero visual regression (es-MX byte-identical).
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-06-SUMMARY.md` when done. Record the common-ns key structure so other sweeps reuse (not duplicate) shared button/status words.
</output>
