---
phase: 21-i18n-multi-language
plan: 11
type: execute
wave: 4
depends_on: ["21-01"]
files_modified:
  - src/entities/**/*.ts
  - src/entities/**/*.tsx
  - src/pages/**/*.tsx
  - src/shared/lib/i18n/locales/es-MX/entities.json
  - src/shared/lib/i18n/locales/en-US/entities.json
  - src/shared/lib/i18n/locales/es-MX/pages.json
  - src/shared/lib/i18n/locales/en-US/pages.json
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "npm run lint:i18n exits 0 across src/entities and src/pages"
    - "entity-layer literals (toasts, ui cards) → entities ns; page-container literals (incl. the Reports page tab labels) → pages ns"
    - "every migrated es-MX value equals the pre-migration literal byte-for-byte"
  artifacts:
    - "entities + pages namespaces populated"
  key_links:
    - "entity model .ts hooks use i18n.t('entities:...'); entity/page components use useTranslation(ns)"
---

<objective>
Big-bang string sweep (D-04) of the `entities` and `pages` layers into the `entities` and `pages` namespaces respectively, driven by `lint:i18n`. Final Wave-4 sweep; disjoint layer directories + own catalogs. Note the pages layer is mostly thin route containers, but the Reports page carries its ~12 report-tab labels — the lint-driven loop bounds the work regardless of density.

Catalog rule (from 21-01): es-MX = exact current literal byte-for-byte (SC-4); en-US = English. Contributes SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@src/shared/lib/i18n/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep entities layer → entities ns</name>
  <files>src/entities/**/*.ts, src/entities/**/*.tsx, src/shared/lib/i18n/locales/es-MX/entities.json, src/shared/lib/i18n/locales/en-US/entities.json</files>
  <read_first>
    - src/shared/lib/i18n/index.ts (singleton — most entity strings are toast/logger literals in model/*.ts hooks, which are non-component → i18n.t)
    - 21-RESEARCH.md Pattern 3 + Pitfall 5
  </read_first>
  <action>
    Run `npm run lint:i18n -- src/entities` to enumerate violations. Entity model `.ts` files (queries/store) fire toasts from hooks — use `i18n.t('entities:<key>')` (import the singleton). Entity `ui/*.tsx` card components use `useTranslation('entities')`. Do NOT touch `src/entities/staff/model/*` strings that 21-02 already handled (mutation is code, not user copy — but any pre-existing toast literals there are in scope; reuse keys if identical). es-MX = exact current literal; en-US = English. SKIP className/data-testid/enum/symbol + test/story files. Fix until `npm run lint:i18n -- src/entities` exits 0.
  </action>
  <verify>
    <automated>npm run lint:i18n -- src/entities && npm run typecheck && npm run lint</automated>
  </verify>
  <done>entities layer lint:i18n exits 0; entities.json seeded byte-identical es-MX + key parity.</done>
  <acceptance_criteria>
    - `npm run lint:i18n -- src/entities` exits 0.
    - entities.json es-MX/en-US top-level key parity holds.
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep pages layer → pages ns</name>
  <files>src/pages/**/*.tsx, src/shared/lib/i18n/locales/es-MX/pages.json, src/shared/lib/i18n/locales/en-US/pages.json</files>
  <read_first>
    - src/shared/lib/i18n/index.ts (singleton)
    - src/pages/reports (the Reports page tab-label array is the densest page — migrate each label with byte-identical es-MX)
  </read_first>
  <action>
    Run `npm run lint:i18n -- src/pages` to enumerate violations. Page containers use `useTranslation('pages')`. Migrate page titles, section headers, and the Reports page's ~12 tab labels to `pages` ns with byte-identical es-MX. en-US = English. Fix until `npm run lint:i18n -- src/pages` exits 0. Reconcile pages.json parity.
  </action>
  <verify>
    <automated>npm run lint:i18n -- src/pages && npm run typecheck && npm run lint</automated>
  </verify>
  <done>pages layer lint:i18n exits 0; pages.json seeded byte-identical es-MX + key parity.</done>
  <acceptance_criteria>
    - `npm run lint:i18n -- src/pages` exits 0.
    - pages.json es-MX/en-US top-level key parity holds.
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entities/pages catalogs → JSX/toasts | Translation strings become entity + page UI text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | entities/pages strings → JSX | low | mitigate | React-escaped output; no HTML sink; source-only migration |
</threat_model>

<verification>
- `npm run lint:i18n -- src/entities` and `-- src/pages` both exit 0.
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- entities + pages layers fully migrated (D-04, SC-4), zero visual regression. With this plan, all five FSD layers are string-clean under the i18n rule.
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-11-SUMMARY.md` when done. This completes the big-bang migration surface — 21-12 turns the rule on repo-wide.
</output>
