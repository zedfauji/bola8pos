---
phase: 21-i18n-multi-language
plan: 13
type: execute
wave: 6
depends_on: ["21-12"]
files_modified:
  - e2e/46-i18n-locale-switch.spec.ts
  - CLAUDE.md
autonomous: false
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "e2e/46-i18n-locale-switch.spec.ts switches locale in Settings → Language and asserts a real string re-renders from Spanish (es-MX default) to English (en-US)"
    - "the Phase 34 visual baseline (45-visual-baseline.spec.ts) re-runs with zero diffs against the existing es-MX-default snapshots (no rebaseline)"
    - "CLAUDE.md documents the i18n stack, namespace scheme, the profiles.locale feature, and the enforcement rule"
  artifacts:
    - e2e/46-i18n-locale-switch.spec.ts
  key_links:
    - "the visual baseline is the SC-4 no-regression gate; zero diffs confirms es-MX preserved every existing string byte-for-byte"
---

<objective>
Close the phase with end-to-end proof: a new Playwright spec exercising the en-US locale switch through the real Settings → Language surface (SC-1/SC-4 behavior), a no-regression re-run of the Phase 34 visual baseline (SC-4), documentation, and a human UAT checkpoint.

Purpose: SC-4's visual-regression contract is enforced by re-running `e2e/visual/45-visual-baseline.spec.ts` (Phase 34's baseline) against the migrated app in its default es-MX locale — because every migrated es-MX catalog value equals the pre-migration literal, the run must produce ZERO diffs. Any diff signals a migration error (a string that changed). The new locale-switch spec proves the en-US catalog actually renders (the switch changes a Spanish default string to English on a real page).

Output: `e2e/46-i18n-locale-switch.spec.ts`; green visual baseline; updated CLAUDE.md; human sign-off. Closes SC-1 (catalogs render), SC-4 (no regression).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-UI-SPEC.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@e2e/helpers/auth.ts
@e2e/visual/45-visual-baseline.spec.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e/46-i18n-locale-switch.spec.ts (en-US end-to-end)</name>
  <files>e2e/46-i18n-locale-switch.spec.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (loginAs(page, 'admin') — admin PIN 0000, per CLAUDE.md)
    - 21-UI-SPEC.md Copywriting Contract (the Language tab is the cleanest observable: es-MX default renders 'Idioma'/'Idioma de la interfaz'; en-US renders 'Language'/'Interface language')
    - 21-UI-SPEC.md Interaction Contract — Settings → Language tab (switch + Save → i18n.changeLanguage → immediate re-render, no reload)
    - an existing spec (e.g. e2e/08-settings-receipt.spec.ts) for the Settings-page navigation pattern
  </read_first>
  <action>
    Write `e2e/46-i18n-locale-switch.spec.ts`: loginAs(page,'admin') → navigate to /settings → open the Language tab (default es-MX label 'Idioma') → assert the field label reads the Spanish default ('Idioma de la interfaz'). Select the en-US option, click Save, and assert (a) a success toast appears and (b) the tab/heading/field label now render the English strings ('Language' / 'Interface language') WITHOUT a page reload — proving `i18n.changeLanguage` re-rendered subscribed components (UI-SPEC step 4). Reset back to es-MX at the end so the test leaves the admin profile at the default (avoid polluting the shared test account's locale for other specs). Follow the existing e2e style (getByRole/getByText, no hardcoded waits where a locator assertion suffices).
  </action>
  <verify>
    <automated>npx playwright test e2e/46-i18n-locale-switch.spec.ts</automated>
  </verify>
  <done>Spec logs in, switches to en-US via Settings → Language, asserts the English re-render + toast, resets to es-MX; passes.</done>
  <acceptance_criteria>
    - `npx playwright test e2e/46-i18n-locale-switch.spec.ts` passes (requires dev server + .env.local E2E creds per CLAUDE.md).
    - The spec asserts BOTH the pre-switch Spanish label and the post-switch English label on the same run.
    - The spec restores the admin locale to es-MX before finishing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Visual-regression no-regression gate (SC-4) + CLAUDE.md docs</name>
  <files>CLAUDE.md</files>
  <read_first>
    - e2e/visual/45-visual-baseline.spec.ts (Phase 34 baseline — re-run, DO NOT rebaseline)
    - CLAUDE.md (the "Implemented Features" list + the E2E spec list — add i18n entries; the "Commands" / stack tables — add the i18n note)
    - 21-RESEARCH.md Validation Architecture (SC-4: re-run 45-visual-baseline, no rebaseline unless a deliberate visual change is confirmed)
  </read_first>
  <action>
    Run `npm run test:e2e:visual` (Phase 34's baseline config). Because the default locale is es-MX and every migrated es-MX value equals its pre-migration literal, the run MUST produce zero diffs. If any diff appears, investigate the offending screenshot: it means a migrated es-MX string changed the rendered text — fix the catalog value to match the original literal (do NOT rebaseline to hide it). Only rebaseline if a diff is a deliberate, confirmed new element (there should be none — the two new surfaces, Settings Language tab + Staff locale column, are on routes; if the baseline covers /settings or /staff and the new tab/column shifts layout, that is a legitimate new-element diff — surface it in Task 3's checkpoint for human confirmation before any rebaseline).

    Update CLAUDE.md: add an "i18n / Multi-Language" subsection documenting the `react-i18next` + `i18next` stack, the singleton location (`src/shared/lib/i18n/`, shared layer for FSD reasons), the 10-namespace scheme (common/featOrders/featMgmt/wPanels/wAdmin/entities/pages/settings/staff/receipt), the `profiles.locale` per-staff preference (D-01/D-02), the phase catalog rule (es-MX = current literal, en-US = English; full Spanish content translation is future work), and the committed `i18next/no-literal-string` enforcement (D-05). Add `e2e/46-i18n-locale-switch.spec.ts` to the E2E spec list and a bullet to "Implemented Features".
  </action>
  <verify>
    <automated>npm run test:e2e:visual</automated>
  </verify>
  <done>Visual baseline re-run shows zero diffs (or only human-confirmed new-element diffs from Task 3); CLAUDE.md documents the i18n system.</done>
  <acceptance_criteria>
    - `npm run test:e2e:visual` exits 0 (zero unexpected diffs) — this is the SC-4 gate.
    - CLAUDE.md contains an i18n subsection with the namespace list + enforcement note, and lists 46-i18n-locale-switch.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — locale switch + no visual regression</name>
  <what-built>
    Full i18n system: react-i18next wired with es-MX/en-US catalogs; a self-service Language tab in Settings (all roles); an admin per-staff locale field on /staff; locale-aware receipts/PDFs; committed ESLint enforcement; passing en-US e2e + zero-diff visual baseline.
  </what-built>
  <action>Pause for human UAT of the locale switch, raw-key check, and es-MX visual parity per the steps below. Do NOT mark the phase complete until the human approves; if they report wrong screens/strings or raw keys, route the fixes back to the owning sweep/surface plan before re-requesting sign-off.</action>
  <how-to-verify>
    1. Run the app (`npm run tauri dev`). Log in as admin (PIN 0000).
    2. Visit /settings → Language tab. Confirm it renders in Spanish by default (es-MX: 'Idioma'). Confirm the tab is visible even when logged in as a bartender (log out, log in as bartender, /settings should show the Language tab despite no manage_settings).
    3. As admin, switch the language to English (en-US), Save. Confirm the UI switches to English immediately (no reload) and a success toast appears. Spot-check 3-4 screens (POS, Reports, Staff, a dialog) look correct in English and that nothing shows a raw `key` string (e.g. `common:actions.save`) — a raw key means a missing catalog entry.
    4. Switch back to es-MX and confirm the app matches how it looked BEFORE this phase (zero visual regression — the reason the automated baseline passed).
    5. On /staff, use "Change language" on another staff member (as admin) and confirm the Language column badge updates; confirm the acting admin's own language did NOT change.
    6. (Optional, if a thermal/PDF path is testable) print a pre-cheque or export a report PDF as an en-US staff and confirm labels are English.
  </how-to-verify>
  <resume-signal>Type "approved" if language switching works, no raw keys appear, and es-MX matches the pre-phase look; otherwise describe the screens/strings that are wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e/visual gate → release | The visual baseline governs whether a string-changing regression can ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Tampering | rebaselining to hide a real regression | medium | mitigate | Task 2 forbids rebaselining to mask a diff; any legitimate new-element diff is surfaced to the human checkpoint (Task 3) before acceptance |
| T-21-04e | Tampering | translated strings rendered in the live app | low | mitigate | Human spot-check (Task 3 step 3) confirms no raw keys / no HTML injection in rendered strings |
</threat_model>

<verification>
- `npx playwright test e2e/46-i18n-locale-switch.spec.ts` passes.
- `npm run test:e2e:visual` exits 0 (zero unexpected diffs — SC-4).
- Human UAT approved (Task 3).
- CLAUDE.md updated.
</verification>

<success_criteria>
- en-US catalog proven to render end-to-end (SC-1).
- Zero visual regression confirmed by baseline re-run + human spot-check (SC-4).
- i18n system documented; phase complete.
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-13-SUMMARY.md` when done. Record the visual-baseline result (diff count), the e2e result, and the human UAT sign-off.
</output>
