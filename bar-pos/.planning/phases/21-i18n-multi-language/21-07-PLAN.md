---
phase: 21-i18n-multi-language
plan: 07
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03", "21-04", "21-05"]
files_modified:
  - src/features/add-combo-to-tab/**
  - src/features/add-item-to-tab/**
  - src/features/assign-pool-session-to-tab/**
  - src/features/bump-kds-item/**
  - src/features/close-tab/**
  - src/features/edit-session-start-time/**
  - src/features/lookup-product-by-barcode/**
  - src/features/manager-pin-gate/**
  - src/features/open-tab/**
  - src/features/print-precheque/**
  - src/features/process-payment/**
  - src/features/process-refund/**
  - src/features/register-caja-entry/**
  - src/features/remove-item-from-tab/**
  - src/features/remove-tab-item/**
  - src/features/split-tab/**
  - src/features/start-pool-timer/**
  - src/features/stop-and-move-table/**
  - src/features/stop-pool-timer/**
  - src/features/transfer-tab/**
  - src/features/void-order/**
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/shared/lib/i18n/locales/en-US/featOrders.json
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "npm run lint:i18n exits 0 across all 21 order/pool/payment feature folders in this plan"
    - "toast literals in mutation hooks use i18n.t('featOrders:...'); JSX text in feature UIs uses useTranslation('featOrders')"
    - "every migrated es-MX featOrders value equals the pre-migration literal byte-for-byte"
  artifacts:
    - "featOrders namespace populated with ordering/pool/payment feature strings"
  key_links:
    - "non-component hook toasts import the i18n singleton; component UIs use the react-i18next hook"
---

<objective>
Big-bang string sweep (D-04) of the ordering / pool-timer / payment / refund / split / void / caja-entry FEATURE cluster into the `featOrders` namespace, driven by the `lint:i18n` violation report. One of six parallel Wave-4 sweeps; owns a disjoint set of feature folders + the `featOrders` catalog exclusively.

Purpose: These features carry the highest density of user-facing toast literals (process-payment, close-tab, transfer-tab, void-order, split-tab, refund) plus button/label JSX. Gated behind the Wave-3 surface plans (21-03/04/05) so single-writer files are migrated first and the BLOCKING db-push resolves before this autonomous fan-out. Catalog rule (from 21-01): es-MX = exact current literal byte-for-byte (SC-4 zero regression); en-US = English rendering. Contributes SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@src/shared/lib/i18n/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep payment/refund/tab-lifecycle features → featOrders</name>
  <files>src/features/process-payment/**, src/features/process-refund/**, src/features/split-tab/**, src/features/close-tab/**, src/features/open-tab/**, src/features/transfer-tab/**, src/features/void-order/**, src/features/add-item-to-tab/**, src/features/add-combo-to-tab/**, src/features/remove-item-from-tab/**, src/features/remove-tab-item/**, src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>
    - src/shared/lib/i18n/index.ts (singleton for non-component i18n.t in mutation hooks)
    - 21-RESEARCH.md Pattern 3 (toast.success(i18n.t(...)) in hooks — NOT useTranslation, hooks may fire outside render) + Pitfall 5 (mode:'all')
  </read_first>
  <action>
    Run `npm run lint:i18n -- src/features/process-payment src/features/process-refund src/features/split-tab src/features/close-tab src/features/open-tab src/features/transfer-tab src/features/void-order src/features/add-item-to-tab src/features/add-combo-to-tab src/features/remove-item-from-tab src/features/remove-tab-item` to enumerate violations. Fix each: JSX text / component-scope literals → `t('featOrders:<key>')` via `const { t } = useTranslation('featOrders')`; `toast.*` literal args and other non-component-scope literals → `i18n.t('featOrders:<key>')` (import the singleton). Keys grouped by feature (e.g. `payment.processing`, `payment.declined`, `refund.confirmTitle`, `transfer.success`). Where a toast embeds a value use interpolation `i18n.t('featOrders:key', { name })` not concatenation. es-MX = exact current literal; en-US = English. SKIP className/data-testid/enum/symbol literals and test/story files (config-excluded).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>All violations in the payment/refund/tab-lifecycle folders fixed; featOrders.json seeded (both locales, byte-identical es-MX); typecheck + lint clean.</done>
  <acceptance_criteria>
    - `npm run typecheck` + `npm run lint` exit 0.
    - featOrders.json es-MX/en-US top-level key parity holds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep pool-timer / kds / barcode / pin-gate / caja-entry features → featOrders</name>
  <files>src/features/start-pool-timer/**, src/features/stop-pool-timer/**, src/features/stop-and-move-table/**, src/features/assign-pool-session-to-tab/**, src/features/edit-session-start-time/**, src/features/bump-kds-item/**, src/features/lookup-product-by-barcode/**, src/features/manager-pin-gate/**, src/features/print-precheque/**, src/features/register-caja-entry/**, src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>
    - src/shared/lib/i18n/locales/es-MX/featOrders.json (Task-1 keys — reuse before adding)
  </read_first>
  <action>
    Same fix loop on the remaining folders: `npm run lint:i18n -- src/features/start-pool-timer src/features/stop-pool-timer src/features/stop-and-move-table src/features/assign-pool-session-to-tab src/features/edit-session-start-time src/features/bump-kds-item src/features/lookup-product-by-barcode src/features/manager-pin-gate src/features/print-precheque src/features/register-caja-entry`. Fix until zero. Reuse existing featOrders keys where wording matches; add new keys with byte-identical es-MX.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>Remaining order-cluster folders migrated; featOrders keys reconciled.</done>
  <acceptance_criteria>
    - `npm run typecheck` + `npm run lint` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove zero violations across the whole order cluster + reconcile catalog</name>
  <files>src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>
    - both featOrders.json files (verify 1:1 key parity, no orphans)
  </read_first>
  <action>
    Run `npm run lint:i18n` over all 21 folders of this plan at once; fix any stragglers. Reconcile featOrders.json: es-MX/en-US identical key sets, every es-MX value equals its original literal, no unused keys.
  </action>
  <verify>
    <automated>npm run lint:i18n -- src/features/add-combo-to-tab src/features/add-item-to-tab src/features/assign-pool-session-to-tab src/features/bump-kds-item src/features/close-tab src/features/edit-session-start-time src/features/lookup-product-by-barcode src/features/manager-pin-gate src/features/open-tab src/features/print-precheque src/features/process-payment src/features/process-refund src/features/register-caja-entry src/features/remove-item-from-tab src/features/remove-tab-item src/features/split-tab src/features/start-pool-timer src/features/stop-and-move-table src/features/stop-pool-timer src/features/transfer-tab src/features/void-order && npm run typecheck && npm run lint</automated>
  </verify>
  <done>lint:i18n exits 0 across all 21 folders; catalog parity holds; typecheck + lint clean.</done>
  <acceptance_criteria>
    - The Task-3 lint:i18n command exits 0.
    - featOrders.json es-MX/en-US key parity verified.
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| featOrders catalog → feature JSX/toasts | Translation strings become UI text on payment-critical flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Tampering | featOrders strings → JSX/toast | low | mitigate | React-escaped children + escaped toast text; no HTML sink; string migration adds no new trust boundary. Behavior of payment mutations unchanged (only the string source moves) |
</threat_model>

<verification>
- `npm run lint:i18n` exits 0 across all 21 folders.
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- Order/pool/payment feature cluster fully migrated to featOrders (D-04, SC-4), zero visual regression.
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-07-SUMMARY.md` when done.
</output>
