---
phase: 21-i18n-multi-language
plan: 12
type: execute
wave: 5
depends_on: ["21-03", "21-04", "21-05", "21-06", "21-07", "21-08", "21-09", "21-10", "21-11"]
files_modified:
  - eslint.config.js
  - eslint.i18n.config.js
  - package.json
autonomous: true
requirements: [SC-3, SC-4]

must_haves:
  truths:
    - "eslint.config.js commits i18next/no-literal-string at error across shared/ui + entities + features + widgets + pages (mode:'all', no grandfather list — D-05)"
    - "npm run lint (the committed CI gate) exits 0 across the whole repo — proving the big-bang migration is complete (SC-4)"
    - "a newly-introduced hardcoded UI string makes npm run lint fail (SC-3)"
    - "the standalone eslint.i18n.config.js helper + lint:i18n script are removed (enforcement now lives in the main gate)"
  artifacts:
    - "i18next block in eslint.config.js"
  key_links:
    - "the committed rule is the single source of SC-3 enforcement going forward; Phase 22+ inherits it automatically"
---

<objective>
Turn the strict, no-grandfather ESLint enforcement on repo-wide (D-05): fold the `i18next/no-literal-string` block into the committed `eslint.config.js`, run the full `npm run lint` as the phase's completeness gate (fix any stragglers the sweeps missed), and remove the standalone `eslint.i18n.config.js` helper + `lint:i18n` script now that the main gate enforces the rule.

Purpose: This is the SC-3/SC-4 enforcement plan. It must run LAST among the migration plans because committing the rule while any layer still holds a hardcoded string would fail every intermediate executor's `npm run lint` gate — which is exactly why 21-01 used a standalone helper config for the sweeps. Once every sweep + surface plan reports clean, the rule moves into the committed config and `npm run lint` passing over the whole repo is the definitive proof that the big-bang migration is complete.

Output: committed i18next block; green repo-wide `npm run lint`; helper config + script deleted. Implements D-05, closes SC-3 + SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-CONTEXT.md
@.planning/phases/21-i18n-multi-language/21-RESEARCH.md
@eslint.config.js
@eslint.i18n.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the i18next block into eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (the existing tailwindcss file-scoped block at lines ~137-182 is the structural template; note the `no-restricted-syntax` flat-config REPLACE gotcha documented there — the i18next block must NOT set `no-restricted-syntax`, so it will not wipe the barrel-export/uiDrift restatement)
    - eslint.i18n.config.js (21-01 — copy its plugin import + rule options verbatim)
    - 21-RESEARCH.md Code Examples (ESLint flat-config wiring) + Pitfall 5 (mode:'all')
  </read_first>
  <action>
    Add `import i18next from 'eslint-plugin-i18next'` to the top of eslint.config.js (alongside the other plugin imports). Append a new flat-config object (before the final `storybook.configs["flat/recommended"]` entry) that mirrors the tailwindcss block's file-scoping shape: `files: ['src/shared/ui/**/*.tsx','src/entities/**/*.{ts,tsx}','src/features/**/*.{ts,tsx}','src/widgets/**/*.{ts,tsx}','src/pages/**/*.tsx']`, `ignores: ['**/*.test.tsx','**/*.test.ts','**/*.stories.tsx','**/mocks.ts']`, `plugins: { i18next }`, and rule `'i18next/no-literal-string': ['error', { mode: 'all', 'jsx-attributes': { exclude: [...] }, callees: { exclude: [...,'t'] }, words: { exclude: [...] } }]` — copy the exact option object from eslint.i18n.config.js. Do NOT add `no-restricted-syntax` to this object (the gotcha comment in the tailwindcss block explains why: a later object restating the same key REPLACES it for matching files). This is the committed, strict, no-grandfather enforcement (D-05).
  </action>
  <verify>
    <automated>node -e "import('./eslint.config.js').then(()=>console.log('config loads')).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>eslint.config.js imports eslint-plugin-i18next and includes the scoped no-literal-string:error block; the config module loads without error.</done>
  <acceptance_criteria>
    - eslint.config.js loads without throwing.
    - `grep -n "i18next/no-literal-string" eslint.config.js` shows the committed error-level rule.
    - `grep -c "no-restricted-syntax" eslint.config.js` is unchanged from before this task (the i18next block added none).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-repo lint gate — prove the migration is complete, fix stragglers</name>
  <files>eslint.config.js</files>
  <read_first>
    - the output of `npm run lint` (this is the SC-4 completeness gate — every remaining i18next/no-literal-string error is a straggler the sweeps missed)
  </read_first>
  <action>
    Run `npm run lint`. For every `i18next/no-literal-string` error reported, migrate that literal to the appropriate namespace (`t('<ns>:<key>')` for components, `i18n.t('<ns>:<key>')` for non-component), adding the key to the matching catalog with byte-identical es-MX + English en-US — reusing the namespace already owned by the file's cluster (common/featOrders/featMgmt/wPanels/wAdmin/entities/pages/settings/staff/receipt). If a genuine false positive surfaces (a technical/symbol literal the excludes miss), add it to the rule's `words`/`jsx-attributes` exclude in eslint.config.js rather than translating it — document each such exclusion with a one-line comment. Repeat until `npm run lint` exits 0. This green repo-wide lint is the definitive proof of SC-4 (all existing strings migrated) and that SC-3's rule is active.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run test</automated>
  </verify>
  <done>`npm run lint` exits 0 repo-wide with the i18next rule active; typecheck + full unit suite green (no regressions).</done>
  <acceptance_criteria>
    - `npm run lint` exits 0 (zero i18next/no-literal-string violations anywhere in the 5 scoped layers).
    - `npm run typecheck` exits 0.
    - `npm run test` passes with no new failures vs the pre-phase baseline (the pre-existing useCloseTab.test.ts:95 failure documented in STATE.md may persist — note it, do not "fix" unrelated).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SC-3 smoke + remove the standalone helper config & script</name>
  <files>eslint.config.js, eslint.i18n.config.js, package.json</files>
  <read_first>
    - package.json scripts (remove the lint:i18n entry added in 21-01)
  </read_first>
  <action>
    SC-3 smoke: temporarily add a hardcoded `<div>DELETE_ME_HARDCODED</div>` to any file under `src/pages/`, run `npm run lint`, confirm it FAILS with an i18next/no-literal-string error on that line, then remove the temporary string and confirm `npm run lint` passes again. Then delete `eslint.i18n.config.js` and remove the `"lint:i18n"` script from package.json — enforcement now lives entirely in the committed `npm run lint` gate (single source, D-05). Leave `eslint-plugin-i18next` in devDependencies (the committed config uses it).
  </action>
  <verify>
    <automated>npm run lint && test ! -f eslint.i18n.config.js && node -e "const p=require('./package.json'); if(p.scripts['lint:i18n']) process.exit(1)"</automated>
  </verify>
  <done>SC-3 verified (deliberate hardcoded string fails lint, then removed); helper config + lint:i18n script removed; npm run lint green.</done>
  <acceptance_criteria>
    - The SC-3 smoke demonstrated a lint failure on a hardcoded string, then a pass after removal (record the evidence in the SUMMARY).
    - `eslint.i18n.config.js` no longer exists.
    - package.json has no `lint:i18n` script.
    - `npm run lint` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint config → CI gate | The committed rule governs whether hardcoded strings can re-enter the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | over-broad exclude lists silently disabling the rule | low | mitigate | Excludes are copied verbatim from the reviewed helper config; any new exclude added for a false positive is commented and justified in eslint.config.js — no blanket disables |
</threat_model>

<verification>
- `npm run lint` exits 0 repo-wide with i18next rule active (SC-3/SC-4 gate).
- SC-3 smoke: a hardcoded string fails lint; removal restores green.
- `eslint.i18n.config.js` + `lint:i18n` script removed.
- `npm run typecheck` + `npm run test` green.
</verification>

<success_criteria>
- Strict no-grandfather ESLint rule committed repo-wide (D-05, SC-3).
- Full-repo lint green proves every existing string migrated (SC-4).
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-12-SUMMARY.md` when done. Record the SC-3 smoke evidence, any exclude-list additions with justification, and confirm the helper config/script removal.
</output>
