---
phase: 21-i18n-multi-language
plan: 04
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/features/edit-staff-locale/ui/EditLocaleDialog.tsx
  - src/features/edit-staff-locale/index.ts
  - src/features/edit-staff-locale/ui/EditLocaleDialog.test.tsx
  - src/widgets/StaffDashboard/StaffDashboard.tsx
  - src/shared/lib/i18n/locales/es-MX/staff.json
  - src/shared/lib/i18n/locales/en-US/staff.json
autonomous: true
requirements: [SC-2, SC-4]

must_haves:
  truths:
    - "The Staff table shows a Language column (Badge variant=outline, es-MX/en-US) for each staff member"
    - "A per-row 'Change language' action, gated manage_staff, opens EditLocaleDialog and writes the target staff's profiles.locale"
    - "EditLocaleDialog does NOT call i18n.changeLanguage on the acting admin — only the target's stored locale changes"
    - "All pre-existing StaffDashboard strings (column headers, buttons, section header, search placeholder) render via t() from the staff namespace with es-MX byte-identical to today"
  artifacts:
    - src/features/edit-staff-locale/ui/EditLocaleDialog.tsx
    - "staff namespace keys (table column, edit trigger, toasts, migrated StaffDashboard strings)"
  key_links:
    - "The 'Change language' row action reuses ProtectedAction action=manage_staff (same gate as Force PIN Change)"
    - "EditLocaleDialog uses useMutationUpdateStaffLocale (21-02) with the target staffId"
---

<objective>
Ship the admin per-staff locale field (new UI surface #2, D-03): a `Language` column + per-row `Change language` action on the Staff management page that lets a manager/admin set ANOTHER staff member's `profiles.locale`, via a new `EditLocaleDialog` (a near-verbatim clone of `EditRoleDialog`, role→locale). Also fully migrate `StaffDashboard`'s pre-existing hardcoded strings into the `staff` namespace (single-owner of this file for the phase).

Purpose: D-03's second write path — admin override. Distinct from 21-03 self-service: this writes a DIFFERENT staff member's locale, so it is gated `manage_staff` (the existing RBAC action already used for "Force PIN Change" on this exact table) and must NOT change the acting admin's own live language (the target picks it up on their next login/hydrate — 21-02's store hydrate, RESEARCH Pattern 2).

Output: `features/edit-staff-locale` FSD slice (EditLocaleDialog + barrel); StaffDashboard with a Language column + gated row action + all its own strings migrated to `staff` ns (es-MX byte-identical, en-US English). Implements D-03 (admin path), contributes SC-2/SC-4.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-i18n-multi-language/21-CONTEXT.md
@.planning/phases/21-i18n-multi-language/21-UI-SPEC.md
@.planning/phases/21-i18n-multi-language/21-PATTERNS.md
@src/features/edit-staff-role/ui/EditRoleDialog.tsx
@src/features/edit-staff-role/index.ts
@src/widgets/StaffDashboard/StaffDashboard.tsx
@src/shared/lib/domain.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: features/edit-staff-locale slice (EditLocaleDialog)</name>
  <files>src/features/edit-staff-locale/ui/EditLocaleDialog.tsx, src/features/edit-staff-locale/index.ts, src/features/edit-staff-locale/ui/EditLocaleDialog.test.tsx, src/shared/lib/i18n/locales/es-MX/staff.json, src/shared/lib/i18n/locales/en-US/staff.json</files>
  <read_first>
    - src/features/edit-staff-role/ui/EditRoleDialog.tsx (full — clone near-verbatim; swap useMutationUpdateStaffRole→useMutationUpdateStaffLocale, UserRoleSchema→LocaleSchema, ROLE_OPTIONS→LOCALE_OPTIONS)
    - src/features/edit-staff-role/index.ts (barrel export shape to mirror)
    - 21-UI-SPEC.md Interaction Contract — Staff page admin field (4 steps; note step 4: dialog must NOT call i18n.changeLanguage on the admin's session) + Copywriting Contract staff.* keys (with {name}/{locale} interpolation)
    - 21-PATTERNS.md "EditLocaleDialog" (props likely narrow to a single target staff, row-action-triggered — confirm against UI-SPEC)
    - useMutationUpdateStaffLocale in src/entities/staff/model/queries.ts (21-02)
  </read_first>
  <behavior>
    - Test: opens with a target staff's current locale pre-selected; Select lists LocaleSchema.options.
    - Test: on Save, calls useMutationUpdateStaffLocale with { staffId: target.id, locale } and on success closes + fires a success toast (with interpolated {name}/{locale}).
    - Test: does NOT call i18n.changeLanguage anywhere (assert the singleton's changeLanguage mock is not called).
    - Test: on failure, toast.error fires and the dialog stays open.
  </behavior>
  <action>
    Create the FSD slice `features/edit-staff-locale/`. `EditLocaleDialog.tsx` clones EditRoleDialog: props narrowed to a single target `staff: Staff | null` + `open`/`onOpenChange` (row-action-triggered per UI-SPEC — one target, not a picker list). A single locale Select (`const LOCALE_OPTIONS = LocaleSchema.options`), pre-selected to `staff.locale`. `handleSubmit`: `LocaleSchema.safeParse` guard → `useMutationUpdateStaffLocale().mutateAsync({ staffId: staff.id, locale })` → `!result.ok` → `toast.error(t('staff:locale.updateError', { name: staff.name }))`; success → `toast.success(t('staff:locale.updateSuccess', { name: staff.name, locale }))` + close. Footer: `variant="outline"` Cancel + primary Save, both disabled while pending (EditRoleDialog structure). Use `useTranslation('staff')`. CRITICAL (UI-SPEC step 4): do NOT call `i18n.changeLanguage()` here — this write only affects the target's stored locale. `index.ts` barrel exports `EditLocaleDialog` (explicit named export — `export *` is banned).

    Add the staff-namespace Language keys from UI-SPEC's copy table: `table.locale`, `locale.editTrigger`, `locale.updateSuccess` (with {name}/{locale}), `locale.updateError` (with {name}). Use UI-SPEC es-MX (Spanish) + en-US values — new elements, safe. Add the four behavior tests.
  </action>
  <verify>
    <automated>npx vitest run src/features/edit-staff-locale/ui/EditLocaleDialog.test.tsx && npm run typecheck</automated>
  </verify>
  <done>EditLocaleDialog clones EditRoleDialog (role→locale); never calls changeLanguage; barrel exports it; staff-ns Language keys added; 4 tests pass.</done>
  <acceptance_criteria>
    - `npx vitest run src/features/edit-staff-locale/ui/EditLocaleDialog.test.tsx` exits 0 incl. the "does not call changeLanguage" assertion.
    - `grep -n "changeLanguage" src/features/edit-staff-locale/ui/EditLocaleDialog.tsx` returns nothing.
    - `grep -n "export \*" src/features/edit-staff-locale/index.ts` returns nothing (explicit exports only).
    - `npm run lint:i18n -- src/features/edit-staff-locale` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: StaffDashboard — Language column + gated row action + full string migration</name>
  <files>src/widgets/StaffDashboard/StaffDashboard.tsx, src/shared/lib/i18n/locales/es-MX/staff.json, src/shared/lib/i18n/locales/en-US/staff.json</files>
  <read_first>
    - src/widgets/StaffDashboard/StaffDashboard.tsx (full — the Role Badge column at lines 65-73 is the exact analog for the Language column; the Force PIN Change ProtectedAction row action at lines 136-147 is the exact gate/pattern for the Change language action; SectionHeader + DataTable searchPlaceholder + all header strings need migration)
    - 21-UI-SPEC.md Interaction Contract — Staff page admin field (steps 1-3: Badge variant=outline column, per-row action gated manage_staff, dialog)
    - src/features/edit-staff-locale (Task 1 — import EditLocaleDialog)
  </read_first>
  <action>
    Add a `Language` column to `columns` mirroring the Role column: `<Badge variant="outline" className="uppercase">{row.original.staff.locale}</Badge>` (renders 'es-MX'/'en-US'), header `t('staff:table.locale')`. In the actions cell, add a `Change language` button INSIDE `<ProtectedAction action="manage_staff" currentRole={currentRole}>` (same wrapper already used for Force PIN Change) that sets a new `localeTarget` state; mount `<EditLocaleDialog staff={localeTarget} open={localeTarget !== null} onOpenChange={...} />` alongside the existing dialogs. Button label `t('staff:locale.editTrigger')`.

    Migrate ALL pre-existing StaffDashboard hardcoded strings to the `staff` namespace via `t()` (this plan is the single owner of StaffDashboard.tsx for the phase — the 21-11 sweep skips it): column headers 'Name'/'Role'/'Clock in'/'Shift duration', buttons 'Clock In'/'Clock Out'/'Force PIN Change', SectionHeader title 'Staff' + description 'Active team members and open shifts.', DataTable `searchPlaceholder="Search staff…"`, and the em-dash placeholder '—' (leave the em-dash as a literal — it is a symbol, in the words.exclude; do NOT translate it). es-MX values MUST be byte-identical to the current literals (all currently English → preserved; en-US identical). Add keys under a `table.*` / `actions.*` structure in staff.json (both locales). Use `useTranslation('staff')`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run lint:i18n -- src/widgets/StaffDashboard/StaffDashboard.tsx</automated>
  </verify>
  <done>Language column + gated Change-language action wired; EditLocaleDialog mounted; all StaffDashboard strings migrated to staff ns byte-identical; scoped i18n lint clean.</done>
  <acceptance_criteria>
    - `npm run lint:i18n -- src/widgets/StaffDashboard/StaffDashboard.tsx` exits 0 (no hardcoded user-facing strings remain).
    - The Change-language button is inside a `ProtectedAction action="manage_staff"` wrapper.
    - es-MX staff.json values for migrated headers equal the pre-migration literals (e.g. `table.name` === 'Name').
    - `npm run lint` + `npm run typecheck` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager/admin → another staff's profiles.locale | Cross-user write (admin override) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Elevation of Privilege | EditLocaleDialog cross-user write | high | mitigate | Row action wrapped in `ProtectedAction action="manage_staff"` (client gate) AND the write goes through profiles RLS (server gate, manage_staff-equivalent) + audited `staff.locale_change` (21-02). Do not rely on UI hiding alone (RESEARCH Security Domain EoP pattern) |
| T-21-04c | Tampering | interpolated {name} in toast | low | mitigate | i18next interpolation escapes by default for string output; toast renders text, not HTML |
</threat_model>

<verification>
- `npx vitest run src/features/edit-staff-locale/ui/EditLocaleDialog.test.tsx` passes.
- `npm run lint:i18n -- src/widgets/StaffDashboard/StaffDashboard.tsx` and `-- src/features/edit-staff-locale` exit 0.
- `npm run typecheck` + `npm run lint` exit 0.
</verification>

<success_criteria>
- Admin can set another staff member's locale, gated manage_staff (D-03 admin path).
- EditLocaleDialog does not change the acting admin's language (SC-2 semantics).
- StaffDashboard fully migrated with zero visual regression (SC-4).
</success_criteria>

<output>
Create `.planning/phases/21-i18n-multi-language/21-04-SUMMARY.md` when done. Note that StaffDashboard.tsx is fully owned/migrated here (the 21-11 admin sweep must skip it) and record the staff-ns keys added.
</output>
