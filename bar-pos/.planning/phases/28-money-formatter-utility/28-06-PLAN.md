---
phase: 28-money-formatter-utility
plan: 06
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/features/process-refund/ui/RefundSheet.tsx
  - src/features/register-caja-entry/ui/RegisterCajaEntryDialog.tsx
  - src/features/manage-promotions/ui/ManagePromotionsTab.tsx
  - src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx
  - src/features/split-tab/ui/SplitTabSheet.tsx
  - src/shared/lib/i18n/locales/es-MX/featOrders.json
  - src/shared/lib/i18n/locales/en-US/featOrders.json
  - src/shared/lib/i18n/locales/es-MX/featMgmt.json
  - src/shared/lib/i18n/locales/en-US/featMgmt.json
autonomous: true
requirements: [SC-2, SC-4]
must_haves:
  truths:
    - "Refund, caja-entry, promotion, paid-tab-edit and split-tab confirmation copy renders money through the shared formatter"
    - "A percentage-type promotion summary renders through formatPercent, not the money formatter"
    - "No featOrders or featMgmt catalog string carries a hardcoded currency character in front of an interpolation placeholder"
    - "No money input field in these features was rewired — only display copy changed (D-03)"
  artifacts:
    - src/shared/lib/i18n/locales/es-MX/featOrders.json
    - src/shared/lib/i18n/locales/en-US/featOrders.json
    - src/shared/lib/i18n/locales/es-MX/featMgmt.json
    - src/shared/lib/i18n/locales/en-US/featMgmt.json
  key_links:
    - "All five features import formatMoney from @shared/lib/format"
    - "Both featOrders and both featMgmt catalogs stay key-for-key identical between locales"
  prohibitions:
    - "parseMoneyInput must not be wired into RefundSheet or SplitTabSheet — D-03 forbids retrofitting these payment-critical input fields in this phase"
    - "A percentage discount must not be rendered with the money formatter — a percent value carrying a currency symbol is a data-meaning error, not a cosmetic one"
---

<objective>
Migrate the five `features`-layer surfaces that interpolate money into translated confirmation and summary copy, moving the currency symbol out of the `featOrders` and `featMgmt` catalogs and into the shared formatter — while leaving every money *input* field in those same components untouched.

Purpose: SC-2 and SC-4 for the refund, caja-entry, promotion, paid-tab-edit, and split-tab flows.
Output: 5 migrated features, 4 catalogs with 5 keys de-dollared.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-money-formatter-utility/28-CONTEXT.md
@.planning/phases/28-money-formatter-utility/28-RESEARCH.md
@.planning/phases/28-money-formatter-utility/28-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates no new exported symbols and no new i18n keys. It consumes `formatMoney` and `formatPercent` from `src/shared/lib/format.ts` (plan 01).

**Catalog ownership note:** this plan is the sole owner of `featOrders.json` and `featMgmt.json` in both locales for phase 28. Plan 04 owns `wPanels.json`; plan 05 owns `wAdmin.json`. Do not edit a catalog file this plan does not own.

<tasks>

<task type="auto">
  <name>Task 1: Migrate refund and caja-entry confirmation copy</name>
  <files>src/features/process-refund/ui/RefundSheet.tsx, src/features/register-caja-entry/ui/RegisterCajaEntryDialog.tsx, src/shared/lib/i18n/locales/es-MX/featOrders.json, src/shared/lib/i18n/locales/en-US/featOrders.json</files>
  <read_first>
    - `src/features/process-refund/ui/RefundSheet.tsx` lines 180-205 — the refund-total rounding expression and the confirmation message it feeds
    - `src/features/register-caja-entry/ui/RegisterCajaEntryDialog.tsx` lines 80-100 — the validated-amount branch feeding the expense/income confirmation keys
    - `src/shared/lib/i18n/locales/es-MX/featOrders.json` lines 66-70 (`refundProcessed`) and lines 240-246 (`expenseRecorded`, `incomeRecorded`)
    - `src/shared/lib/i18n/locales/en-US/featOrders.json` — the same three keys
    - `src/shared/lib/format.ts` — the `formatMoney` signature
    - `.planning/phases/28-money-formatter-utility/28-CONTEXT.md` D-03 — the no-input-retrofit constraint
  </read_first>
  <action>
    In both components, import `formatMoney` from `@shared/lib/format` and pass `formatMoney(value)` where a fixed-point number string is currently handed to a `featOrders` translation key.

    `RefundSheet`'s refund total is currently rounded to cents and then serialized in one expression. Keep the cents rounding — it is refund arithmetic, not formatting — and apply `formatMoney` to the rounded number instead of serializing it by hand.

    In BOTH `featOrders.json` catalogs, delete the literal currency character immediately preceding the placeholder in `refundProcessed`, `expenseRecorded`, and `incomeRecorded`. Leave all surrounding wording and punctuation untouched.

    Do not touch the refund amount input, the inventory-reversal toggle, the manager-PIN gate, or the caja-entry amount input in either component. D-03 explicitly keeps these payment-critical input surfaces as-is for this phase, and `parseMoneyInput` must not appear in either file. This task changes confirmation copy only.
  </action>
  <verify>
    <automated>npx vitest run src/features/process-refund src/features/register-caja-entry --passWithNoTests</automated>
    <automated>grep -Eq "from '@shared/lib/format'" src/features/process-refund/ui/RefundSheet.tsx</automated>
    <automated>grep -Eq "from '@shared/lib/format'" src/features/register-caja-entry/ui/RegisterCajaEntryDialog.tsx</automated>
    <automated>grep -Ec 'parseMoneyInput' src/features/process-refund/ui/RefundSheet.tsx src/features/register-caja-entry/ui/RegisterCajaEntryDialog.tsx | grep -Evq ':[1-9]'</automated>
    <automated>node -e "for(const l of ['es-MX','en-US']){const s=require('fs').readFileSync('./src/shared/lib/i18n/locales/'+l+'/featOrders.json','utf8');JSON.parse(s);for(const k of ['refundProcessed','expenseRecorded','incomeRecorded']){const m=s.match(new RegExp('\"'+k+'\"\\\\s*:\\\\s*\"([^\"]*)\"'));if(!m)throw new Error('missing '+k+' in '+l);if(/\\x24\\{\\{/.test(m[1]))throw new Error('currency prefix survives on '+k+' in '+l)}}"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both components import `formatMoney` from `@shared/lib/format` and pass formatter output into their `t()` calls
    - `refundProcessed`, `expenseRecorded`, and `incomeRecorded` carry no currency character before their placeholder in either locale
    - Neither file references `parseMoneyInput` (D-03)
    - The refund cents-rounding expression is unchanged
    - No input element, PIN gate, or toggle in either component changed
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Confirmation-copy expression edits plus catalog value edits.</reversibility>
  <done>Refund and caja-entry confirmations report amounts through the shared formatter.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate promotion summaries, distinguishing money from percentage discounts</name>
  <files>src/features/manage-promotions/ui/ManagePromotionsTab.tsx, src/shared/lib/i18n/locales/es-MX/featMgmt.json, src/shared/lib/i18n/locales/en-US/featMgmt.json</files>
  <read_first>
    - `src/features/manage-promotions/ui/ManagePromotionsTab.tsx` lines 55-80 — the discount-type branch and the two summary keys it selects between
    - `src/shared/lib/i18n/locales/es-MX/featMgmt.json` lines 298-303 — `summaryFixedAmount` and `summaryFixedPrice`, and any sibling percentage-summary key in the same block
    - `src/shared/lib/i18n/locales/en-US/featMgmt.json` — the same keys
    - `src/shared/lib/format.ts` — the `formatMoney` and `formatPercent` signatures
    - `src/features/manage-promotions/ui/PromotionBuilderForm.test.tsx` — existing assertions on promotion summary strings
  </read_first>
  <action>
    Read the discount-type branch before editing. This component serializes a `discountValue` at two call sites, and that value means different things per discount type — a currency amount for fixed-amount and fixed-price promotions, and a rate for a percentage promotion.

    Route each call site by its branch: the fixed-amount and fixed-price branches get `formatMoney(discountValue)`; any percentage branch gets `formatPercent(discountValue)`. Do not apply the money formatter uniformly to both sites — a percentage rendered with a currency symbol is a meaning error, not a cosmetic one, and would misreport a promotion's terms to whoever configures it.

    In BOTH `featMgmt.json` catalogs, delete the literal currency character immediately preceding the placeholder in `summaryFixedAmount` and `summaryFixedPrice`. Preserve `summaryFixedAmount`'s leading minus — that is a sign, not a currency symbol. If a percentage-summary key in the same block carries a literal percent character before or after its placeholder and its caller now uses `formatPercent`, remove that character too so the suffix is not doubled.

    Update `PromotionBuilderForm.test.tsx` assertions that pin these summary strings, keeping them exact.
  </action>
  <verify>
    <automated>npx vitest run src/features/manage-promotions</automated>
    <automated>grep -Eq 'formatMoney' src/features/manage-promotions/ui/ManagePromotionsTab.tsx</automated>
    <automated>node -e "for(const l of ['es-MX','en-US']){const s=require('fs').readFileSync('./src/shared/lib/i18n/locales/'+l+'/featMgmt.json','utf8');JSON.parse(s);for(const k of ['summaryFixedAmount','summaryFixedPrice']){const m=s.match(new RegExp('\"'+k+'\"\\\\s*:\\\\s*\"([^\"]*)\"'));if(!m)throw new Error('missing '+k+' in '+l);if(/\\x24\\{\\{/.test(m[1]))throw new Error('currency prefix survives on '+k+' in '+l)}}"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Each `discountValue` call site uses the formatter matching its discount type — money for fixed-amount and fixed-price, percent for percentage
    - `summaryFixedAmount` and `summaryFixedPrice` carry no currency character before their placeholder in either locale, and `summaryFixedAmount` retains its leading minus
    - No summary string renders a doubled percent character
    - `npx vitest run src/features/manage-promotions` exits 0 with exact assertions
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Branch-aware expression edits plus catalog value edits.</reversibility>
  <done>Promotion summaries render each discount type with the formatter that matches its meaning.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate paid-tab-edit and split-tab hint copy</name>
  <files>src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx, src/features/split-tab/ui/SplitTabSheet.tsx</files>
  <read_first>
    - `src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx` lines 398-415 — the total-delta hint, whose currency character lives in the caller's template literal rather than in the catalog
    - `src/features/split-tab/ui/SplitTabSheet.tsx` lines 212-232 — the even-split amount hint, whose value is a cents integer divided down and whose currency character is also caller-side
    - `src/shared/lib/i18n/locales/es-MX/featOrders.json` line 210 (`totalDeltaHint`) — read only, to confirm the catalog value carries no currency character
    - `src/shared/lib/format.ts` — the `formatMoney` signature and its `showSign` option
    - `.planning/phases/28-money-formatter-utility/28-CONTEXT.md` D-03 — the no-input-retrofit constraint
  </read_first>
  <action>
    Both of these differ from tasks 1 and 2: the currency character is in the caller's template literal, not in the catalog. Confirm this by reading `totalDeltaHint` in the catalog before editing — if it genuinely carries no currency character, make no catalog edit in this task at all.

    In `EditPaidTabDialog`, the total-delta hint currently takes an absolute value and wraps it in a hand-built currency template. Replace the whole interpolated argument with a single `formatMoney` call. Consider whether dropping the absolute value and passing the signed delta with `{ showSign: true }` reads better here — the surrounding copy says the total "changes by" some amount, so a signed value is more informative than a magnitude. If you make that change, confirm the catalog wording in both locales still reads correctly with a signed value and adjust the wording if not; if it does not read correctly, keep the absolute value and plain `formatMoney`.

    In `SplitTabSheet`, the even-split hint divides a cents integer by one hundred and wraps it in a hand-built currency template. Keep the cents-to-units division — it is unit conversion, not formatting — and apply `formatMoney` to the result. Preserve the existing empty-string fallback for the branch where no even split is computed.

    Do not touch the split-mode selection logic, the sub-tab construction, the per-person or per-amount inputs, or the manager gates in either component. D-03 keeps these input surfaces as-is, and `parseMoneyInput` must not appear in either file.
  </action>
  <verify>
    <automated>npx vitest run src/features/edit-paid-tab src/features/split-tab --passWithNoTests</automated>
    <automated>grep -Eq "from '@shared/lib/format'" src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx</automated>
    <automated>grep -Eq "from '@shared/lib/format'" src/features/split-tab/ui/SplitTabSheet.tsx</automated>
    <automated>grep -Ec 'parseMoneyInput' src/features/edit-paid-tab/ui/EditPaidTabDialog.tsx src/features/split-tab/ui/SplitTabSheet.tsx | grep -Evq ':[1-9]'</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Neither file builds a currency string by hand; both call `formatMoney`
    - `SplitTabSheet` retains its cents-to-units division and its empty-string fallback branch
    - Neither file references `parseMoneyInput` (D-03)
    - No split-mode logic, sub-tab construction, input, or manager gate changed
    - If the delta hint switched to a signed value, both locale catalog wordings were re-read and confirmed to read correctly
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Hint-copy expression edits in two components.</reversibility>
  <done>The paid-tab-edit and split-tab hints render money through the shared formatter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff-visible refund / split / delta confirmations | These strings are the operator's last check before an irreversible money action (refund issued, paid tab altered, tab split). |
| promotion configuration UI → pricing rules | A promotion summary misreporting its own terms leads to a wrongly-configured discount applied to real orders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-14 | Tampering | percentage discount rendered with the money formatter | high | mitigate | Task 2 routes by discount-type branch and requires reading the branch before editing; the prohibition is stated in `must_haves` and the acceptance criteria require per-branch formatter selection. |
| T-28-15 | Tampering | `featOrders` / `featMgmt` catalog placeholder retaining its currency prefix while the caller passes formatted output | high | mitigate | Catalog and caller edited in the same task and commit, in both locales, with a verify gate that fails if a currency character still precedes a placeholder. |
| T-28-16 | Tampering | accidental rewiring of a payment-critical money input while editing adjacent display copy | high | mitigate | D-03 restated in each task's action; verify gates assert `parseMoneyInput` appears in none of the four input-bearing components; acceptance criteria require inputs, PIN gates, and toggles to be unchanged. |

No package-manager installs occur in this plan.
</threat_model>

<verification>
- `npx vitest run src/features` exits 0
- All four owned catalog files parse as JSON and carry no currency character before an edited placeholder
- `npm run lint` on the five touched features exits 0
- `npm run typecheck` exits 0
</verification>

<success_criteria>
- SC-2 for the `featOrders` / `featMgmt` surface: no hand-built money string remains in the five touched features
- SC-4: refund, caja-entry, promotion, delta, and split hints keep their wording, signs, and arithmetic; only the symbol and grouping change
- D-03 is honored: not one money input field was rewired
</success_criteria>

<output>
Create `.planning/phases/28-money-formatter-utility/28-06-SUMMARY.md` when done
</output>
