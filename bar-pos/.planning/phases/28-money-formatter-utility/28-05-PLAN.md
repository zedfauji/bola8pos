---
phase: 28-money-formatter-utility
plan: 05
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx
  - src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx
  - src/shared/lib/i18n/locales/es-MX/wAdmin.json
  - src/shared/lib/i18n/locales/en-US/wAdmin.json
  - src/widgets/ComboMixReport/ComboMixReport.tsx
  - src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx
  - src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx
  - src/widgets/RefundsRegister/RefundsRegister.tsx
  - src/widgets/RefundsRegister/RefundsRegister.test.tsx
  - src/widgets/CajaReportPanel/CajaReportPanel.tsx
  - src/widgets/RecipeVarianceReport/RecipeVarianceReport.tsx
  - src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "Every money cell in the reports suite renders through the shared formatter with exactly one currency symbol"
    - "The caja report's expense/income rows show an explicit sign via the formatter's showSign option (D-06)"
    - "The recipe variance percentage renders through formatPercent, giving that export a real consumer"
    - "Kitchen prep quantity displays are exempted with a stated reason rather than migrated — they are units of measure, not money"
    - "No wAdmin catalog string carries a hardcoded currency character in front of an interpolation placeholder"
  artifacts:
    - src/shared/lib/i18n/locales/es-MX/wAdmin.json
    - src/shared/lib/i18n/locales/en-US/wAdmin.json
  key_links:
    - "All seven money-bearing report widgets import formatMoney from @shared/lib/format"
    - "RecipeVarianceReport imports formatPercent from @shared/lib/format"
  prohibitions:
    - "A non-money quantity must not be routed through formatMoney to silence the lint rule — that would print a currency symbol on a unit-of-measure figure"
    - "A wAdmin catalog placeholder must not keep its literal currency prefix once its caller passes formatter output"
---

<objective>
Migrate the reports and admin surface — seven money-bearing report widgets plus the two `wAdmin`-namespace panels that interpolate amounts into translated strings — onto `@shared/lib/format`, and correctly classify the non-money numeric displays that live alongside them.

Purpose: SC-2 and SC-4 for `/reports` and the admin settings tabs, and SC-1's `formatPercent` export gaining a real consumer.
Output: 9 migrated widgets, 2 catalogs with 2 keys de-dollared each, 1 reasoned quantity exemption.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-money-formatter-utility/28-CONTEXT.md
@.planning/phases/28-money-formatter-utility/28-RESEARCH.md
@.planning/phases/28-money-formatter-utility/28-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates no new exported symbols and no new i18n keys. It consumes `formatMoney` and `formatPercent` from `src/shared/lib/format.ts` (plan 01).

**Catalog ownership note:** this plan is the sole owner of `wAdmin.json` in both locales for phase 28. Plan 04 owns `wPanels.json`; plan 06 owns `featOrders.json` and `featMgmt.json`. Do not edit a catalog file this plan does not own.

<tasks>

<task type="auto">
  <name>Task 1: Migrate the wAdmin-namespace interpolated amounts</name>
  <files>src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx, src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx, src/shared/lib/i18n/locales/es-MX/wAdmin.json, src/shared/lib/i18n/locales/en-US/wAdmin.json</files>
  <read_first>
    - `src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx` lines 85-115 — the peak-hour and slowest-hour revenue labels
    - `src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx` lines 140-160 — the floor/bar/kitchen example-breakdown preview
    - `src/shared/lib/i18n/locales/es-MX/wAdmin.json` line 101 (`hourRevenueValue`) and line 452 (`exampleBreakdown`)
    - `src/shared/lib/i18n/locales/en-US/wAdmin.json` — the same two keys
    - `src/shared/lib/format.ts` — the `formatMoney` signature
  </read_first>
  <action>
    In both widgets, import `formatMoney` from `@shared/lib/format` and pass `formatMoney(value)` wherever a fixed-point number string is currently handed to a `wAdmin` translation key.

    `HourlyBreakdownPanel` feeds the `hourRevenueValue` key from two call sites (peak hour and slowest hour). `TipDistributionSettingsTab` feeds the `exampleBreakdown` key with three separate values (floor, bar, kitchen) in a single call.

    In BOTH `wAdmin.json` catalogs, delete the literal currency character that immediately precedes each placeholder in these two keys — `hourRevenueValue` has one such placeholder, `exampleBreakdown` has three. Leave the surrounding parentheses, slashes, and label words exactly as they are; only the currency character is removed.

    Do not change the tip-split arithmetic in `TipDistributionSettingsTab` — the preview's largest-remainder allocation is Phase 19 behavior and this is a display-string migration.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/HourlyBreakdownPanel src/widgets/SettingsTabsPanel --passWithNoTests</automated>
    <automated>node -e "for(const l of ['es-MX','en-US']){const s=require('fs').readFileSync('./src/shared/lib/i18n/locales/'+l+'/wAdmin.json','utf8');JSON.parse(s);for(const k of ['hourRevenueValue','exampleBreakdown']){const m=s.match(new RegExp('\"'+k+'\"\\\\s*:\\\\s*\"([^\"]*)\"'));if(!m)throw new Error('missing '+k+' in '+l);if(/\\x24\\{\\{/.test(m[1]))throw new Error('currency prefix survives on '+k+' in '+l)}}"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Neither `hourRevenueValue` nor `exampleBreakdown` has a currency character immediately before any placeholder, in either locale
    - Both widgets pass formatter output rather than fixed-point strings into their `t()` calls
    - Both `wAdmin.json` files still parse as JSON
    - The tip-split allocation arithmetic is unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Caller expression edits plus catalog value edits.</reversibility>
  <done>The two admin panels that interpolate money into translated copy no longer own a currency character.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the money cells across the reports suite</name>
  <files>src/widgets/ComboMixReport/ComboMixReport.tsx, src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx, src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx, src/widgets/RefundsRegister/RefundsRegister.tsx, src/widgets/RefundsRegister/RefundsRegister.test.tsx, src/widgets/CajaReportPanel/CajaReportPanel.tsx</files>
  <read_first>
    - `src/widgets/ComboMixReport/ComboMixReport.tsx` lines 105-118 — the net-revenue and average-price cells
    - `src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx` lines 110-125 — the gross-amount and tip-amount cells
    - `src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx` lines 105-118 — the revenue cell
    - `src/widgets/RefundsRegister/RefundsRegister.tsx` lines 60-82 — the per-row amount cell and the total row
    - `src/widgets/CajaReportPanel/CajaReportPanel.tsx` lines 60-70 (the signed entry-amount column), lines 190-210 (income/expense summary), and lines 218-262 (the cash-reconciliation variance and the reconciliation summary figures)
    - `src/widgets/RefundsRegister/RefundsRegister.test.tsx` — existing assertions on rendered amounts
    - `src/shared/lib/format.ts` — the `formatMoney` signature and its `showSign` option
  </read_first>
  <action>
    In each of these five widgets, import `formatMoney` from `@shared/lib/format` and replace every construction that pairs a literal currency character in JSX text with an adjacent fixed-point expression. Each becomes a single expression container calling `formatMoney`. Leave the enclosing `TableCell` / `span` elements and their classNames — including the tabular-numerals and red/green treatments — exactly as they are; only the text expression changes.

    `CajaReportPanel`'s entry-amount column near line 66 uses the same hand-built sign-then-currency construction as the caja dashboard: replace it with one `formatMoney` call that negates the amount for expense rows and passes `{ showSign: true }`, so income rows carry an explicit plus. Do not keep a hand-built sign alongside the option.

    `CajaReportPanel`'s cash-reconciliation variance near line 224 is a bare fixed-point number with no currency character today. It is money — a till over/short figure — so migrate it to `formatMoney` as well. Because a variance is meaningfully signed, pass `{ showSign: true }` here so a surplus reads unambiguously against a shortfall.

    Update `RefundsRegister.test.tsx` assertions that pin rendered amounts to the new symbol-prefixed, grouped form. If any of the other four widgets has a co-located test asserting a money string, update it the same way; do not weaken an exact assertion into a substring match.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/RefundsRegister src/widgets/ComboMixReport src/widgets/PaymentMethodsReport src/widgets/ModifierPopularityReport src/widgets/CajaReportPanel --passWithNoTests</automated>
    <automated>for f in ComboMixReport/ComboMixReport PaymentMethodsReport/PaymentMethodsReport ModifierPopularityReport/ModifierPopularityReport RefundsRegister/RefundsRegister CajaReportPanel/CajaReportPanel; do grep -Eq "from '@shared/lib/format'" "src/widgets/$f.tsx" || { echo "MISSING import in $f"; exit 1; }; done</automated>
    <automated>grep -Ec 'showSign' src/widgets/CajaReportPanel/CajaReportPanel.tsx | grep -Eq '^[2-9]'</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All five widgets import `formatMoney` from `@shared/lib/format`
    - No literal currency character remains adjacent to a numeric expression in any of the five files
    - `CajaReportPanel` uses `showSign` in at least two places (the entry column and the variance figure) and retains no hand-built sign character on those lines
    - Every `TableCell` / `span` className in the five files is unchanged
    - Co-located tests pass with exact updated assertions
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Cell-rendering expression rewrites across five report widgets.</reversibility>
  <done>Every money cell in the reports suite is formatter-sourced and locale-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Classify the non-money numeric displays alongside the reports</name>
  <files>src/widgets/RecipeVarianceReport/RecipeVarianceReport.tsx, src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx</files>
  <read_first>
    - `src/widgets/RecipeVarianceReport/RecipeVarianceReport.tsx` lines 60-72 — the variance-percentage cell
    - `src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx` lines 50-62 — the produced-quantity cell and its unit-of-measure suffix
    - `src/shared/lib/format.ts` — the `formatPercent` signature, in particular its `decimals` option
    - `.planning/phases/28-money-formatter-utility/28-RESEARCH.md` Pitfall 4 — why quantity displays get exemptions rather than migrations
  </read_first>
  <action>
    `RecipeVarianceReport`'s variance cell renders a fixed-point number followed by a literal percent character. Replace it with a `formatPercent` call from `@shared/lib/format`, passing `{ decimals: 2 }` to preserve the current two-decimal precision, and remove the now-redundant literal percent character — `formatPercent` supplies its own suffix. This gives `formatPercent` its first real consumer and is the only percent retrofit in this phase; CONTEXT.md's no-forced-retrofit spirit (D-03) governs the rest, and the other percent-shaped sites in this codebase use a different precision and are left alone.

    `KitchenPrepDashboard`'s produced-quantity cell is a unit-of-measure figure, not money — it is rendered next to a `uom` string. Do not migrate it. Add an `eslint-disable-next-line` comment directly above it naming the `no-restricted-syntax` rule with a reason clause identifying the value as a quantity in a unit of measure. Plan 08's rule would otherwise flag it, and a bare disable would be indistinguishable from a skipped site.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/RecipeVarianceReport src/widgets/KitchenPrepDashboard --passWithNoTests</automated>
    <automated>grep -Eq 'formatPercent' src/widgets/RecipeVarianceReport/RecipeVarianceReport.tsx</automated>
    <automated>grep -Eq 'eslint-disable-next-line no-restricted-syntax' src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx</automated>
    <automated>grep -Ec 'formatMoney' src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx | grep -q '^0$'</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The variance cell renders exactly one percent character, supplied by `formatPercent`, at two-decimal precision
    - `KitchenPrepDashboard` carries a reasoned `eslint-disable-next-line no-restricted-syntax` comment on its quantity cell and does not reference `formatMoney`
    - The quantity cell's rendered value and its unit-of-measure suffix are visually unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">One expression change and one comment.</reversibility>
  <done>Percent-shaped and quantity-shaped numbers are each handled correctly, neither swept into the money formatter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reports UI → operator/owner financial decisions | `/reports` figures drive till reconciliation, refund review, and tip-split configuration; a misread figure produces a wrong real-world action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-11 | Tampering | `CajaReportPanel` sign inversion when replacing hand-built signs with `showSign` | high | mitigate | Negate the amount for expense rows and for a shortfall variance; both sites named explicitly in task 2's action and covered by acceptance criteria requiring `showSign` in at least two places with no residual hand-built sign. |
| T-28-12 | Tampering | non-money quantity routed through `formatMoney` to silence the lint rule | medium | mitigate | Task 3 classifies quantity sites explicitly and asserts `formatMoney` is absent from `KitchenPrepDashboard`; the prohibition is stated in `must_haves`. |
| T-28-13 | Tampering | `wAdmin` catalog placeholder retaining its currency prefix while the caller passes formatted output | high | mitigate | Catalog and caller edited in the same task and commit, in both locales, with a verify gate that fails if a currency character still precedes a placeholder in either key. |

No package-manager installs occur in this plan.
</threat_model>

<verification>
- `npx vitest run src/widgets` exits 0
- Both `wAdmin.json` files parse and carry no currency character before a placeholder in the two edited keys
- `npm run lint` on the touched widgets exits 0
- `npm run typecheck` exits 0
</verification>

<success_criteria>
- SC-2 for the reports and admin surface: no hand-built money string remains in any of the nine touched widgets
- SC-1: `formatPercent` has a real consumer, exercised by rendering `/reports`
- SC-4: report figures keep their signs, precision, and styling; only the symbol and digit grouping change
</success_criteria>

<output>
Create `.planning/phases/28-money-formatter-utility/28-05-SUMMARY.md` when done
</output>
