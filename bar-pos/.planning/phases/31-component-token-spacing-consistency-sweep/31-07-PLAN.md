---
phase: 31-component-token-spacing-consistency-sweep
plan: 07
type: execute
wave: 2
depends_on: [31-01, 31-02, 31-03, 31-04, 31-05, 31-06]
files_modified: []
autonomous: true
requirements: [TOKEN-01, TOKEN-02, COMPONENT-01, COMPONENT-02, COMPONENT-03]

must_haves:
  truths:
    - "After all 6 Wave-1 plans merge, typecheck and lint are green and the full unit suite matches its pre-phase baseline (zero regressions)"
    - "TOKEN-02 verified: zero arbitrary-value padding/margin/gap/space classes exist across src/pages, src/widgets, src/features"
    - "COMPONENT-01/02 phase-wide: no raw <button> or type=checkbox inputs remain in src/pages/widgets/features outside the 4 D-01 payment-critical files (deferred to Phase 33)"
  artifacts:
    - path: ".planning/phases/31-component-token-spacing-consistency-sweep/31-07-SUMMARY.md"
      provides: "Phase-gate verification record (green typecheck/lint/unit + TOKEN-02 zero-violation + aggregate conformance greps)"
      contains: "TOKEN-02"
  key_links:
    - from: "31-07 gate"
      to: "31-01..31-06 merged output"
      via: "aggregate raw-button/checkbox/spacing greps + full unit suite"
      pattern: "npm run test"
---

<objective>
Phase regression gate. After all six Wave-1 conformance plans (31-01..31-06) merge, run the full quality gate and verify the phase-wide conformance assertions that no single Wave-1 plan can check alone: TOKEN-02 zero-violation (no arbitrary spacing) and COMPONENT-01/02 phase-wide completeness (no raw buttons/checkboxes remain outside the D-01 payment-critical exclusions).

Purpose: give TOKEN-02 (a zero-violation requirement with no per-file task) a genuine verification home, and give the verifier a single green signal after the parallel swaps land — the research explicitly recommends a full unit-suite run at phase-gate because this phase touches shared chrome (`HomeDashboard`, `PoolTableGrid`).
Output: green typecheck/lint/unit + documented zero-violation greps in `31-07-SUMMARY.md`. No source files are modified by this plan.

Scope note: this plan modifies zero source files — it is verification only. If any aggregate grep surfaces an unplanned raw element in a non-payment file, that is a gap to report to the orchestrator (candidate for a follow-up plan), not a silently-tolerated result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-VALIDATION.md
@.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full quality gate + TOKEN-02 zero-violation + phase-wide conformance greps</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-VALIDATION.md (Sampling Rate + Per-Task Verification Map — the phase-gate cadence and the 3 targeted E2E specs)
    - .planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md (baseline: 20 raw-button files, 8 raw-input files, 0 arbitrary-spacing files — confirms the aggregate targets)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-CONTEXT.md (D-01 payment-file exclusions to exempt from the aggregate button/checkbox greps)
  </read_first>
  <action>
    Run the full gate from `bar-pos/`: `npm run typecheck`, then `npm run lint`, then `npm run test` (full unit suite, run-once). Confirm the unit-suite result matches the documented pre-phase baseline — per STATE.md the baseline is 1212 passed / 1 pre-existing `useCloseTab.test.ts` failure / 15 todo, plus the 2 pre-existing unrelated typecheck errors noted since Phase 17; any NEW failure/error beyond that baseline is a regression to investigate before declaring the gate green.
    TOKEN-02 verification: run the arbitrary-spacing grep across `src/pages src/widgets src/features` for padding/margin/gap/space arbitrary values only (word-boundary-anchored so `min-h-[160px]`/`min-w-[140px]`/`size-14` are correctly excluded — Phase 29 scoped the spacing scan to padding/margin/gap only). Expect ZERO matches (DRIFT-AUDIT confirms 0).
    COMPONENT-01/02 phase-wide: grep `src/pages src/widgets src/features` for raw `<button` and for `type="checkbox"`, excluding the 4 D-01 payment-critical files (`pos/index.tsx`, `OrderPanel/CartPanel.tsx`, `PaymentModal/ui/PaymentForm.tsx`, `PaymentPane/ui/TabPaymentCard.tsx`). Expect ZERO matches for both (all non-payment raw buttons swapped in 31-01..31-04; both checkbox files swapped in 31-05). Any hit in a non-payment file is a gap — report it to the orchestrator rather than tolerating it.
    Record all command outputs (pass/fail + counts) in `31-07-SUMMARY.md`. Do NOT edit any source file in this plan.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test && ! rg -n -e '\bp[xytblr]?-\[' -e '\bm[xytblr]?-\[' -e '\bgap(-[xy])?-\[' -e '\bspace-[xy]-\[' src/pages src/widgets src/features && ! rg -n '\x3Cbutton' src/pages src/widgets src/features -g '!**/pos/index.tsx' -g '!**/OrderPanel/CartPanel.tsx' -g '!**/PaymentModal/ui/PaymentForm.tsx' -g '!**/PaymentPane/ui/TabPaymentCard.tsx' && ! rg -n 'type="checkbox"' src/pages src/widgets src/features</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (or only the 2 documented pre-existing errors — no NEW typecheck error)
    - `npm run lint` exits 0
    - `npm run test` full unit suite matches baseline (no new failures beyond the 1 pre-existing `useCloseTab.test.ts` failure)
    - TOKEN-02 grep (`\bp[xytblr]?-\[` / `\bm[xytblr]?-\[` / `\bgap(-[xy])?-\[` / `\bspace-[xy]-\[` across pages/widgets/features) returns no matches
    - Raw-button grep across pages/widgets/features (excluding the 4 D-01 payment files) returns no matches
    - `type="checkbox"` grep across pages/widgets/features returns no matches
  </acceptance_criteria>
  <done>Typecheck/lint/full-unit green at baseline; TOKEN-02 zero arbitrary-spacing confirmed; zero raw buttons (outside payment exclusions) and zero raw checkboxes phase-wide; results recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification-only plan — runs read-only checks and greps, modifies no source, introduces no runtime code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07-01 | Tampering | phase gate | accept | No new attack surface: this plan writes no source code and adds no runtime behavior; it only runs typecheck/lint/test/grep to confirm the Wave-1 markup swaps introduced no regression. |
| T-31-07-SC | Tampering | npm installs | accept | Zero package installs — runs only existing project scripts (`npm run typecheck`/`lint`/`test`) and ripgrep. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` + `npm run test` green at documented baseline
- TOKEN-02 arbitrary-spacing grep returns no matches across pages/widgets/features
- Raw-button grep (excluding 4 D-01 payment files) and `type="checkbox"` grep return no matches
- Recommended (dev-server dependent, per 31-VALIDATION.md): `npx playwright test e2e/38-audit-logs.spec.ts e2e/15-home-navigation.spec.ts e2e/16-table-status.spec.ts` — treat the Phase-30-documented Supabase-RPC-latency flake on `16-table-status` as pre-existing, not a new regression
</verification>

<success_criteria>
The phase gate is green: typecheck/lint/full-unit at baseline with zero new regressions; TOKEN-02 has zero arbitrary-spacing violations phase-wide; no raw `<button>` (outside the D-01 payment exclusions) and no raw `type="checkbox"` remain across pages/widgets/features; the SUMMARY records every command's result.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols** and modifies no source files. Its only output is the verification record `31-07-SUMMARY.md`. It introduces no decorators, classes, functions, CLI flags, fields, or source file paths.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-07-SUMMARY.md` when done, recording every gate command's output and the three aggregate grep results.
</output>
