---
phase: 31-component-token-spacing-consistency-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/TableStatusPanel/index.tsx
  - src/features/manage-products/ui/CategoryForm.tsx
  - src/features/add-item-to-tab/ui/ModifierSheet.tsx
autonomous: true
requirements: [COMPONENT-01, COMPONENT-02, COMPONENT-03, TOKEN-01]

must_haves:
  truths:
    - "TableStatusPanel's icon row-remove button renders via Button variant=ghost size=icon-sm"
    - "TableStatusPanel's duplicate main-render 'Back to Pool Tables' POSButton block (lines ~382-395, marked by the ArrowLeft icon) is deleted; the now-unused ArrowLeft import is removed; useNavigate/navigate is kept (used at 4 other call sites); the separate EmptyState 'Back to Pool Tables' CTA (~line 176, no-session state) is left untouched"
    - "CategoryForm's already-FormField-wrapped color input gets a D-05 comment; its '#6B7280' default + placeholder get D-08 comments; ModifierSheet's '#808080' fallback gets a D-08 comment — no color value changes"
  artifacts:
    - path: "src/widgets/TableStatusPanel/index.tsx"
      provides: "Row-remove Button + duplicate main-render back-button block removed + ArrowLeft import dropped"
      contains: "Button"
    - path: "src/features/manage-products/ui/CategoryForm.tsx"
      provides: "D-05 color-input comment + D-08 hex comments"
      contains: "TOKEN-01 exempt"
    - path: "src/features/add-item-to-tab/ui/ModifierSheet.tsx"
      provides: "D-08 hex fallback comment"
      contains: "TOKEN-01 exempt"
  key_links:
    - from: "src/pages/pool-table-status/index.tsx"
      to: "PageContainer backTo='/pool-tables'"
      via: "the surviving persistent back affordance after the duplicate main-render block is deleted"
      pattern: "backTo"
    - from: "src/widgets/TableStatusPanel/index.tsx"
      to: "@shared/ui/button"
      via: "Button for row-remove"
      pattern: "from '@shared/ui/button'"
---

<objective>
Handle the COMPONENT-03 duplicate-primitive finding plus its co-located COMPONENT-01 swap in `TableStatusPanel` (one file, one diff), and add the remaining TOKEN-01/D-05 documentation comments in `CategoryForm` and `ModifierSheet`.

Purpose: COMPONENT-01 + COMPONENT-02 + COMPONENT-03 + TOKEN-01 conformance. The COMPONENT-03 finding (D-09) is the redundant, persistent "Back to Pool Tables" `POSButton` block in the MAIN render of `TableStatusPanel/index.tsx` (lines ~382-395, identified by its `ArrowLeft` icon), superseded by Phase 30's `PageContainer backTo="/pool-tables"`. Removing it and swapping the row-remove button in the same task keeps the two edits to this file atomic.
Output: `TableStatusPanel` has one `Button` row-remove control and no duplicate persistent back affordance; `CategoryForm`/`ModifierSheet` hex user-data sites are documented as TOKEN-01 exempt.

Critical disambiguation: the string "Back to Pool Tables" appears TWICE in `TableStatusPanel/index.tsx` — (1) at ~line 176 as the `label` of an `EmptyState` `action` shown only in the "No active session" empty state, which is a DIFFERENT, legitimate affordance that MUST be kept; and (2) at ~line 393 inside the main-render `POSButton` block that IS the duplicate to delete. Only the second (the block with the `ArrowLeft` icon) is removed. Do NOT touch the EmptyState CTA at ~176.

Scope note: none of these files are D-01 payment-critical files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TableStatusPanel — Button row-remove + delete duplicate main-render back-button block (COMPONENT-01 + COMPONENT-03/D-09)</name>
  <files>src/widgets/TableStatusPanel/index.tsx</files>
  <read_first>
    - src/widgets/TableStatusPanel/index.tsx line 1 (lucide-react import: `AlertCircle, ArrowLeft, Clock, DollarSign, X`), the EmptyState `action: { label: 'Back to Pool Tables' }` at ~line 176 (KEEP — different affordance), the icon row-remove button at ~lines 244-254, and the duplicate main-render "Back button" `POSButton` block at ~lines 382-395 (the one containing `<ArrowLeft className="mr-2 size-4" />` + "Back to Pool Tables")
    - src/shared/ui/button.tsx (variant=ghost, size=icon-sm) and src/shared/ui/POSButton.tsx (already imported/used in this file elsewhere — leave those usages untouched)
    - src/pages/pool-table-status/index.tsx (confirms the surviving `PageContainer backTo="/pool-tables"` affordance from Phase 30)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md ("TableStatusPanel COMPONENT-03 duplicate-button deletion — exact boundaries": navigate is used at lines 113, 122, 178, 407, 422 — KEEP it; only ArrowLeft becomes unused)
  </read_first>
  <action>
    Swap: convert the icon-only row-remove button at ~244 (`title="Remove item"`, `X` icon, opens the PIN-for-removal flow) to `Button variant="ghost" size="icon-sm"`, keeping the existing `className`, `title="Remove item"`, and the `onClick` that sets `selectedItemForRemoval`/`showPinForRemoval`. Add `import { Button } from '@shared/ui/button';` if not already present.
    Delete (COMPONENT-03/D-09): remove the entire main-render "Back button" block at ~lines 382-395 — the `{/* Back button */}` comment, the wrapping `div`, and the `POSButton variant="ghost" touchSize="default"` with the `ArrowLeft` icon + "Back to Pool Tables" that calls `navigate('/pool-tables')`. Replace it with nothing — `PageContainer`'s `backTo="/pool-tables"` (rendered by `src/pages/pool-table-status/index.tsx` since Phase 30) already provides the single persistent back affordance. DO NOT delete or alter the `EmptyState` `action: { label: 'Back to Pool Tables', onClick: () => navigate('/pool-tables') }` at ~line 176 — that is a distinct no-session empty-state CTA and stays.
    Import cleanup: remove `ArrowLeft` from the `lucide-react` import on line 1 (confirmed used ONLY inside the deleted block — the EmptyState CTA uses no icon of its own). Do NOT remove `useNavigate`/`navigate` — still used at lines 113, 122, 178, 407, 422 (all `navigate('/pos')` or `navigate('/pool-tables')` outside the deleted block, including the EmptyState CTA at 178).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/widgets/TableStatusPanel/index.tsx && ! rg -n 'ArrowLeft' src/widgets/TableStatusPanel/index.tsx && rg -n 'navigate\(' src/widgets/TableStatusPanel/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/widgets/TableStatusPanel/index.tsx` returns no matches (row-remove swapped)
    - `rg 'ArrowLeft' src/widgets/TableStatusPanel/index.tsx` returns no matches — proves both the import and the duplicate main-render block are gone (ArrowLeft occurred only there)
    - `rg 'navigate\(' src/widgets/TableStatusPanel/index.tsx` still returns multiple matches (useNavigate/navigate kept, including the EmptyState CTA at ~178)
    - The EmptyState `label: 'Back to Pool Tables'` at ~line 176 is still present (NOT deleted) — the duplicate that was removed was the main-render `POSButton` block only
    - Row-remove control is now `Button variant="ghost" size="icon-sm"` with `title="Remove item"` preserved
    - `npm run typecheck` exits 0 (no unused-import error) and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Row-remove uses `Button`; duplicate main-render back-button block deleted; `ArrowLeft` import removed; EmptyState CTA + `navigate` retained; typecheck (no unused-symbol error) + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryForm + ModifierSheet — D-05 color comment + D-08 hex exemption comments</name>
  <files>src/features/manage-products/ui/CategoryForm.tsx, src/features/add-item-to-tab/ui/ModifierSheet.tsx</files>
  <read_first>
    - src/features/manage-products/ui/CategoryForm.tsx (the `#6B7280` default at line ~33, the color input already inside `FormField label="Color"` at ~128-155, the `#6B7280` placeholder at ~151)
    - src/features/add-item-to-tab/ui/ModifierSheet.tsx (the `'#808080'` fallback at line ~22)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md (exact TOKEN-01 exemption comment text + D-05 native-input comment text)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (CategoryForm:135 color already-compliant, hex sites table)
  </read_first>
  <action>
    CategoryForm D-05: directly above the native `input type="color"` (already inside `FormField label="Color"` — structurally compliant, no markup change) insert the inline comment `{/* native color input — no shared/ui color-picker primitive exists, see 31-CONTEXT.md D-05 */}`.
    CategoryForm D-08: insert the three-line TOKEN-01 exemption comment (verbatim from 31-PATTERNS.md — "TOKEN-01 exempt: category.color is arbitrary per-row USER DATA ... see 31-CONTEXT.md D-08") above BOTH hex sites: the `useState(initialCategory?.color ?? '#6B7280')` default at ~33 and the `placeholder="#6B7280"` at ~151. Do NOT change either `'#6B7280'` value.
    ModifierSheet D-08: insert the same three-line exemption comment above the `color: '#808080'` fallback at ~22. Do NOT change the `'#808080'` value.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && rg -c 'TOKEN-01 exempt' src/features/manage-products/ui/CategoryForm.tsx && rg -c 'TOKEN-01 exempt' src/features/add-item-to-tab/ui/ModifierSheet.tsx && rg -n '#6B7280' src/features/manage-products/ui/CategoryForm.tsx && rg -n '#808080' src/features/add-item-to-tab/ui/ModifierSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'TOKEN-01 exempt' src/features/manage-products/ui/CategoryForm.tsx` returns 2 (comment at both hex sites)
    - `rg -c 'TOKEN-01 exempt' src/features/add-item-to-tab/ui/ModifierSheet.tsx` returns 1
    - `CategoryForm.tsx` still contains a `D-05` reference comment above the `type="color"` input
    - The literal values `#6B7280` (CategoryForm, default + placeholder) and `#808080` (ModifierSheet) are unchanged (still present)
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both files carry TOKEN-01/D-08 exemption comments at their hex sites; CategoryForm's color input carries a D-05 comment; no color value changed; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | JSX swap + block deletion + developer-facing comments inside existing client-tier components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Tampering | TableStatusPanel | accept | No new attack surface: no new endpoints, no new input handling. Deleting the redundant main-render back button removes only a duplicate navigation affordance; the PIN-for-removal flow behind the row-remove button is unchanged. |
| T-31-04-02 | Denial of Service | Navigation regression | accept | Deleting the duplicate main-render block leaves the persistent `PageContainer backTo` affordance plus the separate no-session EmptyState CTA; `navigate` remains for all 5 in-file call sites — verified by keeping `navigate(` matches and the EmptyState label at ~176. |
| T-31-04-SC | Tampering | npm installs | accept | Zero package installs — `Button` and `FormField` already exist in `src/shared/ui/`. |
</threat_model>

<verification>
- `npm run typecheck` (must catch any unused `ArrowLeft`) + `npm run lint` exit 0
- `rg '\x3Cbutton' TableStatusPanel/index.tsx` and `rg 'ArrowLeft' TableStatusPanel/index.tsx` both return no matches
- `rg 'navigate\(' TableStatusPanel/index.tsx` still returns matches; the EmptyState `label: 'Back to Pool Tables'` at ~176 is still present
- `rg -c 'TOKEN-01 exempt'` returns 2 for CategoryForm and 1 for ModifierSheet
- Recommended wave-gate follow-up (not blocking, dev-server dependent): `npx playwright test e2e/16-table-status.spec.ts` — note Phase 30 documented pre-existing live-Supabase-RPC-latency flake on this spec, unrelated to this change
</verification>

<success_criteria>
`TableStatusPanel` has a `Button` row-remove control, no duplicate persistent back affordance, no unused `ArrowLeft` import, a retained `navigate`, and an intact no-session EmptyState CTA; `CategoryForm` and `ModifierSheet` document their `category.color` hex sites as TOKEN-01 exempt with no value change; typecheck + lint pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols** and creates no new files. It rewrites one JSX button tag to `Button`, deletes a redundant JSX block plus one now-unused import (`ArrowLeft`), and adds developer-facing exemption comments (D-05, D-08). All referenced identifiers (`Button`, `navigate`, `PageContainer`, `EmptyState`, existing state setters) pre-exist.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-04-SUMMARY.md` when done.
</output>
