---
phase: 31-component-token-spacing-consistency-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/split-tab/ui/SplitTabSheet.tsx
  - src/features/manage-combos/ui/ComboAvailabilityEditor.tsx
  - src/features/manage-categories/ui/CategoryTreeEditor.tsx
autonomous: true
requirements: [COMPONENT-01, COMPONENT-02, TOKEN-01]

must_haves:
  truths:
    - "SplitTabSheet's 3 raw buttons (two dashed add-tiles + one icon row-remove) render via Button, matching the file's own compliant sibling Button at line ~747 (D-02 in scope)"
    - "ComboAvailabilityEditor's day-of-week toggle chip renders via Button variant=outline with aria-pressed preserved, and its two type=time inputs are wrapped in FormField with the native time input kept (D-05)"
    - "CategoryTreeEditor's expand/collapse chevron renders via Button variant=ghost with its aria-label and tabIndex preserved; its already-FormField-wrapped color input gets a D-05 exemption comment; its '#6366f1' default gets a D-08 exemption comment"
  artifacts:
    - path: "src/features/split-tab/ui/SplitTabSheet.tsx"
      provides: "3 raw buttons converted to Button (D-02)"
      contains: "Button"
    - path: "src/features/manage-combos/ui/ComboAvailabilityEditor.tsx"
      provides: "Day chip as Button + 2 time inputs wrapped in FormField"
      contains: "FormField"
    - path: "src/features/manage-categories/ui/CategoryTreeEditor.tsx"
      provides: "Chevron as Button + D-05 color-input comment + D-08 hex comment"
      contains: "TOKEN-01 exempt"
  key_links:
    - from: "src/features/manage-combos/ui/ComboAvailabilityEditor.tsx"
      to: "@shared/ui/FormField"
      via: "FormField wraps native type=time input"
      pattern: "FormField"
    - from: "src/features/split-tab/ui/SplitTabSheet.tsx"
      to: "@shared/ui/button"
      via: "Button (same-file precedent at ~747)"
      pattern: "from '@shared/ui/button'"
---

<objective>
Convert three multi-concern feature files: `SplitTabSheet` (3 raw buttons, D-02 in scope), `ComboAvailabilityEditor` (1 day-chip button + 2 time inputs to FormField), and `CategoryTreeEditor` (1 chevron button + one already-compliant color input needing a D-05 comment + one hex default needing a D-08 exemption comment). Each file is bundled so its button-swap and input/token changes land in a single diff.

Purpose: COMPONENT-01 + COMPONENT-02 + TOKEN-01 conformance on files that carry more than one drift concern each; grouping them per-file avoids two plans racing on the same file.
Output: 3 files fully conformant — zero raw buttons, time inputs FormField-wrapped, color input + hex default documented as exempt.

Scope note: `SplitTabSheet.tsx` IS in scope per D-02 (tab-splitting UI, not a money-movement surface — COMPONENT-04 names only POS/payments/split-payment/refund/tip-distribution). The D-01 payment-critical files are NOT touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap SplitTabSheet's 3 raw buttons to Button (D-02)</name>
  <files>src/features/split-tab/ui/SplitTabSheet.tsx</files>
  <read_first>
    - src/features/split-tab/ui/SplitTabSheet.tsx (add-check dashed tile lines ~597-605, add-person dashed tile lines ~693-701, icon row-remove lines ~735-744, AND the compliant sibling `Button variant="outline" size="sm"` at ~747)
    - src/shared/ui/button.tsx (variant outline/ghost, size sm/icon-sm)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for SplitTabSheet:597,693,735)
  </read_first>
  <action>
    Convert the two dashed-border add-column tiles (`Add check` at ~597, `Add person` at ~693) to `Button variant="outline"`, keeping each existing dashed `className` (`min-w-[140px] flex flex-col items-center justify-center gap-2 rounded-lg border border-dashed ...`), `type="button"`, the `addItemColumn`/`addPersonColumn` onClicks, the `Plus` icon + label children, and the respective `aria-label`s ("Add sub-check column", "Add person"). Convert the icon-only row-remove button (~735, conditionally rendered when `amountRows.length > 2`) to `Button variant="ghost" size="icon-sm"`, keeping `type="button"`, the `removeAmountRow(row.id)` onClick, the dynamic `aria-label={`Remove check ${i + 1}`}`, and the `X` icon. Use the file's existing `Button` import (the file already has a compliant `Button` at ~747) — no new import. Do NOT convert these to `POSButton`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/split-tab/ui/SplitTabSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/split-tab/ui/SplitTabSheet.tsx` returns no matches
    - The file still contains aria-labels "Add sub-check column", "Add person", and a `Remove check` template, plus `size="icon-sm"` on the row-remove Button
    - The conditional render guard `amountRows.length > 2` is unchanged
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>All 3 SplitTabSheet raw buttons use `Button`; zero raw button elements; aria-labels/handlers/conditional-render preserved; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: ComboAvailabilityEditor — day-chip Button + wrap 2 time inputs in FormField (D-05)</name>
  <files>src/features/manage-combos/ui/ComboAvailabilityEditor.tsx</files>
  <read_first>
    - src/features/manage-combos/ui/ComboAvailabilityEditor.tsx (day-of-week toggle chip lines ~88-103, two type=time inputs lines ~107-138 with bare `label htmlFor={startId/endId}` pairing + the `hasTimeError` message)
    - src/shared/ui/FormField.tsx (label/error/hint wrapper — NOTE it clones its child and injects its own useId() id; startId/endId here are NOT externally referenced so the id clobber is harmless per RESEARCH Pitfall 1 / Assumption A2)
    - src/features/manage-products/ui/CategoryForm.tsx lines ~128-155 (analog: FormField wrapping a native non-text input)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for ComboAvailabilityEditor:88 and :113,125)
  </read_first>
  <action>
    Button: convert the day-of-week toggle chip (`aria-pressed`) at ~88 to `Button variant="outline"`, keeping the existing selected/unselected conditional `className` ternary verbatim, `key={iso}`, `type="button"`, `aria-pressed={selected}`, and the `onToggleDay` onClick. Add `import { Button } from '@shared/ui/button';` if not present.
    Inputs: wrap each of the two native `input type="time"` (From/To) in a `FormField` from `@shared/ui/FormField`. Replace the standalone `label htmlFor={startId}>From` + `input id={startId} type="time"` pair with `FormField label="From"` wrapping the native `input type="time"` (keep `value`, `onChange={onSetStartTime}`, className), and the same for To/`endId`. Keep the native `input type="time"` — no shared time-picker primitive exists (D-05). Leave the cross-field `hasTimeError` message as a sibling `p` below both fields (it applies to the pair, not one field) — do NOT force it into a single FormField's `error` slot. It is safe to let FormField own the id (startId/endId have no external references, confirmed by grep).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/manage-combos/ui/ComboAvailabilityEditor.tsx && rg -n 'FormField' src/features/manage-combos/ui/ComboAvailabilityEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/manage-combos/ui/ComboAvailabilityEditor.tsx` returns no matches
    - The file contains `FormField label="From"` and `FormField label="To"` and still contains `type="time"` on both native inputs
    - The `hasTimeError` message string ("End time must be after start time") is still present as a sibling paragraph, not inside a FormField error slot
    - The day chip contains `variant="outline"` and still has `aria-pressed`
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Day chip uses `Button variant="outline"`; both time inputs wrapped in `FormField` with native input kept; cross-field error preserved as sibling; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: CategoryTreeEditor — chevron Button + D-05 color comment + D-08 hex comment</name>
  <files>src/features/manage-categories/ui/CategoryTreeEditor.tsx</files>
  <read_first>
    - src/features/manage-categories/ui/CategoryTreeEditor.tsx (expand/collapse chevron lines ~166-180 — RE-READ THIS RANGE IN FULL before editing to confirm whether `tabIndex={-1}` is actually present, per RESEARCH Open Question 1; the color input already inside FormField at lines ~82-95; the `'#6366f1'` new-category default at line ~465)
    - src/shared/ui/button.tsx (variant=ghost, size=icon-sm)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for CategoryTreeEditor:166 button, :84-92 color comment, :465 hex)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md (TOKEN-01 exemption comment text to insert verbatim)
  </read_first>
  <action>
    Button: convert the icon-only expand/collapse chevron at ~166 to `Button variant="ghost" size="icon-sm"`, keeping the existing `className` (`flex size-5 shrink-0 items-center justify-center rounded ...`), `type="button"`, the dynamic `aria-label` (Expand/Collapse), the `onClick`, and — after re-reading lines 166-180 — preserve whatever `tabIndex` attribute is actually present (do NOT add or remove `tabIndex` speculatively). Add `import { Button } from '@shared/ui/button';` if not present. The file already imports `POSButton` elsewhere; leave those usages untouched.
    D-05 comment: directly above the native `input type="color"` (already inside `FormField label="Color"` at ~82-95, structurally compliant — no markup change), insert the inline comment: `{/* native color input — no shared/ui color-picker primitive exists, see 31-CONTEXT.md D-05 */}`.
    D-08 comment: directly above the `'#6366f1'` new-category default at ~465, insert the three-line exemption comment verbatim from 31-PATTERNS.md ("TOKEN-01 exempt: category.color is arbitrary per-row USER DATA ... see 31-CONTEXT.md D-08"). Do NOT change the `'#6366f1'` value — comment only.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/manage-categories/ui/CategoryTreeEditor.tsx && rg -n 'TOKEN-01 exempt' src/features/manage-categories/ui/CategoryTreeEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/manage-categories/ui/CategoryTreeEditor.tsx` returns no matches
    - The file still contains the chevron `aria-label` (Expand/Collapse) and `size="icon-sm"`
    - `rg 'TOKEN-01 exempt' src/features/manage-categories/ui/CategoryTreeEditor.tsx` returns a match (D-08 comment present at the hex site)
    - The file contains a `D-05` reference comment above the `type="color"` input, and the literal value `'#6366f1'` is unchanged
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Chevron uses `Button variant="ghost" size="icon-sm"` with tabIndex/aria-label preserved; color input has a D-05 comment; hex default has a D-08 exemption comment; value unchanged; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational JSX swaps + developer-facing comments inside existing client-tier feature components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Tampering | SplitTabSheet / ComboAvailabilityEditor / CategoryTreeEditor | accept | No new attack surface: no new endpoints, no new input handling. FormField wrap does not alter the time-input's controlled value/onChange or any Zod validation; the `hasTimeError` cross-field check is preserved unchanged. |
| T-31-03-02 | Information Disclosure | category.color hex default (D-08) | accept | The `#6366f1` value is a UI default color, not a secret; the change is a comment only, no value or data-flow change. |
| T-31-03-SC | Tampering | npm installs | accept | Zero package installs — `Button` and `FormField` already exist in `src/shared/ui/`. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `rg '\x3Cbutton'` across all 3 plan files returns no matches
- `rg 'FormField' ComboAvailabilityEditor.tsx` and `rg 'TOKEN-01 exempt' CategoryTreeEditor.tsx` each return a match
- No file in this plan is a D-01 payment-critical file (SplitTabSheet is in scope per D-02)
</verification>

<success_criteria>
All 3 files conformant: SplitTabSheet's 3 buttons and the two chip/chevron buttons are `Button`, ComboAvailabilityEditor's time inputs are FormField-wrapped with native input kept, CategoryTreeEditor's color input and hex default are documented as exempt; every aria-label/aria-pressed/handler/validation preserved; typecheck + lint pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols**. No decorators, classes, functions, hooks, CLI flags, struct/dataclass fields, or new file paths are created. It rewrites existing JSX button tags to the existing `Button` primitive, wraps existing native inputs in the existing `FormField` primitive, and adds two developer-facing code comments (D-05 native-input note, D-08 TOKEN-01 exemption). All referenced identifiers (`Button`, `FormField`, existing handlers/state, `startId`/`endId`) pre-exist.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-03-SUMMARY.md` when done.
</output>
