---
phase: 31-component-token-spacing-consistency-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx
  - src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx
autonomous: true
requirements: [COMPONENT-02]

must_haves:
  truths:
    - "ModifierGroupEditor's 2 raw type=checkbox inputs render via the shared/ui Checkbox primitive using onCheckedChange with strict === true comparison"
    - "HardwareSettingsTab's 6 mapped raw type=checkbox inputs render via Checkbox, keeping each id={receipt-<key>} + Label htmlFor pairing"
    - "No checkbox uses truthy-cast (!!c) — Radix CheckedState is boolean|'indeterminate', so === true is required (RESEARCH Pitfall 3)"
  artifacts:
    - path: "src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx"
      provides: "2 Checkbox instances replacing raw type=checkbox"
      contains: "onCheckedChange"
    - path: "src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx"
      provides: "6 mapped Checkbox instances replacing raw type=checkbox"
      contains: "onCheckedChange"
  key_links:
    - from: "src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx"
      to: "@shared/ui/checkbox"
      via: "import { Checkbox }"
      pattern: "from '@shared/ui/checkbox'"
    - from: "src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx"
      to: "@shared/ui/checkbox"
      via: "import { Checkbox }"
      pattern: "from '@shared/ui/checkbox'"
---

<objective>
Swap the 3 raw `type="checkbox"` sites (8 checkbox instances total) to the existing shared/ui `Checkbox` primitive per D-04, using the exact production pattern already in `IngredientForm.tsx:272-278`.

Purpose: COMPONENT-02 conformance on the checkbox class of inputs. NOTE (RESEARCH Pitfall 2 correction): `checkbox.tsx` is NOT an unused primitive — it already has 4 production consumers (`IngredientForm.tsx`, `ModifierSheet.tsx`, `ProductForm.tsx`, `ComboSlotCard.tsx`), so this is a low-risk swap of a proven primitive, not the wiring of an unproven one.
Output: `ModifierGroupEditor` (2 checkboxes) and `HardwareSettingsTab` (6 mapped checkboxes) use `Checkbox` with `onCheckedChange={(c) => setX(c === true)}`; zero raw checkbox inputs remain.

Scope note: neither file is a D-01 payment-critical file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModifierGroupEditor — swap 2 raw checkboxes to Checkbox (D-04)</name>
  <files>src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx</files>
  <read_first>
    - src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx (the "Required" checkbox lines ~253-263, the per-modifier row checkbox with explicit id pairing lines ~335-343)
    - src/shared/ui/checkbox.tsx (Radix wrapper: `checked`/`onCheckedChange`, CheckedState = boolean|'indeterminate')
    - src/features/manage-ingredients/ui/IngredientForm.tsx lines ~272-278 (the exact production pattern to copy: `checked={x}` + `onCheckedChange={checked => { setX(checked === true); }}`)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for ModifierGroupEditor:254-261 and :335-343)
  </read_first>
  <action>
    Replace the "Required (customer must select)" raw `input type="checkbox"` (~253-263) with `Checkbox` from `@shared/ui/checkbox`: `checked={isRequired}` + `onCheckedChange={(c) => { setIsRequired(c === true); }}` — replacing the old `onChange={e => setIsRequired(e.target.checked)}`. Keep the wrapping `label className="flex items-center gap-2 text-sm"` markup as-is (Radix Checkbox.Root renders a labelable element, so implicit label association still works; no id/htmlFor needed here).
    Replace the per-modifier row raw checkbox (~335-343) with `Checkbox`: keep the explicit `id={`mod-${m.id}`}` (pass it straight through — `Checkbox` does not clobber id), keep the sibling `label htmlFor={`mod-${m.id}`}`, use `checked={selected.has(m.id)}` + `onCheckedChange={() => { toggle(m.id); }}` (the toggle ignores the checked-state argument, same as the current `onChange`).
    Add `import { Checkbox } from '@shared/ui/checkbox';`. NEVER use `!!c` — always `=== true` (RESEARCH Pitfall 3).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n 'type="checkbox"' src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx && ! rg -n '!!c' src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx && rg -n 'onCheckedChange' src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type="checkbox"' src/features/manage-modifier-groups/ui/ModifierGroupEditor.tsx` returns no matches
    - The file contains `from '@shared/ui/checkbox'` and 2 `onCheckedChange` handlers, each using `=== true` or the argument-ignoring `toggle(...)` form
    - `rg '!!c' ...` returns no matches (no truthy-cast)
    - The per-modifier `id={` + `mod-` pairing and its sibling `htmlFor` are preserved
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both ModifierGroupEditor checkboxes use `Checkbox` with `=== true`; id/htmlFor pairing preserved; zero raw checkbox inputs; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: HardwareSettingsTab — swap 6 mapped raw checkboxes to Checkbox (D-04)</name>
  <files>src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx</files>
  <read_first>
    - src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx (the mapped-checkbox block lines ~135-159, mapping over `[showCashierName, showCustomerName, showReceiptNumber, boldTotals, printOnStart, autoCut]` with `id={receipt-<key>}` + `Label htmlFor`)
    - src/shared/ui/checkbox.tsx (Radix wrapper contract)
    - src/features/manage-ingredients/ui/IngredientForm.tsx lines ~272-278 (production pattern)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (row for HardwareSettingsTab:147-155)
  </read_first>
  <action>
    In the `.map()` that renders the 6 receipt-setting checkboxes, replace the raw `input type="checkbox"` with `Checkbox` from `@shared/ui/checkbox`. Keep `checked={receipt[key]}` unchanged; replace `onChange={e => patchReceipt({ [key]: e.target.checked })}` with `onCheckedChange={(c) => { patchReceipt({ [key]: c === true }); }}`. Keep the existing `id={`receipt-${key}`}` and the sibling `Label htmlFor={`receipt-${key}`}` pairing (the shared/ui `Label` is already compliant) — only the input tag changes. Add `import { Checkbox } from '@shared/ui/checkbox';`. NEVER use `!!c` — always `=== true`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n 'type="checkbox"' src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx && rg -n 'onCheckedChange' src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx && rg -n 'receipt-' src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type="checkbox"' src/widgets/SettingsTabsPanel/tabs/HardwareSettingsTab.tsx` returns no matches
    - The file contains `from '@shared/ui/checkbox'`, an `onCheckedChange` handler using `c === true`, and still contains the `receipt-` id + `Label htmlFor` pairing
    - `rg '!!c' ...` returns no matches
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>All 6 mapped checkboxes use `Checkbox` with `c === true`; id/Label pairing preserved; zero raw checkbox inputs; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Controlled-input markup swap inside existing client-tier components; `checked`/`onCheckedChange` remain client-local state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Tampering | ModifierGroupEditor / HardwareSettingsTab | accept | No new attack surface: no new endpoints, no new input validation. The `c === true` comparison is a UI-state concern, not a security boundary; underlying settings write path and Zod schemas are untouched. |
| T-31-05-02 | Tampering | HardwareSettingsTab receipt settings | accept | RBAC/`manage_settings` gating on the Settings surface is untouched by a markup-only checkbox swap; the `patchReceipt` write contract is unchanged. |
| T-31-05-SC | Tampering | npm installs | accept | Zero package installs — `Checkbox` already exists in `src/shared/ui/` with 4 production consumers. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `rg 'type="checkbox"'` across both plan files returns no matches
- `rg '!!c'` across both plan files returns no matches (strict `=== true` enforced)
- Manual/Storybook spot-check (per UI-SPEC States Checklist — no unit test files exist for these two files): toggle each checkbox in dev server, confirm checked-state persists and visually matches prior styling
</verification>

<success_criteria>
All 8 checkbox instances across the 2 files use the shared `Checkbox` primitive with `onCheckedChange` + strict `=== true` comparison; every id/htmlFor/Label pairing is preserved; zero raw checkbox inputs remain; typecheck + lint pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols** and creates no new files. It rewrites existing raw `input type="checkbox"` elements to the already-existing `Checkbox` primitive and adds `import { Checkbox } from '@shared/ui/checkbox'` to each file. All other identifiers (`Checkbox`, `patchReceipt`, `setIsRequired`, `toggle`, `receipt`) pre-exist.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-05-SUMMARY.md` when done.
</output>
