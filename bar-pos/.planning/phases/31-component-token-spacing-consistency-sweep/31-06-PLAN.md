---
phase: 31-component-token-spacing-consistency-sweep
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/AuditLogTable/AuditLogFilterBar.tsx
  - e2e/38-audit-logs.spec.ts
  - src/widgets/InventoryPagePanel.tsx
autonomous: true
requirements: [COMPONENT-02]

must_haves:
  truths:
    - "AuditLogFilterBar's 2 type=date inputs are wrapped in FormField with a visible label; the now-redundant aria-label is dropped"
    - "The load-bearing e2e/38-audit-logs.spec.ts date-from locator is switched off the id (which FormField clobbers) to getByLabel('Date from') in the SAME change — the wrap and the test fix are atomic"
    - "InventoryPagePanel's batch-delta signed number input is wrapped in FormField, keeping the native number input (NOT MoneyInput) so negative values still submit (D-06)"
  artifacts:
    - path: "src/widgets/AuditLogTable/AuditLogFilterBar.tsx"
      provides: "2 date inputs wrapped in FormField, aria-label dropped"
      contains: "FormField"
    - path: "e2e/38-audit-logs.spec.ts"
      provides: "date-from locator updated to getByLabel"
      contains: "getByLabel"
    - path: "src/widgets/InventoryPagePanel.tsx"
      provides: "batch-delta number input wrapped in FormField, native input kept"
      contains: "FormField"
  key_links:
    - from: "src/widgets/AuditLogTable/AuditLogFilterBar.tsx"
      to: "e2e/38-audit-logs.spec.ts"
      via: "FormField's <label> is what getByLabel('Date from') resolves against after id clobber"
      pattern: "getByLabel"
    - from: "src/widgets/InventoryPagePanel.tsx"
      to: "@shared/ui/FormField"
      via: "FormField wraps native type=number, no MoneyInput"
      pattern: "FormField"
---

<objective>
Wrap the remaining non-text inputs (2 date inputs, 1 signed-number input) in `FormField` for label consistency (D-05/D-06), keeping the native inputs. The `AuditLogFilterBar` wrap MUST land together with an E2E selector fix in the same task, because `FormField`'s `React.cloneElement(children, { id })` overwrites the child's `id` and would silently break `e2e/38-audit-logs.spec.ts:231`'s `page.locator('#audit-filter-date-from')` (RESEARCH Pitfall 1).

Purpose: COMPONENT-02 conformance on the date/number inputs, with the load-bearing E2E dependency handled atomically.
Output: `AuditLogFilterBar` date inputs FormField-wrapped + the E2E date-from locator switched to `getByLabel('Date from')`; `InventoryPagePanel` batch-delta FormField-wrapped with the native number input kept.

Scope notes: `LogoUploader.tsx` (`type="file"`) stays UNTOUCHED per D-07 — no shared file-input primitive exists and it is already `Label`-paired; this plan does not modify it. None of these files are D-01 payment-critical files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditLogFilterBar FormField wrap + atomic e2e/38 date-from selector fix (D-05, Pitfall 1)</name>
  <files>src/widgets/AuditLogTable/AuditLogFilterBar.tsx, e2e/38-audit-logs.spec.ts</files>
  <read_first>
    - src/widgets/AuditLogTable/AuditLogFilterBar.tsx (2 date inputs lines ~139-158: `input type="date" id="audit-filter-date-from" aria-label="Date from"` and `id="audit-filter-date-to" aria-label="Date to"`, the DATE_INPUT_CLASS, and the Select dropdowns above them that already use visible labels)
    - src/shared/ui/FormField.tsx (CONFIRMS the id clobber: `React.cloneElement(children, { id: useId(), ... })` — the child's explicit id is always overwritten by FormField's auto id, and FormField renders a real `<label htmlFor={autoId}>{label}</label>`)
    - e2e/38-audit-logs.spec.ts (line ~231: `page.locator('#audit-filter-date-from')` — the load-bearing locator that must move to getByLabel)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md (Pitfall 1: recommended fix is option 1 — `page.getByLabel('Date from')`)
  </read_first>
  <action>
    Wrap: replace each bare `input type="date"` with `FormField label="Date from"` / `FormField label="Date to"` from `@shared/ui/FormField`, wrapping the native `input type="date"` (keep `value={toDateInputValue(...)}`, `onChange`, and the `DATE_INPUT_CLASS`). Drop the now-redundant `aria-label` prop on each input — `FormField` supplies a real visible `<label>`, matching the visible-label pattern the sibling Select filters already use. It is expected and fine that FormField replaces the explicit `id="audit-filter-date-from"` with its own auto id; that is exactly why the E2E selector must move (below). Add `import { FormField } from '@shared/ui/FormField';` if not present.
    E2E fix (atomic — same task/commit): in `e2e/38-audit-logs.spec.ts`, change the date-from locator at ~line 231 from `page.locator('#audit-filter-date-from')` to `page.getByLabel('Date from')`. This resolves against FormField's rendered `<label htmlFor>` regardless of the auto id. If the spec also references `#audit-filter-date-to` anywhere, apply the same `getByLabel('Date to')` change for consistency. Do NOT ship the AuditLogFilterBar wrap without this selector update in the same diff.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && rg -n 'FormField' src/widgets/AuditLogTable/AuditLogFilterBar.tsx && ! rg -n 'audit-filter-date-from' e2e/38-audit-logs.spec.ts && rg -n "getByLabel\('Date from'\)" e2e/38-audit-logs.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AuditLogFilterBar.tsx` contains `FormField label="Date from"` and `FormField label="Date to"`, still contains `type="date"` on both native inputs, and no longer contains `aria-label="Date from"`/`aria-label="Date to"`
    - `rg 'audit-filter-date-from' e2e/38-audit-logs.spec.ts` returns no matches (old id locator gone)
    - `rg "getByLabel\('Date from'\)" e2e/38-audit-logs.spec.ts` returns a match (new accessible-name locator present)
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
    - (Wave-gate, dev-server dependent) `npx playwright test e2e/38-audit-logs.spec.ts -g "date range filter"` passes with the new locator
  </acceptance_criteria>
  <done>Both date inputs FormField-wrapped with visible labels + aria-label dropped; the e2e/38 date-from locator switched to `getByLabel('Date from')` in the same change; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: InventoryPagePanel — wrap batch-delta number input in FormField, keep native input (D-06)</name>
  <files>src/widgets/InventoryPagePanel.tsx</files>
  <read_first>
    - src/widgets/InventoryPagePanel.tsx (batch-delta block lines ~349-363: `label htmlFor="batch-delta"` + `input id="batch-delta" type="number"` + the `p` hint "Use negative numbers to remove stock.")
    - src/shared/ui/FormField.tsx (label/hint wrapper — id clobber is harmless here: `batch-delta` has no external references per RESEARCH grep / Assumption A2)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (row for InventoryPagePanel:350-362 — D-06 signed-delta opt-out, MoneyInput FORBIDDEN)
    - .planning/phases/17-modifier-inventory-rules/17-UI-SPEC.md (the signed-delta/no-MoneyInput precedent this follows)
  </read_first>
  <action>
    Replace the bare `label htmlFor="batch-delta">Quantity delta` + `input type="number"` + trailing hint `p` with `FormField label="Quantity delta" hint="Use negative numbers to remove stock."` wrapping the native `input type="number"` (keep `value={batchDelta}`, `onChange={e => setBatchDelta(e.target.value)}`, and its className). Keep the native number input — do NOT use `MoneyInput`: this is a signed quantity delta that must allow negative values (D-06, same precedent as Phase 17's signed-delta opt-out; `MoneyInput` clamps/formats as currency). Add `import { FormField } from '@shared/ui/FormField';` if not present. It is fine that FormField replaces the explicit `batch-delta` id (no external references).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && rg -n 'FormField' src/widgets/InventoryPagePanel.tsx && rg -n 'type="number"' src/widgets/InventoryPagePanel.tsx && ! rg -n 'MoneyInput' src/widgets/InventoryPagePanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `InventoryPagePanel.tsx` contains `FormField label="Quantity delta"` with `hint="Use negative numbers to remove stock."` and still contains `type="number"` on the native input
    - `rg 'MoneyInput' src/widgets/InventoryPagePanel.tsx` returns no matches (native input kept, not MoneyInput)
    - The `value={batchDelta}` / `setBatchDelta` binding is unchanged (no clamping added)
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>batch-delta wrapped in `FormField` with the native number input kept and no MoneyInput; negative values still bind/submit; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Controlled-input markup wrap inside existing client-tier components; input values/onChange and Zod validation unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | Tampering | AuditLogFilterBar / InventoryPagePanel | accept | No new attack surface: no new endpoints, no new validation. FormField wrap keeps the native input's controlled value/onChange; the batch-delta signed-value semantics are explicitly preserved (no MoneyInput clamp introduced). |
| T-31-06-02 | Tampering | E2E selector change | accept | The e2e/38 locator change is test-code only (no production behavior change); it asserts by accessible name (`getByLabel`), a stronger contract than the implementation-detail id it replaces. |
| T-31-06-SC | Tampering | npm installs | accept | Zero package installs — `FormField` already exists in `src/shared/ui/`. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `rg 'FormField'` returns matches in both `AuditLogFilterBar.tsx` and `InventoryPagePanel.tsx`; both still contain their native `type="date"`/`type="number"` inputs
- `rg 'audit-filter-date-from' e2e/38-audit-logs.spec.ts` returns no matches; `rg "getByLabel('Date from')" e2e/38-audit-logs.spec.ts` returns a match
- `rg 'MoneyInput' InventoryPagePanel.tsx` returns no matches
- Recommended wave-gate follow-up (dev-server dependent): `npx playwright test e2e/38-audit-logs.spec.ts -g "date range filter"`
</verification>

<success_criteria>
Both date inputs and the signed-number input are FormField-wrapped with the native inputs kept; the load-bearing e2e/38 date-from locator is updated to `getByLabel('Date from')` in the same change as the wrap; MoneyInput is not introduced anywhere; typecheck + lint pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols** and creates no new files. It wraps existing native inputs in the already-existing `FormField` primitive, drops two redundant `aria-label` props, and updates one Playwright locator string. `import { FormField } from '@shared/ui/FormField'` is added where needed. All other identifiers (`FormField`, `toDateInputValue`, `batchDelta`, `setBatchDelta`, `page.getByLabel`) pre-exist.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-06-SUMMARY.md` when done.
</output>
