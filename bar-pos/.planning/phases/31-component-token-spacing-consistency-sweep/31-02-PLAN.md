---
phase: 31-component-token-spacing-consistency-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/seat-waitlist-party/ui/SeatPartySheet.tsx
  - src/widgets/EmployeeSelector/EmployeeSelector.tsx
  - src/widgets/HomeDashboard/ui/HomeDashboard.tsx
  - src/widgets/PoolTableGrid/index.tsx
  - src/widgets/AuditLogTable/AuditLogTable.tsx
  - src/widgets/ProductSalesPanel/ProductSalesPanel.tsx
autonomous: true
requirements: [COMPONENT-01]

must_haves:
  truths:
    - "Card-tile selectors (seat-a-party table tiles, employee-picker cards) render via Button variant=outline with their card className and aria-pressed/disabled preserved"
    - "The two load-bearing chrome controls keep their data-testids: HomeDashboard nav tiles keep data-testid=home-tile-audit + the Lock/Badge overlays; PoolTableGrid Filters toggle keeps data-testid=pool-filters-toggle + aria-expanded"
    - "AuditLogTable sr-only diff trigger keeps its exact accessible name asserted by e2e/38-audit-logs.spec.ts; ProductSalesPanel sort pills keep their conditional selected/unselected className"
  artifacts:
    - path: "src/features/seat-waitlist-party/ui/SeatPartySheet.tsx"
      provides: "2 table-tile selectors as Button variant=outline"
      contains: "Button"
    - path: "src/widgets/EmployeeSelector/EmployeeSelector.tsx"
      provides: "Staff card selector as Button variant=outline"
      contains: "Button"
    - path: "src/widgets/HomeDashboard/ui/HomeDashboard.tsx"
      provides: "Nav-tile card as Button variant=ghost, data-testid + overlays preserved"
      contains: "data-testid"
    - path: "src/widgets/PoolTableGrid/index.tsx"
      provides: "Filters disclosure toggle as Button variant=ghost"
      contains: "pool-filters-toggle"
    - path: "src/widgets/AuditLogTable/AuditLogTable.tsx"
      provides: "sr-only diff trigger as Button variant=link"
      contains: "sr-only"
    - path: "src/widgets/ProductSalesPanel/ProductSalesPanel.tsx"
      provides: "Sort-toggle pill pair as Button variant=outline"
      contains: "Button"
  key_links:
    - from: "src/widgets/HomeDashboard/ui/HomeDashboard.tsx"
      to: "e2e/15-home-navigation.spec.ts + HomeDashboard.test.tsx"
      via: "data-testid=home-tile-audit + aria-label preserved"
      pattern: "home-tile-audit"
    - from: "src/widgets/AuditLogTable/AuditLogTable.tsx"
      to: "e2e/38-audit-logs.spec.ts"
      via: "sr-only accessible name 'View diff for ...'"
      pattern: "View diff for"
---

<objective>
Replace raw `<button>` elements with the shared/ui `Button` primitive across card/tile selectors, disclosure toggles, sort pills, and one sr-only trigger. Several of these carry load-bearing E2E contracts (data-testid, aria-expanded, sr-only accessible name) that MUST survive the swap unchanged.

Purpose: COMPONENT-01 conformance on the moderate-risk shared-chrome files (`HomeDashboard`, `PoolTableGrid`, `AuditLogTable`), where a dropped testid or renamed accessible name would break an existing Playwright/RTL assertion.
Output: 6 files converted to `Button`; all data-testids, aria-pressed/expanded, disabled, and accessible names preserved verbatim.

Scope: none of these files are D-01 payment-critical files. Icon/card/link/chip controls map to `Button` (not `POSButton`) per UI-SPEC — `POSButton`'s `min-h-[44px] active:scale-95` would visually regress a 160px nav tile, a 20px chevron, or an sr-only element.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap card-tile selectors (SeatPartySheet + EmployeeSelector) to Button variant=outline</name>
  <files>src/features/seat-waitlist-party/ui/SeatPartySheet.tsx, src/widgets/EmployeeSelector/EmployeeSelector.tsx</files>
  <read_first>
    - src/features/seat-waitlist-party/ui/SeatPartySheet.tsx (available-table tile lines ~119-133, occupied/disabled tile lines ~144-153)
    - src/widgets/EmployeeSelector/EmployeeSelector.tsx (staff-card tile lines ~31-46)
    - src/shared/ui/button.tsx (variant=outline, asChild default false)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for SeatPartySheet:119,144 and EmployeeSelector:31)
  </read_first>
  <action>
    Convert the two `SeatPartySheet` table-tile buttons and the one `EmployeeSelector` staff-card button to `Button variant="outline"` from `@shared/ui/button`, keeping each existing card `className` verbatim (`rounded-lg border p-4 text-left ...`, including the conditional selected-state `border-pos-accent bg-pos-accent/10` in SeatPartySheet's cn() call). Preserve every existing attribute exactly: `key`, `type="button"`, `aria-pressed`, `disabled` (the occupied tile), the dynamic `aria-label` on the occupied tile, and the `onClick` setters. Do not collapse the two-tile (available vs occupied) branching — keep both variants. Add `import { Button } from '@shared/ui/button';` to each file if not already present. If a file already imports `cn`, keep using it for the conditional className.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/seat-waitlist-party/ui/SeatPartySheet.tsx src/widgets/EmployeeSelector/EmployeeSelector.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/seat-waitlist-party/ui/SeatPartySheet.tsx src/widgets/EmployeeSelector/EmployeeSelector.tsx` returns no matches
    - `SeatPartySheet.tsx` still contains `aria-pressed`, a `disabled` occupied tile, and `border-pos-accent`
    - Both files contain `variant="outline"` and `from '@shared/ui/button'`
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both card-tile files use `Button variant="outline"`; zero raw button elements; aria-pressed/disabled/aria-label/className preserved; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap load-bearing chrome (HomeDashboard nav tiles + PoolTableGrid Filters toggle)</name>
  <files>src/widgets/HomeDashboard/ui/HomeDashboard.tsx, src/widgets/PoolTableGrid/index.tsx</files>
  <read_first>
    - src/widgets/HomeDashboard/ui/HomeDashboard.tsx (nav-tile markup lines ~156-190; note existing `import { Badge, Button } from '@shared/ui'` at line ~28)
    - src/widgets/PoolTableGrid/index.tsx (Filters disclosure toggle lines ~120-134; note existing `import { EmptyState, POSButton, ... } from '@shared/ui'` at line ~20)
    - src/shared/ui/button.tsx (variant=ghost)
    - src/widgets/HomeDashboard/ui/HomeDashboard.test.tsx (existing unit assertions on the tiles — run after edit)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for HomeDashboard:156 and PoolTableGrid:120)
  </read_first>
  <action>
    In `HomeDashboard.tsx`, convert the large nav-tile `button` (`min-h-[160px]`) to `Button variant="ghost"`, keeping the ENTIRE existing `className` string verbatim (the bespoke card classes fully define the visual; `ghost`'s own hover is superseded by the explicit `hover:bg-accent` via cn's tailwind-merge). Preserve `key`, `type="button"`, `onClick={() => handleItemClick(item)}`, `aria-label={item.label}`, the conditional `data-testid={item.path === '/audit' ? 'home-tile-audit' : undefined}`, and all children: the `Lock` overlay (with `data-testid="lock-icon"`), the `Icon`, the waitlist `Badge` overlay, and the label span. `Button` is already imported (line ~28) — no new import needed.
    In `PoolTableGrid/index.tsx`, convert the "Filters" disclosure `button` to `Button variant="ghost"`, keeping the existing `className`, `type="button"`, `onClick`, `data-testid="pool-filters-toggle"`, `aria-expanded={!filtersCollapsed}`, and the `ChevronRight`/`ChevronDown` conditional icon. Add `Button` to the existing `@shared/ui` import line (it currently imports `POSButton` from there but not `Button`).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/widgets/HomeDashboard/ui/HomeDashboard.test.tsx && ! rg -n '\x3Cbutton' src/widgets/HomeDashboard/ui/HomeDashboard.tsx src/widgets/PoolTableGrid/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/widgets/HomeDashboard/ui/HomeDashboard.tsx src/widgets/PoolTableGrid/index.tsx` returns no matches
    - `HomeDashboard.tsx` still contains `home-tile-audit`, `lock-icon`, and `aria-label={item.label}`
    - `PoolTableGrid/index.tsx` still contains `pool-filters-toggle` and `aria-expanded`
    - `npx vitest run src/widgets/HomeDashboard/ui/HomeDashboard.test.tsx` passes (existing tile assertions still green)
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>HomeDashboard tiles + PoolTableGrid Filters toggle use `Button variant="ghost"`; all data-testids/aria/overlays preserved; HomeDashboard unit test green; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Swap AuditLogTable sr-only trigger + ProductSalesPanel sort pills</name>
  <files>src/widgets/AuditLogTable/AuditLogTable.tsx, src/widgets/ProductSalesPanel/ProductSalesPanel.tsx</files>
  <read_first>
    - src/widgets/AuditLogTable/AuditLogTable.tsx (sr-only diff trigger lines ~69-78; note it already imports `Button` from `@shared/ui/button` at line ~21 and the file header comment about the load-bearing a11y name)
    - src/widgets/ProductSalesPanel/ProductSalesPanel.tsx (sort-toggle pill pair lines ~94-119)
    - src/shared/ui/button.tsx (variant=link, variant=outline)
    - src/widgets/AuditLogTable/AuditLogTable.test.tsx (existing unit assertions — run after edit)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for AuditLogTable:69 and ProductSalesPanel:94,107)
  </read_first>
  <action>
    In `AuditLogTable.tsx`, convert the `sr-only` diff-trigger `button` to `Button variant="link"`, keeping `className="sr-only"` exactly, `type="button"`, the `onClick={() => openSheet(auditLog)}`, and — critically — the exact `aria-label` template `View diff for {action} on {date}` unchanged (asserted by `e2e/38-audit-logs.spec.ts`). `Button` is already imported (line ~21) — swap the tag only, no new import.
    In `ProductSalesPanel.tsx`, convert both sort-toggle pill `button`s ("By Revenue", "By Units") to `Button variant="outline"`, keeping the existing conditional selected/unselected `className` ternary verbatim (the inline `sortBy === 'revenue' ? 'bg-primary ...' : 'border border-input ...'` string), `type="button"`, and the `setSortBy` onClicks. Add `import { Button } from '@shared/ui/button';` to `ProductSalesPanel.tsx` if not already present.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx && ! rg -n '\x3Cbutton' src/widgets/AuditLogTable/AuditLogTable.tsx src/widgets/ProductSalesPanel/ProductSalesPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/widgets/AuditLogTable/AuditLogTable.tsx src/widgets/ProductSalesPanel/ProductSalesPanel.tsx` returns no matches
    - `AuditLogTable.tsx` still contains `sr-only` and the literal `View diff for` accessible-name prefix
    - `ProductSalesPanel.tsx` still contains both "By Revenue" and "By Units" labels and the `bg-primary` selected-state class
    - `npx vitest run src/widgets/AuditLogTable/AuditLogTable.test.tsx` passes
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>AuditLogTable sr-only trigger uses `Button variant="link"` with its accessible name intact; ProductSalesPanel pills use `Button variant="outline"`; unit test green; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational JSX swaps inside existing client-tier widgets; no data flow, auth, or input-validation change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Tampering | HomeDashboard/PoolTableGrid/AuditLogTable | accept | No new attack surface: no new endpoints, no new input handling. RBAC/ProtectedAction gating on these widgets is untouched by a markup-only swap. |
| T-31-02-02 | Repudiation | AuditLogTable sr-only trigger | accept | The audit-log diff trigger only opens a read-only sheet; its accessible name is preserved verbatim, no audit-write path is altered. |
| T-31-02-SC | Tampering | npm installs | accept | Zero package installs — `Button` already exists in `src/shared/ui/`. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0
- `npx vitest run src/widgets/HomeDashboard/ui/HomeDashboard.test.tsx src/widgets/AuditLogTable/AuditLogTable.test.tsx` passes
- `rg '\x3Cbutton'` across all 6 plan files returns no matches
- data-testids `home-tile-audit`, `lock-icon`, `pool-filters-toggle` and the `View diff for` accessible name are all still present
</verification>

<success_criteria>
All 6 files use the shared `Button` primitive; every load-bearing data-testid, aria-expanded, aria-pressed, disabled, and sr-only accessible name is preserved verbatim; HomeDashboard + AuditLogTable unit tests stay green; typecheck and lint pass.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols**. No decorators, classes, functions, hooks, CLI flags, struct/dataclass fields, or new file paths are created. It rewrites existing JSX button tags to the already-existing `Button` primitive and (only in `ProductSalesPanel.tsx`) adds `import { Button } from '@shared/ui/button'`. `HomeDashboard.tsx`, `PoolTableGrid/index.tsx`, and `AuditLogTable.tsx` already import their target primitive. All other identifiers pre-exist.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-02-SUMMARY.md` when done.
</output>
