---
phase: 31-component-token-spacing-consistency-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/agent-chat/ui/AgentButton.tsx
  - src/features/agent-chat/ui/CommandChips.tsx
  - src/features/agent-chat/ui/FileDropZone.tsx
  - src/features/import-ingredients-csv/ui/CsvImportSheet.tsx
  - src/widgets/ManageIngredientsTab/index.tsx
  - src/widgets/PINLoginForm/PINLoginForm.tsx
autonomous: true
requirements: [COMPONENT-01]

must_haves:
  truths:
    - "The global AI-assistant FAB, its command chips, and the file-drop mic/send controls render via shared/ui Button instead of raw button elements"
    - "The three Download-template / Record-adjustment / Not-you-go-back text links render via shared/ui Button variant=link"
    - "Every swapped control keeps its existing aria-label, className, disabled, and onClick behavior byte-for-byte"
  artifacts:
    - path: "src/features/agent-chat/ui/AgentButton.tsx"
      provides: "Icon FAB as Button variant=ghost, rounded-full className preserved"
      contains: "Button"
    - path: "src/features/agent-chat/ui/CommandChips.tsx"
      provides: "Pill chips as Button variant=outline"
      contains: "Button"
    - path: "src/features/agent-chat/ui/FileDropZone.tsx"
      provides: "Mic + send as Button variant=ghost size=icon"
      contains: "Button"
    - path: "src/features/import-ingredients-csv/ui/CsvImportSheet.tsx"
      provides: "Download-template link as Button variant=link"
      contains: "Button"
    - path: "src/widgets/ManageIngredientsTab/index.tsx"
      provides: "Record-adjustment link as Button variant=link"
      contains: "Button"
    - path: "src/widgets/PINLoginForm/PINLoginForm.tsx"
      provides: "Go-back link as Button variant=link"
      contains: "Button"
  key_links:
    - from: "src/features/agent-chat/ui/AgentButton.tsx"
      to: "@shared/ui/button"
      via: "import { Button }"
      pattern: "from '@shared/ui/button'"
    - from: "src/widgets/PINLoginForm/PINLoginForm.tsx"
      to: "@shared/ui/button"
      via: "import { Button }"
      pattern: "from '@shared/ui/button'"
---

<objective>
Replace raw `<button>` elements with the shared/ui `Button` primitive across the isolated leaf-button files: the global agent-chat chrome (D-03) and three underlined text links. Pure markup swap — identical event handlers, identical classNames, identical accessible names. No behavior change.

Purpose: COMPONENT-01 conformance on the lowest-risk, single-concern files, proving the swap pattern before Phase 33 touches payment-critical surfaces.
Output: 6 files converted from raw button to `Button`; zero raw button elements remaining in any of them.

Scope boundary: agent-chat files (`AgentButton.tsx`/`CommandChips.tsx`/`FileDropZone.tsx`) are explicitly IN scope per D-03 even though `AgentButton` mounts globally in `router.tsx` and renders on `/pos`/`/payments` — it is floating chat chrome with zero payment logic. Payment-critical files (`pos/index.tsx`, `CartPanel.tsx`, `PaymentForm.tsx`, `TabPaymentCard.tsx`) are OUT of scope per D-01 and are not touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-RESEARCH.md
@.planning/phases/31-component-token-spacing-consistency-sweep/31-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap agent-chat raw buttons to Button (D-03)</name>
  <files>src/features/agent-chat/ui/AgentButton.tsx, src/features/agent-chat/ui/CommandChips.tsx, src/features/agent-chat/ui/FileDropZone.tsx</files>
  <read_first>
    - src/features/agent-chat/ui/AgentButton.tsx (current FAB markup, lines ~12-22)
    - src/features/agent-chat/ui/CommandChips.tsx (current pill markup, lines ~23-30)
    - src/features/agent-chat/ui/FileDropZone.tsx (current mic/send markup, lines ~152-169)
    - src/shared/ui/button.tsx (Button CVA contract: variant ghost/outline/link, size default/icon/icon-sm, forwards all ButtonProps)
    - src/features/split-tab/ui/SplitTabSheet.tsx line ~747 (compliant `Button variant="outline" size="sm"` sibling pattern to copy)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for AgentButton:12, CommandChips:23, FileDropZone:152,161)
  </read_first>
  <action>
    In `AgentButton.tsx`, replace the raw button FAB with `Button variant="ghost"` from `@shared/ui/button`, keeping the entire existing `className` (`fixed bottom-6 right-6 z-50 flex size-14 ... rounded-full ...`) verbatim so the round FAB shape is unchanged, and keeping `type="button"`, `onClick={toggle}`, `aria-label="Abrir asistente IA"`, and the `hasUnread` ping span + `Bot` icon children. Do NOT use `POSButton` here — it would add a redundant `min-h-[44px]` and fight the `rounded-full` shape (UI-SPEC anti-pattern).
    In `CommandChips.tsx`, replace the raw pill button with `Button variant="outline"`, keeping the `key`, `type="button"`, `onClick`, and the full pill `className` (`rounded-full border ... px-3 py-1 text-xs ...`).
    In `FileDropZone.tsx`, replace both raw icon buttons (mic toggle, send) with `Button variant="ghost" size="icon"`, keeping each existing `size-10` className, `type="button"`, `disabled`, dynamic `aria-label`, `onClick`, and the `Mic`/`MicOff`/`Send` icon children.
    Add `import { Button } from '@shared/ui/button';` to each file if not already present. Preserve all existing imports. Change nothing about state, handlers, or props beyond the tag + variant.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/agent-chat/ui/AgentButton.tsx src/features/agent-chat/ui/CommandChips.tsx src/features/agent-chat/ui/FileDropZone.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/agent-chat/ui/AgentButton.tsx src/features/agent-chat/ui/CommandChips.tsx src/features/agent-chat/ui/FileDropZone.tsx` returns no matches (all raw button elements removed)
    - Each of the 3 files contains `from '@shared/ui/button'` and at least one `<Button` tag
    - `AgentButton.tsx` still contains `aria-label="Abrir asistente IA"` and `rounded-full`
    - `FileDropZone.tsx` still contains two `size="icon"` Buttons and the dynamic mic aria-label branch
    - `npm run typecheck` exits 0 and `npm run lint` exits 0 (no new errors/warnings)
  </acceptance_criteria>
  <done>All 3 agent-chat files use `Button`; zero raw button elements; typecheck + lint clean; every aria-label/className/handler preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the 3 text-link raw buttons to Button variant=link</name>
  <files>src/features/import-ingredients-csv/ui/CsvImportSheet.tsx, src/widgets/ManageIngredientsTab/index.tsx, src/widgets/PINLoginForm/PINLoginForm.tsx</files>
  <read_first>
    - src/features/import-ingredients-csv/ui/CsvImportSheet.tsx (current "Download template" link, lines ~216-223)
    - src/widgets/ManageIngredientsTab/index.tsx (current "Record adjustment" link, lines ~268-276)
    - src/widgets/PINLoginForm/PINLoginForm.tsx (current "Not you? Go back" link, lines ~254-260)
    - src/shared/ui/button.tsx (variant=link renders an underline-on-hover text button)
    - .planning/phases/31-component-token-spacing-consistency-sweep/31-UI-SPEC.md (rows for CsvImportSheet:216, ManageIngredientsTab:268, PINLoginForm:254)
  </read_first>
  <action>
    Replace each of the three underlined text-link `button type="button"` elements with `Button variant="link"` from `@shared/ui/button`. Keep the existing `className` verbatim on each — `CsvImportSheet`: `flex items-center gap-1.5 text-sm text-primary underline-offset-4 hover:underline` + the `Download` icon + "Download template" text + `downloadTemplate` onClick; `ManageIngredientsTab`: `text-xs text-primary underline-offset-4 hover:underline` + "Record adjustment" text + the `setDialogState` onClick; `PINLoginForm`: `text-sm text-muted-foreground underline-offset-2 hover:underline` + "Not you? Go back" text + the `clearSelection` onClick. Add `import { Button } from '@shared/ui/button';` to each file if not already present. Do not alter the onClick handlers, the surrounding layout, or the visible copy.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! rg -n '\x3Cbutton' src/features/import-ingredients-csv/ui/CsvImportSheet.tsx src/widgets/ManageIngredientsTab/index.tsx src/widgets/PINLoginForm/PINLoginForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg '\x3Cbutton' src/features/import-ingredients-csv/ui/CsvImportSheet.tsx src/widgets/ManageIngredientsTab/index.tsx src/widgets/PINLoginForm/PINLoginForm.tsx` returns no matches
    - Each of the 3 files contains `variant="link"` on the swapped Button and `from '@shared/ui/button'`
    - Visible copy strings "Download template", "Record adjustment", and "Not you? Go back" are unchanged (still present verbatim)
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>All 3 text links use `Button variant="link"`; zero raw button elements; copy + handlers unchanged; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan crosses no new trust boundary — all changes are presentational JSX tag swaps inside existing client-tier React components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | All 6 files | accept | No new attack surface: no new endpoints, no new user-input handling beyond existing controlled handlers whose validation logic is unchanged. Markup/class-level swap only. |
| T-31-01-SC | Tampering | npm installs | accept | Zero package installs this phase — the `Button` primitive already exists in `src/shared/ui/`. No registry fetch, no slopcheck needed. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` exits 0 (max-warnings: 0)
- `rg '\x3Cbutton'` across all 6 plan files returns no matches
- No file in this plan is one of the D-01 payment-critical files
</verification>

<success_criteria>
All 6 leaf-button files use the shared `Button` primitive; every accessible name, className, disabled state, and onClick handler is preserved verbatim; typecheck and lint pass with zero new errors/warnings.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces **no new exported symbols**. No decorators, classes, functions, hooks, CLI flags, struct/dataclass fields, or new file paths are created. It only rewrites existing JSX button tags to the already-existing `Button` primitive and adds/keeps `import { Button } from '@shared/ui/button'` where needed. All identifiers referenced (`Button`, existing handlers/state) pre-exist in the codebase.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/31-component-token-spacing-consistency-sweep/31-01-SUMMARY.md` when done.
</output>
