---
phase: 23-reopen-closed-ticket
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - src/shared/lib/supabase.types.ts
  - src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4]
user_setup:
  - service: supabase
    why: "Applying the phase's migrations to the remote project requires an authenticated Supabase CLI session"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (only needed if `supabase db push` cannot use an interactive login in this environment)"
must_haves:
  truths:
    - "All Phase 23 migrations are applied to the remote database"
    - "supabase.types.ts includes Functions.reopen_tab, payments.status, tabs.reopen_count, tabs.last_reopened_at"
    - "reopen_tab flips a paid tab to open, voids its payment(s), records an offsetting caja entry, and writes an audit row (SC-1/SC-2/SC-4)"
    - "reopen_tab enforces the version guard, the 2x cap, and the 24h window (SC-1/SC-3)"
    - "Re-paying a reopened tab via process_payment_atomic does NOT double-count the reopened_void amount"
  artifacts:
    - src/shared/lib/supabase.types.ts
    - src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts
  key_links:
    - "supabase db push → live reopen_tab + payments.status + tabs columns → integration tests"
    - "Plan 03 exclusion filters → the double-count regression test's assertion"
---

<objective>
Apply the phase's four migrations to the remote database, regenerate the TypeScript
types, and convert the Wave-0 integration scaffold into live tests that prove
SC-1/SC-2/SC-3/SC-4 and the phase's highest-risk regression: that re-paying a
reopened tab does not double-count the voided original payment.

Purpose: Build/type checks pass without a push because types come from config, not
the live DB — so the push is mandatory and BLOCKING, and the integration tests are
the only thing that proves the RPC and the five patched sums actually behave. This
plan cannot be skipped without a false-positive verification.
Output: Migrations live, regenerated types, and a green live-Supabase integration suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-reopen-closed-ticket/23-RESEARCH.md
@.planning/phases/23-reopen-closed-ticket/23-PATTERNS.md
@supabase/migrations/20260720000004_reopen_tab_rpc.sql
@supabase/migrations/20260720000005_fix_payment_sums_exclude_reopened_void.sql
</context>

<artifacts_produced>
This phase produces (this plan's slice):
- Applied migrations (payments.status, tabs.reopen_count/last_reopened_at, reopen_tab, payment-sum fixes) on the remote DB
- Regenerated `src/shared/lib/supabase.types.ts` including `Functions.reopen_tab` and the new columns
- Live integration tests in `reopen-tab-rpc.integration.test.ts` (SC-1..SC-4 + double-count/report regressions)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply Phase 23 migrations to the remote database</name>
  <files>supabase/migrations/20260720000002_payments_status_column.sql, supabase/migrations/20260720000003_tabs_reopen_columns.sql, supabase/migrations/20260720000004_reopen_tab_rpc.sql, supabase/migrations/20260720000005_fix_payment_sums_exclude_reopened_void.sql</files>
  <read_first>
    - supabase/migrations/20260720000004_reopen_tab_rpc.sql (confirm the RPC is written before pushing)
    - supabase/migrations/20260720000005_fix_payment_sums_exclude_reopened_void.sql (confirm the 5-function patch is written before pushing)
    - .planning/STATE.md (Phase 17/22 session-log entries — the `npx supabase db push` precedent + the PGRST205 `NOTIFY pgrst` workaround)
  </read_first>
  <action>
    This is the [BLOCKING] schema-push task. Build/type checks pass WITHOUT it (types come from config,
    not the live DB), so it MUST run before verification or the phase reaches a false-positive green
    state. From `bar-pos/`, run `npx supabase db push` to apply the four Phase 23 migrations
    (`20260720000002..20260720000005`). If the environment cannot present an interactive login prompt,
    first `export SUPABASE_ACCESS_TOKEN=<token>` (per the user_setup entry) — the plan is
    `autonomous: false` precisely because this push may require a one-time auth gate. If PostgREST
    returns `PGRST205`/"table not in schema cache" after the push, run `NOTIFY pgrst, 'reload schema';`
    (documented Phase 05 workaround — the project may wake from pause with a stale schema cache).
    Confirm the four migrations show as applied before proceeding to the type regen.
  </action>
  <acceptance_criteria>
    - `npx supabase migration list` (or the project's equivalent) shows `20260720000002`, `20260720000003`, `20260720000004`, `20260720000005` as applied on the remote
    - Calling `reopen_tab` with a bogus tab id returns a Postgres/PostgREST response (function exists), not a "function does not exist" error
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx supabase migration list 2>&1 | tail -20</automated>
  </verify>
  <done>All four Phase 23 migrations are applied to the remote database; the reopen_tab function and the new columns are live.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate supabase.types.ts</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <read_first>
    - CLAUDE.md ("Missing generated types workaround" + the `npx supabase gen types typescript` note)
    - src/shared/lib/supabase.types.ts (confirm the current shape before regenerating so the diff is reviewable)
  </read_first>
  <action>
    Regenerate the generated types from the now-migrated remote schema:
    `npx supabase gen types typescript --project-id <project> > src/shared/lib/supabase.types.ts`
    (or the local-linked variant already used in this repo). Confirm the output contains
    `reopen_tab` under `Functions`, `status` under `payments` Row/Insert/Update, and `reopen_count` +
    `last_reopened_at` under `tabs` Row. If regeneration is not possible in this environment, hand-add
    those exact type members following the "Missing generated types workaround" in CLAUDE.md and note
    it in the SUMMARY. Do NOT leave a project-wide `as any` cast that would hide the new RPC's typing
    from useReopenTab (Plan 05).
  </action>
  <acceptance_criteria>
    - `grep -c "reopen_tab" src/shared/lib/supabase.types.ts` returns at least 1
    - `grep -c "reopen_count\|last_reopened_at" src/shared/lib/supabase.types.ts` returns at least 2
    - `npm run typecheck` shows no new errors beyond the 2 pre-existing documented ones
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && grep -q "reopen_tab" src/shared/lib/supabase.types.ts && grep -q "reopen_count" src/shared/lib/supabase.types.ts && echo TYPES_OK</automated>
  </verify>
  <done>supabase.types.ts includes reopen_tab, payments.status, and the two tabs columns; typecheck introduces no new errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert the integration scaffold to live SC-1..SC-4 + regression tests</name>
  <files>src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts</files>
  <read_first>
    - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts (full — reuse its exact harness: `describe.skipIf(skip)`, service-role `db` client, `createAuthStaff`/`signInClient`, `seedPaidTab`, caja seed/cleanup, afterEach tab cleanup)
    - src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts (the Plan 01 it.todo scaffold to fill in)
    - .planning/phases/23-reopen-closed-ticket/23-RESEARCH.md (Validation Architecture → Test Map — the exact behaviors each test must assert)
  </read_first>
  <behavior>
    - SC-1 happy path: reopen a paid tab (seeded with a completed payment) → `ok:true`; tab.status becomes 'open'; tab.closed_at is null; tab.version bumped by 1; tab.reopen_count is 1; the seeded payment row's status is 'reopened_void'.
    - SC-1 STALE_VERSION: wrong p_expected_version → error message contains 'STALE_VERSION'.
    - SC-1 AUTH_FORBIDDEN: bartender client → error message contains 'AUTH_FORBIDDEN'.
    - SC-3 REOPEN_CAP_EXCEEDED (D-03): seed reopen_count=2 → `ok:false, code:'REOPEN_CAP_EXCEEDED'`; no mutation.
    - SC-3 REOPEN_WINDOW_EXPIRED (D-02, window from most-recent reopen): seed last_reopened_at > 24h ago (and reopen_count<2) → `ok:false, code:'REOPEN_WINDOW_EXPIRED'`.
    - D-05 split-payment void: a paid tab with multiple completed payment siblings (shared tab_id) has ALL of them flipped to reopened_void by one reopen, while any is_refund=true row is left untouched.
    - SC-2 caja offset: a reopen with a non-zero voided total inserts exactly one `type='expense'` caja_entries row for the voided total; a payment-less closed tab (voided total 0) inserts none.
    - SC-4 audit: a successful reopen writes one audit_logs row with action='tab.reopen', entity_type='tab', non-null before/after, and after.reason equal to the passed reason.
    - Double-count regression (implicit, highest-risk): after reopening a paid tab, calling process_payment_atomic for the true owed amount closes the tab exactly once — the reopened_void amount is NOT added to the paid-line sum (assert the tab reaches 'paid' only when the NEW payment covers v_owed, not v_owed minus the voided amount).
  </behavior>
  <action>
    Rewrite `reopen-tab-rpc.integration.test.ts` replacing the it.todo placeholders with live tests
    mirroring the edit_paid_tab harness. Seed a paid tab WITH a real completed payment row (extend
    `seedPaidTab` to also insert a `payments` row with `status='completed'`, `is_refund=false`, an
    amount matching the item subtotal) so the void step and the double-count regression have real data.
    For the cap/window tests, use the service-role `db` client to set `tabs.reopen_count`/
    `tabs.last_reopened_at` directly on a seeded tab before calling the RPC (bump version accordingly).
    Reuse the caja seed/cleanup and cleanupCajaEntryConcepts pattern verbatim. Run the RPC through the
    authenticated `managerClient`/`bartenderClient` (auth.uid()-based role check requires a real JWT,
    not the service-role client). Assert each behavior above.
    Keep every test inside `describe.skipIf(skip)` so CI without live credentials skips cleanly.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts` passes all tests (or skips cleanly if `SUPABASE_SERVICE_ROLE_KEY`/anon/url are absent)
    - No remaining `it.todo` placeholders in the file
    - The double-count regression test is present and asserts the reopened_void amount is excluded from process_payment_atomic's paid-line
    - Test seeds and cleans up its own tabs/payments/caja_entries/audit_logs (no leaked rows)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx vitest run src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts</automated>
  </verify>
  <done>Live integration suite proves SC-1..SC-4 and the double-count regression against the migrated remote DB; no todo placeholders remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration files → remote database | The push is the point where written SQL becomes live behavior; unverified, build/type checks give a false green |
| authenticated manager JWT vs service-role → reopen_tab | The role re-check only sees a real auth.uid(); tests must exercise the authenticated client, not service-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | reopen_tab (verified) | high | mitigate | AUTH_FORBIDDEN test exercises a real bartender JWT and asserts rejection |
| T-23-03 | Tampering (data race) | reopen_tab (verified) | medium | mitigate | STALE_VERSION test asserts the version guard rejects a mismatched p_expected_version |
| T-23-04 | Tampering / Repudiation | process_payment_atomic post-reopen (verified) | high | mitigate | The double-count regression test proves the reopened_void amount is excluded from the paid-line sum |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (RESEARCH.md Package Legitimacy Audit) — nothing to slopcheck |
</threat_model>

<verification>
- BLOCKING push applied all four Phase 23 migrations to the remote DB
- supabase.types.ts includes reopen_tab + the new columns; typecheck clean (bar the 2 documented pre-existing errors)
- `npx vitest run src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts` green against live Supabase
</verification>

<success_criteria>
- Migrations live; types regenerated; SC-1..SC-4 + double-count regression proven by a green live integration suite.
</success_criteria>

<output>
Create `.planning/phases/23-reopen-closed-ticket/23-04-SUMMARY.md` when done
</output>