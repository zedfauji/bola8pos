---
phase: 23-reopen-closed-ticket
plan: 06
type: execute
wave: 5
depends_on: [23-05]
files_modified:
  - e2e/48-reopen-closed-ticket.spec.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "A manager can reopen a paid tab end-to-end from the PaymentPane payment-history row, PIN-gated"
    - "A bartender's own PIN is rejected by the reopen PIN gate (no reopen occurs)"
    - "After reopen, the tab is a normal open tab and its original payment shows as voided (Reopen button gone)"
    - "e2e/48-reopen-closed-ticket.spec.ts is registered in CLAUDE.md's E2E Test Suite list"
  artifacts:
    - e2e/48-reopen-closed-ticket.spec.ts
  key_links:
    - "ReopenTabDialog PIN gate (Plan 05) → the manager-positive / bartender-negative E2E assertions"
    - "reopen_tab RPC (Plan 02) → the DB-verified status flip + payment void in the spec"
---

<objective>
Convert the Wave-0 E2E scaffold into live Playwright tests proving the SC-3 UI flow
end-to-end — a manager reopening a paid ticket from PaymentPane through the PIN gate
— plus the bartender-negative gate, then register the spec and run the full phase gate.

Purpose: The integration suite (Plan 04) proves the RPC; this proves the actual
click-through a manager performs, including the D-06 outcome that a reopened ticket
is a fully normal open tab (no special reopened-mode UI). Per the project's
automation-first testing policy, this replaces any manual UAT with an automated E2E gate.
Output: A green `e2e/48-reopen-closed-ticket.spec.ts`, updated CLAUDE.md, and a clean
full phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-reopen-closed-ticket/23-RESEARCH.md
@e2e/47-edit-paid-tab.spec.ts
</context>

<artifacts_produced>
This phase produces (this plan's slice):
- Live `e2e/48-reopen-closed-ticket.spec.ts` (manager-positive reopen flow + bartender-negative gate)
- CLAUDE.md E2E Test Suite list updated (25 → 26 spec files, `48-reopen-closed-ticket` added)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fill in the reopen E2E spec (manager-positive + bartender-negative)</name>
  <files>e2e/48-reopen-closed-ticket.spec.ts</files>
  <read_first>
    - e2e/47-edit-paid-tab.spec.ts (full — the manager PIN-gated edit flow from /payments, its local seedPaidTab helper mirroring 35-refund.spec.ts, the ManagerPinDialog eligibleStaff-check as the real gate, DB-verify pattern; note resetTestState forces test-profile locales to es-MX so assert this phase's Spanish reopenTab strings)
    - e2e/helpers/auth.ts (loginAs(page,'admin'|'manager'|'bartender'); admin PIN is 0000)
    - src/features/reopen-tab/ui/ReopenTabDialog.tsx (the visible reopenTab.* Spanish strings + confirm button to target)
  </read_first>
  <action>
    Replace the test.fixme placeholders in `e2e/48-reopen-closed-ticket.spec.ts` with live tests
    mirroring 47-edit-paid-tab.spec.ts:
    (1) Manager-positive: seed a paid tab with a completed payment (reuse/adapt 47's seedPaidTab
        helper), log in as manager, open /payments, find the payment-history row, click the Reopen
        button, fill the reason, confirm, pass ManagerPinDialog with the manager PIN; then DB-verify
        the tab.status is 'open', tab.closed_at is null, tab.reopen_count is 1, and the seeded
        payment's status is 'reopened_void'; and UI-verify the Reopen button no longer shows for that
        (now voided) payment.
    (2) Bartender-negative: same setup, but the bartender's own PIN is rejected by ManagerPinDialog's
        eligibleStaff check (dialog stays open, no DB write — tab.status stays 'paid') — mirror 47's
        rewritten bartender assertion (gate is the PIN eligibleStaff check, not button visibility).
    Assert against this phase's genuine Spanish reopenTab strings (resetTestState pins es-MX). Seed
    and clean up all rows the spec creates.
  </action>
  <acceptance_criteria>
    - `grep -c "test.fixme" e2e/48-reopen-closed-ticket.spec.ts` returns 0 (all converted to live tests)
    - The spec DB-verifies tab.status='open' + payment.status='reopened_void' after the manager flow
    - The bartender-negative test asserts no reopen occurred (tab stays 'paid')
    - `npx playwright test e2e/48-reopen-closed-ticket.spec.ts` passes (run twice for stability if flaky)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx playwright test e2e/48-reopen-closed-ticket.spec.ts</automated>
  </verify>
  <done>The E2E spec proves the manager reopen click-through and the bartender-gate rejection against live Supabase, DB-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Register the spec in CLAUDE.md and run the full phase gate</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md ("E2E Test Suite" section — the numbered spec list, currently 25 files ending at 47-edit-paid-tab; the "Implemented Features" list format for a one-line Phase 23 entry)
  </read_first>
  <action>
    In CLAUDE.md's E2E Test Suite section, add `47-edit-paid-tab`'s successor `48-reopen-closed-ticket`
    to the spec list and bump the count (25 → 26 spec files). Optionally add a one-line `reopen-tab`
    entry to the Implemented Features list summarizing the phase (reopen_tab RPC + payments.status
    reopened_void + 24h/2x cap + offsetting caja entry + audit), following the existing entry style.
    Then run the full phase gate from bar-pos: `npm run typecheck` (expect only the 2 documented
    pre-existing errors), `npm run lint` (clean), `npm run test` (full unit suite — no regressions),
    and confirm `npx playwright test e2e/48-reopen-closed-ticket.spec.ts` is green. Record the gate
    results in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "48-reopen-closed-ticket" CLAUDE.md` returns at least 1
    - `npm run typecheck` shows no new errors beyond the 2 documented pre-existing ones
    - `npm run lint` exits 0
    - `npm run test` full unit suite passes with no new failures
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && grep -q "48-reopen-closed-ticket" CLAUDE.md && npm run typecheck 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Spec registered in CLAUDE.md; full phase gate (typecheck/lint/unit/E2E) green with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bartender PIN → ManagerPinDialog eligibleStaff check | The real manager+ enforcement point in the UI is the eligibleStaff filter, not button visibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | reopen UI gate (verified) | high | mitigate | Bartender-negative E2E asserts a non-manager PIN is rejected and no reopen occurs |
| T-23-02 | Repudiation / Tampering | reopen flow (verified) | medium | mitigate | Manager-positive E2E DB-verifies the audit-logged reopen actually flips status + voids payment (cap enforcement proven at the integration layer, Plan 04) |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (RESEARCH.md Package Legitimacy Audit) — nothing to slopcheck |
</threat_model>

<verification>
- `npx playwright test e2e/48-reopen-closed-ticket.spec.ts` green (manager-positive + bartender-negative)
- CLAUDE.md E2E list includes 48-reopen-closed-ticket
- Full phase gate: typecheck (bar 2 pre-existing) / lint / unit suite all clean
</verification>

<success_criteria>
- The reopen UI flow is proven end-to-end and gated by an automated E2E spec; docs updated; phase gate green.
</success_criteria>

<output>
Create `.planning/phases/23-reopen-closed-ticket/23-06-SUMMARY.md` when done
</output>
