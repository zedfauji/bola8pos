---
phase: 23-reopen-closed-ticket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/audit-actions.ts
  - src/shared/lib/rbac.ts
  - src/shared/lib/rbac.test.ts
  - src/shared/lib/domain.ts
  - src/entities/payment/model/queries.ts
  - src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts
  - e2e/48-reopen-closed-ticket.spec.ts
autonomous: true
requirements: [SC-1, SC-4]
must_haves:
  truths:
    - "'tab.reopen' is a registered audit action before any migration calls record_audit('tab.reopen', ...)"
    - "'reopen_tab' is a manager+ RBAC action, and canAccess('bartender','reopen_tab') is false"
    - "PaymentSchema.status and TabSchema.reopenCount/lastReopenedAt parse without breaking existing rows"
    - "A payment row's status surfaces on the mapped Payment object (defaults to 'completed')"
  artifacts:
    - src/shared/lib/audit-actions.ts
    - src/shared/lib/rbac.ts
    - src/shared/lib/domain.ts
    - src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts
    - e2e/48-reopen-closed-ticket.spec.ts
  key_links:
    - "audit-actions.ts 'tab.reopen' → CI grep in src/shared/lib/__tests__/audit-actions.test.ts against migration record_audit() calls"
    - "rbac.ts 'reopen_tab' in MANAGER_EXTRA → rbac.test.ts ALLOWED.manager mirror fixture"
    - "PaymentSchema.status → mapPaymentRow → PaymentPane ReopenTabButton visibility gate (Plan 05)"
---

<objective>
Lay the Wave-0 foundations every later plan depends on: register the `tab.reopen`
audit action, add `reopen_tab` as a manager+ RBAC action, extend the Zod domain
schemas with the new `payments.status` and `tabs.reopen_count`/`last_reopened_at`
fields, surface `status` through `mapPaymentRow`, and stand up the two pending
test scaffolds (integration + E2E) that later plans fill in.

Purpose: The audit action MUST be registered before the reopen_tab migration lands
(CI greps migrations, Pitfall 2). The domain/RBAC/type changes are pure additive
foundations with zero DB dependency, so they ship first and unblock every wave.
Output: Registered audit action + RBAC action + extended schemas + mapped payment
status + two pending test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-reopen-closed-ticket/23-CONTEXT.md
@.planning/phases/23-reopen-closed-ticket/23-RESEARCH.md
@.planning/phases/23-reopen-closed-ticket/23-PATTERNS.md
</context>

<artifacts_produced>
This phase produces (this plan's slice):
- Audit action string `'tab.reopen'` + const `AuditAction.TAB_REOPEN`
- RBAC action `'reopen_tab'` (added to `STAFF_ACTIONS` + `MANAGER_EXTRA`)
- `PaymentSchema.status` (`z.enum(['completed','reopened_void']).default('completed')`)
- `TabSchema.reopenCount`, `TabSchema.lastReopenedAt`
- `mapPaymentRow` now maps `status`
- Pending test scaffolds: `src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts`, `e2e/48-reopen-closed-ticket.spec.ts`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Register 'tab.reopen' audit action and 'reopen_tab' RBAC action</name>
  <files>src/shared/lib/audit-actions.ts, src/shared/lib/rbac.ts, src/shared/lib/rbac.test.ts</files>
  <read_first>
    - src/shared/lib/audit-actions.ts (full — the AuditActionSchema enum array + the AuditAction const object, mirror the existing 'tab.edit_paid'/TAB_EDIT_PAID pair)
    - src/shared/lib/rbac.ts (full — STAFF_ACTIONS array + MANAGER_EXTRA set, mirror the existing 'edit_paid_tab' entries; admin auto-inherits via `new Set(STAFF_ACTIONS)`)
    - src/shared/lib/rbac.test.ts (lines 12-49 — the hand-written ALLOWED mirror fixture; only ALLOWED.manager needs the new action added, admin uses `new Set(STAFF_ACTIONS)`)
    - .planning/phases/23-reopen-closed-ticket/23-PATTERNS.md (sections for audit-actions.ts + rbac.ts + rbac.test.ts)
  </read_first>
  <action>
    In audit-actions.ts: add `'tab.reopen'` to the `AuditActionSchema` enum array in the
    Tabs group (right after `'tab.edit_paid'`), and add `TAB_REOPEN: 'tab.reopen'` to the
    `AuditAction` const object (right after `TAB_EDIT_PAID`). This implements the SC-4
    audit-trail foundation and satisfies Pitfall 2 (CI greps migrations for record_audit
    action strings) BEFORE any migration is written.
    In rbac.ts: add `'reopen_tab'` to the `STAFF_ACTIONS` array (right after `'edit_paid_tab'`)
    and to the `MANAGER_EXTRA` set with a trailing comment noting manager+ per D-04.
    In rbac.test.ts: add `'reopen_tab'` to the `ALLOWED.manager` Set only (D-04 — manager+);
    do NOT touch `ALLOWED.admin` (it is `new Set(STAFF_ACTIONS)` and inherits automatically),
    do NOT add it to bartender or kitchen. This prevents the Phase 22 Pitfall 7 fixture-mismatch
    regression from recurring.
  </action>
  <acceptance_criteria>
    - `grep -c "tab.reopen" src/shared/lib/audit-actions.ts` returns 2 (enum entry + const value)
    - `grep -c "reopen_tab" src/shared/lib/rbac.ts` returns 2 (STAFF_ACTIONS + MANAGER_EXTRA)
    - `npx vitest run src/shared/lib/rbac.test.ts` passes with 'reopen_tab' present in ALLOWED.manager
    - `npx vitest run src/shared/lib/__tests__/audit-actions.test.ts` passes (no migration references 'tab.reopen' yet — the enforcement is a superset check, unregistered-action failure only)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib/rbac.test.ts src/shared/lib/__tests__/audit-actions.test.ts</automated>
  </verify>
  <done>'tab.reopen' registered in both the enum and const; 'reopen_tab' is a manager+ action visible in canAccess and the ALLOWED fixture; both test files green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PaymentSchema + TabSchema and map payments.status</name>
  <files>src/shared/lib/domain.ts, src/entities/payment/model/queries.ts</files>
  <read_first>
    - src/shared/lib/domain.ts (lines 427-458 TabSchema — note `closedAt: TimestampSchema.nullable()` and `version: z.number().int().nonnegative().optional()` conventions to match; lines 611-640 PaymentSchema — note the `isRefund`/`paymentGroupId`/`splitIndex` fields and the amount comment)
    - src/entities/payment/model/queries.ts (full — mapPaymentRow maps snake_case row keys to the Payment object; it currently omits `status`)
    - .planning/phases/23-reopen-closed-ticket/23-PATTERNS.md (domain.ts section — the exact field additions)
  </read_first>
  <action>
    In domain.ts PaymentSchema (D-01): add `status: z.enum(['completed', 'reopened_void']).default('completed')`.
    The `.default('completed')` is required so payment rows read before the migration lands (no
    `status` column yet) still parse cleanly.
    In domain.ts TabSchema (D-03): add `reopenCount` as a nonnegative optional int mirroring the
    existing `version` field convention, and `lastReopenedAt` as a nullable optional timestamp
    mirroring the existing `closedAt` field convention (reuse `TimestampSchema.nullable()`, do NOT
    invent a new date convention). Add a one-line comment on each noting Phase 23 / D-03.
    In queries.ts mapPaymentRow: add `status: row['status'] ?? 'completed'` to the object passed to
    `PaymentSchema.parse(...)`, so a voided payment's status surfaces on the mapped Payment (the
    `?? 'completed'` keeps it safe pre-migration). This is the value Plan 05's ReopenTabButton reads
    to hide itself after a reopen.
  </action>
  <acceptance_criteria>
    - `grep -c "reopened_void" src/shared/lib/domain.ts` returns 1 (the PaymentSchema.status enum)
    - `grep -c "reopenCount\|lastReopenedAt" src/shared/lib/domain.ts` returns 2
    - `grep -c "status: row\['status'\]" src/entities/payment/model/queries.ts` returns 1
    - `npm run typecheck` shows no NEW errors beyond the 2 pre-existing documented ones (tab/model/queries.ts, agent/rag.ts)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>PaymentSchema has `status` with a 'completed' default; TabSchema has `reopenCount`/`lastReopenedAt`; mapPaymentRow surfaces `status`; typecheck introduces no new errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create pending integration + E2E test scaffolds (Wave 0)</name>
  <files>src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts, e2e/48-reopen-closed-ticket.spec.ts</files>
  <read_first>
    - src/features/edit-paid-tab/model/edit-paid-tab-rpc.integration.test.ts (full — the exact live-Supabase seed/cleanup + temp-auth-user + signInWithPassword harness to mirror; note `describe.skipIf(skip)` and `seedPaidTab`)
    - e2e/47-edit-paid-tab.spec.ts (structure — the manager/bartender PIN-gate E2E shape to mirror)
    - .planning/phases/23-reopen-closed-ticket/23-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map, and Wave 0 Gaps)
  </read_first>
  <action>
    Create `src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts` as a pending scaffold:
    a `describe.skipIf(skip)('reopen_tab RPC (integration)', ...)` block with `it.todo(...)`
    placeholders (no live calls yet — Plan 04 fills these) covering: SC-1 happy path (closed→open,
    payment(s) flip to reopened_void, version bump), SC-1 STALE_VERSION, SC-1 AUTH_FORBIDDEN
    (bartender), SC-3 REOPEN_CAP_EXCEEDED (reopen_count=2), SC-3 REOPEN_WINDOW_EXPIRED
    (last_reopened_at > 24h), SC-2 offsetting caja_entries row, SC-4 audit_logs row, and the
    critical implicit regression: re-paying a reopened tab via process_payment_atomic does NOT
    double-count the reopened_void amount. Include the env-guard `const skip = !url || !anonKey || !serviceKey`
    header copied from the edit-paid-tab test.
    Create `e2e/48-reopen-closed-ticket.spec.ts` as a pending scaffold: `test.fixme(...)`
    placeholders (Plan 06 fills these) covering the manager PIN-gated reopen flow from the
    PaymentPane payment-history row and the bartender-negative case.
    Both files are pending-only in this plan — they must compile/collect without running real logic.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts` collects and reports todo tests (0 failed)
    - `grep -c "it.todo" src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts` returns at least 7
    - `grep -c "test.fixme" e2e/48-reopen-closed-ticket.spec.ts` returns at least 2
    - `npx playwright test e2e/48-reopen-closed-ticket.spec.ts --list` lists the fixme tests without error
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx vitest run src/features/reopen-tab/model/reopen-tab-rpc.integration.test.ts</automated>
  </verify>
  <done>Both scaffold files exist, compile, and enumerate pending placeholders for every SC plus the double-count regression; no failing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase RPC | Any authenticated client can call `reopen_tab` directly; the UI PIN gate is UX-only |
| migration record_audit() → AuditActionSchema | CI enforces every audited action is a registered enum value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | reopen_tab authz foundation (rbac.ts 'reopen_tab') | high | mitigate | This plan registers `reopen_tab` as manager+ only; the server-side RPC role re-check (Plan 02) is the real boundary. Foundation ensures `canAccess('bartender','reopen_tab')` is false. |
| T-23-04 | Tampering / Repudiation | payments.status schema foundation | high | mitigate | This plan adds `payments.status` to the schema + mapping so later plans can filter `reopened_void` rows out of every payment sum (Plan 03) — the invariant that prevents double-counting/double-refund. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new dependencies this phase (RESEARCH.md Package Legitimacy Audit) — nothing to slopcheck. |
</threat_model>

<verification>
- `npx vitest run src/shared/lib/rbac.test.ts src/shared/lib/__tests__/audit-actions.test.ts` green
- `npm run typecheck` — no new errors beyond the 2 documented pre-existing ones
- Integration + E2E scaffolds collect with 0 failures
</verification>

<success_criteria>
- `tab.reopen` registered; `reopen_tab` is manager+; PaymentSchema/TabSchema extended; `status` mapped; both test scaffolds pending and green.
</success_criteria>

<output>
Create `.planning/phases/23-reopen-closed-ticket/23-01-SUMMARY.md` when done
</output>
