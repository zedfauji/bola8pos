---
phase: 29-ui-drift-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-ui-drift.ts
  - .planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Running `npx tsx scripts/audit-ui-drift.ts` from bar-pos/ prints violation counts matching the verified baseline: 20 raw-button files, 8 raw-input files, 3 hex-color files, 0 arbitrary-spacing files"
    - "The route cross-check computes both counts dynamically (never hardcoded) and prints 17 real page routes vs the CLAUDE.md routes-table row count, plus the list of routes missing from CLAUDE.md"
    - "`DRIFT-AUDIT.md` exists at .planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md as a Markdown checklist grouped by file, one entry per file per violation category, with forward-slash paths"
    - "`npm run typecheck` passes with the new script under strict TS (scripts/ is in tsconfig include)"
  artifacts:
    - path: "scripts/audit-ui-drift.ts"
      provides: "Standalone Node/tsx drift scanner: filtered fs walk + per-category regex + route cross-check + Markdown writer"
      min_lines: 120
    - path: ".planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md"
      provides: "File-mapped violation backlog consumed by Phases 30-33"
      contains: "Route count"
  key_links:
    - from: "scripts/audit-ui-drift.ts"
      to: "src/pages, src/widgets, src/features"
      via: "fs.readdirSync recursive walk (filtered)"
      pattern: "readdirSync"
    - from: "scripts/audit-ui-drift.ts"
      to: "src/app/router.tsx + CLAUDE.md"
      via: "readFileSync + regex route/row counts"
      pattern: "router\\.tsx"
    - from: "scripts/audit-ui-drift.ts"
      to: ".planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md"
      via: "fs.writeFileSync"
      pattern: "writeFileSync"
---

<objective>
Produce a complete, file-mapped inventory of every design-system drift violation — raw `<button>`/`<input>` elements, hardcoded hex/rgb colors, and arbitrary-value Tailwind spacing classes — across all 17 routes in `src/pages/`, `src/widgets/`, `src/features/`, plus a route-count cross-check between `src/app/router.tsx` and the `CLAUDE.md` routes table.

Purpose: This audit is the backlog Phases 30-33 scope their standardization work from. It must be evidence (a script's reproducible output), not an assertion. Read-only — no application code under `src/` is modified; the only writes are the new script and its committed Markdown output.

Output:
- `scripts/audit-ui-drift.ts` — standalone `tsx` scanner (never imported by `src/`)
- `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md` — the committed, file-grouped checklist artifact (D-03)
</objective>

<artifacts_this_phase_produces>
New symbols created by this phase (exclude from any drift/source-grounding verification — they do not exist before this phase):

**New files:**
- `scripts/audit-ui-drift.ts`
- `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md`

**New top-level functions defined inside `scripts/audit-ui-drift.ts`** (module-local, never exported/imported):
- `collectFiles(root: string): string[]` — filtered recursive file walk
- `scanCategory(files: string[], pattern: RegExp)` — per-category line-attributed match collection
- `crossCheckRoutes()` — router.tsx vs CLAUDE.md route-count diff
- `renderMarkdown(...)` — assembles the DRIFT-AUDIT.md body
- `main()` — orchestrates scans, writes the file, prints the stdout summary

No new CLI flags, classes, decorators, exported symbols, DB objects, env vars, or dependencies are introduced. Zero new npm packages (RESEARCH Package Legitimacy Audit: N/A).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ui-drift-audit/29-CONTEXT.md
@.planning/phases/29-ui-drift-audit/29-RESEARCH.md
@.planning/phases/29-ui-drift-audit/29-VALIDATION.md
@scripts/indexCodebase.ts
@src/app/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/audit-ui-drift.ts (filtered scan + route cross-check + Markdown writer)</name>
  <files>scripts/audit-ui-drift.ts</files>
  <read_first>
    - `.planning/phases/29-ui-drift-audit/29-RESEARCH.md` — the source of truth for this task. It contains the exact verified regex patterns (Pattern 2 table), the filtered-file-walk code example (Pattern 1), the route cross-check code example, the verified baseline counts, and all five pitfalls. Do NOT re-derive any of these — transcribe them.
    - `scripts/indexCodebase.ts` — the established `scripts/` convention this file follows: plain `console.log`/`console.error` output (NOT `src/shared/lib/logger.ts` — that reach has no precedent and lint does not cover `scripts/`), Node builtin `fs`/`path` imports, run via `npx tsx`.
    - `src/app/router.tsx` — the route source of truth the script parses as text (never executes). Confirm the `<Route ...>`, `<Routes>`, and `<Navigate ...>` shapes before writing the route-count regex.
    - `CLAUDE.md` §Routes — the routes table the cross-check diffs against (rows match `^\| \`/`), and the "scripts/ is covered by strict TS / not covered by lint" constraints.
    - `tsconfig.json` — confirm `"include"` contains `"scripts"` (strict, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` all apply to this file).
  </read_first>
  <action>
    Create `scripts/audit-ui-drift.ts` as a standalone Node/tsx script (D-01: plain `fs` walk + regex, no AST/ts-morph, zero new dependencies). Structure it as module-local functions `collectFiles`, `scanCategory`, `crossCheckRoutes`, `renderMarkdown`, and `main`, run bottom-line via `main()`.

    Scan roots (D-05): `src/pages`, `src/widgets`, `src/features` only. `collectFiles(root)` walks each with `fs.readdirSync(root, { recursive: true, withFileTypes: true })`, keeps only `.ts`/`.tsx` files, and excludes: `*.test.ts(x)`, `*.stories.tsx` (Open Question 1 — exclude defensively even though zero currently match), and any path containing the `shared/ui/` segment (D-05). Resolve each file path with `entry.parentPath ?? entry.path` (Node 20.20.2 has `parentPath`; keep the `?? entry.path` fallback per Assumption A1 / Pitfall 4). This single filtered file list feeds all three content scans — do NOT use unfiltered substring grep (Pitfall 1: that inflates buttons from 20 to 29 by counting test mocks and `src/features/open-tab/README.md`).

    Run these four category scans using the exact verified patterns from RESEARCH Pattern 2:
    - Raw button — `/<button[\s>]/` (line-attributed). Expected: 20 files.
    - Raw input — `/<input[\s>]/`. Expected: 8 files.
    - Hardcoded color — `/#[0-9a-fA-F]{3,8}\b/` and `/rgba?\(/`. Expected: 3 files (all hex; zero rgb/rgba in the codebase).
    - Arbitrary-value spacing (D-04: spacing ONLY, not sizing/typography) — `/\b(?:[a-z:]+:)?(p|px|py|pt|pb|pl|pr|m|mx|my|mt|mb|ml|mr|gap|gap-x|gap-y|space-x|space-y)-\[[^\]]+\]/`. Expected: 0 files — report an honest 0; do NOT loosen the pattern to "find something" and do NOT extend it to `w-[Npx]`/`text-[Npx]` (Pitfall 2 / anti-pattern, out of D-04 scope).

    `scanCategory` reads each file once, splits into lines, and records `{ file, line, snippet }` for every match so the output can be grouped by file with line numbers (Open Question 2: include line numbers — the line-counting pass is needed anyway and gives Phases 30-33 precise backlog entries).

    `crossCheckRoutes()` (D-06) reads `src/app/router.tsx` and `CLAUDE.md` and COMPUTES both counts (never hardcode "13" or "14" — Pitfall 3): real page routes = `routerSrc.match(/<Route\s/g).length - routerSrc.match(/<Navigate\s/g).length` (anchor on `<Route ` with trailing whitespace so `<Routes>` is not double-counted — Pitfall 2; then subtract the `<Navigate>` redirect). CLAUDE.md route rows = `claudeMd.match(/^\| \`\//gm).length`. Expected result: 17 real routes, 14 CLAUDE.md rows, diff of 3 (the routes present in router.tsx but absent from the CLAUDE.md table). Print the two numbers and the list of missing route paths — computed dynamically, so if the repo drifted since research the output stays truthful.

    `renderMarkdown` assembles a single Markdown document: a top summary block (per-category file counts + the route cross-check numbers and missing-route list), then one `## <category>` section per violation category, each grouped by file (`### <path>` sub-headings) with a checklist item (`- [ ] line N: <snippet>`) per match. Normalize EVERY displayed path with `.replace(/\\/g, '/')` so Windows backslashes become forward slashes (Pitfall 5) — matches every other git-tracked doc in the repo.

    `main()` runs all scans + the route cross-check, calls `fs.writeFileSync` to write the rendered Markdown to `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md` (D-03: the script writes the file, it does not only print), AND prints a compact summary line per category plus the route diff to `console.log` (so the counts are diffable at the terminal without opening the file).

    Strict-TS discipline (Pitfall 4): guard every `.match()`/`.exec()` result and array index — they type as `T | undefined` under `noUncheckedIndexedAccess`. Never use `any`. Never write `prop?: string`; use `prop: string | undefined`. All paths, roots, and patterns are constants at the top of the file. No Supabase/network/secrets — this script has none.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npx tsx scripts/audit-ui-drift.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-ui-drift.ts` exists and `npm run typecheck` exits 0 (compiles under the repo's strict tsconfig that includes `scripts/`).
    - Running `npx tsx scripts/audit-ui-drift.ts` from `bar-pos/` prints a per-category summary whose counts equal the verified baseline: raw-button = 20 files, raw-input = 8 files, hex-color = 3 files, arbitrary-spacing = 0 files. (If any count differs, the discrepancy must be explained by a documented repo change since 2026-07-10 — not a filtering bug; e.g. confirm `.test.*` and `.stories.*` and `shared/ui/` are still excluded.)
    - The printed route cross-check shows 17 real page routes and the CLAUDE.md route-row count computed from the file (expected 14), plus the list of routes missing from CLAUDE.md — none of these three numbers appear as a hardcoded literal in the source (grep the script: no bare `17`/`14`/`13` standing in for the counts).
    - Source contains `fs.readdirSync(` with `recursive: true`, excludes `.test.`, `.stories.`, and the `shared/ui` path segment, and normalizes output paths with `.replace(/\\/g, '/')`.
    - No `import` of `@shared/*` / `src/shared/*` (script stays a pure `scripts/` tool); output uses `console.log`/`console.error`, not a logger import.
  </acceptance_criteria>
  <done>The script compiles under strict TS and, when run, prints the four baseline counts (20/8/3/0) and the dynamically-computed route diff (17 vs 14, 3 missing), with no hardcoded count literals.</done>
</task>

<task type="auto">
  <name>Task 2: Generate, verify, and commit DRIFT-AUDIT.md backlog</name>
  <files>.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md</files>
  <read_first>
    - `scripts/audit-ui-drift.ts` — the script from Task 1 that produces this artifact; confirm its `fs.writeFileSync` target path is exactly `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md`.
    - `.planning/phases/29-ui-drift-audit/29-RESEARCH.md` — the verified baseline table (20 button / 8 input / 3 hex / 0 spacing / 17 routes / 14 CLAUDE.md rows) to diff the generated output against.
    - `.planning/phases/29-ui-drift-audit/29-CONTEXT.md` — D-02 (single Markdown checklist grouped by file, not JSON) and D-03 (committed at this exact path) define the required artifact shape.
  </read_first>
  <action>
    Run `npx tsx scripts/audit-ui-drift.ts` from `bar-pos/` to generate `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md`. Open the generated file and confirm it is a Markdown checklist grouped by file (per D-02): a summary block at the top (four category counts + route cross-check), then one section per violation category with `### <path>` file sub-headings and `- [ ] line N: <snippet>` checklist items under each. Confirm every path uses forward slashes (no `\`). Confirm the file's summary counts match the verified baseline (20/8/3/0) and the route section shows 17 real routes vs 14 CLAUDE.md rows with the 3 missing routes listed — or, if the repo changed since 2026-07-10, that any delta is real and noted, not a filtering bug.

    Do not hand-edit the generated Markdown to "fix" numbers — the artifact must be the script's genuine output (it is evidence for Phases 30-33). If the format is wrong, fix the script in Task 1's file and regenerate, do not patch the `.md` by hand.

    Stage and commit both the script and the generated artifact together with a Conventional Commit message (e.g. `chore(29-01): add UI drift audit script + DRIFT-AUDIT.md backlog`). No `--no-verify`; pre-commit hooks must pass.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md</automated>
    <human-check>Open DRIFT-AUDIT.md: confirm it is a file-grouped Markdown checklist (one entry per file per category), counts match the 20/8/3/0 baseline, route cross-check shows 17 vs 14 with 3 missing routes, and all paths use forward slashes.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md` exists and was produced by running the script (not hand-written).
    - The file contains a summary section whose four category counts equal 20 (button) / 8 (input) / 3 (hex) / 0 (spacing), and a route-count section stating 17 real routes, the CLAUDE.md row count (14), and the list of routes missing from CLAUDE.md.
    - Violations are grouped by file with per-line checklist entries; all displayed paths use `/` (grep the file: zero `\` in path strings).
    - Both `scripts/audit-ui-drift.ts` and `DRIFT-AUDIT.md` are committed in one Conventional-Commit commit; `git status` is clean for these two paths afterward.
  </acceptance_criteria>
  <done>`DRIFT-AUDIT.md` is a committed, file-grouped Markdown backlog whose counts match the verified baseline, ready for Phases 30-33 to scope from.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | The script has no network I/O, no user/external input, no auth surface, no secrets, and no data persistence beyond a git-tracked Markdown file. It reads only repository files it is run against and writes one doc under `.planning/`. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | `scripts/audit-ui-drift.ts` output | accept | Read-only script; only write target is a git-tracked doc reviewed in the same commit. No untrusted input reaches it (reads repo source files only). Per RESEARCH Security Domain: no STRIDE-relevant surface. |
| T-29-02 | Information Disclosure | scanned source files | accept | Output is a violation list of file paths + short code snippets already in the repo; no secrets are read or emitted (script makes no Supabase/env/network calls). |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — zero new dependencies (RESEARCH Package Legitimacy Audit: N/A). `tsx` and Node builtins are already present. No install step to slopcheck. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 with the new script present (confirms strict-TS compilation, `scripts/` in tsconfig include).
- `npx tsx scripts/audit-ui-drift.ts` prints the verified baseline counts: 20 button / 8 input / 3 hex / 0 spacing files, and the route diff 17 vs 14 (3 missing).
- `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md` exists, is a file-grouped Markdown checklist, and its counts match the baseline.
- No file under `src/` is modified (read-only phase); only `scripts/audit-ui-drift.ts` and the DRIFT-AUDIT.md artifact are added.
</verification>

<success_criteria>
1. **AUDIT-01** — `scripts/audit-ui-drift.ts` identifies every raw `<button>`/`<input>`, hardcoded hex/rgb color, and arbitrary-value spacing class across `pages/`/`widgets/`/`features/`, file-attributed, matching the verified baseline (20/8/3/0), and prints a dynamically-computed route-count cross-check (17 real routes vs 14 CLAUDE.md rows, 3 missing).
2. **AUDIT-02** — `DRIFT-AUDIT.md` is a committed Markdown checklist grouped by file (one entry per file per category, with line numbers), located at `.planning/phases/29-ui-drift-audit/DRIFT-AUDIT.md`, usable as the backlog Phases 30-33 scope from.
3. `npm run typecheck` passes; no `src/` code modified.
</success_criteria>

<output>
Create `.planning/phases/29-ui-drift-audit/29-01-SUMMARY.md` when done.
</output>
