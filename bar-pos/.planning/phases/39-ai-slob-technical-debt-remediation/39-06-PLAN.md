---
phase: 39-ai-slob-technical-debt-remediation
plan: 06
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - e2e/23-payment-edge-cases.spec.ts
  - e2e/24-pool-advanced.spec.ts
  - e2e/24-sprint5-pool-accuracy.spec.ts
  - e2e/24-waitlist.spec.ts
  - e2e/25-rappi-orders.spec.ts
  - e2e/26-field-validation.spec.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md
  - .planning/todos/ (append-only — plan 39-06 creates new todo files, never edits an existing one)
autonomous: true
requirements: [D-03, D-04, D-06]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 34 E2E findings across specs 23 through 26 carry a written classification backed by real Playwright error output or by the literal skip-reason string (D-04), satisfying D-06's requirement for real per-test investigation of the unannotated findings."
    - "The seven `24-sprint5-pool-accuracy.spec.ts` failures are grouped by shared root cause before being written as rows, since a whole-file failure is a fixture signature rather than seven independent bugs (39-RESEARCH.md Pitfall 3)."
    - "Every real product bug found is filed as a todo rather than fixed inline (D-03)."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md — one row per finding, in 39-02-LEDGER.md's format."
    - "One `.planning/todos/*.md` per distinct real product bug (zero is a valid outcome)."
  key_links:
    - "`.audit-tmp/e2e-per-spec/<spec>.json` -> `suites[].suites[].specs[].tests[].results[].error.message`."
    - "39-05-LEDGER.md's verdict on assumption A2 -> this plan's stale-skip triage for `25-rappi-orders.spec.ts`."
  prohibitions:
    - "Do NOT delete a failing test to reach green."
    - "Do NOT fix a real product bug inline; file a todo (D-03)."
    - "Do NOT un-skip a test and record it as fixed without running the spec."
    - "Do NOT touch any spec file outside the 6 listed in `files_modified` — the other Track A plans own them concurrently."
    - "Do NOT introduce new UI, copy, or components while fixing a test (39-UI-SPEC.md guardrails 1-3, 5)."
---

<objective>
Triage the 34 E2E findings in spec files 23 through 26 to a written disposition.

This is the money-and-billing batch: payment edge cases, pool-session billing accuracy, and field validation. It also carries the phase's cleanest shared-cause candidate — all seven tests in `24-sprint5-pool-accuracy.spec.ts` failed, including two that merely assert a button is visible. A spec where even the visibility assertions fail is describing a fixture or navigation problem, not seven independent billing defects, and writing seven separate rows before establishing that would be wasted work.

Purpose: convert 34 raw findings into classified, actionable rows without inflating a single fixture failure into a bug list.
Output: a filled ledger, todos for real bugs, and test-side fixes where the test itself was wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-UI-SPEC.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No new source symbols. This plan produces a classification ledger, zero or more todo files, and test-side edits confined to the 6 spec files listed in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Establish the shared root cause for the two pool-billing spec clusters</name>
  <files>e2e/24-sprint5-pool-accuracy.spec.ts, e2e/24-pool-advanced.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/24-sprint5-pool-accuracy.json` and `.audit-tmp/e2e-per-spec/24-pool-advanced.json` — the real error output for all failures (regenerate a stale file with the `PLAYWRIGHT_JSON_OUTPUT_FILE` single-spec command from 39-RESEARCH.md's Code Examples).
    - `e2e/24-sprint5-pool-accuracy.spec.ts` — its `beforeEach` or fixture setup, which is where a whole-file failure usually originates.
    - `10-CHECKLIST.md` Blocking > e2e entries for both specs, and the Phase 38 row in "Cross-check against existing trackers" describing the pool-table seed-state root cause that already explains five failures in `04-pool-timer.spec.ts`.
    - `39-RESEARCH.md` Pitfall 3.
  </read_first>
  <action>
    Handle `24-sprint5-pool-accuracy.spec.ts` first: seven failures at lines 193, 211, 227, 253, 306, 367, and 413. Lines 193 and 211 only assert that an Edit Start Time button is visible, and a visibility assertion failing alongside the billing assertions points at the fixture never reaching the occupied-table state the whole spec depends on. Read the errors, identify how many distinct root causes actually exist, and say so in the ledger before writing per-test rows. If the cause is pool-table seed state, note the overlap with Phase 38's already-confirmed item 2 and route accordingly rather than opening a parallel investigation.

    Then `24-pool-advanced.spec.ts`: failures at lines 57, 166, and 203, plus skips at lines 53, 99, 128, and 219. Several skip reasons in this spec read like missing available tables, which is a runtime-conditional skip on seed state rather than a stale placeholder — check each rather than assuming.

    The billing assertions in both specs (15-minute minimum charge, proportional charge, rate display) are correctness-critical. If a failure survives the shared-cause analysis and shows the app computing a wrong charge, that is a `real-regression` and gets a todo, not a test edit.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/24-(sprint5-pool-accuracy|pool-advanced)\.spec\.ts:' .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md)" -ge 14</automated>
  </verify>
  <acceptance_criteria>
    - `39-06-LEDGER.md` has rows for all 7 `24-sprint5-pool-accuracy.spec.ts` findings and all 7 `24-pool-advanced.spec.ts` findings.
    - The ledger states an explicit root-cause group count for each spec and maps every test to a group.
    - Any finding routed to Phase 38 cites the specific seed-state evidence in the error output.
    - Any billing-math failure surviving the shared-cause analysis is classified `real-regression`, not `harness`.
  </acceptance_criteria>
  <done>Both pool-billing clusters are reduced to their actual root causes with evidence, rather than enumerated as independent bugs.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the remaining 20 findings in specs 23, 24-waitlist, 25, and 26</name>
  <files>e2e/23-payment-edge-cases.spec.ts, e2e/24-waitlist.spec.ts, e2e/25-rappi-orders.spec.ts, e2e/26-field-validation.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/` JSON for `23-payment-edge-cases`, `24-waitlist`, `25-rappi-orders`, and `26-field-validation`.
    - `10-CHECKLIST.md` Blocking > e2e for the exact titles at each line.
    - `39-05-LEDGER.md` — its verdict on assumption A2, which directly informs how much weight to give `25-rappi-orders.spec.ts`'s `'UI not implemented — EXPECTED FAIL'` skip reasons.
    - `CLAUDE.md` "Implemented Features" and "Routes" — including the `/rappi` route and the waitlist feature set.
    - `39-02-LEDGER.md` — classification scheme and row format.
  </read_first>
  <action>
    `23-payment-edge-cases.spec.ts` — failures at lines 263 and 304, skips at lines 174, 203, and 230. Line 263 (a tab with only a pool charge and no order items can be paid) and line 304 (paying a tab with a running pool session shows an error) are both payment-correctness assertions; treat a genuine behavior mismatch here as `real-regression`. The three skips cover tip entry, discount application, and the Rappi payment method — check whether each is statically disabled or runtime-conditional.

    `24-waitlist.spec.ts` — failures at lines 21, 38, 75, 97, and 248. Five failures spanning add, notify, no-show, realtime, and a table-availability assertion is another shared-cause candidate; establish the group count before writing rows.

    `25-rappi-orders.spec.ts` — a failure at line 117 and skips at lines 36, 51, and 80 whose reasons read `'UI not implemented — EXPECTED FAIL: rappi orders not rendered'` and similar. CLAUDE.md lists `/rappi` as an implemented route with a RappiOrdersPage, so these are prime stale-skip candidates. Apply 39-05's A2 verdict, un-skip, and run the spec rather than deciding from the reason text.

    `26-field-validation.spec.ts` — failures at lines 71, 205, and 231, skips at lines 121, 154, and 165. These assert input-boundary enforcement (tab name capped at 100 chars, 5-digit PIN rejected, caja entry amount 0 rejected, order notes capped at 500, negative opening cash rejected, empty product name rejected). A failure here means an input-validation boundary is not being enforced, which is a correctness finding with a security dimension — classify conservatively and file a todo where the app genuinely accepts what it should reject.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/(23|24|25|26)-' .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md)" -ge 34</automated>
  </verify>
  <acceptance_criteria>
    - `39-06-LEDGER.md` has at least 34 rows keyed by `e2e/<spec>:<line>`, covering every finding in this batch.
    - Every row carries a classification and a one-line evidence excerpt from `error.message` or from the skip reason string.
    - Every skip row states whether the skip is a static disable or runtime-conditional on seed state.
    - Every un-skipped test has a recorded post-un-skip spec-run outcome.
    - Every `26-field-validation.spec.ts` row states whether the input boundary is actually enforced by the app.
  </acceptance_criteria>
  <done>All 34 findings in this batch are classified against real evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply harness fixes, file todos, and record final spec state</name>
  <files>e2e/23-payment-edge-cases.spec.ts, e2e/24-pool-advanced.spec.ts, e2e/24-sprint5-pool-accuracy.spec.ts, e2e/24-waitlist.spec.ts, e2e/25-rappi-orders.spec.ts, e2e/26-field-validation.spec.ts, .planning/todos/, .planning/phases/39-ai-slob-technical-debt-remediation/39-06-LEDGER.md</files>
  <read_first>
    - `39-06-LEDGER.md` as written by Tasks 1 and 2 — the classification is the only authority for what this task may touch.
    - `39-UI-SPEC.md` "Guardrails for Planner and Executor" items 1, 2, 3, and 5.
    - `39-CONTEXT.md` D-03.
    - An existing `.planning/todos/*.md` file for the naming and frontmatter convention.
  </read_first>
  <action>
    Act strictly on the ledger. Fix `harness` rows inside the spec file itself, with no edits under `src/`. Leave `infra` rows untouched and record their routing. Write one todo per distinct root cause for `real-regression` rows, referencing spec file, line, observed error, and expected behavior. Justify `obsolete` rows in writing before updating or removing the test.

    This batch carries the phase's highest concentration of money-affecting assertions — pool charge computation, payment eligibility, and input-boundary enforcement. For any of those, prefer filing a todo over reclassifying: a mistakenly-filed todo costs a review, while a mistakenly-dismissed billing regression ships.

    Then run each touched spec and record final stats. The batch need not be green; every remaining red test must map to a ledger row explaining why.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; git diff --name-only -- src/ | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Every `harness` row has a corresponding spec-file edit and a recorded post-fix result.
    - Every `real-regression` row has a matching `.planning/todos/*.md` file, and `git diff --name-only -- src/` is empty for this plan.
    - Every billing-math and input-validation finding has an explicit written verdict on whether the app behavior is correct.
    - The ledger records a final spec-run result for each touched spec, and every still-failing test maps to a row.
  </acceptance_criteria>
  <done>All 34 findings are dispositioned: fixed, routed, filed, or justified, with no unexplained red test in this batch.</done>
  <reversibility rating="reversible">All edits are test-side or planning-doc-side; no production behavior changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted user input -> persisted record | `26-field-validation.spec.ts` is the automated coverage asserting length caps, numeric floors, and PIN-format rules are enforced. |
| pool session state -> billed charge | `24-sprint5-pool-accuracy.spec.ts` and `24-pool-advanced.spec.ts` are the coverage asserting elapsed time converts to the correct charge. |
| test harness -> release gate | E2E is not CI-enforced; weakening a test silently reduces release coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-16 | Tampering | input-boundary enforcement (`26-field-validation.spec.ts`) | high | mitigate | Each of the six findings requires a written verdict on whether the app actually enforces the boundary; where it does not, a `real-regression` todo is filed rather than the assertion being relaxed. Relaxing a validation assertion to reach green is prohibited. |
| T-39-17 | Tampering | pool billing correctness (`24-*` specs) | high | mitigate | Task 1 separates fixture failure from billing failure by root cause before classification; any surviving wrong-charge behavior is classified `real-regression` and filed, never reclassified as `harness`. |
| T-39-18 | Tampering | payment eligibility rules (`23-payment-edge-cases.spec.ts:263,304`) | high | mitigate | Treated as payment-correctness assertions; a genuine behavior mismatch files a todo rather than being absorbed as a test-side fix. |
| T-39-06 | Tampering | E2E gate strength across specs 23-26 | high | mitigate | Deleting a failing test to reach green is prohibited; `obsolete` requires written justification; every un-skip requires an actual spec run. |
| T-39-09 | Tampering | unauthorized UI change during a test fix | medium | mitigate | 39-UI-SPEC.md guardrails cited in `read_first`; `git diff --name-only -- src/` must be empty, enforced in the Task 3 verify gate. |
</threat_model>

<verification>
- `39-06-LEDGER.md` has at least 34 rows, each with a classification and evidence.
- Both pool-billing specs have an explicit root-cause group count recorded.
- No file under `src/` was modified by this plan.
- `npm run typecheck && npm run lint && npm run test` pass.
</verification>

<success_criteria>
The 34 findings in specs 23 through 26 are fully dispositioned, the seven-failure pool-accuracy cluster is reduced to its real root-cause count, and every money-affecting or validation finding carries a written correctness verdict.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-06-SUMMARY.md` when done.
</output>
