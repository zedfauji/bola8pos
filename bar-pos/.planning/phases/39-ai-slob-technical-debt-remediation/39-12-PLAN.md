---
phase: 39-ai-slob-technical-debt-remediation
plan: 12
type: execute
wave: 5
depends_on: ["39-01", "39-02", "39-03", "39-04", "39-05", "39-06", "39-07", "39-08", "39-09", "39-10", "39-11"]
files_modified:
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-VALIDATION.md
  - CLAUDE.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "One consolidated ledger accounts for every finding this phase was chartered to remediate — all 147 E2E findings and every knip High-tier finding — with each one marked deleted, fixed, routed, filed, or retained-as-false-positive."
    - "The final knip counts are measured with the same method as the phase baseline, so the phase's delta is a real number rather than an estimate."
    - "The full Playwright suite has been run on a machine with a display session and its result recorded, since E2E is not CI-gated in this project."
    - "The out-of-scope categories (D-02, D-08, D-09) are confirmed untouched: no `package.json` dependency was removed, no `src/shared/ui/**` or `*.stories.tsx` finding was acted on, no jscpd clone was extracted, no `as any` cast was annotated, and the madge cycle still exists."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md — the consolidated phase ledger merging all eleven per-plan ledgers."
    - "Updated `.planning/phases/39-ai-slob-technical-debt-remediation/39-VALIDATION.md` with the per-task verification table filled in and `nyquist_compliant` resolved."
    - "Updated CLAUDE.md 'E2E Test Suite' section reflecting the real spec count."
  key_links:
    - "The eleven per-plan `39-NN-LEDGER.md` files -> the consolidated `39-LEDGER.md`."
    - "39-01-LEDGER.md's baseline counts -> the final counts, forming the phase's measured delta."
  prohibitions:
    - "Do NOT close out a finding as resolved without a per-plan ledger row backing it. An unaccounted finding is an open item, not a silent success."
    - "Do NOT remove any unused dependency or devDependency while reconciling `package.json` — D-09 defers that to its own phase."
    - "Do NOT act on any Medium-tier `src/shared/ui/**` or `*.stories.tsx` finding (D-08)."
    - "Do NOT fix the madge circular import, extract a jscpd clone, or annotate an `as any` cast — all deferred to their own phases (D-02)."
---

<objective>
Close the phase: consolidate every disposition into one ledger, measure the real delta, and run the gate the project cannot get from CI.

Eleven plans each produced their own ledger so they could run in parallel without colliding on one file. This plan merges them, checks that nothing fell between two plans' scopes, and produces the single artifact a reviewer can read to answer "what did this phase actually do to 2135 findings."

It also runs the one verification this project cannot automate: CLAUDE.md states E2E is not CI-gated and `playwright.config.ts` requires a real display session with `google-chrome-stable`, so the full-suite result has to be produced deliberately by a human-attended run rather than assumed green.

Purpose: make the phase's outcome auditable and confirm nothing out of scope was touched.
Output: a consolidated ledger, a measured delta, a recorded full-suite E2E result, and updated validation and project docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-VALIDATION.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-CHECKLIST.md
</context>

<artifacts_this_phase_produces>
No source changes. This plan produces the consolidated `39-LEDGER.md`, an updated `39-VALIDATION.md`, and a corrected CLAUDE.md "E2E Test Suite" section.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the eleven per-plan ledgers and find the gaps</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md</files>
  <read_first>
    - All eleven per-plan ledgers: `39-01-LEDGER.md` through `39-11-LEDGER.md`.
    - `10-CHECKLIST.md` Blocking > e2e — the authoritative list of all 147 E2E findings, used to check for any that no plan claimed.
    - `39-CONTEXT.md` D-01 through D-09 — the phase's charter, against which coverage is judged.
  </read_first>
  <action>
    Merge the eleven ledgers into `39-LEDGER.md`, organized by track and then by disposition, with a summary table at the top counting findings by outcome: deleted, test-fixed, routed to Phase 38, filed as todo, retained as false positive, and deferred.

    Then look for gaps rather than assuming completeness. Reconcile the E2E side against 10-CHECKLIST.md's 147-entry list and confirm each `spec:line` appears in exactly one plan's ledger — the Track A plans partitioned the suite by spec file, so a spec that fell between two partitions would be silently unclaimed. Reconcile the knip side against 39-01's baseline and 39-08's post-decision baseline, confirming that the sum of per-plan deltas plus retained false positives accounts for the total.

    Any finding with no ledger row is an open item. List those explicitly in a "Not remediated" section with the reason, rather than letting the summary table imply full coverage.

    Also record, per decision ID D-01 through D-09, where in the phase that decision was honored — this is the traceability a reviewer needs given the phase has no formal requirement IDs.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md &amp;&amp; for n in 01 02 03 04 05 06 07 08 09 10 11; do test -f ".planning/phases/39-ai-slob-technical-debt-remediation/39-$n-LEDGER.md" || { echo "MISSING 39-$n-LEDGER.md"; exit 1; }; done &amp;&amp; echo "all 11 per-plan ledgers present"</automated>
  </verify>
  <acceptance_criteria>
    - `39-LEDGER.md` exists with a summary table counting findings by disposition.
    - Every one of 10-CHECKLIST.md's 147 E2E entries maps to exactly one per-plan ledger row, or appears in the "Not remediated" section with a reason.
    - The knip reconciliation shows per-plan deltas plus retained false positives accounting for the baseline total.
    - Each of D-01 through D-09 has a recorded location where it was honored.
  </acceptance_criteria>
  <done>One ledger accounts for the phase, and anything unaccounted for is named rather than implied resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the final delta and confirm the out-of-scope categories are untouched</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md</files>
  <read_first>
    - `39-01-LEDGER.md` — the phase baseline and its counting method.
    - `39-RESEARCH.md` "Code Examples > Diffing distinct dead-code count after a deletion wave".
    - `10-CHECKLIST.md` "Per-tier count summary" — the original tier totals this phase's delta is reported against.
    - `39-CONTEXT.md` D-02, D-08, and D-09 — the deferred categories.
  </read_first>
  <action>
    Run the full audit pipeline once (`npm run audit:tech-debt`), noting 10-REVIEW.md's WR-04 caveat that `.audit-tmp/` is not cleared between runs — confirm the report mtimes advanced before trusting the output.

    Recompute the distinct High-tier knip count with the same set-union method used for the baseline, and report the phase delta as a before-and-after pair rather than as a percentage. Report the Blocking-tier count separately, since the 34 `unlisted` findings should now be zero.

    Then verify the deferred categories were genuinely left alone, because an accidental in-scope creep is easier to catch now than in review. Confirm `git diff` across the phase shows no removal from `package.json` `dependencies` or `devDependencies` other than the single `@testing-library/user-event` addition (D-09). Confirm no file under `src/shared/ui/**` and no `*.stories.tsx` was modified (D-08). Confirm the madge cycle between `src/entities/inventory/model/queries.ts` and `store.ts` still reports, that the jscpd clone count is materially unchanged, and that the `as any` cast count is materially unchanged (D-02).

    Record all of this in `39-LEDGER.md` as a scope-compliance section.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; git diff --name-only "$(git log --format=%H -n1 --grep='docs(39)' | tail -1)"..HEAD -- 'src/shared/ui' '*.stories.tsx' | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `39-LEDGER.md` records the before-and-after distinct knip High-tier count and the before-and-after Blocking-tier count, both using the baseline's method.
    - The `unlisted` count is zero.
    - A scope-compliance section confirms, with evidence, that D-02, D-08, and D-09 categories were not acted on.
    - `npm run typecheck`, `npm run lint`, and `npm run test` pass.
  </acceptance_criteria>
  <done>The phase's effect is a measured number, and no deferred category was touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: full Playwright suite run on a display session</name>
  <what-built>
    Eleven plans of dead-code deletion and E2E triage are complete. Unit tests, typecheck, and lint are green. The one gate that cannot run automatically is the full Playwright suite: CLAUDE.md records that E2E is not CI-gated and is manual pre-release only, and `playwright.config.ts` sets `headless: false` with `channel: 'chrome'`, so it needs a desktop session and `google-chrome-stable` rather than a headless runner.
  </what-built>
  <how-to-verify>
    On an Ubuntu machine with a real desktop session and `google-chrome-stable` installed, from `bar-pos/`:

    1. Run `npm run test:e2e` and let the full suite complete (59 spec files).
    2. Compare the resulting pass, fail, and skip counts against the phase baseline recorded in 10-CHECKLIST.md: 226 passed, 94 failed, 53 skipped, 1 flaky.
    3. For every test still failing, confirm it maps to a row in `39-LEDGER.md` — routed to Phase 38, filed as a todo, or recorded as a valid skip. A failure with no ledger row is a regression introduced by this phase and must be investigated before sign-off.
    4. Confirm no test that passed at baseline now fails. That is the specific signal that a dead-code deletion removed something live.
    5. Open `npm run test:e2e:report` if any result needs inspecting.

    Report the four counts and whether any previously-passing test regressed.
  </how-to-verify>
  <resume-signal>Type "approved" with the pass/fail/skip counts, or describe any previously-passing test that now fails.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the E2E result, close out validation, and correct the stale spec count in CLAUDE.md</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-LEDGER.md, .planning/phases/39-ai-slob-technical-debt-remediation/39-VALIDATION.md, CLAUDE.md</files>
  <read_first>
    - `39-VALIDATION.md` — the per-task verification table and the sign-off checklist, both currently unfilled.
    - `CLAUDE.md` "E2E Test Suite (`bar-pos/e2e/`)" — the section stating 26 spec files, which 39-RESEARCH.md verified is stale against the actual 59.
    - The full-suite counts reported at the preceding checkpoint.
  </read_first>
  <action>
    Write the checkpoint's reported counts into `39-LEDGER.md` alongside the baseline counts, with an explicit statement of whether any previously-passing test regressed.

    Fill in `39-VALIDATION.md`'s per-task verification table with the real plan and task IDs from this phase, mark each row's status from the recorded results, and resolve the sign-off checklist. Set `nyquist_compliant` honestly: `true` only if every task in the phase carried an automated verify and the full suite ran; otherwise leave it `false` and state which gate was not met, since a false green here would mislead the next phase's planning.

    Then correct CLAUDE.md's "E2E Test Suite" section, which lists 26 spec files against an actual 59 — a documentation gap 39-RESEARCH.md flagged and which this phase's Track A work makes concretely misleading, since four plans just partitioned the real 59-file suite. Update the count and the spec list. Keep the edit scoped to that section; broader CLAUDE.md maintenance is not this phase's job.
  </action>
  <verify>
    <automated>test "$(ls e2e/*.spec.ts | wc -l)" -eq "$(grep -oE '^[0-9]+ spec files' CLAUDE.md | grep -oE '^[0-9]+' | head -1)"</automated>
  </verify>
  <acceptance_criteria>
    - `39-LEDGER.md` records the full-suite pass/fail/skip/flaky counts next to the baseline, with an explicit no-regression statement.
    - `39-VALIDATION.md`'s per-task table uses this phase's real plan and task IDs and each row carries a status.
    - `39-VALIDATION.md`'s `nyquist_compliant` value matches the evidence, with any unmet gate named.
    - CLAUDE.md's "E2E Test Suite" spec-file count matches `ls e2e/*.spec.ts | wc -l`.
  </acceptance_criteria>
  <done>The phase's verification record is complete and honest, and the project's own E2E documentation matches reality.</done>
  <reversibility rating="reversible">Documentation-only changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase output -> future planning | The consolidated ledger is what later phases will treat as the record of what was and was not remediated; a gap presented as coverage misleads every downstream decision. |
| E2E suite -> release confidence | E2E is not CI-gated, so the full-suite result recorded here is the only evidence that eleven plans of deletion did not break a live path. |
| in-scope work -> deferred categories | Four categories were deliberately deferred; silent creep into them would ship unreviewed changes under this phase's approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-35 | Denial of Service | live code removed by a false-positive deletion | high | mitigate | The blocking human-verify checkpoint requires the full 59-spec suite to run on a real display session and requires explicit confirmation that no previously-passing test now fails — the specific signal that a deletion removed something live. Unit tests and typecheck alone cannot catch a runtime-only breakage. |
| T-39-03 | Repudiation | phase audit trail | high | mitigate | Task 1 reconciles all 147 E2E findings against 10-CHECKLIST.md and reconciles the knip deltas against the baseline; anything unaccounted for is listed in a "Not remediated" section rather than absorbed into a summary count. |
| T-39-36 | Tampering | deferred categories (D-02, D-08, D-09) | medium | mitigate | Task 2 verifies with `git diff` evidence that no dependency was removed, no `src/shared/ui/**` or `*.stories.tsx` file was modified, and that the madge, jscpd, and `as any` counts are materially unchanged. |
| T-39-37 | Repudiation | validation sign-off | medium | mitigate | `nyquist_compliant` must match the evidence, with any unmet gate named; a false green would be inherited as truth by the next phase's planning. |
</threat_model>

<verification>
- `39-LEDGER.md` exists, consolidates all eleven per-plan ledgers, and lists any unaccounted finding explicitly.
- Final knip and Blocking-tier counts are recorded against the baseline using the same method.
- The full `npm run test:e2e` result is recorded with a no-regression statement.
- `39-VALIDATION.md` is filled in with real task IDs and an honest `nyquist_compliant` value.
- CLAUDE.md's E2E spec count matches reality.
</verification>

<success_criteria>
A reviewer can read one ledger and see exactly what happened to every finding this phase was chartered to remediate, the delta is a measured number, the full E2E suite has been run with no previously-passing test regressed, and the four deferred categories are provably untouched.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-12-SUMMARY.md` when done.
</output>
