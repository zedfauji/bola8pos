---
phase: 39-ai-slob-technical-debt-remediation
plan: 07
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - e2e/27-inventory-intelligence.spec.ts
  - e2e/30-help-manual.spec.ts
  - e2e/31-categories.spec.ts
  - e2e/36-recipes.spec.ts
  - e2e/37-analytics-reports.spec.ts
  - e2e/38-audit-logs.spec.ts
  - e2e/43-promotions.spec.ts
  - e2e/44-focus-tab-order.spec.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md
  - .planning/todos/ (append-only — plan 39-07 creates new todo files, never edits an existing one)
autonomous: true
requirements: [D-03, D-04, D-06]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 22 E2E findings across specs 27 through 44 carry a written classification backed by real Playwright error output or by the literal skip-reason string (D-04), satisfying D-06's requirement for real per-test investigation of the unannotated findings."
    - "The six `31-categories.spec.ts` failures are grouped by shared root cause before being written as rows, since a create-hierarchy spec fails top-down when its first create step fails (39-RESEARCH.md Pitfall 3)."
    - "Every real product bug found is filed as a todo rather than fixed inline (D-03)."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md — one row per finding, in 39-02-LEDGER.md's format."
    - "One `.planning/todos/*.md` per distinct real product bug (zero is a valid outcome)."
  key_links:
    - "`.audit-tmp/e2e-per-spec/<spec>.json` -> `suites[].suites[].specs[].tests[].results[].error.message`."
    - "`44-focus-tab-order.spec.ts` -> Phase 32's committed focus/touch-target contract, which is what those two failures are asserting against."
  prohibitions:
    - "Do NOT delete a failing test to reach green."
    - "Do NOT fix a real product bug inline; file a todo (D-03)."
    - "Do NOT un-skip a test and record it as fixed without running the spec."
    - "Do NOT touch any spec file outside the 8 listed in `files_modified` — the other Track A plans own them concurrently."
    - "Do NOT introduce new UI, copy, or components while fixing a test (39-UI-SPEC.md guardrails 1-3, 5). This applies with particular force to `44-focus-tab-order.spec.ts`, whose assertions describe an already-shipped accessibility contract that must be restored rather than redesigned."
---

<objective>
Triage the 22 E2E findings in spec files 27 through 44 to a written disposition.

This is the tail batch: inventory intelligence, category hierarchy, recipes, analytics, audit logs, promotions, and the focus/tab-order accessibility regression suite. Two things in it need care. `31-categories.spec.ts` fails six times in a chain that builds root, child, and grandchild categories in sequence, so the later failures are almost certainly downstream of the first. And `44-focus-tab-order.spec.ts` asserts an accessibility contract that Phase 32 shipped deliberately — its two failures describe either a regression in that contract or a test written against a since-changed keypad layout, and those two outcomes have opposite remedies.

Purpose: convert 22 raw findings into classified, actionable rows.
Output: a filled ledger, todos for real bugs, and test-side fixes where the test itself was wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-UI-SPEC.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No new source symbols. This plan produces a classification ledger, zero or more todo files, and test-side edits confined to the 8 spec files listed in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reduce the 31-categories chain and settle the 44-focus-tab-order accessibility findings</name>
  <files>e2e/31-categories.spec.ts, e2e/44-focus-tab-order.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/31-categories.json` and `.audit-tmp/e2e-per-spec/44-focus-tab-order.json` (regenerate a stale file with the `PLAYWRIGHT_JSON_OUTPUT_FILE` single-spec command from 39-RESEARCH.md's Code Examples).
    - `e2e/31-categories.spec.ts` — how tests at lines 169, 193, 222, and 262 depend on each other's created entities.
    - `e2e/44-focus-tab-order.spec.ts` — the assertions at lines 81 and 144.
    - `CLAUDE.md` "Implemented Features", Phase 32 entry — the committed `focusEmphasis` / `touchSize` contract and the explicit statement that `e2e/44-focus-tab-order.spec.ts` regression-tests Tab order on ManagerPinDialog and the inventory filter/sort-header row.
    - `39-UI-SPEC.md` guardrails 3 and 5 — restore shipped behavior, never author new UI.
  </read_first>
  <action>
    `31-categories.spec.ts` first: failures at lines 169, 193, 222, 262, 318, and 385, plus a skip at line 363. Lines 169, 193, and 222 create a root category, then a child under it, then a grandchild under that — a strictly sequential chain where a failure at the root step guarantees failures downstream. Establish from the error output whether one create step is the single cause, and record the group count before writing six rows. Lines 318 (a DB column writable via service role) and 385 (bartender cannot access Settings) are independent of the chain; line 385 is an access-control assertion and gets the conservative treatment.

    `44-focus-tab-order.spec.ts` next: failures at lines 81 and 144. Phase 32 shipped a specific keypad Tab order (1-9, 0, Backspace, Cancel) on ManagerPinDialog and a specific inventory filter-to-sort-header order. Determine from the error output which of two cases applies. If the DOM order genuinely no longer matches the shipped contract, this is an accessibility regression and gets a `real-regression` todo — do not "fix" it by relaxing the test to match the drifted DOM. If instead the test's selectors no longer resolve against a legitimately-refactored but still-conformant DOM, that is a `harness` finding and the test is updated to assert the same contract through working selectors.

    The distinction matters because both failures look identical from the title, and the wrong call either hides a shipped-contract regression or churns a correct test.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/(31-categories|44-focus-tab-order)\.spec\.ts:' .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md)" -ge 9</automated>
  </verify>
  <acceptance_criteria>
    - `39-07-LEDGER.md` has rows for all 7 `31-categories.spec.ts` findings and both `44-focus-tab-order.spec.ts` findings.
    - The ledger states an explicit root-cause group count for `31-categories.spec.ts` and identifies which failures are downstream of which.
    - Each `44-focus-tab-order.spec.ts` row states explicitly whether the shipped Tab-order contract still holds in the DOM, with the evidence.
    - No assertion in `44-focus-tab-order.spec.ts` was relaxed to match a drifted DOM.
  </acceptance_criteria>
  <done>The category-creation chain is reduced to its real cause, and both accessibility findings carry a contract-holds-or-not verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the remaining 13 findings in specs 27, 30, 36, 37, 38, and 43</name>
  <files>e2e/27-inventory-intelligence.spec.ts, e2e/30-help-manual.spec.ts, e2e/36-recipes.spec.ts, e2e/37-analytics-reports.spec.ts, e2e/38-audit-logs.spec.ts, e2e/43-promotions.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/` JSON for `27-inventory-intelligence`, `30-help-manual`, `36-recipes`, `37-analytics-reports`, `38-audit-logs`, and `43-promotions`.
    - `10-CHECKLIST.md` Blocking > e2e for the exact titles at each line.
    - `CLAUDE.md` "Implemented Features" and "Routes" — including `/audit` and its `AuditRoute` gate.
    - `39-02-LEDGER.md` — classification scheme and row format.
  </read_first>
  <action>
    `27-inventory-intelligence.spec.ts` — failures at lines 32 (low-stock badge visible to manager) and 151 (physical count writes `stock_movements`), plus a skip at line 236. Line 151 asserts a stock ledger write; a genuine failure there is an inventory-integrity regression, not a display problem.

    `30-help-manual.spec.ts:15` — route-specific help opening on POS.

    `36-recipes.spec.ts` — failures at lines 21, 43, and 82, plus a skip at line 144. Line 82 asserts that `INVENTORY_NEGATIVE` shows a toast and allows a manager-PIN override; that combines an error path with a privilege gate, so treat a failure there conservatively.

    `37-analytics-reports.spec.ts:82` — a tab rendering without crash. A render crash is a real defect if reproduced; confirm from the error whether the component threw or the navigation never arrived.

    `38-audit-logs.spec.ts` — a failure at line 170 (bartender redirected away from `/audit`) and a skip at line 184. Line 170 is an access-control assertion on the audit log, which is the surface most sensitive to an unnoticed gate removal.

    `43-promotions.spec.ts` — failures at lines 39 and 96, covering promotion CRUD via Settings and auto-application of an active item-target promotion at order time. Line 96 is a pricing-affecting behavior.

    For each, read real error output before classifying, group within-file failures by signature, and check every skip reason for whether it is a static disable or runtime-conditional on seed state.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/(27|30|31|36|37|38|43|44)-' .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md)" -ge 22</automated>
  </verify>
  <acceptance_criteria>
    - `39-07-LEDGER.md` has at least 22 rows keyed by `e2e/<spec>:<line>`, covering every finding in this batch.
    - Every row carries a classification and a one-line evidence excerpt from `error.message` or from the skip reason string.
    - Every skip row states whether the skip is a static disable or runtime-conditional on seed state.
    - Every un-skipped test has a recorded post-un-skip spec-run outcome.
  </acceptance_criteria>
  <done>All 22 findings in this batch are classified against real evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply harness fixes, file todos, and record final spec state</name>
  <files>e2e/27-inventory-intelligence.spec.ts, e2e/30-help-manual.spec.ts, e2e/31-categories.spec.ts, e2e/36-recipes.spec.ts, e2e/37-analytics-reports.spec.ts, e2e/38-audit-logs.spec.ts, e2e/43-promotions.spec.ts, e2e/44-focus-tab-order.spec.ts, .planning/todos/, .planning/phases/39-ai-slob-technical-debt-remediation/39-07-LEDGER.md</files>
  <read_first>
    - `39-07-LEDGER.md` as written by Tasks 1 and 2 — the classification is the only authority for what this task may touch.
    - `39-UI-SPEC.md` "Guardrails for Planner and Executor" items 1, 2, 3, and 5.
    - `39-CONTEXT.md` D-03.
    - An existing `.planning/todos/*.md` file for the naming and frontmatter convention.
  </read_first>
  <action>
    Act strictly on the ledger. Fix `harness` rows inside the spec file itself, with no edits under `src/`. Leave `infra` rows untouched and record their routing. Write one todo per distinct root cause for `real-regression` rows, referencing spec file, line, observed error, and expected behavior. Justify `obsolete` rows in writing before updating or removing the test.

    The two access-control findings in this batch (`31-categories.spec.ts:385` bartender-to-Settings, `38-audit-logs.spec.ts:170` bartender-to-`/audit`) and the accessibility findings in `44-focus-tab-order.spec.ts` all get the conservative treatment: if the error output leaves doubt about whether the gate or the contract still holds, file a `real-regression` todo rather than classifying it away.

    Then run each touched spec and record final stats. The batch need not be green; every remaining red test must map to a ledger row explaining why.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; git diff --name-only -- src/ | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Every `harness` row has a corresponding spec-file edit and a recorded post-fix result.
    - Every `real-regression` row has a matching `.planning/todos/*.md` file, and `git diff --name-only -- src/` is empty for this plan.
    - Both access-control findings and both accessibility findings have an explicit written verdict on whether the gate or contract still holds.
    - The ledger records a final spec-run result for each touched spec, and every still-failing test maps to a row.
  </acceptance_criteria>
  <done>All 22 findings are dispositioned: fixed, routed, filed, or justified, with no unexplained red test in this batch.</done>
  <reversibility rating="reversible">All edits are test-side or planning-doc-side; no production behavior changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bartender session -> audit log and Settings | `38-audit-logs.spec.ts:170` and `31-categories.spec.ts:385` are the automated coverage asserting these gates hold. |
| physical count input -> stock ledger | `27-inventory-intelligence.spec.ts:151` asserts a counted adjustment actually writes `stock_movements`. |
| keyboard-only operator -> POS controls | `44-focus-tab-order.spec.ts` is the regression coverage for Phase 32's shipped focus and Tab-order contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-19 | Elevation of Privilege | audit-log and Settings gating (`38-audit-logs.spec.ts:170`, `31-categories.spec.ts:385`) | high | mitigate | Treated as candidate access-control regressions by default; Task 3 requires an explicit written verdict on whether the gate holds, and any doubt files a `real-regression` todo. Audit-log access is the surface where a silent gate removal is least likely to be noticed by other means. |
| T-39-20 | Repudiation | audit-log coverage itself | medium | mitigate | `38-audit-logs.spec.ts` is the only automated coverage for the audit trail; its skip at line 184 must be classified with a stated reason rather than left unexamined, so a coverage gap in the repudiation control is recorded rather than assumed away. |
| T-39-21 | Tampering | stock ledger integrity (`27-inventory-intelligence.spec.ts:151`) | high | mitigate | A failure to write `stock_movements` on a physical count is classified `real-regression` and filed, not absorbed as a test-side fix. |
| T-39-22 | Denial of Service | keyboard-only operability (`44-focus-tab-order.spec.ts`) | medium | mitigate | Relaxing the assertion to match a drifted DOM is prohibited; Task 1 requires deciding between contract-regression and selector-drift on evidence, and 39-UI-SPEC.md guardrail 3 forbids authoring new behavior in either case. |
| T-39-06 | Tampering | E2E gate strength across specs 27-44 | high | mitigate | Deleting a failing test to reach green is prohibited; `obsolete` requires written justification; every un-skip requires an actual spec run. |
| T-39-09 | Tampering | unauthorized UI change during a test fix | medium | mitigate | 39-UI-SPEC.md guardrails cited in `read_first`; `git diff --name-only -- src/` must be empty, enforced in the Task 3 verify gate. |
</threat_model>

<verification>
- `39-07-LEDGER.md` has at least 22 rows, each with a classification and evidence.
- `31-categories.spec.ts` has an explicit root-cause group count recorded.
- Both `44-focus-tab-order.spec.ts` rows state whether the shipped Tab-order contract still holds.
- No file under `src/` was modified by this plan.
- `npm run typecheck && npm run lint && npm run test` pass.
</verification>

<success_criteria>
The 22 findings in specs 27 through 44 are fully dispositioned, the category-creation chain is reduced to its real cause, and every access-control and accessibility finding carries a written contract verdict. With this plan, all 147 E2E findings in the phase have a disposition.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-07-SUMMARY.md` when done.
</output>
