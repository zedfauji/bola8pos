---
phase: 39-ai-slob-technical-debt-remediation
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - scripts/seed-combos.ts
  - scripts/seed-ingredients.ts
  - scripts/seed-prep.ts
  - scripts/seed-recipes.ts
  - scripts/seed-reports.ts
  - scripts/test-payment-auth.mjs
  - scripts/write-env-local-from-cloud-secret.mjs
  - src/app/kitchen-prep-route.tsx
  - src/shared/lib/agent/index-status.ts
  - src/shared/lib/supabase-realtime.ts
  - src/widgets/OrderPanel/CartSummary.tsx
  - src/widgets/OrderPanel/OrderItemCard.tsx
  - src/widgets/RappiOrderBadge/RappiOrderBadge.tsx
  - src/widgets/SettingsCatalogPanel.tsx
  - src/widgets/SettingsPagePanel.tsx
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md
autonomous: true
requirements: [D-01, D-07]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 14 `supabase/functions/**` unused-file findings are recorded as confirmed false positives with per-function invocation evidence, and every one of those files still exists on disk."
    - "All 19 production-mode-only unused-file findings are adjudicated against their actual wiring (vitest config, playwright config, eslint config, npm scripts) before any of them is deleted, and every one that is wired is recorded as a false positive."
    - "Each of the 15 both-modes non-barrel candidates is either deleted with a recorded repository-wide sanity-check hit count, or recorded as a false positive with the reference that saved it."
    - "`npm run typecheck`, `npm run lint`, and `npm run test` pass after the sweep, and `npm run test:e2e` still collects all 59 spec files."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md — one disposition row per unused-file finding covered by this plan (48 rows)."
  key_links:
    - "`vitest.config.ts` `globalSetup` / `setupFiles` -> `src/test/global-setup.ts`, `src/shared/lib/test-setup.ts` (proof these are live despite the production-mode finding)."
    - "`eslint.config.js` imports -> `eslint-rules/no-ui-drift.js`, `eslint-rules/no-raw-money-format.js`."
    - "`e2e/*.spec.ts` imports -> `e2e/helpers/auth.ts`, `e2e/helpers/supabase.ts`, `e2e/helpers/requireEnv.ts`, `e2e/fixtures.ts`."
    - "`src/shared/lib/edge-function-contracts.ts` name-strings and `supabase.functions.invoke(...)` call sites -> `supabase/functions/*/index.ts`."
  prohibitions:
    - "Do NOT delete any file under `supabase/functions/**` under any circumstance. Deleting one breaks the deployed payment, webhook, or email pipeline with zero compile-time signal (39-RESEARCH.md Pitfall 1)."
    - "Do NOT delete any file whose only knip signal is production-mode. Production mode answers 'what ships in the bundle', so it flags dev and test infrastructure by construction — that is not a dead-code signal."
    - "Do NOT delete any `index.ts` barrel file in this plan. Whole-dead barrels are an FSD public-API question owned by plan 39-08."
    - "Do NOT touch `src/shared/ui/**` or `*.stories.tsx` (D-08), and do NOT modify `package.json` dependencies (D-09)."
    - "Do NOT modify `knip.json`. Suppressing a finding by config is a separate decision; this plan's output for a false positive is a ledger row, not a config change (39-RESEARCH.md Open Question 2, defaulting to skip-and-annotate)."
---

<objective>
Sweep all 61 knip "unused file" findings to a written disposition, deleting only the ones that survive a repository-wide sanity check.

The sweep is dominated by false positives, and the research verified two whole categories of them: the 14 `supabase/functions/**` Deno entry points (invoked by URL, never imported), and the 19 findings that only production mode reports — which are test and tooling infrastructure by construction. `e2e/helpers/auth.ts` alone is imported by 56 spec files. Deleting the production-only set would take out the vitest global setup, the eslint custom rules, and the entire E2E auth helper.

That leaves 15 genuine deletion candidates flagged by both knip modes. This plan deletes those and writes down why every other finding was spared.

Purpose: remove the mechanically-safe dead files while converting the two large false-positive categories into documented decisions instead of silent skips.
Output: up to 15 files deleted, 46 findings recorded as false positives with evidence, and a green typecheck/lint/test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-01-SUMMARY.md
@knip.json
@vitest.config.ts
@playwright.config.ts
@eslint.config.js
</context>

<artifacts_this_phase_produces>
Purely subtractive. This plan produces **no new source symbols**.

Deletions (candidates — each subject to the sanity check, so the final set may be smaller):
`scripts/seed-combos.ts`, `scripts/seed-ingredients.ts`, `scripts/seed-prep.ts`, `scripts/seed-recipes.ts`, `scripts/seed-reports.ts`, `scripts/test-payment-auth.mjs`, `scripts/write-env-local-from-cloud-secret.mjs`, `src/app/kitchen-prep-route.tsx`, `src/shared/lib/agent/index-status.ts`, `src/shared/lib/supabase-realtime.ts`, `src/widgets/OrderPanel/CartSummary.tsx`, `src/widgets/OrderPanel/OrderItemCard.tsx`, `src/widgets/RappiOrderBadge/RappiOrderBadge.tsx`, `src/widgets/SettingsCatalogPanel.tsx`, `src/widgets/SettingsPagePanel.tsx`.

Planning artifact: `39-03-LEDGER.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the 14 supabase/functions findings as confirmed false positives — delete none</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md</files>
  <read_first>
    - `39-PATTERNS.md` section "Pattern: Supabase Edge Function Invocation by URL — knip false-negative risk", including the `Deno.serve` shape and the two cross-check commands it prescribes.
    - `39-RESEARCH.md` Pitfall 1 and the "Don't Hand-Roll" row on edge functions.
    - `10-CHECKLIST.md` High > knip unused-files list and its inline NOTE flagging these as likely false positives for human triage rather than deletion.
    - `src/shared/lib/edge-function-contracts.ts` — the registry that names every invocable edge function as a string, which is the evidence knip's module graph cannot see.
  </read_first>
  <action>
    For each of the 14 findings (`supabase/functions/_shared/audit.ts` plus the 13 `supabase/functions/<name>/index.ts` entries for create-staff, get-server-time, process-payment, process-split-payment, rappi-sync-menu, rappi-webhook, send-receipt-email, send-waitlist-notification, settings-backup, settings-email-status, settings-restore, settings-test-email, and void-order), find the runtime invocation evidence rather than assuming the category.

    For each function directory name, search `src/` for a `functions.invoke` call carrying that name-string, and search `src/shared/lib/edge-function-contracts.ts` for the same name. For `_shared/audit.ts`, the evidence is a relative Deno import from a sibling function file, not a `src/` reference — `supabase/functions/create-staff/index.ts` imports `recordAudit` from it.

    Write one ledger row per function: function name, the evidence found (file and call shape), and the disposition `FALSE POSITIVE — NOT DELETED (Deno HTTP entry point, invoked by URL)`. If any function turns up zero invocation evidence anywhere, do not delete it either — record it as `NEEDS HUMAN TRIAGE` with the searches that came up empty, because an unreferenced-but-deployed function is a deployment question, not a dead-code question.

    Delete nothing in this task. Confirm at the end that all 14 files still exist.
  </action>
  <verify>
    <automated>test "$(ls supabase/functions/*/index.ts supabase/functions/_shared/audit.ts 2>/dev/null | wc -l)" -ge 14 &amp;&amp; test "$(grep -c 'FALSE POSITIVE\|NEEDS HUMAN TRIAGE' .planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md)" -ge 14</automated>
  </verify>
  <acceptance_criteria>
    - All 14 files under `supabase/functions/` that knip flagged still exist on disk.
    - `39-03-LEDGER.md` has one row per function with a named evidence source, or an explicit `NEEDS HUMAN TRIAGE` marker where evidence was absent.
    - `git status` shows no deletion under `supabase/functions/`.
  </acceptance_criteria>
  <done>The largest false-positive category is documented with per-function evidence and zero files removed.</done>
</task>

<task type="auto">
  <name>Task 2: Adjudicate the 19 production-mode-only findings against their real wiring</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md</files>
  <read_first>
    - `vitest.config.ts` — locate the `globalSetup` and `setupFiles` entries.
    - `eslint.config.js` — locate the imports of the custom rule modules.
    - `playwright.config.ts` — locate its setup/fixture wiring.
    - `package.json` `scripts` — locate which `scripts/*.ts` files are invoked by an npm script.
    - `39-RESEARCH.md` "knip Output Mechanics" — the default-vs-production overlap table, which is what makes a production-only finding structurally different from a both-modes finding.
  </read_first>
  <action>
    The 19 findings knip reports only in production mode are: `e2e/fixtures.ts`, `e2e/helpers/auth.ts`, `e2e/helpers/requireEnv.ts`, `e2e/helpers/supabase.ts`, `eslint-rules/no-raw-money-format.js`, `eslint-rules/no-ui-drift.js`, `scripts/generate-design-tokens.ts`, `scripts/indexCodebase.ts`, `src/entities/tab/ui/PoolChargeItem.tsx`, `src/entities/tab/ui/TabDetail.tsx`, `src/features/close-tab/index.ts`, `src/shared/lib/mocks.ts`, `src/shared/lib/promotion-pricing.ts`, `src/shared/lib/rappi-webhook-payload.ts`, `src/shared/lib/supabase-test-client.ts`, `src/shared/lib/test-setup.ts`, `src/shared/lib/test-utils.tsx`, `src/shared/lib/uom.ts`, and `src/test/global-setup.ts`.

    Production mode answers "what ends up in the shipped bundle", so anything reachable only from tests, stories, lint config, or npm scripts is unused *by that question* while being entirely live. Treat this whole set as presumptively false-positive and prove liveness per file rather than proving deadness.

    Establish the evidence per file: config wiring for `test-setup.ts`, `global-setup.ts`, and the two `eslint-rules/*.js` modules; npm-script invocation for `indexCodebase.ts` and `generate-design-tokens.ts`; importer count across `e2e/` for the four `e2e/` helpers; importer count across `*.test.*` and `*.stories.*` for `TabDetail.tsx`, `PoolChargeItem.tsx`, `mocks.ts`, `test-utils.tsx`, and `supabase-test-client.ts`.

    A file in this set may only be deleted if it has zero importers across the entire repository including tests, stories, configs, and npm scripts — in other words, only if it would also be flagged by a default-mode run. None of these are, by definition of the set, so the expected outcome of this task is 19 false-positive rows and zero deletions. If some file genuinely has zero importers anywhere, record the searches that proved it and defer the deletion to a follow-up rather than removing it in this task; a production-only signal is too weak to act on alone.

    Note in the ledger that `src/shared/lib/mocks.ts`, `test-utils.tsx`, `uom.ts`, and `rappi-webhook-payload.ts` also carry line-level export findings — those are handled by plan 39-11 and are unaffected by keeping the file.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; test "$(ls e2e/*.spec.ts | wc -l)" -eq 59</automated>
  </verify>
  <acceptance_criteria>
    - All four `e2e/` helper files, both `eslint-rules/*.js` files, `src/test/global-setup.ts`, and `src/shared/lib/test-setup.ts` still exist.
    - `39-03-LEDGER.md` has one row per production-only finding naming the specific wiring or importer evidence that keeps it.
    - `npm run typecheck`, `npm run lint`, and `npm run test` all pass.
    - `e2e/*.spec.ts` still resolves to 59 spec files.
  </acceptance_criteria>
  <done>Every production-only finding is adjudicated with named evidence, and the test and lint toolchains are provably intact.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the 15 both-modes non-barrel deletion candidates</name>
  <files>scripts/seed-combos.ts, scripts/seed-ingredients.ts, scripts/seed-prep.ts, scripts/seed-recipes.ts, scripts/seed-reports.ts, scripts/test-payment-auth.mjs, scripts/write-env-local-from-cloud-secret.mjs, src/app/kitchen-prep-route.tsx, src/shared/lib/agent/index-status.ts, src/shared/lib/supabase-realtime.ts, src/widgets/OrderPanel/CartSummary.tsx, src/widgets/OrderPanel/OrderItemCard.tsx, src/widgets/RappiOrderBadge/RappiOrderBadge.tsx, src/widgets/SettingsCatalogPanel.tsx, src/widgets/SettingsPagePanel.tsx, .planning/phases/39-ai-slob-technical-debt-remediation/39-03-LEDGER.md</files>
  <read_first>
    - `39-PATTERNS.md` "Pattern: Whole-File Deletion (mechanical, non-registry, non-edge-function)" — the three mechanical steps and the exclusion filter.
    - `39-PATTERNS.md` "Pattern: Dynamic Route Registration (router.tsx)" — required before touching `src/app/kitchen-prep-route.tsx`, since route modules are referenced through `lazy()` and JSX rather than plain imports.
    - `39-01-LEDGER.md` — the disposition row format and the sanity-check command established by the tracer.
    - `src/app/router.tsx` — check whether `kitchen-prep-route` is registered before deleting it.
    - `package.json` `scripts` — check each `scripts/seed-*.ts` name before deleting it.
  </read_first>
  <action>
    Work the 15 candidates flagged by both knip modes: the seven under `scripts/` (`seed-combos.ts`, `seed-ingredients.ts`, `seed-prep.ts`, `seed-recipes.ts`, `seed-reports.ts`, `test-payment-auth.mjs`, `write-env-local-from-cloud-secret.mjs`), `src/app/kitchen-prep-route.tsx`, `src/shared/lib/agent/index-status.ts`, `src/shared/lib/supabase-realtime.ts`, and the five under `src/widgets/` (`OrderPanel/CartSummary.tsx`, `OrderPanel/OrderItemCard.tsx`, `RappiOrderBadge/RappiOrderBadge.tsx`, `SettingsCatalogPanel.tsx`, `SettingsPagePanel.tsx`).

    Apply the tracer's loop to each: search the whole repository for the basename across source, config, CI, and shell files; record the hit count; delete only on zero hits outside the file itself.

    Three candidates need a check beyond the basename search. `src/app/kitchen-prep-route.tsx` must be checked against `src/app/router.tsx` for a `lazy()` registration or a JSX route element referencing it — a registered route is live and gets a false-positive row instead. The `scripts/seed-*.ts` files must be checked against `package.json` `scripts` and against sibling scripts that might shell out to them; note that `seed-dev-data.ts` is the script wired to `npm run setup:dev`, which is a different file, so a seed script sharing the prefix is not automatically live. `src/widgets/RappiOrderBadge/RappiOrderBadge.tsx` is re-exported by its sibling `index.ts` barrel, which plan 39-08 owns — delete the component only if the barrel is also confirmed dead, and otherwise defer the pair to 39-08 rather than leaving a barrel re-exporting a deleted file.

    Run `npm run typecheck`, `npm run lint`, and `npm run test` after the sweep, then re-run both knip modes and record the new unused-file count against the baseline in `39-01-LEDGER.md`.

    Append one disposition row per candidate to `39-03-LEDGER.md`, including the search command, the hit count, and the outcome.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 15 candidates has a disposition row in `39-03-LEDGER.md` recording the search command, the hit count, and the outcome (`DELETED`, `FALSE POSITIVE — NOT DELETED`, or `DEFERRED TO 39-08`).
    - No file was deleted with a nonzero external hit count.
    - `npm run typecheck`, `npm run lint`, and `npm run test` all pass after the sweep.
    - A fresh `npx knip --reporter json` no longer lists any deleted file, and the new unused-file total is recorded against `39-01-LEDGER.md`'s baseline.
    - No `index.ts` barrel file was deleted by this plan.
  </acceptance_criteria>
  <done>Every both-modes non-barrel candidate is either deleted with proof or spared with a written reason, and the toolchain is green.</done>
  <reversibility rating="costly">Deleted files are recoverable from git history but require re-adding and re-wiring; the per-file repository-wide search plus the typecheck/lint/test gate is the control that keeps recovery from being needed.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Functions gateway -> deployed edge function | HTTP entry points reached by URL, with no TypeScript in-edge for knip to observe. |
| test/lint toolchain config -> config-referenced module | `vitest.config.ts`, `playwright.config.ts`, and `eslint.config.js` reach modules by path string, invisible to a production-mode module graph. |
| npm script -> script file | `package.json` `scripts` invokes files by path string, not by import. |
| React router -> lazily-imported route module | `lazy(() => import(...))` plus JSX registration, not a static import chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Denial of Service | `supabase/functions/*/index.ts`, `_shared/audit.ts` | critical | mitigate | Deletion is prohibited outright by the prohibitions block; Task 1 produces evidence rows only, and an acceptance criterion asserts all 14 files still exist and `git status` shows no deletion under `supabase/functions/`. |
| T-39-02 | Denial of Service | test and lint toolchain (`e2e/helpers/*`, `src/test/global-setup.ts`, `src/shared/lib/test-setup.ts`, `eslint-rules/*.js`) | high | mitigate | Production-only findings are presumptively false-positive; Task 2 proves liveness per file from config wiring and importer counts, and the verify gate runs `typecheck`, `lint`, `test`, and asserts the 59-spec count still resolves. |
| T-39-10 | Denial of Service | `src/app/kitchen-prep-route.tsx` | high | mitigate | Route modules are reached via `lazy()` + JSX; Task 3 requires an explicit `src/app/router.tsx` check before deletion (39-PATTERNS.md dynamic-route pattern). |
| T-39-11 | Denial of Service | `scripts/seed-*.ts`, `scripts/*.mjs` | medium | mitigate | Repository-wide basename search spanning `package.json` scripts, CI workflow files, and sibling shell scripts before any deletion; a nonzero external hit count blocks the deletion. |
| T-39-12 | Tampering | barrel/component pair (`RappiOrderBadge/index.ts` + `RappiOrderBadge.tsx`) | medium | mitigate | Deleting the component while its barrel survives would leave a barrel re-exporting a missing module; Task 3 defers the pair to plan 39-08 unless both are confirmed dead together. |
| T-39-03 | Repudiation | audit trail of dispositions | medium | mitigate | 48 ledger rows, each carrying the search command, hit count, and outcome, so a later regression traces back to the decision that allowed it. |
</threat_model>

<verification>
- All 14 `supabase/functions/**` files and all 19 production-only files still exist unless a deletion is separately justified in the ledger.
- `39-03-LEDGER.md` contains a disposition row for all 48 findings covered by this plan.
- `npm run typecheck && npm run lint && npm run test` pass.
- `ls e2e/*.spec.ts | wc -l` still returns 59.
- A fresh knip run shows the unused-file count reduced by exactly the number of files this plan deleted.
</verification>

<success_criteria>
Every knip unused-file finding in the phase has a written disposition; only files that survived a repository-wide sanity check were deleted; the test, lint, and E2E-collection toolchains are provably intact.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-03-SUMMARY.md` when done.
</output>