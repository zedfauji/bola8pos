---
phase: 39-ai-slob-technical-debt-remediation
plan: 04
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - e2e/01-ci.spec.ts
  - e2e/02-caja.spec.ts
  - e2e/03-tab-order.spec.ts
  - e2e/04-pool-timer.spec.ts
  - e2e/06-transfer.spec.ts
  - e2e/07-reports.spec.ts
  - e2e/09-rbac.spec.ts
  - e2e/10-inventory.spec.ts
  - e2e/11-offline.spec.ts
  - e2e/13-tauri-build.spec.ts
  - e2e/14-manual-stubs.spec.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md
  - .planning/todos/ (append-only — plan 39-04 creates new todo files, never edits an existing one)
autonomous: true
requirements: [D-03, D-04, D-05, D-06]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 43 E2E findings across specs 01 through 14 carry a written classification backed by real Playwright error output, not test titles (D-04), satisfying D-06's requirement for real per-test investigation of the unannotated findings (excluding the 11 D-05-routed rows)."
    - "The 11 findings already tied to Phase 38's known root causes are routed there without re-triage and without any code change (D-05)."
    - "`e2e/01-ci.spec.ts`'s three failures are explained against the audit's own clean typecheck / lint / unit-test results, since a passing toolchain and a failing CI spec cannot both be describing the same thing."
    - "Every real product bug found is filed as a todo rather than fixed inline (D-03)."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md — one row per finding, in 39-02-LEDGER.md's format."
    - "One `.planning/todos/*.md` per distinct real product bug (zero is a valid outcome)."
  key_links:
    - "`.audit-tmp/e2e-per-spec/<spec>.json` -> `suites[].suites[].specs[].tests[].results[].error.message`."
    - "10-CHECKLIST.md 'Cross-check against existing trackers' Phase 38 row -> the 11 pre-routed findings."
  prohibitions:
    - "Do NOT re-triage the 11 Phase-38 findings (`02-caja.spec.ts:61`; `04-pool-timer.spec.ts:38,50,65,81,97`; `07-reports.spec.ts:621,647,729,752,774`) — they are confirmed and routing them is the whole action (D-05)."
    - "Do NOT delete a failing test to reach green."
    - "Do NOT fix a real product bug inline; file a todo (D-03)."
    - "Do NOT touch any spec file outside the 11 listed in `files_modified` — the other Track A plans own them concurrently."
    - "Do NOT introduce new UI, copy, or components while fixing a test (39-UI-SPEC.md guardrails 1-3, 5)."
---

<objective>
Triage the 43 E2E findings in spec files 01 through 14 to a written disposition.

This batch holds all 11 findings already confirmed against Phase 38's root causes, so a meaningful part of the work is routing rather than investigation. It also holds the phase's strangest finding: `e2e/01-ci.spec.ts` asserts that `npm run typecheck`, `npm run lint`, and `npm run test` each exit 0, and all three assertions failed — while the same audit run independently recorded 0 tsc errors, 0 eslint findings, and 1391 passing unit tests. One of those two observations is about the harness rather than the code, and settling which is worth doing early because the answer affects how much of the rest of the suite is trustworthy.

Purpose: convert 43 raw findings into classified, actionable rows.
Output: a filled ledger, todos for real bugs, and test-side fixes where the test itself was wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-UI-SPEC.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No new source symbols. This plan produces a classification ledger, zero or more todo files, and test-side edits confined to the 11 spec files listed in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Route the 11 confirmed Phase 38 findings and settle the 01-ci contradiction</name>
  <files>e2e/01-ci.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md</files>
  <read_first>
    - `10-CHECKLIST.md` "Cross-check against existing trackers", the Phase 38 row — it names all three root causes and the exact 11 spec lines.
    - `10-CHECKLIST.md` Blocking > unit test and Blocking > knip headers — the record of 0 tsc errors, 0 eslint findings, and 1391/1406 passing unit tests, which is what `01-ci.spec.ts` contradicts.
    - `e2e/01-ci.spec.ts` — the three assertions at lines 13, 17, and 21 and how they shell out.
    - `.audit-tmp/e2e-per-spec/01-ci.json` — the real error output for those three failures.
    - `39-02-LEDGER.md` — the row format to reuse.
  </read_first>
  <action>
    First, write the 11 Phase-38 rows with no investigation: `02-caja.spec.ts:61` (test-DB pollution producing `OPEN_TABS_EXIST` from a stale prior-run caja), `04-pool-timer.spec.ts` lines 38, 50, 65, 81, and 97 (no pool table left in `available` seed state), and `07-reports.spec.ts` lines 621, 647, 729, 752, and 774 (missing seeded date-ranged report data). Each row gets classification `infra`, action `ROUTED TO PHASE 38`, and no code change. D-05 already spent the triage budget on these.

    Then investigate `01-ci.spec.ts`. Read the actual error output for its three failures and determine whether the spec is failing because the underlying command genuinely fails when spawned from Playwright's environment (working directory, PATH, env vars, or a timeout shorter than the command's runtime), or because the app code is genuinely broken — which the audit's own clean tsc/eslint/vitest results argue against. Run the three commands directly and compare their exit codes to what the spec observed; a green direct run alongside a red spawned run is the signature of a harness problem, and the ledger should say so explicitly.

    This matters beyond three findings: if `01-ci.spec.ts` fails for environmental reasons, that raises the prior that other specs in this phase fail environmentally too, and the ledger should record that inference for the other Track A plans to use.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; test "$(grep -c 'ROUTED TO PHASE 38' .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md)" -ge 11</automated>
  </verify>
  <acceptance_criteria>
    - `39-04-LEDGER.md` contains 11 rows marked `ROUTED TO PHASE 38`, matching the exact spec lines named in D-05.
    - `39-04-LEDGER.md` contains 3 rows for `e2e/01-ci.spec.ts` lines 13, 17, and 21, each recording the spec's observed error and the exit code of the same command run directly.
    - The ledger states an explicit verdict on whether `01-ci.spec.ts`'s failures are harness-environmental or real, with the evidence for that verdict.
    - `git status` shows no code change attributable to the 11 routed findings.
  </acceptance_criteria>
  <done>The pre-confirmed findings are routed with no wasted investigation, and the toolchain-versus-CI-spec contradiction is resolved with evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the remaining 29 findings in specs 03, 04, 06, 07, 09, 10, 11, 13, and 14</name>
  <files>e2e/03-tab-order.spec.ts, e2e/04-pool-timer.spec.ts, e2e/06-transfer.spec.ts, e2e/07-reports.spec.ts, e2e/09-rbac.spec.ts, e2e/10-inventory.spec.ts, e2e/11-offline.spec.ts, e2e/13-tauri-build.spec.ts, e2e/14-manual-stubs.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/` JSON for each spec in this batch — the `suites[].suites[].specs[].tests[].results[].error.message` path (39-RESEARCH.md documents it; regenerate a stale file with the `PLAYWRIGHT_JSON_OUTPUT_FILE` single-spec command from its Code Examples).
    - `10-CHECKLIST.md` Blocking > e2e for the exact titles at each line.
    - `39-RESEARCH.md` Pitfall 3 (batch by spec file, hypothesize shared cause) and Pitfall 4 (an un-skip is not a fix until the spec runs).
    - `CLAUDE.md` "Implemented Features" — the authority on whether a stale skip reason names a since-shipped feature.
    - `39-02-LEDGER.md` — the classification scheme (`infra` / `real-regression` / `obsolete` / `harness` / `valid-skip` / `conditional`) and row format.
  </read_first>
  <action>
    Work the remaining findings spec file by spec file, grouping by error signature within each file before classifying:

    `03-tab-order.spec.ts` — 1 failure at line 14, 2 skips at lines 138 and 163. The failure's error output is already quoted in 39-RESEARCH.md as a `getByRole('dialog')` visibility timeout, one instance of the recurring dialog-timing shape worth correlating against 39-02's findings.

    `04-pool-timer.spec.ts` — 1 unrouted failure at line 189 plus 4 skips at lines 117, 160, 205, and 309. The five routed failures in this file are already handled; check whether line 189 shares their seed-state cause, in which case it routes too rather than being investigated as new.

    `06-transfer.spec.ts` — failures at lines 60 and 155, both involving pool-session preservation across a transfer.

    `07-reports.spec.ts` — 3 unrouted failures at lines 247, 320, and 878. Line 878 references Phase 24's bartender reason-required removal path.

    `09-rbac.spec.ts` — 1 failure at line 250, 3 skips at lines 75, 91, and 152. Cross-check each skip reason against `CLAUDE.md`'s RBAC actions and Implemented Features before assuming staleness.

    `10-inventory.spec.ts` — 5 failures at lines 16, 36, 52, 94, and 131. A five-failure cluster in one file is a prime shared-cause candidate; establish whether one seed or auth condition explains all five before writing five separate rows.

    `11-offline.spec.ts` — 1 failure at line 69, 1 skip at line 65.

    `13-tauri-build.spec.ts` line 14 and `14-manual-stubs.spec.ts` lines 4, 11, 18, and 25 — five skips that are almost certainly platform-gated or deliberately manual. Confirm the stated constraint still holds and classify them `valid-skip` with the reason recorded; do not un-skip a Windows-only or manual-verification stub on an Ubuntu dev machine.

    Every un-skip must be followed by an actual run of that spec before the ledger records an outcome.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/(01|02|03|04|06|07|09|10|11|13|14)-' .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md)" -ge 43</automated>
  </verify>
  <acceptance_criteria>
    - `39-04-LEDGER.md` has at least 43 rows keyed by `e2e/<spec>:<line>`, covering every finding in this batch.
    - Every row carries a classification and a one-line evidence excerpt from `error.message` or from the skip reason string.
    - Every multi-failure spec file in this batch states an explicit root-cause group count.
    - Every un-skipped test has a recorded post-un-skip spec-run outcome.
  </acceptance_criteria>
  <done>All 43 findings in this batch are classified against real evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply harness fixes, file todos, and record final spec state</name>
  <files>e2e/01-ci.spec.ts, e2e/03-tab-order.spec.ts, e2e/04-pool-timer.spec.ts, e2e/06-transfer.spec.ts, e2e/07-reports.spec.ts, e2e/09-rbac.spec.ts, e2e/10-inventory.spec.ts, e2e/11-offline.spec.ts, e2e/13-tauri-build.spec.ts, e2e/14-manual-stubs.spec.ts, .planning/todos/, .planning/phases/39-ai-slob-technical-debt-remediation/39-04-LEDGER.md</files>
  <read_first>
    - `39-04-LEDGER.md` as written by Tasks 1 and 2 — the classification is the only authority for what this task may touch.
    - `39-UI-SPEC.md` "Guardrails for Planner and Executor" items 1, 2, 3, and 5.
    - `39-CONTEXT.md` D-03 — real product bugs become todos; the only exception is a one-line fix unambiguously sharing the debt item's root cause.
    - An existing `.planning/todos/*.md` file for the naming and frontmatter convention.
  </read_first>
  <action>
    Act strictly on the ledger. Fix `harness` rows inside the spec file itself — stale selector, missing wait, assertion written against a since-changed but intentional UI — with no edits under `src/`. Leave `infra` rows untouched and record their routing. Write one todo per distinct root cause for `real-regression` rows, referencing spec file, line, observed error, and expected behavior. Justify `obsolete` rows in writing before updating or removing the test.

    Then run each touched spec and record its final stats in the ledger. The batch is not required to be green — routed and todo-filed findings legitimately stay red. What is required is that every remaining red test maps to a ledger row that explains why.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; git diff --name-only -- src/ | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Every `harness` row has a corresponding spec-file edit and a recorded post-fix result.
    - Every `real-regression` row has a matching `.planning/todos/*.md` file, and `git diff --name-only -- src/` is empty for this plan.
    - Every `obsolete` row carries a written justification before any test removal.
    - The ledger records a final spec-run result for each touched spec, and every still-failing test maps to a row.
  </acceptance_criteria>
  <done>All 43 findings are dispositioned: fixed, routed, filed, or justified, with no unexplained red test in this batch.</done>
  <reversibility rating="reversible">All edits are test-side or planning-doc-side; no production behavior changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> release gate | E2E is not CI-enforced; weakening a test silently reduces release coverage. |
| triage decision -> product risk | Labelling a real regression `infra` routes a live bug to a phase that will never look for it. |
| Playwright-spawned shell -> npm scripts | `01-ci.spec.ts` shells out to `npm run typecheck/lint/test`; a mismatch between spawned and direct exit codes is an environment signal, not a code signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Tampering | E2E gate strength across specs 01-14 | high | mitigate | Deleting a failing test to reach green is prohibited; `obsolete` requires written justification; every un-skip requires an actual spec run (Pitfall 4). |
| T-39-08 | Denial of Service | real regressions hidden behind an `infra` label | high | mitigate | Only the 11 D-05 findings route without evidence; every other `infra` classification must cite the specific seed or timing evidence in `error.message`. |
| T-39-13 | Spoofing | RBAC assertions in `09-rbac.spec.ts` and `10-inventory.spec.ts:131` | high | mitigate | These specs assert role-gating (bartender redirected from `/inventory`, admin-only permission matrix). A failure here could be a genuine access-control regression, so an `infra` or `obsolete` classification on an RBAC assertion requires evidence that the gate itself still holds, and any doubt files a `real-regression` todo instead. |
| T-39-09 | Tampering | unauthorized UI change during a test fix | medium | mitigate | 39-UI-SPEC.md guardrails cited in `read_first`; `git diff --name-only -- src/` must be empty, enforced in the Task 3 verify gate. |
| T-39-07 | Repudiation | triage classification | medium | mitigate | Every row requires an evidence excerpt from real error output; title-only classification is disallowed (D-04). |
</threat_model>

<verification>
- `39-04-LEDGER.md` has at least 43 rows, each with a classification and evidence.
- 11 rows are marked `ROUTED TO PHASE 38` and match D-05's exact spec lines.
- No file under `src/` was modified by this plan.
- `npm run typecheck && npm run lint && npm run test` pass.
</verification>

<success_criteria>
The 43 findings in specs 01 through 14 are fully dispositioned, the Phase 38 routing is recorded without duplicated investigation, and the `01-ci.spec.ts` contradiction is settled with evidence that informs the remaining Track A plans.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-04-SUMMARY.md` when done.
</output>
