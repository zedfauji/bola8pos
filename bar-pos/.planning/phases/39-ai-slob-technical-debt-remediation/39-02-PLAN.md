---
phase: 39-ai-slob-technical-debt-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/16-table-status.spec.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md
  - .planning/todos/ (append-only — plan 39-02 creates new todo files, never edits an existing one)
autonomous: true
requirements: [D-03, D-04, D-06]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 16 E2E findings in `e2e/16-table-status.spec.ts` (12 failed + 4 skipped) carries a written classification of `infra` / `real-regression` / `obsolete` / `valid-skip`, each backed by the test's actual Playwright error output rather than its title (D-04)."
    - "A reusable triage ledger format exists that plans 39-04 through 39-07 fill in for the remaining 131 findings."
    - "Any real product bug found is filed as a todo, not fixed inline (D-03)."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md — one row per finding with error excerpt, classification, evidence, and action taken."
    - "One `.planning/todos/*.md` file per real product bug found (zero is a valid outcome)."
  key_links:
    - "`.audit-tmp/e2e-per-spec/16-table-status.json` -> the `suites[].suites[].specs[].tests[].results[].error.message` path that carries the real failure reason."
    - "39-02-LEDGER.md row format -> plans 39-04, 39-05, 39-06, 39-07."
  prohibitions:
    - "Do NOT delete a failing test to make the suite green — classification is the deliverable, and an obsolete test must be justified in the ledger before removal."
    - "Do NOT fix a real product bug inline; file it as a todo (D-03). The single exception is a one-line fix that is unambiguously the same root cause as the debt item being triaged."
    - "Do NOT touch any spec file other than `e2e/16-table-status.spec.ts` — the other 58 specs are owned by plans 39-04 through 39-07 and concurrent edits would collide."
    - "Do NOT introduce new UI, new copy, or new components while fixing a test (39-UI-SPEC.md guardrails 1-3, 5)."
---

<objective>
Establish the Track A (E2E triage) method and apply it end to end to the single largest failure cluster in the suite.

`e2e/16-table-status.spec.ts` holds 16 of the phase's 147 E2E findings — 12 failures and 4 skips, the densest single-file cluster in the audit. 39-RESEARCH.md Pitfall 3 predicts those 12 failures share 1-2 root causes rather than being 12 independent bugs. This plan is the **tracer for Track A**: it proves (or disproves) the shared-cause hypothesis on real error output, and it produces the ledger row format that the remaining four Track A plans fill in.

Purpose: get the triage method right on one spec before applying it to 58.
Output: 16 classified findings, a reusable ledger format, and todos for any real product bugs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-UI-SPEC.md
@e2e/16-table-status.spec.ts
</context>

<artifacts_this_phase_produces>
This plan produces **no new source symbols**. It produces:
- A classification ledger (`39-02-LEDGER.md`).
- Zero or more todo files under `.planning/todos/`.
- At most, edits inside `e2e/16-table-status.spec.ts` (un-skips, corrected selectors, or a justified test removal).

No new components, routes, copy, or exports.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Read the real failure output for all 12 failures and test the shared-cause hypothesis</name>
  <files>.planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/16-table-status.json` — navigate `suites[].suites[].specs[].tests[].results[].error.message` for each failing test (39-RESEARCH.md documents this exact path; the title alone is what 10-CHECKLIST.md's D-03 discipline was limited to and is explicitly insufficient here).
    - `10-CHECKLIST.md` Blocking > e2e, the 16 entries at `e2e/16-table-status.spec.ts` lines 192, 227, 283, 289, 318, 348, 376, 459, 527, 592, 615, 638, 674, 683, 692, 757.
    - `39-RESEARCH.md` Pitfall 3 (do not treat clustered failures as independent bugs) and the inline annotation on line 348 pointing at the existing todo `2026-07-27-print-popup-fallback-hangs-under-playwright-automation.md`.
    - `e2e/16-table-status.spec.ts` itself.
  </read_first>
  <action>
    Extract the actual error message for each of the 12 failing tests from the per-spec JSON. If `.audit-tmp/e2e-per-spec/16-table-status.json` is stale or missing (it is gitignored and regenerable), regenerate it with the `PLAYWRIGHT_JSON_OUTPUT_FILE` single-spec command documented in 39-RESEARCH.md's Code Examples rather than inferring from the digest.

    Group the 12 failures by error signature, not by test title. 39-RESEARCH.md flags a recurring `getByRole('dialog')` timeout shape across multiple specs as a possible shared UI-timing cause worth ruling in or out first. State the resulting root-cause count explicitly — if 12 failures reduce to 2 causes, say so and say which tests map to which cause.

    Classify each failure into exactly one of: `infra` (seed/test-DB/timing — route to Phase 38, do not fix here per D-05), `real-regression` (the app genuinely broke — file a todo per D-03), `obsolete` (the test asserts behavior that was intentionally changed — update or remove with justification), or `harness` (the test is wrong about the current UI: bad selector, missing wait). Every classification needs a one-line evidence citation from the error text.

    Note line 348 (`T6: Print Pre-cheque`) specifically: an existing todo already describes `window.print()` hanging under Playwright/CDP. If the error output matches, classify it `infra` and cross-reference the existing todo rather than opening a duplicate.

    Write `39-02-LEDGER.md` with one row per finding: spec line, test title, error excerpt (one line), root-cause group, classification, evidence, action. Design this row format to be copied by four later plans — keep it flat and greppable.
  </action>
  <verify>
    <automated>test -f .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md &amp;&amp; test "$(grep -c '^| e2e/16-table-status.spec.ts:' .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md)" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - `39-02-LEDGER.md` contains at least 12 rows keyed by `e2e/16-table-status.spec.ts:<line>`, one per failing test.
    - Every row carries a classification from the four-value set and a one-line evidence excerpt drawn from `error.message`, not from the test title.
    - The ledger states an explicit root-cause group count and maps each failing test to a group.
    - Line 348's row cross-references the existing print-popup todo if the error matches.
  </acceptance_criteria>
  <done>All 12 failures are classified against real error output, with the shared-cause hypothesis explicitly confirmed or refuted.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the 4 skipped tests against shipped-feature reality</name>
  <files>e2e/16-table-status.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md</files>
  <read_first>
    - `e2e/16-table-status.spec.ts` at lines 283, 638, 674, 683 — the four `skip` call sites and their reason strings.
    - `CLAUDE.md` "Implemented Features" list — the authority on whether the feature a stale skip reason references has since shipped.
    - `10-CHECKLIST.md` Low > structural todo-fixme entries at `e2e/16-table-status.spec.ts:675` and `:684`, which document why T13 and T14 were skipped (two simultaneous browser contexts; `setOffline(true)` stalling `fetch()`).
    - `39-RESEARCH.md` Pitfall 4 — un-skipping without running the test is itself unverified remediation.
  </read_first>
  <action>
    For each of the four skips, read the reason string and decide between `valid-skip` (the stated constraint still holds — keep it, and record why), `stale` (the feature has shipped or the constraint no longer applies — un-skip it), and `conditional` (the skip is guarded by a runtime seed condition rather than a hardcoded disable, meaning it is really a seed-data finding and belongs to Phase 38's root causes).

    Distinguish those three carefully: a skip whose reason reads like missing seed state ("No available table...") is a runtime-conditional skip, not a stale placeholder, and un-skipping it changes nothing. A skip whose reason reads "UI not implemented" for a feature CLAUDE.md now lists as shipped is the stale case.

    T13 (line 674) and T14 (line 683) carry documented technical constraints in their adjacent TODO comments — evaluate whether those constraints still hold before touching them.

    For every test un-skipped, run the spec immediately and record the actual outcome. An un-skip that then fails is not "fixed" — it converts into a failure finding and gets classified through Task 1's four-value scheme instead.

    Append four rows to `39-02-LEDGER.md` in the same format.
  </action>
  <verify>
    <automated>test "$(grep -c '^| e2e/16-table-status.spec.ts:' .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md)" -ge 16</automated>
  </verify>
  <acceptance_criteria>
    - `39-02-LEDGER.md` has rows for all 16 findings (12 failures + 4 skips).
    - Each skip row states which of `valid-skip` / `stale` / `conditional` applies and why, citing the reason string.
    - Every test that was un-skipped has a recorded post-un-skip run outcome in its ledger row.
    - No test was un-skipped without a subsequent spec run.
  </acceptance_criteria>
  <done>All four skips are classified, and any un-skip is backed by an actual Playwright run rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Apply harness fixes, file todos for real bugs, and re-run the spec</name>
  <files>e2e/16-table-status.spec.ts, .planning/todos/, .planning/phases/39-ai-slob-technical-debt-remediation/39-02-LEDGER.md</files>
  <read_first>
    - `39-02-LEDGER.md` as written by Tasks 1 and 2 — the classification drives what this task is allowed to touch.
    - `39-UI-SPEC.md` "Guardrails for Planner and Executor" items 1, 2, 3, and 5 — no new components, no restyling, no new copy, and product bugs get routed out rather than redesigned in place.
    - `39-CONTEXT.md` D-03 — real product bugs become todos, with a narrow exception for a trivial one-liner that is unambiguously the same root cause as the debt item.
    - An existing file under `.planning/todos/` for the naming and frontmatter convention.
  </read_first>
  <action>
    Act on the ledger, and only on the ledger:

    For rows classified `harness`, fix the test itself — a stale selector, a missing wait, an assertion written against a since-changed but intentional UI. These are test-side edits inside `e2e/16-table-status.spec.ts`; they must not change any file under `src/`.

    For rows classified `infra`, change nothing. Record the Phase 38 routing in the ledger. These are explicitly out of scope for this phase per D-05.

    For rows classified `real-regression`, write one todo file per distinct root cause under `.planning/todos/`, following the existing naming and frontmatter convention in that directory. Reference the spec file and line, the observed error, and the expected behavior. Do not fix the product code.

    For rows classified `obsolete`, state in the ledger what changed and why the test no longer describes intended behavior before updating or removing it. A removal with no written justification is not acceptable.

    Then run the full spec and record its final stats. The spec is not required to be fully green at the end of this plan — findings routed to Phase 38 and todo-filed regressions legitimately remain red. What is required is that every remaining red test maps to a ledger row explaining why it is still red.
  </action>
  <verify>
    <automated>npx playwright test e2e/16-table-status.spec.ts --reporter=line; echo "exit=$? (non-zero is acceptable only if every remaining failure has a ledger row)"</automated>
  </verify>
  <acceptance_criteria>
    - Every ledger row classified `harness` has a corresponding edit inside `e2e/16-table-status.spec.ts` and a recorded post-fix result.
    - Every ledger row classified `real-regression` has a matching file under `.planning/todos/`, and `git status` shows no modification to any file under `src/` from this plan.
    - Every ledger row classified `infra` records a Phase 38 routing note and no code change.
    - The ledger records the final `npx playwright test e2e/16-table-status.spec.ts` stats, and every still-failing test in that run maps to a ledger row.
  </acceptance_criteria>
  <done>The spec's 16 findings are fully dispositioned: fixed, routed, filed, or justified — with no unexplained red test remaining.</done>
  <reversibility rating="reversible">All edits are test-side or planning-doc-side; no production behavior changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> release gate | E2E is the only gate covering these flows and is not CI-enforced; removing or weakening a test silently reduces release coverage. |
| triage decision -> product risk | Misclassifying a real regression as `infra` routes a live bug to a phase that will never look for it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Tampering | E2E gate strength (`e2e/16-table-status.spec.ts`) | high | mitigate | Deleting a failing test to reach green is prohibited; `obsolete` classification requires written justification in the ledger, and every un-skip requires an actual spec run (Pitfall 4). |
| T-39-07 | Repudiation | triage classification | medium | mitigate | Every ledger row requires a one-line evidence excerpt from `error.message`; title-only classification is explicitly disallowed (D-04). |
| T-39-08 | Denial of Service | product regressions hidden behind an `infra` label | high | mitigate | Only the 11 findings already named in D-05 route to Phase 38 without re-triage; every other `infra` classification must cite the specific seed/timing evidence in the error output. None of `16-table-status.spec.ts`'s findings are in D-05's confirmed list, so all 16 require real evidence. |
| T-39-09 | Tampering | unauthorized UI change during a test fix | medium | mitigate | 39-UI-SPEC.md guardrails 1-3 and 5 are cited in `read_first`; `real-regression` rows are barred from touching `src/` and `git status` cleanliness under `src/` is an acceptance criterion. |
</threat_model>

<verification>
- `39-02-LEDGER.md` has 16 rows, each with a classification and evidence excerpt.
- No file under `src/` was modified by this plan.
- Every un-skipped test has a recorded post-un-skip run result.
- Every still-failing test in the final spec run maps to a ledger row.
</verification>

<success_criteria>
The largest E2E failure cluster in the suite is fully dispositioned against real error output, the shared-cause hypothesis is settled with evidence, and a ledger row format exists for the remaining 131 findings.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-02-SUMMARY.md` when done.
</output>
