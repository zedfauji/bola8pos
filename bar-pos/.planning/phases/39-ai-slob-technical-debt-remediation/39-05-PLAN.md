---
phase: 39-ai-slob-technical-debt-remediation
plan: 05
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - e2e/15-home-navigation.spec.ts
  - e2e/18-modifier-notes-kds.spec.ts
  - e2e/18-updater.spec.ts
  - e2e/18-void-order.spec.ts
  - e2e/19-caja-entries.spec.ts
  - e2e/20-error-scenarios.spec.ts
  - e2e/21-prep.spec.ts
  - e2e/21-product-management.spec.ts
  - e2e/22-sprint3-billing.spec.ts
  - e2e/22-staff-management.spec.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md
  - .planning/todos/ (append-only — plan 39-05 creates new todo files, never edits an existing one)
autonomous: true
requirements: [D-03, D-04, D-06]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 32 E2E findings across specs 15 through 22 carry a written classification backed by real Playwright error output or by the literal skip-reason string (D-04), satisfying D-06's requirement for real per-test investigation of the unannotated findings."
    - "Every `'UI not implemented — EXPECTED FAIL'` skip in `18-void-order.spec.ts` is checked against CLAUDE.md's Implemented Features list, and any un-skip is verified by an actual spec run rather than by pattern-matching the reason text (39-RESEARCH.md Pitfall 4)."
    - "Every real product bug found is filed as a todo rather than fixed inline (D-03)."
  artifacts:
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md — one row per finding, in 39-02-LEDGER.md's format."
    - "One `.planning/todos/*.md` per distinct real product bug (zero is a valid outcome)."
  key_links:
    - "`.audit-tmp/e2e-per-spec/<spec>.json` -> `suites[].suites[].specs[].tests[].results[].error.message`."
    - "CLAUDE.md 'Implemented Features' (`void-order` shipped) -> the stale-skip verdict for `18-void-order.spec.ts`."
  prohibitions:
    - "Do NOT delete a failing test to reach green."
    - "Do NOT fix a real product bug inline; file a todo (D-03)."
    - "Do NOT un-skip a test and record it as fixed without running the spec."
    - "Do NOT touch any spec file outside the 10 listed in `files_modified` — the other Track A plans own them concurrently."
    - "Do NOT introduce new UI, copy, or components while fixing a test (39-UI-SPEC.md guardrails 1-3, 5)."
---

<objective>
Triage the 32 E2E findings in spec files 15 through 22 to a written disposition.

This batch is where 39-RESEARCH.md's assumption A2 gets tested. `18-void-order.spec.ts` carries seven findings whose skip and failure reasons read `'UI not implemented — EXPECTED FAIL'` for void-order — a feature CLAUDE.md's Implemented Features list says shipped. If A2 holds, a chunk of this batch is stale placeholders that re-enable cleanly. If it does not, the reasons are stale but the selectors have also drifted, and each one converts into a harness finding instead. Either way the answer is settled by running the spec, not by reading the reason string.

Purpose: convert 32 raw findings into classified, actionable rows and resolve the stale-skip assumption with evidence.
Output: a filled ledger, todos for real bugs, and test-side fixes where the test itself was wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-UI-SPEC.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No new source symbols. This plan produces a classification ledger, zero or more todo files, and test-side edits confined to the 10 spec files listed in `files_modified`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Settle the void-order stale-skip question across all 7 findings in 18-void-order.spec.ts</name>
  <files>e2e/18-void-order.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md</files>
  <read_first>
    - `e2e/18-void-order.spec.ts` — the failures at lines 93, 176, 212, 237, and 283 and the skips at lines 124 and 279, with their exact reason strings.
    - `.audit-tmp/e2e-per-spec/18-void-order.json` — real error output for the five failures.
    - `CLAUDE.md` "Implemented Features" — the `void-order` entry, and the `/pos` and payment-pane surfaces the spec drives.
    - `39-RESEARCH.md` Pitfall 4 and Assumption A2.
    - `39-02-LEDGER.md` — classification scheme and row format.
  </read_first>
  <action>
    Treat this spec as the batch's anchor. All seven findings reference the same feature, and their reasons name UI surfaces (void button in payment pane, void via POS order panel, bartender void gate, inventory restoration on void, reason validation, subtotal update after void, already-voided state) that CLAUDE.md now lists as shipped.

    For each of the seven, determine which of three states applies: the feature and its selectors both exist so the test re-enables and passes; the feature exists but the test's selectors or flow no longer match the current UI, making it a `harness` finding; or the feature exists and the test is correct but the behavior is genuinely wrong, making it a `real-regression` that gets a todo.

    Resolve this by running the spec, not by reading the reason strings. Un-skip the two skips, run `npx playwright test e2e/18-void-order.spec.ts`, and read what actually happens. Record the run output per test in the ledger. If un-skipping produces a failure, that is a legitimate outcome — the finding converts from `skipped` to a failure and gets classified through the same scheme; it is not a regression introduced by this plan.

    State an explicit verdict on 39-RESEARCH.md's assumption A2 in the ledger, since plan 39-06 and 39-07 will lean on it for their own stale-skip triage.
  </action>
  <verify>
    <automated>test "$(grep -c '^| e2e/18-void-order.spec.ts:' .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md)" -ge 7</automated>
  </verify>
  <acceptance_criteria>
    - `39-05-LEDGER.md` has 7 rows for `e2e/18-void-order.spec.ts` lines 93, 124, 176, 212, 237, 279, and 283.
    - Every row records the actual post-run outcome, not an inference from the skip reason.
    - The ledger states an explicit confirm-or-refute verdict on assumption A2 with the evidence behind it.
  </acceptance_criteria>
  <done>The void-order cluster is fully resolved against real runs, and the stale-skip assumption is settled for the remaining Track A plans.</done>
</task>

<task type="auto">
  <name>Task 2: Triage the remaining 25 findings in specs 15, 18-modifier, 18-updater, 19, 20, 21, and 22</name>
  <files>e2e/15-home-navigation.spec.ts, e2e/18-modifier-notes-kds.spec.ts, e2e/18-updater.spec.ts, e2e/19-caja-entries.spec.ts, e2e/20-error-scenarios.spec.ts, e2e/21-prep.spec.ts, e2e/21-product-management.spec.ts, e2e/22-sprint3-billing.spec.ts, e2e/22-staff-management.spec.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md</files>
  <read_first>
    - `.audit-tmp/e2e-per-spec/` JSON for each spec in this batch (regenerate a stale file with the `PLAYWRIGHT_JSON_OUTPUT_FILE` single-spec command from 39-RESEARCH.md's Code Examples).
    - `10-CHECKLIST.md` Blocking > e2e for the exact titles at each line, and Low > structural todo-fixme for the documented reasons behind `18-modifier-notes-kds.spec.ts:403` and `:419`.
    - `CLAUDE.md` "Implemented Features" and "Routes" — the authority on whether a skip reason names a since-shipped feature or a route that now exists.
    - `39-RESEARCH.md` Pitfall 3 (batch by spec file, hypothesize shared cause).
  </read_first>
  <action>
    Work file by file, grouping by error signature within each before classifying:

    `15-home-navigation.spec.ts:104` — a bartender navigating directly to `/settings` should be redirected or shown an access dialog. This is an access-control assertion; a failure here needs evidence that the gate itself still holds before it can be classified as anything other than a regression.

    `18-modifier-notes-kds.spec.ts` lines 400 and 416 — both skips carry documented technical reasons in adjacent TODO comments (`order_item_modifiers` table absent from the remote DB; Tauri IPC unavailable in Playwright browser mode). Verify whether each stated constraint still holds; if it does, classify `valid-skip` and record why rather than un-skipping.

    `18-updater.spec.ts:23` — the app booting without console errors when the updater plugin is registered.

    `19-caja-entries.spec.ts` — a failure at line 162 (amount-0 form validation) and a skip at line 186.

    `20-error-scenarios.spec.ts` — failures at lines 32, 85, 119, and 175 plus a skip at line 188. These assert error paths (paying a tab with an active pool session, closing caja with open tabs, adding an item to a paid tab, session-cleared redirect). Four failures across one file is a shared-cause candidate; note that the caja and pool-session errors overlap with Phase 38's known seed-state issues, so check whether the error output shows the app failing to enforce the rule or the fixture failing to reach the state where the rule applies.

    `21-prep.spec.ts:95` and `21-product-management.spec.ts` (failure at line 264, skips at lines 59, 96, 170, and 202) — the product-management skips create and edit test entities, so check whether they are runtime-conditional on seed state rather than statically disabled.

    `22-sprint3-billing.spec.ts` lines 71 and 115, and `22-staff-management.spec.ts` (failures at lines 28 and 79, skips at lines 47, 91, 117, and 143) — several staff-management skip reasons read like missing seed profiles, which is a seed-data finding rather than a stale placeholder.

    Distinguish carefully between a static disable (a hardcoded skip for an unimplemented feature) and a runtime-conditional skip (guarded by missing seed state). The second is a Phase 38-shaped finding and un-skipping it changes nothing.
  </action>
  <verify>
    <automated>test "$(grep -cE '^\| e2e/(15|18|19|20|21|22)-' .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md)" -ge 32</automated>
  </verify>
  <acceptance_criteria>
    - `39-05-LEDGER.md` has at least 32 rows keyed by `e2e/<spec>:<line>`, covering every finding in this batch.
    - Every row carries a classification and a one-line evidence excerpt from `error.message` or from the skip reason string.
    - Every skip row states whether the skip is a static disable or runtime-conditional on seed state.
    - Every un-skipped test has a recorded post-un-skip spec-run outcome.
  </acceptance_criteria>
  <done>All 32 findings in this batch are classified against real evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Apply harness fixes, file todos, and record final spec state</name>
  <files>e2e/15-home-navigation.spec.ts, e2e/18-modifier-notes-kds.spec.ts, e2e/18-updater.spec.ts, e2e/18-void-order.spec.ts, e2e/19-caja-entries.spec.ts, e2e/20-error-scenarios.spec.ts, e2e/21-prep.spec.ts, e2e/21-product-management.spec.ts, e2e/22-sprint3-billing.spec.ts, e2e/22-staff-management.spec.ts, .planning/todos/, .planning/phases/39-ai-slob-technical-debt-remediation/39-05-LEDGER.md</files>
  <read_first>
    - `39-05-LEDGER.md` as written by Tasks 1 and 2 — the classification is the only authority for what this task may touch.
    - `39-UI-SPEC.md` "Guardrails for Planner and Executor" items 1, 2, 3, and 5.
    - `39-CONTEXT.md` D-03.
    - An existing `.planning/todos/*.md` file for the naming and frontmatter convention.
  </read_first>
  <action>
    Act strictly on the ledger. Fix `harness` rows inside the spec file itself, with no edits under `src/`. Leave `infra` rows untouched and record their routing. Write one todo per distinct root cause for `real-regression` rows, referencing spec file, line, observed error, and expected behavior. Justify `obsolete` rows in writing before updating or removing the test.

    Pay particular attention to the access-control findings in this batch (`15-home-navigation.spec.ts:104` bartender-to-`/settings`, and `21-product-management.spec.ts:264` bartender product-management gating). If the error output leaves any doubt about whether the gate still holds, file a `real-regression` todo rather than classifying it away — a silently-removed role gate is the highest-consequence outcome available in this batch.

    Then run each touched spec and record final stats. The batch need not be green; every remaining red test must map to a ledger row explaining why.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; git diff --name-only -- src/ | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Every `harness` row has a corresponding spec-file edit and a recorded post-fix result.
    - Every `real-regression` row has a matching `.planning/todos/*.md` file, and `git diff --name-only -- src/` is empty for this plan.
    - Both access-control findings have an explicit written verdict on whether the role gate still holds.
    - The ledger records a final spec-run result for each touched spec, and every still-failing test maps to a row.
  </acceptance_criteria>
  <done>All 32 findings are dispositioned: fixed, routed, filed, or justified, with no unexplained red test in this batch.</done>
  <reversibility rating="reversible">All edits are test-side or planning-doc-side; no production behavior changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> release gate | E2E is not CI-enforced; weakening a test silently reduces release coverage. |
| bartender session -> admin-only surface | `15-home-navigation.spec.ts:104` and `21-product-management.spec.ts:264` are the tests that assert this boundary holds. |
| triage decision -> product risk | Labelling a real regression `infra` routes a live bug to a phase that will never look for it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-14 | Elevation of Privilege | bartender access gating (`15-home-navigation.spec.ts:104`, `21-product-management.spec.ts:264`) | high | mitigate | These failures are treated as candidate access-control regressions by default; Task 3 requires an explicit written verdict on whether the gate holds, and any doubt files a `real-regression` todo rather than reclassifying. |
| T-39-06 | Tampering | E2E gate strength across specs 15-22 | high | mitigate | Deleting a failing test to reach green is prohibited; `obsolete` requires written justification; every un-skip requires an actual spec run. |
| T-39-15 | Tampering | void-order coverage (`18-void-order.spec.ts`) | high | mitigate | Void is an irreversible money-affecting action whose only automated coverage is this spec; its seven findings are resolved by real runs in Task 1 rather than by re-skipping, and any remaining gap is recorded rather than silently accepted. |
| T-39-08 | Denial of Service | real regressions hidden behind an `infra` label | high | mitigate | Every `infra` classification in this batch must cite specific seed or timing evidence from `error.message`; none of these findings are in D-05's pre-confirmed list. |
| T-39-09 | Tampering | unauthorized UI change during a test fix | medium | mitigate | 39-UI-SPEC.md guardrails cited in `read_first`; `git diff --name-only -- src/` must be empty, enforced in the Task 3 verify gate. |
</threat_model>

<verification>
- `39-05-LEDGER.md` has at least 32 rows, each with a classification and evidence.
- The ledger carries an explicit verdict on 39-RESEARCH.md assumption A2.
- No file under `src/` was modified by this plan.
- `npm run typecheck && npm run lint && npm run test` pass.
</verification>

<success_criteria>
The 32 findings in specs 15 through 22 are fully dispositioned, the stale-skip assumption is settled with real run evidence for the remaining Track A plans, and both access-control findings have written verdicts.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-05-SUMMARY.md` when done.
</output>
