---
phase: 39-ai-slob-technical-debt-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/audit-ui-drift.ts
  - .planning/phases/39-ai-slob-technical-debt-remediation/39-01-LEDGER.md
autonomous: false
requirements: [D-01, D-07]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`npx knip --reporter json` reports zero `unlisted` findings (D-01: the 34 `@testing-library/user-event` Blocking findings are gone)."
    - "A fresh knip baseline exists on disk and its distinct-finding count is recorded, so every later plan measures against a real number rather than 39-RESEARCH.md's snapshot."
    - "One whole-file deletion has gone through the full grep-sanity -> delete -> typecheck -> test -> knip-recount loop and is committed, proving the loop for every later Track B plan (D-07)."
  artifacts:
    - "package.json declares `@testing-library/user-event` in `devDependencies`."
    - ".planning/phases/39-ai-slob-technical-debt-remediation/39-01-LEDGER.md — baseline counts + disposition of every finding this plan touched."
    - ".audit-tmp/knip-report.json and .audit-tmp/knip-production.json regenerated this phase (mtime after phase start)."
  key_links:
    - "package.json devDependencies -> the 34 `*.test.tsx` files importing `@testing-library/user-event` (they must still pass `npm run test`)."
    - "39-01-LEDGER.md baseline counts -> every later Track B plan's acceptance criteria."
  prohibitions:
    - "Do NOT delete any file under `supabase/functions/**` — those are Deno HTTP entry points with no TS in-edge (39-RESEARCH.md Pitfall 1)."
    - "Do NOT touch any finding under `src/shared/ui/**` or `*.stories.tsx` — Medium tier, out of scope (D-08)."
    - "Do NOT remove or add any entry in `dependencies`/`devDependencies` other than `@testing-library/user-event` — unused-dep removal is deferred (D-09)."
    - "Do NOT bump `@testing-library/user-event` past `^14.6.1` — pin to what is already resolved so the diff carries zero behavior risk."
---

<objective>
Establish the Track B (knip dead-code) execution loop and clear the 34 Blocking-tier `unlisted` findings.

This is the phase's **tracer plan for Track B**: it re-establishes a trustworthy baseline, makes the one trivial Blocking fix, and then drives exactly one whole-file deletion all the way through grep-sanity -> `git rm` -> `typecheck` -> `test` -> knip recount. Every later Track B plan repeats that loop at scale; if the loop is wrong, it is wrong here on one file instead of on 61.

Purpose: prove the deletion loop end-to-end before scaling it, and drop the Blocking-tier count from 181 to 147 in one line.
Output: a fresh knip baseline, a declared devDependency, one deleted dead file, and a ledger that later plans measure against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-CONTEXT.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-RESEARCH.md
@.planning/phases/39-ai-slob-technical-debt-remediation/39-PATTERNS.md
@knip.json
@package.json
</context>

<artifacts_this_phase_produces>
This phase is **subtractive**. It produces no new source symbols, no new components, no new routes.

This plan produces:
- One added line in `package.json` `devDependencies` (`@testing-library/user-event`) + the matching `package-lock.json` update.
- One deleted file: `scripts/audit-ui-drift.ts`.
- One planning artifact: `39-01-LEDGER.md` (baseline counts + per-finding disposition).

Nothing else. No new exports, no new files under `src/`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the knip baseline and record the real distinct-finding count</name>
  <files>.audit-tmp/knip-report.json, .audit-tmp/knip-production.json, .planning/phases/39-ai-slob-technical-debt-remediation/39-01-LEDGER.md</files>
  <read_first>
    - `39-RESEARCH.md` sections "knip Output Mechanics" and "Code Examples > Diffing distinct dead-code count after a deletion wave" (the `(file, line, name)` set-union pattern this task reuses verbatim).
    - `39-RESEARCH.md` "State of the Art" note on `10-REVIEW.md` WR-04: `.audit-tmp/` is never cleared between runs, so a stale artifact can masquerade as a fresh one.
    - `knip.json` (current config: an `ignore` array only, no `entry`/`project` overrides).
  </read_first>
  <action>
    Record the pre-change mtime of `.audit-tmp/knip-report.json`, then regenerate both reports with `npx knip --reporter json > .audit-tmp/knip-report.json` and `npx knip --production --reporter json > .audit-tmp/knip-production.json`. Confirm both mtimes advanced (WR-04 defense — a silently-failed regeneration leaves the old file in place with the old mtime).

    Then compute the distinct High-tier baseline by unioning `(file, line, name)` tuples for `exports` and `types`, and `file` for `files`, across both reports, excluding any path starting with `src/shared/ui/` (D-08) and ignoring the `dependencies`/`devDependencies` categories entirely (D-09). Use the `python3` pattern from 39-RESEARCH.md's Code Examples rather than inventing a counting method — a different methodology produces a different number and destroys comparability with every later plan.

    Write `39-01-LEDGER.md` recording, at minimum: the four baseline numbers (unused files, distinct unused exports, distinct unused types, duplicate-export pairs), their sum, the count of `unlisted` findings, and whether each number matches 39-RESEARCH.md's verified figures (61 / 613 / 305 / 3 = 982 distinct, 34 unlisted). Where a number differs, state the delta and the likely cause (commits landed since the 2026-08-03 audit run) — do not silently adopt the new number without saying it changed.
  </action>
  <verify>
    <automated>test "$(node -e "const a=require('./.audit-tmp/knip-report.json').issues;console.log(Array.isArray(a))")" = "true" &amp;&amp; node -e "const a=require('./.audit-tmp/knip-report.json').issues;const n=a.reduce((s,i)=>s+((i.unlisted||[]).length),0);console.log('unlisted:',n)"</automated>
  </verify>
  <acceptance_criteria>
    - `.audit-tmp/knip-report.json` and `.audit-tmp/knip-production.json` both have an mtime later than the start of this task.
    - `39-01-LEDGER.md` exists and contains a line for each of the four baseline categories with a numeric value and an explicit match/mismatch verdict against 982.
    - `39-01-LEDGER.md` records the current `unlisted` count (expected 34 pre-fix).
  </acceptance_criteria>
  <done>A fresh, verified knip baseline is on disk and its distinct-finding count is written down with an explicit comparison to the research figure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: package legitimacy gate for @testing-library/user-event</name>
  <what-built>Nothing yet — this is the package-legitimacy gate that must clear before any `npm install` runs.</what-built>
  <how-to-verify>
    `@testing-library/user-event` carries a `[SUS]` verdict in 39-RESEARCH.md's Package Legitimacy Audit. The flag fired on a "too-new" heuristic only: the package's latest patch (`14.6.3`) happened to publish on the audit's run date. The package itself is the canonical `testing-library` org package at ~45.5M weekly downloads, and version `14.6.1` is **already installed and already executing** in this repo transitively via `storybook` (confirmed with `npm ls @testing-library/user-event --all`) — the 1391 passing unit tests exercise it today.

    Confirm before approving:
    1. Open https://www.npmjs.com/package/@testing-library/user-event
    2. Confirm the repository field points at `github.com/testing-library/user-event`.
    3. Confirm weekly downloads are in the tens of millions, and that the package is not a recently-created lookalike.

    This gate exists because package-legitimacy checkpoints are never auto-approvable, regardless of how strong the false-positive argument is.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name the concern to halt Track B.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Declare @testing-library/user-event as a direct devDependency (34 Blocking findings)</name>
  <files>package.json, package-lock.json, .planning/phases/39-ai-slob-technical-debt-remediation/39-01-LEDGER.md</files>
  <read_first>
    - `package.json` `devDependencies` block.
    - `39-RESEARCH.md` "Version verification — corrected provenance chain": the checklist's claim that the package resolves via `@testing-library/react` is wrong; it resolves only via `storybook`, which is why the direct declaration is real hardening rather than cosmetic.
    - `10-CHECKLIST.md` Blocking > knip: the complete list of 34 `*.test.tsx` files importing it.
  </read_first>
  <action>
    Run `npm install --save-dev @testing-library/user-event@^14.6.1`. Pin to `^14.6.1` — the version already resolved and already exercised — not the newer `14.6.3`, so this commit is a pure "declare what is already used" change with no behavior delta.

    Confirm `npm run test` still passes at its existing 1391-passing baseline. A change in passing count means the install resolved a different version than the transitive one; if that happens, pin exactly and re-run rather than accepting the drift.

    Append to `39-01-LEDGER.md`: all 34 `unlisted` findings marked `RESOLVED — declared directly`, with the post-fix `unlisted` count from a knip re-run.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.devDependencies['@testing-library/user-event'])process.exit(1)" &amp;&amp; npx knip --reporter json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const n=JSON.parse(s).issues.reduce((a,i)=>a+((i.unlisted||[]).length),0);console.log('unlisted:',n);process.exit(n===0?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains `@testing-library/user-event` at `^14.6.1`.
    - `npx knip --reporter json` reports zero `unlisted` findings across all files.
    - `npm run test` passes with no reduction from the pre-change passing count.
    - `39-01-LEDGER.md` records all 34 findings as resolved.
  </acceptance_criteria>
  <done>Blocking-tier knip findings drop from 34 to 0; the unit suite is unchanged.</done>
  <reversibility rating="reversible">A single devDependency declaration of an already-resolved version; reverting is one `npm uninstall`.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Tracer — one whole-file deletion driven end to end</name>
  <files>scripts/audit-ui-drift.ts, .planning/phases/39-ai-slob-technical-debt-remediation/39-01-LEDGER.md</files>
  <read_first>
    - `scripts/audit-ui-drift.ts` (the file being deleted — read it before deleting it).
    - `39-PATTERNS.md` section "Pattern: Whole-File Deletion (mechanical, non-registry, non-edge-function)" — the exact three mechanical steps this task follows, and the exclusion filter.
    - `10-CHECKLIST.md` High > knip "Default mode — unused files": `scripts/audit-ui-drift.ts` is the first entry and is flagged in **both** knip modes, which is the strongest available signal.
  </read_first>
  <action>
    Delete `scripts/audit-ui-drift.ts` following 39-PATTERNS.md's whole-file pattern, and treat this task as the reference implementation every later Track B plan copies.

    Sanity-check first (D-07): search the whole repository — not just `src/` — for the basename `audit-ui-drift`, across `.ts`, `.tsx`, `.js`, `.json`, `.sh`, and `.yml`, so that a `package.json` script entry, a CI workflow step, or a sibling script invocation is caught. knip's module graph does not see any of those call shapes. Record the search command and its hit count in the ledger; a nonzero hit count outside the file itself means stop and reclassify as a false positive rather than deleting.

    Then `git rm` the file, run `npm run typecheck`, `npm run lint`, and `npm run test`, and re-run knip to confirm the finding is gone and no new finding appeared in its place.

    Append to `39-01-LEDGER.md` a single row in the disposition format that later plans reuse: file path, knip mode(s) that flagged it, the sanity-check command and hit count, the disposition (`DELETED` / `FALSE POSITIVE — NOT DELETED` / `DEFERRED`), and the post-deletion verification result. Do not delete any other file in this plan — scaling the sweep is 39-03's job.
  </action>
  <verify>
    <automated>test ! -f scripts/audit-ui-drift.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-ui-drift.ts` no longer exists.
    - A repository-wide search for `audit-ui-drift` returns zero hits in source, config, and CI files (matches inside `.planning/` and `.audit-tmp/` documentation are expected and do not count).
    - `npm run typecheck`, `npm run lint`, and `npm run test` all pass.
    - A re-run of `npx knip --reporter json` no longer lists `scripts/audit-ui-drift.ts` under any `files` array.
    - `39-01-LEDGER.md` contains one disposition row for this file in the format later plans reuse.
  </acceptance_criteria>
  <done>One confirmed-dead file is deleted, the full verification loop is proven green, and the ledger row format is established.</done>
  <reversibility rating="costly">Restoring a deleted file from git history is possible but requires re-adding and re-wiring; the grep-sanity step exists precisely to avoid needing it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion -> runtime | A symbol or file that knip calls dead but that is reached dynamically crosses from compile-time-clean to runtime-broken with no typecheck signal. |
| npm registry -> local devDependencies | An install pulls third-party code into the developer/test toolchain. |
| test harness -> release gate | Weakening or removing a test silently reduces what the release gate actually verifies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Denial of Service | `supabase/functions/*/index.ts` | high | mitigate | Out of this plan's deletion set entirely; the tracer deliberately targets `scripts/audit-ui-drift.ts`, and the prohibitions block forbids touching `supabase/functions/**`. |
| T-39-02 | Denial of Service | `scripts/audit-ui-drift.ts` deletion | high | mitigate | Task 3 requires a repository-wide basename search across `.ts/.tsx/.js/.json/.sh/.yml` (catching `package.json` scripts and CI steps that knip cannot see) before `git rm`, then typecheck + lint + test. |
| T-39-03 | Repudiation | audit trail of dispositions | medium | mitigate | `39-01-LEDGER.md` records the sanity-check command, hit count, and disposition per finding, so a later regression can be traced to the decision that caused it. |
| T-39-SC | Tampering | `npm install --save-dev @testing-library/user-event` | high | mitigate | Blocking human checkpoint before install (never auto-approvable), verifying the npm page and repo field; version pinned to the already-resolved `^14.6.1`; scope limited to one package by the prohibitions block. |
</threat_model>

<verification>
- `npx knip --reporter json` reports zero `unlisted` findings.
- `npm run typecheck && npm run lint && npm run test` all pass.
- `scripts/audit-ui-drift.ts` is absent and no longer appears in either knip report.
- `39-01-LEDGER.md` records the baseline counts and one disposition row.
</verification>

<success_criteria>
Blocking-tier knip findings go from 34 to 0, one whole-file deletion is committed with proof, and the baseline + ledger format that plans 39-03 and 39-08 through 39-11 depend on exists on disk.
</success_criteria>

<output>
Create `.planning/phases/39-ai-slob-technical-debt-remediation/39-01-SUMMARY.md` when done.
</output>
