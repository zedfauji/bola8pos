---
phase: 07-waitlist-whatsapp
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql
  - src/shared/lib/supabase.types.ts
autonomous: true
requirements: []
decisions: [D-01, D-02, D-04, D-06, D-08]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-04: two Postgres functions exist — `start_pool_session(p_table_id uuid, p_tab_id uuid)` and `seat_waitlist_party_and_start_session(p_entry_id, p_table_id, p_staff_id, p_shift_id, p_caja_session_id)` — and the waitlist one CALLS the shared one rather than duplicating its body."
    - "D-01: seating a waitlist party at a pool table creates a tab whose `customer_name` is exactly `\"{waitlist_entries.name} ({waitlist_entries.party_size})\"` (e.g. `García (4)`)."
    - "D-02: the seat→open-tab→start-timer sequence is all-or-nothing — a forced failure at the tab-insert or session-start step leaves `waitlist_entries.status` still `waiting`, no `tabs` row, and no `pool_sessions` row."
    - "D-06: `pool`/`carom` tables get tab + pool session; `consumption` tables get tab only with no `pool_sessions` row; `floating` tables get neither (plain seat only, matching D-05's manual-only rule)."
    - "D-08: `pg_net` is enabled on the live project — `net.http_post` resolves and an `UPDATE waitlist_entries SET status='notified'` succeeds instead of raising `schema \"net\" does not exist`."
  artifacts:
    - supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql
    - "`public.start_pool_session(uuid, uuid) RETURNS jsonb` live on the Supabase project"
    - "`public.seat_waitlist_party_and_start_session(uuid, uuid, uuid, uuid, uuid) RETURNS jsonb` live on the Supabase project"
    - src/shared/lib/supabase.types.ts (regenerated, or the documented `as any` fallback recorded in SUMMARY)
  key_links:
    - "`seat_waitlist_party_and_start_session` → `start_pool_session`: the wrapper must RAISE (not return) when the shared call reports `ok:false`, otherwise the tab insert commits and D-02 atomicity is silently broken."
    - "`resources.table_type` (text, CHECK `IN ('pool','carom','consumption','floating')`) → the D-06 branch: read server-side inside the RPC so the branch cannot be bypassed by a stale client cache."
    - "`trg_waitlist_notify` on `waitlist_entries` → `net.http_post`: the wrapper's status UPDATE passes through this AFTER UPDATE OF status trigger; the trigger body only touches `net` when NEW.status = 'notified', so seating is unaffected either way, but pg_net must exist for the separate notify path."
---

<objective>
Author, push, and prove the two atomic Postgres RPCs that make "seat waitlist party → open tab → start pool timer" a single transaction (D-02, D-04), including the `resources.table_type` branch (D-06) and the D-01 tab-naming convention, and enable the `pg_net` extension that the pre-existing `trg_waitlist_notify` trigger already depends on (D-08).

Purpose: every client-side task in plans 07-10 and 07-11 calls these functions. Nothing downstream can be verified until they are live on the Supabase project, so this plan is deliberately server-only and ends with the migration pushed and smoke-tested against real rows.

Output: one migration file, two live Postgres functions, `pg_net` enabled, and a regenerated `supabase.types.ts` so the client tasks can call the RPCs without a cast.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-waitlist-whatsapp/07-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (07-10 and 07-11 consume them; do not redefine):

**`public.start_pool_session(p_table_id uuid, p_tab_id uuid) RETURNS jsonb`**
- `SECURITY DEFINER`, `SET search_path = public`, `LANGUAGE plpgsql`
- `GRANT EXECUTE ... TO authenticated`
- Success: `{"ok": true, "session": <full pool_sessions row as jsonb>}`
- Failure: `{"ok": false, "code": "NOT_FOUND"}` or `{"ok": false, "code": "POOL_TABLE_OCCUPIED"}`
- **No role gate** (deliberate — see task 1)

**`public.seat_waitlist_party_and_start_session(p_entry_id uuid, p_table_id uuid, p_staff_id uuid, p_shift_id uuid, p_caja_session_id uuid) RETURNS jsonb`**
- `SECURITY DEFINER`, `SET search_path = public`, `LANGUAGE plpgsql`
- `GRANT EXECUTE ... TO authenticated`
- Success: `{"ok": true, "tab_id": <uuid|null>, "session": <pool_sessions row jsonb|null>}`
- Failure: `{"ok": false, "code": "NOT_FOUND" | "POOL_TABLE_OCCUPIED" | "DUPLICATE_ENTRY" | "AUTH_FORBIDDEN"}`
- **Has a role gate** mirroring the `waitlist_entries` manager+ RLS policy it bypasses

Every `code` value above is already a member of the `AppErrorCode` union in `src/shared/lib/result.ts` — do NOT add new codes.

Migration file: `supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Author both atomic RPCs + pg_net in one migration, push it, and prove the pool happy path end-to-end on the live DB</name>

  <files>supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql</files>

  <read_first>
    - `supabase/migrations/20260721000005_remove_tab_item_rpc.sql` — the closest structural precedent: `SECURITY DEFINER` + `SET search_path = public` + `jsonb_build_object('ok', false, 'code', ...)` early returns + `GRANT EXECUTE ... TO authenticated` + a commented manual DOWN block. Copy this shape.
    - `supabase/migrations/20260710000006_stop_pool_session_rpc.sql` — same domain (pool sessions); shows the `FOR UPDATE` lock convention and the jsonb return payload the resource hooks already consume.
    - `supabase/migrations/20260501000001_waitlist_entries.sql` — `waitlist_entries` columns (`name` NOT NULL, `party_size` smallint NOT NULL, `status` text CHECK, `table_id`, `seated_at`) and its manager+ RLS policies.
    - `supabase/migrations/20260501000003_waitlist_notify_trigger.sql` and `20260501000004_waitlist_trigger_url.sql` — `trg_waitlist_notify` is `AFTER UPDATE OF status ON waitlist_entries`; its body calls `net.http_post` only when `NEW.status = 'notified'`.
    - `supabase/migrations/20260414000004_tabs_and_orders.sql` (lines 1-30) — `tabs` columns and the `customer_name_or_table` CHECK.
    - `supabase/migrations/20260414000009_rls_policies.sql` (lines 21-27) — `get_user_role()` helper, returns the `user_role` enum; the waitlist RLS policies compare it with `IN ('manager', 'admin')`.
    - `supabase/migrations/20260512000001_versioned_rows.sql` — `bump_version_on_update` fires BEFORE UPDATE on `tabs` and `pool_sessions` ONLY. Both RPCs only INSERT into those tables, so no `version` handling is needed. `resources` and `waitlist_entries` carry no version trigger and can be UPDATEd freely.
    - `supabase/migrations/20260728000002_resources_is_temp_floating.sql` — `resources.table_type` is plain `text` with CHECK `IN ('pool','carom','consumption','floating')`.
    - `supabase/migrations/20260728000003_deactivate_floating_resource_trigger.sql` — confirms `resources.is_deleted` exists and that floating resources are soft-deleted on session stop.
    - `src/entities/resource/model/queries.ts` lines 225-280 — the exact 2-step write this RPC replaces (`pool_sessions` insert, then `resources` status + `current_session_id` update).
    - `src/entities/tab/model/queries.ts` lines 437-470 — the exact `tabs` insert shape `useMutationOpenTab` produces (`customer_name`, `table_number`, `staff_id`, `shift_id`, `status`, `notes`, `caja_session_id`).
  </read_first>

  <action>
Create `supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql` containing, in this order inside a single `BEGIN; ... COMMIT;`:

**(a) pg_net (D-08).** `CREATE EXTENSION IF NOT EXISTS pg_net;` as the first statement. Header comment must record that this fixes a pre-existing dependency: `20260501000003`'s header wrongly claims "pg_net is pre-enabled on Supabase hosted", the live project has no `net` schema, and `notify-waitlist` is therefore completely non-functional today. This is NOT a trigger rewrite (D-08 explicitly rejected that).

**(b) `public.start_pool_session(p_table_id uuid, p_tab_id uuid) RETURNS jsonb`** — `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body:
  1. `SELECT r.status::text INTO v_status FROM resources r WHERE r.id = p_table_id AND r.is_deleted = FALSE FOR UPDATE;` — the `::text` cast is required because `resources.status` is the `pool_table_status` enum. `IF NOT FOUND THEN RETURN jsonb_build_object('ok', false, 'code', 'NOT_FOUND'); END IF;`
  2. `IF v_status <> 'available' THEN RETURN jsonb_build_object('ok', false, 'code', 'POOL_TABLE_OCCUPIED'); END IF;`
  3. `INSERT INTO pool_sessions (table_id, tab_id) VALUES (p_table_id, p_tab_id) RETURNING * INTO v_session;` — declare `v_session pool_sessions%ROWTYPE`. Pass `p_tab_id` through verbatim including NULL; the current hook allows `tabId: string | null` and that must not change.
  4. `UPDATE resources SET status = 'occupied', current_session_id = v_session.id WHERE id = p_table_id;`
  5. `RETURN jsonb_build_object('ok', true, 'session', to_jsonb(v_session));`
  Then `GRANT EXECUTE ON FUNCTION public.start_pool_session(uuid, uuid) TO authenticated;`

  Add **no role gate** to this function, and write a comment saying so and why: its only current caller path (`StartSessionSheet` → `start_pool_timer`) is bartender-accessible today, and a `manager+` gate here would silently regress the manual pool flow. This mirrors the explicit "deliberately does NOT add a manager/admin role gate" note in `remove_tab_item`.

  The `FOR UPDATE` lock + `status <> 'available'` guard is a *new* behavior the raw 2-step write did not have. Comment it: it is what makes double-seating a single table impossible, and it means a stale offline-queued `start-pool-timer` replay now fails cleanly with `POOL_TABLE_OCCUPIED` instead of creating a second concurrent session on an occupied table.

**(c) `public.seat_waitlist_party_and_start_session(p_entry_id uuid, p_table_id uuid, p_staff_id uuid, p_shift_id uuid, p_caja_session_id uuid) RETURNS jsonb`** — same modifiers. Body, in this exact order:
  1. Role gate mirroring the RLS policy this `SECURITY DEFINER` function bypasses: `IF get_user_role() NOT IN ('manager', 'admin') THEN RETURN jsonb_build_object('ok', false, 'code', 'AUTH_FORBIDDEN'); END IF;` — use the same comparison form as `waitlist_entries_update_manager` in `20260501000001`. This restores, not adds, an authorization rule; no new RBAC action is introduced (`manage_waitlist` already covers this on the client).
  2. `SELECT * INTO v_entry FROM waitlist_entries WHERE id = p_entry_id FOR UPDATE;` → `IF NOT FOUND` → `NOT_FOUND`. `IF v_entry.status = 'seated' THEN RETURN ... 'DUPLICATE_ENTRY'; END IF;` (double-tap protection on a re-sent seat).
  3. `SELECT r.table_type, r.status::text INTO v_table_type, v_status FROM resources r WHERE r.id = p_table_id AND r.is_deleted = FALSE FOR UPDATE;` → `IF NOT FOUND` → `NOT_FOUND`. `IF v_status <> 'available' THEN RETURN ... 'POOL_TABLE_OCCUPIED'; END IF;` — this second lock is deliberate: taking it *before* any write means the later `start_pool_session` call cannot lose a race, so the only remaining failure modes inside it are genuine exceptions.
  4. `UPDATE waitlist_entries SET status = 'seated', table_id = p_table_id, seated_at = now() WHERE id = p_entry_id;` (same three columns the existing client-side `seatParty` writes).
  5. **`floating` branch (D-05/D-06):** `IF v_table_type = 'floating' THEN RETURN jsonb_build_object('ok', true, 'tab_id', NULL, 'session', NULL); END IF;` — floating tables get zero automation, preserving today's fully-manual behavior for the Phase-26 path.
  6. Tab insert (D-01): `INSERT INTO tabs (customer_name, table_number, staff_id, shift_id, status, notes, caja_session_id) VALUES (v_entry.name || ' (' || v_entry.party_size::text || ')', NULL, p_staff_id, p_shift_id, 'open', NULL, p_caja_session_id) RETURNING id INTO v_tab_id;` — the customer name format is exactly `name`, one space, `(`, party_size, `)`. `table_number` is NULL, matching `StartSessionSheet`'s existing call; the `customer_name_or_table` CHECK is satisfied by `customer_name`.
  7. **`consumption` branch (D-06):** `IF v_table_type = 'consumption' THEN RETURN jsonb_build_object('ok', true, 'tab_id', v_tab_id, 'session', NULL); END IF;` — tab only, no `pool_sessions` row. **Do NOT set `resources.status = 'occupied'` for consumption tables.** Nothing in this codebase ever clears that status without a pool session to stop (`deactivate_floating_resource` fires on session stop only), so writing it would strand consumption tables as permanently occupied. Leaving status untouched preserves exactly today's behavior. Put this reasoning in a comment so it is not "helpfully" added later.
  8. **`pool`/`carom` branch (D-06):** `v_start := start_pool_session(p_table_id, v_tab_id);` then:
     ```
     IF NOT COALESCE((v_start->>'ok')::boolean, false) THEN
       RAISE EXCEPTION 'SEAT_START_SESSION_FAILED:%', COALESCE(v_start->>'code', 'UNKNOWN')
         USING errcode = 'P0S01';
     END IF;
     ```
     **This RAISE is the single most load-bearing line in the migration.** `start_pool_session` signals failure by *returning* `ok:false`, and a plain return from a called plpgsql function does not roll anything back — without this RAISE the tab insert and the waitlist status update would commit while no session exists, which is precisely the non-atomic outcome D-02 rejected. Comment it as such.
     Then `RETURN jsonb_build_object('ok', true, 'tab_id', v_tab_id, 'session', v_start->'session');`
  9. Final fallback `RETURN jsonb_build_object('ok', false, 'code', 'VALIDATION_ERROR');` for an unrecognised `table_type`.
  Then `GRANT EXECUTE ON FUNCTION public.seat_waitlist_party_and_start_session(uuid, uuid, uuid, uuid, uuid) TO authenticated;`

Use only typed `uuid` parameters and static SQL. No `EXECUTE`, no `format()`, no string-concatenated identifiers anywhere in this file (the only concatenation permitted is the D-01 `customer_name` value, which is data, not SQL).

Close with a commented manual DOWN block in the `remove_tab_item` style (REVOKE + DROP FUNCTION for both functions; do **not** include `DROP EXTENSION pg_net` — the pre-existing trigger depends on it).

Then push it: `npx supabase db push` from `bar-pos/`. If `CREATE EXTENSION` is rejected for lack of privilege, fall back to enabling `pg_net` from the Supabase Dashboard → Database → Extensions, then re-run the push; record whichever route was used in the SUMMARY.

Finally, prove the pool happy path against the live project (Supabase MCP `execute_sql` or `psql`), inside an explicit transaction you roll back at the end so no fixture data is left behind:
insert a throwaway `waitlist_entries` row (`name='García'`, `party_size=4`, `status='waiting'`), pick an `available` `pool`-type resource, call `seat_waitlist_party_and_start_session` with real `staff_id`/`shift_id`/`caja_session_id` values read from existing rows, and assert in one query that the entry is `seated`, a `tabs` row exists with `customer_name = 'García (4)'`, and a `pool_sessions` row exists for that table with `tab_id` equal to the returned `tab_id`. Roll back.
  </action>

  <verify>
    <automated>cd bar-pos && grep -v '^--' supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql | grep -cE "USING errcode = 'P0S01'" | grep -qx 1 &amp;&amp; grep -v '^--' supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql | grep -cE "SECURITY DEFINER" | grep -qx 2 &amp;&amp; grep -v '^--' supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql | grep -qE "CREATE EXTENSION IF NOT EXISTS pg_net" &amp;&amp; echo MIGRATION_STRUCTURE_OK</automated>
    <automated>Live DB, via Supabase MCP execute_sql or psql: `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('start_pool_session','seat_waitlist_party_and_start_session');` returns exactly 2 rows, both with `prosecdef = true`. And `SELECT extname FROM pg_extension WHERE extname = 'pg_net';` returns 1 row.</automated>
    <human-check>The rolled-back happy-path transaction described in the action printed a row asserting entry.status='seated', tabs.customer_name='García (4)', and a matching pool_sessions row. Paste that output into the SUMMARY.</human-check>
  </verify>

  <acceptance_criteria>
    - Source assertion: `seat_waitlist_party_and_start_session` contains a `RAISE EXCEPTION ... USING errcode = 'P0S01'` guarding the `start_pool_session` result, and that RAISE is reached whenever `v_start->>'ok'` is not `true`.
    - Source assertion: neither function contains `EXECUTE`, `format(`, or `||` used to build SQL text; the only `||` present builds the D-01 `customer_name` data value.
    - Source assertion: `start_pool_session` contains no `get_user_role()` call; `seat_waitlist_party_and_start_session` contains exactly one, comparing against `('manager', 'admin')`.
    - Source assertion: the consumption branch returns before any `pool_sessions` INSERT and contains no `UPDATE resources SET status`.
    - Behavior assertion (live DB): calling `seat_waitlist_party_and_start_session` for a `waiting` entry and an `available` `pool` resource returns `ok:true` with non-null `tab_id` and non-null `session`, and the created tab's `customer_name` equals `<entry name> (<party_size>)` character for character.
    - CLI: `SELECT proname, prosecdef FROM pg_proc WHERE proname IN ('start_pool_session','seat_waitlist_party_and_start_session')` returns 2 rows with `prosecdef = true`.
    - CLI: `SELECT extname FROM pg_extension WHERE extname='pg_net'` returns 1 row.
  </acceptance_criteria>

  <verify-gate>Both live-DB queries above must return the stated row counts before this task is considered done. A migration that is authored but not pushed does not satisfy this task — plans 07-10 and 07-11 call these functions.</verify-gate>

  <done>The migration file exists, is pushed to the live Supabase project, both functions are present as `SECURITY DEFINER`, `pg_net` is enabled, and the pool happy path has been demonstrated end-to-end against real rows in a rolled-back transaction.</done>

  <reversibility rating="costly">Creating the functions is a plain migration and droppable, but `CREATE EXTENSION pg_net` and the pushed functions land on the live production project; undoing requires a coordinated follow-up migration. Nothing depends on them yet at this point in the wave, which is why the one-way gate sits in 07-10, not here.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Regenerate Supabase types so 07-10/07-11 can call the RPCs without a cast, and smoke-test the pg_net notify path</name>

  <files>src/shared/lib/supabase.types.ts</files>

  <read_first>
    - `CLAUDE.md` — "Missing generated types workaround" section (the `const db = supabase as any` + file-level eslint-disable fallback) and "Generated files — never edit manually".
    - `src/shared/lib/supabase.types.ts` — confirm `stop_pool_session` is already present in the `Functions` block; that is the precedent proving this file is kept in sync with pushed RPCs.
    - `src/features/notify-waitlist/model/useNotifyWaitlist.ts` — the plain `UPDATE waitlist_entries SET status='notified'` that is currently failing outright because the `net` schema does not exist.
    - `.planning/todos/pending/2026-08-04-notify-waitlist-fails-pg-net-schema-missing.md` — the folded todo this closes.
  </read_first>

  <action>
Run `npx supabase gen types typescript` per CLAUDE.md and write the result to `src/shared/lib/supabase.types.ts`, so `start_pool_session` and `seat_waitlist_party_and_start_session` appear in the generated `Functions` block and downstream plans can call `supabase.rpc(...)` with full typing. Confirm `npm run typecheck` still passes after the regeneration — if unrelated schema drift appears in the diff, keep it (the file is generated, not hand-edited) but call it out in the SUMMARY.

If type generation cannot run (project not linked, missing credentials, CLI failure), do NOT hand-edit the file. Instead record in the SUMMARY that 07-10 and 07-11 must use the documented `const db = supabase as any` + file-level eslint-disable workaround, and leave `supabase.types.ts` untouched.

Then smoke-test D-08 against the live project inside a rolled-back transaction: `UPDATE waitlist_entries SET status = 'notified' WHERE id = <a throwaway waiting entry>;`. Before this plan that statement aborted with `schema "net" does not exist`, because `trg_waitlist_notify` fires `AFTER UPDATE OF status` and a trigger exception rolls back the whole statement. It must now succeed. Also confirm `net.http_post` is resolvable: `SELECT 1 FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname = 'net' AND p.proname = 'http_post';` returns at least one row.
  </action>

  <verify>
    <automated>cd bar-pos &amp;&amp; npm run typecheck</automated>
    <automated>Live DB: `SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname='net' AND p.proname='http_post';` returns &gt;= 1.</automated>
    <automated>cd bar-pos &amp;&amp; grep -q "start_pool_session" src/shared/lib/supabase.types.ts &amp;&amp; grep -q "seat_waitlist_party_and_start_session" src/shared/lib/supabase.types.ts &amp;&amp; echo TYPES_REGENERATED || echo TYPES_FALLBACK_TO_ANY_CAST_RECORD_IN_SUMMARY</automated>
  </verify>

  <acceptance_criteria>
    - CLI: `npm run typecheck` exits 0.
    - Behavior assertion (live DB): an `UPDATE waitlist_entries SET status='notified'` on a throwaway `waiting` entry completes without raising `schema "net" does not exist`; the transaction is rolled back afterward.
    - CLI: the `net.http_post` lookup query returns at least one row.
    - Source assertion: either `supabase.types.ts` contains both new function names, OR the SUMMARY explicitly records that regeneration was unavailable and the `as any` fallback applies to 07-10/07-11. One of the two must be true — a silent omission is a failure.
    - `src/shared/lib/supabase.types.ts` was produced by the generator, not hand-edited (the diff contains no manually-authored entries).
  </acceptance_criteria>

  <done>`supabase.types.ts` is regenerated (or the fallback is explicitly recorded), typecheck is green, and the previously-broken notify UPDATE now succeeds against the live DB.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the D-06 branch and D-02 rollback against the live database</name>

  <files>(none — live-database verification only; this task writes no repository files and must leave no fixture rows behind)</files>

  <read_first>
    - `supabase/migrations/20260807000001_pool_session_atomic_rpcs.sql` — the file authored in task 1; the branch order and return shapes being asserted here.
    - `.planning/phases/07-waitlist-whatsapp/07-CONTEXT.md` — D-02 (atomic, not sequential-best-effort) and D-06 (the four-value `table_type` branch) are the decisions this task exists to prove.
  </read_first>

  <action>
Run four scenarios against the live project via Supabase MCP `execute_sql` or `psql`, each inside its own explicit transaction that you ROLL BACK at the end so no fixture rows persist. For each, create a throwaway `waitlist_entries` row and use an `available` resource of the required `table_type` (create a throwaway resource row of the needed type inside the same rolled-back transaction if none is available):

1. **consumption → tab only (D-06).** Assert the return is `ok:true` with a non-null `tab_id` and `session` exactly `null`; assert `SELECT count(*) FROM pool_sessions WHERE table_id = <id>` is unchanged; assert `SELECT status FROM resources WHERE id = <id>` is still `available` (the deliberate no-status-write decision from task 1); assert the entry is `seated`.
2. **carom → tab + session (D-06).** Assert `ok:true`, non-null `tab_id`, non-null `session`, a new `pool_sessions` row for that table, and `resources.status = 'occupied'` with `current_session_id` set. Carom is treated identically to pool — this scenario is what confirms that, closing the open discretion item from 07-CONTEXT.md.
3. **floating → neither (D-05/D-06).** Assert `ok:true` with `tab_id` null AND `session` null, no new `tabs` row, no new `pool_sessions` row, and the entry still flipped to `seated`.
4. **forced failure → full rollback (D-02).** Call the RPC for a `pool` table with a `p_staff_id` that does not exist in `profiles`, so the `tabs` insert raises a foreign-key violation. Assert the call errors, then in a fresh transaction assert `waitlist_entries.status` for that entry is still `waiting`, `table_id` is still NULL, and no `tabs`/`pool_sessions` rows were created. This is the direct proof of D-02; if any of those rows survive, the RAISE guard from task 1 is missing or misplaced and task 1 must be fixed before this plan can close.

Record every scenario's assertion output verbatim in the SUMMARY.
  </action>

  <verify>
    <automated>Live DB, scenario 4: after the forced-failure call, `SELECT status, table_id FROM waitlist_entries WHERE id = &lt;entry&gt;;` returns `waiting, NULL`.</automated>
    <automated>Live DB, scenario 1: the RPC return jsonb satisfies `(r-&gt;&gt;'ok')::boolean = true AND r-&gt;'session' = 'null'::jsonb AND r-&gt;&gt;'tab_id' IS NOT NULL`.</automated>
    <automated>Live DB, scenario 3: the RPC return jsonb satisfies `(r-&gt;&gt;'ok')::boolean = true AND r-&gt;'session' = 'null'::jsonb AND r-&gt;'tab_id' = 'null'::jsonb`.</automated>
  </verify>

  <acceptance_criteria>
    - Behavior assertion: consumption-type seat produces a tab, no `pool_sessions` row, and leaves `resources.status = 'available'`.
    - Behavior assertion: carom-type seat produces a tab AND a `pool_sessions` row and sets `resources.status = 'occupied'` — identical to pool.
    - Behavior assertion: floating-type seat produces neither a tab nor a session, while still marking the entry `seated`.
    - Behavior assertion: a forced tab-insert failure leaves `waitlist_entries.status = 'waiting'`, `table_id IS NULL`, and zero new `tabs`/`pool_sessions` rows.
    - Every scenario ran inside a transaction that was rolled back; a post-run `SELECT count(*) FROM waitlist_entries WHERE name LIKE '<fixture prefix>%'` returns 0.
  </acceptance_criteria>

  <done>All four scenarios pass with their assertions pasted into the SUMMARY, and no fixture rows remain in the live database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client → Postgres RPC | Any signed-in staff terminal can `supabase.rpc(...)` these functions with arbitrary uuid arguments. |
| RPC body → `SECURITY DEFINER` execution context | Both functions run as their owner, bypassing RLS on `waitlist_entries`, `resources`, `tabs`, and `pool_sessions`. |
| Postgres → outbound HTTP (`net.http_post`) | `pg_net` gives the database the ability to make outbound HTTP calls, used by the pre-existing `trg_waitlist_notify`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0709-01 | Tampering | `start_pool_session`, `seat_waitlist_party_and_start_session` | high | mitigate | All five/two parameters typed `uuid`; zero `EXECUTE`/`format()`/identifier concatenation in either body; `SET search_path = public` pins schema resolution so a rogue temp-schema object cannot shadow `resources`/`tabs`. Enforced by a task-1 source assertion. |
| T-0709-02 | Elevation of Privilege | `seat_waitlist_party_and_start_session` bypassing the `waitlist_entries_update_manager` RLS policy | high | mitigate | The function re-asserts the bypassed policy in its own body via `get_user_role() NOT IN ('manager','admin') → AUTH_FORBIDDEN`, as its first statement before any read or write. Without this a bartender could seat parties and open tabs, which the RLS policy forbids today. |
| T-0709-03 | Elevation of Privilege | `start_pool_session` granted to `authenticated` with no role gate | low | accept | Deliberate. A bartender can already INSERT `pool_sessions` and UPDATE `resources` directly under current RLS (the manual `StartSessionSheet` flow is bartender-accessible via `start_pool_timer`). Adding a gate here would regress an existing, intended capability. Matches the documented precedent in `remove_tab_item`. |
| T-0709-04 | Repudiation | Auto-created tabs carry no audit trail distinguishing them from manually-opened tabs | low | accept | `tabs.staff_id` records who seated the party, and D-01's `"{name} ({party_size})"` naming makes waitlist-originated tabs self-identifying. No `record_audit` call was requested by any decision; adding one is out of scope for this scope addition. |
| T-0709-05 | Denial of Service | `FOR UPDATE` locks held on `resources` and `waitlist_entries` for the transaction duration | low | accept | Both transactions are short (three INSERT/UPDATEs, no network I/O inside the lock). Lock scope is a single resource row and a single waitlist row, matching the existing `stop_pool_session` locking pattern. |
| T-0709-06 | Information Disclosure | Outbound HTTP capability from the database via `pg_net` | medium | accept | Not new attack surface: `trg_waitlist_notify` has assumed `net.http_post` since migration `20260501000003` and is currently broken solely because the extension was never enabled. The target URL and key are hardcoded in `20260501000004` and unchanged by this plan; D-08 explicitly scoped this as "enable the extension", not "redesign the notification mechanism". |
| T-0709-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs in this plan — SQL migration and generated-types regeneration only. No `## Package Legitimacy Audit` is required. |
</threat_model>

<verification>
- `npm run typecheck` and `npm run lint` are green (no source files outside the generated types file changed).
- Both functions are live and `SECURITY DEFINER` on the Supabase project.
- `pg_net` is enabled and `net.http_post` resolves.
- Pool, carom, consumption, and floating branches each behave per D-06, verified against real rows.
- A forced mid-transaction failure leaves zero partial writes (D-02).
- No fixture rows remain in the live database after verification.
</verification>

<success_criteria>
Plans 07-10 and 07-11 can be executed without any further database work: both RPCs exist, are typed (or the `as any` fallback is explicitly recorded), and their success/failure contracts have been demonstrated against real data.
</success_criteria>

<output>
Create `.planning/phases/07-waitlist-whatsapp/07-09-SUMMARY.md` when done. It MUST record: the migration filename, the exact final signatures of both functions, whether type regeneration succeeded or the `as any` fallback applies, the route used to enable `pg_net`, and the verbatim assertion output from all four task-3 scenarios.
</output>
