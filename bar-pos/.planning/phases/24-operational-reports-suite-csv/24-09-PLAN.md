---
phase: 24-operational-reports-suite-csv
plan: 09
type: execute
wave: 5
depends_on: [24-06, 24-02]
files_modified:
  - src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx
  - src/widgets/ModifierPopularityReport/index.ts
  - src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx
  - src/widgets/PaymentMethodsReport/index.ts
  - src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "ModifierPopularityReport renders a Recharts horizontal BarChart sorted by attach-count desc with the top row/bar emerald-500 and the rest grayscale, plus a table capped to top-20 with the top20Note caption; full uncapped rows still flow to ExportButtons (D-09/D-10/D-15)"
    - "PaymentMethodsReport renders a Recharts donut (innerRadius set) with the leading method slice emerald-500, plus a two-grain table (per-session rows + a day rollup row pinned bottom via border-t-2) (D-08/D-14)"
    - "HourlyBreakdownPanel renders a Recharts BarChart (one bar per hour, busiest bar emerald-500) and gains a day-of-week column, reading the migrated RPC-backed hook (D-03/D-13)"
    - "Each chart copies ComboMixReport's CHART_COLORS + chartColor() verbatim (no shared util), wraps the chart in rounded-lg border p-4, includes Tooltip + Legend, height 300 (280 acceptable for the horizontal bar)"
    - "Exactly one accent-colored (emerald-500) element per chart/table"
  artifacts:
    - src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx
    - src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx
  key_links:
    - "ModifierPopularityReport → useModifierPopularityReport (Plan 06); slice(0,20) for table only, full rows to ExportButtons (D-10)"
    - "PaymentMethodsReport → usePaymentMethodsReport; is_rollup row pinned bottom"
    - "HourlyBreakdownPanel → migrated useHourlyBreakdown + findPeakHour highlight"
---

<objective>
Build the three chart-bearing report widgets (D-13/D-14/D-15). Two are new
(`ModifierPopularityReport` horizontal bar + capped table; `PaymentMethodsReport` donut +
two-grain table) and one extends an existing widget in place (`HourlyBreakdownPanel` gains a
per-hour bar chart with the busiest bar highlighted + a day-of-week column, now reading the
migrated RPC hook).

Purpose: SC-3 (Recharts widgets render each report). All three copy `ComboMixReport`'s chart
conventions verbatim — CHART_COLORS + chartColor() helper, the rounded-lg border chart
container, Tooltip + Legend, and the single-accent (emerald-500) top-element rule. The two
new widgets also carry their own tables (modifier: top-20 cap on screen, full data to
export per D-10; payment: per-session rows + a pinned day-rollup row per D-08).
Output: 2 new widget folders + HourlyBreakdownPanel extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-operational-reports-suite-csv/24-CONTEXT.md
@.planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md
@.planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md
@src/widgets/ComboMixReport/ComboMixReport.tsx
@src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx
</context>

<artifacts_produced>
- `ModifierPopularityReport` widget (+ barrel) — horizontal BarChart + top-20 table, reportType `modifier-popularity`
- `PaymentMethodsReport` widget (+ barrel) — donut + two-grain table, reportType `payment-methods`
- `HourlyBreakdownPanel` — extended with a per-hour bar chart + day-of-week column
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: ModifierPopularityReport (horizontal bar + top-20 table, D-09/D-10/D-15)</name>
  <files>src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx, src/widgets/ModifierPopularityReport/index.ts</files>
  <read_first>
    - src/widgets/ComboMixReport/ComboMixReport.tsx (whole file — CHART_COLORS/chartColor lines ~18-31, top-row highlight ~63-65/100-108, chart container + Recharts wiring ~68-87, ExportButtons ~69-74)
    - .planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md ("ModifierPopularityReport / PaymentMethodsReport" section lines ~231-285, esp. the cap-to-20 note ~283)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Chart Contract + Report Tab Inventory (modifier-popularity row) + Copywriting Contract (modifierPopularityReport.* keys)
  </read_first>
  <action>Create ModifierPopularityReport({ dateRange }) using `useModifierPopularityReport(dateRange.from, dateRange.to)`. Copy ComboMixReport's CHART_COLORS + chartColor() verbatim. Sort rows by attachCount desc; render a Recharts horizontal BarChart (`layout="vertical"`, height 280 acceptable, wrapped in `rounded-lg border p-4`, Tooltip + Legend) with the top bar emerald-500 and the rest grayscale. Render a table of `sorted.slice(0, 20)` ONLY for the on-screen view (D-10), with the top row emerald-accented and a caption `t('wAdmin:modifierPopularityReport.top20Note')` below it; numeric cells tabular-nums. Pass the FULL uncapped `rows` to `ExportButtons reportType='modifier-popularity' data={{ rows, dateRange }}` (export stays uncapped). EmptyState via the modifierPopularityReport empty keys. Root `<div className="space-y-4">`, index.ts barrel.</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/widgets/ModifierPopularityReport/ModifierPopularityReport.tsx','utf8');const ok=s.includes('useModifierPopularityReport')&&s.includes('slice(0, 20)')&&s.includes('CHART_COLORS')&&s.includes(\"reportType=\")&&s.includes('rounded-lg border p-4');console.log(ok?'ok':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Widget renders a horizontal Recharts BarChart with Tooltip + Legend inside `rounded-lg border p-4`
    - On-screen table is capped via `slice(0, 20)`; ExportButtons receives the full uncapped rows
    - Exactly one emerald-500 accent element (top bar / top row); CHART_COLORS copied verbatim
    - `npm run typecheck` + `npm run lint` green
  </acceptance_criteria>
  <done>Modifier popularity ranks modifiers with a top-20 chart+table and uncapped export.</done>
</task>

<task type="auto">
  <name>Task 2: PaymentMethodsReport (donut + two-grain table, D-08/D-14)</name>
  <files>src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx, src/widgets/PaymentMethodsReport/index.ts</files>
  <read_first>
    - src/widgets/ComboMixReport/ComboMixReport.tsx (CHART_COLORS/chartColor + chart container + ExportButtons)
    - src/widgets/CajaReportPanel (the per-session grouping presentation to mirror for the per-session grain)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Chart Contract (donut innerRadius, leading slice emerald) + Report Tab Inventory (payment-methods two-grain + rollup row pinned via border-t-2) + Copywriting Contract (paymentMethodsReport.* keys)
    - src/shared/lib/domain.ts PaymentMethodRowSchema (isRollup flag)
  </read_first>
  <action>Create PaymentMethodsReport({ dateRange }) using `usePaymentMethodsReport(dateRange.from, dateRange.to)`. Copy CHART_COLORS + chartColor() verbatim. Render a Recharts PieChart with `innerRadius` set (true donut, height 300, `rounded-lg border p-4`, Tooltip + Legend), slice colors from CHART_COLORS with the leading method slice emerald-500. Render ONE table (D-08) with per-caja-session rows grouped like CajaReportPanel PLUS the day-level rollup row (the `isRollup` row) pinned to the bottom via `border-t-2`; tabular-nums numeric cells. ExportButtons reportType='payment-methods' data={{ rows, dateRange }}. EmptyState via paymentMethodsReport empty keys. Root `<div className="space-y-4">`, index.ts barrel.</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/widgets/PaymentMethodsReport/PaymentMethodsReport.tsx','utf8');const ok=s.includes('usePaymentMethodsReport')&&s.includes('innerRadius')&&s.includes('border-t-2')&&s.includes(\"reportType=\")&&s.includes('CHART_COLORS');console.log(ok?'ok':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Widget renders a Recharts donut (innerRadius) with Tooltip + Legend, leading slice emerald-500
    - One two-grain table with the rollup row pinned bottom (border-t-2)
    - ExportButtons reportType='payment-methods' present
    - `npm run typecheck` + `npm run lint` green
  </acceptance_criteria>
  <done>Payment methods shows share-by-method donut + per-session/rollup table in one export.</done>
</task>

<task type="auto">
  <name>Task 3: Extend HourlyBreakdownPanel — bar chart + day-of-week column (D-03/D-13)</name>
  <files>src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx</files>
  <read_first>
    - src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx (current table-based widget + how it consumes useHourlyBreakdown + the existing peak/slowest highlight)
    - src/widgets/ComboMixReport/ComboMixReport.tsx (CHART_COLORS + chart container + Recharts wiring to add)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Report Tab Inventory (hourly row: BarChart one bar per hour, busiest bar emerald-500; table gains day-of-week column) + Chart Contract
    - src/entities/tab/model/queries-reports.ts findPeakHour (client-side busiest derivation, retained in Plan 06)
  </read_first>
  <action>Extend HourlyBreakdownPanel in place: add a Recharts BarChart (one bar per hour, wrapped in `rounded-lg border p-4`, Tooltip + Legend, height 300) with the busiest hour's bar emerald-500 (derive busiest via the existing findPeakHour helper) and all other bars grayscale (`var(--chart-2)`), and add a day-of-week column to the existing 24-row table (the migrated useHourlyBreakdown now returns dayOfWeek per row, D-03). Copy CHART_COLORS + chartColor() verbatim. Keep the existing peak/slowest row highlighting. Do not rename or split the widget (D-04 — extend in place).</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/widgets/HourlyBreakdownPanel/HourlyBreakdownPanel.tsx','utf8');const ok=(s.includes('BarChart')||s.includes('Bar '))&&(s.includes('dayOfWeek')||s.includes('dow'))&&s.includes('rounded-lg border p-4');console.log(ok?'ok':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - HourlyBreakdownPanel renders a Recharts bar chart with the busiest bar emerald-500 (single accent)
    - The table gains a day-of-week column reading the RPC-backed hook
    - `npm run typecheck` + `npm run lint` green; existing hourly export/test not regressed
  </acceptance_criteria>
  <done>Peak-hours reads as genuine peak analysis: bar chart + busiest highlight + day-of-week.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → report hook | Widgets render only what the (view_reports-gated) report hooks return |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-09-I | Information Disclosure | payment-methods widget shows revenue-by-method | low | accept | Rendered only inside `/reports` (view_reports / manager+ gated); no cross-role widening (client transform of gated data). Same posture as T-24-03-I. |
| T-24-09-M | Misrepresentation | uncapped export vs top-20 on-screen could mislead | low | mitigate | The `top20Note` caption states the on-screen table is capped and export contains all rows (D-10) — no silent truncation. |
| T-24-09-SC | Tampering | npm installs | low | accept | No new packages (recharts already installed). |
</threat_model>

<verification>
- 3 chart widgets typecheck + lint clean; each has Tooltip + Legend, one emerald accent
- Modifier export uncapped; payment rollup pinned; hourly gains chart + dow column
</verification>

<success_criteria>
- ModifierPopularityReport + PaymentMethodsReport render with charts (SC-3 / D-14 / D-15)
- HourlyBreakdownPanel extended with bar chart + day-of-week (SC-3 / D-13 / D-03)
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-09-SUMMARY.md` when done
</output>
