---
phase: 24-operational-reports-suite-csv
plan: 10
type: execute
wave: 6
depends_on: [24-07, 24-08, 24-09]
files_modified:
  - src/pages/reports/index.tsx
  - e2e/07-reports.spec.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "ReportsPage's Tabs list gains 4 new TabsTrigger/TabsContent pairs (deletions-pre, deletions-post, modifier-popularity, payment-methods) inserted after the voids tab, each following the DateRangePicker + Widget(dateRange) shape"
    - "Each new tab mounts its widget with dateRange; labels use the Plan-01 pages:reports.tabs.* keys"
    - "An E2E test proves the new tabs render, a CSV export writes a file, and reason-required removal works as a bartender without a PIN gate"
    - "CLAUDE.md Implemented Features + E2E suite list reflect phase 24"
    - "Full phase gate green: typecheck (no new errors), lint, unit tests, and the reports E2E"
  artifacts:
    - src/pages/reports/index.tsx
    - e2e/07-reports.spec.ts
  key_links:
    - "reports/index.tsx new TabsContent → DeletionsPreSendPanel/DeletionsPostCloseReport/ModifierPopularityReport/PaymentMethodsReport (Plans 08/09)"
    - "reports/index.tsx tab labels → pages:reports.tabs.* keys (Plan 01)"
---

<objective>
Wire the four new report widgets into the Reports page, prove the phase end-to-end, and
update the project docs. `ReportsPage` gains four `TabsTrigger`/`TabsContent` pairs
(inserted right after `voids` to keep risk-adjacent reports grouped, per UI-SPEC), each
following the existing date-ranged tab shape. An E2E spec exercises the new tabs + CSV
export + the bartender reason-required removal, then the full phase gate runs.

Purpose: Closes SC-1..SC-4 at the UI/E2E layer — this is the phase's final integration and
verification plan. It mounts the Plan-08/09 widgets, confirms the Plan-02 CSV export works
end-to-end, and confirms the Plan-04/07 removal audit path works as a bartender (no PIN
gate). CLAUDE.md is updated so the new RPCs/widgets/reportTypes are documented.
Output: 4 wired tabs, extended E2E spec, updated CLAUDE.md, green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-operational-reports-suite-csv/24-CONTEXT.md
@.planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md
@src/pages/reports/index.tsx
@e2e/07-reports.spec.ts
</context>

<artifacts_produced>
- 4 new report tabs on `/reports` (deletions-pre, deletions-post, modifier-popularity, payment-methods)
- Extended reports E2E coverage (new tabs render + CSV export + bartender reason-required removal)
- CLAUDE.md updated (Implemented Features, RPC/widget inventory, E2E suite list)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Wire 4 new report tabs into ReportsPage</name>
  <files>src/pages/reports/index.tsx</files>
  <read_first>
    - src/pages/reports/index.tsx (the Tabs/TabsList/TabsTrigger pattern + the per-tab TabsContent `<div className="space-y-4"><DateRangePicker/><Widget dateRange={dateRange}/></div>` shape; note the voids tab at line ~58/88 — insert after it)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Report Tab Inventory (tab values + label keys + insertion order: after voids, before categories)
    - src/widgets/DeletionsPreSendPanel/index.ts, DeletionsPostCloseReport/index.ts, ModifierPopularityReport/index.ts, PaymentMethodsReport/index.ts (the barrels to import)
  </read_first>
  <action>Import the 4 new widgets and add 4 TabsTrigger entries (values `deletions-pre`, `deletions-post`, `modifier-popularity`, `payment-methods`; labels `t('reports.tabs.deletionsPre')` etc.) inserted immediately after the `voids` trigger, and 4 matching TabsContent blocks each following the standard `<div className="space-y-4"><DateRangePicker fromStr toStr onChange={handleDateChange}/><Widget dateRange={dateRange}/></div>` shape. Do not reorder existing tabs.</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/pages/reports/index.tsx','utf8');const need=['deletions-pre','deletions-post','modifier-popularity','payment-methods','DeletionsPreSendPanel','PaymentMethodsReport'];const miss=need.filter(t=>!s.includes(t));console.log(miss.length?'MISSING '+miss.join(','):'ok');process.exit(miss.length?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - 4 new TabsTrigger + 4 new TabsContent present, values deletions-pre/deletions-post/modifier-popularity/payment-methods
    - Each new tab uses DateRangePicker + its widget with dateRange
    - `npm run typecheck` + `npm run lint` green (labels via t(), no literals)
  </acceptance_criteria>
  <done>All four new reports are reachable tabs on `/reports`.</done>
</task>

<task type="auto">
  <name>Task 2: Reports E2E — new tabs render + CSV export + bartender reason-required removal</name>
  <files>e2e/07-reports.spec.ts</files>
  <read_first>
    - e2e/07-reports.spec.ts (existing reports E2E structure, loginAs helper usage) — if a dedicated analytics spec e2e/37-analytics-reports.spec.ts exists, extend whichever spec is the natural home; confirm which exists before writing
    - e2e/helpers/auth.ts (loginAs(page, 'admin'|'bartender'); admin PIN 0000)
    - .planning/phases/24-operational-reports-suite-csv/24-RESEARCH.md Validation Architecture (SC-3 e2e extension point) + Pitfall 3 (bartender removal must NOT hit AUTH_FORBIDDEN)
  </read_first>
  <action>Add E2E coverage (in 07-reports.spec.ts or the existing analytics spec): (a) as admin/manager, open each of the 4 new tabs and assert the widget renders (chart or DataTable + the deletions-pre historical-gap Alert visible); (b) trigger a CSV export from one report's ExportButtons dropdown and assert a .csv file is produced via the save() flow (mirror any existing export assertion in the spec); (c) as a BARTENDER, remove an order item through RemoveTabItemDialog with a reason and assert it succeeds (no PIN dialog, no AUTH_FORBIDDEN) and the deletions-pre report then shows the removal. Register a new spec filename in CLAUDE.md's E2E list only if you create a new file rather than extending 07.</action>
  <verify>
    <automated>cd bar-pos && npx playwright test e2e/07-reports.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The 4 new tabs render under an authorized role in the E2E run
    - A CSV export assertion passes (file written via save())
    - Bartender reason-required removal passes with no PIN gate and no AUTH_FORBIDDEN, and the removal appears in the deletions-pre report
  </acceptance_criteria>
  <done>The phase's user-facing behavior is proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md + run the full phase gate</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Implemented Features list; Key DB Tables; E2E Test Suite list; RBAC/reports sections — mirror how Phase 23's reopen-tab entry was added)
    - .planning/phases/24-operational-reports-suite-csv/24-CONTEXT.md (decision summary for the feature-entry wording)
  </read_first>
  <action>Add a concise Implemented Features entry summarizing phase 24: the 6 report RPCs (peak-hours/voids migrated to server-side + deletions ×2 + modifier-popularity + payment-methods), the atomic audited `remove_tab_item` RPC with `order_item.remove` audit + required reason, the generic `rowsToCsv` CSV export wired to all 17 report tabs, and the Recharts widgets. Note that "charts-data" is served by the chart-bearing report RPCs (no separate endpoint). Update the E2E suite list if a new spec file was created. Then run the full phase gate.</action>
  <verify>
    <automated>cd bar-pos && npm run typecheck; npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md documents the phase-24 RPCs, remove_tab_item + order_item.remove, CSV export, and Recharts widgets
    - `npm run typecheck` shows no new errors beyond the 2 pre-existing documented ones
    - `npm run lint` clean; `npm run test` green (no new failures)
  </acceptance_criteria>
  <done>Phase 24 is documented and the full gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → /reports page | `/reports` is gated by `ReportsRoute` (view_reports, manager+); the report tabs render there |
| bartender → remove_tab_item | Bartender can remove items (unchanged access), now with reason + audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-10-E | Elevation of Privilege | regression that adds a PIN gate to removal | medium | mitigate | The E2E bartender-removal test (Task 2) asserts removal still works without a PIN gate and without AUTH_FORBIDDEN, catching any accidental gate. |
| T-24-10-I | Information Disclosure | new report tabs on /reports | low | accept | All new tabs live under the existing `ReportsRoute` (view_reports / manager+) guard — no new unguarded surface. |
| T-24-10-SC | Tampering | npm installs | low | accept | No new packages this phase. |
</threat_model>

<verification>
- 4 new tabs wired and typecheck-clean
- Reports E2E green (new tabs + CSV export + bartender reason removal)
- CLAUDE.md updated; full gate (typecheck/lint/test) green
</verification>

<success_criteria>
- All 4 new report tabs reachable on /reports (SC-1 / SC-3)
- CSV export proven end-to-end (SC-2)
- Bounded RPC-backed reports render for a full day's data (SC-4)
- Phase documented + gate green
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-10-SUMMARY.md` when done
</output>
