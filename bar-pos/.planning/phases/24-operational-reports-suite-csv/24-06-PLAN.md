---
phase: 24-operational-reports-suite-csv
plan: 06
type: execute
wave: 4
depends_on: [24-05, 24-01]
files_modified:
  - src/entities/tab/model/queries-reports.ts
  - src/entities/tab/model/deletions-pre-report.integration.test.ts
  - src/entities/tab/model/deletions-post-report.integration.test.ts
  - src/entities/tab/model/modifier-popularity-report.integration.test.ts
  - src/entities/tab/model/payment-methods-report.integration.test.ts
autonomous: true
requirements: [SC-1, SC-4]
must_haves:
  truths:
    - "useHourlyBreakdown's query body calls db.rpc('get_peak_hours_report', {p_from, p_to}) instead of the unbounded order_items join; busiest hour still derived client-side via the retained findPeakHour helper (D-03)"
    - "useVoidRefundReport's query body calls db.rpc('get_voids_report', ...) returning the unchanged VoidRefundRow shape (D-01/D-02)"
    - "useDeletionsPreReport, useDeletionsPostReport, useModifierPopularityReport, usePaymentMethodsReport each call their RPC and Zod-parse rows into the Plan-01 row types"
    - "The pure helpers findPeakHour/aggregateHourlyRevenue/fillMissingHours are NOT deleted (still used by tests / client highlight)"
    - "Each of the 4 new report hooks has a live integration test asserting correctly-shaped rows for a seeded date range"
  artifacts:
    - src/entities/tab/model/queries-reports.ts
  key_links:
    - "use*Report hooks → RPC json {ok, rows} → Zod row parse → report widgets (Plans 08/09)"
    - "useHourlyBreakdown findPeakHour(rows) client-side → HourlyBreakdownPanel busiest highlight (Plan 09)"
---

<objective>
Wire the client server-state layer to the new RPCs: migrate the two existing report hook
bodies (hourly→peak-hours, voids) from unbounded client-side queries to `db.rpc(...)`
calls, and add the four new report hooks (deletions-pre, deletions-post,
modifier-popularity, payment-methods), each Zod-parsing rows into the Plan-01 row types.

Purpose: SC-1 (hooks consume the RPCs) + SC-4 (client no longer runs unbounded report
queries). Per RESEARCH, keep the pure helpers (`findPeakHour`, `aggregateHourlyRevenue`,
`fillMissingHours`) — only swap the live query bodies; the client keeps deriving "busiest"
via `findPeakHour` so there's one less server/client thing to keep in sync. Each new hook
follows the `useCajaReport` Pattern 2 shape (useQuery → db.rpc → unwrap `{ok, rows}` →
`RowSchema.parse(mapSnakeToCamel(r))`).
Output: 2 migrated hook bodies, 4 new hooks, 4 live integration test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-operational-reports-suite-csv/24-RESEARCH.md
@.planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md
@src/entities/tab/model/queries-reports.ts
</context>

<artifacts_produced>
Hooks this plan creates/changes (consumed by widget plans 08/09):
- `useHourlyBreakdown` — body migrated to `get_peak_hours_report`
- `useVoidRefundReport` — body migrated to `get_voids_report`
- `useDeletionsPreReport(from, to)`, `useDeletionsPostReport(from, to)`, `useModifierPopularityReport(from, to)`, `usePaymentMethodsReport(from, to)` (new)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate useHourlyBreakdown + useVoidRefundReport bodies to db.rpc (D-01/D-02/D-03)</name>
  <files>src/entities/tab/model/queries-reports.ts</files>
  <read_first>
    - src/entities/tab/model/queries-reports.ts (useHourlyBreakdown lines ~291-322, useVoidRefundReport ~346-373, and findPeakHour/aggregateHourlyRevenue/fillMissingHours pure helpers ~119-124)
    - .planning/phases/24-operational-reports-suite-csv/24-RESEARCH.md Pattern 2 (useCajaReport db.rpc shape) + State-of-the-Art note ("do not delete the pure helpers")
    - src/entities/caja/model/queries.ts useCajaReport (the db.rpc + Result unwrap reference)
  </read_first>
  <action>Swap `useHourlyBreakdown`'s query body to call `db.rpc('get_peak_hours_report', { p_from: from.toISOString(), p_to: to.toISOString() })`, unwrap `{ok, rows}`, parse rows into the extended HourlyRow shape, and keep deriving busiest via the existing `findPeakHour` helper (D-03). Swap `useVoidRefundReport`'s body to `db.rpc('get_voids_report', ...)` returning the unchanged VoidRefundRow shape (D-01/D-02). Do NOT delete `findPeakHour`/`aggregateHourlyRevenue`/`fillMissingHours` — they remain for client highlight + tests. Keep the same queryKey/staleTime conventions the other report hooks use.</action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/entities/tab/model/queries-reports.test.ts 2>/dev/null; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_peak_hours_report" src/entities/tab/model/queries-reports.ts` >= 1 and same for `get_voids_report`
    - `findPeakHour` still exported (grep >= 1)
    - `npm run typecheck` shows no new errors
  </acceptance_criteria>
  <done>The two migrated reports now read bounded RPCs; pure helpers retained.</done>
</task>

<task type="auto">
  <name>Task 2: Add deletions-pre + deletions-post hooks + live integration tests</name>
  <files>src/entities/tab/model/queries-reports.ts, src/entities/tab/model/deletions-pre-report.integration.test.ts, src/entities/tab/model/deletions-post-report.integration.test.ts</files>
  <read_first>
    - src/entities/tab/model/queries-reports.ts (existing hook shape to copy)
    - src/entities/tab/model/void-refund-report.integration.test.ts (mirror this integration test's seeding + assertion structure — this is the canonical live-RPC test pattern)
    - src/shared/lib/domain.ts DeletionsPreRowSchema / DeletionsPostRowSchema (Plan 01)
  </read_first>
  <action>Add `useDeletionsPreReport(from, to)` (db.rpc('get_deletions_pre_report',...), parse into DeletionsPreRow) and `useDeletionsPostReport(from, to)` (db.rpc('get_deletions_post_report',...), parse into DeletionsPostRow), both following Pattern 2. Create the two integration test files mirroring void-refund-report.integration.test.ts: seed one order-item removal / one paid-tab edit, then assert the hook/RPC returns a correctly-shaped row for the seeded range. These tests hit the live RPCs pushed in Plan 05.</action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/entities/tab/model/deletions-pre-report.integration.test.ts src/entities/tab/model/deletions-post-report.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both hooks exported and typecheck-clean
    - Both integration test files exist and pass against the live RPCs
    - Tests assert the exact DeletionsPreRow/DeletionsPostRow column sets
  </acceptance_criteria>
  <done>Both deletions reports have typed hooks proven against the live database.</done>
</task>

<task type="auto">
  <name>Task 3: Add modifier-popularity + payment-methods hooks + live integration tests</name>
  <files>src/entities/tab/model/queries-reports.ts, src/entities/tab/model/modifier-popularity-report.integration.test.ts, src/entities/tab/model/payment-methods-report.integration.test.ts</files>
  <read_first>
    - src/entities/tab/model/void-refund-report.integration.test.ts (integration test pattern)
    - src/shared/lib/domain.ts ModifierPopularityRowSchema / PaymentMethodRowSchema (Plan 01)
    - .planning/phases/24-operational-reports-suite-csv/24-RESEARCH.md Pitfall 4 (modifier attach-count spot-check) + Assumption A3 (payment refund/reopened_void exclusion)
  </read_first>
  <action>Add `useModifierPopularityReport(from, to)` and `usePaymentMethodsReport(from, to)` following Pattern 2, parsing into ModifierPopularityRow / PaymentMethodRow. Create the two integration tests mirroring void-refund-report.integration.test.ts. The modifier test seeds order-items with modifier_ids and spot-checks attach-count against a manual `COUNT(*) WHERE 'xyz' = ANY(modifier_ids)` (Pitfall 4). The payment test seeds payments across methods and asserts reopened_void/refund rows are excluded (Assumption A3) and the rollup row sums the per-session rows (D-08).</action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/entities/tab/model/modifier-popularity-report.integration.test.ts src/entities/tab/model/payment-methods-report.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both hooks exported and typecheck-clean
    - Modifier integration test asserts attach-count matches a manual ANY(modifier_ids) count (no double-count)
    - Payment integration test asserts reopened_void + refund rows are excluded and the rollup equals the sum of session rows
  </acceptance_criteria>
  <done>Modifier-popularity + payment-methods have typed hooks proven against the live database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client hook → PostgREST RPC | Hooks pass caller-supplied from/to; the RPC binds them as timestamptz params |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06-V | Input Validation | from/to date params reaching the RPC | low | mitigate | Params passed as ISO strings and bound to `timestamptz` server-side (Postgres rejects malformed input at the function boundary); the client `assertDateRangeValid` 365-day cap continues to apply on the report pages. Zod-parse of returned rows rejects malformed RPC output. |
| T-24-06-I | Information Disclosure | hooks surface aggregate report data | low | accept | Hooks are only mounted inside `/reports` widgets (view_reports gated); same posture as T-24-03-I. |
| T-24-06-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- Migrated hook bodies call the RPCs; pure helpers retained
- 4 new hooks exist and their live integration tests pass
- `npm run typecheck` + `npm run test` green
</verification>

<success_criteria>
- 2 report hooks migrated to RPC + 4 new report hooks added, all typed (SC-1 / SC-4)
- 4 live integration tests green (SC-1)
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-06-SUMMARY.md` when done
</output>
