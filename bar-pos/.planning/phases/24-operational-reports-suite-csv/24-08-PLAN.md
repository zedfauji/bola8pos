---
phase: 24-operational-reports-suite-csv
plan: 08
type: execute
wave: 5
depends_on: [24-06, 24-02]
files_modified:
  - src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx
  - src/widgets/DeletionsPreSendPanel/index.ts
  - src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx
  - src/widgets/DeletionsPostCloseReport/index.ts
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "DeletionsPreSendPanel renders a DataTable of order_item.remove rows (orderId/itemName/removedAt/staffName/reason) with an EmptyState when empty (D-05/D-16)"
    - "DeletionsPreSendPanel renders a standing, non-dismissible Alert (AlertTriangle icon, default variant) above the table stating the historical-gap caveat (specifics.md requirement)"
    - "DeletionsPostCloseReport renders a DataTable of tab.edit_paid rows (tabId/editedAt/staffName/reason/fieldsChanged) with NO historical-gap banner (D-05)"
    - "Both widgets are tabular-only (no chart) and expose ExportButtons with their reportType passing {rows, dateRange} (D-16)"
    - "All copy resolves to Plan-01 wAdmin i18n keys; numeric cells use tabular-nums"
  artifacts:
    - src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx
    - src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx
  key_links:
    - "DeletionsPreSendPanel → useDeletionsPreReport (Plan 06) → get_deletions_pre_report → audit_logs order_item.remove"
    - "DeletionsPre/PostCloseReport ExportButtons reportType='deletions-pre'/'deletions-post' → useExportReport csv branch (Plan 02)"
    - "historical-gap Alert → shared/ui Alert/AlertTitle/AlertDescription primitives"
---

<objective>
Build the two tabular-only deletions report widgets (D-05/D-16). `DeletionsPreSendPanel`
lists pre-send item removals (variant A) with a permanent historical-gap Alert banner —
because removals were un-audited before this phase, the report must visibly tell operators
it only covers data from this phase forward (specifics.md). `DeletionsPostCloseReport`
lists post-close corrections (variant B, from `tab.edit_paid` audit rows) with NO banner
(Phase 22 already audited that data from its own ship date).

Purpose: SC-3 (widgets render each report on the Reports page). Both are near-verbatim
copies of `VoidRefundPanel` (the closest event-log/reason-column DataTable precedent), plus
the pre-send panel adds a standing `Alert` using shared/ui primitives (no widget-level
precedent — the Alert primitive itself is the analog).
Output: 2 new widget folders (component + index barrel).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-operational-reports-suite-csv/24-CONTEXT.md
@.planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md
@.planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md
@src/widgets/VoidRefundPanel/VoidRefundPanel.tsx
</context>

<artifacts_produced>
- `DeletionsPreSendPanel` widget (+ barrel) — DataTable + standing historical-gap Alert, reportType `deletions-pre`
- `DeletionsPostCloseReport` widget (+ barrel) — DataTable, no banner, reportType `deletions-post`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: DeletionsPreSendPanel (DataTable + standing historical-gap Alert)</name>
  <files>src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx, src/widgets/DeletionsPreSendPanel/index.ts</files>
  <read_first>
    - src/widgets/VoidRefundPanel/VoidRefundPanel.tsx (whole file — the DataTable + column def + loading/empty wiring to copy)
    - .planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md ("DeletionsPreSendPanel / DeletionsPostCloseReport" section lines ~197-227)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Report Tab Inventory (deletions-pre row + "Alert banner default variant, AlertTriangle icon, always visible") + Copywriting Contract (deletionsPreSendPanel.* keys)
    - src/shared/ui (Alert/AlertTitle/AlertDescription, DataTable, EmptyState, LoadingSpinner exports)
  </read_first>
  <action>Create DeletionsPreSendPanel({ dateRange }) following VoidRefundPanel's structure: `useDeletionsPreReport(dateRange.from, dateRange.to)`, ColumnDef<DeletionsPreRow> for orderId/itemName/removedAt(toLocaleString)/staffName(font-medium)/reason(truncate, fallback em-dash), LoadingSpinner/EmptyState (t('wAdmin:deletionsPreSendPanel.emptyTitle/emptyDescription')), DataTable, and an ExportButtons reportType='deletions-pre' data={{rows, dateRange}}. ADD a standing, non-dismissible `<Alert variant="default">` with an `AlertTriangle` icon rendered unconditionally ABOVE the DataTable, using `t('wAdmin:deletionsPreSendPanel.historicalGapTitle/historicalGapDescription')`. Root wrapper `<div className="space-y-4">`. Numeric cells `tabular-nums`. Add the index.ts barrel export. No chart (D-16).</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/widgets/DeletionsPreSendPanel/DeletionsPreSendPanel.tsx','utf8');const ok=s.includes('useDeletionsPreReport')&&s.includes('AlertTriangle')&&s.includes('historicalGap')&&s.includes(\"reportType=\");console.log(ok?'ok':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Widget calls `useDeletionsPreReport` and renders a DataTable of the 5 columns
    - A standing Alert (AlertTriangle, always visible) sits above the table using the historicalGap i18n keys
    - ExportButtons reportType='deletions-pre' present; no chart component imported
    - `npm run typecheck` + `npm run lint` green
  </acceptance_criteria>
  <done>Pre-send deletions are listed with a permanent historical-gap notice.</done>
</task>

<task type="auto">
  <name>Task 2: DeletionsPostCloseReport (DataTable, no banner)</name>
  <files>src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx, src/widgets/DeletionsPostCloseReport/index.ts</files>
  <read_first>
    - src/widgets/VoidRefundPanel/VoidRefundPanel.tsx (structure to copy)
    - .planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md Report Tab Inventory (deletions-post row: tabId/editedAt/staffName/reason/fieldsChanged, "No historical-gap banner") + Copywriting Contract (deletionsPostCloseReport.* keys)
    - src/shared/lib/domain.ts DeletionsPostRowSchema
  </read_first>
  <action>Create DeletionsPostCloseReport({ dateRange }) as a straight VoidRefundPanel-shaped copy: `useDeletionsPostReport(dateRange.from, dateRange.to)`, ColumnDef<DeletionsPostRow> for tabId/editedAt/staffName/reason/fieldsChanged, LoadingSpinner/EmptyState (t('wAdmin:deletionsPostCloseReport.emptyTitle/emptyDescription')), DataTable, ExportButtons reportType='deletions-post' data={{rows, dateRange}}. NO Alert banner (D-05 — Phase 22 already covers this data). Root `<div className="space-y-4">`, tabular-nums numeric cells, index.ts barrel. No chart (D-16).</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/widgets/DeletionsPostCloseReport/DeletionsPostCloseReport.tsx','utf8');const ok=s.includes('useDeletionsPostReport')&&s.includes(\"reportType=\")&&!s.includes('historicalGap');console.log(ok?'ok':'FAIL');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Widget calls `useDeletionsPostReport` and renders a DataTable of the 5 columns
    - No historical-gap Alert present
    - ExportButtons reportType='deletions-post' present; no chart
    - `npm run typecheck` + `npm run lint` green
  </acceptance_criteria>
  <done>Post-close corrections are listed as a plain event-log table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → report hook | Widgets only render rows the (view_reports-gated) hooks return; no direct DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-08-I | Information Disclosure | deletions widgets display audit before/after-derived data | low | accept | Rendered only inside `/reports` (view_reports gated); reason/actor columns are the intended audit-transparency content. Same posture as T-24-03-I. |
| T-24-08-M | Misrepresentation | pre-send report could look "empty/broken" for historical dates | low | mitigate | The standing historical-gap Alert (Task 1) explicitly states the report only covers data since this phase shipped, so an operator doesn't misread the gap as a bug (specifics.md requirement). |
| T-24-08-SC | Tampering | npm installs | low | accept | No new packages (reuses installed shared/ui + tanstack-table). |
</threat_model>

<verification>
- Both widgets typecheck, lint clean, render DataTable + ExportButtons
- Pre-send widget shows the standing Alert; post-close widget does not
</verification>

<success_criteria>
- 2 tabular deletions widgets render on the Reports page (SC-3 / D-05 / D-16)
- Historical-gap surfaced in-UI for pre-send (specifics.md)
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-08-SUMMARY.md` when done
</output>
