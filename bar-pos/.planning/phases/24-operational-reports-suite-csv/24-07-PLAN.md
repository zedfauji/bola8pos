---
phase: 24-operational-reports-suite-csv
plan: 07
type: execute
wave: 4
depends_on: [24-05, 24-01]
files_modified:
  - src/features/remove-tab-item/useRemoveTabItem.ts
  - src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx
  - src/features/remove-tab-item/ui/RemoveTabItemDialog.test.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "useRemoveTabItem's mutationFn calls db.rpc('remove_tab_item', {p_item_id, p_reason}) instead of the 3-step client delete/check/void sequence (D-06)"
    - "RemoveTabItemInput carries a required reason string; the removal TODO inventory-restore comment is gone (now handled server-side)"
    - "RemoveTabItemDialog has a required reason Input gating submission (canConfirm false when reason is empty), mirroring VoidOrderDialog (D-07)"
    - "RemoveTabItemDialog adds NO PIN gate and NO destructive confirmClassName (UI-SPEC: not applicable — removal stays bartender-accessible)"
    - "onSuccess still invalidates tabKeys.detail + tabKeys.lists"
  artifacts:
    - src/features/remove-tab-item/useRemoveTabItem.ts
    - src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx
  key_links:
    - "useRemoveTabItem db.rpc('remove_tab_item') → live RPC (Plan 05) → order_item.remove audit → deletions-pre report (Plan 06/08)"
    - "RemoveTabItemDialog reason Input → p_reason → record_audit after-state reason key"
---

<objective>
Rewire the order-item removal feature to the new atomic RPC and add the mandatory reason
field. `useRemoveTabItem` collapses its 3-step client mutation (delete → check remaining →
void) plus the stale inventory-restore TODO into one `db.rpc('remove_tab_item', ...)` call
(D-06); `RemoveTabItemDialog` gains a required reason textarea mirroring VoidOrderDialog
(D-07). No PIN gate, no destructive styling — removal keeps today's bartender-accessible
access level.

Purpose: SC-1 (order_item.remove audit reachable from the UI). This is the client half of
the D-06/D-07 audit-gap fix; the server half (the RPC) shipped in Plan 04 and is live after
Plan 05. The reason the operator types flows to `p_reason` → the audit after-state → the
deletions-pre report's reason column.
Output: RPC-backed hook, reason-gated dialog, dialog test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-operational-reports-suite-csv/24-CONTEXT.md
@.planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md
@.planning/phases/24-operational-reports-suite-csv/24-UI-SPEC.md
@src/features/remove-tab-item/useRemoveTabItem.ts
@src/features/void-order/ui/VoidOrderDialog.tsx
</context>

<artifacts_produced>
- `useRemoveTabItem` mutation rewired to `remove_tab_item` RPC; `RemoveTabItemInput` gains `reason: string`
- `RemoveTabItemDialog` required-reason Input (i18n keys `featOrders:removeTabItem.reasonLabel|reasonPlaceholder|reasonRequired` from Plan 01)
- `RemoveTabItemDialog.test.tsx` reason-required case
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rewire useRemoveTabItem to the remove_tab_item RPC (D-06)</name>
  <files>src/features/remove-tab-item/useRemoveTabItem.ts</files>
  <read_first>
    - src/features/remove-tab-item/useRemoveTabItem.ts (current 3-step delete/check/void mutation + the inventory-restore TODO at lines ~80-83 + the onSuccess invalidation ~96-100)
    - .planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md ("useRemoveTabItem.ts" section — the new db.rpc('remove_tab_item') shape lines ~150-163)
    - src/features/void-order/model/useVoidOrder.ts (Result-returning db.rpc mutation reference)
    - src/shared/lib/result.ts (map the RPC's parsed.code — NOT_FOUND / TAB_NOT_OPEN — to an AppErrorCode; add one only if a suitable code is absent)
  </read_first>
  <action>Replace the mutationFn's 3-step sequence with a single `supabase.rpc('remove_tab_item', { p_item_id: input.itemId, p_reason: input.reason })` call, keep the `isOnline()` offline guard, unwrap the `{ ok, code?, message? }` jsonb envelope into a Result (map `code` to an AppErrorCode), and keep the existing `onSuccess` invalidation of `tabKeys.detail(input.tabId)` + `tabKeys.lists()` unchanged. Add `reason: string` to `RemoveTabItemInput`. Delete the now-obsolete inventory-restore TODO comment (the RPC handles restore). Keep the structured logger calls.</action>
  <verify>
    <automated>cd bar-pos && npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('src/features/remove-tab-item/useRemoveTabItem.ts','utf8');const okc=s.includes(\"rpc('remove_tab_item'\")&&/reason\s*:\s*string/.test(s);const noTodo=!/TODO:\s*Restore inventory/.test(s);console.log(okc&&noTodo?'ok':'FAIL');process.exit(okc&&noTodo?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - mutationFn calls `rpc('remove_tab_item', ...)` and no longer issues `from('order_items').delete()`
    - `RemoveTabItemInput` includes `reason: string`
    - The inventory-restore TODO comment is removed
    - onSuccess still invalidates tabKeys.detail + tabKeys.lists
    - `npm run typecheck` shows no new errors
  </acceptance_criteria>
  <done>Removal is one atomic audited RPC call; the client no longer orchestrates delete/void/restore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add required reason field to RemoveTabItemDialog + test (D-07)</name>
  <files>src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx, src/features/remove-tab-item/ui/RemoveTabItemDialog.test.tsx</files>
  <behavior>
    - The confirm/remove action is disabled while the reason input is empty or whitespace-only
    - Typing a non-empty reason enables the confirm action
    - Confirming passes the trimmed reason to the removal mutation
    - No PIN dialog is rendered by this component
  </behavior>
  <read_first>
    - src/features/void-order/ui/VoidOrderDialog.tsx (reason state + trimmedReason + canConfirm lines ~43-47; the labeled Input block ~113-126; ConfirmDialog confirmDisabled ~65)
    - src/features/remove-tab-item/ui/RemoveTabItemDialog.tsx (current dialog — where to insert the reason field + how it calls the mutation)
    - .planning/phases/24-operational-reports-suite-csv/24-PATTERNS.md ("RemoveTabItemDialog.tsx" section lines ~167-193) + 24-UI-SPEC.md Copywriting Contract (removeTabItem.* keys)
  </read_first>
  <action>Add a `reason`/`trimmedReason` state and a `canConfirm = trimmedReason.length > 0 && ...` boolean gating the ConfirmDialog's `confirmDisabled`, plus a labeled required `Input` using i18n keys `featOrders:removeTabItem.reasonLabel` / `reasonPlaceholder` (keys already exist from Plan 01). Pass the trimmed reason into the removal mutation input. Do NOT add a manager PIN gate or a destructive `confirmClassName` (UI-SPEC: not applicable). Create RemoveTabItemDialog.test.tsx asserting the confirm is blocked on empty reason and enabled once a reason is typed.</action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/features/remove-tab-item/ui/RemoveTabItemDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RemoveTabItemDialog.test.tsx passes with an empty-reason-blocks-submit case and a reason-enables-submit case
    - Dialog renders the reason Input via `t('...removeTabItem.reasonLabel')` (no hardcoded literal — `npm run lint` green)
    - No ManagerPinDialog / PIN component imported in RemoveTabItemDialog.tsx
  </acceptance_criteria>
  <done>Removal requires a reason (captured for the audit), with no new access gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → remove_tab_item RPC | Any authenticated (bartender+) caller removes an order item; the RPC audits it |
| operator input → p_reason | Free-text reason is trimmed client-side and stored in the append-only audit after-state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-07-R | Repudiation | order-item removal | high | mitigate | The dialog forces a non-empty reason and the RPC audits every removal (Plan 04) — a removal can no longer happen without an attributed, reasoned audit row. |
| T-24-07-E | Elevation of Privilege | adding a PIN/role gate here would change access | medium | mitigate | Explicitly NO PIN gate / no role component (D-07); asserted by the negative check in Task 2 and the bartender-removal E2E in Plan 10. |
| T-24-07-V | Input Validation | free-text reason | low | mitigate | Trimmed + required client-side (mirrors VoidOrderDialog); stored as a JSON value in an append-only table (no injection surface). |
| T-24-07-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- useRemoveTabItem calls remove_tab_item RPC; no direct order_items delete remains
- RemoveTabItemDialog blocks submit on empty reason, no PIN gate
- `npm run typecheck` + `npm run test` + `npm run lint` green
</verification>

<success_criteria>
- Removal wired to the atomic audited RPC (SC-1 / D-06)
- Required reason field, bartender-accessible (D-07)
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-07-SUMMARY.md` when done
</output>
