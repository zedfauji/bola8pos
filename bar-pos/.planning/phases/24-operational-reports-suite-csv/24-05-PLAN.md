---
phase: 24-operational-reports-suite-csv
plan: 05
type: execute
wave: 3
depends_on: [24-03, 24-04]
files_modified:
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-1, SC-4]
user_setup:
  - service: supabase
    why: "Applying phase-24 migrations to the remote Supabase project (linked CLI session, same as Phases 17/22/23)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (ONLY needed if `npx supabase db push` cannot use the existing interactive/linked login in this environment — Phase 23 confirmed the pre-linked session was sufficient)"
must_haves:
  truths:
    - "All 5 phase-24 migrations (20260721000002..000006) are applied to the remote database — get_peak_hours_report, get_voids_report, get_modifier_popularity_report, get_payment_methods_report, remove_tab_item, get_deletions_pre_report, get_deletions_post_report all callable"
    - "supabase.types.ts Functions includes the 7 new RPC signatures so client hooks typecheck without an `as any` cast"
  artifacts:
    - src/shared/lib/supabase.types.ts
  key_links:
    - "npx supabase db push → live RPCs → Plan 06 integration tests + Plan 07 remove_tab_item hook"
    - "npx supabase gen types typescript → supabase.types.ts Functions → typechecked db.rpc(...) calls"
---

<objective>
[BLOCKING] Apply all five Plan-03/Plan-04 migrations to the remote Supabase database in
one push, then regenerate `supabase.types.ts` so every new RPC is a typed function on the
generated client. This is the mandatory schema-push gate: build and typecheck pass without
it (types come from the generated file, not the live DB), but no report hook or the
remove-tab-item flow works against the real database until the functions exist remotely.

Purpose: SC-1 (RPCs shipped to remote) + unblocks SC-4 verification. This plan is flagged
`autonomous: false` per the schema-push detection gate — `npx supabase db push` may present
an interactive `[Y/n]` confirmation. Phase 23 confirmed the pre-linked CLI session proceeded
without blocking and needed no access token, but a human may need to intervene if the CLI
prompts for auth.
Output: 5 migrations live on remote; supabase.types.ts regenerated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-operational-reports-suite-csv/24-RESEARCH.md
@.planning/phases/23-reopen-closed-ticket/23-04-PLAN.md
</context>

<artifacts_produced>
- 7 RPC functions live on the remote database (peak-hours, voids, modifier-popularity, payment-methods, remove_tab_item, deletions-pre, deletions-post)
- Regenerated `src/shared/lib/supabase.types.ts` with the 7 new `Functions` entries
</artifacts_produced>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: [BLOCKING] Apply phase-24 migrations to the remote database</name>
  <files>supabase/migrations/ (no file edit — remote DB state change only; verified via migration list)</files>
  <read_first>
    - .planning/phases/23-reopen-closed-ticket/23-04-PLAN.md (the exact db-push precedent + the PGRST205 `NOTIFY pgrst, 'reload schema'` workaround if the new RPCs 404 from PostgREST after push)
    - .planning/STATE.md (Phase 17/22/23 session-log entries — `npx supabase db push` proceeded without a hard interactive block)
    - CLAUDE.md ("Missing generated types workaround" + the `npx supabase gen types typescript` note)
  </read_first>
  <action>From `bar-pos/`, run `npx supabase db push` to apply the five phase-24 migrations (20260721000002 through 20260721000006). This plan is `autonomous: false` — if the CLI prompts for interactive auth that cannot be satisfied, stop and surface the prompt for human intervention (Phase 23 confirmed the pre-linked session proceeded without a hard block and needed no SUPABASE_ACCESS_TOKEN). After the push, run `npx supabase migration list` and confirm all 5 phase-24 timestamps appear in BOTH LOCAL and REMOTE columns. If a client call to a new RPC returns PGRST205, issue the `NOTIFY pgrst, 'reload schema'` reload documented in the Phase 23 plan. No source file is committed by this task — it is a remote schema state change.</action>
  <verify>
    <automated>cd bar-pos && npx supabase migration list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list` shows all 5 phase-24 timestamps applied on the remote (no LOCAL-only rows for these)
    - Calling any new RPC (e.g. `get_peak_hours_report`) from a psql/REST probe returns `{"ok":true,...}` rather than a "function does not exist" error
  </acceptance_criteria>
  <done>All 5 phase-24 migrations are applied to the remote database and the 7 RPCs are callable.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate supabase.types.ts</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <read_first>
    - CLAUDE.md ("Missing generated types workaround" — the exact `npx supabase gen types typescript` invocation and the `--project-id` used in Phase 23)
    - .planning/phases/23-reopen-closed-ticket/23-04-SUMMARY.md (the regen command that added Functions.reopen_tab — mirror it)
  </read_first>
  <action>Regenerate the generated types via `npx supabase gen types typescript --project-id <project> > src/shared/lib/supabase.types.ts` (use the project id from the Phase 23 summary). Confirm the `Functions` section now contains get_peak_hours_report, get_voids_report, get_modifier_popularity_report, get_payment_methods_report, remove_tab_item, get_deletions_pre_report, get_deletions_post_report with their exact `p_from`/`p_to` / `p_item_id`/`p_reason` arg shapes. Do not hand-edit — regenerate.</action>
  <verify>
    <automated>cd bar-pos && node -e "const s=require('fs').readFileSync('src/shared/lib/supabase.types.ts','utf8');const need=['get_peak_hours_report','get_voids_report','get_modifier_popularity_report','get_payment_methods_report','remove_tab_item','get_deletions_pre_report','get_deletions_post_report'];const miss=need.filter(t=>!s.includes(t));console.log(miss.length?'MISSING '+miss.join(','):'ok');process.exit(miss.length?1:0)" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - supabase.types.ts `Functions` contains all 7 new RPC names
    - `npm run typecheck` shows no new errors beyond the 2 pre-existing documented ones
  </acceptance_criteria>
  <done>The generated client types know every phase-24 RPC; downstream hooks typecheck without `as any`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → remote Supabase | `db push` mutates the production schema of the linked project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05-T | Tampering | applying migrations to the live remote DB | medium | mitigate | Push is human-gated (blocking checkpoint); `migration list` divergence check confirms exactly the 5 intended migrations applied. All migrations are additive (CREATE FUNCTION) — no destructive DDL. |
| T-24-05-D | Denial of Service | a malformed migration failing mid-push | low | mitigate | Each migration passed Plan 03/04 structural grep gates before this plan; `supabase db push` is transactional per-migration and stops on first error, leaving the list divergent (caught by the acceptance check). |
| T-24-05-SC | Tampering | npm/CLI supply chain | low | accept | Uses the already-installed supabase CLI; no new packages. |
</threat_model>

<verification>
- `npx supabase migration list` shows all 5 phase-24 migrations applied remotely
- supabase.types.ts contains all 7 new RPC signatures
- `npm run typecheck` green (no new errors)
</verification>

<success_criteria>
- 7 RPCs live on remote (SC-1)
- Generated types regenerated so downstream plans typecheck (SC-1)
</success_criteria>

<output>
Create `.planning/phases/24-operational-reports-suite-csv/24-05-SUMMARY.md` when done
</output>
