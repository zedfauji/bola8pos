---
phase: 18-split-payment-multi-method
plan: 06
type: execute
wave: 4
depends_on: [18-04, 18-05]
files_modified:
  - e2e/41-split-payment.spec.ts
  - CLAUDE.md
autonomous: false
requirements: [SC-2, SC-3, SC-4]
user_setup: []

must_haves:
  truths:
    - "An E2E spec drives a real tab close with 2 mixed payment methods end-to-end (UI → edge fn → RPC → paid tab → per-leg receipts)"
    - "The existing single-payment + adjacent split-bill E2E specs still pass (no regression — D-11/D-12)"
    - "The full unit suite is green (baseline maintained)"
    - "CLAUDE.md documents the split-payment feature + the new E2E spec"
  artifacts:
    - path: "e2e/41-split-payment.spec.ts"
      provides: "Split payment happy-path + validation E2E"
      contains: "Split payment"
    - path: "CLAUDE.md"
      provides: "Implemented Features + E2E spec list entries for split payment"
      contains: "split-payment"
  key_links:
    - from: "e2e/41-split-payment.spec.ts"
      to: "PaymentForm split mode"
      via: "toggle + 2-row fill + submit"
      pattern: "Split payment"
---

<objective>
Prove split payment works end-to-end and ships without regression: a new Playwright spec drives a real 2-method tab close through the deployed edge function, the existing payment + split-bill E2E specs stay green (D-11/D-12), the full unit suite stays green, and CLAUDE.md is updated. A blocking human sign-off confirms the physical cash-drawer-once behavior (18-RESEARCH.md Assumption A2, which unit tests can only assert at the mock layer).

Purpose: This is the phase verification gate — SC-2/SC-3/SC-4 confirmed against a running dev server + live remote, plus the operator UAT sign-off the UI-SPEC flagged.

Output: New E2E spec, updated CLAUDE.md, human-verified drawer behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-split-payment-multi-method/18-CONTEXT.md
@.planning/phases/18-split-payment-multi-method/18-VALIDATION.md
@.planning/phases/18-split-payment-multi-method/18-UI-SPEC.md
</context>

## Artifacts this phase produces

Created by THIS plan: `e2e/41-split-payment.spec.ts` + CLAUDE.md documentation entries. Consumes the full Phase 18 stack (columns/RPC from 02, deployed edge fn from 04, PaymentForm UI from 05).

<tasks>

<task type="auto">
  <name>Task 1: E2E spec 41-split-payment.spec.ts</name>
  <read_first>
    - e2e/05-payments.spec.ts (single-payment happy-path spec — the closest analog: opening a tab, adding items, opening PaymentPane, submitting, asserting paid)
    - e2e/34-split-bill.spec.ts (adjacent split-bill spec — sub-tab pattern, do NOT confuse with split PAYMENT; reference for tab-seeding helpers)
    - e2e/helpers/auth.ts (loginAs — admin PIN 0000)
    - .planning/phases/18-split-payment-multi-method/18-VALIDATION.md (Wave 0 requirement: e2e/4X-split-payment.spec.ts covers SC-2/SC-3/D-08/D-09)
    - .planning/phases/18-split-payment-multi-method/18-UI-SPEC.md (Copywriting Contract — exact strings to locate: "Split payment", "+ Add payment method", "Remaining to pay", "Fully allocated", "Process split payment", "Receipt {i} of {n}")
  </read_first>
  <action>
    Create `e2e/41-split-payment.spec.ts` (number 41 is the next free after 40-kds-bar, verified). Use `loginAs(page, 'admin')` and the tab-seeding approach used by `05-payments.spec.ts`/`34-split-bill.spec.ts`.

    Tests:
    - T1 happy path (D-08/D-09, SC-2/SC-3): open a tab, add items totalling a known amount, open PaymentPane, toggle "Split payment" ON, confirm two rows appear + a "Remaining to pay" box; fill row 1 as cash for half the total, row 2 as card for the other half; assert the box reads "Fully allocated ✓" and "Process split payment" becomes enabled; submit; assert the receipt sequence header "Receipt 1 of 2" appears, advance through both receipts via the Done button, and assert the tab is closed/paid afterward.
    - T2 validation gate (SC-3, Pitfall 3): with split ON and only row 1 filled (row 2 amount 0 / remaining ≠ 0), assert "Process split payment" is disabled and the remaining box does NOT read "Fully allocated".
    - T3 add/remove row (D-02): add a 3rd and 4th row; assert "+ Add payment method" becomes disabled at 4 rows; remove down to 2 and assert the remove control disappears at exactly 2 rows.

    Follow the project convention of runnable-but-reporting tests where seed data may be environment-dependent (annotate rather than hard-skip, per the 32-combos/40-kds-bar precedent) so the spec is always executable.
  </action>
  <verify>
    <automated>cd bar-pos && npx playwright test e2e/41-split-payment.spec.ts --reporter=line</automated>
  </verify>
  <done>e2e/41-split-payment.spec.ts exists and its happy-path + validation + add/remove-row tests pass against a running dev server + live remote (or annotate cleanly when seed env absent).</done>
  <acceptance_criteria>
    - `grep -c "Split payment\|Process split payment\|Remaining to pay" e2e/41-split-payment.spec.ts` >= 3 (drives the real UI copy)
    - `grep -c "Receipt 1 of 2\|Receipt " e2e/41-split-payment.spec.ts` >= 1 (D-09 sequence)
    - `npx playwright test e2e/41-split-payment.spec.ts` runs (T1 passes with dev server + seed; T2/T3 assert UI gating)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Regression gate + CLAUDE.md docs + human UAT sign-off (cash-drawer-once)</name>
  <read_first>
    - CLAUDE.md ("Implemented Features (as of ...)" list + "E2E Test Suite" spec list — the two sections to update)
    - .planning/phases/18-split-payment-multi-method/18-VALIDATION.md (Sampling Rate → phase gate: full suite + payment E2E specs)
    - .planning/phases/18-split-payment-multi-method/18-UI-SPEC.md (#8 — drawer-once is a human-verify item; Assumption A2)
  </read_first>
  <action>
    Run the automated regression gate, then update docs, then request the hardware UAT sign-off:
    1. `cd bar-pos && npm run test` — full unit suite green (baseline per STATE.md: 1187+ passing; only the documented pre-existing useCloseTab.test.ts:95 failure is acceptable).
    2. `cd bar-pos && npx playwright test e2e/05-payments.spec.ts e2e/17-payment-pane.spec.ts e2e/23-payment-edge-cases.spec.ts e2e/34-split-bill.spec.ts e2e/35-refund.spec.ts e2e/41-split-payment.spec.ts` — single-payment (D-11), split-bill, per-leg refund (D-12), and the new split-payment specs all pass.
    3. Update CLAUDE.md: add a `split-payment (multi-method)` bullet to the "Implemented Features" list describing the up-to-4-method atomic close (payment_group_id/split_index + process_split_payment_atomic + PaymentForm split toggle), and add `41-split-payment` to the "E2E Test Suite" spec count + list.
    Then present the hardware verification steps in <how-to-verify> for the operator sign-off. If any regression spec fails, fix the underlying code — do NOT weaken the spec.
  </action>
  <what-built>
    Full Phase 18 stack is complete: payments columns + process_split_payment_atomic RPC (Plans 02/03), deployed process-split-payment edge function (Plan 04), PaymentForm split-mode UI + unit tests (Plan 05), and the 41-split-payment E2E spec (Task 1). This task runs the regression gate, updates docs, and captures the physical-hardware UAT sign-off.
  </what-built>
  <how-to-verify>
    On real hardware with a cash drawer + thermal printer connected (or the operator's staging terminal):
    1. Open a tab, add items, open PaymentPane, toggle "Split payment" ON.
    2. Enter TWO cash rows that split the total, submit.
    3. Confirm the physical cash drawer pops exactly ONCE (not twice) — this is the 18-UI-SPEC #8 / Assumption A2 behavior unit tests can only assert at the mock layer.
    4. Confirm TWO receipts print in sequence (one per leg, D-09), and the receipt sequence header shows "Receipt 1 of 2" then "Receipt 2 of 2".
    5. Confirm the tab is closed/paid.
    Type "approved" if the drawer opens once and two receipts print, or describe the discrepancy.
  </how-to-verify>
  <verify>
    <automated>cd bar-pos && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>Full unit suite green; payment + split-bill + refund + split-payment E2E specs pass; CLAUDE.md updated; human confirmed drawer-opens-once + two-receipts on a real 2-cash split.</done>
  <acceptance_criteria>
    - `npm run test` exits 0 (or only the documented pre-existing useCloseTab.test.ts:95 failure)
    - The 6 regression E2E specs (05, 17, 23, 34, 35, 41) pass — single-payment (D-11) + refund (D-12) unaffected
    - `grep -c "split-payment\|Split Payment\|41-split-payment" CLAUDE.md` >= 2 (Features list + E2E list)
    - Human sign-off recorded confirming cash drawer opens exactly once for a 2-cash split (Pitfall 4 / A2)
  </acceptance_criteria>
  <resume-signal>Type "approved" once regression is green, docs are updated, and the physical drawer-once + two-receipt behavior is confirmed on hardware.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright client → deployed edge function → live remote | End-to-end exercise of the full untrusted-input → validated-write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering/DoS | end-to-end atomicity | mitigate | E2E T1 asserts the tab reaches paid only after both legs succeed; the RPC transaction (Plan 02) is the guarantee — E2E is the confirmation |
| T-18-11 | Repudiation | refund per-leg regression | mitigate | Regression run includes 35-refund.spec.ts, confirming per-leg refunds still work post-schema-change (D-12) |
| T-18-09 | Denial of Service | physical cash drawer | mitigate | Human UAT sign-off confirms drawer-opens-once on a real 2-cash split (the one behavior not fully automatable) |
| T-18-SC | Tampering | npm installs | accept | No package installs this phase |
</threat_model>

<verification>
- e2e/41-split-payment.spec.ts passes (SC-2/SC-3 end-to-end)
- Full unit suite + 6 regression E2E specs green (D-11/D-12 no regression)
- CLAUDE.md updated
- Human sign-off on drawer-once + two-receipt hardware behavior
</verification>

<success_criteria>
- End-to-end 2-method atomic close verified via Playwright (SC-2, SC-3, D-08, D-09)
- Zero regression: single-payment + split-bill + refund specs pass (SC-4, D-11, D-12)
- Docs current; operator UAT sign-off captured
</success_criteria>

<output>
Create `.planning/phases/18-split-payment-multi-method/18-06-SUMMARY.md` when done
</output>
