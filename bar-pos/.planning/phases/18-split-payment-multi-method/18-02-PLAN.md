---
phase: 18-split-payment-multi-method
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql
  - src/entities/payment/model/split-payment-rpc.integration.test.ts
autonomous: true
requirements: [SC-1, SC-2]
user_setup: []

must_haves:
  truths:
    - "The payments table has payment_group_id + split_index columns to tag which rows belong to one atomic split checkout"
    - "process_split_payment_atomic inserts 1-4 payment rows in a single transaction and closes the tab only if all legs succeed (all-or-nothing per D-08)"
    - "process_split_payment_atomic rejects a leg array whose amounts do not sum to p_expected_total (±0.01) per D-05"
    - "process_split_payment_atomic preserves the Phase 15 version guard (P0V01/P0V02) and idempotent-replay behavior"
  artifacts:
    - path: "supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql"
      provides: "ALTER TABLE payments +2 columns + indexes + process_split_payment_atomic RPC"
      contains: "process_split_payment_atomic"
      min_lines: 120
    - path: "src/entities/payment/model/split-payment-rpc.integration.test.ts"
      provides: "Live-Supabase integration tests for columns + RPC (green after Plan 03 push)"
      contains: "process_split_payment_atomic"
  key_links:
    - from: "process_split_payment_atomic"
      to: "payments"
      via: "loop INSERT with payment_group_id + split_index"
      pattern: "payment_group_id"
    - from: "process_split_payment_atomic"
      to: "tabs"
      via: "FOR UPDATE version guard + close"
      pattern: "FOR UPDATE"
---

<objective>
Add the schema and the atomic multi-leg RPC that are the backend heart of split payment: two new `payments` columns (`payment_group_id`, `split_index`) and the `process_split_payment_atomic` PL/pgSQL function that inserts 1-4 legs in ONE transaction, validates the leg sum against the client total, and closes the tab all-or-nothing (D-08). Also create the live-Supabase integration test scaffold that becomes the true green gate once Plan 03 pushes the migration.

Purpose: Deliver the single-transaction guarantee D-08 requires — the sequential `callProcessPayment` loop used by the pre-existing even-split feature is explicitly NOT reusable (18-RESEARCH.md Anti-Patterns). The RPC composes three proven precedents: the `split_tab_by_amount` jsonb-array loop, the `process_payment_atomic` version guard, and the multi-payment tab-close logic.

Output: One new migration file + one new integration test file. The migration is NOT pushed here (BLOCKING push is Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-split-payment-multi-method/18-CONTEXT.md
@.planning/phases/18-split-payment-multi-method/18-RESEARCH.md
@.planning/phases/18-split-payment-multi-method/18-PATTERNS.md
</context>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):
- `payments.payment_group_id` (UUID column), `payments.split_index` (SMALLINT column)
- `idx_payments_payment_group_id`, `idx_payments_group_split_unique` (indexes)
- `process_split_payment_atomic` (PL/pgSQL RPC)
- audit action string `'payment.process_split'`
- `supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql`
- `src/entities/payment/model/split-payment-rpc.integration.test.ts`

Created by other Phase 18 plans (do not flag): `SplitPaymentLegSchema`, `callProcessSplitPayment`, `processSplitPayment` (Plan 01); `supabase/functions/process-split-payment/index.ts` (Plan 04); PaymentForm split-mode reducer (Plan 05); `e2e/41-split-payment.spec.ts` (Plan 06).

<tasks>

<task type="auto">
  <name>Task 1: ALTER TABLE payments — add payment_group_id + split_index + indexes</name>
  <read_first>
    - supabase/migrations/20260421000001_payments_discount_columns.sql (additive ALTER TABLE precedent — BEGIN/COMMIT wrapping, IF NOT EXISTS idiom)
    - supabase/migrations/20260424000005_payments_constraint.sql (confirms UNIQUE(tab_id) was ALREADY dropped — do NOT re-drop; D-10's drop instruction is stale per 18-RESEARCH.md)
    - .planning/phases/18-split-payment-multi-method/18-PATTERNS.md (section "Analog 1 — column addition")
  </read_first>
  <action>
    Create `supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` (timestamp is +1 sequence after the two existing 20260707000001/000002 migrations — verified free). Open with a `-- UP:` header comment block describing purpose (split payment columns + atomic RPC, D-08/D-10). Wrap DDL in `BEGIN; ... COMMIT;`.

    Add the columns additively (do NOT drop UNIQUE(tab_id) — already gone): `ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_group_id UUID, ADD COLUMN IF NOT EXISTS split_index SMALLINT CHECK (split_index >= 0 AND split_index <= 3);` — the CHECK enforces D-02's 4-row cap (indices 0-3).

    Add `CREATE INDEX IF NOT EXISTS idx_payments_payment_group_id ON payments(payment_group_id) WHERE payment_group_id IS NOT NULL;` and `CREATE UNIQUE INDEX IF NOT EXISTS idx_payments_group_split_unique ON payments(payment_group_id, split_index) WHERE payment_group_id IS NOT NULL;` (prevents duplicate split_index within a group).

    Leave both columns nullable with no backfill (18-RESEARCH.md discretion resolution: nullable, populated on new-code paths only, never backfilled — D-11 single-method rows keep working with NULL group/index). Add a `-- DOWN:` comment block at the file end listing the reversal DDL (DROP INDEX both + ALTER TABLE DROP COLUMN both), commented out per the S6 DOWN-script convention.
  </action>
  <verify>
    <automated>cd bar-pos && grep -v '^--' supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql | grep -c "payment_group_id UUID\|split_index SMALLINT\|idx_payments_group_split_unique"</automated>
  </verify>
  <done>Migration file exists with both columns (nullable, split_index CHECK 0-3), both indexes, and no ALTER dropping UNIQUE(tab_id). Grep count >= 3.</done>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS payment_group_id UUID" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 1
    - `grep -c "split_index SMALLINT CHECK (split_index >= 0 AND split_index <= 3)" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 1
    - `grep -c "idx_payments_group_split_unique" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` >= 1
    - `grep -c "DROP CONSTRAINT.*tab_id\|UNIQUE (tab_id)" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 0 (D-10 stale-instruction guard)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CREATE FUNCTION process_split_payment_atomic (atomic multi-leg RPC)</name>
  <read_first>
    - supabase/migrations/20260512000002_rpc_versioned_group_a.sql (lines 44-262 — the CURRENT live process_payment_atomic body: idempotency lookup, FOR UPDATE version guard P0V01/P0V02, per-method validation, tab-close v_owed/v_paid_line logic, EXCEPTION block re-raising P0V01/P0V02, REVOKE/GRANT)
    - supabase/migrations/20260427000002_split_tab_rpcs.sql (lines 261-378 — split_tab_by_amount: jsonb-array sum validation + FOR ... IN SELECT jsonb_array_elements loop)
    - supabase/migrations/20260703000001_record_audit_terminal_id.sql (current record_audit signature — 6 positional args used in the process_payment_atomic call)
    - .planning/phases/18-split-payment-multi-method/18-PATTERNS.md (section "Analog 2" + "Analog 3" — the exact blocks to copy)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Pattern 3 idempotency, Pitfall 1/2/3, "Recommended new RPC signature")
  </read_first>
  <action>
    Append to `supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` (inside the same UP transaction, after the DDL) a `CREATE OR REPLACE FUNCTION public.process_split_payment_atomic(p_tab_id UUID, p_staff_id UUID, p_legs JSONB, p_expected_total NUMERIC, p_idempotency_key TEXT, p_discount_scope TEXT DEFAULT NULL, p_discount_type TEXT DEFAULT NULL, p_discount_value NUMERIC DEFAULT NULL, p_discount_amount NUMERIC DEFAULT NULL, p_expected_version INT DEFAULT NULL) RETURNS JSONB LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`.

    Body, in order:
    1. FORBIDDEN guard: `IF NOT EXISTS (SELECT 1 FROM profiles WHERE id = p_staff_id AND is_active = true) THEN RETURN ...code 'FORBIDDEN'`.
    2. Leg-count validation: compute `v_leg_count := jsonb_array_length(p_legs)`; `IF v_leg_count < 1 OR v_leg_count > 4 THEN RETURN ...code 'TOO_MANY_LEGS'` (message references the 1-4 range, D-02).
    3. Idempotency replay (Pattern 3): `SELECT id, payment_group_id INTO v_existing_id, v_existing_group FROM payments WHERE idempotency_key = p_idempotency_key || '-leg0' LIMIT 1;` — if found, re-aggregate all leg ids for `v_existing_group` into `v_payment_ids` and RETURN `jsonb_build_object('ok', true, 'idempotent', true, 'paymentGroupId', v_existing_group, 'paymentIds', v_payment_ids)`.
    4. Version guard — COPY VERBATIM from process_payment_atomic lines 103-119: `SELECT status, rappi_order_id, version INTO v_tab_status, v_rappi_tab, v_current FROM tabs WHERE id = p_tab_id FOR UPDATE;` then `IF NOT FOUND THEN RAISE EXCEPTION 'NOT_FOUND_VERSIONED' USING ERRCODE='P0V02'; END IF;` and `IF p_expected_version IS NOT NULL AND v_current <> p_expected_version THEN RAISE EXCEPTION 'STALE_VERSION' USING ERRCODE='P0V01'; END IF;` then `IF v_tab_status IS DISTINCT FROM 'open' THEN RETURN ...code 'TAB_NOT_OPEN'`.
    5. Pool-session-active guard: copy the `POOL_SESSION_ACTIVE` block (stop pool before payment).
    6. Sum validation (D-05): `SELECT COALESCE(SUM((leg->>'amount')::numeric), 0) INTO v_legs_sum FROM jsonb_array_elements(p_legs) AS leg;` then `IF ABS(v_legs_sum - p_expected_total) > 0.01 THEN RETURN ...code 'SPLIT_TOTAL_MISMATCH'` (message includes both numbers).
    7. `v_group_id := gen_random_uuid();` and `v_payment_ids := '{}';`.
    8. Loop with index: use `FOR v_i IN 0..(v_leg_count - 1) LOOP v_leg := p_legs->v_i; ...` For each leg extract `v_method := (v_leg->>'method')`, `v_leg_amount := (v_leg->>'amount')::numeric`, `v_leg_tip := COALESCE((v_leg->>'tipAmount')::numeric, 0)`, `v_leg_tendered := (v_leg->>'tenderedAmount')::numeric`, `v_leg_ref := v_leg->>'referenceNumber'`, `v_leg_rappi := v_leg->>'rappiOrderId'`. Per-leg validation: method IN ('cash','card','rappi') else 'INVALID_METHOD'; `IF v_leg_amount <= 0 THEN RETURN ...code 'EMPTY_LEG'` (Pitfall 3 — pre-empt amount_positive CHECK); cash requires tendered and `ROUND(v_leg_tendered,2) >= ROUND(v_leg_amount + v_leg_tip, 2)` else 'INSUFFICIENT_TENDER'/'TENDERED_REQUIRED'; non-cash forbids tendered ('TENDERED_NOT_ALLOWED'); rappi requires `v_rappi_tab = v_leg_rappi` else 'RAPPI_ORDER_MISMATCH'. Then `INSERT INTO payments (tab_id, amount, tip_amount, method, processed_by, tendered_amount, reference_number, idempotency_key, payment_group_id, split_index, discount_scope, discount_type, discount_value, discount_amount) VALUES (p_tab_id, ROUND(v_leg_amount,2), ROUND(v_leg_tip,2), v_method::payment_method, p_staff_id, CASE WHEN v_method='cash' THEN ROUND(v_leg_tendered,2) ELSE NULL END, NULLIF(TRIM(v_leg_ref),''), p_idempotency_key || '-leg' || v_i::text, v_group_id, v_i, CASE WHEN v_i = 0 THEN p_discount_scope ELSE NULL END, CASE WHEN v_i = 0 THEN p_discount_type ELSE NULL END, CASE WHEN v_i = 0 THEN p_discount_value ELSE NULL END, CASE WHEN v_i = 0 THEN p_discount_amount ELSE NULL END) RETURNING id INTO v_payment_id;` — discount stored ONLY on split_index=0 (D-04: discount computed once, not per row; avoids double-count in SUM(discount_amount) reports). Append `v_payment_id` to `v_payment_ids`.
    9. Tab-close — COPY VERBATIM from process_payment_atomic lines 184-222: compute `v_owed` (order_items subtotal, parent_order_item_id IS NULL), `v_paid_line` (SUM non-refund payments), `IF v_paid_line + 0.0001 >= v_owed THEN UPDATE tabs SET status='paid', closed_at=NOW(), updated_at=NOW(), version=version+1 WHERE id=p_tab_id AND status='open'; GET DIAGNOSTICS v_tab_updated=ROW_COUNT; IF v_tab_updated=0 THEN DELETE FROM payments WHERE payment_group_id = v_group_id; RETURN ...code 'TAB_NOT_OPEN'` (delete ALL group legs on the race, adapted from the single-row delete). ELSE partial-pay branch bumps version.
    10. Audit (18-PATTERNS.md audit block): `PERFORM record_audit('payment.process_split', 'payment', v_group_id, NULL, jsonb_build_object('paymentIds', to_jsonb(v_payment_ids), 'legCount', v_leg_count), 'rpc');`.
    11. RETURN `jsonb_build_object('ok', true, 'idempotent', false, 'paymentGroupId', v_group_id, 'paymentIds', to_jsonb(v_payment_ids))`.
    12. EXCEPTION block: `WHEN unique_violation THEN` re-lookup `p_idempotency_key || '-leg0'` and return idempotent replay if found else code 'DUPLICATE'; `WHEN sqlstate 'P0V01' THEN RAISE;` `WHEN sqlstate 'P0V02' THEN RAISE;` `WHEN OTHERS THEN RETURN ...code 'INTERNAL', message 'Split payment failed'`.

    After the function, add `REVOKE ALL ON FUNCTION public.process_split_payment_atomic(UUID, UUID, JSONB, NUMERIC, TEXT, TEXT, TEXT, NUMERIC, NUMERIC, INT) FROM PUBLIC;` and `GRANT EXECUTE ... TO service_role;` (called via the edge function's service-role admin client, matching process_payment_atomic).
  </action>
  <verify>
    <automated>cd bar-pos && grep -v '^--' supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql | grep -c "process_split_payment_atomic\|P0V01\|P0V02\|'-leg' || v_i\|SPLIT_TOTAL_MISMATCH\|payment.process_split"</automated>
  </verify>
  <done>process_split_payment_atomic defined with: 1-4 leg validation, sum==p_expected_total guard, per-leg derived idempotency keys, verbatim P0V01/P0V02 version guard + re-raise, verbatim multi-payment close logic, record_audit('payment.process_split'), REVOKE/GRANT to service_role. Grep count >= 6.</done>
  <acceptance_criteria>
    - `grep -c "RAISE EXCEPTION 'STALE_VERSION' USING ERRCODE = 'P0V01'" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 1
    - `grep -c "WHEN sqlstate 'P0V01' THEN" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 1 (re-raise, not swallowed)
    - `grep -c "p_idempotency_key || '-leg'" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` >= 1 (Pitfall 1 fix)
    - `grep -c "SPLIT_TOTAL_MISMATCH\|EMPTY_LEG\|TOO_MANY_LEGS" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` >= 3
    - `grep -c "record_audit(" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` >= 1 with action `'payment.process_split'`
    - `grep -c "GRANT EXECUTE ON FUNCTION public.process_split_payment_atomic" supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` == 1 (service_role only)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integration test scaffold — columns + RPC (live Supabase, green after Plan 03 push)</name>
  <read_first>
    - src/features/process-refund/process-refund-rpc.integration.test.ts (lines 1-70 — env-guard pattern: hasEnv/hasAuthEnv, getServiceDb/getAuthClient, itInt/itAuth skip aliases)
    - src/entities/tab/model/depletion.integration.test.ts (live-Supabase RPC-call convention in the entities/*/model directory)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Validation Architecture → Test Map SC-1/SC-2)
  </read_first>
  <action>
    Create `src/entities/payment/model/split-payment-rpc.integration.test.ts` with a file-level `/* eslint-disable */` header (matching the process-refund integration test). Reuse the env-guard + `getServiceDb()`/`getAuthClient(name, pin)` helpers and the `itInt`/`itAuth` skip-when-no-env aliases copied from `process-refund-rpc.integration.test.ts`.

    Tests (all `itInt`/`itAuth`, skip gracefully when env absent):
    - SC-1 column check: query `information_schema.columns` for table `payments` and assert rows exist for `payment_group_id` (data_type uuid, is_nullable YES) and `split_index` (data_type smallint, is_nullable YES).
    - SC-2 happy path: seed a tab with items (service role), sign in as manager, call `process_split_payment_atomic` with 2 legs (cash + card) summing to the tab total; assert `ok=true`, `paymentIds.length === 2`, `paymentGroupId` present, both rows share the same `payment_group_id` with `split_index` 0 and 1, and the tab status flipped to `paid` (D-08 all-or-nothing close).
    - SC-2 sum mismatch: call with legs summing to total+5.00 → assert `ok=false` and `code === 'SPLIT_TOTAL_MISMATCH'` and no payment rows were inserted (atomic rollback).
    - SC-2 too many legs: call with 5 legs → assert `ok=false`, `code === 'TOO_MANY_LEGS'`.
    - SC-2 idempotent replay: call the same 2-leg request twice with the same idempotencyKey → second call returns `idempotent=true` with the same paymentGroupId and exactly 2 rows total (no double-charge — T-18-01).

    Add an `afterEach` cleanup that deletes seeded payments/orders/tabs by the test's tab id via service role (mirror the process-refund test cleanup).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/entities/payment/model/split-payment-rpc.integration.test.ts</automated>
  </verify>
  <done>Integration test file created and PARSES/RUNS. Without live-DB env the tests skip (itInt/itAuth guards) → exit 0. The green live run is gated in Plan 04 after the Plan 03 push makes the RPC live. NOTE: pre-push these tests skip; they are the SC-1/SC-2 behavioral gate.</done>
  <acceptance_criteria>
    - `npx vitest run src/entities/payment/model/split-payment-rpc.integration.test.ts` exits 0 (tests skip when env absent, do not error on import)
    - `grep -c "process_split_payment_atomic" src/entities/payment/model/split-payment-rpc.integration.test.ts` >= 3
    - `grep -c "SPLIT_TOTAL_MISMATCH\|TOO_MANY_LEGS\|idempotent" src/entities/payment/model/split-payment-rpc.integration.test.ts` >= 3
    - `grep -c "information_schema.columns" src/entities/payment/model/split-payment-rpc.integration.test.ts` >= 1 (SC-1 column existence)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge function (service-role) → process_split_payment_atomic | Deno service-role client passes cashier legs into the SECURITY DEFINER RPC; RPC re-validates all money invariants |
| RPC → payments/tabs | Atomic multi-row write + conditional tab-close in one transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering/DoS | process_split_payment_atomic | mitigate | Single PL/pgSQL transaction — all 1-4 INSERTs + conditional tab UPDATE succeed or roll back together (D-08); on close-race, DELETE all group legs before returning TAB_NOT_OPEN |
| T-18-02 | Tampering | sum validation | mitigate | Server-side `ABS(SUM(leg.amount) - p_expected_total) > 0.01` reject inside the transaction — authoritative over the client pre-check (D-05) |
| T-18-01 | Repudiation/Tampering | idempotency | mitigate | Per-leg derived keys `p_idempotency_key||'-leg'||i`; `-leg0` sentinel replay + unique_violation handler return the existing group, never a second charge |
| T-18-04 | Tampering | amount_positive CHECK | mitigate | RPC returns `EMPTY_LEG` for any `amount <= 0` before the INSERT hits the table CHECK (Pitfall 3) |
| T-18-05 | Elevation of Privilege | RPC grants | mitigate | REVOKE FROM PUBLIC + GRANT EXECUTE to service_role only; version guard P0V01/P0V02 re-raised (not swallowed) so client concurrency plumbing fires |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (18-RESEARCH.md) |
</threat_model>

<verification>
- Migration file grep gates pass (columns, indexes, RPC blocks present)
- Integration test file runs (skips without env; will green in Plan 04 post-push)
- No DROP of UNIQUE(tab_id) (already dropped upstream — D-10 stale)
</verification>

<success_criteria>
- payments +2 columns + 2 indexes defined (SC-1)
- process_split_payment_atomic: 1-4 legs, sum==total, atomic all-or-nothing, version guard, idempotent replay, audit (SC-2, D-05/D-06/D-08/D-10)
- Live integration test scaffold in place covering SC-1/SC-2
</success_criteria>

<output>
Create `.planning/phases/18-split-payment-multi-method/18-02-SUMMARY.md` when done
</output>
