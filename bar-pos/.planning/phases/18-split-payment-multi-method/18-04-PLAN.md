---
phase: 18-split-payment-multi-method
plan: 04
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - supabase/functions/process-split-payment/index.ts
autonomous: true
requirements: [SC-2]
user_setup: []

must_haves:
  truths:
    - "POST /functions/v1/process-split-payment verifies the JWT, calls process_split_payment_atomic, and returns one ReceiptData per leg (D-09)"
    - "The single-payment edge function (process-payment) is untouched (D-11)"
    - "The live-Supabase integration test for the split RPC is green (SC-1 + SC-2 confirmed against the pushed schema)"
  artifacts:
    - path: "supabase/functions/process-split-payment/index.ts"
      provides: "New Deno edge function mirroring process-payment structure"
      contains: "process_split_payment_atomic"
      min_lines: 150
  key_links:
    - from: "supabase/functions/process-split-payment/index.ts"
      to: "process_split_payment_atomic"
      via: "admin.rpc call"
      pattern: "process_split_payment_atomic"
    - from: "supabase/functions/process-split-payment/index.ts"
      to: "payments/tabs/orders/pool_sessions"
      via: "per-leg receipt assembly queries"
      pattern: "receipts"
---

<objective>
Ship and deploy the `process-split-payment` Deno edge function — a structural mirror of `process-payment/index.ts` that JWT-verifies, calls `process_split_payment_atomic`, and assembles one `ReceiptData` per leg (D-09). Then run the live-Supabase integration test (authored in Plan 02) green against the now-pushed schema, confirming SC-1/SC-2 end to end.

Purpose: A NEW edge function (not an extension of `process-payment`) guarantees D-11 by construction — the well-tested single-payment path keeps its exact `BodySchema`/superRefine with zero risk of regression (18-RESEARCH.md Assumption A1).

Output: New edge function file, deployed to remote; green split-RPC integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-split-payment-multi-method/18-RESEARCH.md
@.planning/phases/18-split-payment-multi-method/18-PATTERNS.md
@supabase/functions/process-payment/index.ts
</context>

## Artifacts this phase produces

Created by THIS plan: `supabase/functions/process-split-payment/index.ts` (new edge function) + its deployed remote state. Upstream symbols (RPC, columns) authored/applied in Plans 02-03; the integration test file (run green here) authored in Plan 02.

<tasks>

<task type="auto">
  <name>Task 1: Write process-split-payment edge function (mirror process-payment)</name>
  <read_first>
    - supabase/functions/process-payment/index.ts (FULL file, 314 lines — copy structure end to end: BodySchema+superRefine, corsHeaders, jsonResponse, statusForCode, Deno.serve handler, ES256 JWT verify via /auth/v1/user, admin.rpc call, receipt assembly queries)
    - .planning/phases/18-split-payment-multi-method/18-PATTERNS.md (section "supabase/functions/process-split-payment/index.ts" — the exact per-section adaptations)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Architecture Patterns diagram — edge fn responsibilities; Pattern receipt-assembly-once-per-leg)
  </read_first>
  <action>
    Create `supabase/functions/process-split-payment/index.ts`. Copy `process-payment/index.ts` verbatim as the starting skeleton, then adapt:

    - Imports: keep the exact pins `https://esm.sh/@supabase/supabase-js@2.49.1` and `https://deno.land/x/zod@v3.23.8/mod.ts` (do NOT bump — must match the deployed process-payment function).
    - `BodySchema`: replace the flat payment fields with `{ tabId: z.string().uuid(), legs: z.array(legSchema).min(1).max(4), expectedTotal: z.number().nonnegative().multipleOf(0.01), idempotencyKey: z.string().min(1).max(255), discountScope/discountType/discountValue/discountAmount optional }` where `legSchema` = `z.object({ method: z.enum(['cash','card','rappi']), amount: z.number().nonnegative().multipleOf(0.01), tipAmount: z.number().nonnegative().multipleOf(0.01), tenderedAmount: ...nullable().optional(), referenceNumber: z.string().max(64).nullable().optional(), rappiOrderId: z.string().max(128).nullable().optional() }).superRefine(...)` carrying the same per-method rules as process-payment's superRefine (cash requires tendered, non-cash forbids tendered, rappi requires rappiOrderId). Add a top-level superRefine asserting `Math.abs(sum(legs.amount) - expectedTotal) <= 0.01`.
    - Keep `corsHeaders`, `jsonResponse`, and `statusForCode` verbatim; extend `statusForCode` so `SPLIT_TOTAL_MISMATCH`/`TOO_MANY_LEGS`/`EMPTY_LEG` return 409 (grouped with the existing 409 validation codes).
    - Keep the Deno.serve OPTIONS/POST guards, env checks, and the ES256 `/auth/v1/user` JWT verification block VERBATIM (do not use admin.auth.getUser()).
    - Swap the RPC call to `admin.rpc('process_split_payment_atomic', { p_tab_id: body.tabId, p_staff_id: authUser.id, p_legs: body.legs, p_expected_total: body.expectedTotal, p_idempotency_key: body.idempotencyKey, p_discount_scope: body.discountScope ?? null, p_discount_type: body.discountType ?? null, p_discount_value: body.discountValue ?? null, p_discount_amount: body.discountAmount ?? null })`. Type the RPC result as `{ ok: boolean; idempotent?: boolean; paymentGroupId?: string; paymentIds?: string[]; code?: string; message?: string }`; on `!rpc.ok || !rpc.paymentGroupId` return `{ success: false, error: { code, message } }` with `statusForCode(code)`.
    - Receipt assembly: run the items query (orders/order_items/products, skipping voided) and pool_sessions query ONCE for the tab (18-PATTERNS.md: item/pool computation reused across legs). Then fetch all payment rows for the group: `admin.from('payments').select('id, amount, tip_amount, method, processed_at, tendered_amount, reference_number, split_index').eq('payment_group_id', rpc.paymentGroupId).order('split_index')`. Build `receipts: ReceiptData[]` — one per leg — where the shared `items` array + `barName`/`barAddress`/`cashierName`/`customerName` are identical across legs, and `subtotal`/`total`/`tipAmount`/`tenderedAmount`/`changeAmount`/`terminalReference`/`receiptNumber` (`paymentId.slice(0,8).toUpperCase()`) differ per leg row. For each leg: `subtotal = leg.amount`, `tipAmount = leg.tip_amount`, `total = round(leg.amount + leg.tip_amount, 2)`, `changeAmount = leg.tendered_amount != null ? round(leg.tendered_amount - total, 2) : null`.
    - Return `jsonResponse({ success: true, paymentGroupId: rpc.paymentGroupId, paymentIds: rpc.paymentIds, receipts, idempotent: rpc.idempotent === true })`.

    Do NOT modify `supabase/functions/process-payment/index.ts` (D-11).
  </action>
  <verify>
    <automated>cd bar-pos && grep -c "process_split_payment_atomic\|/auth/v1/user\|legs: z.array\|receipts" supabase/functions/process-split-payment/index.ts</automated>
  </verify>
  <done>New edge function exists mirroring process-payment: JWT verify + process_split_payment_atomic call + per-leg receipts[] assembly; process-payment untouched. Grep count >= 4.</done>
  <acceptance_criteria>
    - `grep -c "admin.rpc('process_split_payment_atomic'" supabase/functions/process-split-payment/index.ts` == 1
    - `grep -c "esm.sh/@supabase/supabase-js@2.49.1" supabase/functions/process-split-payment/index.ts` == 1 (pin matches process-payment)
    - `grep -c "/auth/v1/user" supabase/functions/process-split-payment/index.ts` == 1 (ES256 workaround preserved)
    - `grep -c "\.min(1)\.max(4)" supabase/functions/process-split-payment/index.ts` >= 1 (1-4 legs, D-02)
    - `git diff supabase/functions/process-payment/index.ts` is EMPTY (D-11)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Deploy edge function + run split-RPC integration test green</name>
  <read_first>
    - supabase/functions/process-split-payment/index.ts (the function to deploy)
    - src/entities/payment/model/split-payment-rpc.integration.test.ts (the integration test to green — authored in Plan 02)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Environment Availability — Supabase CLI available; SUPABASE_ACCESS_TOKEN for non-TTY deploy)
  </read_first>
  <action>
    Deploy the new function: `cd bar-pos && npx supabase functions deploy process-split-payment` (set `SUPABASE_ACCESS_TOKEN` if the CLI needs non-interactive auth; if an auth error occurs, surface it as an auth checkpoint per the standard auth-gate flow, then retry). This is required because the E2E (Plan 06) hits the real deployed function.

    Then run the live integration test with the E2E env loaded: `npx vitest run src/entities/payment/model/split-payment-rpc.integration.test.ts` (requires `VITE_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` + `VITE_SUPABASE_ANON_KEY` + `E2E_MANAGER_NAME`/`E2E_MANAGER_PIN` from `.env.local`). All `itInt`/`itAuth` tests must now execute (not skip) and pass against the pushed RPC — this is the authoritative SC-1/SC-2 behavioral gate. If any test fails, fix the RPC (Plan 02 migration) — DO NOT weaken the test.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/entities/payment/model/split-payment-rpc.integration.test.ts</automated>
  </verify>
  <done>process-split-payment deployed to remote; split-RPC integration test runs against live DB and all cases pass (columns exist, 2-leg happy path closes tab, sum-mismatch rejects, 5-leg rejects, idempotent replay returns same group).</done>
  <acceptance_criteria>
    - `npx supabase functions deploy process-split-payment` reports success (function listed in remote deploy output)
    - `npx vitest run src/entities/payment/model/split-payment-rpc.integration.test.ts` exits 0 with tests EXECUTED (not skipped) when E2E env present — confirm at least the happy-path + sum-mismatch + idempotent cases report as passed
    - The happy-path test asserts tab status flipped to `paid` and 2 rows share one payment_group_id with split_index 0/1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → POST /functions/v1/process-split-payment | Bearer user-JWT crosses; function verifies via /auth/v1/user before any DB write |
| edge function → process_split_payment_atomic (service-role) | Function elevates via service-role key only after JWT verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing | process-split-payment JWT verify | mitigate | ES256 `/auth/v1/user` verification copied verbatim from process-payment; 401 on invalid session before RPC |
| T-18-02 | Tampering | edge BodySchema | mitigate | Two-layer validation: Deno Zod pre-check (legs 1-4, sum≈expectedTotal) then authoritative RPC re-validation |
| T-18-08 | Information Disclosure | receipt assembly | mitigate | `terminalReference` only surfaced from stored `reference_number`, never logged raw (matches process-payment convention) |
| T-18-SC | Tampering | Deno import pins | accept | Reuses already-deployed esm.sh/deno.land pins from process-payment (no new package provenance introduced) |
</threat_model>

<verification>
- Edge function file grep gates pass; process-payment untouched (D-11)
- Function deployed to remote
- split-RPC integration test green against live DB (SC-1 + SC-2 authoritative gate)
</verification>

<success_criteria>
- process-split-payment edge function live on remote, mirrors process-payment (SC-2)
- Per-leg receipts[] returned (D-09)
- Integration test green: columns + atomic 1-4 leg RPC + sum-match + idempotency (SC-1, SC-2)
</success_criteria>

<output>
Create `.planning/phases/18-split-payment-multi-method/18-04-SUMMARY.md` when done
</output>
