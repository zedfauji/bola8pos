---
phase: 18-split-payment-multi-method
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-1]
user_setup: []

must_haves:
  truths:
    - "The 20260707000003 migration is applied to the live remote Supabase (payment_group_id + split_index columns + process_split_payment_atomic RPC exist in the DB)"
    - "supabase.types.ts reflects payments.payment_group_id + payments.split_index in Row/Insert/Update"
  artifacts:
    - path: "src/shared/lib/supabase.types.ts"
      provides: "payments type block extended with payment_group_id + split_index"
      contains: "payment_group_id"
  key_links:
    - from: "supabase.types.ts payments.Row"
      to: "live payments table columns"
      via: "manual transcription (Docker/type-gen unavailable)"
      pattern: "payment_group_id"
---

<objective>
Apply the Phase 18 schema/RPC migration to the live remote Supabase and reflect the two new `payments` columns in the generated types file. This is the [BLOCKING] gate: build + typecheck pass WITHOUT the push (types come from config, not the DB), which would create a false-positive verification state — so the push must run before any live-DB verification (Plan 04 integration green, Plan 06 E2E).

Purpose: Make `process_split_payment_atomic` callable and the new columns writable in the real database, then extend `supabase.types.ts` so downstream TypeScript sees `payment_group_id`/`split_index`.

Output: Applied migration (remote) + extended `supabase.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-split-payment-multi-method/18-RESEARCH.md
@supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql
</context>

## Artifacts this phase produces

Created by THIS plan: the applied (live) state of `payments.payment_group_id`, `payments.split_index`, `process_split_payment_atomic`; and the `supabase.types.ts` payments-block extension. Upstream symbols (migration file, RPC) were authored in Plan 02.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration to remote Supabase (supabase db push)</name>
  <read_first>
    - supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql (the migration being applied)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Environment Availability — Supabase CLI 2.91.1 available; Docker unavailable; SUPABASE_ACCESS_TOKEN for non-TTY)
    - STATE.md decision "[Phase 17-modifier-inventory-rules 17-03]" (prior single `npx supabase db push` precedent, no CLI splitter needed)
  </read_first>
  <action>
    Run `cd bar-pos && npx supabase db push` to apply `supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` (Plan 02 — payments +2 columns + 2 indexes + process_split_payment_atomic RPC) to the live remote project. Set the `SUPABASE_ACCESS_TOKEN` env var first if the CLI needs non-interactive auth. If the push requires an interactive DB-password prompt that cannot be suppressed, pause and have the human run the push. After the push reports success, execute the 3 verification queries in <how-to-verify> (via the Supabase MCP `execute_sql` tool or `psql`) and confirm all pass before resuming. Do NOT skip the push — build/typecheck pass without it (types come from config, not the DB), which would be a false-positive verification state.
  </action>
  <what-built>
    Plan 02 authored `supabase/migrations/20260707000003_split_payment_columns_and_rpc.sql` (payments +2 columns + 2 indexes + process_split_payment_atomic RPC). This task applies it to the live remote project via `cd bar-pos && npx supabase db push` (set `SUPABASE_ACCESS_TOKEN` env var if the CLI prompts non-interactively). If push requires an interactive DB-password prompt that cannot be suppressed, the executor pauses here for the human to run the push.
  </what-built>
  <how-to-verify>
    After `npx supabase db push` reports the migration applied, run these 3 verification queries against remote (via the Supabase MCP `execute_sql` tool or `psql`):
    1. Columns live: `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='payments' AND column_name IN ('payment_group_id','split_index');` → expect 2 rows (uuid/YES, smallint/YES).
    2. Indexes live: `SELECT indexname FROM pg_indexes WHERE tablename='payments' AND indexname IN ('idx_payments_payment_group_id','idx_payments_group_split_unique');` → expect 2 rows.
    3. RPC live: `SELECT proname FROM pg_proc WHERE proname='process_split_payment_atomic';` → expect 1 row; optionally `pg_get_functiondef` contains `'-leg'` and `P0V01`.
    Confirm all 3 queries return the expected results, then type "approved".
  </how-to-verify>
  <verify>
    <automated>MISSING — live-DB apply verified by the 3 queries above; the automated behavioral gate is the Plan 04 integration-test green run (src/entities/payment/model/split-payment-rpc.integration.test.ts) which cannot pass until this push completes.</automated>
  </verify>
  <done>Migration applied to remote; all 3 verification queries pass; human approved.</done>
  <acceptance_criteria>
    - `information_schema.columns` query returns payment_group_id (uuid, YES) + split_index (smallint, YES)
    - `pg_indexes` query returns both idx_payments_payment_group_id + idx_payments_group_split_unique
    - `pg_proc` query returns process_split_payment_atomic
  </acceptance_criteria>
  <resume-signal>Type "approved" once the 3 verification queries confirm columns + indexes + RPC are live, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend supabase.types.ts payments block (manual — Docker unavailable)</name>
  <read_first>
    - src/shared/lib/supabase.types.ts (lines 864-925 — the payments Row/Insert/Update block)
    - .planning/phases/18-split-payment-multi-method/18-RESEARCH.md (Pitfall 5 — Docker/type-gen unavailable, manual transcription fallback per CLAUDE.md)
    - STATE.md decision "[Phase 17-modifier-inventory-rules 17-03]" (manual type-extension precedent)
  </read_first>
  <action>
    Attempt `cd bar-pos && npx supabase gen types typescript --local > src/shared/lib/supabase.types.ts` first. If it fails (Docker unavailable — expected per 18-RESEARCH.md Pitfall 5; the command overwrites the file with error text, so restore from git if that happens: `git checkout src/shared/lib/supabase.types.ts`), fall back to manual transcription.

    Manual fallback: in the `payments:` block (starts line 864), add `payment_group_id: string | null` and `split_index: number | null` to the `Row` object (alphabetical-ish placement near the other nullable columns, e.g. after `method` / before `processed_at` — match the existing field ordering style), and `payment_group_id?: string | null` + `split_index?: number | null` to BOTH the `Insert` and `Update` objects (optional since both DB columns are nullable with no default). Do not alter any other table block.
  </action>
  <verify>
    <automated>cd bar-pos && grep -c "payment_group_id" src/shared/lib/supabase.types.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tab/model/queries\|agent/rag" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>payments Row/Insert/Update carry payment_group_id + split_index; typecheck shows no NEW errors.</done>
  <acceptance_criteria>
    - `grep -c "payment_group_id: string | null" src/shared/lib/supabase.types.ts` >= 1 (Row)
    - `grep -c "split_index" src/shared/lib/supabase.types.ts` >= 3 (Row + Insert + Update)
    - `npx tsc --noEmit` shows no NEW errors beyond the 2 pre-existing (tab/model/queries.ts, agent/rag.ts)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration file → remote Supabase | `supabase db push` mutates the production schema; gated behind a blocking human-verified checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | remote schema push | mitigate | Blocking human-verify checkpoint with 3 explicit post-push verification queries before proceeding; migration is additive + idempotent (IF NOT EXISTS) |
| T-18-07 | Information Disclosure | supabase.types.ts drift | accept | Manual transcription (Docker unavailable) risks drift vs. generated types; mitigated by the 2-field scope + typecheck gate; re-generate when Docker returns (CLAUDE.md follow-up) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase |
</threat_model>

<verification>
- 3 remote verification queries pass (columns + indexes + RPC live)
- supabase.types.ts carries both new fields; typecheck clean (no new errors)
</verification>

<success_criteria>
- Migration 20260707000003 applied to remote Supabase (SC-1 live)
- supabase.types.ts reflects payment_group_id + split_index
</success_criteria>

<output>
Create `.planning/phases/18-split-payment-multi-method/18-03-SUMMARY.md` when done
</output>
