---
phase: 08-polish-reports-e2e-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - bar-pos/src/features/export-report/model/useExportReport.ts
  - bar-pos/src/features/export-report/ui/ExportButtons.tsx
  - bar-pos/src/shared/lib/exporters/excel.ts
  - bar-pos/src/shared/lib/exporters/pdf.tsx
  - bar-pos/src/pages/reports/index.tsx
autonomous: true
requirements:
  - S6-07
  - S6-08

must_haves:
  truths:
    - "ExportType union has 9 new variants: combo-mix-excel, combo-mix-pdf, recipe-variance-excel, recipe-variance-pdf, waitlist-analytics-excel, waitlist-analytics-pdf, refunds-register-excel, refunds-register-pdf, combo-overrides-excel"
    - "ExportButtons Props union has 5 new branches: combo-mix, recipe-variance, waitlist-analytics, refunds-register, combo-overrides"
    - "excel.ts has 5 new workbook builder functions exported"
    - "pdf.tsx has 4 new PDF builder functions exported (combo-overrides Excel only, no PDF)"
    - "ReportsPage has 5 new TabsTrigger + TabsContent entries appended after existing 7 tabs"
    - "No as-never casts remain in widget files after this plan"
  artifacts:
    - path: "bar-pos/src/features/export-report/model/useExportReport.ts"
      provides: "Extended ExportType union with 9 new variants"
      contains: "combo-mix-excel"
    - path: "bar-pos/src/features/export-report/ui/ExportButtons.tsx"
      provides: "5 new Props union branches"
      contains: "reportType: 'combo-mix'"
    - path: "bar-pos/src/pages/reports/index.tsx"
      provides: "5 new report tabs in ReportsPage"
      contains: "TabsTrigger value=\"combos\""
  key_links:
    - from: "ExportButtons.tsx reportType='combo-mix'"
      to: "useExportReport type='combo-mix-excel'"
      via: "handleExport dispatches to exportReport('combo-mix-excel', ...)"
      pattern: "combo-mix-excel"
    - from: "ReportsPage combos tab"
      to: "ComboMixReport widget"
      via: "import { ComboMixReport } from '@widgets/ComboMixReport'"
      pattern: "ComboMixReport"
---

<objective>
Wave 5: extend the export infrastructure (ExportType + ExportButtons + workbook builders) and wire all 5 new report widgets into ReportsPage as new tabs. Also removes the temporary `as never` casts added in Plan 08-03.

Purpose: Make the analytics widgets fully accessible to operators via the /reports page with working Excel/PDF export.
Output: Fully wired ReportsPage with 12 tabs; working export for all 5 new report types.
</objective>

<execution_context>
@C:/Users/giris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/giris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/giris/Documents/Code/Redesign-POS/More\ POS/bola8pos-kiro/.planning/phases/08-polish-reports-e2e-hardening/08-RESEARCH.md
@C:/Users/giris/Documents/Code/Redesign-POS/More\ POS/bola8pos-kiro/.planning/phases/08-polish-reports-e2e-hardening/08-UI-SPEC.md

<interfaces>
<!-- Current ExportType union — from bar-pos/src/features/export-report/model/useExportReport.ts -->
```typescript
export type ExportType =
  | 'caja-excel' | 'caja-pdf'
  | 'products-excel' | 'products-pdf'
  | 'hourly-excel' | 'hourly-pdf'
  | 'voids-excel' | 'voids-pdf'
  | 'categories-excel' | 'categories-pdf'
  | 'staff-excel' | 'staff-pdf'
  | 'tips-excel' | 'tips-pdf';
// Add 9 new variants here
```

<!-- Current ExportButtons Props union — from bar-pos/src/features/export-report/ui/ExportButtons.tsx -->
```typescript
type Props = CajaProps | ProductsProps | HourlyProps | VoidsProps | CategoriesProps | StaffProps | TipsProps;
// Add 5 new prop types here
```

<!-- Current switch statement in useExportReport.ts -->
```typescript
switch (type) {
  case 'caja-excel': { ... break; }
  // ... 14 existing cases
  default: {
    const never: never = type;
    throw new Error(`Unknown export type: ${String(never)}`);
  }
}
```

<!-- Current excel.ts export list -->
```typescript
// Already exported: cajaReportToWorkbook, productSalesToWorkbook, hourlySalesToWorkbook,
// voidRefundToWorkbook, categoryRevenueToWorkbook, staffMetricsToWorkbook, staffTipsToWorkbook,
// workbookToBytes
// Add: comboMixToWorkbook, recipeVarianceToWorkbook, waitlistMetricsToWorkbook,
//       refundsRegisterToWorkbook, comboOverridesToWorkbook
```

<!-- Current ReportsPage tab structure -->
```typescript
// 7 existing tabs: session, products, hourly, voids, categories, staff, tips
// Add 5 more: combos, variance, waitlist, refunds-reg, overrides
// TabsList will have 12 triggers total — add flex-wrap if overflow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ExportType + ExportButtons + workbook builders</name>
  <files>
    bar-pos/src/features/export-report/model/useExportReport.ts,
    bar-pos/src/features/export-report/ui/ExportButtons.tsx,
    bar-pos/src/shared/lib/exporters/excel.ts,
    bar-pos/src/shared/lib/exporters/pdf.tsx
  </files>
  <read_first>
    - bar-pos/src/features/export-report/model/useExportReport.ts (read full file — understand ExportType union, switch cases, imports)
    - bar-pos/src/features/export-report/ui/ExportButtons.tsx (read full file — understand Props union, handleExport dispatch logic)
    - bar-pos/src/shared/lib/exporters/excel.ts (read full file — understand workbookToBytes, moneyCell, XLSX.utils.aoa_to_sheet pattern used in existing builders)
    - bar-pos/src/shared/lib/exporters/pdf.tsx (read the first 80 lines — understand the PDF builder function signature pattern)
  </read_first>
  <action>
**Step 1 — Extend ExportType union in useExportReport.ts.**

Add 9 new variants to the ExportType union:
```typescript
export type ExportType =
  | 'caja-excel' | 'caja-pdf'
  | 'products-excel' | 'products-pdf'
  | 'hourly-excel' | 'hourly-pdf'
  | 'voids-excel' | 'voids-pdf'
  | 'categories-excel' | 'categories-pdf'
  | 'staff-excel' | 'staff-pdf'
  | 'tips-excel' | 'tips-pdf'
  | 'combo-mix-excel' | 'combo-mix-pdf'
  | 'recipe-variance-excel' | 'recipe-variance-pdf'
  | 'waitlist-analytics-excel' | 'waitlist-analytics-pdf'
  | 'refunds-register-excel' | 'refunds-register-pdf'
  | 'combo-overrides-excel';
```

Add 5 new context type aliases after the existing context types:
```typescript
type ComboMixContext = { rows: ComboMixRow[]; dateRange: { from: Date; to: Date } };
type RecipeVarianceContext = { rows: RecipeVarianceRow[]; dateRange: { from: Date; to: Date } };
type WaitlistMetricsContext = { rows: WaitlistMetricsRow[]; dateRange: { from: Date; to: Date } };
type RefundRegisterContext = { rows: RefundRegisterRow[]; dateRange: { from: Date; to: Date } };
type ComboOverridesContext = { rows: ComboOverrideRow[]; dateRange: { from: Date; to: Date } };
```

Add corresponding imports from `@shared/lib/domain` and from the new excel/pdf builder functions.

Add 9 new overload signatures to the `exportReport` function (before the implementation overload):
```typescript
async function exportReport(type: 'combo-mix-excel' | 'combo-mix-pdf', data: ComboMixContext): Promise<Result<void>>;
async function exportReport(type: 'recipe-variance-excel' | 'recipe-variance-pdf', data: RecipeVarianceContext): Promise<Result<void>>;
async function exportReport(type: 'waitlist-analytics-excel' | 'waitlist-analytics-pdf', data: WaitlistMetricsContext): Promise<Result<void>>;
async function exportReport(type: 'refunds-register-excel' | 'refunds-register-pdf', data: RefundRegisterContext): Promise<Result<void>>;
async function exportReport(type: 'combo-overrides-excel', data: ComboOverridesContext): Promise<Result<void>>;
```

Add 9 new switch cases BEFORE the `default` case:
```typescript
case 'combo-mix-excel': {
  const ctx = data as ComboMixContext;
  const wb = comboMixToWorkbook(ctx.rows, ctx.dateRange);
  bytes = workbookToBytes(wb);
  break;
}
case 'combo-mix-pdf': {
  const ctx = data as ComboMixContext;
  bytes = await comboMixToPdfBytes(ctx.rows, ctx.dateRange);
  break;
}
case 'recipe-variance-excel': {
  const ctx = data as RecipeVarianceContext;
  const wb = recipeVarianceToWorkbook(ctx.rows, ctx.dateRange);
  bytes = workbookToBytes(wb);
  break;
}
case 'recipe-variance-pdf': {
  const ctx = data as RecipeVarianceContext;
  bytes = await recipeVarianceToPdfBytes(ctx.rows, ctx.dateRange);
  break;
}
case 'waitlist-analytics-excel': {
  const ctx = data as WaitlistMetricsContext;
  const wb = waitlistMetricsToWorkbook(ctx.rows, ctx.dateRange);
  bytes = workbookToBytes(wb);
  break;
}
case 'waitlist-analytics-pdf': {
  const ctx = data as WaitlistMetricsContext;
  bytes = await waitlistMetricsToPdfBytes(ctx.rows, ctx.dateRange);
  break;
}
case 'refunds-register-excel': {
  const ctx = data as RefundRegisterContext;
  const wb = refundsRegisterToWorkbook(ctx.rows, ctx.dateRange);
  bytes = workbookToBytes(wb);
  break;
}
case 'refunds-register-pdf': {
  const ctx = data as RefundRegisterContext;
  bytes = await refundsRegisterToPdfBytes(ctx.rows, ctx.dateRange);
  break;
}
case 'combo-overrides-excel': {
  const ctx = data as ComboOverridesContext;
  const wb = comboOverridesToWorkbook(ctx.rows, ctx.dateRange);
  bytes = workbookToBytes(wb);
  break;
}
```

**Step 2 — Extend ExportButtons Props union in ExportButtons.tsx.**

Add 5 new Props types after the existing TipsProps:
```typescript
type ComboMixProps = {
  reportType: 'combo-mix';
  data: { rows: ComboMixRow[]; dateRange: { from: Date; to: Date } };
};
type RecipeVarianceProps = {
  reportType: 'recipe-variance';
  data: { rows: RecipeVarianceRow[]; dateRange: { from: Date; to: Date } };
};
type WaitlistAnalyticsProps = {
  reportType: 'waitlist-analytics';
  data: { rows: WaitlistMetricsRow[]; dateRange: { from: Date; to: Date } };
};
type RefundsRegisterProps = {
  reportType: 'refunds-register';
  data: { rows: RefundRegisterRow[]; dateRange: { from: Date; to: Date } };
};
type ComboOverridesProps = {
  reportType: 'combo-overrides';
  data: { rows: ComboOverrideRow[]; dateRange: { from: Date; to: Date } };
};
```

Add these 5 to the `Props` union type.

Extend `handleExport` with 5 new `else if` branches:
```typescript
} else if (props.reportType === 'combo-mix') {
  const type = format === 'excel' ? 'combo-mix-excel' : 'combo-mix-pdf';
  await exportReport(type, props.data);
} else if (props.reportType === 'recipe-variance') {
  const type = format === 'excel' ? 'recipe-variance-excel' : 'recipe-variance-pdf';
  await exportReport(type, props.data);
} else if (props.reportType === 'waitlist-analytics') {
  const type = format === 'excel' ? 'waitlist-analytics-excel' : 'waitlist-analytics-pdf';
  await exportReport(type, props.data);
} else if (props.reportType === 'refunds-register') {
  const type = format === 'excel' ? 'refunds-register-excel' : 'refunds-register-pdf';
  await exportReport(type, props.data);
} else if (props.reportType === 'combo-overrides') {
  // PDF not supported for overrides — only excel
  await exportReport('combo-overrides-excel', props.data);
}
```

Add imports for the 5 new domain types to ExportButtons.tsx.

**Step 3 — Add 5 workbook builders to excel.ts.**

Read the existing `excel.ts` to understand `moneyCell`, `XLSX.utils.aoa_to_sheet`, `workbookToBytes`. Then add these 5 builders after existing exports:

```typescript
// Phase 8 S6-08 workbook builders

export function comboMixToWorkbook(
  rows: ComboMixRow[],
  dateRange: { from: Date; to: Date }
): XLSX.WorkBook {
  const title = `Combo Mix ${dateRange.from.toLocaleDateString('es-MX')} – ${dateRange.to.toLocaleDateString('es-MX')}`;
  const header = ['Date', 'Combo', 'Units Sold', 'Gross Revenue', 'Avg Price', 'Overrides'];
  const data = [
    [title], [], header,
    ...rows.map(r => [r.date, r.comboName, r.qtySold, moneyCell(r.netRevenue), moneyCell(r.avgPrice), r.overrideCount]),
  ];
  const ws = XLSX.utils.aoa_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Combo Mix');
  return wb;
}

export function recipeVarianceToWorkbook(
  rows: RecipeVarianceRow[],
  dateRange: { from: Date; to: Date }
): XLSX.WorkBook {
  const title = `Recipe Variance ${dateRange.from.toLocaleDateString('es-MX')} – ${dateRange.to.toLocaleDateString('es-MX')}`;
  const header = ['Date', 'Ingredient', 'Theoretical Used', 'Physical Delta', 'Variance %'];
  const data = [
    [title], [], header,
    ...rows.map(r => [r.date, r.ingredientName, r.theoreticalUsed, r.physicalDelta, r.variancePct]),
  ];
  const ws = XLSX.utils.aoa_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Recipe Variance');
  return wb;
}

export function waitlistMetricsToWorkbook(
  rows: WaitlistMetricsRow[],
  dateRange: { from: Date; to: Date }
): XLSX.WorkBook {
  const title = `Waitlist Analytics ${dateRange.from.toLocaleDateString('es-MX')} – ${dateRange.to.toLocaleDateString('es-MX')}`;
  const header = ['Date', 'Parties Seated', 'Avg Quoted Wait (min)', 'Avg Actual Wait (min)', 'No-Show Rate (%)'];
  const data = [
    [title], [], header,
    ...rows.map(r => [r.date, r.partiesSeated, r.avgQuotedWait ?? '', r.avgActualWait ?? '', r.noShowRate ?? '']),
  ];
  const ws = XLSX.utils.aoa_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Waitlist Analytics');
  return wb;
}

export function refundsRegisterToWorkbook(
  rows: RefundRegisterRow[],
  dateRange: { from: Date; to: Date }
): XLSX.WorkBook {
  const title = `Refunds Register ${dateRange.from.toLocaleDateString('es-MX')} – ${dateRange.to.toLocaleDateString('es-MX')}`;
  const header = ['Date', 'Operator', 'Payment ID', 'Amount', 'Reason', 'Restock Count'];
  const data = [
    [title], [], header,
    ...rows.map(r => [
      new Date(r.date).toLocaleDateString('es-MX'),
      r.operatorName,
      r.originalPaymentId,
      moneyCell(r.amount),
      r.reason,
      r.restockCount,
    ]),
  ];
  const ws = XLSX.utils.aoa_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Refunds Register');
  return wb;
}

export function comboOverridesToWorkbook(
  rows: ComboOverrideRow[],
  dateRange: { from: Date; to: Date }
): XLSX.WorkBook {
  const title = `Combo Overrides ${dateRange.from.toLocaleDateString('es-MX')} – ${dateRange.to.toLocaleDateString('es-MX')}`;
  const header = ['Timestamp', 'Actor', 'Combo', 'Reason'];
  const data = [
    [title], [], header,
    ...rows.map(r => [
      new Date(r.ts).toLocaleString('es-MX'),
      r.actorName,
      r.comboName,
      r.reason ?? '',
    ]),
  ];
  const ws = XLSX.utils.aoa_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Combo Overrides');
  return wb;
}
```

Add necessary imports for the 5 domain types at the top of excel.ts.

**Step 4 — Add 4 PDF builders to pdf.tsx.**

Read the existing `pdf.tsx` to understand the function signature pattern (likely: `async function xxxToPdfBytes(rows, dateRange): Promise<Uint8Array>`). Add 4 new builders matching the pattern. For `combo-overrides`, no PDF builder is needed (Excel only). The PDF builders should output a simple single-page table matching the Excel column structure.

If jsPDF `autoTable` is used, follow the exact same call pattern as existing PDF builders.

**Step 5 — Update widget files to remove `as never` casts.**

Now that ExportButtons has the proper types, update the 5 widget files to use the real `reportType` prop:
- `ComboMixReport.tsx`: change `reportType={"combo-mix" as never}` to `reportType="combo-mix"`
- `RecipeVarianceReport.tsx`: change `reportType={"recipe-variance" as never}` to `reportType="recipe-variance"`
- `WaitlistAnalyticsReport.tsx`: change `reportType={"waitlist-analytics" as never}` to `reportType="waitlist-analytics"`
- `RefundsRegister.tsx`: change `reportType={"refunds-register" as never}` to `reportType="refunds-register"`
- `ComboOverrideReport.tsx`: change `reportType={"combo-overrides" as never}` to `reportType="combo-overrides"`

Run `cd bar-pos && npm run typecheck` — must exit 0 with no `as never` workaround needed.

Commit: `feat(08-04): extend ExportType + ExportButtons + workbook builders for Phase 8 reports`
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "combo-mix-excel" bar-pos/src/features/export-report/model/useExportReport.ts` returns a line
    - `grep -n "reportType: 'combo-mix'" bar-pos/src/features/export-report/ui/ExportButtons.tsx` returns a line
    - `grep -rn "as never" bar-pos/src/widgets/` returns nothing (all casts removed)
    - `grep -n "comboMixToWorkbook" bar-pos/src/shared/lib/exporters/excel.ts` returns a line
    - `cd bar-pos && npm run typecheck` exits 0
    - `cd bar-pos && npm run lint` exits 0
  </acceptance_criteria>
  <done>ExportType union extended; ExportButtons Props extended; 5 workbook builders added; 4 PDF builders added; as-never casts removed from widgets.</done>
</task>

<task type="auto">
  <name>Task 2: Wire 5 new tabs into ReportsPage</name>
  <files>
    bar-pos/src/pages/reports/index.tsx
  </files>
  <read_first>
    - bar-pos/src/pages/reports/index.tsx (read full file — see all 7 existing tab triggers + content, imports, dateRange state, handleDateChange pattern)
    - bar-pos/src/widgets/ComboMixReport/index.ts (confirm export name)
    - bar-pos/src/widgets/RecipeVarianceReport/index.ts (confirm export name)
    - bar-pos/src/widgets/WaitlistAnalyticsReport/index.ts (confirm export name)
    - bar-pos/src/widgets/RefundsRegister/index.ts (confirm export name)
    - bar-pos/src/widgets/ComboOverrideReport/index.ts (confirm export name)
  </read_first>
  <action>
**Step 1 — Add widget imports to ReportsPage.**

Add 5 new widget imports after the existing widget imports in `bar-pos/src/pages/reports/index.tsx`:
```typescript
import { ComboMixReport } from '@widgets/ComboMixReport';
import { RecipeVarianceReport } from '@widgets/RecipeVarianceReport';
import { WaitlistAnalyticsReport } from '@widgets/WaitlistAnalyticsReport';
import { RefundsRegister } from '@widgets/RefundsRegister';
import { ComboOverrideReport } from '@widgets/ComboOverrideReport';
```

Keep FSD import order: `@widgets/*` group must be before `@shared/*`.

**Step 2 — Add 5 new TabsTrigger entries.**

Find the `<TabsList className="mb-4">` block (currently with 7 triggers). Add 5 more triggers after "tips":
```tsx
<TabsTrigger value="combos">Combo Mix</TabsTrigger>
<TabsTrigger value="variance">Recipe Variance</TabsTrigger>
<TabsTrigger value="waitlist">Waitlist</TabsTrigger>
<TabsTrigger value="refunds-reg">Refunds Register</TabsTrigger>
<TabsTrigger value="overrides">Overrides</TabsTrigger>
```

Also add `flex-wrap` to TabsList to handle 12 total triggers on narrow Tauri windows:
```tsx
<TabsList className="mb-4 flex flex-wrap">
```

**Step 3 — Add 5 new TabsContent blocks.**

Append after the last existing `</TabsContent>` (tips tab) and before the closing `</Tabs>`:

```tsx
<TabsContent value="combos">
  <div className="space-y-4">
    <DateRangePicker fromStr={fromStr} toStr={toStr} onChange={handleDateChange} />
    <ComboMixReport dateRange={dateRange} />
  </div>
</TabsContent>

<TabsContent value="variance">
  <div className="space-y-4">
    <DateRangePicker fromStr={fromStr} toStr={toStr} onChange={handleDateChange} />
    <RecipeVarianceReport dateRange={dateRange} />
  </div>
</TabsContent>

<TabsContent value="waitlist">
  <div className="space-y-4">
    <DateRangePicker fromStr={fromStr} toStr={toStr} onChange={handleDateChange} />
    <WaitlistAnalyticsReport dateRange={dateRange} />
  </div>
</TabsContent>

<TabsContent value="refunds-reg">
  <div className="space-y-4">
    <DateRangePicker fromStr={fromStr} toStr={toStr} onChange={handleDateChange} />
    <RefundsRegister dateRange={dateRange} />
  </div>
</TabsContent>

<TabsContent value="overrides">
  <div className="space-y-4">
    <DateRangePicker fromStr={fromStr} toStr={toStr} onChange={handleDateChange} />
    <ComboOverrideReport dateRange={dateRange} />
  </div>
</TabsContent>
```

**Step 4 — Verify and commit.**

Run: `cd bar-pos && npm run typecheck && npm run lint && npm run test`

All must pass. Then commit: `feat(08-04): wire 5 analytics tabs into ReportsPage`
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint && grep -c "TabsTrigger" src/pages/reports/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TabsTrigger" bar-pos/src/pages/reports/index.tsx` returns `12` (7 existing + 5 new)
    - `grep -n "value=\"combos\"" bar-pos/src/pages/reports/index.tsx` returns a line
    - `grep -n "value=\"refunds-reg\"" bar-pos/src/pages/reports/index.tsx` returns a line
    - `grep -n "flex flex-wrap" bar-pos/src/pages/reports/index.tsx` returns a line (TabsList flex-wrap)
    - `cd bar-pos && npm run typecheck` exits 0
    - `cd bar-pos && npm run lint` exits 0
    - `cd bar-pos && npm run test` passes
  </acceptance_criteria>
  <done>ReportsPage has 12 tabs; 5 new widgets wired with DateRangePicker; typecheck + lint + test all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExportButtons → useExportReport → Tauri fs write | Export data flows from memory to disk via Tauri plugin-fs |
| reportType discriminated union | TypeScript enforces correct data shape per report type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | ExportButtons `canAccess(role, 'view_reports')` | mitigate | Existing RBAC gate in ExportButtons returns null for non-manager/admin roles — all 5 new reportType branches inherit this check automatically |
| T-08-04-02 | Tampering | CSV injection in Excel cells | accept | SheetJS writes all cells with `.t: 's'` (string type) — formula injection requires `.t: 'f'` which SheetJS never sets from `aoa_to_sheet` string data. Safe by default. |
| T-08-04-03 | Denial of Service | Large date range export | mitigate | `assertDateRangeValid` in query hooks prevents >365-day ranges before data is fetched, bounding export data size |
</threat_model>

<verification>
```bash
cd bar-pos && npm run typecheck && npm run lint && npm run test
```

After passing, verify tab count:
```bash
grep -c "TabsTrigger" bar-pos/src/pages/reports/index.tsx
# Must output: 12
```

Verify no `as never` casts remain:
```bash
grep -rn "as never" bar-pos/src/widgets/
# Must output nothing
```
</verification>

<success_criteria>
- ExportType has 23 total variants (14 existing + 9 new)
- ExportButtons Props union has 12 branches (7 existing + 5 new)
- 5 workbook builder functions in excel.ts
- 4 PDF builder functions in pdf.tsx (combo-overrides has no PDF)
- ReportsPage has 12 TabsTrigger entries with flex-wrap
- All `as never` casts removed from widget files
- typecheck + lint + test all green
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-reports-e2e-hardening/08-04-SUMMARY.md`
</output>
