---
phase: 10-ai-slob-technical-debt-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - knip.json
  - .jscpd.json
  - scripts/run-tech-debt-audit.sh
  - .gitignore
autonomous: true
# No requirement IDs: phase_req_ids is null and .planning/REQUIREMENTS.md does not
# exist in this project. Spec-less probe fallback was an explicit visible skip.
requirements: []

must_haves:
  truths:
    - "`npm run audit:tech-debt` exits 0 even when every underlying tool finds problems (D-01 full re-audit must never be blocked by a tool's findings-exit-code)."
    - "Running the audit produces one machine-readable report file per check under `.audit-tmp/`, each parseable without opening any source file (D-03)."
    - "knip, jscpd and madge are installed as devDependencies only — no runtime dependency, no production bundle change (D-04)."
    - "`npm run lint`, `npm run typecheck` and `npm run test` still exit 0 after the new devDependencies and config files land."
  artifacts:
    - "bar-pos/knip.json"
    - "bar-pos/.jscpd.json"
    - "bar-pos/scripts/run-tech-debt-audit.sh"
    - "package.json `scripts['audit:tech-debt']` entry"
    - "`.audit-tmp/` gitignore entry"
  key_links:
    - "package.json `audit:tech-debt` -> scripts/run-tech-debt-audit.sh (npm script wiring convention, package.json lines 25-27)"
    - "scripts/run-tech-debt-audit.sh -> .audit-tmp/*.json (every tool writes exactly one named report the later synthesis plans read)"
    - "knip.json / .jscpd.json ignore lists -> the same generated/vendor surfaces eslint.config.js already ignores (lines 19-30) plus graphify-out"
---

<objective>
Build the tech-debt audit pipeline: install the three new static-analysis devDependencies locked by D-04 (knip, jscpd, madge), configure them against this repo's existing ignore conventions, and wire them into one `npm run audit:tech-debt` entry point that writes machine-readable reports to a gitignored scratch directory.

Purpose: D-03 forbids Claude reading the codebase file-by-file to find structural debt. Every finding in this phase's deliverable must originate from a tool's structured output, so the tooling pipeline has to exist and be proven end-to-end before any synthesis work starts.

Output: `knip.json`, `.jscpd.json`, `scripts/run-tech-debt-audit.sh`, a `audit:tech-debt` npm script, a `.gitignore` entry for `.audit-tmp/`, and three new devDependencies in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-CONTEXT.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-PATTERNS.md
</context>

## Artifacts this phase produces

Newly created by Phase 10 (exclude these from drift/source-grounding verification — they do not exist before this phase):

| Artifact | Created in |
|---|---|
| `bar-pos/knip.json` | 10-01 Task 1 |
| `bar-pos/.jscpd.json` | 10-01 Task 2 |
| `bar-pos/scripts/run-tech-debt-audit.sh` | 10-01 Task 1 (extended in 10-01 T2/T3 and 10-02 T1) |
| `package.json` script `audit:tech-debt` | 10-01 Task 1 |
| `.gitignore` entry `.audit-tmp/` | 10-01 Task 1 |
| devDependencies `knip`, `jscpd`, `madge` | 10-01 Task 1 |
| `.audit-tmp/**` report files (untracked, never committed) | 10-02 Task 2 |
| `.planning/phases/10-ai-slob-technical-debt-checklist/10-CHECKLIST.md` | 10-03 Task 1 |

No `.madgerc` is created — madge is driven entirely by CLI flags from the audit script (PATTERNS.md explicitly allows "CLI flags only, no file"). A config file carrying the same three flags would be a second place to keep in sync for no gain.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end audit pipeline through knip only</name>
  <files>package.json, package-lock.json, knip.json, scripts/run-tech-debt-audit.sh, .gitignore</files>
  <read_first>
    - `package.json` — the `scripts` block (lines 6-33) for the existing wiring convention, and `devDependencies` (lines 86-137) to confirm knip/jscpd/madge are genuinely absent before installing
    - `.gitignore` — the "Dev scratch / one-off files" and "Local lint/type dumps" sections, which already establish the precedent for gitignoring throwaway analysis output
    - `eslint.config.js` lines 19-30 — the authoritative project ignore list (`node_modules`, `dist`, `build`, `target`, `src-tauri`, `src/shared/lib/supabase.types.ts`) that every new tool config must mirror
    - `tsconfig.json` lines 26-34 — the `@app/@pages/@widgets/@features/@entities/@shared` path aliases knip must resolve
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md` — "Package Legitimacy Audit", "Pattern 1", "Pattern 2", "Pitfall 1", "Pitfall 5"
  </read_first>
  <action>
Wire ONE tool (knip) through every layer of this phase's pipeline — dependency install, tool config, orchestration script, npm script wiring, gitignore, real report output — before adding any other tool. This is the architectural proof: if the scratch-directory convention, the exit-code-swallowing contract, or knip's alias/entry resolution is wrong, it is wrong after one commit instead of after seven.

Step 1 — package legitimacy re-verification before install. RESEARCH.md's automated legitimacy seam flagged `knip` and `jscpd` as `SUS` with reason `too-new`, and the researcher overrode both to OK after confirming the signal is computed from the *latest version's* publish date, not the package's first-publish date. Re-confirm that override mechanically rather than trusting the prose: run `npm view knip time.created`, `npm view jscpd time.created`, `npm view madge time.created` and assert each returned timestamp is earlier than 2024. Also run `npm view knip scripts.postinstall`, `npm view jscpd scripts.postinstall`, `npm view madge scripts.postinstall` and assert all three return empty. If any assertion fails, HALT and report — do not install.

Step 2 — install exactly the three packages locked by D-04, as devDependencies, at the versions RESEARCH.md verified: `npm install -D knip@^6.31.0 jscpd@^5.0.14 madge@^8.0.0`. Commit both `package.json` and `package-lock.json`. Do not add ts-prune, depcheck, unimported, or dependency-cruiser — D-04 chose knip and madge over those alternatives explicitly.

Step 3 — create `knip.json`. Start deliberately minimal: a `$schema` key pointing at `https://unpkg.com/knip@6/schema.json` and an `ignore` array only. Rely on knip's built-in plugin auto-detection for entry points (RESEARCH.md Pattern 2 — knip's Vite, Vitest, Storybook, Playwright, ESLint and Tailwind plugins auto-activate because this repo has `vite.config.ts`, `vitest.config.ts`, `.storybook/main.ts`, `playwright.config.ts`, `eslint.config.js` and `tailwind.config.ts`, and they supply the entry points and alias resolution without hand-listing). The `ignore` array must contain, at minimum: `src/shared/lib/supabase.types.ts` (generated), `src-tauri/**`, `dist/**`, `storybook-static/**`, `graphify-out/**`, `src/graphify-out/**`. The last two are untracked working-tree directories that are NOT covered by `.gitignore`, so no tool will skip them automatically (RESEARCH.md Pitfall 5). Escalation rule, applied only if needed: after the first run, if knip reports more than 80% of `src/**/*.tsx` files as unused, that is the signature of failed entry-point detection, not real debt — add an explicit `"entry": ["src/main.tsx"]` and `"project": ["src/**/*.{ts,tsx}"]` and re-run. Do not add ignore patterns to silence individual findings; suppressing findings is the opposite of this phase's purpose.

Step 4 — create `scripts/run-tech-debt-audit.sh`, executable (`chmod +x`), following the existing `scripts/setup-ubuntu.sh` precedent for a shell utility invoked via npm. Its contract:
  - Shebang `#!/usr/bin/env bash`.
  - `set -uo pipefail` — deliberately WITHOUT `-e`. This is the one script in this repo where a subprocess exiting non-zero is expected and must not abort the run: knip exits 1 when it finds issues, madge exits 1 when it finds cycles, eslint/vitest/playwright exit non-zero on failures. That exit code IS the audit signal. Every tool invocation is therefore suffixed with `|| true`. Add a comment at the top of the file explaining that this inversion is intentional so a future reader does not "fix" it back to `set -e`.
  - Resolve an `OUT` directory as `.audit-tmp` relative to the repo root (derive it from the script's own location, e.g. via `dirname "$0"`, so the script works regardless of the caller's cwd), and `mkdir -p "$OUT"`.
  - Two knip invocations, both writing into `$OUT`: default mode to `knip-report.json`, and production mode (`--production`) to `knip-production.json`, both with `--reporter json`. RESEARCH.md Open Question 2 resolved this as "run both" — default mode is the primary finding source, and the production-mode diff identifies which default-mode findings are Storybook/test-only surfaces rather than shipped-code debt, which is exactly the Medium-tier discriminator Plan 03 needs.
  - A final line echoing the resolved `$OUT` path so the operator knows where reports landed.
  Do not add tool invocations for jscpd, madge, eslint, tsc, vitest or playwright yet — those are Task 2, Task 3 and Plan 02.

Step 5 — add `"audit:tech-debt": "bash scripts/run-tech-debt-audit.sh"` to the `scripts` block in `package.json`, following the existing `seed:dev` / `setup:dev-users` convention of one npm entry point delegating to a file in `scripts/` rather than inlining a multi-step pipeline as an npm script string.

Step 6 — add `.audit-tmp/` to `.gitignore` under the existing "Dev scratch / one-off files" section. The report files are throwaway intermediates; only `10-CHECKLIST.md` is this phase's committed deliverable, and the raw reports embed source snippets that have no business in git history.

Step 7 — run `npm run audit:tech-debt` once and confirm both knip reports land and parse. Then confirm the pre-existing quality gates are unaffected. Baselines were measured on 2026-08-03 immediately before planning and all three were green (`typecheck` exit 0; `lint` exit 0; `test` 151 files / 1391 tests passed, exit 0), so these are genuine regression gates here, not aspirational ones.
  </action>
  <verify>
    <automated>npm run audit:tech-debt && jq -e 'type == "object" or type == "array"' .audit-tmp/knip-report.json > /dev/null && jq -e 'type == "object" or type == "array"' .audit-tmp/knip-production.json > /dev/null && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm view knip time.created`, `npm view jscpd time.created` and `npm view madge time.created` each return a timestamp earlier than `2024-01-01`, and `npm view <pkg> scripts.postinstall` returns empty output for all three.
    - `node -e "const p=require('./package.json');['knip','jscpd','madge'].forEach(k=>{if(!p.devDependencies[k])throw new Error(k)});if(['knip','jscpd','madge'].some(k=>p.dependencies[k]))throw new Error('runtime dep')"` exits 0 — all three are devDependencies and none leaked into `dependencies`.
    - `package-lock.json` is modified in the same commit as `package.json`.
    - `jq -e '.ignore | index("src/graphify-out/**") and index("graphify-out/**") and index("src/shared/lib/supabase.types.ts")' knip.json` exits 0.
    - `bash -n scripts/run-tech-debt-audit.sh` exits 0, and `test -x scripts/run-tech-debt-audit.sh` exits 0.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c 'set -uo pipefail'` returns `1`, and `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c 'set -e'` returns `0`.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c 'npx knip'` returns `2` (default mode and production mode).
    - `jq -e '.scripts["audit:tech-debt"]' package.json` exits 0 and its value invokes `scripts/run-tech-debt-audit.sh`.
    - `grep -c '^\.audit-tmp/$' .gitignore` returns `1`, and `git status --porcelain .audit-tmp` produces no output after a run.
    - `npm run audit:tech-debt` exits 0 even though knip itself exits non-zero when it reports findings.
    - `.audit-tmp/knip-report.json` and `.audit-tmp/knip-production.json` both exist and parse as JSON.
    - `npm run typecheck`, `npm run lint` and `npm run test` all still exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">
    Three devDependencies plus four config/script files, none imported by application code and none affecting the production bundle. Removal is `npm uninstall` plus deleting the files. D-04 and RESEARCH.md both independently rate this reversible; no decision checkpoint is warranted.
  </reversibility>
  <done>`npm run audit:tech-debt` runs the whole pipeline end-to-end for knip, writes two parseable JSON reports into a gitignored `.audit-tmp/`, exits 0 despite knip's own non-zero findings-exit, and the lint/typecheck/test baseline is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add jscpd and madge to the audit script</name>
  <files>.jscpd.json, scripts/run-tech-debt-audit.sh</files>
  <read_first>
    - `scripts/run-tech-debt-audit.sh` — as written by Task 1, so the new invocations match its existing `|| true` and `$OUT` conventions rather than inventing new ones
    - `knip.json` — as written by Task 1, so `.jscpd.json`'s ignore list stays consistent with it instead of drifting into a per-tool exclusion philosophy
    - `eslint.config.js` lines 19-30 (ignore list) and lines 70-77 (`boundaries/elements` layer definitions)
    - `tsconfig.json` lines 26-34 — the `paths` block madge reads via `--ts-config`
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md` — "Pattern 3", "Pattern 4", "Pitfall 2", "Pitfall 3"
  </read_first>
  <action>
Extend the proven pipeline to the other two D-04 tools. No structural change to the script — two more `|| true`-guarded invocations writing into `$OUT`.

Step 1 — jscpd v5 flag confirmation FIRST. jscpd 5.x is a Rust rewrite and most public documentation, blog posts and Stack Overflow answers describe the older v4 TypeScript engine with a partially different flag set (RESEARCH.md Pitfall 3). Run `npx jscpd --help` once and treat its output as ground truth. The flags RESEARCH.md confirmed against `docs/rust.md` are `--min-tokens`, `--min-lines`, `--reporters`, `--output`, `--ignore`, `--gitignore`, `--skip-local`, `--min-duplicated-lines`, `--silent`. If `--help` disagrees with any flag used below, follow `--help` and note the divergence in the plan summary.

Step 2 — create `.jscpd.json` carrying policy only (thresholds, minimums, reporter, ignores). Do not put the output path in the config file — the script passes `--output "$OUT/jscpd-out"` so there is exactly one place that knows where reports go. Contents: `threshold` 0, `minLines` 5, `minTokens` 50, `reporters` `["json"]`, `gitignore` true, and an `ignore` array covering `**/node_modules/**`, `**/dist/**`, `**/src-tauri/**`, `**/graphify-out/**`, `**/src/graphify-out/**`, `**/storybook-static/**`, `**/coverage/**`, `**/src/shared/lib/supabase.types.ts`, `**/*.test.ts`, `**/*.test.tsx`, `**/*.stories.tsx`. `gitignore: true` alone is not sufficient: `graphify-out/` and `src/graphify-out/` are untracked but not gitignored, so they must be listed explicitly (RESEARCH.md Pitfall 5). `threshold` 0 makes jscpd exit non-zero whenever any duplication exists — that is intended, the script's `|| true` absorbs it and the report is what matters.

Step 3 — append the jscpd invocation to `scripts/run-tech-debt-audit.sh`: `npx jscpd . --reporters json --output "$OUT/jscpd-out" || true`. Its report lands at `$OUT/jscpd-out/jscpd-report.json`.

Step 4 — append the madge invocation. madge is driven entirely by CLI flags; no `.madgerc` is created. Use `--circular --json`, `--ts-config tsconfig.json` (this resolves the six `@app/@pages/@widgets/@features/@entities/@shared` aliases directly from the existing `paths` block — no separate alias config is needed since this project has no webpack config), `--extensions ts,tsx`, and `--exclude '(graphify-out|supabase\.types\.ts|\.stories\.tsx|\.test\.tsx?)$'`, redirecting stdout to `$OUT/madge-circular.json`, suffixed with `|| true` (madge exits 1 when cycles are found).

Deviation from RESEARCH.md worth stating explicitly: point madge at the `src` directory, NOT at `src/main.tsx`. RESEARCH.md's skeleton used the `src/main.tsx` reachable graph, which is right for a build gate but wrong for an audit — D-01 mandates a full re-audit of the whole codebase including code that is no longer reachable, and a cycle inside committed-but-unreachable code is still debt this checklist must surface.

Step 5 — confirm the shape of `jscpd-report.json` empirically. RESEARCH.md Assumption A3 flags that jscpd v5's exact JSON field names were never confirmed from official docs. Inspect the report's top-level key names ONCE with `jq 'keys'` (and `jq '.statistics | keys'` if present) — do not read the whole file, and do not open any source file it references. Record the actual field names in the plan summary so Plan 02 and Plan 03 query the right paths.
  </action>
  <verify>
    <automated>npm run audit:tech-debt && jq -e 'type == "object"' .audit-tmp/jscpd-out/jscpd-report.json > /dev/null && jq -e 'type == "array" or type == "object"' .audit-tmp/madge-circular.json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npx jscpd --help` was run and its flag list reconciled against the flags used; any divergence is recorded in the plan summary.
    - `jq -e '.reporters | index("json")' .jscpd.json` exits 0 and `jq -e '.gitignore == true' .jscpd.json` exits 0.
    - `jq -e '.ignore | index("**/graphify-out/**") and index("**/src/graphify-out/**")' .jscpd.json` exits 0.
    - `jq -e 'has("output") | not' .jscpd.json` exits 0 — the output path lives only in the script.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c 'npx jscpd'` returns `1` and `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c 'npx madge'` returns `1`.
    - The madge line in the script contains `--circular`, `--json`, `--ts-config tsconfig.json` and `--extensions ts,tsx`, and its final positional argument is `src` (not `src/main.tsx`).
    - `test ! -f .madgerc` exits 0 — no redundant madge config file was created.
    - `npm run audit:tech-debt` exits 0; `.audit-tmp/jscpd-out/jscpd-report.json` and `.audit-tmp/madge-circular.json` both exist and parse as JSON.
    - The actual top-level key names of `jscpd-report.json` are recorded in the plan summary.
  </acceptance_criteria>
  <done>All three D-04 tools run from the single audit script and each writes a parseable report into `.audit-tmp/`; jscpd's real JSON schema is known rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Add the four existing quality gates to the audit script</name>
  <files>scripts/run-tech-debt-audit.sh</files>
  <read_first>
    - `scripts/run-tech-debt-audit.sh` — current state after Task 2
    - `package.json` lines 6-33 — the exact existing `lint`, `typecheck`, `test` and `test:e2e` command definitions these invocations must mirror
    - `playwright.config.ts` — confirms `headless: false` with `channel: 'chrome'`, which is why E2E needs a real display session
    - `vitest.config.ts` — confirms the `unit` project name used by the existing `test` script
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md` — "Pattern 1" (JSON-first invocation) and the "Don't Hand-Roll" row on parsing `tsc` output
  </read_first>
  <action>
Add the four pre-existing quality gates to the same script so one command produces the complete D-01 full re-audit. Each must emit machine-readable output — D-03 forbids eyeballing human-readable terminal text and then re-reading source to reconstruct context.

Append four `|| true`-guarded invocations to `scripts/run-tech-debt-audit.sh`, keeping the existing `$OUT` convention:

  - ESLint: mirror the existing `lint` script's scope and strictness (`eslint src --max-warnings 0`) but add `--format json`, writing to `$OUT/eslint-report.json`. Note that `eslint-report.json` at the repo root is already gitignored from an earlier era — write to `$OUT`, not the root, so the two do not collide.
  - TypeScript: `npx tsc --noEmit --pretty false` writing combined stdout+stderr to `$OUT/tsc-errors.txt`. `tsc` has no JSON reporter; `--pretty false` yields one stable `file(line,col): error TSxxxx: message` line per error, which a five-line regex can parse. Do not build a richer parser than that.
  - Vitest: mirror the existing `test` script's `--project unit` scope, with the JSON reporter writing to `$OUT/vitest-results.json`. Confirm the correct output-file mechanism against `npx vitest run --help` before writing the line — the gate is that the file exists and parses, regardless of whether that is achieved via an output-file flag or a stdout redirect.
  - Playwright: the E2E suite with the JSON reporter writing to `$OUT/playwright-results.json`. Confirm the correct output mechanism against `npx playwright test --help`; Playwright's documented approach is an environment variable naming the JSON output file rather than a stdout redirect, because other reporters and worker output share stdout. Again, the gate is a file that exists and parses.

Order the E2E invocation last in the script — it is by far the slowest step (several minutes) and every other report is already on disk if it is interrupted.

Do not change any of the four existing npm scripts. This phase re-runs them; it does not modify them.
  </action>
  <verify>
    <automated>bash -n scripts/run-tech-debt-audit.sh && test "$(grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -cE 'npx (eslint|tsc|vitest|playwright)')" = "4" && npm run lint && npm run typecheck && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/run-tech-debt-audit.sh` exits 0.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -cE 'npx (eslint|tsc|vitest|playwright)'` returns `4`.
    - The ESLint line contains `--format json`, `src` and `--max-warnings 0`, and its output path is under `$OUT`, not the repo root.
    - The tsc line contains `--noEmit` and `--pretty false` and redirects both stdout and stderr.
    - The vitest line contains `--project unit`.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c '|| true'` returns a count equal to the number of tool invocations in the file (currently 8: knip x2, jscpd, madge, eslint, tsc, vitest, playwright) — no invocation is left unguarded.
    - The playwright invocation is the last tool invocation in the file.
    - `npm run lint`, `npm run typecheck` and `npm run test` still exit 0 — this task changes only the audit script and must not touch the four existing npm scripts.
    - `git diff --stat HEAD~1 -- package.json` shows no change to the `lint`, `typecheck`, `test` or `test:e2e` script definitions.
  </acceptance_criteria>
  <done>`scripts/run-tech-debt-audit.sh` invokes all eight checks (knip default, knip production, jscpd, madge, eslint, tsc, vitest, playwright), every one guarded against its own findings-exit-code, and the script passes a syntax check. Executing the full run is Plan 02's job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Three new devDependencies and their transitive trees are fetched and executed locally during the audit |
| audit tooling → `.audit-tmp/` → git | Reports embed source paths and, for jscpd, verbatim source snippets; only a curated markdown deliverable should ever reach git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-SC | Tampering | `npm install -D knip jscpd madge` | high | mitigate | Task 1 Step 1 asserts `npm view <pkg> time.created` predates 2024 and `scripts.postinstall` is empty for all three, BEFORE installing; install halts on any failed assertion. Package names came from user decision D-04, not agent discovery. RESEARCH.md's `SUS (too-new)` seam verdict on knip/jscpd was overridden to OK on evidence the signal reads the latest release date, not first-publish date — this task re-derives that evidence mechanically rather than inheriting it as prose. `package-lock.json` is committed in the same commit, pinning the resolved tree. |
| T-10-01 | Information Disclosure | jscpd/knip/madge report output | medium | mitigate | Reports write only to `.audit-tmp/`, added to `.gitignore` in Task 1 Step 6; jscpd clone reports contain verbatim source fragments that must never enter git history. Only `10-CHECKLIST.md`, which cites `file:line` without source bodies, is committed. |
| T-10-02 | Information Disclosure | audit tooling telemetry | low | accept | None of knip/jscpd/madge is documented to phone home by default (RESEARCH.md Security Domain). Accepted: no telemetry-disabling config is added for tools that have no telemetry. If a future tool with telemetry is added, it must be disabled before running against this source. |
| T-10-03 | Tampering | `\|\| true` exit-code swallowing in the audit script | low | mitigate | Swallowing exit codes can mask a genuinely broken invocation (typo'd flag, missing binary) as "no findings". Mitigated by asserting per-tool that the expected report file exists AND parses as JSON, rather than trusting the script's own exit code. |

This phase adds no runtime code, no auth/authz surface, no data handling and no new user input path — ASVS V2/V3/V4/V5/V6 are all non-applicable (RESEARCH.md Security Domain). Supply chain is the only real vector, and it is the one gated above.
</threat_model>

<verification>
- `npm run audit:tech-debt` exits 0 and `bash -n scripts/run-tech-debt-audit.sh` passes.
- Every one of the eight checks appears exactly once (twice for knip) in the script and is guarded with `|| true`.
- knip, jscpd and madge are present in `devDependencies` and absent from `dependencies`; `package-lock.json` is committed alongside.
- `knip.json` and `.jscpd.json` both exclude `graphify-out`, `src/graphify-out` and `src/shared/lib/supabase.types.ts`.
- `.audit-tmp/` is gitignored and `git status --porcelain` is clean of it after a run.
- `npm run lint`, `npm run typecheck` and `npm run test` all still exit 0 (baselines measured green on 2026-08-03).
</verification>

<success_criteria>
One `npm run audit:tech-debt` command runs the complete eight-check audit, absorbs every tool's findings-exit-code without aborting, and deposits one machine-readable report per check into a gitignored `.audit-tmp/` — with the three D-04 devDependencies installed, legitimacy-verified, and provably confined to devDependencies.
</success_criteria>

<output>
Create `.planning/phases/10-ai-slob-technical-debt-checklist/10-01-SUMMARY.md` when done. Record: jscpd v5's actual `--help` flag set vs. the flags used, the real top-level key names of `jscpd-report.json`, the vitest/playwright output-file mechanisms that actually worked, and whether the knip entry-point escalation rule had to fire.
</output>
