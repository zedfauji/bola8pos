---
phase: 10-ai-slob-technical-debt-checklist
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/run-tech-debt-audit.sh
autonomous: true
# No requirement IDs: phase_req_ids is null and .planning/REQUIREMENTS.md does not
# exist in this project. Spec-less probe fallback was an explicit visible skip.
requirements: []

must_haves:
  truths:
    - "The audit covers all six D-02 structural-smell categories, not just the four pre-existing quality gates — dead code, unjustified `as any`, duplicate abstractions, oversized files, unused deps, and stale TODO/FIXME comments."
    - "One full audit run has actually executed across the entire codebase (D-01), leaving a complete set of reports on disk."
    - "Every raw report has a compact digest small enough for the synthesis plan to read without opening a single source file (D-03)."
  artifacts:
    - "scripts/run-tech-debt-audit.sh extended with the three structural-smell probes"
    - ".audit-tmp/ populated with one report per check (untracked)"
    - ".audit-tmp/digests/ with one compact per-tool digest (untracked)"
  key_links:
    - "D-02 smell categories -> concrete probe: dead code+unused deps=knip, duplication=jscpd, cross-layer coupling=madge, `as any`=grep, TODO/FIXME=grep, oversized files=wc -l"
    - ".audit-tmp/*.json -> .audit-tmp/digests/*.txt (the context-budget firewall between raw reports and Plan 03)"
---

<objective>
Complete the audit's coverage of D-02's structural-smell categories with three cheap grep/`wc` probes the three new tools do not cover, then execute the full audit once across the entire codebase (D-01) and reduce every raw report to a compact digest.

Purpose: D-03 requires every finding to originate from tool output rather than Claude reading source files — but the raw reports are themselves large enough to blow a synthesis agent's context. The digests are the firewall: Plan 03 reads only digests, never raw reports, never source.

Output: a complete `.audit-tmp/` report set and a `.audit-tmp/digests/` directory, both untracked; one commit extending the audit script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-CONTEXT.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md
@.planning/phases/10-ai-slob-technical-debt-checklist/10-01-SUMMARY.md
</context>

## Artifacts this phase produces

Newly created by Phase 10 (exclude these from drift/source-grounding verification — they do not exist before this phase):

| Artifact | Created in |
|---|---|
| `bar-pos/knip.json` | 10-01 Task 1 |
| `bar-pos/.jscpd.json` | 10-01 Task 2 |
| `bar-pos/scripts/run-tech-debt-audit.sh` | 10-01 Task 1 (extended in 10-01 T2/T3 and 10-02 T1) |
| `package.json` script `audit:tech-debt` | 10-01 Task 1 |
| `.gitignore` entry `.audit-tmp/` | 10-01 Task 1 |
| devDependencies `knip`, `jscpd`, `madge` | 10-01 Task 1 |
| `.audit-tmp/**` report files (untracked, never committed) | 10-02 Task 2 |
| `.audit-tmp/digests/**` (untracked, never committed) | 10-02 Task 2 |
| `.planning/phases/10-ai-slob-technical-debt-checklist/10-CHECKLIST.md` | 10-03 Task 1 |

<tasks>

<task type="auto">
  <name>Task 1: Add the three structural-smell probes D-02 requires</name>
  <files>scripts/run-tech-debt-audit.sh</files>
  <read_first>
    - `scripts/run-tech-debt-audit.sh` — current state after Plan 01, so the new probes reuse its `$OUT` and `|| true` conventions
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-CONTEXT.md` — decision D-02, which enumerates the exact smell categories in scope
    - `CLAUDE.md` — the "Paperclip Sprint Team Standards" section, whose rule is `No any without a justification comment on the same line`; this defines what makes an `as any` "unjustified"
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-RESEARCH.md` — the proposed severity tiers under "Open Questions" item 1, which reference these three probes
  </read_first>
  <action>
D-02 broadened this audit beyond lint/typecheck/test/E2E to six structural-smell categories. knip covers dead code and unused dependencies; jscpd covers duplicate abstractions; madge covers cross-layer coupling. Three categories have no tool yet: unjustified `as any`, stale TODO/FIXME comments, and oversized files. Add them as three cheap probes to the same script rather than installing more tooling — a grep and a `wc -l` are the correct rung here, and D-04's tool list is closed at knip/jscpd/madge.

Append to `scripts/run-tech-debt-audit.sh`, each `|| true`-guarded (grep exits 1 when it matches nothing, which is a valid audit outcome, not a failure), each writing into `$OUT`:

  - Unjustified-`as any` probe → `$OUT/as-any.txt`. Recursively grep `src` for `.ts`/`.tsx` files, emitting `file:line:content` (`grep -rn` with `--include` filters), matching the cast form. Do NOT try to determine "justified vs unjustified" in the shell — emit every occurrence with its full source line, because the project rule is that a justification comment must appear on the *same line*, so the captured line itself carries the evidence and Plan 03 can classify from the digest alone without opening the file. Exclude `src/graphify-out` and `src/shared/lib/supabase.types.ts` consistently with the other tools' ignore lists.
  - Stale TODO/FIXME probe → `$OUT/todo-fixme.txt`. Recursively grep `src` and `e2e` for `.ts`/`.tsx` files, matching `TODO` or `FIXME`, emitting `file:line:content`. Do not attempt a git-blame age calculation in the script — RESEARCH.md's Low tier only requires listing them with `file:line`, and blame-per-line across the whole tree is expensive for no added triage value at this tier.
  - Oversized-file probe → `$OUT/file-sizes.txt`. Line-count every `.ts`/`.tsx` file under `src`, excluding `src/graphify-out` and `src/shared/lib/supabase.types.ts` (a generated file's size is not debt), and sort descending by line count so the head of the file is the finding list. Emit the whole sorted list; Plan 03 applies the >400-line threshold when reading, so the raw data stays threshold-agnostic and the threshold can be recalibrated without re-running the audit.

Keep the probes above the Playwright invocation so they complete in the first seconds of a run rather than after the multi-minute E2E step.
  </action>
  <verify>
    <automated>bash -n scripts/run-tech-debt-audit.sh && test "$(grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -cE 'as-any\.txt|todo-fixme\.txt|file-sizes\.txt')" = "3"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/run-tech-debt-audit.sh` exits 0.
    - `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -cE 'as-any\.txt|todo-fixme\.txt|file-sizes\.txt'` returns `3`.
    - Each of the three probe lines is suffixed with `|| true` — verified because `grep -v '^[[:space:]]*#' scripts/run-tech-debt-audit.sh | grep -c '|| true'` now returns `11` (8 tool invocations from Plan 01 plus these 3).
    - The three probe lines all write under `$OUT`, not the repo root.
    - The `as any` probe emits `file:line:content` (line numbers enabled), so a same-line justification comment is visible in the output without opening the file.
    - The oversized-file probe's output is sorted by line count descending and excludes `src/shared/lib/supabase.types.ts`.
    - The playwright invocation is still the last tool invocation in the file.
  </acceptance_criteria>
  <done>All six D-02 smell categories have a probe in the audit script, and the script still passes a syntax check.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full audit and reduce every report to a digest</name>
  <files>scripts/run-tech-debt-audit.sh</files>
  <precondition>An interactive display session and `google-chrome-stable` are available — `playwright.config.ts` sets `headless: false` with `channel: 'chrome'`, so the E2E leg cannot run on a headless runner. RESEARCH.md verified `$DISPLAY=:0` and `/usr/bin/google-chrome-stable` present on 2026-08-03. If either is absent, HALT and report rather than silently producing an audit with an empty E2E report.</precondition>
  <read_first>
    - `scripts/run-tech-debt-audit.sh` — final state after Task 1
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-01-SUMMARY.md` — the recorded actual field names of `jscpd-report.json` and the vitest/playwright output mechanisms that worked, so the digest queries target real paths rather than assumed ones
    - `.planning/phases/10-ai-slob-technical-debt-checklist/10-CONTEXT.md` — decisions D-01 (full re-audit, not a delta) and D-03 (findings from tool output, never file-by-file reading)
  </read_first>
  <action>
Execute the complete audit once and then compress it, so Plan 03 can synthesize from digests instead of drowning in raw reports.

Step 1 — run `npm run audit:tech-debt` to completion. This is the D-01 full re-audit: the whole codebase, regardless of when each file was written, including the multi-minute E2E leg. Expect it to take several minutes. Do not scope it to files changed since Phase 11.

Step 2 — assert completeness before digesting. Every expected report must exist and, where it is JSON, parse. The expected set is: `knip-report.json`, `knip-production.json`, `jscpd-out/jscpd-report.json`, `madge-circular.json`, `eslint-report.json`, `tsc-errors.txt`, `vitest-results.json`, `playwright-results.json`, `as-any.txt`, `todo-fixme.txt`, `file-sizes.txt`. A missing or unparseable file means a tool invocation is broken and was masked by its `|| true` guard (threat T-10-03) — fix the invocation and re-run rather than proceeding with a hole in the audit.

Step 3 — write one compact digest per report into `.audit-tmp/digests/` using `jq` (available, v1.8.1) or `node -e` one-liners. Read only the digests afterward. Each digest must be small enough to read in full and must carry, per finding, at minimum a `file:line` reference and a one-phrase description — that is the granularity CONTEXT.md's "Integration Points" convention requires so a future remediation phase can act directly. Rules for the digests:
  - A total count per category, always. Plan 03's reconciliation gate compares CHECKLIST.md's stated counts against these.
  - The full finding list where the category is small (under ~100 findings). Where a category is larger, emit the total count plus a per-directory rollup plus the full list for the top-level FSD layer breakdown — never silently truncate without stating the total.
  - For jscpd, emit only clone metadata (both file paths, both line ranges, duplicated line count) — never the duplicated source bodies. Those are the bulk of the report and they are exactly what T-10-01 says must not spread further.
  - For eslint, roll up by `ruleId` with counts, plus the `file:line` list for any rule with errors (severity 2). Warnings are already zero-tolerance under `--max-warnings 0`, so include them too.
  - For vitest and playwright, emit only failing and skipped test titles with their file paths; passing tests are not findings.
  - For madge, emit each cycle as its ordered file list — Plan 03 classifies same-slice vs cross-layer from those paths.
  - For the three probes, the raw text files are already digest-shaped; copy or lightly summarise (for `file-sizes.txt`, keep only entries above 300 lines so the >400 threshold has headroom for recalibration).

Do NOT open any source file during this task. If a finding seems to need context, that context comes from the tool's own payload; RESEARCH.md's anti-pattern list calls out reading source to "double check" a finding as a direct D-03 violation.

Step 4 — record the per-category totals in the plan summary. Plan 03 reconciles against them.

Nothing from `.audit-tmp/` is committed. The only committed artifact of this plan is Task 1's script change.
  </action>
  <verify>
    <automated>for f in knip-report.json knip-production.json jscpd-out/jscpd-report.json madge-circular.json eslint-report.json vitest-results.json playwright-results.json; do jq -e . ".audit-tmp/$f" > /dev/null || { echo "MISSING/UNPARSEABLE: $f"; exit 1; }; done; for f in tsc-errors.txt as-any.txt todo-fixme.txt file-sizes.txt; do test -f ".audit-tmp/$f" || { echo "MISSING: $f"; exit 1; }; done; test "$(ls .audit-tmp/digests | wc -l)" -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run audit:tech-debt` ran to completion in this task and exited 0.
    - All 7 JSON reports exist under `.audit-tmp/` and each satisfies `jq -e .`.
    - All 4 text reports (`tsc-errors.txt`, `as-any.txt`, `todo-fixme.txt`, `file-sizes.txt`) exist under `.audit-tmp/`.
    - `.audit-tmp/digests/` contains at least 10 digest files, one per report category.
    - `grep -rc 'function\|const \|=>' .audit-tmp/digests/jscpd*` shows the jscpd digest carries no duplicated source bodies — only paths, line ranges and counts.
    - Every digest states an explicit total count for its category.
    - `git status --porcelain .audit-tmp` produces no output — nothing from the scratch directory is staged or untracked-but-visible.
    - The plan summary records the per-category finding totals.
    - No source file under `src/` was opened during this task (verifiable from the task's own tool-call record).
  </acceptance_criteria>
  <done>A complete, whole-codebase audit has run and every report is reduced to a digest Plan 03 can read in full, with per-category totals recorded for reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit tooling → `.audit-tmp/` → git | jscpd reports embed verbatim duplicated source; digests and the committed checklist must carry references only |
| E2E leg → live display session / Chrome | The Playwright leg launches a real browser against the local dev environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | jscpd clone bodies in reports and digests | medium | mitigate | Task 2 Step 3 restricts the jscpd digest to clone metadata (paths, line ranges, duplicated line counts) and explicitly excludes source bodies; `.audit-tmp/` is gitignored so neither reports nor digests reach git. |
| T-10-03 | Tampering | `\|\| true` masking a broken tool invocation | low | mitigate | Task 2 Step 2 asserts every one of the 11 expected reports exists and every JSON one parses, before any digesting; a hole in the audit is treated as a defect to fix and re-run, not an empty finding set. |
| T-10-04 | Denial of Service | E2E leg on a headless/display-less machine | low | mitigate | Declared as a `<precondition>` — display session plus `google-chrome-stable` must be present, and the task halts rather than producing a silently-empty E2E report that would understate the audit. |

No runtime code, auth surface, data handling or user input path is introduced by this plan — ASVS V2/V3/V4/V5/V6 remain non-applicable (RESEARCH.md Security Domain).
</threat_model>

<verification>
- `scripts/run-tech-debt-audit.sh` passes `bash -n` and contains 11 `|| true`-guarded invocations covering all six D-02 categories plus the four pre-existing gates.
- All 11 expected reports exist under `.audit-tmp/`; the 7 JSON ones parse.
- `.audit-tmp/digests/` holds at least 10 digests, each stating a total count.
- The jscpd digest contains no duplicated source bodies.
- `git status --porcelain .audit-tmp` is empty.
</verification>

<success_criteria>
A complete whole-codebase audit (D-01) covering every D-02 structural-smell category has run once, and its output exists in two forms: full raw reports for provenance and compact digests small enough for a synthesis agent to read entirely — satisfying D-03's requirement that findings come from tool output rather than Claude reading source files.
</success_criteria>

<output>
Create `.planning/phases/10-ai-slob-technical-debt-checklist/10-02-SUMMARY.md` when done. Record the per-category finding totals (one line per category), any tool invocation that had to be fixed after the completeness assertion, and the digest file names Plan 03 should read.
</output>
