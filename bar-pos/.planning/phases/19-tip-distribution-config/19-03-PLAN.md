---
phase: 19-tip-distribution-config
plan: 03
type: execute
wave: 2
depends_on: [19-02]
files_modified:
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-2, SC-4]
user_setup: []

must_haves:
  truths:
    - "Both Phase 19 migrations are applied to the remote Supabase project so tip_distribution_entries exists and close_caja_session has the version fix + tip computation live"
    - "supabase.types.ts contains a tip_distribution_entries Row/Insert/Update/Relationships block so future typed reads compile"
    - "The Plan 02 integration test runs GREEN against the live DB — no STALE_VERSION, allocations sum to total tips (SC-2/SC-4 proven live)"
  artifacts:
    - path: "src/shared/lib/supabase.types.ts"
      provides: "tip_distribution_entries table type block (Row/Insert/Update/Relationships) added alphabetically"
      contains: "tip_distribution_entries"
  key_links:
    - from: "supabase/migrations/20260709000001_tip_distribution_entries_table.sql"
      to: "remote Supabase"
      via: "supabase db push"
      pattern: "tip_distribution_entries"
    - from: "supabase/migrations/20260709000002_close_caja_session_tip_distribution.sql"
      to: "remote Supabase"
      via: "supabase db push"
      pattern: "close_caja_session"
---

<objective>
Apply the two Phase 19 migrations to the remote Supabase project and extend supabase.types.ts with the tip_distribution_entries table types. This is the BLOCKING gate that makes the new table and the extended close_caja_session RPC live, then proves the Plan 02 integration test green end-to-end.

Purpose: Nothing downstream (the caja read hook, the report panel, the E2E) can observe a real tip distribution until the migrations are pushed. The pushed close_caja_session also carries the STALE_VERSION fix, so this plan is where SC-4's "close-caja works and reflects the distribution" first becomes verifiable live.

Output: extended supabase.types.ts + both migrations applied to remote + the Plan 02 integration test passing live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tip-distribution-config/19-RESEARCH.md
@.planning/phases/19-tip-distribution-config/19-02-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):
- tip_distribution_entries Row/Insert/Update/Relationships block in src/shared/lib/supabase.types.ts
- both Phase 19 migrations applied to the remote Supabase project (state change, not a file)

Created by other Phase 19 plans (do not flag): the migration files themselves (Plan 02); TipDistribution schemas (Plan 01); settings/caja entity extensions (Plan 04); UI (Plan 05).

<tasks>

<task type="auto">
  <name>Task 1: Extend supabase.types.ts with the tip_distribution_entries block</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <read_first>
    - src/shared/lib/supabase.types.ts (lines 168-212 — the caja_entries Row/Insert/Update/Relationships block; place the new block alphabetically — tip_distribution_entries sorts near the end of the public.Tables list; find the correct alphabetical slot)
    - supabase/migrations/20260709000001_tip_distribution_entries_table.sql (the authoritative column list + types the block must mirror)
    - CLAUDE.md ("Generated files — never edit manually" + the "Missing generated types workaround" note — Docker is frequently unavailable so this file is extended by hand, matching the Phase 15/17 precedent recorded in STATE.md)
  </read_first>
  <action>
    Manually add a tip_distribution_entries entry to the public.Tables object in src/shared/lib/supabase.types.ts, matching the caja_entries block's structure. Row fields (all present): id string; caja_session_id string; floor_pct number; bar_pct number; kitchen_pct number; total_tips number; floor_amount number; bar_amount number; kitchen_amount number; created_at string. Insert: same, with id?, created_at?, and the *_amount / total_tips fields optional (they have DB defaults) — caja_session_id + the three *_pct required. Update: all optional. Relationships: one FK entry tip_distribution_entries_caja_session_id_fkey → referencedRelation "caja_sessions", columns ["caja_session_id"], referencedColumns ["id"], isOneToOne true (the caja_session_id column is UNIQUE, so this FK is one-to-one). Place the block in its correct alphabetical position among the other table keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tab/model/queries\|agent/rag" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>supabase.types.ts has a tip_distribution_entries block (Row/Insert/Update/Relationships) with all 10 columns and the caja_sessions FK; typecheck clean (no new errors beyond the 2 pre-existing).</done>
  <acceptance_criteria>
    - grep -c "tip_distribution_entries:" src/shared/lib/supabase.types.ts >= 1
    - grep -c "floor_amount\|bar_amount\|kitchen_amount" src/shared/lib/supabase.types.ts >= 3
    - grep -c "tip_distribution_entries_caja_session_id_fkey" src/shared/lib/supabase.types.ts == 1
    - typecheck produces no NEW errors beyond the 2 pre-existing
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply both Phase 19 migrations via supabase db push + verify live</name>
  <files>supabase/migrations/20260709000001_tip_distribution_entries_table.sql, supabase/migrations/20260709000002_close_caja_session_tip_distribution.sql (applied; authored in Plan 02)</files>
  <action>Run `npx supabase db push` from bar-pos to apply migrations 20260709000001 + 20260709000002 to the remote project, then PAUSE for the human verification steps in how-to-verify (the 4 SQL checks + the live Plan 02 integration-test run). Automate the push; do NOT proceed until the human confirms all checks pass.</action>
  <what-built>
    Two migrations authored in Plan 02 (tip_distribution_entries table + append-only RLS; close_caja_session recreated with the version=version+1 fix, tip pooling, largest-remainder split, entry INSERT, and record_audit('tip_distribution.compute')) plus the supabase.types.ts extension. This checkpoint applies them to the remote Supabase project and proves the live behavior.
  </what-built>
  <how-to-verify>
    1. From bar-pos, apply both migrations to remote: run `npx supabase db push`. If the CLI reports the two 20260709000001/000002 files as pending, confirm apply. (If a schema-cache staleness error appears afterward, run `NOTIFY pgrst, 'reload schema';` via the SQL editor — the Phase 5/17 precedent in STATE.md.)
    2. Confirm the table is live: SELECT to_regclass('public.tip_distribution_entries'); returns non-null.
    3. Confirm RLS is append-only: SELECT policyname, cmd FROM pg_policies WHERE tablename='tip_distribution_entries'; shows exactly ONE policy (tip_distribution_entries_select_manager, cmd SELECT) and NO INSERT/UPDATE/DELETE policy.
    4. Confirm the RPC carries both changes: SELECT pg_get_functiondef('public.close_caja_session(uuid,uuid,numeric,text)'::regprocedure); and verify the body contains both `version = version + 1` and `INSERT INTO tip_distribution_entries`.
    5. Run the Plan 02 integration test live (with .env.local E2E creds present): `npx vitest run src/entities/caja/model/tip-distribution-rpc.integration.test.ts` — assert it is GREEN (regression test shows NO STALE_VERSION and version increments; allocations-sum test shows floor+bar+kitchen === total_tips; config-absent fallback uses 34/33/33; zero-tip yields 0/0/0; RLS test denies direct writes).
  </how-to-verify>
  <resume-signal>Type "approved" once db push succeeded, the 4 verification queries pass, and the integration test is green — or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration files → remote Supabase | supabase db push applies DDL + SECURITY DEFINER function changes to the production project; a bad push affects live caja closing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-PUSH | Tampering/DoS | close_caja_session replacement | mitigate | Blocking human checkpoint verifies pg_get_functiondef contains both the version fix and the tip INSERT before sign-off; the live integration test must be green (proves close still works) before proceeding |
| T-19-RLS-LIVE | Elevation of Privilege | tip_distribution_entries policies | mitigate | Checkpoint query 3 asserts exactly one SELECT policy and no write policy is present on the live table |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase |
</threat_model>

<verification>
- typecheck clean after supabase.types.ts extension
- db push applied; to_regclass + pg_policies + pg_get_functiondef queries pass
- Plan 02 integration test green live (SC-2 allocations sum, SC-4 no STALE_VERSION)
</verification>

<success_criteria>
- Both migrations live on remote; tip_distribution_entries queryable; close_caja_session carries version fix + tip computation
- supabase.types.ts typed for the new table; integration test green (SC-2/SC-4 proven live)
</success_criteria>

<output>
Create `.planning/phases/19-tip-distribution-config/19-03-SUMMARY.md` when done
</output>
