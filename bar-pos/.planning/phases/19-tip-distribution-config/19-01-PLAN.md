---
phase: 19-tip-distribution-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/tip-distribution-math.ts
  - src/shared/lib/tip-distribution-math.test.ts
autonomous: true
requirements: [SC-1, SC-2]
user_setup: []

must_haves:
  truths:
    - "SettingsKeySchema accepts 'tip_distribution' so the settings snapshot layer can fetch/store the 3-way split config (SC-1)"
    - "TipDistributionSettingsSchema validates {floorPct,barPct,kitchenPct} are each 0-100 (D-01: no hard sum-to-100 rule at the schema level)"
    - "TipDistributionEntrySchema mirrors the stored entry row shape (pcts + total + 3 bucket amounts + createdAt)"
    - "computeTipDistribution splits a total using largest-remainder so floor+bar+kitchen ALWAYS equals the total to the cent, leftover cents to the largest-percentage bucket, floor>bar>kitchen tiebreak (D-02, SC-2)"
  artifacts:
    - path: "src/shared/lib/domain.ts"
      provides: "SettingsKeySchema 'tip_distribution' literal + TipDistributionSettingsSchema + TipDistributionEntrySchema + registry entries"
      contains: "TipDistributionEntrySchema"
    - path: "src/shared/lib/tip-distribution-math.ts"
      provides: "computeTipDistribution pure largest-remainder split (spec oracle for the PL/pgSQL RPC)"
      contains: "export function computeTipDistribution"
    - path: "src/shared/lib/tip-distribution-math.test.ts"
      provides: "fast-check property tests proving the sum-preservation + tiebreak invariants (Wave 0)"
      contains: "computeTipDistribution"
  key_links:
    - from: "src/shared/lib/tip-distribution-math.ts"
      to: "src/shared/lib/tip-distribution-math.test.ts"
      via: "property test imports the pure function"
      pattern: "computeTipDistribution"
---

<objective>
Author the phase's contract layer: the Zod schemas describing the tip-distribution config value and the per-caja-close entry row, plus the single pure TypeScript function computeTipDistribution that specifies D-02's largest-remainder split algorithm and is property-tested as the authoritative oracle for the PL/pgSQL implementation in Plan 02.

Purpose: Everything downstream (settings entity, caja read hook, Settings tab, report panel, integration test) depends on these types and this algorithm. Defining them first prevents the scavenger-hunt anti-pattern — executors receive exact schema shapes instead of inferring them. The pure function locks D-02 behavior (no tip money lost; leftover cents to the largest-percentage bucket; floor>bar>kitchen tiebreak) in a fast-check-tested form.

Output: extended domain.ts (2 new schemas + SettingsKeySchema literal + registry entries) and a new tip-distribution-math.ts (+ property test). No DB, no UI. Pure, fully unit-verifiable in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tip-distribution-config/19-CONTEXT.md
@.planning/phases/19-tip-distribution-config/19-RESEARCH.md
@.planning/phases/19-tip-distribution-config/19-VALIDATION.md
</context>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):
- SettingsKeySchema gains the 'tip_distribution' enum literal (src/shared/lib/domain.ts)
- TipDistributionSettingsSchema + TipDistributionSettings type (src/shared/lib/domain.ts)
- TipDistributionEntrySchema + TipDistributionEntry type (src/shared/lib/domain.ts)
- registry entries TipDistributionSettings + TipDistributionEntry in the domain schema registry object
- computeTipDistribution pure function + TipDistributionResult type (src/shared/lib/tip-distribution-math.ts)
- src/shared/lib/tip-distribution-math.test.ts

Created by other Phase 19 plans (do not flag): tip_distribution_entries table + close_caja_session extension + 'tip_distribution.compute' audit action (Plan 02); supabase.types.ts block (Plan 03); settings-entity parseTipDistribution/DEFAULT_TIP_DISTRIBUTION + caja useTipDistributionEntry (Plan 04); TipDistributionSettingsTab + TipBucketDistributionPanel (Plan 05).

<tasks>

<task type="auto">
  <name>Task 1: Add tip-distribution Zod schemas + SettingsKey literal + registry entries</name>
  <files>src/shared/lib/domain.ts</files>
  <read_first>
    - src/shared/lib/domain.ts (lines 15-19 MoneySchema/UuidSchema/TimestampSchema primitives; lines 797-806 SettingsKeySchema; lines 927-946 CajaEntrySchema shape to mirror; lines 1249-1269 the schema registry object where Settings and Caja schemas are registered)
    - .planning/phases/19-tip-distribution-config/19-RESEARCH.md (section "Zod schema shape to add" — the exact TipDistributionSettingsSchema + TipDistributionEntrySchema field lists)
    - .planning/phases/19-tip-distribution-config/19-CONTEXT.md (D-01 percentages need not sum to 100; Claude's Discretion — precision follows payments.tip_amount NUMERIC(10,2))
  </read_first>
  <action>
    In src/shared/lib/domain.ts:

    1. Add 'tip_distribution' as a new literal to the SettingsKeySchema z.enum list (append after 'payment_labels'). Do NOT reorder existing literals.

    2. Immediately after the settings schemas block (near the other SettingsSchema definitions), add and export TipDistributionSettingsSchema = z.object with three fields floorPct, barPct, kitchenPct, each z.number().min(0).max(100). Export the inferred type TipDistributionSettings. Do NOT add a .refine forcing sum===100 — D-01 requires non-100 sums to be schema-legal.

    3. Near the CajaEntrySchema block, add and export TipDistributionEntrySchema = z.object with: id UuidSchema, cajaSessionId UuidSchema, floorPct/barPct/kitchenPct each z.number().min(0).max(100), totalTips MoneySchema, floorAmount MoneySchema, barAmount MoneySchema, kitchenAmount MoneySchema, createdAt TimestampSchema. Export the inferred type TipDistributionEntry. (MoneySchema = z.number().nonnegative().multipleOf(0.01) — matches the DB NUMERIC(10,2) non-negative amounts.)

    4. Register both new schemas in the domain schema registry object (block around lines 1249-1269): add TipDistributionSettings: TipDistributionSettingsSchema next to the settings entries and TipDistributionEntry: TipDistributionEntrySchema next to the caja entries.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tab/model/queries\|agent/rag" || echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>domain.ts exports TipDistributionSettingsSchema + TipDistributionEntrySchema (+ types), SettingsKeySchema includes 'tip_distribution', both schemas registered; typecheck clean (only the 2 pre-existing tab/model/queries + agent/rag errors may remain).</done>
  <acceptance_criteria>
    - grep -c "'tip_distribution'" src/shared/lib/domain.ts >= 1 (SettingsKey literal added)
    - grep -c "export const TipDistributionSettingsSchema" src/shared/lib/domain.ts == 1
    - grep -c "export const TipDistributionEntrySchema" src/shared/lib/domain.ts == 1
    - grep -c "TipDistributionEntry: TipDistributionEntrySchema" src/shared/lib/domain.ts == 1 (registry)
    - grep -c "floorPct\|barPct\|kitchenPct" src/shared/lib/domain.ts >= 3
    - typecheck produces no NEW errors beyond the 2 pre-existing (tab/model/queries.ts, agent/rag.ts)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeTipDistribution pure largest-remainder split + fast-check property tests</name>
  <files>src/shared/lib/tip-distribution-math.ts, src/shared/lib/tip-distribution-math.test.ts</files>
  <read_first>
    - .planning/phases/19-tip-distribution-config/19-RESEARCH.md (Pattern 4 "Largest-remainder rounding" — the exact truncate-then-remainder-to-largest algorithm + floor>bar>kitchen tiebreak via >= ordering; Pitfall 5 total_tips=0 must yield 0/0/0)
    - .planning/phases/19-tip-distribution-config/19-CONTEXT.md (D-02 rounding rule; Claude's Discretion "deterministic tiebreak floor>bar>kitchen")
    - src/shared/lib/pool-billing.ts (existing pure-money helper for the cents-based rounding idiom, if present) OR any src/features/split-tab math helper for the Math.round(x*100) cents idiom used across the codebase
    - any existing fast-check property test file in the repo (e.g. src/shared/lib usage) for the fc import-order convention (fast-check import must precede vitest per import/order; STATE.md notes fc.float needs Math.fround in v4 — prefer integer cents to avoid float flakiness)
  </read_first>
  <behavior>
    - computeTipDistribution(0, any pcts) yields floor 0, bar 0, kitchen 0 (Pitfall 5)
    - computeTipDistribution(100, floorPct 34 / barPct 33 / kitchenPct 33) yields floor 34.00, bar 33.00, kitchen 33.00 (sums to 100)
    - computeTipDistribution(100, all three 33.33) truncates each to 33.33; remainder 0.01 goes to floor: floor 33.34, bar 33.33, kitchen 33.33; sum === 100.00
    - Non-100 config computeTipDistribution(100, floor 50 / bar 30 / kitchen 0): remainder 20 goes to largest pct (floor): floor 70, bar 30, kitchen 0; sum === 100 (D-02 no money lost even when pcts do not sum to 100)
    - Property invariant: for arbitrary totalTips >= 0 and arbitrary pcts in [0,100], floor+bar+kitchen === totalTips exactly (to the cent), every bucket >= 0, and each non-winning bucket equals trunc(total*pct/100) to the cent
    - Property tiebreak: when two or more buckets share the largest pct, floor wins over bar wins over kitchen
  </behavior>
  <action>
    Create src/shared/lib/tip-distribution-math.ts exporting a pure function computeTipDistribution(totalTips: number, config: { floorPct: number; barPct: number; kitchenPct: number }): TipDistributionResult and a TipDistributionResult type { floor: number; bar: number; kitchen: number }.

    Implement in integer cents to avoid float drift: totalCents = Math.round(totalTips * 100); for each bucket cents = Math.trunc((totalCents * pct) / 100) (Math.trunc equals floor for non-negative). Compute remainder = totalCents - (floorCents + barCents + kitchenCents). Assign the FULL remainder to the bucket with the largest pct, breaking ties in floor>bar>kitchen order using floorPct >= barPct AND floorPct >= kitchenPct, else barPct >= kitchenPct, else kitchen (mirrors 19-RESEARCH.md Pattern 4, which the Plan 02 PL/pgSQL replicates). Return each bucket as cents / 100. The remainder-to-largest rule guarantees floor + bar + kitchen === totalTips regardless of whether pcts sum to 100 (D-02 no tip money lost).

    Create src/shared/lib/tip-distribution-math.test.ts: import fast-check (BEFORE vitest per import/order), then vitest. Add the four example cases from the behavior block plus two fc.assert(fc.property(...)) properties: (1) the sum-preservation + per-bucket-truncation invariant, and (2) the floor>bar>kitchen tiebreak. Model totalTips as fc.integer with min 0 max 1000000 mapped to /100 and each pct as fc.integer min 0 max 10000 mapped to /100 (integer-cent/percent generators — no fc.float). Use numRuns >= 500.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/tip-distribution-math.test.ts</automated>
  </verify>
  <done>computeTipDistribution exists and is pure; property tests (>=500 runs each) prove floor+bar+kitchen===totalTips for arbitrary inputs, zero-tip yields 0/0/0, floor>bar>kitchen tiebreak; test file green.</done>
  <acceptance_criteria>
    - npx vitest run src/shared/lib/tip-distribution-math.test.ts exits 0 with the property + example cases passing
    - grep -c "export function computeTipDistribution" src/shared/lib/tip-distribution-math.ts == 1
    - grep -c "fc.property\|fc.assert" src/shared/lib/tip-distribution-math.test.ts >= 2
    - grep -c "Math.trunc\|Math.round" src/shared/lib/tip-distribution-math.ts >= 2 (integer-cent implementation)
    - a test asserts computeTipDistribution(0, ...) returns all-zero (Pitfall 5)
    - a test asserts a non-100 config still sums to the full total (D-02 no-money-lost)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan is pure TypeScript types + a pure function — no I/O, no DB, no network, no user-input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | computeTipDistribution rounding | mitigate | Integer-cent arithmetic + property test asserting floor+bar+kitchen === totalTips prevents silent penny loss/gain that could later misstate a payout ledger |
| T-19-05 | Information Disclosure | Zod schema bounds | mitigate | min(0).max(100) on pct fields + MoneySchema non-negative on amounts reject malformed values before they reach the DB layer (V5 input validation) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (19-RESEARCH.md Package Legitimacy Audit: N/A — reuses fast-check/zod already in package.json) |
</threat_model>

<verification>
- typecheck clean (no new errors beyond the 2 pre-existing)
- npx vitest run src/shared/lib/tip-distribution-math.test.ts green
- Both new schemas registered in the domain registry + exported
</verification>

<success_criteria>
- SettingsKeySchema accepts 'tip_distribution'; TipDistributionSettingsSchema + TipDistributionEntrySchema exported and registered (SC-1 foundation)
- computeTipDistribution is a property-tested largest-remainder split that never loses/gains a cent (SC-2 algorithm oracle, D-02)
</success_criteria>

<output>
Create `.planning/phases/19-tip-distribution-config/19-01-SUMMARY.md` when done
</output>
