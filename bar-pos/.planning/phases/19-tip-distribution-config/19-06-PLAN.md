---
phase: 19-tip-distribution-config
plan: 06
type: execute
wave: 4
depends_on: [19-03, 19-05]
files_modified:
  - e2e/42-tip-distribution.spec.ts
  - CLAUDE.md
autonomous: false
requirements: [SC-3, SC-4]
user_setup: []

must_haves:
  truths:
    - "An E2E spec proves the full loop: admin sets the split in Settings, a caja with tipped payments is closed, and the Reports 'Tip Split' panel shows bucket amounts matching the configured percentages (SC-3 + SC-4)"
    - "The full unit suite + typecheck + lint are green (no regressions from the schema/entity/UI/migration changes)"
    - "CLAUDE.md documents the tip-distribution feature, the tip_distribution_entries table, the 'tip_distribution.compute' audit action, and the new E2E spec"
    - "A human confirms the Settings warn-but-allow behavior and the close-caja report distribution (VALIDATION.md manual-only SC-3/SC-4)"
  artifacts:
    - path: "e2e/42-tip-distribution.spec.ts"
      provides: "Playwright end-to-end coverage of the configure -> close-caja -> report loop"
      contains: "tip-split"
    - path: "CLAUDE.md"
      provides: "Docs: Implemented Features entry, Key DB Tables row, RBAC/audit note, E2E spec list update"
      contains: "tip_distribution"
  key_links:
    - from: "e2e/42-tip-distribution.spec.ts"
      to: "Reports 'Tip Split' panel"
      via: "close caja then assert bucket amounts"
      pattern: "tip-split"
---

<objective>
Close the phase with an end-to-end proof and the regression/docs gate: a Playwright spec that configures the split as admin, closes a caja carrying tipped payments, and asserts the new "Tip Split" report panel shows the computed floor/bar/kitchen amounts (SC-4); a full green-suite + typecheck + lint gate; CLAUDE.md documentation; and a final blocking human UAT sign-off for the two manual-only behaviors in 19-VALIDATION.md (SC-3 warn-but-allow save, SC-4 report reflects the distribution).

Purpose: This is where SC-3 and SC-4 are proven against the real, pushed system, and where the phase's changes are confirmed not to have regressed the existing suite. It depends on the migrations being live (Plan 03) and the UI existing (Plan 05).

Output: new E2E spec + CLAUDE.md updates + a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tip-distribution-config/19-CONTEXT.md
@.planning/phases/19-tip-distribution-config/19-VALIDATION.md
@.planning/phases/19-tip-distribution-config/19-05-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan (exclude from drift verification):
- e2e/42-tip-distribution.spec.ts
- CLAUDE.md documentation edits (Implemented Features, Key DB Tables, audit action note, E2E spec list)

Created by other Phase 19 plans (do not flag): everything else (schemas, math, migrations, entity hooks, Settings tab, report panel).

<tasks>

<task type="auto">
  <name>Task 1: E2E spec — configure split, close caja with tips, assert report distribution</name>
  <files>e2e/42-tip-distribution.spec.ts</files>
  <read_first>
    - e2e/02-caja.spec.ts (the caja open/close E2E flow: openCaja/close helpers, selectors for the caja UI, how a session is opened and closed)
    - e2e/helpers/auth.ts (loginAs(page, 'admin') — admin PIN 0000; the auth helper used by all specs)
    - e2e/41-split-payment.spec.ts (the most recent spec — payment/tip entry flow selectors + the tab -> payment -> receipt path to reuse for creating a tipped, closed tab)
    - src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx + src/widgets/TipBucketDistributionPanel/TipBucketDistributionPanel.tsx (the exact input ids/labels + the 'Tip Split' tab labels + bucket display text the spec must locate)
    - .planning/phases/19-tip-distribution-config/19-VALIDATION.md (SC-4 manual/E2E instructions)
  </read_first>
  <action>
    Create e2e/42-tip-distribution.spec.ts (number 42 is the next free spec index after 41-split-payment). Use loginAs(page, 'admin'). Cover the full loop in one or more tests:

    1. Configure: navigate to Settings, open the 'Tip Split' tab, set floor/bar/kitchen to a clean split that makes the assertion arithmetic exact (e.g. 50/30/20), Save, and assert the success toast.
    2. Transact + close: open a caja (reuse the 02-caja open helper/flow), create a tab, add an item, pay it with a known tip amount (reuse the 41-split-payment / payment-pane flow) so the session has a deterministic total tip (e.g. total tips = 10.00), then close the caja (no open tabs remain).
    3. Assert report: navigate to Reports, open the 'Tip Split' tab, select the just-closed session in the selector, and assert the panel shows Floor 5.00, Bar 3.00, Kitchen 2.00 (matching the 50/30/20 split of 10.00) and total tips 10.00.

    Follow the existing specs' resilience conventions (scoped locators to avoid the always-mounted AI-assistant panel; annotate rather than hard-skip if a seed dependency is environmental, per the Phase 5/17 precedent). If the interactive tip/caja seeding proves environmentally flaky, keep the deterministic assertion on the report panel reading a session whose entry was created by the Plan 02 integration seeding, and annotate the dependency — but prefer the full click-through.
  </action>
  <verify>
    <automated>npx playwright test e2e/42-tip-distribution.spec.ts --reporter=line</automated>
  </verify>
  <done>e2e/42-tip-distribution.spec.ts exists and, with a running dev server + E2E creds, drives configure -> close-caja-with-tips -> report and asserts the bucket amounts equal the configured split of the session's total tips (SC-4). Without the dev server it is the documented E2E gate run in the checkpoint.</done>
  <acceptance_criteria>
    - grep -c "tip-split\|Tip Split" e2e/42-tip-distribution.spec.ts >= 2 (settings tab + report tab)
    - grep -c "loginAs" e2e/42-tip-distribution.spec.ts >= 1
    - grep -c "close" e2e/42-tip-distribution.spec.ts >= 1 (caja close in the flow)
    - the spec asserts specific bucket amounts derived from a known split + known total tips (SC-4)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regression gate + CLAUDE.md documentation</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md ("Implemented Features (as of ...)" list; "Key DB Tables (Remote Supabase)" table; "RBAC Actions" / audit notes; "E2E Test Suite" spec list — the four places to update)
    - .planning/phases/19-tip-distribution-config/19-01-SUMMARY.md .. 19-05-SUMMARY.md (to describe the shipped artifacts accurately)
  </read_first>
  <action>
    Run the full regression gate and fix any regressions this phase introduced: npm run typecheck (no NEW errors beyond the 2 pre-existing tab/model/queries + agent/rag), npm run lint (max-warnings 0), npm run test (full unit suite green except the single pre-existing useCloseTab.test.ts:95 failure documented since Phase 15 — do NOT let any NEW failure through).

    Update CLAUDE.md in four places:
    - "Implemented Features" — add a bullet: tip-distribution config (settings key='tip_distribution' floor/bar/kitchen %; per-caja-close tip_distribution_entries computed inside close_caja_session via largest-remainder; 'Tip Split' Settings tab + Reports panel; Phase 19). Note the bundled close_caja_session version-bump fix.
    - "Key DB Tables" — add a tip_distribution_entries row (immutable per-caja-close floor/bar/kitchen allocation snapshot).
    - The audit/RBAC section — note the new 'tip_distribution.compute' audit action and that tip_distribution config write is admin-only (existing settings RLS).
    - "E2E Test Suite" — add 42-tip-distribution to the spec list and bump the count.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "tab/model/queries\|agent/rag" || echo "TYPECHECK_CLEAN"; npx eslint src; npx vitest run
</automated>
  </verify>
  <done>typecheck clean (no new errors), eslint clean, full unit suite green (only the pre-existing useCloseTab.test.ts:95 failure allowed); CLAUDE.md updated in all four places (Implemented Features, Key DB Tables, audit/RBAC note, E2E spec list).</done>
  <acceptance_criteria>
    - npx vitest run exits 0 aside from the single documented pre-existing failure (no NEW failures)
    - npx eslint src exits 0 (max-warnings 0)
    - grep -c "tip_distribution" CLAUDE.md >= 2 (feature + table)
    - grep -c "42-tip-distribution" CLAUDE.md == 1 (E2E list)
    - grep -c "tip_distribution.compute" CLAUDE.md >= 1 (audit action note)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human UAT sign-off — SC-3 warn-but-allow + SC-4 report distribution</name>
  <files>(no files created — human verification gate)</files>
  <action>Ensure the dev server is running (npm run dev / tauri dev), then PAUSE for the human UAT steps in how-to-verify. Optionally run `npx playwright test e2e/42-tip-distribution.spec.ts` first. Do NOT mark the phase complete until the human confirms SC-3 and SC-4.</action>
  <what-built>
    The full Phase 19 feature is live: the 'Tip Split' admin Settings tab, the close_caja_session tip computation (with the version-bump fix), and the 'Tip Split' Reports panel. This checkpoint is the human UAT for the two manual-only behaviors called out in 19-VALIDATION.md (SC-3, SC-4).
  </what-built>
  <how-to-verify>
    With the dev server running (npm run dev / tauri dev) and logged in as admin (PIN 0000):
    1. SC-3 (Settings warn-but-allow): open Settings -> Tip Split. Confirm three percentage inputs pre-filled 34/33/33. Change them to values that do NOT sum to 100 (e.g. 50/40/20 = 110%). Confirm a non-blocking warning appears AND the Save button is still enabled; Save and confirm the success toast. Then set a clean split (e.g. 50/30/20) and Save.
    2. SC-4 (report reflects distribution): open a caja, create a tab, add an item, pay it with a known tip (e.g. $10.00 tip), close the tab, then close the caja (confirm the caja closes with NO "STALE_VERSION"/"updated by another terminal" error — the bundled version fix).
    3. Open Reports -> Tip Split, select the just-closed session, and confirm the panel shows Floor/Bar/Kitchen amounts equal to the configured split of the total tips (e.g. Floor $5.00 / Bar $3.00 / Kitchen $2.00 for 50/30/20 of $10.00) and that they sum to the total tips.
    4. Confirm the existing per-staff "Tip Distribution" report tab still works and is unchanged (D-07).
    5. (Optional) Run npx playwright test e2e/42-tip-distribution.spec.ts and confirm green.
  </how-to-verify>
  <resume-signal>Type "approved" once SC-3 (warn-but-allow save) and SC-4 (report matches the configured split, caja closes without a version error) are confirmed — or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E/human actions → live remote Supabase | The UAT exercises the real close_caja_session against remote data; a bad computation would surface as a mismatched report amount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-E2E | Tampering | end-to-end tip computation | mitigate | E2E asserts exact bucket amounts for a known split + total, catching any drift between computeTipDistribution (Plan 01) and the PL/pgSQL RPC (Plan 02) |
| T-19-REG | Denial of Service | schema/RPC changes regress existing flows | mitigate | Full typecheck + lint + unit suite gate; the human UAT explicitly confirms the caja still closes (version-fix regression guard) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase |
</threat_model>

<verification>
- e2e/42-tip-distribution.spec.ts drives configure -> close -> report and asserts bucket amounts (SC-4)
- typecheck + lint + full unit suite green (no new regressions)
- CLAUDE.md updated; human UAT sign-off received (SC-3, SC-4)
</verification>

<success_criteria>
- End-to-end: admin configures split, caja closes with tips, report shows the matching floor/bar/kitchen distribution (SC-3 + SC-4)
- No regressions (suite green); docs updated; human sign-off recorded
</success_criteria>

<output>
Create `.planning/phases/19-tip-distribution-config/19-06-SUMMARY.md` when done
</output>
