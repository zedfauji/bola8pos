---
phase: 25-receipt-item-grouping-2-level
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/groupOrderItemsForReceipt.ts
  - src/shared/lib/groupOrderItemsForReceipt.test.ts
  - src/shared/lib/edge-function-contracts.ts
  - src/shared/lib/receipt-format.ts
  - src/shared/lib/receipt-format.test.ts
  - src/shared/lib/i18n/locales/es-MX/receipt.json
  - src/shared/lib/i18n/locales/en-US/receipt.json
  - src/features/print-precheque/usePrintPreCheque.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3]
user_setup: []

must_haves:
  truths:
    - "A ReceiptData whose items span 2+ distinct categories renders one category header line per category in buildThermalReceiptText output, items nested under their header (D-01)."
    - "A ReceiptData whose items all share one category (or carry no category at all) renders ZERO category header lines — byte-identical to today's output apart from modifier lines (SC-3)."
    - "buildThermalReceiptText renders one indented modifier line per modifier name, matching buildPreChequeText's existing two-space plus-sign convention (D-05)."
    - "buildPreChequeText renders the same category headers as buildThermalReceiptText from the same grouping call (D-05)."
    - "Every emitted line is at most 32 UTF-8 bytes wide (WR-02), including headers built from accented category names."
    - "Category-less rows (pool charges, synthetic lines) collect into a single trailing group, never interleaved."
    - "src/shared/lib/groupOrderItems.ts is unmodified (D-02)."
  artifacts:
    - src/shared/lib/groupOrderItemsForReceipt.ts
    - src/shared/lib/groupOrderItemsForReceipt.test.ts
  key_links:
    - "ReceiptDataSchema.items[] optional categoryId/categoryName/modifierNames fields are the contract the process-payment Edge Function (plan 02) fills."
    - "groupByCategory<T> generic signature is the contract pdf.tsx (plan 04) reuses for CajaReportTopProduct rows."
    - "formatModifierLines is the contract KdsCard (plan 03) reuses."
---

<objective>
Create the shared 3-level grouping utility (Category → Item → Modifiers, D-01) and prove its output shape end-to-end through the hardest consumer: the 32-column thermal receipt text builders.

Purpose: D-02 rates this utility's output shape `costly` to change once 4 call sites depend on it. This plan is the tracer — it locks the shape against the one surface with byte-width constraints, an existing regression test suite, and a server-side data contract, so waves 2's three parallel expansion plans consume a proven interface instead of a guessed one.

Output: `groupOrderItemsForReceipt.ts` (`groupByCategory<T>`, `formatModifierLines`), extended `ReceiptDataSchema.items`, both receipt text builders rendering category headers plus modifier lines, and a property-tested single-category degenerate case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-receipt-item-grouping-2-level/25-CONTEXT.md
@.planning/phases/25-receipt-item-grouping-2-level/25-PATTERNS.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 25 (new — exclude from drift/source-grounding checks):

| Artifact | Created in | Kind |
|----------|-----------|------|
| `src/shared/lib/groupOrderItemsForReceipt.ts` | plan 01 | new file |
| `groupByCategory<T>` | plan 01 | new exported function |
| `formatModifierLines` | plan 01 | new exported function |
| `CategoryGroup<T>` | plan 01 | new exported type |
| `CategorizedRow` | plan 01 | new exported type |
| `src/shared/lib/groupOrderItemsForReceipt.test.ts` | plan 01 | new test file |
| `ReceiptDataSchema.items[].categoryId` / `.categoryName` / `.modifierNames` | plan 01 | new schema fields |
| `PreChequeData.items[].categoryId` / `.categoryName` | plan 01 | new type fields |
| `receipt.category.other` (es-MX + en-US) | plan 01 | new i18n keys |
| `CajaReportTopProductSchema.categoryId` / `.categoryName` | plan 04 | new schema fields |
| `supabase/migrations/20260726000001_caja_report_top_products_category.sql` | plan 04 | new migration |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end category+modifier slice — utility, contract, thermal receipt</name>
  <files>src/shared/lib/groupOrderItemsForReceipt.ts, src/shared/lib/groupOrderItemsForReceipt.test.ts, src/shared/lib/edge-function-contracts.ts, src/shared/lib/receipt-format.ts, src/shared/lib/i18n/locales/es-MX/receipt.json, src/shared/lib/i18n/locales/en-US/receipt.json</files>
  <read_first>
    - src/shared/lib/receipt-format.ts — the full file. `byteWidth`/`truncateToByteWidth`/`padRight`/`lineLeftRight`/`centerLine`/`divider` (lines 6-68) are mandatory (WR-02); `buildPreChequeText`'s item loop (lines 128-137) is the modifier-line convention to reuse; `buildThermalReceiptText`'s item loop (lines 176-180) is the block being replaced.
    - src/shared/lib/groupOrderItems.ts — style reference ONLY (Map-based accumulate then final sort). Per D-02 this file must NOT be modified or imported.
    - src/shared/lib/category-tree.ts — top-of-file JSDoc-invariants convention for pure `shared/lib` transform modules.
    - src/shared/lib/edge-function-contracts.ts lines 1-60 — `ReceiptDataSchema`; `UuidSchema`/`MoneySchema` are already imported at the top of the file.
    - src/shared/lib/i18n/locales/es-MX/receipt.json and en-US/receipt.json — nested key layout (`receipt.*`, `precheque.*`, `pdf.*`).
  </read_first>
  <behavior>
    Tests to write first in `src/shared/lib/groupOrderItemsForReceipt.test.ts`:
    - `groupByCategory` on rows spanning 2 categories returns 2 groups, sorted by `categoryName` via `localeCompare`.
    - `groupByCategory` preserves input order of items WITHIN each group.
    - Rows with `categoryId === null` collapse into exactly one group whose `categoryId` and `categoryName` are both `null`, always the LAST element of the returned array even when its name would sort first.
    - Two distinct `categoryId` values sharing the same `categoryName` stay as two separate groups (grouping key is id, not name).
    - `formatModifierLines(['Extra cheese', 'No ice'])` returns exactly two strings, each beginning with two spaces then a plus sign then a space then the name.
    - `formatModifierLines([])` returns an empty array.
    - Control characters (`\u001B`, `\u0000`, `\n`) present in a modifier name or category name are removed from the produced line/group label.
    Tests to add to `src/shared/lib/receipt-format.test.ts` in Task 3 (not here).
  </behavior>
  <action>
Create `src/shared/lib/groupOrderItemsForReceipt.ts` as a pure module with zero imports from other project modules (it must stay usable from both `shared/lib` and `widgets`). Export:

- `export type CategorizedRow = { categoryId: string | null; categoryName: string | null }`
- `export type CategoryGroup<T> = { categoryId: string | null; categoryName: string | null; items: T[] }`
- `export function groupByCategory<T extends CategorizedRow>(rows: readonly T[]): CategoryGroup<T>[]`
- `export function formatModifierLines(modifierNames: readonly string[]): string[]`

`groupByCategory` implementation contract: accumulate into a `Map<string, CategoryGroup<T>>` keyed by `row.categoryId ?? UNCATEGORIZED_KEY` where `UNCATEGORIZED_KEY` is a module-private sentinel string. A row counts as uncategorized when `categoryId` is null/undefined OR `categoryName` is null/undefined/empty-after-trim. On first sight of a key, seed the group with `categoryId` and the sanitized `categoryName` (both `null` for the uncategorized bucket); subsequent rows push into `.items` preserving input order. Return named groups sorted with `a.categoryName.localeCompare(b.categoryName)`, then append the uncategorized group last if present. Never mutate the input array.

`formatModifierLines` returns one string per name: two spaces, `+`, one space, then the sanitized name. Empty/whitespace-only names are dropped.

Add a module-private `sanitize(s: string): string` that strips C0/C1 control characters via a character-class regex over the `\u0000`-`\u001F` and `\u007F`-`\u009F` ranges and trims the result. Apply it to every `categoryName` stored on a group and every modifier name rendered by `formatModifierLines`. This closes T-25-01 (see threat model): `printer.rs` writes `line.as_bytes()` straight to the device, so an unsanitized ESC byte inside a catalog-authored name would be interpreted as an ESC/POS command. Do not export `sanitize`.

Write a top-of-file JSDoc block, mirroring `category-tree.ts`, stating: pure functions, no I/O, callers resolve category names before calling, uncategorized bucket always last, and a pointer to the co-located test file.

Extend `ReceiptDataSchema.items`' inner `z.object` in `src/shared/lib/edge-function-contracts.ts` with three fields, added AFTER `lineTotal`: `categoryId: UuidSchema.nullable().optional()`, `categoryName: z.string().nullable().optional()`, `modifierNames: z.array(z.string()).default([])`. They must be optional so the 5 existing `ReceiptData` literal call sites (`edge-function-contracts.test.ts`, `pos-printer.test.ts`, `receipt-format.test.ts`, `ReceiptPreview.stories.tsx`, `PaymentModal.stories.tsx`) keep type-checking untouched. Because `exactOptionalPropertyTypes` is on, any NEW literal that sets these fields must write `categoryName: string | undefined`, never a `?:`-style omission mismatch.

Add the i18n key `receipt.category.other` to BOTH `src/shared/lib/i18n/locales/es-MX/receipt.json` (value `Otros`) and `src/shared/lib/i18n/locales/en-US/receipt.json` (value `Other`), as a nested `category` object inside the existing top-level `receipt` object. These are net-new keys with no pre-migration literal, so Phase 21's byte-identical-es-MX rule does not apply — give each locale its genuine translation.

Rewire `buildThermalReceiptText` in `src/shared/lib/receipt-format.ts`: import `groupByCategory` and `formatModifierLines` from `@shared/lib/groupOrderItemsForReceipt`; replace the flat `for (const item of receipt.items)` block (currently lines 176-180) with a loop over `groupByCategory(receipt.items)`. Header rule (implements the CONTEXT.md discretion item on header formatting and on category-less rows): emit a header line ONLY when the returned group array length is greater than 1; the header is `centerLine(label)` where `label` is `group.categoryName` when non-null, else `tr('receipt.category.other')`. Never build a header by raw string concatenation — `centerLine` is the byte-safe helper. Inside each group keep the existing `lineLeftRight(quantity-times-name, formatMoney(item.lineTotal))` row exactly as-is, then push each string returned by `formatModifierLines(item.modifierNames ?? [])`. Do not add blank separator lines and do not add a `divider()` per group — the single-group case must stay byte-identical to today's output apart from the new modifier lines (SC-3).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib/groupOrderItemsForReceipt.test.ts src/shared/lib/receipt-format.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/lib/groupOrderItemsForReceipt.test.ts` exits 0 with at least 7 passing assertions covering the behaviors listed in `<behavior>`.
    - The pre-existing `src/shared/lib/receipt-format.test.ts` suite passes unmodified in this task (its fixtures carry no category fields, so the single-group path must produce the same text it produced before).
    - `grep -n "groupByCategory\|formatModifierLines" src/shared/lib/receipt-format.ts` returns at least 3 lines (import plus both usages).
    - `grep -c "categoryId\|categoryName\|modifierNames" src/shared/lib/edge-function-contracts.ts` returns 3 or more.
    - `git diff --stat src/shared/lib/groupOrderItems.ts` produces no output (D-02: file untouched).
    - `grep -rn "category" src/shared/lib/i18n/locales/es-MX/receipt.json src/shared/lib/i18n/locales/en-US/receipt.json` returns a match in both files.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-02: once plans 02/03/04 bind to `groupByCategory`/`formatModifierLines`/`CategoryGroup<T>`, changing the output shape means re-touching all four render paths. Locking it here, verified against the byte-width-constrained surface, is the cheapest place to get it wrong.</reversibility>
  <done>A `ReceiptData` fixture with items in 2 categories plus modifiers produces thermal text containing both centered category headers and indented modifier lines, all lines ≤32 UTF-8 bytes; a fixture with no category fields produces the pre-existing output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-cheque consumes the same grouping (D-05)</name>
  <files>src/shared/lib/receipt-format.ts, src/features/print-precheque/usePrintPreCheque.ts</files>
  <read_first>
    - src/shared/lib/receipt-format.ts — the `PreChequeData` type (lines 86-108) and `buildPreChequeText` (lines 111-152), as modified by Task 1.
    - src/features/print-precheque/usePrintPreCheque.ts — the full file; the `items` mapping inside `mutationFn` (the `.flatMap(...).map(...)` block) is the only place `PreChequeData.items` is constructed in app code.
    - src/entities/tab/model/queries.ts lines 258-280 — `tabListSelect` confirms `order_items.product.category` is already fetched as `category:categories(id, name, color, sort_order, created_at)`, so `item.product?.category?.id` and `.name` are available with zero new plumbing (research A1: immediate category, no ancestor walk — `parent_id` is deliberately not selected).
  </read_first>
  <behavior>
    - `buildPreChequeText` with items in 2 categories emits the same centered header lines as `buildThermalReceiptText` for the same category set.
    - `buildPreChequeText` with all items in one category (or none) emits no header lines — existing pre-cheque tests pass unchanged.
    - `buildPreChequeText` still emits the per-item note line after the modifier lines when `item.notes` is set.
  </behavior>
  <action>
Add two fields to the `PreChequeData` items element type in `src/shared/lib/receipt-format.ts`: `categoryId: string | null` and `categoryName: string | null`. Make them required (not optional) — there is exactly one app-code construction site and updating it here keeps the type honest; `receipt-format.test.ts` fixtures get them in Task 3.

Rewrite `buildPreChequeText`'s item loop to mirror Task 1's `buildThermalReceiptText` loop: iterate `groupByCategory(data.items)`, emit `centerLine(label)` only when the group array length is greater than 1 (`label` = `group.categoryName` or `tr('receipt.category.other')`), then per item keep the existing `lineLeftRight` row, then push `formatModifierLines(item.modifierNames)` instead of the current inline two-space plus-sign loop, then keep the existing notes line using `tr('precheque.note')` unchanged. The inline modifier loop that Task 1 extracted must no longer exist anywhere in this file — both builders route through `formatModifierLines` (this is the concrete "no duplicated logic" half of SC-2).

In `src/features/print-precheque/usePrintPreCheque.ts`, extend the object literal produced by the inner `.map(item => ({ ... }))` with `categoryId: item.product?.category?.id ?? null` and `categoryName: item.product?.category?.name ?? null`, placed next to the existing `name` field. Do not add a new query, a new hook, or a category-tree ancestor walk — the immediate category already rides along in `tabListSelect`.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/lib/receipt-format.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (proves `usePrintPreCheque.ts` satisfies the widened `PreChequeData`).
    - `grep -c 'formatModifierLines' src/shared/lib/receipt-format.ts` returns 3 or more (import plus one call in each builder).
    - Searching `src/shared/lib/receipt-format.ts` for a remaining inline modifier loop finds none: `grep -n 'for (const mod of' src/shared/lib/receipt-format.ts` returns no output.
    - `grep -n 'category' src/features/print-precheque/usePrintPreCheque.ts` returns at least 3 matches (the pre-existing `routing` filter plus the two new fields).
    - `npm run lint` exits 0 with zero warnings.
  </acceptance_criteria>
  <reversibility rating="reversible">D-05 is a text-formatting change with no schema or wire contract involved; reverting is a local edit to two functions.</reversibility>
  <done>Both text builders produce identical category-header formatting for the same category set, and neither contains a hand-rolled modifier-line loop.</done>
</task>

<task type="auto">
  <name>Task 3: Property tests for the single-level degenerate case (SC-3)</name>
  <files>src/shared/lib/groupOrderItemsForReceipt.test.ts, src/shared/lib/receipt-format.test.ts</files>
  <read_first>
    - src/shared/lib/category-tree.test.ts — the established `fast-check` property-test convention in this repo (`fc.assert(fc.property(...))`, arbitrary construction, `numRuns` usage if present).
    - src/shared/lib/groupOrderItemsForReceipt.test.ts — as written in Task 1.
    - src/shared/lib/receipt-format.test.ts — the `baseReceipt()` and pre-cheque fixture factories at the top of the file; new fixtures must extend those, not replace them.
  </read_first>
  <action>
Add three `fast-check` properties to `src/shared/lib/groupOrderItemsForReceipt.test.ts`:

1. Single-category degeneracy (SC-3): for any non-empty array of rows all sharing one non-null `categoryId`/`categoryName`, `groupByCategory` returns exactly one group whose `items` deep-equals the input array in the same order.
2. Total conservation: for any array of rows, the concatenation of all returned groups' `items` is a permutation of the input with the same length — no row is dropped or duplicated.
3. Uncategorized-last invariant: for any array, if a group with `categoryId === null` is present it is at index `length - 1`, and at most one such group exists.

Add two concrete cases to `src/shared/lib/receipt-format.test.ts`:

- A `buildThermalReceiptText` case using a `baseReceipt()` override whose `items` carry two distinct categories and non-empty `modifierNames`: assert the output contains both category names, contains an indented modifier line for a named modifier, and that `Math.max(...text.split('\n').map(l => new TextEncoder().encode(l).length))` is at most 32.
- A `buildThermalReceiptText` case with all items sharing one category: assert `text.split('\n')` contains no line whose trimmed value equals that category name (single group emits no header — SC-3).

Update the existing pre-cheque fixture in `receipt-format.test.ts` to supply the now-required `categoryId`/`categoryName` fields, using `null` for both so every existing pre-cheque assertion keeps its current expected output.
  </action>
  <verify>
    <automated>cd bar-pos && npm run test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/lib/groupOrderItemsForReceipt.test.ts` exits 0 and `grep -c 'fc.assert' src/shared/lib/groupOrderItemsForReceipt.test.ts` returns 3 or more.
    - `npm run test` (full unit suite) exits 0 — no pre-existing test regressed.
    - `grep -n 'TextEncoder' src/shared/lib/receipt-format.test.ts` returns at least 1 match (the 32-byte width assertion exists).
    - `npm run lint` exits 0 with zero warnings.
  </acceptance_criteria>
  <done>Full unit suite green; the degenerate single-category path is machine-proven to leave existing receipts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → renderer | `ReceiptData` arrives from the `process-payment` Edge Function; `categoryName`/`modifierNames` are new untrusted string fields crossing into receipt text. |
| renderer → thermal printer | `pos-printer.ts` passes the built lines to Tauri `print_receipt`; `src-tauri/src/commands/printer.rs` writes `line.as_bytes()` with no escaping or codepage transcoding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | `groupByCategory` / `formatModifierLines` → `printer.rs` byte stream | medium | mitigate | Module-private `sanitize()` strips `\u0000`-`\u001F` and `\u007F`-`\u009F` from every category name and modifier name before it reaches a receipt line, so a catalog-authored ESC (`\u001B`) byte cannot be interpreted as an ESC/POS command by the printer (Task 1). |
| T-25-02 | Denial of Service | 32-column layout with unbounded category names | low | mitigate | Category headers go through `centerLine()`, which truncates at 32 UTF-8 bytes on a character boundary; the 32-byte width assertion in Task 3 is the regression gate. |
| T-25-03 | Information Disclosure | new `ReceiptDataSchema` fields | low | accept | `categoryName` and `modifierNames` are menu/catalog values, not customer data; they are already visible to any authenticated staff member on the POS product grid. No new PII reaches the receipt. |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs zero packages (RESEARCH.md: "Package Legitimacy Audit: N/A — this phase introduces zero new dependencies"). No install task exists to gate. |

ASVS level 1, block-on: high. No high/critical threats in this plan.
</threat_model>

<verification>
1. `npm run test` — full unit suite green.
2. `npm run typecheck` — exits 0.
3. `npm run lint` — exits 0, zero warnings.
4. `git diff --stat src/shared/lib/groupOrderItems.ts` — empty (D-02 boundary held).
</verification>

<success_criteria>
- SC-1: `groupByCategory` groups items into sorted category buckets with a trailing uncategorized bucket, unit- and property-tested.
- SC-2 (partial): both `receipt-format.ts` text builders consume `groupByCategory` and `formatModifierLines`; no hand-rolled modifier loop remains in the file.
- SC-3: property test proves single-category input degenerates to a pass-through group; existing receipt/pre-cheque tests pass unmodified apart from the mechanical fixture widening.
</success_criteria>

<output>
Create `.planning/phases/25-receipt-item-grouping-2-level/25-01-SUMMARY.md` when done.
</output>
