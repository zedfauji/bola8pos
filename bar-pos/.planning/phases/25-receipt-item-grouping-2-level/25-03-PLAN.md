---
phase: 25-receipt-item-grouping-2-level
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/widgets/KdsBoard/index.tsx
  - src/widgets/KdsBoard/KdsCard.test.tsx
autonomous: true
requirements: [SC-2]
user_setup: []

must_haves:
  truths:
    - "A KDS card for an item with 2 modifiers renders 2 separate indented modifier lines, not one slash-joined string (D-04)."
    - "A KDS card for an item with no modifiers renders no modifier block at all — unchanged from today."
    - "The KDS board layout is unchanged: one card per item, no category section headers, no clustering (D-04)."
    - "KdsCard imports formatModifierLines from the shared utility and contains no local modifier-string assembly (SC-2)."
    - "npm run lint stays at zero warnings — the widgets layer is under the i18next/no-literal-string gate."
  artifacts:
    - src/widgets/KdsBoard/KdsCard.test.tsx
  key_links:
    - "formatModifierLines from src/shared/lib/groupOrderItemsForReceipt.ts (plan 01) is the single source of the two-space plus-sign convention shared with the pre-cheque."
---

<objective>
Bring the KDS card's modifier display in line with the pre-cheque convention by consuming plan 01's shared `formatModifierLines`, replacing the card's own slash-joined string.

Purpose: `KdsCard` (in `src/widgets/KdsBoard/index.tsx`) is shared by `/kds` and `/kds-bar` — there is no separate bar-specific card — so one edit covers both boards. Per D-04 this is a formatting change only: the card must NOT consume the category grouper, because cards stay one-per-item and are explicitly not clustered under category headers.

Output: `KdsCard` rendering one line per modifier via the shared formatter, plus the first unit test file for this widget (25-VALIDATION.md Wave-0 item: confirmed no `KdsBoard` test file exists today).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-receipt-item-grouping-2-level/25-CONTEXT.md
@.planning/phases/25-receipt-item-grouping-2-level/25-PATTERNS.md
@.planning/phases/25-receipt-item-grouping-2-level/25-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 25 (new — exclude from drift/source-grounding checks):

| Artifact | Created in | Kind |
|----------|-----------|------|
| `groupByCategory<T>`, `formatModifierLines`, `CategoryGroup<T>`, `CategorizedRow` in `src/shared/lib/groupOrderItemsForReceipt.ts` | plan 01 | new module + exports |
| `ReceiptDataSchema.items[].categoryId` / `.categoryName` / `.modifierNames` | plan 01 | new schema fields |
| `receipt.category.other` (es-MX + en-US) | plan 01 | new i18n keys |
| `src/widgets/KdsBoard/KdsCard.test.tsx` | plan 03 | new test file |
| `CajaReportTopProductSchema.categoryId` / `.categoryName` | plan 04 | new schema fields |
| `supabase/migrations/20260726000001_caja_report_top_products_category.sql` | plan 04 | new migration |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KdsCard renders one line per modifier via the shared formatter</name>
  <files>src/widgets/KdsBoard/index.tsx, src/widgets/KdsBoard/KdsCard.test.tsx</files>
  <read_first>
    - src/widgets/KdsBoard/index.tsx — the full file. `KdsCard` is lines 37-88; the modifier block is lines 61-65 and currently renders a single `<p data-testid="kds-item-modifiers">` containing `item.modifierNames.join(' / ')`. Note the existing `/* eslint-disable i18next/no-literal-string */` fence at lines 21-29 scoping Tailwind class strings — do not widen it.
    - src/shared/lib/groupOrderItemsForReceipt.ts — `formatModifierLines` as shipped by plan 01, including its exact indent and separator.
    - src/entities/kds/model/types.ts — `KdsOrderItemSchema`. `modifierNames: z.array(z.string()).default([])` and `categoryId: UuidSchema` already exist; `notes: z.string().nullable()` already exists and is already rendered by the card. No type change is needed in this plan.
    - src/shared/lib/receipt-format.ts — `buildPreChequeText` as modified by plan 01, to confirm the visual convention being matched (D-04's "match the pre-cheque's indented format").
  </read_first>
  <behavior>
    Tests to write first in `src/widgets/KdsBoard/KdsCard.test.tsx` (React Testing Library + Vitest, rendering the exported `KdsBoard`'s card via a mocked `useKdsItems`, or by exporting `KdsCard` for test if that is the smaller change):
    - An item with `modifierNames: ['Extra cheese', 'No ice']` renders two distinct elements under `data-testid="kds-item-modifiers"`, one containing `Extra cheese` and one containing `No ice`.
    - The rendered modifier text does not contain a slash separator between the two names.
    - An item with `modifierNames: []` renders no element with `data-testid="kds-item-modifiers"`.
    - An item with `notes` set still renders the existing notes paragraph, unchanged.
  </behavior>
  <action>
Import `formatModifierLines` from `@shared/lib/groupOrderItemsForReceipt` at the top of `src/widgets/KdsBoard/index.tsx`, in the `@shared/...` import group alongside `ComboBadge`/`POSButton`/`RoutingBadge`.

Replace `KdsCard`'s modifier block. Keep the `item.modifierNames.length > 0` guard and keep `data-testid="kds-item-modifiers"` on the outer element so existing selectors keep resolving. Inside the guard, call `formatModifierLines(item.modifierNames)` once and render one `<p>` per returned line, keyed by the line string. Each `<p>` keeps the existing `text-sm opacity-80` typography and must add `whitespace-pre` so the formatter's leading indent survives HTML whitespace collapsing; move `mt-1` onto the wrapping element rather than repeating it per line. Move `data-testid="kds-item-modifiers"` onto a wrapping `<div>` when you introduce one.

Render each line as a bare `{line}` expression — the string already carries its separator from the shared formatter, so no new literal appears in JSX and the `i18next/no-literal-string` gate (which runs in `mode: 'all'` over the `widgets` layer) stays satisfied without touching the existing eslint fence. Do not add a translation key for the separator; it is punctuation produced in `shared/lib`, not UI copy.

Change nothing else. Specifically: do not import or call `groupByCategory`, do not add category section headers, do not regroup or reorder cards, do not touch `ComboKdsCard`, and do not touch the `KdsBoard` pending/in-progress column layout. D-04 explicitly forbids category clustering on this surface — cards stay one-per-item.

Create `src/widgets/KdsBoard/KdsCard.test.tsx` implementing the `<behavior>` cases. This is the first test file for this widget (25-VALIDATION.md flagged it as unconfirmed; it does not exist). If reaching `KdsCard` requires rendering the whole `KdsBoard`, prefer adding a named `export` to `KdsCard` over building a heavy `useKdsItems` mock — a named export is the smaller diff and does not change any runtime behavior.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/widgets/KdsBoard/KdsCard.test.tsx && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/widgets/KdsBoard/KdsCard.test.tsx` exits 0 with 4 or more passing assertions.
    - `grep -n "formatModifierLines" src/widgets/KdsBoard/index.tsx` returns exactly 2 matches (import and call).
    - Searching for the old joined rendering finds nothing: `grep -n "modifierNames.join" src/widgets/KdsBoard/index.tsx` returns no output.
    - `grep -c "groupByCategory" src/widgets/KdsBoard/index.tsx` returns 0 — D-04's no-clustering constraint held.
    - `grep -n "kds-item-modifiers" src/widgets/KdsBoard/index.tsx` returns exactly 1 match.
    - `git diff src/widgets/KdsBoard/index.tsx | grep -c '^[-+].*ComboKdsCard'` returns 0 — the combo card is untouched.
    - `npm run lint` exits 0 with zero warnings (the `widgets` layer is under the `i18next/no-literal-string` gate).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Both `/kds` and `/kds-bar` cards show one indented line per modifier sourced from the shared formatter; board layout, combo cards, and notes rendering are unchanged; the widget has its first unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → React render | `KdsOrderItem.modifierNames` originates from the `modifiers` catalog table and is rendered as text in the KDS card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08 | Tampering | modifier names rendered in `KdsCard` | low | mitigate | Names are rendered as React text children (`{line}`), never via `dangerouslySetInnerHTML`, so React escapes them. Plan 01's `formatModifierLines` additionally strips C0/C1 control characters, so the same sanitization that protects the thermal printer also applies here for free. |
| T-25-09 | Information Disclosure | KDS board contents | low | accept | No change to what data the board fetches or who can see it — `useKdsItems` and its RLS scoping are untouched. This plan only changes how already-displayed `modifierNames` are laid out. |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero package installs in this plan (RESEARCH.md Package Legitimacy Audit: N/A). |

ASVS level 1, block-on: high. No high/critical threats in this plan.
</threat_model>

<verification>
1. `npx vitest run src/widgets/KdsBoard/KdsCard.test.tsx` exits 0.
2. `npm run test` — full unit suite green.
3. `npm run lint` — exits 0, zero warnings.
4. `npm run typecheck` — exits 0.
5. `npx playwright test e2e/40-kds-bar.spec.ts` — the bar KDS E2E spec still passes (requires a desktop session and `google-chrome-stable`; skip with a recorded note if no display is available and defer to the phase UAT).
</verification>

<success_criteria>
- SC-2 (KDS surface): `KdsCard` consumes the shared `formatModifierLines` and holds no local modifier-string assembly; `groupByCategory` is deliberately absent per D-04.
</success_criteria>

<output>
Create `.planning/phases/25-receipt-item-grouping-2-level/25-03-SUMMARY.md` when done.
</output>
