---
phase: 33.1-e2e-rbac-drift-fixes
plan: 02
type: execute
wave: 2
depends_on: [33.1-01]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [COMPONENT-04]
tags: [e2e, gate, traceability, bugfix]

must_haves:
  truths:
    - "The 5 originally-failing tests now pass: e2e/06-transfer.spec.ts T4 and T5, e2e/09-rbac.spec.ts T7 (skips cleanly), T-RP-01, and T-RP-02"
    - "e2e/06-transfer.spec.ts passes 5/5 in two consecutive isolated runs, confirming T5's prior failure was a downstream cascade of T4's throw, not an independent bug (D-03)"
    - "All 5 Phase-33 SC3 gate specs pass in isolation: 05-payments, 41-split-payment, 42-tip-distribution, 06-transfer, 09-rbac"
    - "The full Vitest unit suite passes with zero regressions (no test outside Phase 33.1's stated scope regresses; PermissionMatrix.test.tsx still 24/96)"
    - "REQUIREMENTS.md COMPONENT-04 traceability row is annotated to reference Phase 33.1's E2E-gate closure, without duplicating the row or adding a new requirement ID (D-08)"
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "Phase 33.1 fixes <-> COMPONENT-04 'verified by existing E2E specs passing unchanged' clause"
---

<objective>
Prove Phase 33.1 closed Phase 33's Success Criterion 3 gap, then record it. First run the phase gate: re-run `06-transfer` in isolation twice (D-03) to confirm T5 clears as a T4 cascade, then run all 5 Phase-33 gate specs plus the full unit suite to confirm the 5 originally-failing tests pass and nothing outside scope regresses. Then annotate the COMPONENT-04 traceability row in REQUIREMENTS.md (D-08).

Purpose: Turn "fixes landed" into "gate verified + traceability updated" — the deliverable that makes COMPONENT-04's "verified by existing E2E specs passing unchanged" clause true.
Output: Recorded gate results in the SUMMARY + one annotated REQUIREMENTS.md row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-CONTEXT.md
@.planning/phases/33.1-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — 06-transfer isolation x2, 5-spec gate, full unit suite</name>
  <files>none (verification only — reads e2e/*.spec.ts, writes results into the plan SUMMARY)</files>
  <read_first>
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md (Validation Architecture + Open Questions re: the T5 cascade; Common Pitfall 3)
    - .planning/phases/33-payment-critical-page-sweep-isolated/33-VERIFICATION.md (the original Success Criterion 3 gap and the 5 named gate specs)
    - playwright.config.ts (confirms workers:1 / retries:1 — context for why T5's failure cascades from T4)
  </read_first>
  <action>
    With Plan 01's three fixes landed, run the gate. E2E requires the dev server + `.env.local` E2E credentials (see CLAUDE.md); start them per the repo's standard E2E setup before running.

    (a) Per D-03, run `npx playwright test e2e/06-transfer.spec.ts` in isolation twice and confirm 5/5 pass on both runs. This proves T5's prior "Target page, context or browser has been closed" failure was a downstream cascade of T4's throw under `workers:1`, not an independent bug. If T5 still fails after two green-T4 runs, STOP and escalate — do NOT patch transfer-tab or pool-session RPC code (RESEARCH Pitfall 3).

    (b) Run the full Phase-33 SC3 gate — all 5 named specs in isolation — and confirm each passes: `npx playwright test e2e/05-payments.spec.ts`, `e2e/41-split-payment.spec.ts`, `e2e/42-tip-distribution.spec.ts`, `e2e/06-transfer.spec.ts`, `e2e/09-rbac.spec.ts`. In `09-rbac`, confirm T7 reports as skipped (its escape hatch), not failed, and that T-RP-01/T-RP-02 pass.

    (c) Run `npm run test` (full Vitest unit suite) and confirm zero regressions, including PermissionMatrix.test.tsx remaining green at 24/96.

    Record every command's pass/fail result (and T7's skip) in the plan SUMMARY. This task writes no source files.
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/06-transfer.spec.ts` exits 0 on two consecutive isolated runs (5/5 each)
    - `npx playwright test e2e/05-payments.spec.ts e2e/41-split-payment.spec.ts e2e/42-tip-distribution.spec.ts e2e/06-transfer.spec.ts e2e/09-rbac.spec.ts` all pass; 09-rbac T7 is reported skipped (not failed); T-RP-01/T-RP-02 pass
    - `npm run test` exits 0 (no unit regression; PermissionMatrix.test.tsx 24/96)
    - The 5 originally-failing tests (06-transfer T4/T5, 09-rbac T7/T-RP-01/T-RP-02) are confirmed passing/skipping; no test outside Phase 33.1 scope regresses
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/06-transfer.spec.ts && npx playwright test e2e/06-transfer.spec.ts && npx playwright test e2e/05-payments.spec.ts e2e/41-split-payment.spec.ts e2e/42-tip-distribution.spec.ts e2e/09-rbac.spec.ts && npm run test</automated>
  </verify>
  <done>06-transfer passes 5/5 on two isolated runs; all 5 gate specs green (T7 skipped); full unit suite green; results recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate COMPONENT-04 traceability row (D-08)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (traceability-table row `| COMPONENT-04 | Phase 33 | Pending |` at ~line 89; requirement-definition bullet for COMPONENT-04 at ~line 28)
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-CONTEXT.md (D-08)
  </read_first>
  <action>
    Only after Task 1's gate is green (D-08: "once this phase closes"), annotate the existing COMPONENT-04 traceability-table row in `.planning/REQUIREMENTS.md` (currently `| COMPONENT-04 | Phase 33 | Pending |`, ~line 89) to record that Phase 33.1 is what closes COMPONENT-04's clause "verified by existing E2E specs (05-payments, 41-split-payment, 42-tip-distribution, 06-transfer, 09-rbac) passing unchanged" — for example by extending the Phase cell to reference the Phase 33.1 E2E-gate closure. Exact wording is Claude's discretion (per CONTEXT), but it must name Phase 33.1 and convey that the E2E gate is now closed.

    Do NOT duplicate the row and do NOT add a new requirement ID — this inserted phase has no formal ID (ROADMAP lists "Requirements: TBD"). Leave the COMPONENT-04 requirement-definition bullet (~line 28) untouched. Use a scoped Edit (single-row replacement), not a whole-file rewrite.
  </action>
  <acceptance_criteria>
    - `grep -c 'COMPONENT-04' .planning/REQUIREMENTS.md` returns 2 (unchanged: one definition bullet + one traceability row — no duplication)
    - The COMPONENT-04 traceability row now references `33.1`
    - `grep -c '33.1' .planning/REQUIREMENTS.md` returns >= 1
    - The requirement-definition bullet for COMPONENT-04 is byte-unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'COMPONENT-04' .planning/REQUIREMENTS.md && grep -n '33.1' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>COMPONENT-04 traceability row annotated with a Phase 33.1 E2E-gate-closure note; no duplicate row; definition bullet unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright/Vitest runner <-> app under test | Gate execution asserts against the app; test-tooling boundary, not shipped code |
| planning doc edit | REQUIREMENTS.md is a project-planning artifact, not runtime code |

No runtime code is modified in this plan. No auth, session, input-handling, or cryptography surface is touched.

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-04 | Tampering | .planning/REQUIREMENTS.md traceability annotation | low | accept | Documentation-only edit; scoped single-row Edit (no whole-file rewrite) prevents accidental loss of sibling rows. No runtime effect. Accepted. |
| T-33.1-05 | Repudiation | Phase gate results | low | mitigate | Gate command results are recorded verbatim in the plan SUMMARY, giving an auditable record that the 5 tests passed/skipped. |
| T-33.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in Phase 33.1; slopcheck N/A. Accepted. |

All threats are low; none reach the `high` block threshold, so this plan remains autonomous with no blocking checkpoint.
</threat_model>

<verification>
- Gate: `06-transfer` 5/5 twice; 5 named specs green (T7 skipped); `npm run test` green.
- Traceability: exactly 2 `COMPONENT-04` occurrences in REQUIREMENTS.md, the table row referencing 33.1, definition bullet unchanged.
</verification>

<success_criteria>
- The 5 originally-failing tests pass/skip as intended; no out-of-scope regression (Phase 33 SC3 closed).
- COMPONENT-04 traceability row annotated per D-08 without duplication.
</success_criteria>

## Artifacts this phase produces

None — bug-fix / verification phase. No new symbols or files. The only write is a one-row annotation to `.planning/REQUIREMENTS.md`'s existing COMPONENT-04 traceability entry.

<output>
Create `.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-02-SUMMARY.md` when done.
</output>
