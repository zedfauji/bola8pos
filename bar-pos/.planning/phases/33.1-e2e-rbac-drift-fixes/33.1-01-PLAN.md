---
phase: 33.1-e2e-rbac-drift-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/06-transfer.spec.ts
  - src/widgets/OrderPanel/ActiveTabSelector.tsx
  - src/widgets/OrderPanel/OrderPanel.tsx
  - src/widgets/OrderPanel/index.ts
  - e2e/09-rbac.spec.ts
autonomous: true
requirements: [COMPONENT-04]
tags: [e2e, rbac, bugfix, accessibility, drift]

must_haves:
  truths:
    - "e2e/06-transfer.spec.ts T4 asserts exact per-tab toast text ('Tab opened for Transfer Conflict A' / '...B'), so two stacked toasts can no longer trip a Playwright strict-mode violation (D-01)"
    - "The Split-bill button in ActiveTabSelector.tsx exposes a static accessible name 'Split bill' with no interpolated customer name, so /delete tab/i no longer false-matches it (D-04)"
    - "src/widgets/OrderPanel/OrderPanel.tsx no longer exists (dead code deleted) and the barrel exports only CartPanel + ActiveTabSelector (D-05)"
    - "e2e/09-rbac.spec.ts T-RP-01 expects 96 switches (24 rows x 4 roles) and T-RP-02 targets the Kitchen view_kds switch with exact:true (D-07)"
    - "npm run typecheck and npm run lint pass after the edits (no FSD-boundary or type regressions)"
  artifacts:
    - e2e/06-transfer.spec.ts
    - src/widgets/OrderPanel/ActiveTabSelector.tsx
    - src/widgets/OrderPanel/index.ts
    - e2e/09-rbac.spec.ts
  key_links:
    - "ActiveTabSelector Split-bill aria-label <-> e2e/09-rbac.spec.ts T7 /delete tab/i locator (must not collide)"
    - "STAFF_ACTIONS length (24) <-> e2e/09-rbac.spec.ts T-RP-01 expected switch count (96)"
---

<objective>
Land the three independent, already-root-caused drift fixes for Phase 33.1 as isolated commits: (1) scope the T4 toast assertions in `e2e/06-transfer.spec.ts` to exact per-tab text (D-01/D-02, test-side only); (2) replace the Split-bill button's dynamic `aria-label` with a static `"Split bill"` and delete the dead `OrderPanel.tsx` file plus its barrel export (D-04/D-05/D-06, the only shipped-code change); (3) update the stale `88`->`96` switch count and add `exact: true` to the Kitchen view_kds locator in `e2e/09-rbac.spec.ts` (D-07, test-side only).

Purpose: Make the 5 originally-failing tests (06-transfer T4/T5, 09-rbac T7/T-RP-01/T-RP-02) pass, closing Phase 33's Success Criterion 3 gap, with zero product-behavior change.
Output: 5 edited/deleted files across 3 atomic commits. Behavioral confirmation (full E2E gate + unit suite) is verified in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-CONTEXT.md
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-UI-SPEC.md
@.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope T4 toast assertions to exact per-tab text (e2e/06-transfer.spec.ts)</name>
  <files>e2e/06-transfer.spec.ts</files>
  <read_first>
    - e2e/06-transfer.spec.ts (focus on the test titled `T4: transfer tab to already-occupied table — error toast shown`, currently lines ~102-148; it opens two tabs named `Transfer Conflict A` and `Transfer Conflict B` back-to-back)
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md (Root Cause 1 + its Code Examples section for the exact target strings)
  </read_first>
  <action>
    In the test titled `T4: transfer tab to already-occupied table — error toast shown`, replace the two ambiguous shared-regex toast assertions with exact per-tab text (D-01). The first assertion follows the creation of the `Transfer Conflict A` tab (currently line ~112) and must become `await expect(page.getByText('Tab opened for Transfer Conflict A')).toBeVisible({ timeout: 20_000 });`. The second follows the creation of the `Transfer Conflict B` tab (currently line ~119) and must become `await expect(page.getByText('Tab opened for Transfer Conflict B')).toBeVisible({ timeout: 20_000 });`. The toast copy is `Tab opened for {customerName}` (from OpenTabDialog), so these exact strings are unique and mutually non-substring, resolving each locator to a single toast even when both are stacked.

    Fix the assertions inside the two-tab test titled `T4:` — NOT the single-tab test titled `Transfer tab to new table` (line ~28). 33.1-PATTERNS.md points at line 28, but that test opens only one tab and never collides; the real collision site is the `T4:` test's two sequential opens (confirmed against the file). Leave the `/tab opened/i` assertions in every single-tab test (currently lines ~28, ~46, ~66, and the T5 test at ~158) untouched, to minimize diff (D-01/D-03).

    Do NOT change `src/app/App.tsx`'s `<Toaster>` props, any `toast.success()` call site, or add a stable toast `id`/dedup — the toast stacking is intentional, correct UX (D-01/D-02, RESEARCH Pitfall 1). Do NOT add any sleep or wait-for-toast-to-clear helper — exact-text scoping is the non-flaky fix.
  </action>
  <acceptance_criteria>
    - `grep -c "Tab opened for Transfer Conflict A" e2e/06-transfer.spec.ts` returns 1
    - `grep -c "Tab opened for Transfer Conflict B" e2e/06-transfer.spec.ts` returns 1
    - The single-tab tests still use their original `/tab opened/i` assertions (their count is unchanged); no edit to App.tsx or any toast call site (git diff touches only e2e/06-transfer.spec.ts)
    - `npm run lint` passes on the edited file (max-warnings 0)
    - Behavioral proof deferred to Plan 02: `npx playwright test e2e/06-transfer.spec.ts` passes 5/5 in isolation
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Tab opened for Transfer Conflict A" e2e/06-transfer.spec.ts && grep -c "Tab opened for Transfer Conflict B" e2e/06-transfer.spec.ts && npm run lint</automated>
  </verify>
  <done>T4's two toast assertions use exact per-tab text; single-tab tests and all app/toast code untouched; lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Static Split-bill aria-label + delete dead OrderPanel.tsx (ActiveTabSelector.tsx, OrderPanel.tsx, index.ts)</name>
  <files>src/widgets/OrderPanel/ActiveTabSelector.tsx, src/widgets/OrderPanel/OrderPanel.tsx, src/widgets/OrderPanel/index.ts</files>
  <read_first>
    - src/widgets/OrderPanel/ActiveTabSelector.tsx (the Split-bill `POSButton` at lines ~112-128 carrying `data-testid="split-bill-button"`; the sibling static-label buttons at lines ~94 and ~106)
    - src/widgets/OrderPanel/index.ts (3-line barrel)
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-UI-SPEC.md (Interaction & Accessibility Contract — locks the exact `aria-label="Split bill"` string and the deletion scope)
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md (Root Cause 2)
  </read_first>
  <action>
    (a) In `src/widgets/OrderPanel/ActiveTabSelector.tsx`, on the Split-bill `POSButton` (the element with `data-testid="split-bill-button"`, currently line ~123), replace its dynamic aria-label attribute — which currently interpolates `currentTab.customerName` into the accessible name via a template literal — with the exact static attribute `aria-label="Split bill"` (D-04). This matches the static-label convention of the sibling buttons `aria-label="Transfer tab"` (line ~94) and `aria-label="New tab"` (line ~106) in the same file; only one Split-bill button ever renders (gated on `currentTab.status === 'open'`), so no disambiguation is lost. Do NOT touch `data-testid="split-bill-button"`, the visible button text `Split bill`, `disabled`, `title`, or `onClick`.

    (b) Delete the entire file `src/widgets/OrderPanel/OrderPanel.tsx` (D-05). It is confirmed dead code: the `OrderPanel` component is imported nowhere in `src/` (pos/index.tsx composes `ActiveTabSelector` + `CartPanel` directly), and there is no co-located `OrderPanel.stories.tsx` / `OrderPanel.test.tsx` to update.

    (c) In `src/widgets/OrderPanel/index.ts`, remove only the line `export { OrderPanel } from './OrderPanel';`, keeping the `CartPanel` and `ActiveTabSelector` export lines (D-05).

    Make no change to `e2e/09-rbac.spec.ts` T7 — its existing `test.skip()` escape hatch (`getByRole('button', { name: /delete tab/i })` -> not visible -> skip) resolves correctly once this collision is gone (D-06). Do NOT touch `PermissionMatrix.tsx` or `App.tsx` (RESEARCH Pitfall 1).
  </action>
  <acceptance_criteria>
    - `grep -c 'aria-label="Split bill"' src/widgets/OrderPanel/ActiveTabSelector.tsx` returns 1
    - No dynamic aria-label remains in ActiveTabSelector.tsx: `grep -c 'aria-label=' src/widgets/OrderPanel/ActiveTabSelector.tsx` counts only static double-quoted labels (`"Transfer tab"`, `"New tab"`, `"Split bill"` = 3); the file contains no `${` inside any aria-label
    - `src/widgets/OrderPanel/OrderPanel.tsx` no longer exists (`test ! -f src/widgets/OrderPanel/OrderPanel.tsx`)
    - `grep -c OrderPanel src/widgets/OrderPanel/index.ts` returns 0
    - `npm run typecheck` passes and `npm run lint` passes (FSD boundaries intact, max-warnings 0)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'aria-label="Split bill"' src/widgets/OrderPanel/ActiveTabSelector.tsx && test ! -f src/widgets/OrderPanel/OrderPanel.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Split-bill button exposes static `aria-label="Split bill"`; OrderPanel.tsx removed; barrel exports only CartPanel + ActiveTabSelector; typecheck + lint green.</done>
</task>

<task type="auto">
  <name>Task 3: Update stale PermissionMatrix count + exact locator (e2e/09-rbac.spec.ts)</name>
  <files>e2e/09-rbac.spec.ts</files>
  <read_first>
    - e2e/09-rbac.spec.ts (test `T-RP-01` at lines ~250-273: count comments at ~262/~268, `toHaveCount(88)` at ~270; test `T-RP-02` at lines ~275-300: switch locator at ~288)
    - src/widgets/RBACDashboard/PermissionMatrix.test.tsx (already-passing unit test asserting 24 rows / 96 switches — the authoritative reference count; do not recount independently)
    - .planning/phases/33.1-e2e-rbac-drift-fixes/33.1-RESEARCH.md (Root Cause 3)
  </read_first>
  <action>
    In `e2e/09-rbac.spec.ts` (D-07):
    (a) In test `T-RP-01`, change `await expect(switches).toHaveCount(88);` to `await expect(switches).toHaveCount(96);`. Update the two stale count comments accordingly: `Table has 22 action rows — STAFF_ACTIONS has 22 items` becomes `Table has 24 action rows — STAFF_ACTIONS has 24 items`, and `Switch elements present (22 rows × 4 roles = 88 switches)` becomes `Switch elements present (24 rows × 4 roles = 96 switches)`. `STAFF_ACTIONS` now has 24 entries; this matches PermissionMatrix.test.tsx which already asserts 24 rows / 96 switches. Exact comment wording is Claude's discretion, but it must state 24 rows and 96 switches.
    (b) In test `T-RP-02`, add `exact: true` to the switch locator so it reads `page.getByRole('switch', { name: 'Kitchen can view_kds', exact: true })`. Without `exact: true` the plain-string name substring-matches the sibling `Kitchen can view_kds_bar` switch, causing a strict-mode violation.

    Do NOT touch `src/shared/lib/rbac.ts` (`STAFF_ACTIONS`) or `src/widgets/RBACDashboard/PermissionMatrix.tsx` (the `{role} can {action}` aria-label template) — both are correct as-is (D-07, RESEARCH Pitfall 1). No change to T7.
  </action>
  <acceptance_criteria>
    - `grep -c 'toHaveCount(96)' e2e/09-rbac.spec.ts` returns 1
    - `grep -c "name: 'Kitchen can view_kds', exact: true" e2e/09-rbac.spec.ts` returns 1
    - T-RP-01 comments reference "24" rows and "96" switches
    - `src/shared/lib/rbac.ts` and `src/widgets/RBACDashboard/PermissionMatrix.tsx` are unchanged (git diff touches only e2e/09-rbac.spec.ts)
    - `npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx` still passes (24/96)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'toHaveCount(96)' e2e/09-rbac.spec.ts && grep -c "name: 'Kitchen can view_kds', exact: true" e2e/09-rbac.spec.ts && npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx</automated>
  </verify>
  <done>T-RP-01 asserts 96 with 24-row comments; T-RP-02 uses exact:true; no RBAC/app source change; PermissionMatrix unit test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen-reader / DOM <- client render | The Split-bill button's accessible name is exposed to assistive tech and the DOM |
| Playwright test runner <-> app under test | E2E specs assert against app-rendered text/roles (test-tooling boundary, not shipped) |

No new trust boundary is introduced. No auth, session, input-handling, RLS/RBAC-enforcement, or cryptography surface is touched (RESEARCH Security Domain: Not applicable).

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33.1-01 | Information Disclosure | ActiveTabSelector Split-bill button `aria-label` | low | mitigate | Removing the `customerName` interpolation narrows the accessible name to a static string, reducing incidental exposure of the customer name via the accessibility tree. Net reduction, no new exposure. |
| T-33.1-02 | Tampering | e2e/06-transfer.spec.ts & e2e/09-rbac.spec.ts locator edits | low | accept | Test-only files; not shipped in the app bundle. No runtime attack surface. Accepted. |
| T-33.1-03 | Elevation of Privilege | RBAC enforcement (role_permissions / canAccess) | low | accept | Untouched — the T-RP fix is a test-count/locator change only; STAFF_ACTIONS, PermissionMatrix, and RLS are not modified. Accepted. |
| T-33.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in Phase 33.1; slopcheck N/A. Accepted. |

All threats are low; none reach the `high` block threshold, so this plan remains autonomous with no blocking checkpoint.
</threat_model>

<verification>
- Task greps + `npm run typecheck` + `npm run lint` pass (fast, source-level).
- `npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx` remains green (24/96).
- Full E2E gate + unit suite behavioral confirmation is owned by Plan 02 (Wave 2) after these edits land.
</verification>

<success_criteria>
- e2e/06-transfer.spec.ts T4 uses exact per-tab toast text (D-01/D-02).
- ActiveTabSelector Split-bill button uses static `aria-label="Split bill"`; OrderPanel.tsx deleted; barrel exports only CartPanel + ActiveTabSelector (D-04/D-05/D-06).
- e2e/09-rbac.spec.ts T-RP-01 expects 96, T-RP-02 uses `exact: true` (D-07).
- typecheck + lint green; PermissionMatrix unit test green.
</success_criteria>

## Artifacts this phase produces

None — bug-fix only. No new decorators, classes, functions, CLI flags, struct/dataclass fields, or new files. Net change is a deletion (`src/widgets/OrderPanel/OrderPanel.tsx` and its barrel export removed) plus three surgical string/count edits (one static `aria-label`, two E2E spec locators/counts). No new public symbol is created.

<output>
Create `.planning/phases/33.1-e2e-rbac-drift-fixes/33.1-01-SUMMARY.md` when done.
</output>
