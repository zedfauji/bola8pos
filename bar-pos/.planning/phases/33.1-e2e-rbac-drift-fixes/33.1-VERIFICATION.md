---
phase: 33.1-e2e-rbac-drift-fixes
verified: 2026-07-14T18:24:05Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 33.1: E2E Test/RBAC Drift Fixes Verification Report

**Phase Goal:** Fix pre-existing test/seed drift surfaced by Phase 33 verification (none of the affected files were in Phase 33's scope) — specifically: (1) toast-stacking/timing bug causing `e2e/06-transfer.spec.ts` T4/T5 to fail, (2) aria-label collision in `ActiveTabSelector.tsx`/`OrderPanel.tsx` causing `e2e/09-rbac.spec.ts` T7 to fail, (3) `PermissionMatrix.tsx` switch-count/action-set drift causing `e2e/09-rbac.spec.ts` T-RP-01/T-RP-02 to fail. Closes Phase 33's outstanding Success Criterion 3 gap.
**Verified:** 2026-07-14T18:24:05Z
**Status:** passed
**Re-verification:** No — initial verification (no prior 33.1-VERIFICATION.md existed)

## Note on scope deviation (pre-confirmed, not re-litigated here)

Wave 2 (33.1-02) was planned as pure verification ("files: none") but running it surfaced two genuine, previously-undiagnosed test bugs blocking the gate (an unbounded `actionTimeout` + dead click on an already-auto-dismissed dialog in `06-transfer.spec.ts`, and a non-idempotent form-fill in `42-tip-distribution.spec.ts` T1). Both were root-caused and fixed test-side only (`playwright.config.ts`, `e2e/06-transfer.spec.ts`, `e2e/42-tip-distribution.spec.ts`); no application source code changed. A subsequent code-review pass (33.1-REVIEW.md) found two more small issues in the same files, both fixed by the orchestrator (`navigationTimeout` bound, a discarded soft-check result now captured as a test annotation). This verification independently re-confirms all of it against the current codebase state — not the SUMMARY narrative.

## Goal Achievement

### Observable Truths

All 10 truths below are the merged must-haves from `33.1-01-PLAN.md` and `33.1-02-PLAN.md` frontmatter (no separate ROADMAP `success_criteria` array exists for this inserted phase; the ROADMAP goal text is used as the north star and Truth 6/8 map directly to closing Phase 33's SC3 gap).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `e2e/06-transfer.spec.ts` T4 asserts exact per-tab toast text (no strict-mode collision on stacked toasts) | ✓ VERIFIED | Direct read confirms lines 112/119: `getByText('Tab opened for Transfer Conflict A')` and `'...B'`, each exact and unique. `grep -c` for both strings returns 1 each. |
| 2 | Split-bill button exposes static `aria-label="Split bill"` (no customer-name interpolation) | ✓ VERIFIED | `ActiveTabSelector.tsx:123` reads `aria-label="Split bill"` — static, no `${}`. Sibling buttons at lines 94/106 confirm the convention. |
| 3 | `OrderPanel.tsx` deleted; barrel exports only `CartPanel` + `ActiveTabSelector` | ✓ VERIFIED | `test -f src/widgets/OrderPanel/OrderPanel.tsx` → does not exist. `index.ts` contains exactly 2 export lines (`CartPanel`, `ActiveTabSelector`). `grep -rn OrderPanel src/` outside the widget's own directory finds zero references — no dangling imports. |
| 4 | `e2e/09-rbac.spec.ts` T-RP-01 expects 96 switches (24 rows × 4 roles), T-RP-02 uses `exact: true` on the Kitchen `view_kds` locator | ✓ VERIFIED | Line 270: `toHaveCount(96)`. Line 288: `name: 'Kitchen can view_kds', exact: true`. Directly counted `STAFF_ACTIONS` in `src/shared/lib/rbac.ts` = 24 entries; `STAFF_ROLES` = 4. 24×4=96, matches. |
| 5 | `npm run typecheck` and `npm run lint` pass (no new regressions) | ✓ VERIFIED | Ran both independently: typecheck shows only the 2 pre-existing, documented errors (`entities/tab/model/queries.ts:780`, `shared/lib/agent/rag.ts:60`) — zero new. `npm run lint` (`eslint src --max-warnings 0`) exits clean. |
| 6 | The 5 originally-failing tests now pass: `06-transfer` T4/T5, `09-rbac` T7 (skips), T-RP-01, T-RP-02 | ✓ VERIFIED | Independently re-ran both spec files (not taken from SUMMARY): `06-transfer.spec.ts` 5/5 pass (see Truth 7). `09-rbac.spec.ts` full run: 17 passed, 3 skipped, 0 failed — `-g "T7"` isolated run confirms T7 is the skip (`test.skip()` escape hatch fires cleanly, not a failure); T-RP-01/T-RP-02 both in the passed set. |
| 7 | `06-transfer.spec.ts` passes 5/5 in two consecutive isolated runs (D-03 — T5 was a T4 cascade, not independent) | ✓ VERIFIED | Independently re-ran `npx playwright test e2e/06-transfer.spec.ts` **twice** in this verification session (not reusing SUMMARY's runs): run 1 — 5/5 passed (2.0m); run 2 — 5/5 passed (2.0m). A third run earlier in this session also passed 5/5. Confirms the fix is real and reproducible, not a one-off. |
| 8 | All 5 Phase-33 SC3 gate specs pass in isolation: `05-payments`, `41-split-payment`, `42-tip-distribution`, `06-transfer`, `09-rbac` | ✓ VERIFIED | Independently re-ran all 5: `05-payments` 8/8 pass; `41-split-payment` 3/3 pass (a first attempt showed 1 test failing when run as part of the full file — reran the full file again: 3/3 clean, and the same test passed in `-g` isolation — a live-Supabase state flake unrelated to this phase's file scope, matching the exact flakiness pattern already documented in `33-VERIFICATION.md` for other specs, not a regression); `42-tip-distribution` 1/1 pass; `06-transfer` 5/5 (×2, see Truth 7); `09-rbac` 17 passed/3 skipped/0 failed (T7 skip + 2 pre-existing unrelated skips). |
| 9 | Full Vitest unit suite passes with zero regressions (`PermissionMatrix.test.tsx` still 24/96) | ✓ VERIFIED | Independently ran `npm run test`: 136 test files passed, 2 skipped (138); 1225 tests passed, 15 todo, 0 failed — matches SUMMARY's claimed numbers exactly. `PermissionMatrix.test.tsx` run standalone: 4/4 pass, asserting 24 rows / 96 switches. |
| 10 | `REQUIREMENTS.md` COMPONENT-04 traceability row annotated to reference Phase 33.1's E2E-gate closure, without duplication or a new requirement ID | ✓ VERIFIED | Line 89: `\| COMPONENT-04 \| Phase 33 (E2E gate closed by Phase 33.1) \| Complete \|`. `grep -c COMPONENT-04` = 2 (1 definition bullet + 1 row, no duplication). `git show 21d6f9b` confirms a scoped single-row diff; the definition bullet at line 28 is untouched by that commit. No new requirement ID introduced anywhere in `REQUIREMENTS.md` or `ROADMAP.md` for Phase 33.1 (ROADMAP explicitly states "Requirements: TBD ... no new ID"). |

**Score:** 10/10 truths verified (0 present-but-behavior-unverified — every behavior-dependent truth was independently exercised by re-running the actual E2E/unit suites in this verification session, not inferred from SUMMARY claims)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `e2e/06-transfer.spec.ts` | Exact per-tab toast text (T4), dead Close-click removed (T4/T5) | ✓ VERIFIED | Confirmed via direct read + 3 independent full-file runs, all 5/5. |
| `src/widgets/OrderPanel/ActiveTabSelector.tsx` | Static `aria-label="Split bill"` | ✓ VERIFIED | Line 123, confirmed. |
| `src/widgets/OrderPanel/OrderPanel.tsx` | Deleted | ✓ VERIFIED | File does not exist; zero dangling references. |
| `src/widgets/OrderPanel/index.ts` | Barrel exports only `CartPanel` + `ActiveTabSelector` | ✓ VERIFIED | 2-line barrel confirmed. |
| `e2e/09-rbac.spec.ts` | 96-switch count, `exact: true` locator | ✓ VERIFIED | Both greps confirmed; full-file run 17 passed/3 skipped/0 failed. |
| `playwright.config.ts` | `actionTimeout` bounded (15s non-fast), `navigationTimeout` bounded (30s non-fast) | ✓ VERIFIED | Lines 41-42: `actionTimeout: fastE2e ? 10_000 : 15_000`, `navigationTimeout: fastE2e ? 15_000 : 30_000`. The REVIEW.md WR-01 finding (unbounded `navigationTimeout`) has been fixed since the review — confirmed current, not just claimed. |
| `e2e/42-tip-distribution.spec.ts` | Clear-then-fill pattern for T1's 3 percentage inputs | ✓ VERIFIED | Lines 49-54 confirm clear-then-fill for floor/bar/kitchen; independently ran, 1/1 pass. |
| `.planning/REQUIREMENTS.md` | COMPONENT-04 row annotated, no duplication | ✓ VERIFIED | See Truth 10. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| ActiveTabSelector Split-bill `aria-label` | `e2e/09-rbac.spec.ts` T7 `/delete tab/i` locator | Must not collide | ✓ WIRED (no collision) | Static "Split bill" no longer contains any customer-name substring that could match `/delete tab/i`; T7 skips cleanly on its own escape hatch (unrelated: the Delete Tab UI feature isn't present, not an aria-label bug). |
| `STAFF_ACTIONS` length (24) | `e2e/09-rbac.spec.ts` T-RP-01 expected count (96) | Count must match `rows × roles` | ✓ WIRED | Directly counted 24 items in `rbac.ts`; 24×4=96 matches the spec's `toHaveCount(96)` and the independent `PermissionMatrix.test.tsx` unit assertion. |
| Playwright `actionTimeout`/`navigationTimeout` config | Dead-click / stalled-navigation failure mode | Bounds any future stale-element hang | ✓ WIRED | Both bounded in non-fast mode; confirmed by 3 independent green `06-transfer` runs post-fix, and by the SUMMARY's documented pre-fix reproduction (checked out the parent commit, reproduced the identical hang) — the fix demonstrably closes the exact defect it targets. |

### Behavioral Spot-Checks (independently re-run in this verification session — not taken from SUMMARY.md)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `06-transfer` T4/T5 pass twice consecutively (D-03) | `npx playwright test e2e/06-transfer.spec.ts` ×3 in this session | 5/5, 5/5, 5/5 | ✓ PASS |
| `09-rbac` T7 skips, T-RP-01/T-RP-02 pass | `npx playwright test e2e/09-rbac.spec.ts` (full) + `-g "T7"` isolated | 17 passed / 3 skipped / 0 failed; T7 confirmed as the skip | ✓ PASS |
| `42-tip-distribution` T1 passes (non-idempotent-fill fix) | `npx playwright test e2e/42-tip-distribution.spec.ts` | 1/1 passed (50.8s) | ✓ PASS |
| `41-split-payment` all pass | `npx playwright test e2e/41-split-payment.spec.ts` ×2 + `-g "T3"` isolated | Run 1: 2/3 (1 flake, T3) + isolated T3: 1/1 pass; Run 2 (full file): 3/3 pass | ✓ PASS (flake not reproducible; out of this phase's file scope) |
| `05-payments` all pass | `npx playwright test e2e/05-payments.spec.ts` | 8/8 passed | ✓ PASS |
| Full Vitest unit suite, zero regressions | `npm run test` | 136 files passed / 2 skipped; 1225 tests passed / 15 todo / 0 failed | ✓ PASS |
| `PermissionMatrix.test.tsx` still 24/96 | `npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx` | 4/4 passed | ✓ PASS |
| `npm run typecheck` clean (no new errors) | `npm run typecheck` | Only the 2 documented pre-existing errors | ✓ PASS |
| `npm run lint` clean | `npm run lint` (`eslint src --max-warnings 0`) | Exit clean | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|-------------|--------|----------|
| COMPONENT-04 | 33.1-01, 33.1-02 | "verified by existing E2E specs (05-payments, 41-split-payment, 42-tip-distribution, 06-transfer, 09-rbac) passing unchanged" clause — closed by Phase 33.1's fixes | ✓ SATISFIED | All 5 gate specs independently confirmed green in this session (Truth 8); `REQUIREMENTS.md` row updated to reflect closure (Truth 10). |

No orphaned requirements: `grep -n "33.1" .planning/REQUIREMENTS.md` returns only the COMPONENT-04 traceability row; no other requirement ID maps to Phase 33.1 anywhere in `REQUIREMENTS.md` or `ROADMAP.md` (ROADMAP explicitly declares "Requirements: TBD ... no new ID" — this is intentional per D-08, not a gap).

### Anti-Patterns Found

No blocker-level anti-patterns. `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` scan across all files touched by this phase (`e2e/06-transfer.spec.ts`, `e2e/09-rbac.spec.ts`, `e2e/42-tip-distribution.spec.ts`, `src/widgets/OrderPanel/ActiveTabSelector.tsx`, `src/widgets/OrderPanel/index.ts`, `playwright.config.ts`) returns zero matches.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `e2e/06-transfer.spec.ts` | 149 | `` `duplicate-table conflict toast shown: ${conflictToastShown}` `` — boolean interpolated into a template literal triggers `@typescript-eslint/restrict-template-expressions` if `e2e/` were linted with the `src` config | ℹ️ Info | Introduced by the 33.1-REVIEW.md WR-02 remediation commit (`644d870`). Not caught by the project's actual enforced gate (`npm run lint` = `eslint src --max-warnings 0`, scoped to `src/` only — `e2e/` is not linted by the project's CI script). Does not affect test execution (confirmed 5/5 pass, 3× in this session). Cosmetic; trivial fix (`String(conflictToastShown)`) not required for phase-goal achievement. |

The two REVIEW.md Info items (IN-01: "New Tab +" aria-label Label-in-Name mismatch, IN-02: redundant re-check in T-RP-05) are pre-existing conditions outside this phase's fix scope — not introduced by Phase 33.1, not blockers.

### Deferred Items

None — all 10 must-haves resolved within this phase; nothing pushed to a later phase.

### Gaps Summary

None. Every truth declared in both plans' `must_haves.truths` frontmatter is independently verified against the current codebase and current live-Supabase-backed test runs — not inferred from SUMMARY.md's narrative. The phase's central deviation (Wave 2 discovering and fixing two genuine test bugs instead of doing pure verification) is fully substantiated: the `06-transfer.spec.ts` dead-click fix and `playwright.config.ts` timeout bounds were independently re-run 3× (5/5 every time), and the `42-tip-distribution.spec.ts` clear-then-fill fix was independently re-run (1/1). The one transient flake observed (`41-split-payment` T3, once) was reproduced as passing both in isolation and on a full-file rerun, and traces to a file entirely outside Phase 33.1's declared scope — consistent with the live-Supabase test-flakiness pattern already documented in Phase 33's own verification report, not a regression from this phase.

### Human Verification Required

None. All must-haves were verified programmatically, including full behavioral re-execution of every claimed E2E/unit test result (not accepted from SUMMARY.md at face value).

---

_Verified: 2026-07-14T18:24:05Z_
_Verifier: Claude (gsd-verifier)_
