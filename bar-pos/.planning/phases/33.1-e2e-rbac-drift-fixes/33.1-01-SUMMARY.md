---
phase: 33.1-e2e-rbac-drift-fixes
plan: 01
subsystem: testing
tags: [playwright, e2e, rbac, aria-label, accessibility, drift]

# Dependency graph
requires:
  - phase: 33-payment-critical-page-sweep-isolated
    provides: The 5 gate E2E specs (05-payments, 41-split-payment, 42-tip-distribution, 06-transfer, 09-rbac) and the Success Criterion 3 gap this plan closes
provides:
  - Exact-text toast assertions in e2e/06-transfer.spec.ts T4 (no more strict-mode collision on stacked toasts)
  - Static aria-label="Split bill" on ActiveTabSelector's Split-bill button (no more customer-name interpolation collision)
  - Deletion of dead src/widgets/OrderPanel/OrderPanel.tsx and its barrel export
  - Corrected 96-switch count + exact:true locator in e2e/09-rbac.spec.ts T-RP-01/T-RP-02
affects: [33.1-02-e2e-rbac-drift-fixes-wave-2, phase-33-verification-reclose]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - e2e/06-transfer.spec.ts
    - src/widgets/OrderPanel/ActiveTabSelector.tsx
    - src/widgets/OrderPanel/index.ts
    - e2e/09-rbac.spec.ts

key-decisions:
  - "Toast stacking is correct, intended UX (sonner default 4s lifetime + no id-based dedup) — fixed the test's locator specificity, not App.tsx or any toast.success() call site"
  - "Deleted OrderPanel.tsx outright (D-05) rather than mirroring the aria-label fix into dead code — confirmed zero imports anywhere in src/ outside its own barrel"
  - "PermissionMatrix drift (88->96) was purely a stale E2E test expectation — rbac.ts and PermissionMatrix.tsx are correct as-is, already matched by the passing PermissionMatrix.test.tsx unit test"

patterns-established: []

requirements-completed: [COMPONENT-04]

coverage:
  - id: D1
    description: "e2e/06-transfer.spec.ts T4's two toast assertions use exact per-tab text (Tab opened for Transfer Conflict A / B) instead of the ambiguous /tab opened/i regex"
    requirement: "COMPONENT-04"
    verification:
      - kind: other
        ref: "grep -c 'Tab opened for Transfer Conflict A' e2e/06-transfer.spec.ts && grep -c 'Tab opened for Transfer Conflict B' e2e/06-transfer.spec.ts (both return 1)"
        status: pass
      - kind: e2e
        ref: "npx playwright test e2e/06-transfer.spec.ts (full T4/T5 behavioral run deferred to Plan 02 per plan's own verification note — requires live dev server + Supabase, unavailable in this execution environment)"
        status: unknown
    human_judgment: false
  - id: D2
    description: "ActiveTabSelector's Split-bill button exposes a static aria-label=\"Split bill\" (no interpolated customer name); OrderPanel.tsx deleted; barrel exports only CartPanel + ActiveTabSelector"
    requirement: "COMPONENT-04"
    verification:
      - kind: other
        ref: "grep -c 'aria-label=\"Split bill\"' src/widgets/OrderPanel/ActiveTabSelector.tsx (1); test ! -f src/widgets/OrderPanel/OrderPanel.tsx; grep -c OrderPanel src/widgets/OrderPanel/index.ts (0)"
        status: pass
      - kind: other
        ref: "npm run typecheck (only the 2 pre-existing, unrelated errors in entities/tab/model/queries.ts and shared/lib/agent/rag.ts)"
        status: pass
      - kind: other
        ref: "npx eslint src/widgets/OrderPanel/ActiveTabSelector.tsx src/widgets/OrderPanel/index.ts --max-warnings 0 (exit 0, only pre-existing boundaries-plugin legacy-selector warning)"
        status: pass
    human_judgment: false
  - id: D3
    description: "e2e/09-rbac.spec.ts T-RP-01 expects 96 switches (24 rows x 4 roles) with updated comments; T-RP-02 uses exact:true on the Kitchen view_kds locator"
    requirement: "COMPONENT-04"
    verification:
      - kind: other
        ref: "grep -c 'toHaveCount(96)' e2e/09-rbac.spec.ts (1); grep -c \"name: 'Kitchen can view_kds', exact: true\" e2e/09-rbac.spec.ts (1)"
        status: pass
      - kind: unit
        ref: "npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx (already asserts 24/96 unchanged; could not execute in this sandbox — vitest globalSetup requires live network reachability to the project's remote Supabase instance, unavailable here)"
        status: unknown
    human_judgment: true
    rationale: "Both e2e and unit behavioral runs require network access to the live remote Supabase project (globalSetup fetches it, e2e specs need loginAs against real auth), which is unavailable in this execution sandbox. Structural/grep verification and typecheck/lint (file-scoped) passed; git diff confirms zero changes to rbac.ts/PermissionMatrix.tsx/App.tsx. Plan 02 (Wave 2) owns the full behavioral E2E gate + unit suite confirmation per this plan's own <verification> section."

duration: 25min
completed: 2026-07-14
status: complete
---

# Phase 33.1 Plan 01: E2E/RBAC Drift Fixes (Wave 1) Summary

**Three surgical drift fixes: exact-text toast assertions, a static Split-bill aria-label + dead-code deletion, and a corrected 88->96 PermissionMatrix switch count — zero product-behavior change.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-14
- **Tasks:** 3
- **Files modified:** 5 (4 modified, 1 deleted)

## Accomplishments
- `e2e/06-transfer.spec.ts` T4 now asserts exact per-tab toast text, eliminating the Playwright strict-mode violation caused by two stacked "tab opened" toasts (sonner's intentional 4s stacking behavior, unchanged)
- `ActiveTabSelector.tsx`'s Split-bill button uses a static `aria-label="Split bill"` matching its sibling buttons' convention, removing the aria-label collision that broke `e2e/09-rbac.spec.ts` T7's `test.skip()` escape hatch
- Deleted the dead `src/widgets/OrderPanel/OrderPanel.tsx` file (confirmed unimported anywhere in `src/` outside its own barrel) and its barrel export
- `e2e/09-rbac.spec.ts` T-RP-01/T-RP-02 updated to the current `STAFF_ACTIONS` count (24 rows / 96 switches) and an `exact: true` locator, matching the already-correct app and its already-passing unit test

## Task Commits

Each task was committed atomically:

1. **Task 1: Scope T4 toast assertions to exact per-tab text** - `94ee1f1` (test)
2. **Task 2: Static Split-bill aria-label + delete dead OrderPanel.tsx** - `db8d9de` (fix)
3. **Task 3: Update stale PermissionMatrix count + exact locator** - `f82afb2` (test)

_No TDD tasks in this plan — all three are surgical, already-root-caused drift fixes._

## Files Created/Modified
- `e2e/06-transfer.spec.ts` - T4's two toast assertions scoped to exact per-tab text
- `src/widgets/OrderPanel/ActiveTabSelector.tsx` - Split-bill button's `aria-label` changed from `` `Split tab ${currentTab.customerName}` `` to static `"Split bill"`
- `src/widgets/OrderPanel/OrderPanel.tsx` - deleted (dead code, confirmed unimported)
- `src/widgets/OrderPanel/index.ts` - removed the `OrderPanel` barrel export line
- `e2e/09-rbac.spec.ts` - T-RP-01 count `88`->`96` + comments; T-RP-02 locator gained `exact: true`

## Decisions Made
- Toast stacking is correct, intended sonner UX — fixed via test-side exact-text scoping only, no `App.tsx`/`toast.success()` call-site changes (per CONTEXT.md D-01/D-02)
- Deleted `OrderPanel.tsx` outright rather than mirroring the fix into dead code, per CONTEXT.md D-05's scope expansion — confirmed zero live imports via grep before deletion
- No changes to `rbac.ts` or `PermissionMatrix.tsx` — root cause was purely a stale E2E expectation, already corroborated by the passing `PermissionMatrix.test.tsx` unit test (24/96)

## Deviations from Plan

None - plan executed exactly as written. All three tasks matched their `<action>` blocks precisely; no Rule 1-4 auto-fixes were needed.

## Issues Encountered

**Behavioral test execution unavailable in this sandbox.** The plan's per-task `<verify>` blocks specify running `npm run lint` (file-scoped, done and passing) and `npx vitest run src/widgets/RBACDashboard/PermissionMatrix.test.tsx` (Task 3). The latter could not be executed here: this repo's `vitest.config.ts` globalSetup (`src/test/global-setup.ts`) unconditionally fetches the live remote Supabase project on every `vitest run` invocation (even for pure unit/component tests with no DB dependency) and throws if unreachable. This sandbox has no outbound network access to `https://shsrhxleopmovzpzqmex.supabase.co` (confirmed via `curl`, connection timeout). This is a pre-existing test-infrastructure constraint, not something introduced or fixable within this plan's scope.

All structural verification that does not require live network passed cleanly:
- All `grep`-based acceptance criteria for all 3 tasks: PASS
- `npm run typecheck`: PASS (only the 2 pre-existing, unrelated errors already documented in STATE.md — `entities/tab/model/queries.ts:780` and `shared/lib/agent/rag.ts:60`)
- `npx eslint` on each task's changed files: PASS (exit 0; only pre-existing, unrelated errors confirmed via diff-scope check — e.g. a pre-existing non-null-assertion at `e2e/06-transfer.spec.ts:77` and `e2e/09-rbac.spec.ts:85`, both outside this plan's diff)
- `git diff`/`git status` confirms zero unintended changes to `App.tsx`, `PermissionMatrix.tsx`, or `rbac.ts`

Per this plan's own `<verification>` section: "Full E2E gate + unit suite behavioral confirmation is owned by Plan 02 (Wave 2) after these edits land." This defers the live-network-dependent runs to Plan 02, which is expected to have Supabase connectivity.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02 (Wave 2) should run, with live Supabase connectivity available:
- `npx playwright test e2e/06-transfer.spec.ts` (confirm T4/T5 5/5 pass, per RESEARCH.md's D-03 note that T5 is a likely downstream cascade artifact of T4)
- `npx playwright test e2e/09-rbac.spec.ts` (confirm T7 skips cleanly, T-RP-01/T-RP-02 pass)
- `npm run test` (full unit suite, confirm zero regressions including `PermissionMatrix.test.tsx`)
- All 5 of Phase 33's required gate specs (`05-payments`, `41-split-payment`, `42-tip-distribution`, `06-transfer`, `09-rbac`) green in isolation, closing Phase 33's Success Criterion 3 gap

---
*Phase: 33.1-e2e-rbac-drift-fixes*
*Completed: 2026-07-14*

## Self-Check: PASSED

- FOUND: e2e/06-transfer.spec.ts
- FOUND: src/widgets/OrderPanel/ActiveTabSelector.tsx
- CONFIRMED DELETED: src/widgets/OrderPanel/OrderPanel.tsx
- FOUND: src/widgets/OrderPanel/index.ts
- FOUND: e2e/09-rbac.spec.ts
- FOUND commit: 94ee1f1
- FOUND commit: db8d9de
- FOUND commit: f82afb2
