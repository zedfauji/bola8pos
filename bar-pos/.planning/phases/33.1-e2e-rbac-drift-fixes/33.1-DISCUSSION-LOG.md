# Phase 33.1: E2E Test/RBAC Drift Fixes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-13
**Phase:** 33.1-e2e-rbac-drift-fixes
**Areas discussed:** OrderPanel.tsx mirror edit, Sonner toast id fix, REQUIREMENTS.md traceability

---

## OrderPanel.tsx dead-code aria-label bug

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror the fix | 1-line edit matching ActiveTabSelector.tsx's fix. Zero test impact today, cheap insurance against resurfacing. | |
| Delete OrderPanel.tsx entirely | Confirmed dead code — remove instead of patching a bug nobody runs. | ✓ |
| Leave it untouched | Out of scope — only fix what's provably breaking a test today. | |

**User's choice:** Delete OrderPanel.tsx entirely (and its barrel export in index.ts).
**Notes:** User asked for clarification twice before deciding — wanted to understand (1) the bug mechanism (aria-label interpolating customer name → substring match collision), (2) why OrderPanel.tsx is dead code (superseded by the ActiveTabSelector/CartPanel split, git history confirms it predates the split and was never removed), and (3) whether any new feature made it obsolete (no — pure refactor leftover). After confirming it's provably unused with zero import references, chose deletion over patch-in-place.

---

## Sonner toast-stacking UX vs. test-only fix

| Option | Description | Selected |
|--------|-------------|----------|
| Test-side only | Scope getByText assertions to exact text per toast. Zero prod behavior change. | ✓ |
| Also add stable toast id | App-side dedup so rapid duplicate toasts replace instead of stack. Real UX change, unverified against sonner docs. | |

**User's choice:** Test-side only.
**Notes:** User asked why stacking happens in the app at all — confirmed it's sonner's out-of-the-box defaults (`TOAST_LIFETIME=4000`, `VISIBLE_TOASTS_AMOUNT=3`), never explicitly configured, not a deliberate product decision. After that explanation, user agreed stacking is fine UX (each action gets its own confirmation) and the bug is purely in the test's ambiguous regex assertion.

---

## REQUIREMENTS.md traceability annotation

| Option | Description | Selected |
|--------|-------------|----------|
| Annotate now | Small doc edit, keeps traceability table accurate as soon as phase closes SC3. | ✓ |
| Skip, leave for later | Defer to a docs-update pass. | |

**User's choice:** Annotate now.
**Notes:** No further clarification requested.

---

## Claude's Discretion

- Exact wording of updated code comments in `e2e/06-transfer.spec.ts` and `e2e/09-rbac.spec.ts`.
- Sequencing of the `OrderPanel.tsx` deletion and its barrel-export removal (same commit either order).

## Deferred Ideas

None — discussion stayed within phase scope.
