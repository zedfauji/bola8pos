---
phase: 17-modifier-inventory-rules
plan: 04
type: execute
wave: 3
depends_on: ["17-01", "17-03"]
files_modified:
  - src/entities/modifier-inventory-rule/model/queries.ts
  - src/entities/modifier-inventory-rule/model/types.ts
  - src/entities/modifier-inventory-rule/index.ts
  - src/entities/tab/model/depletion.integration.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "useModifierInventoryRules(modifierId) fetches the rules for one modifier; useMutationSaveModifierInventoryRules replaces all rows for a modifier (delete-all-then-insert)"
    - "The mutation supports saving N ingredient rows for one modifier (D-03)"
    - "Integration test proves deplete_for_order_item writes a stock_movements row with ref_type='order_item_modifier' for a modifier-tagged order_item (SC-2)"
    - "Integration test proves a recipe-less product with a modifier still depletes (D-04) and a recipe-only order (no modifiers) still writes exactly the recipe rows with ref_type='order_item' (SC-4)"
  artifacts:
    - path: "src/entities/modifier-inventory-rule/model/queries.ts"
      provides: "modifierInventoryRuleKeys + useModifierInventoryRules + useMutationSaveModifierInventoryRules"
      exports: ["modifierInventoryRuleKeys", "useModifierInventoryRules", "useMutationSaveModifierInventoryRules"]
    - path: "src/entities/modifier-inventory-rule/index.ts"
      provides: "entity public barrel"
      contains: "useModifierInventoryRules"
    - path: "src/entities/tab/model/depletion.integration.test.ts"
      provides: "I5/I6 modifier-depletion integration cases"
      contains: "order_item_modifier"
  key_links:
    - from: "src/entities/modifier-inventory-rule/model/queries.ts"
      to: "modifier_inventory_rules"
      via: "db.from('modifier_inventory_rules') delete/insert/select"
      pattern: "modifier_inventory_rules"
    - from: "src/entities/modifier-inventory-rule/model/queries.ts"
      to: "src/shared/lib/domain.ts"
      via: "ModifierInventoryRuleSchema.parse"
      pattern: "ModifierInventoryRuleSchema"
---

<objective>
Build the data-access layer for modifier inventory rules — a new `entities/modifier-inventory-rule/` FSD slice with a read hook and a delete-all-then-insert save mutation (SC-1, D-03) — and prove the v3 RPC behavior end-to-end with new integration cases (SC-2, SC-4, D-04).

Purpose: The entity is the typed CRUD surface the admin UI (17-05) consumes. The integration cases are the automated proof that modifier deltas write to the ledger via the distinct `order_item_modifier` ref_type without disturbing recipe depletion.

Output: `entities/modifier-inventory-rule/` slice; extended `depletion.integration.test.ts` with modifier cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modifier-inventory-rules/17-PATTERNS.md
@.planning/phases/17-modifier-inventory-rules/17-RESEARCH.md
@src/entities/recipe/model/queries.ts
@src/entities/recipe/index.ts
@src/entities/recipe/model/types.ts
@src/entities/tab/model/depletion.integration.test.ts
</context>

## Artifacts this phase produces

This plan produces:
- `src/entities/modifier-inventory-rule/model/queries.ts` → `modifierInventoryRuleKeys`, `useModifierInventoryRules(modifierId)`, `useMutationSaveModifierInventoryRules()`.
- `src/entities/modifier-inventory-rule/model/types.ts` → re-exports `ModifierInventoryRuleSchema`, `ModifierInventoryRuleCreateSchema`, `ModifierInventoryRule`, `ModifierInventoryRuleCreate` from domain.
- `src/entities/modifier-inventory-rule/index.ts` → public barrel.
- Integration cases I5/I6 in `depletion.integration.test.ts`.

(Full phase artifact inventory is listed in 17-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Create entities/modifier-inventory-rule FSD slice</name>
  <files>src/entities/modifier-inventory-rule/model/queries.ts, src/entities/modifier-inventory-rule/model/types.ts, src/entities/modifier-inventory-rule/index.ts</files>
  <read_first>
    - src/entities/recipe/model/queries.ts (full file: the eslint-disable header + `const db = supabase as any` cast, the query-key factory, the query hook, and the delete-all-then-insert `useMutationSaveRecipe` mutation — the exact shapes to clone)
    - src/entities/recipe/model/types.ts (the domain re-export pattern — clone verbatim for the new entity's types.ts)
    - src/entities/recipe/index.ts (the explicit named-export barrel — no `export *`, banned by ESLint)
    - src/entities/ingredient/index.ts (second barrel example)
    - .planning/phases/17-modifier-inventory-rules/17-PATTERNS.md (section "entities/modifier-inventory-rule/model/queries.ts" — has the full adapted code)
  </read_first>
  <action>
    Create `model/types.ts` re-exporting `ModifierInventoryRuleSchema`, `ModifierInventoryRuleCreateSchema`, and types `ModifierInventoryRule`, `ModifierInventoryRuleCreate` from `@shared/lib/domain` (clone recipe's types.ts pattern; single source of truth stays domain.ts).
    Create `model/queries.ts` with the file-level `/* eslint-disable @typescript-eslint/no-explicit-any, @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-call, @typescript-eslint/no-unsafe-member-access */` header and `const db = supabase as any` (pre-regen cast; the table exists in the DB but not yet in supabase.types.ts generics). Export `modifierInventoryRuleKeys = { all: ['modifier_inventory_rules'] as const, byModifier: (id) => [...all, 'modifier', id] as const }`. Export `useModifierInventoryRules(modifierId: string | null)` — a `useQuery` enabled only when modifierId is non-empty, selecting `*` from `modifier_inventory_rules` where `modifier_id = modifierId`, mapping each row through `ModifierInventoryRuleSchema.parse({ id, modifierId: row['modifier_id'], ingredientId: row['ingredient_id'], delta: Number(row['delta']) })`, `staleTime: 5 * 60 * 1000`. Export `useMutationSaveModifierInventoryRules()` returning `Result<ModifierInventoryRule[]>`: (1) DELETE from modifier_inventory_rules where modifier_id = input.modifierId; (2) if input.rules.length > 0, INSERT rows mapped to `{ modifier_id, ingredient_id, delta }` (supports N rows — D-03); (3) re-select and parse canonical rows; return `ok(parsed)` / `err({ code: 'SUPABASE_ERROR', message })` on any error via the `err`/`ok` from `@shared/lib/result` and `logger` from `@shared/lib/logger-instance`. NO parent-upsert step (the modifier row already exists — unlike recipes). `onSuccess` invalidates `modifierInventoryRuleKeys.byModifier(variables.modifierId)`. The mutation input type must use `rules: ModifierInventoryRuleCreate[]` and `modifierId: string` (write `field: T | undefined` not `field?: T` for any optional — exactOptionalPropertyTypes).
    Create `index.ts` barrel exporting (explicit named exports only — `export *` is banned): the three query symbols and the four type/schema symbols from `./model/queries` and `./model/types`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint src/entities/modifier-inventory-rule</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run lint` reports 0 errors for the new files.
    - Source assertion: `grep -rn "useModifierInventoryRules\|useMutationSaveModifierInventoryRules\|modifierInventoryRuleKeys" src/entities/modifier-inventory-rule/index.ts` returns all three.
    - Source assertion: `queries.ts` contains `const db = supabase as any` with the file-level eslint-disable header, and the mutation does NOT call `.upsert(` (no parent-upsert step).
    - Source assertion: `index.ts` contains no `export *`.
  </acceptance_criteria>
  <done>entities/modifier-inventory-rule slice exists with read hook + delete-all-then-insert save mutation + explicit barrel; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend depletion.integration.test.ts with modifier cases (I5/I6)</name>
  <files>src/entities/tab/model/depletion.integration.test.ts</files>
  <read_first>
    - src/entities/tab/model/depletion.integration.test.ts (full file: the beforeAll setup, the per-test fresh-order_item pattern, the afterAll cleanup lists, and I1-I4 assertion style)
    - supabase/migrations/20260706000002_modifier_inventory_rules_table.sql (column names for the insert)
    - .planning/phases/17-modifier-inventory-rules/17-RESEARCH.md (Validation Architecture table: I5 requirement)
  </read_first>
  <action>
    In `beforeAll`, after the existing setup: insert a `modifiers` row (`{ name: '__test_extra_lime__', price_delta: 0, sort_order: 0 }`) capturing its id as `modifierId`; insert a `modifier_inventory_rules` row `{ modifier_id: modifierId, ingredient_id: ingredientBId, delta: 3 }`; also create a SECOND product with NO recipe (`__test_bottle__`, e.g. base_price 3.0, same category) capturing `recipelessProductId` — for the D-04 case. Add two fresh order_item inserts: `modifierItemId` (product = the recipe-backed `productId`, quantity 1, `modifier_ids: [modifierId]`) and `recipelessModifierItemId` (product = `recipelessProductId`, quantity 1, `modifier_ids: [modifierId]`). Extend the `insertItem` helper or add a variant that accepts `product_id` + `modifier_ids`.
    Add `it('I5: modifier rule writes an order_item_modifier stock_movement alongside recipe rows', ...)`: call `anonClient.rpc('deplete_for_order_item', { p_order_item_id: modifierItemId, p_direction: 1, p_allow_negative: false })`, expect no error; then query stock_movements where `ref_id = modifierItemId AND ref_type = 'order_item_modifier'` and assert exactly 1 row with `quantity_delta` ≈ -3 (= -(1 × qty(1) × delta(3))); also assert the recipe rows still appear with `ref_type = 'order_item'` for the same item (2 rows), proving no collision.
    Add `it('I6: recipe-less product with a modifier still depletes (D-04)', ...)`: call the RPC for `recipelessModifierItemId`, expect no error, assert exactly 1 stock_movements row with `ref_type = 'order_item_modifier'`, `quantity_delta` ≈ -3, and ZERO rows with `ref_type = 'order_item'` (the product has no recipe).
    Add a comment noting I1-I4 (recipe-only, no modifiers) MUST still pass unchanged (SC-4 regression). In `afterAll`, add cleanup: delete modifier_inventory_rules where modifier_id = modifierId, delete the modifiers row, delete the new order_items and the recipeless product; add the new ingredient/item ids to the existing stock_movements + order_items cleanup filters.
  </action>
  <verify>
    <automated>npx vitest run src/entities/tab/model/depletion.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - When `SUPABASE_SERVICE_ROLE_KEY` + `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` are present: `npx vitest run src/entities/tab/model/depletion.integration.test.ts` passes I1-I6 (I1-I4 unchanged, I5/I6 new). When creds absent, the suite is `describe.skipIf`-skipped (still exits 0).
    - Source assertion: `grep -c "order_item_modifier" src/entities/tab/model/depletion.integration.test.ts` >= 2 (I5 + I6).
    - Source assertion: I6 asserts zero `ref_type='order_item'` rows for the recipe-less product (D-04 proof).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Integration test covers modifier-driven depletion (I5), recipe-less modifier depletion (I6), and preserves I1-I4 recipe-only regression; cleanup extended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React client → `modifier_inventory_rules` (PostgREST) | The entity hooks issue authenticated REST reads/writes to the new table; RLS (17-02) governs writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering (V5) | `useModifierInventoryRules` row mapping | mitigate | Every DB row is run through `ModifierInventoryRuleSchema.parse` before entering React state — a malformed/zero delta from the DB is rejected at the boundary, not silently rendered. |
| T-17-11 | Information Disclosure | `db = supabase as any` cast masking a query error | mitigate | Query/mutation errors are logged via `logger.error` and surfaced as `Result` `err(...)`; the cast only bypasses generic typing (documented CLAUDE.md workaround), not error handling. |
| T-17-12 | Elevation of Privilege (V4) | save mutation replace strategy under a bartender session | accept | The delete-all-then-insert is governed server-side by the `modifier_inventory_rules_write_manager` RLS policy (17-02); a bartender's delete/insert is rejected by RLS regardless of what the client attempts. Verified by the manual RLS check in 17-VALIDATION.md. |
| T-17-SC | Tampering | package installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` exit 0.
- `npx vitest run src/entities/tab/model/depletion.integration.test.ts` passes I1-I6 (with creds) or skips cleanly (without).
- `npm run test` (full unit suite) shows no regressions.
</verification>

<success_criteria>
- SC-1: rules are readable/writable through a typed entity with parse-on-read validation, supporting N rows per modifier (D-03).
- SC-2: integration test proves modifier deltas hit the ledger with `ref_type='order_item_modifier'`.
- SC-4: I1-I4 recipe-only cases unchanged; I6 proves recipe-independence (D-04) without disturbing recipe rows.
</success_criteria>

<output>
Create `.planning/phases/17-modifier-inventory-rules/17-04-SUMMARY.md` when done.
</output>
