---
phase: 17-modifier-inventory-rules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260706000002_modifier_inventory_rules_table.sql
  - supabase/migrations/20260706000003_deplete_for_order_item_v3.sql
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "modifier_inventory_rules table exists with signed delta (D-02) and a (modifier_id, ingredient_id) unique key allowing N ingredients per modifier (D-03)"
    - "deplete_for_order_item runs the modifier-rule loop independently of whether the product has a base recipe (D-04)"
    - "modifier-driven stock movements use ref_type='order_item_modifier' so they never collide with the recipe loop's ref_type='order_item' on the same order_item+ingredient"
    - "modifier-driven depletion reuses the exact p_allow_negative bypass + audit_log 'stock_override' insert as the recipe loop (D-05)"
  artifacts:
    - path: "supabase/migrations/20260706000002_modifier_inventory_rules_table.sql"
      provides: "modifier_inventory_rules table + RLS (select authenticated, write manager/admin)"
      contains: "CREATE TABLE modifier_inventory_rules"
    - path: "supabase/migrations/20260706000003_deplete_for_order_item_v3.sql"
      provides: "deplete_for_order_item(uuid, smallint, boolean) with recipe loop + modifier loop"
      contains: "order_item_modifier"
  key_links:
    - from: "supabase/migrations/20260706000003_deplete_for_order_item_v3.sql"
      to: "modifier_inventory_rules"
      via: "SELECT ingredient_id, delta FROM modifier_inventory_rules WHERE modifier_id = ANY(v_modifier_ids)"
      pattern: "modifier_inventory_rules WHERE modifier_id = ANY"
    - from: "supabase/migrations/20260706000003_deplete_for_order_item_v3.sql"
      to: "record_stock_movement"
      via: "PERFORM record_stock_movement(..., 'order_item_modifier', ...)"
      pattern: "'order_item_modifier'"
---

<objective>
Author the two SQL migrations that make modifiers deplete inventory: the `modifier_inventory_rules` join table (SC-1) and the v3 rewrite of `deplete_for_order_item` that adds a modifier-driven depletion loop alongside the existing recipe loop (SC-2), while keeping recipe-only depletion byte-for-byte unchanged (SC-4).

Purpose: This is the load-bearing backend change. Getting the migration base body right (the CURRENT one, not the stale `_v2`), the recipe-independence change (D-04), and the `ref_type='order_item_modifier'` collision-avoidance tag (Claude's-discretion item, research-resolved) is the entire risk of this phase.

Output: two new migration files. NOT pushed here — push is the blocking gate in plan 17-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modifier-inventory-rules/17-CONTEXT.md
@.planning/phases/17-modifier-inventory-rules/17-RESEARCH.md
@.planning/phases/17-modifier-inventory-rules/17-PATTERNS.md
@supabase/migrations/20260510000002_rpc_role_guards.sql
@supabase/migrations/20260428000001_recipes_tables.sql
@supabase/migrations/20260426000003_record_stock_movement_rpc.sql
@supabase/migrations/20260426000002_stock_movements_idempotency_index.sql
</context>

## Artifacts this phase produces

This plan produces:
- Table `modifier_inventory_rules(id uuid PK, modifier_id uuid FK→modifiers, ingredient_id uuid FK→ingredients, delta numeric CHECK(delta <> 0), UNIQUE(modifier_id, ingredient_id))`.
- RLS policies `modifier_inventory_rules_select_authenticated` and `modifier_inventory_rules_write_manager`.
- `deplete_for_order_item(uuid, smallint, boolean)` v3 body: preserved kitchen role guard, recipe loop wrapped in `IF FOUND`, new modifier loop, `ref_type='order_item_modifier'`, shared `p_allow_negative`/`audit_log 'stock_override'` branch.

(Full phase artifact inventory is listed in 17-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Create modifier_inventory_rules table migration + RLS</name>
  <files>supabase/migrations/20260706000002_modifier_inventory_rules_table.sql</files>
  <read_first>
    - supabase/migrations/20260428000001_recipes_tables.sql (lines 43-61: recipe_items table + its two RLS policies — the exact structural + RLS template)
    - supabase/migrations/20260414000003_products_and_categories.sql (lines 46-53: modifiers table — confirms FK target is modifiers(id) uuid PK)
    - .planning/phases/17-modifier-inventory-rules/17-PATTERNS.md (section "modifier_inventory_rules_table.sql")
  </read_first>
  <action>
    Create the migration with `CREATE TABLE modifier_inventory_rules ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), modifier_id uuid NOT NULL REFERENCES modifiers(id) ON DELETE CASCADE, ingredient_id uuid NOT NULL REFERENCES ingredients(id), delta numeric NOT NULL CHECK (delta <> 0), UNIQUE (modifier_id, ingredient_id) )`. `delta` is a SIGNED numeric (D-02) — do NOT add a `> 0` check; `CHECK (delta <> 0)` rejects meaningless zero-delta rows. The `(modifier_id, ingredient_id)` unique key with a per-row delta supports N ingredients per modifier (D-03). `ON DELETE CASCADE` on modifier_id mirrors recipe_items→recipes; leave ingredient_id FK at the default `NO ACTION` (blocks ingredient deletion while referenced, same as recipe_items). Then `ALTER TABLE modifier_inventory_rules ENABLE ROW LEVEL SECURITY;` and add two policies named exactly `modifier_inventory_rules_select_authenticated` (FOR SELECT TO authenticated USING (true)) and `modifier_inventory_rules_write_manager` (FOR ALL TO authenticated USING (get_user_role() IN ('manager','admin')) WITH CHECK (get_user_role() IN ('manager','admin'))) — verbatim structural clone of the recipe_items policies (V4 access control / defense-in-depth, not relying on the UI gate). Add a header comment block describing the table and a DOWN-script comment block (DROP TABLE IF EXISTS modifier_inventory_rules CASCADE;) per the codebase's post-Phase-8 migration convention.
  </action>
  <verify>
    <automated>grep -v '^\s*--' supabase/migrations/20260706000002_modifier_inventory_rules_table.sql | grep -c "modifier_inventory_rules_write_manager\|CREATE TABLE modifier_inventory_rules\|CHECK (delta <> 0)\|get_user_role() IN ('manager', 'admin')"</automated>
  </verify>
  <acceptance_criteria>
    - The grep count above is >= 3 (table create, CHECK, and manager-write policy all present, comments excluded).
    - Source assertion: `grep -n "REFERENCES modifiers(id) ON DELETE CASCADE" supabase/migrations/20260706000002_modifier_inventory_rules_table.sql` matches.
    - Source assertion: file contains NO `CHECK (delta > 0)` (signed delta preserved).
    - Migration includes a `-- DOWN` comment block with `DROP TABLE IF EXISTS modifier_inventory_rules`.
  </acceptance_criteria>
  <done>modifier_inventory_rules table + RLS migration authored, signed-delta CHECK, manager-only write, DOWN block present.</done>
</task>

<task type="auto">
  <name>Task 2: Create deplete_for_order_item v3 migration (add modifier loop)</name>
  <files>supabase/migrations/20260706000003_deplete_for_order_item_v3.sql</files>
  <read_first>
    - supabase/migrations/20260510000002_rpc_role_guards.sql (§3, lines 321-415: the CURRENT authoritative deplete_for_order_item body — THIS is the base, NOT 20260428000004_v2.sql which lacks the role guard)
    - supabase/migrations/20260426000003_record_stock_movement_rpc.sql (record_stock_movement signature: p_ingredient_id, p_delta, p_reason, p_ref_type, p_ref_id, p_notes)
    - supabase/migrations/20260426000002_stock_movements_idempotency_index.sql (partial unique index (ref_type, ref_id, ingredient_id) WHERE reason IN ('sale','refund',...) — why distinct ref_type avoids collision)
    - .planning/phases/17-modifier-inventory-rules/17-PATTERNS.md (section "deplete_for_order_item_v3.sql" — the required-change checklist)
    - .planning/phases/17-modifier-inventory-rules/17-RESEARCH.md (Pitfall 1: wrong base file; Pattern 2: distinct ref_type; Pattern 3: IF FOUND wrapper)
  </read_first>
  <action>
    `CREATE OR REPLACE FUNCTION deplete_for_order_item(p_order_item_id uuid, p_direction smallint, p_allow_negative boolean DEFAULT false) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` — copy the body VERBATIM from `20260510000002_rpc_role_guards.sql` §3 (lines 321-413), then apply exactly these edits:
    (a) DECLARE: add `v_modifier_ids uuid[];`, `v_mod_item record;`, `v_mod_delta numeric;`.
    (b) Change the order_items lookup to also read modifier_ids: `SELECT product_id, quantity, modifier_ids INTO v_product_id, v_qty, v_modifier_ids FROM order_items WHERE id = p_order_item_id;` (keep the `IF NOT FOUND THEN RETURN; END IF;` immediately after).
    (c) Move the `v_reason := CASE WHEN p_direction = 1 THEN 'sale' ELSE 'refund' END;` assignment UP to right after that `IF NOT FOUND` guard (before the recipes lookup) — both loops share v_reason.
    (d) D-04: replace the recipes-lookup `IF NOT FOUND THEN RETURN; END IF;` with an `IF FOUND THEN ... END IF;` wrapper enclosing ONLY the existing `FOR v_item IN SELECT ingredient_id, qty FROM recipe_items ...` loop. The function must NOT early-return when a product has no recipe — the modifier loop below must still run for recipe-less products (e.g. a bottled beer with an "extra lime" modifier).
    (e) After the recipe `IF FOUND` block, add the modifier loop: `FOR v_mod_item IN SELECT ingredient_id, delta FROM modifier_inventory_rules WHERE modifier_id = ANY(v_modifier_ids) LOOP` computing `v_mod_delta := -p_direction::numeric * v_qty::numeric * v_mod_item.delta;` (NO yield_qty divisor — D-01, delta is absolute-per-line) and calling `record_stock_movement(v_mod_item.ingredient_id, v_mod_delta, v_reason, 'order_item_modifier', p_order_item_id, NULL)`. Do NOT add a NULL guard on v_modifier_ids — the column is NOT NULL DEFAULT '{}', so an empty array yields zero loop iterations (recipe-only orders stay unchanged, SC-4).
    (f) D-05: wrap that record_stock_movement call in the IDENTICAL `EXCEPTION WHEN OTHERS THEN IF p_allow_negative AND SQLERRM LIKE '%INVENTORY_NEGATIVE%' THEN UPDATE ingredients SET quantity_on_hand = quantity_on_hand + v_mod_delta WHERE id = v_mod_item.ingredient_id; INSERT INTO audit_log (action, entity_type, entity_id, details, created_at) VALUES ('stock_override', 'order_item', p_order_item_id, jsonb_build_object('ingredient_id', v_mod_item.ingredient_id, 'delta', v_mod_delta, 'reason', 'manager_override'), now()); ELSE RAISE; END IF; END;` block used by the recipe loop.
    (g) PRESERVE the `IF get_user_role() IS NULL OR get_user_role() = 'kitchen' THEN RAISE EXCEPTION 'AUTH_FORBIDDEN...'` guard at BEGIN and the trailing `GRANT EXECUTE ON FUNCTION deplete_for_order_item(uuid, smallint, boolean) TO authenticated;`. Use `ref_type = 'order_item_modifier'` (distinct from the recipe loop's `'order_item'`) so the `(ref_type, ref_id, ingredient_id)` partial unique index never collides even when the same ingredient appears in both loops; this keeps retries idempotent. Add a header comment + DOWN comment (note: down = re-apply the 20260510000002 body).
  </action>
  <verify>
    <automated>grep -v '^\s*--' supabase/migrations/20260706000003_deplete_for_order_item_v3.sql | grep -c "order_item_modifier\|modifier_inventory_rules WHERE modifier_id = ANY\|IF FOUND THEN\|get_user_role() = 'kitchen'"</automated>
  </verify>
  <acceptance_criteria>
    - The grep count above is >= 4 (modifier ref_type, modifier query, IF FOUND wrapper, preserved kitchen guard all present).
    - Source assertion: the file contains NO standalone `IF NOT FOUND THEN RETURN; END IF;` after the recipes SELECT (D-04 wrapper applied) — `grep -c "recipes" ...` context shows the recipe loop is inside an `IF FOUND` block.
    - Source assertion: `grep -c "v_mod_delta := -p_direction" ...` matches (no yield divisor on the modifier delta).
    - Source assertion: modifier loop uses `ref_type` literal `'order_item_modifier'`, recipe loop still uses `'order_item'` (both literals present exactly once+).
  </acceptance_criteria>
  <done>deplete_for_order_item v3 authored on the correct base body; recipe loop conditional (D-04), modifier loop added with distinct ref_type + shared override branch (D-05), kitchen guard preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST client → `modifier_inventory_rules` | Any authenticated client can attempt writes to the new table via the auto-generated REST API. |
| Order-mutation RPC → `record_stock_movement` | `deplete_for_order_item` (SECURITY DEFINER) writes to the append-only stock ledger and mutates `ingredients.quantity_on_hand`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Elevation of Privilege (V4 Access Control) | `modifier_inventory_rules` writes | mitigate | RLS policy `modifier_inventory_rules_write_manager` with `WITH CHECK (get_user_role() IN ('manager','admin'))` — a bartender hitting PostgREST directly cannot INSERT/UPDATE/DELETE rules. Same pattern already enforced on recipe_items. |
| T-17-04 | Tampering (V5 Input Validation) | `modifier_inventory_rules.delta` | mitigate | Server-side `CHECK (delta <> 0)` + FK constraints on modifier_id/ingredient_id reject malformed rows at the DB tier, independent of the client Zod schema (plan 17-01). |
| T-17-05 | Tampering | fabricated `order_items.modifier_ids` triggering unintended depletion | accept | `modifier_id = ANY(v_modifier_ids)` against a foreign/nonexistent modifier simply matches zero rules (no-op, not an error). `order_items.modifier_ids` was already client-writable pre-phase; no new attack surface. |
| T-17-06 | Repudiation | manager negative-stock override via a modifier delta | mitigate | The shared `p_allow_negative` branch writes an `audit_log` `'stock_override'` row inside SECURITY DEFINER context regardless of which loop caused the negative (D-05) — non-repudiable. |
| T-17-07 | Elevation of Privilege | kitchen role calling `deplete_for_order_item` | mitigate | The v3 body PRESERVES the `get_user_role() = 'kitchen'` RAISE guard from the base 20260510000002 body (regression risk if copied from the stale _v2 file — Pitfall 1). |
| T-17-SC | Tampering | package installs | accept | Zero npm/pip/cargo installs this phase (RESEARCH Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- Both migration files parse as valid SQL (visual review against the analog bodies).
- Grep gates in each task pass.
- Full DB-level verification (table live, RPC applies modifier deltas) happens in plan 17-03 (push) + 17-04 (integration test) — this plan only authors the files.
</verification>

<success_criteria>
- SC-1: `modifier_inventory_rules` table + RLS authored, mapping (modifier, ingredient) → signed delta.
- SC-2: `deplete_for_order_item` v3 applies modifier deltas via `record_stock_movement` alongside the recipe loop, atomically in one SECURITY DEFINER function.
- SC-4: recipe-only path preserved — recipe loop unchanged inside the `IF FOUND` wrapper; empty modifier_ids is a no-op.
</success_criteria>

<output>
Create `.planning/phases/17-modifier-inventory-rules/17-02-SUMMARY.md` when done.
</output>
