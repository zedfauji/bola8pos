---
phase: 17-modifier-inventory-rules
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "Both Phase-17 migrations are applied to the remote Supabase project (table live, deplete_for_order_item v3 live)"
    - "supabase.types.ts includes a modifier_inventory_rules Row/Insert/Update shape so consumers can drop the `as any` cast in a later regen"
  artifacts:
    - path: "src/shared/lib/supabase.types.ts"
      provides: "modifier_inventory_rules table type shape"
      contains: "modifier_inventory_rules"
  key_links:
    - from: "src/shared/lib/supabase.types.ts"
      to: "modifier_inventory_rules"
      via: "manual type transcription of the migration columns"
      pattern: "modifier_inventory_rules"
---

<objective>
Apply the two Phase-17 migrations to the live remote Supabase project (BLOCKING), then transcribe the new table's type shape into `supabase.types.ts` so downstream code has a typed contract (SC-1, SC-2 live in the DB).

Purpose: The entity queries (17-04), the integration test (17-04), and the admin UI (17-05) all require the table and the v3 RPC to actually exist in remote Supabase. This is the single gate that turns authored SQL into live behavior.

Output: migrations applied (human-confirmed); `supabase.types.ts` extended with the `modifier_inventory_rules` shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modifier-inventory-rules/17-RESEARCH.md
@supabase/migrations/20260706000002_modifier_inventory_rules_table.sql
@supabase/migrations/20260706000003_deplete_for_order_item_v3.sql
</context>

## Artifacts this phase produces

This plan produces:
- The applied (live) `modifier_inventory_rules` table + `deplete_for_order_item` v3 in remote Supabase.
- `supabase.types.ts` `modifier_inventory_rules` Row/Insert/Update entry.

(Full phase artifact inventory is listed in 17-01-PLAN.md.)

<tasks>

<task type="auto">
  <name>Task 1: Transcribe modifier_inventory_rules shape into supabase.types.ts</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <read_first>
    - src/shared/lib/supabase.types.ts (find the existing `recipe_items:` table block — copy its Row/Insert/Update/Relationships structure as the template)
    - supabase/migrations/20260706000002_modifier_inventory_rules_table.sql (the authoritative column list)
  </read_first>
  <action>
    Add a `modifier_inventory_rules` table entry to the `public.Tables` section of `supabase.types.ts`, modeled on the existing `recipe_items` block. Row: `{ id: string; modifier_id: string; ingredient_id: string; delta: number }`. Insert: same with `id?: string` (has default) and all others required. Update: all optional. Relationships: two FK entries (`modifier_id` → `modifiers.id`, `ingredient_id` → `ingredients.id`) mirroring recipe_items' relationship entries. This is a MANUAL transcription because Docker/`supabase gen types` is unavailable in this environment (documented CLAUDE.md workaround); keep entries alphabetically consistent with neighboring tables if the file is sorted. The `deplete_for_order_item` function signature is UNCHANGED (still uuid, smallint, boolean) — do NOT add or alter a Functions entry for it.
  </action>
  <verify>
    <automated>grep -c "modifier_inventory_rules" src/shared/lib/supabase.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "modifier_inventory_rules" src/shared/lib/supabase.types.ts` returns >= 1.
    - Source assertion: the new block declares `delta: number` in its Row type.
    - `npm run typecheck` exits 0 (no malformed type entry).
  </acceptance_criteria>
  <done>supabase.types.ts contains a modifier_inventory_rules Row/Insert/Update/Relationships block with delta:number and the two FKs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply Phase-17 migrations to remote Supabase</name>
  <what-built>
    Two migrations authored in plan 17-02: `20260706000002_modifier_inventory_rules_table.sql` (new table + RLS) and `20260706000003_deplete_for_order_item_v3.sql` (RPC extended with the modifier loop). The executor applies them to the live remote Supabase project.
  </what-built>
  <how-to-verify>
    1. Executor: apply both migrations to remote — run `npx supabase db push` from `bar-pos/` (or, if the CLI multi-statement splitter rejects the function migration as in prior phases, apply `20260706000003` as a single statement via the Supabase Management API / `apply_migration` MCP tool). Apply `...000002` (table) before `...000003` (RPC references the table).
    2. Confirm the table is live: in the Supabase SQL editor run `SELECT to_regclass('public.modifier_inventory_rules');` — expect a non-null result.
    3. Confirm the RPC is the v3 body: run `SELECT pg_get_functiondef('public.deplete_for_order_item(uuid, smallint, boolean)'::regprocedure);` and verify the output contains both `order_item_modifier` and the preserved `get_user_role() = 'kitchen'` guard.
    4. Confirm RLS: `SELECT policyname FROM pg_policies WHERE tablename = 'modifier_inventory_rules';` — expect `modifier_inventory_rules_select_authenticated` and `modifier_inventory_rules_write_manager`.
  </how-to-verify>
  <resume-signal>Type "approved" once the table, RLS policies, and v3 function are confirmed live in remote Supabase, or describe any apply error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration apply → remote Postgres | DDL + SECURITY DEFINER function replacement applied to the live production database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering / Denial of Service | `deplete_for_order_item` CREATE OR REPLACE on live DB | mitigate | Blocking human checkpoint verifies the v3 body is live and still contains the kitchen guard + `order_item_modifier` before resuming; `pg_get_functiondef` inspection catches a wrong-base-body regression (Pitfall 1) before any order flow runs against it. |
| T-17-09 | Elevation of Privilege (V4) | new table RLS live | mitigate | Checkpoint step 4 asserts both RLS policies exist post-apply; a table shipped with RLS disabled or missing the manager-write policy would be caught here. |
| T-17-SC | Tampering | package installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `to_regclass('public.modifier_inventory_rules')` non-null; both RLS policies present.
- `pg_get_functiondef` of `deplete_for_order_item(uuid, smallint, boolean)` contains `order_item_modifier` and the kitchen guard.
- `npm run typecheck` exits 0 with the extended supabase.types.ts.
</verification>

<success_criteria>
- SC-1: `modifier_inventory_rules` table + RLS live in remote Supabase.
- SC-2: `deplete_for_order_item` v3 (with modifier loop) live in remote Supabase.
</success_criteria>

<output>
Create `.planning/phases/17-modifier-inventory-rules/17-03-SUMMARY.md` when done.
</output>
