---
phase: 17-modifier-inventory-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/domain.test.ts
  - src/shared/lib/domain-helpers.ts
  - src/shared/lib/depletion.test.ts
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "ModifierInventoryRuleSchema parses a valid rule and rejects a zero delta and a non-numeric delta"
    - "computeModifierDepletion scales the delta linearly by orderQty (D-01) and inverts sign between sale (direction=1) and refund (direction=-1)"
    - "computeModifierDepletion returns an empty Map for an empty rules array"
  artifacts:
    - path: "src/shared/lib/domain.ts"
      provides: "ModifierInventoryRuleSchema + ModifierInventoryRuleCreateSchema + ModifierInventoryRule/ModifierInventoryRuleCreate types"
      contains: "ModifierInventoryRuleSchema"
    - path: "src/shared/lib/domain-helpers.ts"
      provides: "computeModifierDepletion pure function (no yield divisor)"
      contains: "export function computeModifierDepletion"
    - path: "src/shared/lib/domain.test.ts"
      provides: "Zod parse tests for the new schema"
      contains: "ModifierInventoryRuleSchema"
    - path: "src/shared/lib/depletion.test.ts"
      provides: "property tests for computeModifierDepletion"
      contains: "computeModifierDepletion"
  key_links:
    - from: "src/shared/lib/domain-helpers.ts"
      to: "src/shared/lib/domain.ts"
      via: "imports ModifierInventoryRule type"
      pattern: "ModifierInventoryRule"
---

<objective>
Establish the pure, DB-independent foundations for Phase 17: the Zod schema that shapes a modifier→ingredient rule row (SC-1) and the pure delta-computation helper that the depletion RPC's behavior mirrors (SC-2). Both are test-first (Nyquist Wave 0).

Purpose: Lock the data shape (D-02 signed delta, D-03 join-row shape) and the scaling math (D-01) as tested, importable units before any migration or UI touches them. This is the interface-first contract every later plan builds on.

Output: `ModifierInventoryRuleSchema`, `ModifierInventoryRuleCreateSchema`, their inferred types in `domain.ts`; `computeModifierDepletion` in `domain-helpers.ts`; unit + property tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-modifier-inventory-rules/17-CONTEXT.md
@.planning/phases/17-modifier-inventory-rules/17-PATTERNS.md
@.planning/phases/17-modifier-inventory-rules/17-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols created across all of Phase 17 (this plan produces the first group):

- **This plan (17-01):** `ModifierInventoryRuleSchema`, `ModifierInventoryRuleCreateSchema`, type `ModifierInventoryRule`, type `ModifierInventoryRuleCreate` (in `src/shared/lib/domain.ts`); `computeModifierDepletion(rules, orderQty, direction): Map<string, number>` (in `src/shared/lib/domain-helpers.ts`).
- Later plans: DB table `modifier_inventory_rules(id, modifier_id, ingredient_id, delta)` + RLS policies `modifier_inventory_rules_select_authenticated` / `modifier_inventory_rules_write_manager` (17-02); `deplete_for_order_item(uuid, smallint, boolean)` v3 with modifier loop + `ref_type='order_item_modifier'` (17-02); `supabase.types.ts` `modifier_inventory_rules` shape (17-03); entity `entities/modifier-inventory-rule/` → `modifierInventoryRuleKeys`, `useModifierInventoryRules`, `useMutationSaveModifierInventoryRules` (17-04); feature `features/manage-modifier-inventory-rules/` → `useManageModifierInventoryRules`, `ModifierIngredientRulesDialog` (17-05); `CatalogModifiersTab.tsx` per-row "Ingredient rules" button + `rulesModifierId` state (17-05).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ModifierInventoryRuleSchema + types to domain.ts (test-first)</name>
  <files>src/shared/lib/domain.ts, src/shared/lib/domain.test.ts</files>
  <read_first>
    - src/shared/lib/domain.ts (lines 1602-1666: RecipeItemSchema, RecipeItemCreateSchema, exported types — the exact analog to mirror; line 207 ModifierSchema; line 1530 IngredientSchema for UuidSchema usage)
    - src/shared/lib/domain.test.ts (lines 312-405: ModifierGroupSchema / ModifierGroupItemSchema describe blocks — the test-file structure to mirror)
    - .planning/phases/17-modifier-inventory-rules/17-PATTERNS.md (section "src/shared/lib/domain.ts (+ ModifierInventoryRuleSchema)")
  </read_first>
  <behavior>
    - Test: `ModifierInventoryRuleSchema.parse` accepts `{ id, modifierId, ingredientId, delta: 2 }` (positive) and `{ ..., delta: -1 }` (negative) — signed delta per D-02.
    - Test: `ModifierInventoryRuleSchema.parse` throws on `delta: 0` (nonzero refinement) and on a non-numeric delta (e.g. `delta: 'x'`).
    - Test: `ModifierInventoryRuleSchema.parse` throws when `modifierId` or `ingredientId` is missing / not a UUID.
    - Test: `ModifierInventoryRuleCreateSchema` (id-omitted) parses `{ modifierId, ingredientId, delta }` without an `id`.
  </behavior>
  <action>
    First write the failing tests in a new `describe('ModifierInventoryRuleSchema', ...)` block in `src/shared/lib/domain.test.ts`, mirroring the `ModifierGroupItemSchema` block's structure. Then add, in `domain.ts` near `RecipeItemSchema` (line 1602) under a new comment header `// MODIFIER INVENTORY RULES (Phase 17)`: `export const ModifierInventoryRuleSchema = z.object({ id: UuidSchema, modifierId: UuidSchema, ingredientId: UuidSchema, delta: z.number().multipleOf(0.001).refine(v => v !== 0, 'delta must be nonzero') })`; `export const ModifierInventoryRuleCreateSchema = ModifierInventoryRuleSchema.omit({ id: true })`; plus `export type ModifierInventoryRule = z.infer<typeof ModifierInventoryRuleSchema>` and `export type ModifierInventoryRuleCreate = z.infer<typeof ModifierInventoryRuleCreateSchema>`. Signed delta (D-02); the join-row shape `(modifierId, ingredientId, delta)` mirrors `recipe_items` and supports N ingredients per modifier (D-03). Use the existing `UuidSchema` primitive — do NOT introduce a new UUID validator.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/domain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/lib/domain.test.ts` passes including the new `ModifierInventoryRuleSchema` block.
    - Source assertion: `grep -n "ModifierInventoryRuleSchema\|ModifierInventoryRuleCreateSchema" src/shared/lib/domain.ts` returns both exports plus the two type aliases.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>ModifierInventoryRuleSchema/Create + types exist in domain.ts, signed-delta and nonzero rules enforced, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add computeModifierDepletion helper + property tests (test-first)</name>
  <files>src/shared/lib/domain-helpers.ts, src/shared/lib/depletion.test.ts</files>
  <read_first>
    - src/shared/lib/domain-helpers.ts (lines 386-402: computeDepletion — the exact analog; note it divides by yieldQty, which the modifier helper must NOT do)
    - src/shared/lib/depletion.test.ts (full file: the computeDepletion describe block incl. the P6 fast-check property test to mirror)
    - .planning/phases/17-modifier-inventory-rules/17-PATTERNS.md (section "src/shared/lib/domain-helpers.ts (+ computeModifierDepletion)")
  </read_first>
  <behavior>
    - Test: for `rules = [{ ingredientId: 'lime', delta: 2 }]`, `computeModifierDepletion(rules, 1, 1)` returns a Map with `lime => -2` (sale subtracts; D-01 scaling).
    - Test: sign inversion — `computeModifierDepletion(rules, q, -1).get(id)` is the exact additive inverse of `computeModifierDepletion(rules, q, 1).get(id)` (refund vs sale).
    - Test: linear scaling — delta at orderQty=3 equals 3× delta at orderQty=1.
    - Test: empty rules array → empty Map (size 0).
    - Property (fast-check, mirror P6): for arbitrary rules/orderQty, `Σ|delta|` equals `orderQty × Σ|rule.delta|` (no yield divisor).
  </behavior>
  <action>
    First add a new `describe('computeModifierDepletion', ...)` block to `src/shared/lib/depletion.test.ts` mirroring the existing `computeDepletion` block (reuse the fast-check `import * as fc` already at the top; build a flat `rules: ModifierInventoryRule[]` arb instead of the recipe-with-yieldQty fixture). Then add to `domain-helpers.ts`: `export function computeModifierDepletion(rules: ModifierInventoryRule[], orderQty: number, direction: 1 | -1): Map<string, number>` that loops `rules` and sets `deltas.set(rule.ingredientId, -direction * orderQty * rule.delta)` — NO `yieldQty` divisor (modifier deltas are absolute-per-line, D-01). Import the `ModifierInventoryRule` type from `@shared/lib/domain` in the helper's existing `import type` block. Honor `noUncheckedIndexedAccess` when reading Map values in tests (`?? 0` fallback, as computeDepletion tests already do).
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/depletion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/lib/depletion.test.ts` passes including the new `computeModifierDepletion` block and property test.
    - Source assertion: `grep -n "export function computeModifierDepletion" src/shared/lib/domain-helpers.ts` returns a match; the function body contains no `/ .*yield` divisor.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>computeModifierDepletion exists, scales by quantity without yield division, inverts on refund, property test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan produces pure client-side schema + math with no I/O, no DB, no network. It defines the validation contract later plans enforce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering (V5 Input Validation) | `ModifierInventoryRuleSchema` | mitigate | Zod schema is the client-side input-validation layer; `.refine(v => v !== 0)` rejects meaningless zero-delta rules and `z.number()` rejects non-numeric deltas before they reach the DB. Server-side CHECK constraint (plan 17-02) is the defense-in-depth pair. |
| T-17-02 | Tampering | `computeModifierDepletion` | accept | Pure function, no privilege, no persistence; wrong output surfaces only as a failed property test. No security impact. |
| T-17-SC | Tampering | package installs | accept | This phase installs zero npm packages (RESEARCH.md "Package Legitimacy Audit: Not applicable"); no supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/shared/lib/domain.test.ts src/shared/lib/depletion.test.ts` — all green.
- `npm run typecheck` exits 0; `npm run lint` exits 0.
</verification>

<success_criteria>
- SC-1 (partial): rule shape is a tested Zod schema with signed, nonzero delta and UUID FKs.
- SC-2 (partial): the pure delta math (quantity-scaled, sign-inverting, no yield divisor) is tested and importable.
</success_criteria>

<output>
Create `.planning/phases/17-modifier-inventory-rules/17-01-SUMMARY.md` when done.
</output>
