---
phase: 30-shared-shell-primitive-extension
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/pages/audit/index.tsx
  - src/pages/settings/index.tsx
  - src/pages/staff/index.tsx
  - src/pages/inventory/index.tsx
  - src/pages/reports/index.tsx
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "audit, settings, staff, inventory, reports each render their header + back link via PageContainer (first-time adoption)"
    - "Each back link is an anchor with accessible name 'Home' and href '/home'"
    - "inventory's Physical Count button keeps its data-testid='physical-count-btn' and canPhysicalCount gate"
    - "reports' 13-tab Tabs body still renders and scrolls; ReportsPage.test.tsx still passes"
    - "No page in this plan imports BackToHomeButton anymore"
  artifacts:
    - path: "src/pages/audit/index.tsx"
      provides: "PageContainer title='Audit Log' backTo='/home', no BackToHomeButton, no standalone SectionHeader"
      contains: "PageContainer"
    - path: "src/pages/inventory/index.tsx"
      provides: "PageContainer title='Inventory' backTo='/home' with LowStockBadge + Physical Count in actions slot"
      contains: "physical-count-btn"
    - path: "src/pages/reports/index.tsx"
      provides: "PageContainer title='Reports' backTo='/home' wrapping the Tabs"
      contains: "backTo=\"/home\""
  key_links:
    - from: "src/pages/reports/index.tsx"
      to: "PageContainer"
      via: "wraps Tabs, ReportsPage.test.tsx Home-link assertion preserved"
      pattern: "backTo=\"/home\""
---

<objective>
Give the 5 routes that currently lack `PageContainer` their first-time adoption of the shared shell (SHELL-01, D-01): `audit`, `settings`, `staff` are clean card pages; `inventory` has a live `<LowStockBadge/>` and a gated Physical-Count button in its header; `reports` is a full-height 13-tab page with no current title. Each drops its ad-hoc `<h1>`/`SectionHeader` + `<BackToHomeButton/>` in favor of a `PageContainer title=... backTo="/home"` header, keeping its existing outer `flex h-screen flex-col` + `main flex-1 overflow-auto` scroll shell.

Purpose: SHELL-01 — replace ad-hoc per-page header wrappers with the single shared shell.
Output: 5 pages on `PageContainer`, back-nav inline in the shared header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md
@.planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md
@.planning/phases/30-shared-shell-primitive-extension/30-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates NO new symbols. It adds first-time `PageContainer` usage (with the `backTo` prop created in 30-01) to 5 page files and removes `BackToHomeButton` + ad-hoc `<h1>`/standalone-`SectionHeader` header markup from them. `backTo`/`backLabel` are new this phase (created in 30-01) — do not flag their usage here as drift.

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageContainer on the 3 clean card pages (audit, settings, staff)</name>
  <files>src/pages/audit/index.tsx, src/pages/settings/index.tsx, src/pages/staff/index.tsx</files>
  <read_first>
    - src/pages/audit/index.tsx (uses a standalone `<SectionHeader title="Audit Log" />` inside a `max-w-6xl` card)
    - src/pages/settings/index.tsx (uses `<h1>Settings</h1>` inside a `max-w-5xl` card)
    - src/pages/staff/index.tsx (uses `<h1 className="mb-6">Staff</h1>` above a `max-w-6xl` card wrapping CajaDashboard + StaffDashboard)
    - src/shared/ui/PageContainer.tsx (post-30-01 signature — renders SectionHeader internally, default width max-w-[1400px] p-6 space-y-6)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§Pattern A migration shape)
  </read_first>
  <action>
    For each of the 3 files: remove the `BackToHomeButton` import + its `<BackToHomeButton />` JSX line. Replace the page's ad-hoc header + content-width wrapper with a single `<PageContainer title=... backTo="/home">` whose children are the page's real content widget(s). Keep the OUTER `<div className="flex h-screen flex-col"><main className="flex-1 overflow-auto ...">...</main></div>` scroll shell — PageContainer goes INSIDE `<main>` (it does not replace the scroll shell, per 30-RESEARCH.md Anti-Patterns). Concretely: audit → `title="Audit Log"`, children `<AuditLogTable />`, and remove the now-unused `SectionHeader` import (PageContainer renders the header). settings → `title="Settings"`, children `<SettingsTabsPanel />`, drop the `<h1>` and the `mx-auto max-w-5xl space-y-8` wrapper. staff → `title="Staff"`, children `<CajaDashboard />` + `<StaffDashboard />`, drop the `<h1>` and the `mx-auto max-w-6xl space-y-8` wrapper. The per-page `max-w-Nxl` cap is intentionally replaced by PageContainer's shared `max-w-[1400px]` — that width normalization IS the SHELL-01 goal, not a regression. Remove `main`'s own `p-6 md:p-8` padding where PageContainer's `p-6` would otherwise double it (keep `main`'s `flex-1 overflow-auto`).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "BackToHomeButton" src/pages/{audit,settings,staff}/index.tsx` returns nothing.
    - `src/pages/audit/index.tsx` no longer imports `SectionHeader` directly and renders `<PageContainer title="Audit Log" backTo="/home">`.
    - settings and staff each render `<PageContainer title="Settings"|"Staff" backTo="/home">` with their original content widgets as children; no orphan `<h1>` remains.
    - Each file retains its outer `<div className="flex h-screen flex-col">` + `<main className="flex-1 overflow-auto">`.
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>audit/settings/staff on PageContainer with backTo='/home'; no BackToHomeButton or orphan headers; typecheck/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt PageContainer on inventory (badge+actions) and reports (full-height tabs)</name>
  <files>src/pages/inventory/index.tsx, src/pages/reports/index.tsx</files>
  <read_first>
    - src/pages/inventory/index.tsx (header is `<h1>Inventory <LowStockBadge/></h1>` in a `flex items-center justify-between` row with a `canPhysicalCount`-gated `<Button data-testid="physical-count-btn">Physical Count</Button>`; body = `<InventoryPagePanel />` + `<PhysicalCountForm />`)
    - src/pages/reports/index.tsx (full-height `flex h-screen flex-col`, `main flex-1 overflow-auto p-6 md:p-8`, a 13-trigger `<Tabs>` with `flex flex-wrap` TabsList, NO current title and NO max-width card)
    - src/pages/reports/ReportsPage.test.tsx (asserts the Home link — must keep passing)
    - src/shared/ui/PageContainer.tsx (post-30-01; `actions` slot renders on the header's right)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§Pattern A + the actions/badge notes)
  </read_first>
  <action>
    inventory: remove the `BackToHomeButton` import + JSX line. Wrap the body in `<PageContainer title="Inventory" backTo="/home" actions={...}>` inside the existing `<main className="flex-1 overflow-auto">` (remove main's own `p-6 md:p-8` to avoid doubling PageContainer's `p-6`). `PageContainer.title` is a string, so the live `<LowStockBadge />` component cannot go through the `badge` string prop — instead place BOTH `<LowStockBadge />` and the gated Physical Count `<Button>` into the `actions` slot (a fragment), preserving the exact `canPhysicalCount` conditional and the `data-testid="physical-count-btn"` + `onClick` that opens PhysicalCountForm. Keep `<InventoryPagePanel />` and the gated `<PhysicalCountForm .../>` as PageContainer children. This relocates the low-stock badge from beside the title to the header's action area — a chrome-only consequence of standardizing on the shared header, no handler/gate change.
    reports: remove the `BackToHomeButton` import + JSX line. Wrap the entire `<Tabs>...</Tabs>` in `<PageContainer title="Reports" backTo="/home">` placed inside the existing `<main className="flex-1 overflow-auto">` (remove main's `p-6 md:p-8`; PageContainer supplies `p-6`). Do NOT pass a width-neutralizing className — reports is vertical scrolling content, so the shared `max-w-[1400px]` centering is acceptable (same as kds/rbac). Leave the 13 `<TabsTrigger>`s, `<TabsContent>`s, DateRangePicker wiring, and all report widgets exactly as-is. Adding the "Reports" title is the intended SHELL-01 header standardization (reports had none before).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/pages/reports/ReportsPage.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "BackToHomeButton" src/pages/{inventory,reports}/index.tsx` returns nothing.
    - inventory renders `<PageContainer title="Inventory" backTo="/home" actions={...}>`; the string `physical-count-btn` and the `canPhysicalCount` gate are still present; `<LowStockBadge />` still renders (now inside actions).
    - reports renders `<PageContainer title="Reports" backTo="/home">` wrapping the `<Tabs>`; all 13 TabsTriggers still present.
    - `ReportsPage.test.tsx` passes (Home-link assertion intact).
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>inventory + reports on PageContainer with backTo='/home'; Physical Count gate/testid preserved; reports tabs intact; ReportsPage.test.tsx green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | First-time PageContainer adoption on presentational pages; no new input boundary. The `canPhysicalCount` RBAC gate on inventory is preserved unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | (n/a) | audit/settings/staff/inventory/reports pages | accept | No new attack surface — internal navigation-chrome refactor only (30-RESEARCH.md Security Domain). inventory's `canPhysicalCount` access gate is moved verbatim into the actions slot, not altered. No package installs → supply-chain threat N/A. |
</threat_model>

<verification>
- `npx vitest run src/pages/reports/ReportsPage.test.tsx` — pass.
- `npm run typecheck` — only the 2 pre-existing unrelated errors.
- `npm run lint` — exit 0.
- `npm run test` (full unit suite) — no regressions.
</verification>

<success_criteria>
- audit, settings, staff, inventory, reports all use `PageContainer` with `backTo="/home"` for the first time.
- inventory's Physical Count gate + testid and reports' 13-tab body are functionally unchanged.
- Outer scroll shells preserved; width normalized to the shared `max-w-[1400px]`.
</success_criteria>

<output>
Create `.planning/phases/30-shared-shell-primitive-extension/30-03-SUMMARY.md` when done.
</output>
