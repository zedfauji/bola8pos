---
phase: 30-shared-shell-primitive-extension
plan: 05
type: execute
wave: 3
depends_on: ["30-02", "30-03", "30-04"]
files_modified:
  - src/shared/ui/BackToHomeButton.tsx
  - src/shared/ui/AppShell.tsx
  - src/widgets/AppNav/
  - src/shared/ui/index.ts
  - CLAUDE.md
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]
must_haves:
  truths:
    - "BackToHomeButton.tsx, AppShell.tsx, and the AppNav widget folder no longer exist"
    - "src/shared/ui/index.ts no longer exports BackToHomeButton, AppShell, or AppShellProps"
    - "No source file imports any of the deleted symbols (typecheck/lint clean)"
    - "CLAUDE.md routes table has exactly 17 rows matching router.tsx (adds /kds, /kitchen-prep, /audit)"
    - "Full unit suite passes with no regressions"
  artifacts:
    - path: "src/shared/ui/index.ts"
      provides: "Barrel with BackToHomeButton/AppShell exports removed"
      absent: "BackToHomeButton"
    - path: "CLAUDE.md"
      provides: "Routes table corrected to 17 rows"
      contains: "/kitchen-prep"
  key_links:
    - from: "src/shared/ui/index.ts"
      to: "deleted components"
      via: "export lines removed (no dangling re-export)"
      pattern: "^(?!.*BackToHomeButton)"
---

<objective>
Finalize the phase: now that all 15 callers no longer reference `BackToHomeButton` (30-02/30-03/30-04 complete), delete the dead components and fix the docs. Delete `BackToHomeButton.tsx` (SHELL-01 completion, D-01), `AppShell.tsx` + the `AppNav/` widget folder (SHELL-02, D-03 — confirmed zero real consumers), remove their barrel exports, and add the 3 missing rows to `CLAUDE.md`'s routes table so it matches `router.tsx`'s 17 real routes (SHELL-03, D-04). Then run the phase verification gate.

Purpose: complete SHELL-01/02/03 by removing what the shared shell replaced and correcting the routes documentation.
Output: dead code gone, barrel clean, CLAUDE.md routes table = 17 rows, suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md
@.planning/phases/30-shared-shell-primitive-extension/30-CONTEXT.md
@.planning/phases/30-shared-shell-primitive-extension/30-VALIDATION.md
</context>

## Artifacts this phase produces

This plan REMOVES symbols (does not create new ones):

- DELETED file: `src/shared/ui/BackToHomeButton.tsx` (and its `BackToHomeButton` export)
- DELETED file: `src/shared/ui/AppShell.tsx` (and its `AppShell` / `AppShellProps` exports)
- DELETED folder: `src/widgets/AppNav/` (contains `ui/AppNav.tsx`; no barrel export existed)
- MODIFIED: `src/shared/ui/index.ts` — 3 export lines removed
- MODIFIED: `CLAUDE.md` — 3 routes-table rows added (`/kds`, `/kitchen-prep`, `/audit`)

Source-grounding note: the removals above are intentional deletions, not drift; a post-migration reference to `BackToHomeButton`/`AppShell`/`AppNav` anywhere in `src/` is a bug this plan must eliminate.

<tasks>

<task type="auto">
  <name>Task 1: Delete dead components + remove barrel exports (SHELL-01 completion, SHELL-02)</name>
  <files>src/shared/ui/BackToHomeButton.tsx, src/shared/ui/AppShell.tsx, src/widgets/AppNav/, src/shared/ui/index.ts</files>
  <read_first>
    - src/shared/ui/index.ts (lines 63-70: line 64 `export { BackToHomeButton } from './BackToHomeButton';`, line 69 `export { AppShell } from './AppShell';`, line 70 `export type { AppShellProps } from './AppShell';`)
    - .planning/phases/30-shared-shell-primitive-extension/30-CONTEXT.md (D-03 — delete outright, "removed, not resurrected", zero re-export shim)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§`src/shared/ui/index.ts` and §`AppShell.tsx` / `AppNav/`)
  </read_first>
  <action>
    Delete the files `src/shared/ui/BackToHomeButton.tsx` and `src/shared/ui/AppShell.tsx`, and delete the entire `src/widgets/AppNav/` folder (including `src/widgets/AppNav/ui/AppNav.tsx`). In `src/shared/ui/index.ts`, remove exactly three lines: the `export { BackToHomeButton } from './BackToHomeButton';` line (Navigation section), the `export { AppShell } from './AppShell';` line, and the `export type { AppShellProps } from './AppShell';` line (Layout section). Do NOT add any replacement/shim export (D-03). Leave `PageContainer`/`SectionHeader`/`SplitLayout`/`ScrollArea`/`DateRangePicker` exports untouched. This task depends on 30-02/30-03/30-04 having removed every caller first; if typecheck reports a dangling import of any deleted symbol, that caller was missed upstream — report it rather than re-adding the file.
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Files `src/shared/ui/BackToHomeButton.tsx` and `src/shared/ui/AppShell.tsx` do not exist.
    - Folder `src/widgets/AppNav/` does not exist.
    - `grep -rn "BackToHomeButton\|AppShell\|from '@widgets/AppNav'\|widgets/AppNav" src` returns no import/export/usage matches (a stale code comment referencing the old name, if any, is acceptable but prefer none).
    - `src/shared/ui/index.ts` contains no `BackToHomeButton`, `AppShell`, or `AppShellProps` export lines.
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors (no new "cannot find module" errors → confirms zero dangling imports).
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Dead files/folder deleted; 3 barrel exports removed; no dangling references; typecheck/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Correct CLAUDE.md routes table to 17 rows (SHELL-03)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (§Routes — the current 14-row `| Path | Page | Notes |` table; note existing row style, e.g. `/kds-bar` → "Bar KDS board (bartender+)", `/reports` → "Caja reports (gated by ReportsRoute)")
    - src/app/router.tsx (the 17 real routes + guards: `/kds` wrapped in `KdsRoute`, `/kitchen-prep` under plain `ProtectedRoute`, `/audit` wrapped in `AuditRoute`)
    - src/app/kds-route.tsx, src/app/audit-route.tsx (the permission checks to source Notes text from — do not invent copy)
    - .planning/phases/30-shared-shell-primitive-extension/30-CONTEXT.md (D-04 — routes-table-only, no other CLAUDE.md section touched)
    - .planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md (§Copywriting Contract — source Notes from the route's guard/comment, otherwise leave blank; do not invent)
  </read_first>
  <action>
    Add exactly three rows to the existing routes table in `CLAUDE.md`, matching the table's existing `| Path | Page | Notes |` column format and alignment: `/kds` → KdsPage → Notes sourced from `KdsRoute`'s permission check (mirror the `/kds-bar` row's style, e.g. "Kitchen KDS board (gated by KdsRoute)"); `/kitchen-prep` → KitchenPrepPage → "Batch chef prep production" (plain `ProtectedRoute`, no special guard); `/audit` → AuditPage → Notes sourced from `AuditRoute` (e.g. "Audit log (gated by AuditRoute)"). Insert them at positions consistent with the existing ordering (grouping the KDS rows together and placing `/audit` near the other admin/manager routes reads best, but exact row order is not asserted — only that all 17 routes are present). Do NOT modify any other CLAUDE.md section (Implemented Features, RBAC Actions, DB Tables, etc.) per D-04. Do NOT invent descriptive copy beyond what each route's guard/comment implies.
  </action>
  <verify>
    <automated>cd bar-pos && grep -c "^| \`/" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The routes table now contains rows for `/kds`, `/kitchen-prep`, and `/audit` (previously absent).
    - `grep -c "^| \`/" CLAUDE.md` returns 17 (the count of `| \`/...` table rows equals router.tsx's 17 real routes).
    - Every path in the table exists in `src/app/router.tsx`, and every `router.tsx` route (login, home, pos, pool-tables, pool-tables/:tableId, inventory, staff, reports, settings, rappi, payments, kds, kds-bar, kitchen-prep, waitlist, rbac, audit) appears in the table.
    - No CLAUDE.md section other than the routes table was changed (diff scoped to the 3 added rows).
  </acceptance_criteria>
  <done>CLAUDE.md routes table = 17 rows matching router.tsx; only the routes table changed.</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification gate</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - .planning/phases/30-shared-shell-primitive-extension/30-VALIDATION.md (Sampling Rate + phase gate expectations)
    - .planning/phases/30-shared-shell-primitive-extension/30-RESEARCH.md (Pitfall 1 — the 'Home' back-link is a load-bearing E2E dependency asserted by PaymentsPage.test.tsx, ReportsPage.test.tsx, 15-home-navigation, 16-table-status, 17-payment-pane, and auth.ts logout())
  </read_first>
  <action>
    Run the full phase gate and confirm no regressions. Execute the full unit suite and the fast back-link regression tests, then the targeted E2E specs that guard the back-link contract and the full-bleed pages. If any targeted E2E cannot run (dev server / `.env.local` creds unavailable in this environment), record that explicitly in the SUMMARY as a manual follow-up for `/gsd-verify-work` rather than silently skipping. Confirm the typecheck output still shows ONLY the 2 pre-existing unrelated errors (`src/entities/tab/model/queries.ts(778,11)`, `src/shared/lib/agent/rag.ts(60,7)`) — no phase-introduced errors.
  </action>
  <verify>
    <automated>cd bar-pos && npm run test && npx vitest run src/shared/ui/PageContainer.test.tsx src/pages/payments/PaymentsPage.test.tsx src/pages/reports/ReportsPage.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` (full unit suite) passes with no new failures vs the pre-phase baseline (the known pre-existing `useCloseTab.test.ts:95` failure documented since Phase 15 is the only tolerated failure).
    - `PageContainer.test.tsx` (3/3), `PaymentsPage.test.tsx`, `ReportsPage.test.tsx` all pass — the 'Home' back-link accessible-name/href contract is intact.
    - `npm run typecheck` shows exactly the 2 pre-existing unrelated errors; `npm run lint` exits 0.
    - Targeted E2E (`e2e/15-home-navigation.spec.ts`, `e2e/16-table-status.spec.ts`, `e2e/17-payment-pane.spec.ts`) pass, OR their run is explicitly recorded as a deferred manual gate in the SUMMARY if the environment lacks a dev server/creds.
  </acceptance_criteria>
  <done>Full unit suite + back-link tests green; typecheck shows only the 2 documented pre-existing errors; lint clean; E2E run (or deferral) recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Dead-code deletion + docs edit. Removing unreferenced components cannot expand attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | (n/a) | deleted components + CLAUDE.md | accept | No new attack surface — internal dead-code removal + static docs correction (30-RESEARCH.md Security Domain). No package installs → supply-chain threat N/A. |
</threat_model>

<verification>
- `grep -rn "BackToHomeButton\|AppShell\|widgets/AppNav" src` — no import/usage matches.
- `grep -c "^| \`/" CLAUDE.md` — returns 17.
- `npm run test` — full unit suite green (only the documented pre-existing failure tolerated).
- `npm run typecheck` — only the 2 pre-existing unrelated errors.
- `npm run lint` — exit 0.
- Targeted E2E specs pass or deferral recorded.
</verification>

<success_criteria>
- SHELL-01: BackToHomeButton deleted; back-nav flows only through PageContainer's `backTo` across all 15 non-exempt routes.
- SHELL-02: AppShell + AppNav removed, no re-export shim, no dangling imports.
- SHELL-03: CLAUDE.md routes table = 17 rows matching router.tsx.
- Full unit suite + back-link contract tests green; typecheck/lint clean.
</success_criteria>

<output>
Create `.planning/phases/30-shared-shell-primitive-extension/30-05-SUMMARY.md` when done.
</output>
