---
phase: 30-shared-shell-primitive-extension
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/pages/kds/index.tsx
  - src/pages/kds-bar/index.tsx
  - src/pages/kitchen-prep/index.tsx
  - src/pages/pool-tables/index.tsx
  - src/pages/rappi/index.tsx
  - src/pages/rbac/index.tsx
  - src/pages/waitlist/index.tsx
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "Each of the 7 pages renders its back link via PageContainer's backTo prop, not a standalone BackToHomeButton"
    - "Each back link is an anchor with accessible name 'Home' and href '/home'"
    - "No page in this plan imports BackToHomeButton anymore"
    - "Existing PageContainer props (title, actions, description) on each page are preserved unchanged"
  artifacts:
    - path: "src/pages/kds/index.tsx"
      provides: "PageContainer with backTo='/home', no BackToHomeButton"
      contains: "backTo=\"/home\""
    - path: "src/pages/rbac/index.tsx"
      provides: "PageContainer with backTo='/home', no BackToHomeButton"
      contains: "backTo=\"/home\""
  key_links:
    - from: "src/pages/kds/index.tsx"
      to: "PageContainer backTo prop"
      via: "prop replaces standalone BackToHomeButton"
      pattern: "backTo=\"/home\""
---

<objective>
Migrate the 7 pages that ALREADY render `<PageContainer>` off the standalone `<BackToHomeButton />` onto the new `backTo` prop (D-01). These are the lowest-risk migrations: each is an identical mechanical swap — drop the `BackToHomeButton` import + its JSX line, add `backTo="/home"` to the page's existing `<PageContainer>` call.

Purpose: SHELL-01 — every route's back-nav flows through the single shared shell.
Output: kds, kds-bar, kitchen-prep, pool-tables, rappi, rbac, waitlist each use `backTo` on their existing PageContainer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md
@.planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md
</context>

## Artifacts this phase produces

This plan creates NO new symbols. It consumes `PageContainer`'s `backTo` prop (created in 30-01) and removes `BackToHomeButton` usage from 7 page files. The `backTo`/`backLabel` props are new this phase (created in 30-01) — do not flag their usage here as drift.

<tasks>

<task type="auto">
  <name>Task 1: Swap BackToHomeButton → backTo prop on all 7 existing-PageContainer pages</name>
  <files>src/pages/kds/index.tsx, src/pages/kds-bar/index.tsx, src/pages/kitchen-prep/index.tsx, src/pages/pool-tables/index.tsx, src/pages/rappi/index.tsx, src/pages/rbac/index.tsx, src/pages/waitlist/index.tsx</files>
  <read_first>
    - src/pages/kds/index.tsx (canonical analog — the exact shape all 7 share: `<div className="flex h-screen flex-col"><BackToHomeButton /><main className="flex-1 overflow-auto"><PageContainer title=... actions=...>...</PageContainer></main></div>`)
    - src/shared/ui/PageContainer.tsx (post-30-01 signature confirming `backTo` prop exists)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§Pattern B — the migration shape)
    - Read EACH of the 7 target files before editing it — they differ in which extra props they pass (`actions={<LiveTimeDisplay />}` on kds/kds-bar, plain title on others). Do not assume; preserve whatever props each page already passes.
  </read_first>
  <action>
    For each of the 7 files, apply the identical Pattern B migration: (1) remove `BackToHomeButton` from the `@shared/ui` named-import list (keep the other imports on that line, e.g. `LiveTimeDisplay`, `PageContainer`); if `BackToHomeButton` was the only import from `@shared/ui`, keep the remaining ones. (2) Delete the standalone `<BackToHomeButton />` JSX line (it sits directly inside the outer `<div className="flex h-screen flex-col">`, above `<main>`). (3) Add `backTo="/home"` to that page's existing `<PageContainer ...>` opening tag, alongside its current props (title/actions/description) — do NOT remove or reorder existing props, only add `backTo`. Keep the outer `<div className="flex h-screen flex-col">` + `<main className="flex-1 overflow-auto">` scroll shell exactly as-is (PageContainer does NOT replace it — per 30-RESEARCH.md Anti-Patterns). Do not add `backLabel` (default 'Home' is correct for all 7). Do NOT touch login/home (D-02 — not in this plan's file list anyway).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "BackToHomeButton" src/pages/{kds,kds-bar,kitchen-prep,pool-tables,rappi,rbac,waitlist}/index.tsx` returns nothing (zero remaining references in these 7 files).
    - Each of the 7 files contains `backTo="/home"` on its `<PageContainer>` call.
    - Each file still contains its original `<main className="flex-1 overflow-auto">` scroll wrapper and its pre-existing PageContainer props (e.g. kds still has `actions={<LiveTimeDisplay />}`).
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors — no new errors.
    - `npm run lint` exits 0 (no dangling-import or import/order regressions).
  </acceptance_criteria>
  <done>All 7 pages render back-nav via `backTo="/home"`; no BackToHomeButton references remain in them; typecheck/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Prop-swap on presentational page components; no new input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | (n/a) | 7 page components | accept | No new attack surface — internal navigation-chrome refactor only (30-RESEARCH.md Security Domain). No package installs → supply-chain threat N/A. |
</threat_model>

<verification>
- `npm run typecheck` — only the 2 pre-existing unrelated errors.
- `npm run lint` — exit 0.
- `npm run test` (full unit suite) — no regressions (the pages have no dedicated unit tests asserting BackToHomeButton, but the suite guards against dangling references).
</verification>

<success_criteria>
- All 7 already-PageContainer pages use `backTo="/home"`; standalone BackToHomeButton removed from each.
- Outer full-height scroll shells preserved (no visual layout change beyond the back link moving into the header).
</success_criteria>

<output>
Create `.planning/phases/30-shared-shell-primitive-extension/30-02-SUMMARY.md` when done.
</output>
