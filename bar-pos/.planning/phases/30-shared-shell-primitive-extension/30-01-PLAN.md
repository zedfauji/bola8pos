---
phase: 30-shared-shell-primitive-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/PageContainer.tsx
  - src/shared/ui/SectionHeader.tsx
  - src/shared/ui/PageContainer.test.tsx
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "PageContainer accepts optional backTo and backLabel props"
    - "When backTo is provided, SectionHeader renders an anchor (role=link) with href equal to backTo"
    - "When backTo is provided but backLabel is not, the link text is exactly 'Home'"
    - "When backTo is omitted, no back link renders (SectionHeader is unchanged from today)"
    - "Existing SectionHeader consumers (title/description/action/badge) are unaffected"
  artifacts:
    - path: "src/shared/ui/PageContainer.tsx"
      provides: "backTo?/backLabel? props forwarded to SectionHeader via conditional spread"
      contains: "backTo"
    - path: "src/shared/ui/SectionHeader.tsx"
      provides: "Inline ghost back-Link block above the h2 title"
      contains: "ChevronLeft"
    - path: "src/shared/ui/PageContainer.test.tsx"
      provides: "Regression tests for backTo/backLabel behavior (3 cases)"
      contains: "getByRole"
  key_links:
    - from: "src/shared/ui/PageContainer.tsx"
      to: "src/shared/ui/SectionHeader.tsx"
      via: "conditional-spread prop forwarding"
      pattern: "backTo && \\{ backTo \\}"
    - from: "src/shared/ui/SectionHeader.tsx"
      to: "react-router-dom Link"
      via: "back-navigation anchor"
      pattern: "Link to=\\{backTo\\}"
---

<objective>
Extend the shared `PageContainer` / `SectionHeader` primitives with optional `backTo` / `backLabel` props so every route can render its back-navigation affordance through one shared shell instead of the standalone `BackToHomeButton` (D-01). This plan builds the foundation that all 15 caller-migration plans (30-02, 30-03, 30-04) consume — no page is migrated here.

Purpose: SHELL-01 requires a single shared layout shell with inline back-nav. This is the contract every downstream migration depends on.
Output: `PageContainer` and `SectionHeader` gain `backTo`/`backLabel`; a new `PageContainer.test.tsx` locks the three behavioral cases (Wave 0 test-first).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md
@.planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md
@.planning/phases/30-shared-shell-primitive-extension/30-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols/files this plan creates (source-grounding: these are NEW — do not flag as drift):

- `PageContainerProps.backTo` — new optional `string` prop on `src/shared/ui/PageContainer.tsx`
- `PageContainerProps.backLabel` — new optional `string` prop on `src/shared/ui/PageContainer.tsx`
- `SectionHeaderProps.backTo` — new optional `string` prop on `src/shared/ui/SectionHeader.tsx`
- `SectionHeaderProps.backLabel` — new optional `string` prop on `src/shared/ui/SectionHeader.tsx`
- `src/shared/ui/PageContainer.test.tsx` — new test file (did not exist before this phase)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create PageContainer.test.tsx (RED)</name>
  <files>src/shared/ui/PageContainer.test.tsx</files>
  <read_first>
    - src/shared/ui/PageContainer.tsx (current prop signature — no backTo yet)
    - src/shared/ui/SectionHeader.tsx (current markup the back-link is inserted into)
    - src/pages/payments/PaymentsPage.test.tsx (reference for `getByRole('link', { name: /home/i })` + `toHaveAttribute('href', ...)` + MemoryRouter wrapping pattern used across this repo)
    - .planning/phases/30-shared-shell-primitive-extension/30-VALIDATION.md (Wave 0 Requirements — the 3 cases to cover)
  </read_first>
  <behavior>
    - Case (a) no backTo: render `<PageContainer title="X">child</PageContainer>` → `screen.queryByRole('link')` is null (no back link rendered; SectionHeader unchanged).
    - Case (b) backTo without backLabel: render `<PageContainer title="X" backTo="/home">` → `getByRole('link', { name: /home/i })` exists AND `toHaveAttribute('href', '/home')`.
    - Case (c) backTo with custom backLabel: render `<PageContainer title="X" backTo="/pool-tables" backLabel="Pool Tables">` → `getByRole('link', { name: /pool tables/i })` exists AND `toHaveAttribute('href', '/pool-tables')`.
  </behavior>
  <action>
    Create `src/shared/ui/PageContainer.test.tsx` following the existing repo test idiom: import `render`/`screen` from `@testing-library/react`, wrap the component under test in `MemoryRouter` from `react-router-dom` (PageContainer's back link is a react-router `Link`, so a Router ancestor is mandatory or it throws), and use `describe`/`it`/`expect` from `vitest`. Write the three cases in the `<behavior>` block above. Assert on the anchor's accessible name and `href` attribute exactly as `PaymentsPage.test.tsx` does — the string `Home` is a load-bearing contract asserted by `PaymentsPage.test.tsx`, `ReportsPage.test.tsx`, `e2e/15-home-navigation.spec.ts`, `e2e/17-payment-pane.spec.ts`, and the shared `e2e/helpers/auth.ts` `logout()` teardown, so case (b) must assert literal `/home/i` text, not `/back/i`. Provide a trivial child node (e.g. a `<div data-testid="child" />`). Do NOT implement the component change in this task — the tests are expected to FAIL until Task 2 (cases b/c fail because backTo renders nothing yet).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/ui/PageContainer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/shared/ui/PageContainer.test.tsx` exists with exactly three `it(...)` cases matching (a)/(b)/(c).
    - Cases (b) and (c) FAIL when run against the current (unextended) PageContainer (RED confirmed) — case (a) may already pass.
    - Test wraps the component in `MemoryRouter`; uses `getByRole('link', ...)` + `toHaveAttribute('href', ...)`.
    - No production source file is modified in this task.
  </acceptance_criteria>
  <done>PageContainer.test.tsx exists; cases (b)/(c) fail against the current component (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SectionHeader + PageContainer with backTo/backLabel (GREEN)</name>
  <files>src/shared/ui/SectionHeader.tsx, src/shared/ui/PageContainer.tsx</files>
  <read_first>
    - src/shared/ui/SectionHeader.tsx (exact current markup — the back-link block goes as the FIRST child inside the existing `<div className="space-y-1">`, before the `<h2>` row)
    - src/shared/ui/PageContainer.tsx (existing conditional-spread pattern for `description`/`actions` at lines 57-60 — replicate it for backTo/backLabel)
    - src/shared/ui/BackToHomeButton.tsx (source of the exact markup being absorbed: `ChevronLeft` + `Button variant="ghost" size="sm" asChild` + `Link` — do NOT change its import set, move it)
    - .planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md (§Component Contract: PageContainer — the exact target markup and the `-ml-2.5 h-6 px-2 text-xs` / `h-3.5 w-3.5` class values, already pinned; do not re-derive)
    - src/shared/ui/PageContainer.test.tsx (the three cases this task must satisfy)
  </read_first>
  <action>
    In `src/shared/ui/SectionHeader.tsx`: add `backTo?: string` and `backLabel?: string` to `SectionHeaderProps`. Add imports `import { ChevronLeft } from 'lucide-react';`, `import { Link } from 'react-router-dom';`, `import { Button } from '@shared/ui/button';` (match the file's existing `@shared/ui/*` import style and keep `import/order` alphabetical/grouped to avoid a lint regression). Insert a back-link block as the FIRST child of the existing `<div className="space-y-1">` (above the `<h2>` row), rendered only when `backTo` is truthy: a `Button` with `variant="ghost" size="sm" asChild className="-ml-2.5 h-6 px-2 text-xs"` wrapping a `Link` with `to={backTo}`, containing `<ChevronLeft className="mr-1 h-3.5 w-3.5" />` followed by `{backLabel ?? 'Home'}`. These exact classes are pinned in 30-UI-SPEC.md — the `-ml-2.5` cancels the button's own horizontal padding so the chevron aligns with the title's left edge; do not invent different values. Leave the `<h2>`, badge, description, and action markup exactly as-is.
    In `src/shared/ui/PageContainer.tsx`: add `backTo?: string` and `backLabel?: string` to `PageContainerProps`, destructure them in the function params, and forward them to `<SectionHeader>` using the SAME conditional-spread idiom already used for description/actions — `{...(backTo && { backTo })}` and `{...(backLabel && { backLabel })}`. Do NOT write `backTo={backTo}` directly: `exactOptionalPropertyTypes` is on project-wide, and passing an explicit `undefined` differs from omitting the prop (matches the existing line-57-60 pattern). Do not change the outer `cn('mx-auto w-full max-w-[1400px] space-y-6 p-6', className)` wrapper.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/ui/PageContainer.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All three `PageContainer.test.tsx` cases PASS (GREEN).
    - `src/shared/ui/SectionHeader.tsx` contains the string `ChevronLeft` and renders `{backLabel ?? 'Home'}` inside a `Link to={backTo}`.
    - `src/shared/ui/PageContainer.tsx` forwards backTo/backLabel via `{...(backTo && { backTo })}` (NOT `backTo={backTo}`).
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors (`src/entities/tab/model/queries.ts(778,11)` and `src/shared/lib/agent/rag.ts(60,7)`) — no new errors introduced.
    - `npm run lint` exits 0 (no new import/order or other violations).
  </acceptance_criteria>
  <done>Tests green; typecheck shows only the 2 documented pre-existing errors; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds two hardcoded-string props forwarded to a react-router `Link`. No new untrusted-input boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | (n/a) | PageContainer/SectionHeader props | accept | No new attack surface — internal navigation-chrome refactor only; `backTo` values are developer-authored route-string literals, never user-controlled (per 30-RESEARCH.md Security Domain). No package installs → supply-chain threat N/A. |
</threat_model>

<verification>
- `npx vitest run src/shared/ui/PageContainer.test.tsx` — 3/3 pass.
- `npm run typecheck` — only the 2 pre-existing unrelated errors present (diff before/after shows same 2 lines).
- `npm run lint` — exit 0.
- Existing SectionHeader consumers still compile (no page migrated yet, so `backTo` absent everywhere → no back link rendered anywhere yet — verified by typecheck).
</verification>

<success_criteria>
- `PageContainerProps` and `SectionHeaderProps` expose optional `backTo`/`backLabel`.
- Back link renders as an anchor with accessible name defaulting to `Home` and `href` = `backTo`, only when `backTo` is provided.
- No behavior change for callers that don't pass `backTo`.
- Foundation ready for 30-02/30-03/30-04 to consume.
</success_criteria>

<output>
Create `.planning/phases/30-shared-shell-primitive-extension/30-01-SUMMARY.md` when done.
</output>
