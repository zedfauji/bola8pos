---
phase: 30-shared-shell-primitive-extension
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/pages/pos/index.tsx
  - src/pages/payments/index.tsx
  - src/pages/pool-table-status/index.tsx
autonomous: true
requirements: [SHELL-01]
must_haves:
  truths:
    - "pos and payments render the shared back-nav header while preserving their full-bleed, full-height split-view layouts"
    - "pos's cart/order split-view and payments' Tabs body remain edge-to-edge (not constrained to a centered 1400px card)"
    - "payments' back link stays an anchor with accessible name 'Home' and href '/home' (PaymentsPage.test.tsx passes)"
    - "pool-table-status renders backTo='/pool-tables' backLabel='Pool Tables' (NOT /home) — its existing correct target is not regressed"
    - "None of the 3 pages import BackToHomeButton anymore"
  artifacts:
    - path: "src/pages/pos/index.tsx"
      provides: "PageContainer with backTo='/home' + width/padding-neutralizing className override"
      contains: "max-w-none"
    - path: "src/pages/payments/index.tsx"
      provides: "PageContainer with backTo='/home' + neutralizing className, Tabs body full-height"
      contains: "backTo=\"/home\""
    - path: "src/pages/pool-table-status/index.tsx"
      provides: "PageContainer with backTo='/pool-tables' backLabel='Pool Tables', hand-rolled back-link removed"
      contains: "backTo=\"/pool-tables\""
  key_links:
    - from: "src/pages/pool-table-status/index.tsx"
      to: "PageContainer backTo"
      via: "corrected parent-route target"
      pattern: "backTo=\"/pool-tables\""
    - from: "src/pages/payments/index.tsx"
      to: "PaymentsPage.test.tsx Home-link assertion"
      via: "SectionHeader Link href=/home"
      pattern: "backTo=\"/home\""
---

<objective>
Migrate the 3 layout-sensitive pages onto the shared shell (SHELL-01, D-01): `pos` and `payments` are full-height horizontal split-views that must stay edge-to-edge, so they adopt `PageContainer` with a `className` override that neutralizes the default `max-w-[1400px]/p-6/space-y-6` while keeping the inline back-nav header; `pool-table-status` is the one page whose back target is deliberately `/pool-tables` (not `/home`) and must not be regressed to the generic default.

Purpose: SHELL-01 for the pages where PageContainer's default block layout would visually break a full-bleed split-view — resolved via the existing `twMerge`-backed `className` escape hatch, no new component.
Output: pos, payments, pool-table-status on `PageContainer` with layout preserved and correct back targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md
@.planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md
@.planning/phases/30-shared-shell-primitive-extension/30-RESEARCH.md
</context>

## Artifacts this phase produces

This plan creates NO new symbols. It adds first-time `PageContainer` usage (with the `backTo`/`backLabel` props created in 30-01, plus the pre-existing `className` prop) to 3 page files and removes `BackToHomeButton` / hand-rolled back-link markup from them. `backTo`/`backLabel` are new this phase (created in 30-01) — do not flag their usage here as drift.

<tasks>

<task type="auto">
  <name>Task 1: Full-bleed PageContainer adoption for pos and payments (className override)</name>
  <files>src/pages/pos/index.tsx, src/pages/payments/index.tsx</files>
  <read_first>
    - src/pages/pos/index.tsx (outer `<div className="flex h-screen flex-col bg-background"><BackToHomeButton /><div className="flex min-w-0 flex-1 items-center justify-center p-4 md:p-6">...split-view...`; imports `{ BackToHomeButton, POSButton, ProtectedAction }` from `@shared/ui`)
    - src/pages/payments/index.tsx (outer `<div className="flex h-screen flex-col bg-background"><div className="flex shrink-0 items-center border-b px-4 py-2"><BackToHomeButton /></div><Tabs className="flex flex-1 flex-col overflow-hidden">...`; imports `{ BackToHomeButton }` from `@shared/ui`)
    - src/pages/payments/PaymentsPage.test.tsx (asserts `getByRole('link', { name: /home/i })` + `href='/home'` — MUST keep passing after migration)
    - src/shared/ui/PageContainer.tsx (post-30-01; outer div is `cn('mx-auto w-full max-w-[1400px] space-y-6 p-6', className)` — className merged via twMerge so later conflicting utilities win)
    - src/shared/lib/utils.ts (confirms `cn = twMerge(clsx(...))` — the override is safe, not naive concatenation)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§Pattern C) and 30-RESEARCH.md (Pitfall 2 — the exact full-bleed failure mode)
  </read_first>
  <action>
    For BOTH pages: drop `BackToHomeButton` from the `@shared/ui` import (keep the other named imports on that line for pos: `POSButton`, `ProtectedAction`) and remove the standalone `<BackToHomeButton />` (and, for payments, its surrounding `<div className="flex shrink-0 items-center border-b px-4 py-2">` wrapper). Wrap each page's existing full-height body in `<PageContainer title=... backTo="/home" className="...">` where the `className` neutralizes PageContainer's block defaults so the split-view stays edge-to-edge and full-height: pass width/padding/spacing overrides such as `mx-0 max-w-none space-y-0 p-0` plus a height utility so the body still fills the viewport (start from `mx-0 h-screen max-w-none space-y-0 p-0` and trim to what each page's existing structure needs — since these two pages already sit at the top of the route with their own `h-screen` outer div, keep exactly one `h-screen`/full-height owner; do not stack two). pos → `title="POS"`; keep the `flex min-w-0 flex-1 items-center justify-center p-4 md:p-6` split-view block and everything inside it unchanged as PageContainer's children. payments → `title="Payments"`; keep the `<Tabs className="flex flex-1 flex-col overflow-hidden">` body and its TabsList/TabsContent unchanged as children. The header (title + back link) renders above the split body; the split body must retain its `flex-1 overflow-hidden`/`overflow-y-auto` fill. Verify visually against the covering E2E specs (see verification) — do NOT let `space-y-6`/`p-6` defaults collapse the `flex-1` regions (Pitfall 2). Do not change any handler, prop, or validation on PaymentPane/RefundsList/OrderPanel (COMPONENT-04 zero-behavior-change intent applies).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/pages/payments/PaymentsPage.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "BackToHomeButton" src/pages/{pos,payments}/index.tsx` returns nothing.
    - Both files render `<PageContainer title="POS"|"Payments" backTo="/home" className="...">` with a className containing `max-w-none` and `p-0` (defaults neutralized).
    - pos still contains its `flex min-w-0 flex-1` split-view block; payments still contains `<Tabs className="flex flex-1 flex-col overflow-hidden">`.
    - Exactly one full-height (`h-screen`) owner remains per page (no doubled `h-screen` nesting that would break scroll).
    - `PaymentsPage.test.tsx` passes (`getByRole('link', { name: /home/i })`, `href='/home'`).
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>pos + payments on PageContainer with neutralizing className; full-bleed split-views preserved; PaymentsPage.test.tsx green; typecheck/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: pool-table-status — adopt backTo='/pool-tables' (correct target, not /home)</name>
  <files>src/pages/pool-table-status/index.tsx</files>
  <read_first>
    - src/pages/pool-table-status/index.tsx (already uses `<PageContainer title="Table Status">` but ALSO hand-rolls its own `<div className="px-4 pt-4"><Button variant="ghost" size="sm" asChild><Link to="/pool-tables"><ChevronLeft.../>Pool Tables</Link></Button></div>` back-link above `<main>`; imports `ChevronLeft`, `Link`, `Button`)
    - src/shared/ui/PageContainer.tsx (post-30-01 signature)
    - .planning/phases/30-shared-shell-primitive-extension/30-PATTERNS.md (§Pattern D — the one correct-target exception; do NOT default this to /home)
    - .planning/phases/30-shared-shell-primitive-extension/30-UI-SPEC.md (backTo table — pool-table-status row = `/pool-tables`, `Pool Tables`)
  </read_first>
  <action>
    Remove the hand-rolled `<div className="px-4 pt-4"><Button ...><Link to="/pool-tables">...Pool Tables...</Link></Button></div>` back-link block entirely, and delete its now-unused imports (`ChevronLeft` from lucide-react, `Button` from `@shared/ui/button`; keep `Link`/`useNavigate`/`useParams` only if still used elsewhere in the file — check before removing `Link`). Add `backTo="/pool-tables"` and `backLabel="Pool Tables"` to the existing `<PageContainer title="Table Status">` call. Do NOT use the `/home` default here — this page's parent is the pool-tables grid (D-01 explicitly calls this out as the one target-correction). Keep the outer `<div className="flex h-screen flex-col"><main className="flex-1 overflow-auto">` shell and `<TableStatusPanel tableId={tableId} />` child unchanged. Do NOT touch `src/widgets/TableStatusPanel/index.tsx`'s separate internal `ArrowLeft` button — that is COMPONENT-03, deferred to Phase 31 (out of scope this phase).
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/pool-table-status/index.tsx` renders `<PageContainer title="Table Status" backTo="/pool-tables" backLabel="Pool Tables">`.
    - The hand-rolled `<div className="px-4 pt-4">...Pool Tables...</div>` back-link block is gone; no unused `ChevronLeft`/`Button` imports remain (lint would flag them).
    - The string `/home` does NOT appear as this page's backTo value.
    - `src/widgets/TableStatusPanel/index.tsx` is unchanged (not in this plan's file list).
    - `npm run typecheck` shows only the 2 pre-existing unrelated errors; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>pool-table-status uses backTo='/pool-tables' backLabel='Pool Tables' via PageContainer; hand-rolled link + unused imports removed; typecheck/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Layout-only migration on payment-adjacent and pool pages. No prop/handler/validation change on PaymentPane/RefundsList/OrderPanel/TableStatusPanel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | (n/a) | pos/payments/pool-table-status pages | accept | No new attack surface — internal navigation-chrome refactor only (30-RESEARCH.md Security Domain). Payment-critical surfaces receive markup/className-only swaps with zero behavior change, guarded by existing E2E specs. No package installs → supply-chain threat N/A. |
</threat_model>

<verification>
- `npx vitest run src/pages/payments/PaymentsPage.test.tsx` — pass.
- `npm run typecheck` — only the 2 pre-existing unrelated errors.
- `npm run lint` — exit 0.
- Targeted E2E (phase gate, requires dev server + `.env.local`): `npx playwright test e2e/03-tab-order.spec.ts e2e/05-payments.spec.ts e2e/17-payment-pane.spec.ts e2e/41-split-payment.spec.ts e2e/16-table-status.spec.ts` — confirms pos/payments split-views still function full-bleed and pool-table-status back link still routes to `/pool-tables` (T15).
</verification>

<success_criteria>
- pos, payments on PageContainer with neutralizing className — full-bleed, full-height layouts intact, zero behavior change.
- pool-table-status back target correctly `/pool-tables` with label `Pool Tables`.
- No BackToHomeButton references remain in any of the 3 pages.
</success_criteria>

<output>
Create `.planning/phases/30-shared-shell-primitive-extension/30-04-SUMMARY.md` when done.
</output>
