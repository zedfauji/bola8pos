---
phase: 20-promotions-engine
plan: 04
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx
  - src/features/manage-promotions/ui/PromotionBuilderForm.tsx
  - src/features/manage-promotions/ui/ManagePromotionsTab.tsx
  - src/features/manage-promotions/ui/ManagePromotionsTab.test.tsx
  - src/features/manage-promotions/ui/PromotionBuilderForm.test.tsx
  - src/features/manage-promotions/index.ts
  - src/widgets/SettingsTabsPanel/index.tsx
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "A Settings → Promotions tab (gated on manage_products) lists promotions with target-type badge and an inline Active toggle, and offers create/edit/delete"
    - "The create/edit dialog exposes name, discount type + value, target type + target picker, priority, active toggle, and a day/time availability editor"
    - "The discount-value input and target picker swap correctly when the discount/target type changes, with no stale value carried over"
    - "The availability editor is a near-verbatim clone of ComboAvailabilityEditor (day chips + time windows, no visual changes)"
  artifacts:
    - path: "src/features/manage-promotions/ui/ManagePromotionsTab.tsx"
      provides: "Promotions admin list + create/delete + inline Active switch + target-type badges"
      min_lines: 120
    - path: "src/features/manage-promotions/ui/PromotionBuilderForm.tsx"
      provides: "Create/edit dialog with conditional discount/target fields + fixed-price stacking hint"
      min_lines: 120
    - path: "src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx"
      provides: "Day-of-week + time-window picker (ComboAvailabilityEditor clone)"
      min_lines: 120
    - path: "src/features/manage-promotions/index.ts"
      provides: "explicit-named barrel exporting ManagePromotionsTab"
  key_links:
    - from: "SettingsTabsPanel"
      to: "ManagePromotionsTab"
      via: "tab entry inside the canManageProducts push block"
      pattern: "key: 'promotions'"
    - from: "ManagePromotionsTab"
      to: "entities/promotion hooks"
      via: "usePromotions / useMutationCreatePromotion / useMutationDeletePromotion"
      pattern: "usePromotions\\(\\)"
---

<objective>
Build the SC-4 admin surface: Settings → Promotions. Clone the Combos admin three-file pattern (list tab + builder dialog + availability editor) into `features/manage-promotions/`, adapting fields to promotions per the locked UI-SPEC (discount type/value, target type/picker, priority, inline Active toggle, fixed-price stacking hint). Register the tab in `SettingsTabsPanel` gated on `manage_products`.

Purpose: Give managers a create/edit/disable surface for promotions modeled 1:1 on the pattern they already know (D-06), reusing existing shadcn primitives with zero net-new installs.
Output: Three UI components + barrel + tab registration + RTL tests. All prices/discounts rendered here are read-only display (Pitfall 1) — never fed into a mutation as a final charged price.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-UI-SPEC.md
@.planning/phases/20-promotions-engine/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PromotionAvailabilityEditor + PromotionBuilderForm</name>
  <files>src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx, src/features/manage-promotions/ui/PromotionBuilderForm.tsx</files>
  <read_first>
    - src/features/manage-combos/ui/ComboAvailabilityEditor.tsx (full — clone near-verbatim: DAY_LABELS, WindowRow, drafts state, delete-then-reinsert save mutation, day-toggle styling, "No windows = always available" copy)
    - src/features/manage-combos/ui/ComboBuilderForm.tsx (name field, conditional sub-form pattern, useComboEligibleProducts product query, native `<select>` classes)
    - src/entities/promotion/model/queries.ts (usePromotion, usePromotionAvailabilityWindows, useMutationUpdatePromotion — authored in Plan 20-02)
    - src/shared/ui/select.tsx, src/shared/ui/MoneyInput.tsx, src/shared/ui/CategoryTreePicker (shadcn primitives to reuse)
    - .planning/phases/20-promotions-engine/20-UI-SPEC.md (§2 field order, Copywriting Contract, badge colors, States Checklist)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (PromotionAvailabilityEditor + PromotionBuilderForm sections)
  </read_first>
  <action>
    `PromotionAvailabilityEditor.tsx`: clone `ComboAvailabilityEditor.tsx` near-verbatim. Swap ONLY the prop `comboId` → `promotionId`, table `combo_availability` → `promotion_availability`, column `combo_product_id` → `promotion_id`, and the query invalidation key `comboKeys.availability(...)` → `promotionKeys.availability(...)`. Keep every string, class, and layout identical (day chips, time inputs, "No windows = always available" shown top and bottom, "+ Add window"/"Save"/"Saving…"/"Remove", "End time must be after start time" validation). Use `const db = supabase as any` (pre-regen).

    `PromotionBuilderForm.tsx`: clone `ComboBuilderForm.tsx` scaffolding (imports, `const db = supabase as any`, loading state "Loading promotion…", `useId`-based labels, right-aligned Save). Field order per UI-SPEC §2: (1) Promotion name `Input` required maxLength 100; (2) Discount type — the three D-04 shapes via shadcn `Select` with options "Percentage off"=`percentage`, "Fixed amount off"=`fixed_amount`, "Fixed override price"=`fixed_price`; (3) Discount value — conditional: `Input type="number" min={0} max={100} step={1}` with `%` hint for `percentage`, `MoneyInput` for `fixed_amount`/`fixed_price` (clear the value on type switch — no stale value); (4) Fixed-price stacking hint text (render ONLY when `discount_type === 'fixed_price'`, exact copy from UI-SPEC Copywriting Contract); (5) Target type — shadcn `Select` options "Item"=`item`, "Category"=`category`, "Pool time billing"=`pool_billing`, "Pool time bonus"=`pool_grant`; (6) Target picker — conditional: native `<select>` product picker (verbatim `ComboBuilderForm` SlotEditor classes + a `useEligibleProducts`-style query for `is_active` non-combo products) when `item`, `CategoryTreePicker` when `category`, hidden for `pool_billing`/`pool_grant`; (7) Priority `Input type="number" min={0} step={1}` default `0` with the "Lower number applies first…" hint; (8) Active `Switch` labeled "Active" default off. Save calls `useMutationUpdatePromotion` with the full camelCase payload; `toast.success('Promotion saved')` / `toast.error(e instanceof Error ? e.message : 'Failed to save promotion')`. All discount/price values here are display/config only.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/features/manage-promotions/ui/PromotionAvailabilityEditor.tsx src/features/manage-promotions/ui/PromotionBuilderForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; `npm run lint` reports 0 errors for both files.
    - `PromotionAvailabilityEditor` references `promotion_availability`/`promotion_id` and contains the string `No windows = always available`.
    - `PromotionBuilderForm` renders the fixed-price stacking hint only when `discount_type === 'fixed_price'`, and swaps the discount-value input + target picker on type change without carrying a stale value.
    - Discount-type and target-type use shadcn `Select`; product target uses the native `<select>` classes; category target uses `CategoryTreePicker`.
  </acceptance_criteria>
  <done>The availability editor is a faithful clone and the builder form implements all 8 fields with correct conditional swapping and the fixed-price hint.</done>
</task>

<task type="auto">
  <name>Task 2: ManagePromotionsTab + barrel + SettingsTabsPanel registration</name>
  <files>src/features/manage-promotions/ui/ManagePromotionsTab.tsx, src/features/manage-promotions/index.ts, src/widgets/SettingsTabsPanel/index.tsx</files>
  <read_first>
    - src/features/manage-combos/ui/ManageCombosTab.tsx (full — list/empty/error/loading states, create-then-open-edit-dialog flow, edit dialog composition of builder + border-t + availability editor, delete ConfirmDialog)
    - src/features/manage-combos/index.ts (barrel shape)
    - src/widgets/SettingsTabsPanel/index.tsx (the canManageProducts push block ~lines 65-95, the `key: 'combos'` entry template, the ManageCombosTab import)
    - src/entities/promotion/model/queries.ts (usePromotions, useMutationCreatePromotion, useMutationDeletePromotion)
    - src/shared/ui/switch.tsx, src/shared/ui/badge.tsx
    - .planning/phases/20-promotions-engine/20-UI-SPEC.md (§1 list interaction, Copywriting Contract, target-type badge color table, States Checklist)
  </read_first>
  <action>
    `ManagePromotionsTab.tsx`: clone `ManageCombosTab.tsx` structure. Use `usePromotions()` for the list; description line "Manage promotions — automatic discounts on items, categories, or pool time during set hours."; header "+ Add promotion" button (shows "Creating…" while pending). Loading "Loading promotions…", error "Could not load promotions: {message}", empty-state box (heading "No promotions yet", body + "Add promotion" CTA). Each list `<li className="flex items-center gap-3 px-3 py-2.5">`: promotion name (truncate, flex-1) + a cosmetic row-summary sub-text (percentage → "{value}% off", fixed_amount → "-${value}", fixed_price → "Fixed at ${value}", pool_grant → "+{value} min pool time") + a target-type `Badge` using the ad-hoc classes from UI-SPEC (item=`bg-blue-500/20 text-blue-300`, category=`bg-purple-500/20 text-purple-300`, pool_billing=`bg-cyan-500/20 text-cyan-300`, pool_grant=`bg-amber-500/20 text-amber-300`) + an inline `Switch` (`aria-label="{name} active"`) wired to `useMutationUpdatePromotion({ id, isActive })` + Edit (Pencil, `aria-label="Edit {name}"`) + Delete (Trash2, `aria-label="Delete {name}"`). "+ Add promotion" runs `useMutationCreatePromotion` (draft `is_active:false`) then opens the edit dialog on the new id. Edit dialog: `DialogContent className="max-w-lg sm:max-w-lg overflow-y-auto max-h-[90vh]"` composing `PromotionBuilderForm` + `border-t pt-4` + `PromotionAvailabilityEditor`. Delete uses `ConfirmDialog` (title "Delete '{name}'?", description "This will remove the promotion. Orders that already applied it keep their recorded discount — nothing is retroactively changed.", confirm "Delete Promotion", variant destructive) → `useMutationDeletePromotion`; toasts "Promotion deleted" / "Failed to delete promotion". The row summary is cosmetic display only.

    `index.ts`: `export { ManagePromotionsTab } from './ui/ManagePromotionsTab';`.

    `SettingsTabsPanel/index.tsx`: add `import { ManagePromotionsTab } from '@features/manage-promotions';` next to the ManageCombosTab import, and add `{ key: 'promotions', label: 'Promotions', render: () => <ManagePromotionsTab /> }` inside the SAME `if (canManageProducts) { out.push(...) }` block, adjacent to the `key: 'combos'` entry (gate on `manage_products`, NOT a new action — RESEARCH A1).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/features/manage-promotions src/widgets/SettingsTabsPanel/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0; lint 0 errors for the touched paths.
    - `SettingsTabsPanel` contains `key: 'promotions'` inside the `canManageProducts` push block and imports `ManagePromotionsTab`.
    - ManagePromotionsTab renders the target-type badge with the four UI-SPEC color classes and an inline Active `Switch` per row.
    - The delete ConfirmDialog uses the UI-SPEC copy referencing that already-applied discounts are not retroactively changed.
  </acceptance_criteria>
  <done>The Promotions tab is registered (manager+), lists promotions with badge + inline toggle, and supports create/edit/delete via the cloned dialog composition.</done>
</task>

<task type="auto">
  <name>Task 3: RTL tests for ManagePromotionsTab + PromotionBuilderForm</name>
  <files>src/features/manage-promotions/ui/ManagePromotionsTab.test.tsx, src/features/manage-promotions/ui/PromotionBuilderForm.test.tsx</files>
  <read_first>
    - src/features/manage-combos/ui/ManageCombosTab.tsx (component under test's analog for query-mocking approach)
    - src/features/manage-modifier-inventory-rules (or any existing feature ui .test.tsx) for the RTL + QueryClient wrapper + mocked-hook style used in this repo
    - .planning/phases/20-promotions-engine/20-UI-SPEC.md (States Checklist — the exact states to cover)
  </read_first>
  <action>
    Create RTL tests mocking the `entities/promotion` hooks (via `vi.mock`). `ManagePromotionsTab.test.tsx`: assert (1) loading renders "Loading promotions…"; (2) error renders "Could not load promotions: {message}"; (3) empty renders "No promotions yet" + "Add promotion" CTA; (4) a populated list renders a row with the promotion name, its target-type badge, and an Active switch with `aria-label="{name} active"`; (5) clicking "+ Add promotion" calls the create mutation. `PromotionBuilderForm.test.tsx`: assert (1) switching discount type from percentage to fixed_amount swaps the input and clears the stale value; (2) the fixed-price stacking hint appears only for `fixed_price`; (3) switching target type to `category` shows the CategoryTreePicker and to `item` shows the native product select. Keep tests deterministic and green.
  </action>
  <verify>
    <automated>npx vitest run src/features/manage-promotions/ui/ManagePromotionsTab.test.tsx src/features/manage-promotions/ui/PromotionBuilderForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass under `npx vitest run`.
    - ManagePromotionsTab test covers loading/error/empty/populated/create states.
    - PromotionBuilderForm test covers discount-type swap-and-clear, fixed-price hint conditional, and target-picker swap.
  </acceptance_criteria>
  <done>RTL coverage exists for the tab's five list states and the builder's conditional-field behavior; all green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- React components: `ManagePromotionsTab`, `PromotionBuilderForm`, `PromotionAvailabilityEditor`
- Barrel export: `@features/manage-promotions` → `ManagePromotionsTab`
- SettingsTabsPanel tab entry: `key: 'promotions'`
- Files: `src/features/manage-promotions/**`, `src/widgets/SettingsTabsPanel/index.tsx` (modified)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → promotions table | Manager/admin CRUD gated at the DB by manage_products RLS (Plan 20-01); UI is a thin client |
| cosmetic price display → billing | Row summaries and discount previews are display-only, never sent as a charged price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege | Promotions settings tab visibility/write | mitigate | Tab gated on `can('manage_products')`; DB RLS independently enforces writes (defense-in-depth, Plan 20-01) |
| T-20-04b | Tampering | client-rendered discount summary | mitigate | Row summaries/preview are read-only display; server (evaluate_promotions_for_item) is the sole price authority — documented in code comments |
| T-20-04c | Tampering | form input bounds | mitigate | Zod (Plan 20-02) + DB CHECK (Plan 20-01) reject negative/over-100 values regardless of the form widget |
| T-20-SC | Tampering | shadcn/npm installs | mitigate | Zero net-new component installs (UI-SPEC Registry Safety); slopcheck N/A |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` for all touched paths.
- `npx vitest run` for the two RTL test files.
- Visual/interaction UAT of the live tab is exercised in the Plan 20-09 e2e/UAT gate.
</verification>

<success_criteria>
- Settings → Promotions tab registered (manager+) with list, inline Active toggle, target-type badges, create/edit/delete.
- Builder form implements all fields with correct conditional swaps + fixed-price hint.
- Availability editor is a faithful ComboAvailabilityEditor clone.
- Typecheck, lint, and RTL tests green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-04-SUMMARY.md` when done.
</output>
