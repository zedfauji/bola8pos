---
phase: 20-promotions-engine
plan: 08
type: execute
wave: 5
depends_on: ["20-06"]
files_modified:
  - src/entities/pool-table/model/queries.ts
  - src/entities/pool-table/model/queries.test.ts
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "Stopping a pool session goes through the stop_pool_session SECURITY DEFINER RPC — the client no longer writes total_charge/billed_minutes via a raw .update()"
    - "The client passes only the session id + expected version; server computes billing, applies pool_billing discounts, and consumes pool_grant minutes"
    - "A stale expected version surfaces the existing STALE_VERSION toast/error path"
  artifacts:
    - path: "src/entities/pool-table/model/queries.ts"
      provides: "useMutationStopSession rewired to call stop_pool_session"
      contains: "stop_pool_session"
  key_links:
    - from: "useMutationStopSession"
      to: "stop_pool_session RPC"
      via: "supabase.rpc('stop_pool_session', { p_session_id, p_expected_version })"
      pattern: "rpc\\('stop_pool_session'"
    - from: "P0V01 / PGRST version mismatch"
      to: "staleVersionError → handleVersionError toast"
      via: "error mapping in the mutation"
      pattern: "staleVersionError"
---

<objective>
Rewire the pool-session stop write to the new `stop_pool_session` RPC (Plan 20-05), removing the raw client `.update()` on `pool_sessions` and the client-side authoritative billing computation. After this plan, pool billing — including D-05a pool_billing discounts and D-05b grant consumption — is computed and written server-side atomically; the client only triggers it and renders the result.

Purpose: Close the Pitfall 3 gap (no RPC boundary for the pool stop write) so the discount + audit + charge are atomic, and let the server be the sole authority for `total_charge`/`billed_minutes`.
Output: `useMutationStopSession` calls the RPC; its unit test is updated. This is parallel-safe with Plan 20-07 (disjoint files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
@.planning/phases/20-promotions-engine/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire useMutationStopSession to call stop_pool_session</name>
  <files>src/entities/pool-table/model/queries.ts</files>
  <read_first>
    - src/entities/pool-table/model/queries.ts (useMutationStopSession ~308-425 — the current fetch + client computePoolSessionBilling + two version-guarded/legacy raw .update() branches on pool_sessions + the pool_tables status update + onMutate optimistic block; mapPoolSessionRow ~40)
    - supabase/migrations/20260710000006_stop_pool_session_rpc.sql (the RPC contract authored in Plan 20-05 — args p_session_id/p_expected_version, returned jsonb shape, P0V01 on stale version)
    - src/entities/tab/model/queries.ts (useMutationAddOrder ~611-629 — the established pattern for passing p_expected_version + mapping stale-version errors, to mirror)
    - src/features/stop-pool-timer/ui/StopSessionConfirm.tsx (~61,87 — confirms the mutateAsync input shape; keep it compatible)
  </read_first>
  <action>
    In `useMutationStopSession`, replace the pool_sessions billing WRITE path with a single RPC call. Keep the mutation input shape (`{ sessionId, tableId, ratePerHour }`) so `StopSessionConfirm` needs no change; `ratePerHour` is no longer used for the authoritative write (the RPC reads the rate server-side) — leave it in the input for the UI's pre-confirm preview only. Remove from the mutation's write path: the `settings` billing fetch, the client `computePoolSessionBilling` call, and BOTH the version-guarded and legacy raw `.update()` branches on `pool_sessions`. Fetch the session's cached version (from the pre-fetch `SELECT * ... .single()` you already do, or from cached query data — mirror `useMutationAddOrder`'s cached-version approach) and call `supabase.rpc('stop_pool_session', { p_session_id: sessionId, p_expected_version: cachedVersion ?? null })` via `supabaseQuery`. On a P0V01 / PGRST stale-version error, return `staleVersionError(...)` (same mapping the file already uses); on other errors log + return `unknownError`. Map the RPC's returned jsonb (id, stopped_at, billed_minutes, total_charge, version, tab_id, table_id) into a `PoolSession` through `mapPoolSessionRow` (adapt field access to the returned shape). Keep the subsequent `pool_tables` status update (`status: 'available', current_session_id: null`) and the `onMutate` optimistic table-status block unchanged. Add a comment: `total_charge`/`billed_minutes` are now server-authoritative (Pitfall 3); the client no longer computes them for the write.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/entities/pool-table/model/queries.ts && grep -q "stop_pool_session" src/entities/pool-table/model/queries.ts && ! grep -n "computePoolSessionBilling" src/entities/pool-table/model/queries.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useMutationStopSession` calls `supabase.rpc('stop_pool_session', ...)` with `p_session_id` + `p_expected_version`; no raw `.update()` on `pool_sessions` remains in the stop path.
    - `computePoolSessionBilling` is no longer imported/used in `queries.ts` (server computes billing).
    - Stale-version errors map to `staleVersionError`; the `pool_tables` status update + `onMutate` optimistic block are preserved.
    - `StopSessionConfirm` still typechecks (input shape unchanged); `npm run typecheck` + lint exit 0.
  </acceptance_criteria>
  <done>The pool stop write is a single atomic RPC call; the client no longer computes or writes the authoritative pool charge.</done>
</task>

<task type="auto">
  <name>Task 2: Update the useMutationStopSession unit test</name>
  <files>src/entities/pool-table/model/queries.test.ts</files>
  <read_first>
    - src/entities/pool-table/model/queries.test.ts (existing stop-session test coverage + the supabase mock style used in this file)
    - src/entities/tab/model/queries.test.ts (mock pattern for asserting supabase.rpc was called with the right args)
  </read_first>
  <action>
    Update (or add) unit coverage for the rewired hook: mock `supabase.rpc` and assert (1) stopping a session calls `rpc('stop_pool_session', { p_session_id, p_expected_version })` with the cached version; (2) the returned jsonb is mapped into a `PoolSession` with the server-provided `total_charge`/`billed_minutes`; (3) a P0V01/PGRST stale-version RPC error resolves to a `staleVersionError` Result (not a raw throw). Remove/replace any assertions that expected a client-side `computePoolSessionBilling` write. Keep tests deterministic and green.
  </action>
  <verify>
    <automated>npx vitest run src/entities/pool-table/model/queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file passes and asserts the RPC call args + the stale-version mapping.
    - No remaining assertion depends on a client-side pool billing write.
  </acceptance_criteria>
  <done>Unit tests reflect the RPC-based stop path, including the version-guard mapping.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Modified hook: `useMutationStopSession` (now RPC-backed)
- No new exported symbols; `stop_pool_session` RPC itself is created in Plan 20-05
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → stop_pool_session | Client submits only session id + expected version; billing/discount/grant logic is server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03d | Tampering | client-written pool total_charge | mitigate | Raw `.update()` removed; the SECURITY DEFINER RPC is the sole writer of `total_charge`/`billed_minutes` |
| T-20-03c | Tampering | concurrent stop race | mitigate | `p_expected_version` forwarded to the RPC's FOR UPDATE / P0V01 guard; stale version surfaces the existing retry toast |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages; slopcheck N/A |
</threat_model>

<verification>
- `npm run typecheck` + `npm run lint` for the touched file; `npx vitest run` for the hook test.
- Live pool discount/grant behavior asserted by the Plan 20-05 integration scaffold running post-push + the Plan 20-09 gate.
</verification>

<success_criteria>
- useMutationStopSession calls stop_pool_session; no client-side pool billing write remains.
- Stale-version handling preserved; StopSessionConfirm unaffected.
- Typecheck, lint, and the hook test green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-08-SUMMARY.md` when done.
</output>
