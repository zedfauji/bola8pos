---
phase: 20-promotions-engine
plan: 09
type: execute
wave: 6
depends_on: ["20-07", "20-08"]
files_modified:
  - src/entities/promotion/model/hh-parity.integration.test.ts
  - e2e/43-promotions.spec.ts
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "Every migrated HH product yields, via the server promotions engine, the identical charged price the legacy resolveProductPrice produced during its window"
    - "An end-to-end promotions spec exercises admin create/edit/disable and an order that applies a promotion at the discounted price"
    - "A human has confirmed the live engine (item/category discount, pool billing discount, bonus minutes, admin UI) before any legacy columns are dropped"
  artifacts:
    - path: "src/entities/promotion/model/hh-parity.integration.test.ts"
      provides: "D-07 parity gate: legacy HH price == server-evaluated price for all migrated rows"
    - path: "e2e/43-promotions.spec.ts"
      provides: "Playwright coverage for the promotions admin + order-time discount flow"
  key_links:
    - from: "legacy resolveProductPrice output"
      to: "server evaluate_promotions output"
      via: "parity assertion per migrated HH row (columns still present)"
      pattern: "resolveProductPrice"
---

<objective>
Prove the new server engine reproduces the legacy happy-hour prices for every migrated row (D-07 parity), add end-to-end Playwright coverage, and gate on a human UAT — all BEFORE any legacy `happy_hour_*` column is dropped (Pitfall 4's verification window). This is the safety checkpoint that authorizes the destructive Plan 20-10.

Purpose: Guarantee zero pricing regression from the HH→promotions supersession while the old client path is still available as a fallback.
Output: A parity integration test + `e2e/43-promotions.spec.ts` + a BLOCKING human verification. `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 parity integration test (legacy price == server price)</name>
  <files>src/entities/promotion/model/hh-parity.integration.test.ts</files>
  <read_first>
    - src/shared/lib/domain-helpers.ts (resolveProductPrice ~58-74 — the legacy authority the migration must reproduce; still callable because columns are not yet dropped)
    - supabase/migrations/20260710000007_migrate_happy_hour_data.sql (what the migration produced — fixed_price promotions + windows)
    - src/entities/promotion/model/evaluate-promotions-rpc.integration.test.ts (live RPC integration harness to mirror for the end-to-end parity assertion)
    - .planning/phases/20-promotions-engine/20-VALIDATION.md (D-07 migration correctness row)
  </read_first>
  <action>
    Create `src/entities/promotion/model/hh-parity.integration.test.ts` (env-guarded `describe.skipIf(!hasE2eEnv)`, `const db = supabase as any`). Structural parity: query every product with `happy_hour_price IS NOT NULL` whose category has an HH window; for each, assert there is exactly one active `fixed_price`, `item`-target promotion with `discount_value == happy_hour_price` and a `promotion_availability` row whose `start_time`/`end_time` equal the category's `happy_hour_start`/`happy_hour_end`. Behavioral parity (end-to-end, one representative row): for a migrated HH product, compute the legacy expected price with `resolveProductPrice(product, category, <time inside the window>)`, then place an order via `create_order_with_items` sending `unit_price = product.basePrice` and assert the returned `order_items.unit_price` equals that legacy expected price (i.e. `happy_hour_price`). The test FAILS if any migrated row diverges — this is the gate that must be green before Plan 20-10 drops columns.
  </action>
  <verify>
    <automated>npx vitest run src/entities/promotion/model/hh-parity.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - With E2E env present, all migrated HH rows pass structural parity (one fixed_price item promo with matching value + window per row).
    - The behavioral case confirms the server-returned `unit_price` equals `resolveProductPrice`'s legacy output for a representative product during its window.
    - Without E2E env the test skips cleanly (exit 0).
  </acceptance_criteria>
  <done>A parity test proves the server engine reproduces legacy HH pricing for every migrated row; it is green before any column drop.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/43-promotions.spec.ts</name>
  <files>e2e/43-promotions.spec.ts</files>
  <read_first>
    - e2e/42-tip-distribution.spec.ts (structure/numbering convention, loginAs helper usage, settings-tab navigation)
    - e2e/helpers/auth.ts (loginAs — admin PIN 0000)
    - .planning/phases/20-promotions-engine/20-UI-SPEC.md (Copywriting Contract + Interaction Contract — the exact labels/flows to drive)
  </read_first>
  <action>
    Create `e2e/43-promotions.spec.ts` following `42-tip-distribution.spec.ts`'s structure. Cover: (1) admin opens Settings → Promotions, clicks "+ Add promotion", fills name + a percentage discount + item target + priority, toggles Active, saves ("Promotion saved"), and sees the row with its target-type badge + Active switch; (2) editing that promotion and disabling it via the inline Active switch; (3) deleting it via the "Delete Promotion" confirm; (4) an order flow where an active item-target promotion is applied — placing the order yields a discounted line/total on the ticket (server-applied), confirming the cashier is charged the discounted price without any confirmation step (D-02). Use `test.info().annotations.push` for any environment-conditional branch (matching the repo's day-conditional E2E convention) so the spec is always runnable.
  </action>
  <verify>
    <automated>npx playwright test e2e/43-promotions.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/43-promotions.spec.ts --list` enumerates the spec's tests without a syntax/load error.
    - The spec covers admin create/edit/disable/delete + an order-time discount assertion.
  </acceptance_criteria>
  <done>A promotions E2E spec exists (next number after 42), exercising the admin surface and the auto-applied order discount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human UAT + live integration suite before column drop</name>
  <files>src/entities/promotion/, e2e/43-promotions.spec.ts</files>
  <read_first>
    - .planning/phases/20-promotions-engine/20-CONTEXT.md (all locked decisions to spot-check)
    - .planning/phases/20-promotions-engine/20-VALIDATION.md (phase gate — full suite + live integration + e2e)
  </read_first>
  <what-built>
    The full server-side promotions engine is live (post Plan 20-06 push) with client rewires (Plans 20-07/20-08) in place. All Phase-20 live integration tests (`evaluate-promotions-rpc`, `applied-promotions-rls`, `pool-promotions-rpc`, `hh-parity`, `promotions-schema`) can now run against the live DB, and `e2e/43-promotions.spec.ts` is ready.
  </what-built>
  <action>
    AUTOMATION FIRST: run the live integration suite and E2E, capturing results — `npx vitest run src/entities/promotion/model/` (all promotion integration tests now unskipped against live DB) and `npx playwright test e2e/43-promotions.spec.ts`. Also run `npm run test` + `npm run typecheck` + `npm run lint` (full gates). Then present the results and the manual UAT checklist below for the human to confirm on the live app before authorizing the destructive drop.
  </action>
  <how-to-verify>
    1. Live integration suite green: evaluate (server-price-wins over inflated client price), applied_promotions RLS denial, pool_billing discount, pool_grant consumption, and hh-parity all pass.
    2. In the running app (admin): create a 20% item promotion, add that item to a tab, place the order, and confirm the ticket line/total shows the discounted price with NO confirmation prompt (D-02).
    3. Pool: with a "20% off pool time" (pool_billing) promotion active in-window, stop a pool session and confirm the charge is reduced; with a pool_grant promotion, buy the trigger item then stop a session on that tab and confirm minutes/charge reduced.
    4. Admin UI: edit + disable (inline Active switch) + delete a promotion; availability day/time editor saves.
    5. Full `npm run test` + `npm run typecheck` + `npm run lint` green.
  </how-to-verify>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - All live promotion integration tests pass against the live DB; hh-parity is green.
    - Human confirms item/category discount, pool billing discount, pool bonus minutes, and admin create/edit/disable/delete on the live app.
    - `npm run test` + `npm run typecheck` + `npm run lint` exit 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live suite is green and all five UAT checks pass — this authorizes Plan 20-10 to drop the legacy happy_hour columns. Otherwise describe the failure for gap closure.</resume-signal>
  <done>The live engine is verified end-to-end and parity is proven; dropping the legacy columns is authorized.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Test files: `src/entities/promotion/model/hh-parity.integration.test.ts`, `e2e/43-promotions.spec.ts`
- No new production symbols
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-drop verification → destructive drop | The human UAT gate stands between the additive engine and the irreversible column drop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Data integrity | HH→promotions parity | mitigate | Parity test asserts identical prices for all migrated rows while the legacy path is still available; drop is blocked until it passes |
| T-20-03 | Tampering | order-time pricing authority | mitigate | E2E + live integration assert the server overrides an inflated client `unit_price` |
| T-20-09 | Denial of correct billing | premature column drop | mitigate | Destructive drop is isolated behind this blocking UAT gate (Pitfall 4) |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages; slopcheck N/A |
</threat_model>

<verification>
- Parity integration test green (or clean-skip without env).
- E2E spec loads and enumerates.
- Blocking checkpoint: full live integration + unit + typecheck + lint green, plus human UAT sign-off.
</verification>

<success_criteria>
- D-07 parity proven for all migrated rows.
- Promotions E2E spec authored.
- Human UAT + live suite green; column drop authorized.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-09-SUMMARY.md` when done.
</output>
