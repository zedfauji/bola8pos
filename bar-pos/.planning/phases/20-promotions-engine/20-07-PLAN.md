---
phase: 20-promotions-engine
plan: 07
type: execute
wave: 5
depends_on: ["20-06"]
files_modified:
  - src/widgets/OrderPanel/ProductGrid.tsx
  - src/shared/lib/domain-helpers.ts
  - src/shared/lib/domain-helpers.test.ts
  - src/entities/promotion/model/queries.ts
  - src/widgets/OrderPanel/HappyHourBanner.tsx
  - src/widgets/OrderPanel/HappyHourBanner.test.tsx
  - e2e/20-sprint2-revenue.spec.ts
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "The cart sends product.basePrice (undiscounted) to create_order_with_items — the client no longer computes the charged price for HH/promotion items"
    - "A cosmetic isPromotionActive helper exists for read-only display and never feeds a mutation payload"
    - "The order panel shows an 'Active Promotions' banner (repurposed from HappyHourBanner) listing currently-active promotions, with a correct countdown that omits 'Ends in…' for always-available promotions"
  artifacts:
    - path: "src/widgets/OrderPanel/ProductGrid.tsx"
      provides: "addItem now passes basePrice (server is sole price authority)"
      contains: "basePrice"
    - path: "src/shared/lib/domain-helpers.ts"
      provides: "isPromotionActive cosmetic helper"
      exports: ["isPromotionActive"]
    - path: "src/entities/promotion/model/queries.ts"
      provides: "useActivePromotions hook feeding the banner"
      exports: ["useActivePromotions"]
    - path: "src/widgets/OrderPanel/HappyHourBanner.tsx"
      provides: "Active Promotions banner (data-testid active-promotions-banner)"
      contains: "active-promotions-banner"
  key_links:
    - from: "ProductGrid.addItem"
      to: "create_order_with_items unit_price"
      via: "passes product.basePrice (undiscounted); server overwrites with discounted price"
      pattern: "addItem\\("
    - from: "HappyHourBanner"
      to: "useActivePromotions"
      via: "renders currently-active promotion names + countdown"
      pattern: "useActivePromotions"
---

<objective>
Complete the client half of moving pricing authority to the server (Pitfall 1): stop sending a client-computed discounted `unit_price` — send `product.basePrice` instead and let `evaluate_promotions_for_item` write the charged price. Repurpose the existing `HappyHourBanner` into a generic, cosmetic "Active Promotions" banner backed by the new promotions data, fixing its always-available countdown bug.

Purpose: Eliminate the double-discount / silent-override risk (RESEARCH Pitfall 1) now that the server evaluates promotions live (post Plan 20-06 push), while preserving a read-only "promotions active" affordance for cashiers.
Output: ProductGrid RPC-payload rewire + a cosmetic `isPromotionActive` helper + a `useActivePromotions` hook + the banner repurpose (with the e2e testid rename). Pool billing's client rewire is Plan 20-08; retiring `resolveProductPrice` entirely + dropping columns is Plan 20-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-UI-SPEC.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Send basePrice to the RPC + cosmetic isPromotionActive helper + useActivePromotions hook</name>
  <files>src/widgets/OrderPanel/ProductGrid.tsx, src/shared/lib/domain-helpers.ts, src/shared/lib/domain-helpers.test.ts, src/entities/promotion/model/queries.ts</files>
  <read_first>
    - src/widgets/OrderPanel/ProductGrid.tsx (handleProductSelect ~131 and handleModifierConfirm ~154 — the two addItem calls currently passing resolveProductPrice; the HappyHourBanner usage ~175; imports ~13)
    - src/widgets/OrderPanel/CartPanel.tsx (handlePlaceOrder — confirms item.unitPrice flows verbatim into create_order_with_items; no change needed here)
    - src/shared/lib/domain-helpers.ts (isHappyHourActive ~266 for the day/time technique to mirror in isPromotionActive; resolveProductPrice ~58 stays until Plan 20-10)
    - src/entities/promotion/model/queries.ts (existing hooks + PromotionAvailability mapping to extend with useActivePromotions)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Pitfall 1 — warning signs; Security Domain — cosmetic-only badge)
  </read_first>
  <action>
    In `ProductGrid.tsx`, change BOTH `addItem(...)` calls to pass the undiscounted base price: `handleProductSelect` → `addItem(product, [], product.basePrice)`; `handleModifierConfirm` → `addItem(selectedProduct, selectedModifiers, selectedProduct.basePrice)`. Remove the now-unused `resolveProductPrice` import and any resulting unused `category` locals used solely for pricing (keep `catalogNow`/`getCurrentTime` only if still needed by the banner in Task 2). Add a code comment: base price is intentional — `evaluate_promotions_for_item` is the sole authority for the charged `unit_price` (Pitfall 1).

    In `domain-helpers.ts`, add `isPromotionActive(promotion: Promotion, windows: PromotionAvailability[], currentTime: Date = new Date()): boolean` — a COSMETIC/DISPLAY-ONLY pure helper mirroring `is_promotion_available` semantics (isActive false ⇒ false; empty windows ⇒ always active; otherwise match ISODOW + start/end time). Add a doc comment stating it must NEVER be used to compute a charged price (server is authoritative). Add unit tests in `domain-helpers.test.ts` for: no-windows ⇒ true; inside-window ⇒ true; outside-window ⇒ false; inactive promotion (isActive false) ⇒ false.

    In `entities/promotion/model/queries.ts`, add `useActivePromotions()` returning currently-active promotions for the banner: query `promotions` where `is_active = true`, fetch their `promotion_availability` windows, and expose a parsed `{ promotion: Promotion; windows: PromotionAvailability[] }[]`. The banner (Task 2) applies `isPromotionActive` to filter to active-now; keep `staleTime` ~30s.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/domain-helpers.test.ts && npm run typecheck && npm run lint -- src/widgets/OrderPanel/ProductGrid.tsx src/shared/lib/domain-helpers.ts src/entities/promotion/model/queries.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both `addItem` calls in ProductGrid pass `*.basePrice` (no `resolveProductPrice` in the RPC-payload path); the `resolveProductPrice` import is removed from ProductGrid and lint is clean.
    - `isPromotionActive` exists with a display-only doc comment and passes the 4 new unit tests.
    - `useActivePromotions` is exported from entities/promotion.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The cart submits undiscounted basePrice; a cosmetic promotion-active helper + active-promotions hook exist, cleanly linted and typed.</done>
</task>

<task type="auto">
  <name>Task 2: Repurpose HappyHourBanner into the Active Promotions banner</name>
  <files>src/widgets/OrderPanel/HappyHourBanner.tsx, src/widgets/OrderPanel/HappyHourBanner.test.tsx, src/widgets/OrderPanel/ProductGrid.tsx, e2e/20-sprint2-revenue.spec.ts</files>
  <read_first>
    - src/widgets/OrderPanel/HappyHourBanner.tsx (full — container/icon/interval to preserve; endMinutes Infinity bug to fix)
    - src/widgets/OrderPanel/HappyHourBanner.test.tsx (existing tests + the data-testid selector to update)
    - e2e/20-sprint2-revenue.spec.ts (references data-testid `happy-hour-banner` — update the selector)
    - .planning/phases/20-promotions-engine/20-UI-SPEC.md (Interaction Contract §4 — copy, countdown behavior, testid, cosmetic-only guarantee)
  </read_first>
  <action>
    Repurpose `HappyHourBanner.tsx` in place per UI-SPEC §4, preserving the exact container (`rounded-lg border border-amber-700 bg-amber-950 px-3 py-2 text-sm text-amber-200`, `role="status"`), the `Zap` icon, and the 1000ms `setInterval` tick. Change the prop shape from `categories: Category[]` to consume active promotions (accept the `useActivePromotions`-derived `{ promotion, windows }[]`, filtered active-now via `isPromotionActive`); render `null` when none are active. Copy: `"Promotions Active — {names}"` where `{names}` is the comma-joined active promotion names. Countdown: keep `"Ends in {countdown}"` when active promotions have a known window end time (earliest end for the current window), but FIX the bug — when an active promotion has NO time window ("always available"), OMIT the "Ends in…" suffix entirely instead of defaulting `endMinutes` to `Infinity`. Rename `data-testid="happy-hour-banner"` → `data-testid="active-promotions-banner"`. Add a comment: this banner is cosmetic/read-only — it never computes a charged price (Pitfall 1). Update `ProductGrid.tsx`'s banner usage to pass the new prop (sourced from `useActivePromotions`) instead of `categories`. Update `HappyHourBanner.test.tsx` to the new prop shape + new testid + a case asserting the "Ends in…" suffix is omitted for an always-available promotion. Update the `happy-hour-banner` selector in `e2e/20-sprint2-revenue.spec.ts` to `active-promotions-banner`.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/OrderPanel/HappyHourBanner.test.tsx && npm run typecheck && npm run lint -- src/widgets/OrderPanel/HappyHourBanner.tsx src/widgets/OrderPanel/ProductGrid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Banner renders `data-testid="active-promotions-banner"` and the copy "Promotions Active — {names}"; grep for `happy-hour-banner` across `src/` and `e2e/` returns zero matches.
    - Always-available active promotion ⇒ no "Ends in…" suffix (bug fixed); the new test case asserts this.
    - ProductGrid passes the active-promotions prop; the container/icon/1s-tick are visually unchanged.
    - Typecheck + lint + the banner test all pass.
  </acceptance_criteria>
  <done>The banner is a cosmetic Active Promotions banner backed by live promotion data, countdown bug fixed, and all old testid references updated.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- TS helper: `isPromotionActive` (`src/shared/lib/domain-helpers.ts`)
- Hook: `useActivePromotions` (`src/entities/promotion/model/queries.ts`)
- Renamed test id: `active-promotions-banner` (was `happy-hour-banner`)
- Modified components: `ProductGrid` (basePrice payload), `HappyHourBanner` (repurposed)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cart → create_order_with_items | Client sends undiscounted basePrice; server computes the charged price |
| cosmetic banner/helper → billing | isPromotionActive + banner are read-only display, never a mutation input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | client-computed discounted unit_price | mitigate | ProductGrid now submits `product.basePrice`; server evaluate_promotions_for_item is sole authority — removes the double-discount / silent-override risk (Pitfall 1) |
| T-20-08 | Tampering / misleading UI | stale "Promotions Active" badge | accept (documented) | Banner + isPromotionActive are cosmetic-only, documented in code; clock skew/cache staleness cannot change the charged price (server authoritative) |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run` for domain-helpers + banner tests; `npm run typecheck` + `npm run lint` for touched files.
- Grep confirms no residual `happy-hour-banner` testid.
- Live "cart sends base, server discounts" behavior is asserted end-to-end in the Plan 20-09 e2e/UAT gate.
</verification>

<success_criteria>
- ProductGrid submits basePrice; no client discount leaks into the RPC payload.
- Cosmetic isPromotionActive + useActivePromotions exist; banner repurposed with countdown bug fixed and testid renamed everywhere.
- Tests, typecheck, lint green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-07-SUMMARY.md` when done.
</output>
