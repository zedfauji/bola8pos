---
phase: 20-promotions-engine
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - supabase/migrations/20260710000003_applied_promotions_table.sql
  - supabase/migrations/20260710000004_evaluate_promotions_rpc.sql
  - src/shared/lib/promotion-pricing.ts
  - src/shared/lib/promotion-pricing.test.ts
  - src/entities/promotion/model/applied-promotions-rls.integration.test.ts
  - src/entities/promotion/model/evaluate-promotions-rpc.integration.test.ts
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "applied_promotions records one immutable row per applied promotion instance, readable only by manager/admin, writable by no client"
    - "evaluate_promotions_for_item is the sole writer of order_items.unit_price for promotion-eligible items — it ignores/overwrites whatever price the client sent"
    - "Multiple eligible promotions stack by compounding sequentially in priority order (percentage/fixed_amount compound; fixed_price resets the running price)"
    - "Combo parent items (is_combo=true) and combo child items (combo_slot_id IS NOT NULL) are excluded from promotion evaluation"
    - "create_order_with_items evaluates promotions per inserted item in the same transaction as depletion"
  artifacts:
    - path: "supabase/migrations/20260710000003_applied_promotions_table.sql"
      provides: "applied_promotions append-only audit table + SELECT-manager-only RLS"
      contains: "CREATE TABLE IF NOT EXISTS applied_promotions"
    - path: "supabase/migrations/20260710000004_evaluate_promotions_rpc.sql"
      provides: "evaluate_promotions_for_item SECURITY DEFINER + create_order_with_items v3 wiring"
      contains: "CREATE OR REPLACE FUNCTION evaluate_promotions_for_item"
    - path: "src/shared/lib/promotion-pricing.ts"
      provides: "applyPromotionStack pure fn mirroring the SQL compounding (cosmetic/preview + property test only, NOT billing authority)"
      exports: ["applyPromotionStack"]
    - path: "src/shared/lib/promotion-pricing.test.ts"
      provides: "P11 fast-check property test for sequential-compounding math"
  key_links:
    - from: "create_order_with_items"
      to: "evaluate_promotions_for_item"
      via: "PERFORM in the per-item loop next to deplete_for_order_item"
      pattern: "PERFORM evaluate_promotions_for_item"
    - from: "evaluate_promotions_for_item"
      to: "order_items.unit_price"
      via: "UPDATE ... SET unit_price = v_running_price"
      pattern: "UPDATE order_items SET unit_price"
    - from: "evaluate_promotions_for_item"
      to: "applied_promotions"
      via: "INSERT per applied promotion (SECURITY DEFINER, bypasses RLS)"
      pattern: "INSERT INTO applied_promotions"
---

<objective>
Ship the SC-2 + SC-3 core: the immutable `applied_promotions` audit table, the `evaluate_promotions_for_item` SECURITY DEFINER function (sequential compounding, combo exclusion, audit insert), and its wiring into a new `create_order_with_items` v3 so promotions auto-apply atomically at item-add time. This is the first server-side pricing authority in the codebase — from here, the server (not the client) writes the charged `unit_price` for promotion-eligible items.

Purpose: Move pricing authority server-side (RESEARCH Summary + Pitfall 1) using the proven `create_order_with_items` per-item PERFORM loop and the `tip_distribution_entries` immutable-audit pattern.
Output: Two additive migrations + a pure-fn/property-test pair + two live-integration scaffolds. Handles item/category price discounts only; pool_billing (D-05a) and pool_grant (D-05b) branches are added in Plan 20-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
@.planning/phases/20-promotions-engine/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: applied_promotions append-only audit table + RLS</name>
  <files>supabase/migrations/20260710000003_applied_promotions_table.sql</files>
  <read_first>
    - supabase/migrations/20260709000001_tip_distribution_entries_table.sql (immutable audit table + SELECT-manager-only RLS + zero write policies)
    - src/shared/lib/domain.ts (AppliedPromotionSchema fields authored in Plan 20-02 — column names must match its snake_case mapping)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (applied_promotions_table section)
  </read_first>
  <action>
    Create migration `20260710000003_applied_promotions_table.sql`. Table `applied_promotions` columns: `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `promotion_id uuid REFERENCES promotions(id) ON DELETE SET NULL` (per RESEARCH Open Question 4 — audit survives promotion deletion), `promotion_name_snapshot text NOT NULL`, `target_type text NOT NULL`, `discount_type text`, `discount_value numeric`, `tab_id uuid REFERENCES tabs(id) ON DELETE CASCADE`, `order_item_id uuid REFERENCES order_items(id) ON DELETE CASCADE`, `pool_session_id uuid REFERENCES pool_sessions(id) ON DELETE CASCADE`, `original_amount numeric`, `discounted_amount numeric`, `pool_minutes_granted integer`, `consumed_at timestamptz`, `created_at timestamptz NOT NULL DEFAULT now()`. (The `pool_session_id`, `pool_minutes_granted`, `consumed_at` columns are present now but only written by Plan 20-05's pool paths.) Do NOT add a UNIQUE constraint (many rows per tab/order allowed). Add indexes on `order_item_id`, `tab_id`, `pool_session_id`, and a partial index on `(tab_id) WHERE consumed_at IS NULL AND pool_minutes_granted IS NOT NULL` for the Plan-20-05 grant-consumption lookup.

    Enable RLS. Add ONE policy: `applied_promotions_select_manager` `FOR SELECT TO authenticated USING (get_user_role() IN ('manager','admin'))`. Add NO INSERT/UPDATE/DELETE policies (append-only by omission; the SECURITY DEFINER `evaluate_promotions_for_item` / pool RPCs are the sole writers and bypass RLS). BEGIN/COMMIT wrap.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260710000003_applied_promotions_table.sql | grep -q "CREATE TABLE IF NOT EXISTS applied_promotions" && grep -v '^--' supabase/migrations/20260710000003_applied_promotions_table.sql | grep -c "CREATE POLICY" | grep -qx 1 && grep -v '^--' supabase/migrations/20260710000003_applied_promotions_table.sql | grep -q "ON DELETE SET NULL" && ! grep -v '^--' supabase/migrations/20260710000003_applied_promotions_table.sql | grep -qi "for insert\|for update\|for delete"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 `CREATE POLICY` (SELECT, manager/admin); zero INSERT/UPDATE/DELETE policies.
    - `promotion_id` FK is `ON DELETE SET NULL`; `promotion_name_snapshot text NOT NULL` present.
    - `pool_session_id`, `pool_minutes_granted`, `consumed_at` columns exist.
    - No UNIQUE constraint on the table.
  </acceptance_criteria>
  <done>applied_promotions is an append-only, manager-readable, client-unwritable audit table with all columns the item and pool paths need.</done>
</task>

<task type="auto">
  <name>Task 2: evaluate_promotions_for_item + create_order_with_items v3</name>
  <files>supabase/migrations/20260710000004_evaluate_promotions_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260512000002_rpc_versioned_group_a.sql (CURRENT create_order_with_items body — the authoritative base for v3, incl. p_expected_version FOR UPDATE guard and the depletion PERFORM loop; lines ~273-357)
    - supabase/migrations/20260428000003_create_order_with_items_v2.sql (original depletion-loop shape for reference)
    - supabase/migrations/20260511000001_audit_logs_table.sql AND supabase/migrations/20260703000001_record_audit_terminal_id.sql (confirm record_audit's CURRENT 7-arg signature incl. p_terminal_id)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Code Examples → sequential compounding pseudocode; Pitfall 6 combo exclusion; Security Domain GREATEST(0,...) clamp)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (evaluate_promotions_rpc section — analog 1 hook point + analog 2 compounding)
  </read_first>
  <action>
    Create migration `20260710000004_evaluate_promotions_rpc.sql` with two definitions.

    This function implements D-02 (silent auto-apply, no cashier confirmation), D-03 (stacking via sequential compounding in priority order), and D-04 (the three discount shapes).

    (a) `evaluate_promotions_for_item(p_order_item_id uuid) RETURNS void LANGUAGE plpgsql SECURITY DEFINER`. Body:
    - Resolve `v_product_id`, `v_category_id` (join order_items → products → products.category_id), `v_base_price` (order_items.unit_price as the undiscounted input), `v_tab_id` (order_items → orders.tab_id), plus `v_is_combo` (products.is_combo) and `v_combo_slot_id` (order_items.combo_slot_id).
    - Pitfall 6 EARLY RETURN: `IF v_is_combo IS TRUE OR v_combo_slot_id IS NOT NULL THEN RETURN; END IF;` (skip combo parent + child lines — no promotion evaluation, no $0 audit noise).
    - Set `v_running_price := v_base_price`. Loop `FOR v_promo IN SELECT * FROM promotions p WHERE p.is_active AND ((p.target_type = 'item' AND p.target_product_id = v_product_id) OR (p.target_type = 'category' AND p.target_category_id = v_category_id)) AND is_promotion_available(p.id, now()) ORDER BY p.priority ASC, p.created_at ASC, p.id ASC`. (Only `item`/`category` target types here; `pool_billing`/`pool_grant` are intentionally not matched — Plan 20-05 handles them.)
    - In-loop: `v_original := v_running_price;` then CASE on `discount_type`: `'percentage'` → `v_running_price := GREATEST(0, ROUND(v_running_price * (1 - v_promo.discount_value / 100), 2))`; `'fixed_amount'` → `v_running_price := GREATEST(0, ROUND(v_running_price - v_promo.discount_value, 2))`; `'fixed_price'` → `v_running_price := v_promo.discount_value` (resets, does not compound). ROUND at every step (money drift guard).
    - Per applied promo, `INSERT INTO applied_promotions (promotion_id, promotion_name_snapshot, target_type, discount_type, discount_value, tab_id, order_item_id, original_amount, discounted_amount) VALUES (v_promo.id, v_promo.name, v_promo.target_type, v_promo.discount_type, v_promo.discount_value, v_tab_id, p_order_item_id, v_original, v_running_price)`.
    - After the loop, only if at least one promotion applied: `UPDATE order_items SET unit_price = v_running_price WHERE id = p_order_item_id`. Then best-effort `PERFORM record_audit('promotion.apply', 'applied_promotion', p_order_item_id, NULL, jsonb_build_object('orderItemId', p_order_item_id, 'originalAmount', v_base_price, 'discountedAmount', v_running_price), 'rpc', NULL)` using the confirmed current record_audit signature (7 args; pass NULL for terminal). `GRANT EXECUTE ON FUNCTION evaluate_promotions_for_item(uuid) TO authenticated`.

    (b) `create_order_with_items` v3: `CREATE OR REPLACE FUNCTION public.create_order_with_items(...)` copying the CURRENT body from `20260512000002_rpc_versioned_group_a.sql` VERBATIM (same signature `(uuid, uuid, order_status, text, jsonb, boolean, int)`, SECURITY INVOKER, the `p_expected_version` FOR UPDATE guard, the depletion loop). Add exactly one line inside the existing `FOR v_inserted_item IN SELECT id FROM order_items WHERE order_id = v_order.id LOOP` block, immediately AFTER the `PERFORM deplete_for_order_item(v_inserted_item.id, 1::smallint);` line: `PERFORM evaluate_promotions_for_item(v_inserted_item.id);`. Do not change the signature, the version guard, or the return payload. Keep the existing `GRANT EXECUTE ... (uuid, uuid, order_status, text, jsonb, boolean, int) TO authenticated`. Add a header comment noting v3 supersedes the v2/Phase-15 body and that the return payload's `unit_price` now reflects the post-evaluation discounted price.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260710000004_evaluate_promotions_rpc.sql | grep -q "CREATE OR REPLACE FUNCTION evaluate_promotions_for_item" && grep -v '^--' supabase/migrations/20260710000004_evaluate_promotions_rpc.sql | grep -q "PERFORM evaluate_promotions_for_item" && grep -v '^--' supabase/migrations/20260710000004_evaluate_promotions_rpc.sql | grep -q "v_is_combo IS TRUE OR v_combo_slot_id IS NOT NULL" && grep -v '^--' supabase/migrations/20260710000004_evaluate_promotions_rpc.sql | grep -q "GREATEST(0" && grep -v '^--' supabase/migrations/20260710000004_evaluate_promotions_rpc.sql | grep -q "UPDATE order_items SET unit_price"</automated>
  </verify>
  <acceptance_criteria>
    - `evaluate_promotions_for_item(p_order_item_id uuid)` is `SECURITY DEFINER` and its WHERE clause matches only `target_type IN ('item','category')`.
    - Combo exclusion early-return present (`v_is_combo IS TRUE OR v_combo_slot_id IS NOT NULL`).
    - Compounding CASE clamps with `GREATEST(0, ...)`, rounds each step, and `fixed_price` assigns `discount_value` outright.
    - `create_order_with_items` v3 preserves the 7-arg signature + `p_expected_version` guard and adds exactly one `PERFORM evaluate_promotions_for_item(...)` inside the existing item loop.
    - `unit_price` is updated only when ≥1 promotion applied.
  </acceptance_criteria>
  <done>evaluate_promotions_for_item performs item/category sequential compounding with combo exclusion and audit rows; create_order_with_items v3 invokes it per item in-transaction; server is now sole authority for promotion-eligible unit_price.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: promotion-pricing pure fn + P11 property test + integration scaffolds</name>
  <files>src/shared/lib/promotion-pricing.ts, src/shared/lib/promotion-pricing.test.ts, src/entities/promotion/model/applied-promotions-rls.integration.test.ts, src/entities/promotion/model/evaluate-promotions-rpc.integration.test.ts</files>
  <read_first>
    - src/features/add-combo-to-tab/pricing.test.ts (P2 property-test style — fast-check import order, Math.fround for float bounds)
    - src/shared/lib/domain-helpers.ts (calculateDiscountAmount rounding technique `Math.round(x*100)/100` — reuse the TECHNIQUE only, not the DiscountType fn; Pitfall 5)
    - src/entities/audit-log/model/rls-denial.integration.test.ts (RLS-denial integration scaffold pattern for applied_promotions)
    - .planning/phases/20-promotions-engine/20-VALIDATION.md (task 20-03-01 row — P11 + live RPC integration)
  </read_first>
  <behavior>
    - applyPromotionStack(basePrice, []) === basePrice.
    - A single 'percentage' 20 promo on 100 → 80; a 'fixed_amount' 10 on 80 → 70; a 'fixed_price' 5 resets to 5 regardless of running price.
    - Property (P11): for any base ≥ 0 and any ordering WITHIN the same priority group of commutative ops (percentage/fixed_amount only, no fixed_price), the final price is identical (order-independence within a priority group), and the result is always ≥ 0 and rounded to 2 decimals.
    - applyPromotionStack never returns a negative number (GREATEST(0,...) parity with SQL).
  </behavior>
  <action>
    Create `src/shared/lib/promotion-pricing.ts` exporting `applyPromotionStack(basePrice: number, promos: { discountType: PromotionDiscountType; discountValue: number }[]): number` — a pure function that mirrors the SQL compounding EXACTLY (same CASE semantics, same per-step `Math.round(x*100)/100`, same `Math.max(0, ...)` clamp, `fixed_price` resets). Add a file-level doc comment stating this is the CLIENT-SIDE COSMETIC/PREVIEW + property-test mirror ONLY — `evaluate_promotions_for_item` (SQL) is the sole billing authority; the Plan 20-09 parity test guards against drift.

    Create `src/shared/lib/promotion-pricing.test.ts` with the unit cases + the P11 fast-check property (use `Math.fround` for float bounds per fast-check v4; fast-check import before vitest per ESLint import/order).

    Create `src/entities/promotion/model/applied-promotions-rls.integration.test.ts` (env-guarded, mirrors audit-log/rls-denial): assert a non-manager client cannot SELECT applied_promotions and no client can INSERT/UPDATE/DELETE it. Create `src/entities/promotion/model/evaluate-promotions-rpc.integration.test.ts` (env-guarded): create an item/category promotion, call `create_order_with_items` with an inflated client `unit_price`, and assert the returned `order_items.unit_price` equals the server-evaluated discounted price (client value ignored) and an `applied_promotions` row exists. Both integration files skip without E2E env.
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/promotion-pricing.test.ts src/entities/promotion/model/applied-promotions-rls.integration.test.ts src/entities/promotion/model/evaluate-promotions-rpc.integration.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `promotion-pricing.test.ts` passes including the P11 property (≥ 200 runs) and all unit cases.
    - `applyPromotionStack` clamps to ≥ 0 and never reuses `DiscountType`/`calculateDiscountAmount`.
    - Both integration scaffolds run green (skip) without E2E env; the evaluate scaffold asserts server-price-wins over an inflated client `unit_price`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Pure compounding mirror + P11 property test are green; RLS-denial and evaluate-RPC integration scaffolds exist and pass (skip) pre-push.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- DB table: `applied_promotions`; policy `applied_promotions_select_manager`; indexes on order_item_id/tab_id/pool_session_id + partial grant index
- DB functions: `evaluate_promotions_for_item(uuid)`; `create_order_with_items` v3 (same signature, new body)
- TS: `applyPromotionStack` (`src/shared/lib/promotion-pricing.ts`)
- Files: the two migrations, `promotion-pricing.ts`/`.test.ts`, `applied-promotions-rls.integration.test.ts`, `evaluate-promotions-rpc.integration.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → create_order_with_items | Client submits `unit_price`; server treats it as an undiscounted input and overwrites it for promotion-eligible items |
| SECURITY DEFINER RPC → applied_promotions | Only the definer function writes audit rows; clients cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | client-supplied unit_price | mitigate | evaluate_promotions_for_item overwrites `order_items.unit_price` for eligible items — the sole authority; integration test asserts an inflated client price is ignored |
| T-20-02 | Repudiation / Tampering | applied_promotions audit rows | mitigate | Append-only RLS (SELECT manager+ only, no client write policies); rows written only inside the SECURITY DEFINER function in the same transaction |
| T-20-03b | Denial of correct billing | negative/over-cap discount stacking | mitigate | `GREATEST(0, ...)` clamp + per-step ROUND in SQL; `applyPromotionStack` mirror + P11 property test guard the math |
| T-20-03c | Tampering | promotion disabled mid-transaction race | accept | Covered by existing Phase 15 `p_expected_version` FOR UPDATE guard already in create_order_with_items; evaluation runs inside that guarded transaction |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages; fast-check already present; slopcheck N/A |
</threat_model>

<verification>
- `grep` gates confirm migration structure (comment-filtered).
- `npx vitest run` for the pure-fn/property + integration scaffolds; `npm run typecheck` green.
- Live behavior (server-price-wins, audit rows, combo exclusion) verified post-push in Plan 20-09.
</verification>

<success_criteria>
- applied_promotions append-only table + evaluate_promotions_for_item + create_order_with_items v3 authored.
- Combo lines excluded; compounding + clamp + fixed_price reset correct.
- P11 property test + integration scaffolds green (skip pre-push).
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-03-SUMMARY.md` when done.
</output>
