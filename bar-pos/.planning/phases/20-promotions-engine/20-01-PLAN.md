---
phase: 20-promotions-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260710000001_promotions_schema.sql
  - supabase/migrations/20260710000002_is_promotion_available_fn.sql
  - src/entities/promotion/model/promotions-schema.integration.test.ts
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "A promotions table exists with percentage/fixed_amount/fixed_price discount shapes and item/category/pool_billing/pool_grant targeting"
    - "A promotion_availability table stores HH-style day-of-week + time-window rows keyed to a promotion"
    - "is_promotion_available(promotion_id, ts) returns true when a promotion has no windows, and matches ISODOW + time window otherwise"
    - "Only manage_products-permitted roles can write promotions/promotion_availability; all authenticated roles can read"
  artifacts:
    - path: "supabase/migrations/20260710000001_promotions_schema.sql"
      provides: "promotions + promotion_availability tables, CHECK constraints, per-verb role_permissions RLS, FK indexes"
      contains: "CREATE TABLE IF NOT EXISTS promotions"
    - path: "supabase/migrations/20260710000002_is_promotion_available_fn.sql"
      provides: "is_promotion_available STABLE SECURITY DEFINER evaluator"
      contains: "CREATE OR REPLACE FUNCTION is_promotion_available"
    - path: "src/entities/promotion/model/promotions-schema.integration.test.ts"
      provides: "Live-Supabase schema + RLS + availability integration scaffold (skips without E2E env)"
  key_links:
    - from: "is_promotion_available"
      to: "promotion_availability"
      via: "COUNT(*) window match against days_of_week / start_time / end_time"
      pattern: "promotion_availability WHERE promotion_id"
    - from: "promotions RLS insert/update/delete"
      to: "role_permissions"
      via: "EXISTS role_permissions action = 'manage_products'"
      pattern: "role_permissions WHERE role = get_user_role\\(\\) AND action = 'manage_products'"
---

<objective>
Create the foundational promotions schema: the `promotions` table (three discount shapes, four target types, priority, is_active), the `promotion_availability` table (HH-style day/time windows), the `is_promotion_available()` evaluator, and current-pattern per-verb RLS. This is the SC-1 backbone every later plan builds on.

Purpose: Establish the server-side source of truth for promotion rules and their time-gating, cloned from the proven `combo_slots`/`combo_availability`/`is_combo_available` pattern but using the CURRENT `role_permissions` RLS pattern (not the deprecated combo-era `auth.jwt()` style).
Output: Two additive migration files + one live-integration test scaffold. No data is migrated and no columns are dropped in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
@.planning/phases/20-promotions-engine/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: promotions + promotion_availability tables + per-verb RLS</name>
  <files>supabase/migrations/20260710000001_promotions_schema.sql</files>
  <read_first>
    - supabase/migrations/20260425000001_combo_schema.sql (combo_slots/combo_availability table shape + CHECK style; DO NOT copy its deprecated inline auth.jwt() RLS)
    - supabase/migrations/20260510000001_rls_rewrite_phase13.sql (CURRENT per-verb role_permissions RLS pattern to replicate)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (Pattern Assignments → promotions_schema section; Shared Patterns → RLS)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Code Examples → "Current RLS pattern"; Pitfall 5 naming warning)
  </read_first>
  <action>
    Create migration `20260710000001_promotions_schema.sql` defining two tables.

    Table `promotions` columns: `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `name text NOT NULL`, `discount_type text NOT NULL CHECK (discount_type IN ('percentage','fixed_amount','fixed_price'))` (the three discount shapes per D-04), `discount_value numeric NOT NULL CHECK (discount_value >= 0)`, `target_type text NOT NULL CHECK (target_type IN ('item','category','pool_billing','pool_grant'))` (item/category + both pool-time meanings per D-05), `target_product_id uuid REFERENCES products(id) ON DELETE CASCADE`, `target_category_id uuid REFERENCES categories(id) ON DELETE CASCADE`, `priority integer NOT NULL DEFAULT 0`, `is_active boolean NOT NULL DEFAULT true`, `created_at timestamptz NOT NULL DEFAULT now()`. Add two additional CHECK constraints: `CHECK (discount_type <> 'percentage' OR discount_value <= 100)` (percentage bound, per Security Domain negative/over-100 threat) and `CHECK (target_type <> 'item' OR target_product_id IS NOT NULL)` plus `CHECK (target_type <> 'category' OR target_category_id IS NOT NULL)` (target-consistency). Use `is_active boolean NOT NULL DEFAULT true` (not a status enum) per D-03 discretion / RESEARCH recommendation.

    Table `promotion_availability`: clone `combo_availability` column-for-column, swapping the owner FK to `promotion_id uuid NOT NULL REFERENCES promotions(id) ON DELETE CASCADE`. Keep `days_of_week integer[] NOT NULL DEFAULT '{1,2,3,4,5,6,7}'`, `start_time time`, `end_time time`, `start_date date`, `end_date date`, `created_at timestamptz NOT NULL DEFAULT now()`.

    Add FK indexes: `idx_promotions_target_product` on `target_product_id`, `idx_promotions_target_category` on `target_category_id`, `idx_promotion_availability_promotion` on `promotion_id`, and a partial index `idx_promotions_active` on `(is_active) WHERE is_active` for the evaluator hot path.

    Enable RLS on BOTH tables. Write SEPARATE per-verb policies (NEVER a combined `FOR ALL`) using the CURRENT pattern: SELECT `TO authenticated USING (true)`; INSERT/UPDATE/DELETE gated by `EXISTS(SELECT 1 FROM role_permissions WHERE role = get_user_role() AND action = 'manage_products')` (reuse the existing `manage_products` action per RESEARCH Assumption A1 — Combos gate on the same action; no new role_permissions seed rows). Name policies `promotions_select_authenticated`, `promotions_insert_manager_admin`, `promotions_update_manager_admin`, `promotions_delete_manager_admin`, and the four equivalents prefixed `promotion_availability_`. Wrap the whole migration in BEGIN/COMMIT.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260710000001_promotions_schema.sql | grep -c "CREATE POLICY" | grep -qx 8 && grep -v '^--' supabase/migrations/20260710000001_promotions_schema.sql | grep -q "discount_type IN ('percentage','fixed_amount','fixed_price')" && grep -v '^--' supabase/migrations/20260710000001_promotions_schema.sql | grep -q "target_type IN ('item','category','pool_billing','pool_grant')" && ! grep -v '^--' supabase/migrations/20260710000001_promotions_schema.sql | grep -qi "auth.jwt"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `CREATE TABLE IF NOT EXISTS promotions` and `CREATE TABLE IF NOT EXISTS promotion_availability`.
    - Exactly 8 `CREATE POLICY` statements (4 per table), all per-verb; zero `FOR ALL` policies; zero occurrences of `auth.jwt`.
    - `discount_value >= 0` CHECK present; `discount_type <> 'percentage' OR discount_value <= 100` CHECK present.
    - Every INSERT/UPDATE/DELETE policy references `role_permissions ... action = 'manage_products'`.
    - `promotion_availability.promotion_id` FK is `ON DELETE CASCADE`.
  </acceptance_criteria>
  <done>Both tables, all CHECK constraints, indexes, and 8 per-verb RLS policies exist in one additive BEGIN/COMMIT migration; no destructive statements.</done>
</task>

<task type="auto">
  <name>Task 2: is_promotion_available() evaluator function</name>
  <files>supabase/migrations/20260710000002_is_promotion_available_fn.sql</files>
  <read_first>
    - supabase/migrations/20260425000003_combo_triggers.sql (is_combo_available function body to clone near-verbatim)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (is_promotion_available_fn section — exact clone target)
  </read_first>
  <action>
    Create migration `20260710000002_is_promotion_available_fn.sql` defining `is_promotion_available(p_promotion_id uuid, p_ts timestamptz) RETURNS boolean` as `LANGUAGE plpgsql STABLE SECURITY DEFINER`. Clone `is_combo_available`'s body exactly, swapping table `combo_availability` → `promotion_availability` and column `combo_product_id` → `promotion_id`. Preserve: the zero-window short-circuit (`IF v_row_count = 0 THEN RETURN true`), the hardcoded `AT TIME ZONE 'America/Mexico_City'` for ISODOW/time/date extraction (Assumption A2 — matches 100% of existing time-window code), and the `days_of_week` ANY-match + nullable start/end time/date comparisons. `GRANT EXECUTE ON FUNCTION is_promotion_available(uuid, timestamptz) TO authenticated`.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260710000002_is_promotion_available_fn.sql | grep -q "CREATE OR REPLACE FUNCTION is_promotion_available" && grep -v '^--' supabase/migrations/20260710000002_is_promotion_available_fn.sql | grep -q "America/Mexico_City" && grep -v '^--' supabase/migrations/20260710000002_is_promotion_available_fn.sql | grep -q "STABLE SECURITY DEFINER" && grep -v '^--' supabase/migrations/20260710000002_is_promotion_available_fn.sql | grep -q "RETURN true"</automated>
  </verify>
  <acceptance_criteria>
    - Function signature is `is_promotion_available(p_promotion_id uuid, p_ts timestamptz) RETURNS boolean`.
    - Declared `STABLE SECURITY DEFINER`.
    - Contains the zero-window `RETURN true` short-circuit and the `America/Mexico_City` timezone conversion.
    - Queries `promotion_availability` by `promotion_id` only (no reference to `combo_availability`).
    - `GRANT EXECUTE ... TO authenticated` present.
  </acceptance_criteria>
  <done>`is_promotion_available` exists as a STABLE SECURITY DEFINER function mirroring `is_combo_available`, over `promotion_availability`.</done>
</task>

<task type="auto">
  <name>Task 3: Live schema/RLS/availability integration test scaffold (Wave 0)</name>
  <files>src/entities/promotion/model/promotions-schema.integration.test.ts</files>
  <read_first>
    - src/entities/audit-log/model/rls-denial.integration.test.ts (env-guarded describe.skipIf pattern, service-role vs anon client setup, RLS-denial assertions)
    - src/entities/caja/model/tip-distribution-rpc.integration.test.ts (live-Supabase RPC integration scaffold structure)
    - .planning/phases/20-promotions-engine/20-VALIDATION.md (task 20-01-01 row — SC-1 secure behavior)
  </read_first>
  <action>
    Create `src/entities/promotion/model/promotions-schema.integration.test.ts` mirroring the env-guarded style of `rls-denial.integration.test.ts` (`describe.skipIf(!hasE2eEnv)` so it skips cleanly when E2E credentials are absent, runs live after the Plan 20-06 db push). Use a file-level `/* eslint-disable */` + `const db = supabase as any` cast because `promotions`/`promotion_availability` are not yet in `supabase.types.ts`. Author at minimum these live cases (they will be RED/skipped until the push lands): (1) inserting a promotion with `discount_value = -1` is rejected by the CHECK constraint; (2) a `percentage` promotion with `discount_value = 150` is rejected; (3) `is_promotion_available(id, now())` returns `true` for a promotion with zero availability rows; (4) `is_promotion_available` returns `false` for a time outside a configured window and `true` inside it; (5) an anon/bartender client INSERT into `promotions` is denied by RLS while SELECT succeeds. Keep the file self-contained; do not import from `entities/promotion/model/queries.ts` (created in Plan 20-02) — talk to Supabase directly.
  </action>
  <verify>
    <automated>npx vitest run src/entities/promotion/model/promotions-schema.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/entities/promotion/model/promotions-schema.integration.test.ts` exits 0 (all cases skip when E2E env is absent — no failures, no errors).
    - File uses `describe.skipIf` and does not import `queries.ts`.
    - Cases for negative discount, percentage > 100, zero-window availability, in/out-of-window, and RLS insert-denial are present.
  </acceptance_criteria>
  <done>An env-guarded integration scaffold exists that will exercise the schema, CHECK constraints, availability evaluator, and RLS once the schema is live; it passes (skips) today.</done>
</task>

</tasks>

<artifacts_produced>
New symbols/objects this plan creates (exclude from source-drift verification):
- DB tables: `promotions`, `promotion_availability`
- DB function: `is_promotion_available(uuid, timestamptz)`
- DB policies: `promotions_select_authenticated`, `promotions_insert_manager_admin`, `promotions_update_manager_admin`, `promotions_delete_manager_admin`, `promotion_availability_select_authenticated`, `promotion_availability_insert_manager_admin`, `promotion_availability_update_manager_admin`, `promotion_availability_delete_manager_admin`
- DB indexes: `idx_promotions_target_product`, `idx_promotions_target_category`, `idx_promotion_availability_promotion`, `idx_promotions_active`
- Files: `supabase/migrations/20260710000001_promotions_schema.sql`, `supabase/migrations/20260710000002_is_promotion_available_fn.sql`, `src/entities/promotion/model/promotions-schema.integration.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → promotions/promotion_availability | Any signed-in terminal can attempt reads/writes; only manager/admin (via `manage_products`) may write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Elevation of Privilege | promotions/promotion_availability write policies | mitigate | Per-verb INSERT/UPDATE/DELETE policies require `EXISTS(role_permissions … action='manage_products')`; SELECT open to authenticated only |
| T-20-01b | Tampering | discount_value input | mitigate | DB CHECK `discount_value >= 0` and `percentage ⇒ ≤ 100` reject negative/over-cap values regardless of client validation |
| T-20-01c | Tampering | availability window evaluation | mitigate | `is_promotion_available` is STABLE SECURITY DEFINER, hardcoded `America/Mexico_City`, single source of truth for time-gating |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (RESEARCH Package Legitimacy Audit: zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `grep` gates in each task confirm structural constraints (comment-filtered).
- Integration scaffold passes (skips) under `npx vitest run`.
- Full live verification of these objects is deferred to Plan 20-06's blocking `supabase db push` and the Plan 20-09 verification gate.
</verification>

<success_criteria>
- Two additive migrations authored: `promotions`/`promotion_availability` schema + `is_promotion_available`.
- 8 per-verb RLS policies, zero `FOR ALL`, zero `auth.jwt`.
- CHECK constraints enforce discount shapes, target types, and value bounds.
- Integration scaffold exists and passes (skips) without E2E env.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-01-SUMMARY.md` when done.
</output>
