---
phase: 20-promotions-engine
plan: 10
type: execute
wave: 7
depends_on: ["20-09"]
files_modified:
  - src/entities/category/model/queries.ts
  - src/entities/product/model/queries.ts
  - src/features/manage-products/ui/CategoryForm.tsx
  - src/features/manage-products/ui/ProductForm.tsx
  - src/features/manage-products/ui/CatalogCategoriesTab.tsx
  - src/features/manage-categories/ui/CategoryTreeEditor.tsx
  - src/shared/lib/domain.ts
  - supabase/migrations/20260711000001_drop_happy_hour_columns.sql
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-1]
must_haves:
  truths:
    - "No code writes or reads categories.happy_hour_start/end or products.happy_hour_price from the database anymore"
    - "Admins can no longer edit happy-hour fields in the Category/Product forms (superseded by the Promotions admin)"
    - "The legacy happy_hour_start/end and happy_hour_price DB columns are dropped after the parity gate passed"
  artifacts:
    - path: "supabase/migrations/20260711000001_drop_happy_hour_columns.sql"
      provides: "destructive drop of the three legacy HH columns (separate migration, post-verification)"
      contains: "DROP COLUMN"
    - path: "src/entities/category/model/queries.ts"
      provides: "category mapping no longer selects happy_hour columns"
    - path: "src/features/manage-products/ui/ProductForm.tsx"
      provides: "no happy-hour price input"
  key_links:
    - from: "drop_happy_hour_columns migration"
      to: "categories/products schema"
      via: "ALTER TABLE ... DROP COLUMN after Plan 20-09 parity gate"
      pattern: "DROP COLUMN"
---

<objective>
Retire the legacy happy-hour storage now that parity is proven (Plan 20-09): stop all DB reads/writes of the `happy_hour_*` columns, remove HH editing from the Category/Product admin forms (superseded by the Promotions admin per D-01), then drop the columns in a separate destructive migration behind a BLOCKING push. Per Pitfall 4, this runs ONLY after the Plan 20-09 UAT gate confirmed the server engine reproduces every legacy price.

Purpose: Complete the D-01 supersession on the storage + admin side without a fixture cascade — the pure client calc-path removal is Plan 20-11.
Output: DB-typed consumers neutralized + HH admin inputs removed + the destructive column-drop migration/push. The camelCase Zod fields (`happyHourStart`/`happyHourEnd`/`happyHourPrice`) are marked `@deprecated` and always resolve to `null` (kept vestigial to bound blast radius; full removal is deferred housekeeping). `autonomous: false` (blocking push).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutralize DB-typed HH consumers + remove HH admin editing</name>
  <files>src/entities/category/model/queries.ts, src/entities/product/model/queries.ts, src/features/manage-products/ui/CategoryForm.tsx, src/features/manage-products/ui/ProductForm.tsx, src/features/manage-products/ui/CatalogCategoriesTab.tsx, src/features/manage-categories/ui/CategoryTreeEditor.tsx, src/shared/lib/domain.ts</files>
  <read_first>
    - src/entities/category/model/queries.ts (where happy_hour_start/end are selected + mapped into CategorySchema)
    - src/entities/product/model/queries.ts (where happy_hour_price is selected + mapped into ProductSchema, and any create/update payload writing it)
    - src/features/manage-products/ui/CategoryForm.tsx and ProductForm.tsx (the HH input fields + their write payloads)
    - src/features/manage-products/ui/CatalogCategoriesTab.tsx and src/features/manage-categories/ui/CategoryTreeEditor.tsx (any HH column display/edit)
    - src/shared/lib/domain.ts (CategorySchema.happyHourStart/End ~184-185, ProductSchema.happyHourPrice ~231 — mark @deprecated, keep nullable)
    - .planning/phases/20-promotions-engine/20-CONTEXT.md (D-01 — client HH path retired/replaced by promotions)
  </read_first>
  <action>
    Stop all DB dependency on the HH columns while they still physically exist (so typecheck stays green across the subsequent drop):
    - `entities/category/model/queries.ts`: remove `happy_hour_start`/`happy_hour_end` from the SELECT and from the row→CategorySchema mapping; supply `happyHourStart: null, happyHourEnd: null` when constructing the parsed object (fields kept nullable).
    - `entities/product/model/queries.ts`: remove `happy_hour_price` from the SELECT and mapping; supply `happyHourPrice: null`; remove `happy_hour_price` from any INSERT/UPDATE payload.
    - `CategoryForm.tsx` / `ProductForm.tsx`: remove the happy-hour input fields (start/end for category, price for product) and any state/handlers writing them; do NOT include `happy_hour_*` in the submit payload. Add a short comment: happy-hour pricing is now managed in Settings → Promotions (D-01).
    - `CatalogCategoriesTab.tsx` / `CategoryTreeEditor.tsx`: remove any HH column display/edit affordance.
    - `domain.ts`: annotate `happyHourStart`/`happyHourEnd`/`happyHourPrice` with a `@deprecated` doc comment ("superseded by the promotions engine — always null; vestigial, pending housekeeping removal"). Keep them `.nullable()` so fixtures/stories/tests still compile.
    Run typecheck — it must be green while the columns still exist (no code should reference them now except the vestigial nullable Zod fields).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint -- src/entities/category/model/queries.ts src/entities/product/model/queries.ts src/features/manage-products/ui/CategoryForm.tsx src/features/manage-products/ui/ProductForm.tsx && ! grep -rn "happy_hour" src/entities/category/model/queries.ts src/entities/product/model/queries.ts src/features/manage-products/ui/CategoryForm.tsx src/features/manage-products/ui/ProductForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No `happy_hour` snake_case reference remains in the category/product queries or the Category/Product forms.
    - The forms no longer render HH inputs or write HH fields; category/product mappings supply `null` for the deprecated fields.
    - `domain.ts` marks the three fields `@deprecated` (still nullable).
    - `npm run typecheck` + lint exit 0 (columns still present in DB at this point).
  </acceptance_criteria>
  <done>All DB-typed HH consumers are neutralized and HH admin editing is removed; typecheck is green before the drop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Drop legacy happy_hour columns + push + update types</name>
  <files>supabase/migrations/20260711000001_drop_happy_hour_columns.sql, src/shared/lib/supabase.types.ts</files>
  <read_first>
    - supabase/migrations/20260710000007_migrate_happy_hour_data.sql (confirms data was migrated additively — this is the paired destructive follow-up)
    - src/shared/lib/supabase.types.ts (categories/products Row blocks containing happy_hour_start/end/happy_hour_price to remove)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Pitfall 4 — separate destructive migration, only after verification)
    - .planning/STATE.md (Phase 17-03 / 19-03 push precedents)
  </read_first>
  <what-built>
    Task 1 removed all code dependency on the HH columns; the Plan 20-09 parity gate confirmed the server engine reproduces every legacy price. The destructive column drop is now safe.
  </what-built>
  <action>
    Create migration `20260711000001_drop_happy_hour_columns.sql` (BEGIN/COMMIT): `ALTER TABLE categories DROP COLUMN IF EXISTS happy_hour_start; ALTER TABLE categories DROP COLUMN IF EXISTS happy_hour_end; ALTER TABLE products DROP COLUMN IF EXISTS happy_hour_price;`. AUTOMATION FIRST: run `npx supabase db push` (this second push applies only this drop) and, if needed, `NOTIFY pgrst, 'reload schema';`. Verify with `SELECT column_name FROM information_schema.columns WHERE table_name IN ('categories','products') AND column_name IN ('happy_hour_start','happy_hour_end','happy_hour_price');` returning zero rows. Update `src/shared/lib/supabase.types.ts` to remove `happy_hour_start`/`happy_hour_end` from the `categories` Row/Insert/Update and `happy_hour_price` from the `products` Row/Insert/Update (regenerate if possible, else edit deterministically). Pause for the human only if the push cannot run non-interactively.
  </action>
  <how-to-verify>
    1. `npx supabase db push` applied the drop migration with no errors.
    2. The information_schema query returns zero rows (all three columns gone).
    3. `supabase.types.ts` no longer lists the three columns; `npm run typecheck` is green.
  </how-to-verify>
  <verify>
    <automated>npm run typecheck && ! grep -q "happy_hour" src/shared/lib/supabase.types.ts && grep -v '^--' supabase/migrations/20260711000001_drop_happy_hour_columns.sql | grep -c "DROP COLUMN" | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - Migration drops exactly the three legacy columns; push applied with no errors and the columns are confirmed gone live.
    - `supabase.types.ts` has zero `happy_hour` references; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the three columns are confirmed dropped live and typecheck is green.</resume-signal>
  <done>The legacy HH columns are dropped live (post-parity), types updated, typecheck green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Migration file `20260711000001_drop_happy_hour_columns.sql`
- Deprecated (retained, nullable) Zod fields: `Category.happyHourStart/End`, `Product.happyHourPrice` (now always null)
- Removed: HH inputs in CategoryForm/ProductForm; happy_hour columns from supabase.types.ts + category/product query mappings
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified engine → destructive drop | The column drop is gated by the Plan 20-09 parity UAT; no rollback exists once dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Denial of correct billing | premature/erroneous column drop | mitigate | Drop is a separate migration + separate push, only after the Plan 20-09 parity gate + human UAT confirmed zero pricing regression (Pitfall 4) |
| T-20-10 | Availability | code referencing dropped columns | mitigate | Task 1 removes every DB-typed reference BEFORE the drop; typecheck green pre- and post-drop |
| T-20-SC | Tampering | supabase CLI / npm | mitigate | Uses the already-verified toolchain; no new packages |
</threat_model>

<verification>
- Task 1: typecheck + lint green, zero snake_case happy_hour in the touched consumers (columns still live).
- Task 2: blocking push + information_schema confirmation + zero happy_hour in supabase.types.ts.
</verification>

<success_criteria>
- No code reads/writes the HH columns; HH admin editing removed.
- The three legacy columns are dropped live post-parity; types updated; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-10-SUMMARY.md` when done.
</output>
