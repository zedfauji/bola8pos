---
phase: 20-promotions-engine
plan: 06
type: execute
wave: 4
depends_on: ["20-01", "20-03", "20-05", "20-04"]
files_modified:
  - supabase/migrations/20260710000007_migrate_happy_hour_data.sql
  - src/shared/lib/supabase.types.ts
autonomous: false
requirements: [SC-1, SC-2, SC-3]
must_haves:
  truths:
    - "Every product with a happy_hour_price whose category has an HH window becomes an equivalent fixed_price promotion + availability window (additive; old columns untouched)"
    - "All Phase-20 migrations are applied to the live Supabase database"
    - "supabase.types.ts reflects promotions/promotion_availability/applied_promotions and the new RPC signatures"
  artifacts:
    - path: "supabase/migrations/20260710000007_migrate_happy_hour_data.sql"
      provides: "D-07 additive HH→promotions data migration (no column drops)"
      contains: "target_type"
    - path: "src/shared/lib/supabase.types.ts"
      provides: "generated/extended types for the new tables + RPCs"
  key_links:
    - from: "legacy happy_hour_price/happy_hour_start/end"
      to: "promotions + promotion_availability rows"
      via: "INSERT ... SELECT from products JOIN categories with a NOT EXISTS idempotency guard"
      pattern: "happy_hour_price IS NOT NULL"
---

<objective>
Migrate the legacy client-side happy-hour configuration into the new promotions engine (D-01 supersession, D-07 additive auto-conversion) and apply all Phase-20 migrations to the live database via a BLOCKING `supabase db push`, then bring `supabase.types.ts` up to date. After this plan, the server-side engine is LIVE with the pre-existing HH data represented as fixed_price promotions — nothing is dropped yet (Pitfall 4 verification window is preserved for Plan 20-09).

Purpose: Realize SC-1/SC-2/SC-3 on the live DB with zero admin re-entry burden (D-07), while keeping the old columns as a fallback until parity is verified.
Output: One additive data migration + the schema push + refreshed generated types. This plan is `autonomous: false` because the push is a BLOCKING gate that may require credentials/interactive confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 additive HH → promotions data migration</name>
  <files>supabase/migrations/20260710000007_migrate_happy_hour_data.sql</files>
  <read_first>
    - src/shared/lib/domain.ts (Category.happyHourStart/End ~184-185, Product.happyHourPrice ~231 — the legacy fields being converted)
    - src/shared/lib/domain-helpers.ts (resolveProductPrice ~58-74 — the exact legacy semantics: HH price applies when category window active AND product.happyHourPrice not null; the migration must reproduce this mapping to fixed_price)
    - supabase/migrations/20260710000001_promotions_schema.sql (target table/column names)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Pitfall 4 — additive only, NO column drops in this migration)
  </read_first>
  <action>
    Create migration `20260710000007_migrate_happy_hour_data.sql` (BEGIN/COMMIT, additive only — NO `DROP COLUMN`). Using a CTE chain: source = `products p JOIN categories c ON c.id = p.category_id WHERE p.happy_hour_price IS NOT NULL AND c.happy_hour_start IS NOT NULL AND c.happy_hour_end IS NOT NULL`. Insert one `promotions` row per source product: `name = 'Happy Hour: ' || p.name`, `discount_type = 'fixed_price'`, `discount_value = p.happy_hour_price`, `target_type = 'item'`, `target_product_id = p.id`, `priority = 0`, `is_active = true`. Add an idempotency guard so re-running does not duplicate: `WHERE NOT EXISTS (SELECT 1 FROM promotions x WHERE x.target_product_id = p.id AND x.discount_type = 'fixed_price' AND x.name LIKE 'Happy Hour:%')`. `RETURNING id, target_product_id` from the insert CTE, then insert a `promotion_availability` row per created promotion: `days_of_week = '{1,2,3,4,5,6,7}'`, `start_time = <source category happy_hour_start>`, `end_time = <source category happy_hour_end>` (join the RETURNING set back to the source CTE on product id to recover each category's window). Add a header comment: this reproduces `resolveProductPrice`'s legacy override semantics as fixed_price promotions; a `fixed_price` promotion during the same window yields the identical charged price. Do NOT touch `happy_hour_*` columns.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260710000007_migrate_happy_hour_data.sql | grep -q "INSERT INTO promotions" && grep -v '^--' supabase/migrations/20260710000007_migrate_happy_hour_data.sql | grep -q "INSERT INTO promotion_availability" && grep -v '^--' supabase/migrations/20260710000007_migrate_happy_hour_data.sql | grep -q "NOT EXISTS" && ! grep -v '^--' supabase/migrations/20260710000007_migrate_happy_hour_data.sql | grep -qi "drop column"</automated>
  </verify>
  <acceptance_criteria>
    - Inserts `fixed_price` `item`-target promotions from products with `happy_hour_price` whose category has an HH window.
    - Inserts a matching `promotion_availability` row carrying each source category's `start_time`/`end_time`.
    - Has a `NOT EXISTS` idempotency guard; contains NO `DROP COLUMN` / destructive statement.
  </acceptance_criteria>
  <done>Legacy HH data is convertible to equivalent fixed_price promotions additively, idempotently, with the original time windows preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply all Phase-20 migrations via supabase db push</name>
  <files>supabase/migrations/</files>
  <read_first>
    - CLAUDE.md (Supabase section — db push + gen types workaround)
    - .planning/STATE.md (Phase 17-03 / 19-03 push precedents — single `npx supabase db push`, NOTIFY pgrst reload if schema cache stale)
  </read_first>
  <what-built>
    All Phase-20 migrations authored in Plans 20-01, 20-03, 20-05, and this plan's 20-07 data migration are ready to apply: `promotions`/`promotion_availability` schema + RLS, `is_promotion_available`, `applied_promotions`, `evaluate_promotions_for_item` (v1 + v2 pool_grant), `create_order_with_items` v3, `stop_pool_session`, and the HH data migration.
  </what-built>
  <action>
    AUTOMATION FIRST — Claude runs the push before requesting human confirmation: execute `npx supabase db push` from `bar-pos/` (set `SUPABASE_ACCESS_TOKEN` if a non-interactive push requires it). If the schema cache is stale afterward, run `NOTIFY pgrst, 'reload schema';`. Then run verification queries and capture output: `SELECT to_regclass('public.promotions'), to_regclass('public.promotion_availability'), to_regclass('public.applied_promotions');` (all non-null); `SELECT proname FROM pg_proc WHERE proname IN ('is_promotion_available','evaluate_promotions_for_item','stop_pool_session');` (all three present); confirm `create_order_with_items` body now contains `evaluate_promotions_for_item` via `pg_get_functiondef`; `SELECT count(*) FROM promotions WHERE name LIKE 'Happy Hour:%';` (matches the count of pre-migration HH products). Only if the push cannot run non-interactively, pause for the human to authenticate/confirm.
  </action>
  <how-to-verify>
    1. Confirm `npx supabase db push` reported all Phase-20 migrations applied (no errors).
    2. Confirm the three `to_regclass` results are non-null and the three functions exist.
    3. Confirm `create_order_with_items` definition references `evaluate_promotions_for_item`.
    4. Confirm the `Happy Hour:%` promotion count equals the number of pre-migration products with `happy_hour_price` + an HH-window category.
  </how-to-verify>
  <verify>
    <automated>MISSING — live-DB push verification is inherently manual (blocking checkpoint); the automated live integration tests in Plans 20-03/20-05 (currently skipped) run and must pass in Plan 20-09 once this push lands.</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` applied all Phase-20 migrations with no errors.
    - Three tables + three functions confirmed live; `create_order_with_items` wired to `evaluate_promotions_for_item`.
    - HH promotion count matches pre-migration HH product count.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all four verification checks pass, or describe the failure.</resume-signal>
  <done>All Phase-20 migrations are live on Supabase; schema objects verified; HH data migrated additively.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate/extend supabase.types.ts for the new tables + RPCs</name>
  <files>src/shared/lib/supabase.types.ts</files>
  <read_first>
    - src/shared/lib/supabase.types.ts (existing table/RPC type structure — Row/Insert/Update/Relationships blocks + Functions section; e.g. the tip_distribution_entries + close_caja_session entries added in Phase 19)
    - CLAUDE.md (gen types workaround: `npx supabase gen types typescript --local`; if Docker/local unavailable, transcribe manually as prior phases did)
    - .planning/STATE.md (Phase 17-03 / 15-03 manual-extension precedent when Docker unavailable)
  </read_first>
  <action>
    Attempt `npx supabase gen types typescript` (linked/remote) to regenerate `src/shared/lib/supabase.types.ts`. If regeneration is unavailable (Docker/local pipe issue, as in prior phases), manually add — matching the file's existing alphabetical placement + Row/Insert/Update/Relationships convention — type blocks for `promotions`, `promotion_availability`, and `applied_promotions`, plus Functions entries for `is_promotion_available(p_promotion_id, p_ts) → boolean`, `evaluate_promotions_for_item(p_order_item_id) → void`, `stop_pool_session(p_session_id, p_expected_version) → Json`, and the updated `create_order_with_items` 7-arg signature. Column types must match the migrations (discount_type/target_type as string, numeric→number, integer→number, booleans, nullable FKs). Keep the entity/UI `const db = supabase as any` casts as-is for now (removing them is a follow-up); do not break existing types.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q "applied_promotions" src/shared/lib/supabase.types.ts && grep -q "stop_pool_session" src/shared/lib/supabase.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `supabase.types.ts` contains `promotions`, `promotion_availability`, `applied_promotions` table types and the `is_promotion_available` / `evaluate_promotions_for_item` / `stop_pool_session` function entries.
    - `create_order_with_items` reflects the 7-arg signature.
  </acceptance_criteria>
  <done>Generated types cover all new tables + RPCs; typecheck green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Migration file `20260710000007_migrate_happy_hour_data.sql`
- Live DB objects (from the push): all Phase-20 tables/functions become live
- supabase.types.ts entries: `promotions`, `promotion_availability`, `applied_promotions`, `is_promotion_available`, `evaluate_promotions_for_item`, `stop_pool_session`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live schema | Applying migrations is a privileged operation gated by the blocking human checkpoint |
| legacy HH data → promotions | Automated conversion; parity is verified before any column is dropped (Plan 20-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering / data integrity | HH→promotions conversion correctness | mitigate | Additive-only migration + `NOT EXISTS` idempotency guard; parity vs resolveProductPrice verified in Plan 20-09 BEFORE columns are dropped (Pitfall 4) |
| T-20-07b | Denial of service | destructive push before verification | mitigate | No `DROP COLUMN` in this plan; the destructive drop is isolated to Plan 20-10 behind a UAT gate |
| T-20-SC | Tampering | supabase CLI / npm | mitigate | Uses the already-verified local Supabase toolchain from prior phases; no new packages installed |
</threat_model>

<verification>
- `grep` gate confirms the data migration is additive.
- Blocking checkpoint verifies live schema objects + HH promotion count.
- `npm run typecheck` green after types refresh.
</verification>

<success_criteria>
- HH data migrated to fixed_price promotions additively (old columns intact).
- All Phase-20 migrations live; three tables + three functions verified.
- supabase.types.ts updated; typecheck green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-06-SUMMARY.md` when done.
</output>
