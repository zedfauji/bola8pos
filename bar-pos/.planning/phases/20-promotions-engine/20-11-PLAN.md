---
phase: 20-promotions-engine
plan: 11
type: execute
wave: 8
depends_on: ["20-10"]
files_modified:
  - src/shared/lib/domain-helpers.ts
  - src/shared/lib/domain-helpers.test.ts
  - src/features/add-item-to-tab/ui/ModifierSheet.tsx
  - src/entities/product/ui/ProductCard.tsx
  - src/entities/product/ui/ProductCard.test.tsx
  - src/widgets/TableStatusPanel/index.tsx
  - src/widgets/TableStatusPanel/isHappyHourActive.ts
  - src/widgets/TableStatusPanel/isHappyHourActive.test.ts
  - src/features/print-precheque/usePrintPreCheque.ts
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "The client-side happy-hour calculation path (resolveProductPrice, isHappyHourActive) is fully removed from the codebase"
    - "Product cards and the modifier sheet display the base price; no client HH pricing logic remains"
    - "The table-status happy-hour indicator (order-based isHappyHourActive) is retired"
    - "The full unit suite, typecheck, and lint are green after the retirement"
  artifacts:
    - path: "src/shared/lib/domain-helpers.ts"
      provides: "resolveProductPrice + isHappyHourActive removed"
    - path: "src/entities/product/ui/ProductCard.tsx"
      provides: "base-price display, no HH badge/logic"
  key_links:
    - from: "ModifierSheet / ProductCard"
      to: "product.basePrice"
      via: "direct display (no HH computation)"
      pattern: "basePrice"
---

<objective>
Complete D-01 by removing the now-dead client-side happy-hour calculation path: delete `resolveProductPrice` and `isHappyHourActive` from `domain-helpers`, switch the remaining display consumers (ModifierSheet, ProductCard) to base price, retire the table-status HH indicator, and clean residual references. After Plan 20-10 dropped the columns, these functions only ever returned base price / false — this plan removes them cleanly.

Purpose: Leave a single source of truth for promotion pricing (the server engine) with no lingering client HH logic that could mislead future work.
Output: The client HH calc path is gone; display consumers use base price; full gate green. (The vestigial `@deprecated` nullable Zod fields from Plan 20-10 remain as documented housekeeping — out of scope to remove here.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove resolveProductPrice + isHappyHourActive; switch ModifierSheet + ProductCard to base price</name>
  <files>src/shared/lib/domain-helpers.ts, src/shared/lib/domain-helpers.test.ts, src/features/add-item-to-tab/ui/ModifierSheet.tsx, src/entities/product/ui/ProductCard.tsx, src/entities/product/ui/ProductCard.test.tsx</files>
  <read_first>
    - src/shared/lib/domain-helpers.ts (resolveProductPrice ~58-74 + isHappyHourActive ~266 to delete; keep calculateDiscountAmount/DiscountType which belong to the unrelated manual-discount feature)
    - src/shared/lib/domain-helpers.test.ts (the resolveProductPrice + isHappyHourActive test blocks to remove)
    - src/features/add-item-to-tab/ui/ModifierSheet.tsx (categoryForPricing ~17 + resolveProductPrice ~44 in runningTotal)
    - src/entities/product/ui/ProductCard.tsx (isHappyHourActive/resolveProductPrice ~25-26 driving displayPrice + inHappyHour badge)
    - src/entities/product/ui/ProductCard.test.tsx (any HH-badge assertions to update)
  </read_first>
  <action>
    Delete `resolveProductPrice` and the category-based `isHappyHourActive` from `domain-helpers.ts`, plus any now-unused imports they required. Keep `calculateDiscountAmount`/`DiscountType`/`DiscountScope` (the unrelated manual-payment feature). Remove their test blocks from `domain-helpers.test.ts`.

    `ModifierSheet.tsx`: remove the `resolveProductPrice` import and the `categoryForPricing` helper + `category` memo; compute `runningTotal` from `product.basePrice + modifierSum` directly (rounded to 2 decimals as before). The "Item total" display now reflects base price (server applies any promotion at order time).

    `ProductCard.tsx`: remove the `isHappyHourActive`/`resolveProductPrice` imports and the `inHappyHour`/`displayPrice` HH logic; display `product.basePrice`. Remove the happy-hour visual badge. Update `ProductCard.test.tsx` to drop HH-badge assertions and assert the base price renders. (The `category` prop stays; its deprecated nullable HH fields are simply unused.)
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/domain-helpers.test.ts src/entities/product/ui/ProductCard.test.tsx && npm run typecheck && ! grep -n "resolveProductPrice" src/shared/lib/domain-helpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resolveProductPrice` and the category-based `isHappyHourActive` no longer exist in `domain-helpers.ts`; their tests are removed.
    - ModifierSheet + ProductCard compute/display from `product.basePrice` with no HH logic; ProductCard has no HH badge.
    - `npx vitest run` for the touched tests + `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>The client HH pricing/display logic is removed from domain-helpers, ModifierSheet, and ProductCard; base price is displayed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the table-status HH indicator + residual cleanup + full gate</name>
  <files>src/widgets/TableStatusPanel/index.tsx, src/widgets/TableStatusPanel/isHappyHourActive.ts, src/widgets/TableStatusPanel/isHappyHourActive.test.ts, src/features/print-precheque/usePrintPreCheque.ts</files>
  <read_first>
    - src/widgets/TableStatusPanel/index.tsx (~92 — the isHappyHourActive(activeOrders) usage + the indicator it drives)
    - src/widgets/TableStatusPanel/isHappyHourActive.ts + isHappyHourActive.test.ts (the order-based HH helper + test to delete)
    - src/features/print-precheque/usePrintPreCheque.ts (~22, ~94 — stale TODO comments referencing isHappyHourActive)
    - src/shared/lib/receipt-format.ts and src/shared/lib/supabase-contracts.ts (grep for any happyHour flag that references the dropped columns — neutralize if present)
  </read_first>
  <action>
    Delete `src/widgets/TableStatusPanel/isHappyHourActive.ts` and its `isHappyHourActive.test.ts`. In `TableStatusPanel/index.tsx`, remove the `isHappyHourActive` import + the `happyHour` memo (~92) and the UI element it drove (the HH indicator is superseded; the "Active Promotions" banner covers promotion visibility in the order panel). Remove the stale `isHappyHourActive` TODO comments in `usePrintPreCheque.ts`. Grep for any remaining `happyHour`/`happy_hour` in `receipt-format.ts` / `supabase-contracts.ts`; if a receipt/contract `happyHourActive` flag exists and depended on the dropped columns, remove it or hardcode it false with a comment (promotions are server-applied; the receipt reflects the already-discounted line). Then run the FULL gate.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `src/widgets/TableStatusPanel/isHappyHourActive.ts` and its test are deleted; `TableStatusPanel/index.tsx` no longer imports/uses them.
    - No stale `isHappyHourActive` TODO comments remain in `usePrintPreCheque.ts`; residual receipt/contract HH flags neutralized.
    - `npm run typecheck`, `npm run lint`, and `npm run test` all exit 0 (full suite green after the supersession).
  </acceptance_criteria>
  <done>The table-status HH indicator + residual HH references are retired; the full unit suite, typecheck, and lint are green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Removed: `resolveProductPrice`, category-based `isHappyHourActive` (domain-helpers), order-based `isHappyHourActive` (TableStatusPanel), the HH visual badge in ProductCard
- No new production symbols (net removal plan)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client display → billing | Display consumers now show base price; the server-applied discounted price appears on the placed order/ticket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | lingering client HH pricing logic | mitigate | resolveProductPrice/isHappyHourActive removed entirely — no client code can compute a charged price; the server engine is the single source of truth |
| T-20-11 | Availability | dangling imports after deletion | mitigate | Full `npm run typecheck` + `npm run lint` + `npm run test` gate confirms no dangling references remain |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages (net-removal plan); slopcheck N/A |
</threat_model>

<verification>
- Task 1: touched tests + typecheck green; `resolveProductPrice` absent from domain-helpers.
- Task 2: full `npm run typecheck` + `npm run lint` + `npm run test` green; deleted files confirmed gone.
</verification>

<success_criteria>
- Client HH calc path fully removed; display consumers use base price.
- Table-status HH indicator retired; residual references cleaned.
- Full unit suite, typecheck, and lint green — Phase 20 supersession complete.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-11-SUMMARY.md` when done.
</output>
