---
phase: 20-promotions-engine
plan: 13
type: execute
wave: 2
depends_on: ["20-12"]
gap_closure: true
files_modified:
  - e2e/43-promotions.spec.ts
  - e2e/helpers/supabase.ts
autonomous: true
requirements: [SC-3, SC-4]
requirements_note: >-
  Phase 20 has no numbered requirement IDs (ROADMAP Requirements line reads
  "TBD (POS-COMPARISON.md §20 — source doc no longer present; scope locked in
  20-CONTEXT.md)"). Per the spec-less probe fallback, the phase's own Success
  Criteria are used as requirement IDs. Spec-less probe fallback skipped:
  phase has no requirement IDs to probe (visible skip).

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-20-3: with a promotion created through the UI and switched Active, the POS order screen shows the amber 'Promotions Active — {name}' banner; it is absent when no promotion is active"
    - "G-20-4: an item-targeted promotion created through the UI (not seeded) discounts the order line at order time with no confirmation step (SC-3, D-02)"
    - "G-20-5: with an active pool_billing promotion, stopping a pool session writes a discounted total_charge rather than the undiscounted base charge"
    - "G-20-6: with an active pool_grant promotion, ordering the granting product then stopping the session consumes the grant (consumed_at set) and deducts the bonus minutes before the rate is applied"
  artifacts:
    - "e2e/43-promotions.spec.ts — T3 (banner + UI-created item discount), T4 (pool billing discount), T5 (pool grant consumption)"
    - "e2e/helpers/supabase.ts — grant/session lookup helper(s) if the existing set is insufficient"
  key_links:
    - "UI-created active promotion ↔ useActivePromotions ↔ the Promotions Active banner in src/widgets/OrderPanel/HappyHourBanner.tsx"
    - "active pool_billing promotion ↔ stop_pool_session step 5 (sequential compounding onto v_base_charge) ↔ pool_sessions.total_charge"
    - "pool_grant applied_promotions row (pool_minutes_granted, consumed_at NULL) ↔ stop_pool_session step 3 (v_prepaid deduction) ↔ pool_sessions.billed_minutes"
    - "every seeded promotion ↔ its finally-block cleanup — a leaked active promotion silently corrupts 04-pool-timer and 05-payments charge assertions"
---

<objective>
Re-exercise the four UAT paths that could not be tested at all while promotion
creation was broken — G-20-3 (Active Promotions banner), G-20-4 (item/category
discount at order time), G-20-5 (pool billing discount), G-20-6 (pool bonus-minute
grant) — with real browser coverage rather than assuming they resolve as a
by-product of the 20-12 fix.

Purpose: the 20-UAT.md gaps explicitly record these four as "blocked, not
independently diagnosed". Their server-side math already has integration-test
coverage; what has never been proven is that a promotion an admin creates through
the UI actually flows through to the POS banner, the order line, and the pool
session charge. This plan supplies that proof.

Output: three new Playwright tests in e2e/43-promotions.spec.ts, each pinned to
the gap it closes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-UAT.md
@.planning/phases/20-promotions-engine/20-12-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Test-only plan. It produces **no new production symbols** — no functions, hooks,
types, components, RPCs, migrations, CLI flags, or routes.

| Kind | Name | Location |
|------|------|----------|
| test case | `T3` — banner + UI-created item discount | `e2e/43-promotions.spec.ts` |
| test case | `T4` — pool billing discount at session stop | `e2e/43-promotions.spec.ts` |
| test case | `T5` — pool grant consumed at session stop | `e2e/43-promotions.spec.ts` |
| helper (only if the existing set is insufficient) | a pool-grant / pool-session lookup exported from `e2e/helpers/supabase.ts` | `e2e/helpers/supabase.ts` |

If the existing helpers in `e2e/helpers/supabase.ts` already cover the lookups,
add no new helper — prefer reuse over a new export.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: T3 — banner appears and a UI-created promotion discounts the order line (G-20-3, G-20-4)</name>
  <files>e2e/43-promotions.spec.ts</files>
  <read_first>
    - e2e/43-promotions.spec.ts (read in full as corrected by plan 20-12 — T1 is the create-via-UI recipe to reuse, T2 is the order-time assertion recipe to reuse)
    - src/widgets/OrderPanel/HappyHourBanner.tsx (read in full — this file kept its legacy name but is now the promotion-driven Active Promotions banner; get its exact rendered text, aria-label and the "Ends in…" suffix condition from the source, do not guess the selector)
    - src/shared/lib/i18n/locales/en-US/wPanels.json (the `promotionsActive` value is `Promotions Active — {{names}}` and `promotionsActiveAriaLabel` is `Promotions active`; E2E runs in en-US per the pinned staff locale)
    - src/entities/promotion/model/queries.ts (lines 209-266 — useActivePromotions has a 30s staleTime; the test must account for that cache window rather than expecting an instant refetch)
    - e2e/helpers/supabase.ts (lines 22-60, 153-200, 260-300 — getServiceClient, resetTestState, openCaja, forceCloseAllOpenTabs, getOpenTabIdByCustomerName)
  </read_first>
  <action>
    Add one test `T3` to the existing `Promotions` describe block in
    e2e/43-promotions.spec.ts, annotated with requirement `SC-3, SC-4` and gaps
    `G-20-3, G-20-4`. Set a 120000 ms timeout — it spans two logins.

    Phase A (admin, create through the UI — this is the part that makes T3 a real
    closure of G-20-3/G-20-4 rather than a re-run of T2's seeded shortcut): log in
    as admin, go to /settings, open the Promotions tab, click `+ Add promotion`,
    and in the `Edit Promotion` dialog set a unique name, discount type Percentage
    off, discount value `25`, Applies to `Item`, Product `Budweiser`, Priority `0`,
    and switch Active on. Save and assert the `Promotion saved` toast. Reuse T1's
    exact selector idioms — same `getByLabel` / `getByRole` calls, same explicit
    Applies-to selection T1 gained in plan 20-12. Capture Budweiser's `base_price`
    via `getServiceClient()` before or after this phase and compute the expected
    discounted price as base × 0.75 rounded to 2 decimals, mirroring T2's
    `Math.round(x * 100) / 100` idiom.

    Phase B (banner, G-20-3): log out, `openCaja(430)`, log in as bartender, go to
    /pos. Assert the Active Promotions banner is visible and contains the promotion
    name just created. Derive the selector from HappyHourBanner.tsx — prefer its
    aria-label over matching the em-dash-joined display string. Because
    `useActivePromotions` carries a 30s `staleTime`, give this assertion a generous
    timeout (30000 ms) rather than adding a reload loop; a fresh login mounts a
    fresh query client, so the first read is not served from a stale cache. Do not
    assert on the `Ends in…` suffix — this promotion has no availability window, and
    per the UAT expectation the suffix is omitted in exactly that case; assert the
    suffix is absent instead.

    Phase C (order-time discount, G-20-4): still as bartender, open a tab with a
    unique customer name, add Budweiser, and place the order. Assert no
    confirmation or "apply promotion?" prompt appears between the click and the
    `Order placed successfully` toast (D-02: silent and automatic). Then verify the
    charged price the way T2 does — read the tab, its newest order, and that
    order's `order_items.unit_price` through the service client, asserting it equals
    the computed discounted price and does not equal `base_price`. The page's order
    history panel does not render a per-item price; do not try to assert on page
    text, per T2's own documented note.

    Phase D (banner disappears): deactivate the promotion through the service client
    (set `is_active` false), then have the bartender navigate away from and back to
    /pos and assert the banner is no longer present. Use a `not.toBeVisible` with a
    timeout that comfortably exceeds the 30s staleTime, or force a fresh mount via
    a full page reload — pick whichever the banner's actual query wiring makes
    deterministic, and say which in a comment.

    Wrap phases B through D in try/finally. In `finally`, delete the promotion's
    `applied_promotions` rows then the promotion row, and call
    `forceCloseAllOpenTabs()`. A promotion leaked in the active state silently
    changes prices for every later spec in the suite — the cleanup is not optional.
  </action>
  <verify>
    <automated>cd bar-pos &amp;&amp; npx playwright test e2e/43-promotions.spec.ts -g "T3"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (G-20-3): during the T3 run, the Active Promotions banner is visible on /pos carrying the promotion name created moments earlier through the admin UI, and is absent after the promotion is deactivated.
    - Behavior assertion (G-20-4): `order_items.unit_price` for the placed Budweiser line equals `base_price * 0.75` rounded to 2 decimals and is strictly not equal to `base_price`, with no confirmation dialog appearing between "Place Order" and the success toast.
    - Behavior assertion: the promotion used by T3 is created through the browser UI, not seeded via `getServiceClient().from('promotions').insert(...)` — grep the T3 body to confirm it contains no promotions insert.
    - Test command: `npx playwright test e2e/43-promotions.spec.ts -g "T3"` passes.
    - Test command: `npx playwright test e2e/43-promotions.spec.ts` passes in full — T1 and T2 are not regressed by the new test's shared state.
    - CLI output: after the run, `select count(*) from promotions where name like 'E2E%' or name like '__e2e%'` returns 0 — no leaked test promotion.
  </acceptance_criteria>
  <precondition>
    A dev server on port 1420 and `bar-pos/.env.local` with E2E credentials against a remote Supabase carrying every Phase 20 migration; Playwright on Ubuntu additionally needs a real display session and `google-chrome-stable` (playwright.config.ts pins `headless: false`, `channel: 'chrome'`).
  </precondition>
  <done>
    G-20-3 and G-20-4 are closed by a passing browser test that creates its
    promotion the same way a user does, sees the banner, and reads the discounted
    price off the order line.
  </done>
</task>

<task type="auto">
  <name>Task 2: T4/T5 — pool billing discount and pool bonus-minute grant at session stop (G-20-5, G-20-6)</name>
  <files>e2e/43-promotions.spec.ts, e2e/helpers/supabase.ts</files>
  <read_first>
    - supabase/migrations/20260710000006_stop_pool_session_rpc.sql (read lines 100-200 in full — step 3 consumes grants into v_prepaid, step 4 computes v_billed_minutes as CEIL(chargeable/15)*15 with chargeable = GREATEST(0, base_billed - v_prepaid), step 5 compounds pool_billing promotions onto v_base_charge, step 6 writes total_charge; these formulas dictate every expected value below)
    - supabase/migrations/20260710000005_evaluate_promotions_pool_grant.sql (lines 150-200 — a pool_grant promotion matches on target_product_id OR target_category_id and writes an applied_promotions row with pool_minutes_granted = discount_value and consumed_at NULL)
    - e2e/04-pool-timer.spec.ts (read in full — the canonical start/stop pool session browser flow and its table/session selectors)
    - e2e/helpers/supabase.ts (lines 321-365 — getPoolSessionStartedAt, getLatestStoppedPoolChargeForTab, getOccupiedPoolTableIds; check these before writing any new helper)
    - e2e/43-promotions.spec.ts (as extended by Task 1 — reuse its describe block, seeding style and finally-block cleanup discipline)
  </read_first>
  <action>
    Add two tests to the existing `Promotions` describe block.

    T4 (G-20-5, pool billing discount). Annotate requirement `SC-3` and gap
    `G-20-5`. Seed one active promotion directly through `getServiceClient()` —
    target type `pool_billing`, discount type `percentage`, discount value `50`,
    priority `0`, `is_active` true, with no product or category FK (pool_billing
    requires none). Open a caja, log in, start a pool table session linked to a tab
    using the flow from 04-pool-timer.spec.ts, then stop it through the UI. Read the
    table's `rate_per_hour` from the service client and compute the expectation from
    the RPC's own arithmetic: a session stopped within seconds yields base_billed
    under a minute, so `v_billed_minutes` is CEIL of that over 15, times 15, i.e.
    15 minutes, giving a base charge of `round((15/60) * rate_per_hour, 2)`; the 50%
    promotion halves it. Assert `pool_sessions.total_charge` equals that halved
    value and is strictly less than the base charge. Also assert an
    `applied_promotions` row exists for this session with `target_type`
    `pool_billing` and `original_amount` equal to the undiscounted base charge —
    that row is the evidence the discount came from the promotion engine rather
    than from a rate misread.

    T5 (G-20-6, pool bonus-minute grant). Annotate requirement `SC-3` and gap
    `G-20-6`. Seed one active promotion — target type `pool_grant`, `discount_value`
    `30` (the grant is denominated in minutes), `target_product_id` set to
    Budweiser's id, `is_active` true. Note that `discount_type` is not read on the
    grant path; set it to `percentage` to satisfy the table's NOT NULL and enum
    CHECK. Open a caja, log in as bartender, open a tab, start a pool session on
    that same tab, order Budweiser, and confirm through the service client that an
    `applied_promotions` row now exists for the tab with `pool_minutes_granted` 30
    and `consumed_at` null. Then stop the session through the UI and assert two
    things: that same row now has a non-null `consumed_at` and a `pool_session_id`
    matching the stopped session, and that the session's `billed_minutes` is 0 with
    `total_charge` 0 — because a seconds-long session's base_billed minus a 30
    minute prepaid grant clamps to zero chargeable minutes. State in a comment that
    the identical session without the grant would have billed 15 minutes, which is
    what makes this a differential assertion rather than a tautology.

    Both tests must seed and tear down inside try/finally: delete their
    `applied_promotions` rows, then the promotion row, then call
    `forceCloseAllOpenTabs()` and stop any session they started. Critically, T4 and
    T5 must not have overlapping active promotions — an active pool_grant would zero
    the charge T4 is measuring, and an active pool_billing would halve the charge T5
    expects to be zero. Keep each test's seed scoped to its own body and assert at
    the start of each that no other active promotion of the other pool target type
    exists, failing fast with a clear message if one leaked from a prior run.

    Add a helper to e2e/helpers/supabase.ts only if the existing exports genuinely
    cannot express these lookups; check `getLatestStoppedPoolChargeForTab`,
    `getPoolSessionStartedAt` and `getOccupiedPoolTableIds` first and reuse them.
  </action>
  <verify>
    <automated>cd bar-pos &amp;&amp; npx playwright test e2e/43-promotions.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion (G-20-5): in the T4 run, `pool_sessions.total_charge` for the stopped session equals `round((15/60) * rate_per_hour, 2) * 0.5` rounded to 2 decimals and is strictly less than the undiscounted base charge; a matching `applied_promotions` row exists with `target_type` `pool_billing` and `original_amount` equal to the base charge.
    - Behavior assertion (G-20-6): in the T5 run, the grant's `applied_promotions` row transitions from `consumed_at` null to non-null across the session stop and gains the stopped session's id, and the session records `billed_minutes` 0 and `total_charge` 0.
    - Test command: `npx playwright test e2e/43-promotions.spec.ts` passes with all five tests (T1, T2, T3, T4, T5) green in a single run, proving the tests do not contaminate each other through shared promotion state.
    - Test command: `npx playwright test e2e/04-pool-timer.spec.ts` passes after the promotions spec has run, proving no active promotion leaked into the pool billing assertions of a neighbouring spec.
    - Source assertion: both T4 and T5 bodies contain a `finally` block that deletes from `applied_promotions` and `promotions`.
  </acceptance_criteria>
  <precondition>
    Same as Task 1, plus at least one pool table available to start a session on (04-pool-timer.spec.ts's own preconditions apply verbatim).
  </precondition>
  <done>
    G-20-5 and G-20-6 are closed by passing browser tests that measure the actual
    charge and the actual grant-consumption transition; the full promotions spec and
    the neighbouring pool-timer spec are both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E service-role client → Supabase | `getServiceClient()` holds a service-role key that bypasses RLS; it exists only in the Playwright process and reads its key from `bar-pos/.env.local`. |
| Seeded test promotion → live pricing path | A promotion row seeded by a test is read by `evaluate_promotions_for_item` and `stop_pool_session` exactly as a real one is, and changes real charged prices until deleted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Tampering | leaked active test promotion | medium | mitigate | Every seeded promotion is created and destroyed inside a try/finally in its own test body, and each pool test asserts at entry that no conflicting active promotion of the other pool target type exists. The acceptance criteria additionally require `04-pool-timer.spec.ts` to pass after the promotions spec, which fails loudly if a promotion leaks. |
| T-20-06 | Information Disclosure | service-role key in the E2E process | low | accept | Pre-existing and unchanged by this plan. The key lives only in the gitignored `bar-pos/.env.local` consumed by `requireIntegrationEnv()`; no task may hardcode it or echo it into test output. |
| T-20-07 | Repudiation | applied_promotions audit rows written by tests | low | accept | Test runs write real `applied_promotions` and `promotion.apply` audit rows. This is inherent to integration-grade E2E against a shared remote and matches the existing behavior of T2, 04-pool-timer and 42-tip-distribution. Test rows are name-prefixed for identification and deleted in cleanup. |
| T-20-SC | Tampering | supply chain | n/a | accept | This plan installs no npm/pip/cargo packages. No package legitimacy audit is required and none of the tasks may add a dependency. |

No high-severity threats identified; nothing in this register blocks execution.
</threat_model>

<verification>
1. `npx playwright test e2e/43-promotions.spec.ts` — all five tests pass in one run.
2. `npx playwright test e2e/04-pool-timer.spec.ts e2e/05-payments.spec.ts` — pass after the promotions spec, confirming no pricing contamination.
3. Post-run database check: no `promotions` rows remain whose name carries an E2E prefix.
4. `npm run typecheck` and `npm run lint` — 0 errors (the spec and helpers are type-checked and linted like any other source).
</verification>

<success_criteria>
- G-20-3, G-20-4, G-20-5 and G-20-6 each have a named, passing Playwright test asserting the observable behavior the UAT could not reach, not merely an inference from the 20-12 fix.
- G-20-4's promotion is created through the browser UI, closing the loop between SC-4 (admin can create) and SC-3 (server auto-applies) in a single flow.
- No production source file is modified by this plan; the suite is left in a state where a leaked promotion cannot silently pass.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-13-SUMMARY.md` when done.
</output>
