---
phase: 20-promotions-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/lib/domain.ts
  - src/shared/lib/domain.test.ts
  - src/shared/lib/audit-actions.ts
  - src/entities/promotion/model/types.ts
  - src/entities/promotion/model/queries.ts
  - src/entities/promotion/model/index.ts
  - src/entities/promotion/index.ts
  - src/entities/promotion/model/queries.test.ts
autonomous: true
requirements: [SC-1, SC-4]
must_haves:
  truths:
    - "Zod schemas define promotion, promotion-availability, and applied-promotion shapes as the single source of truth (inferred types, no hand-written interfaces)"
    - "Promotion discount/target enums are distinct from the existing manual-payment DiscountType/DiscountScope enums (no reuse)"
    - "entities/promotion exposes TanStack Query hooks to list, create, update, disable promotions and read/save availability windows, mirroring entities/combo"
    - "A promotion.apply audit action is registered so the server RPC can record applied promotions"
  artifacts:
    - path: "src/shared/lib/domain.ts"
      provides: "PromotionDiscountTypeSchema, PromotionTargetTypeSchema, PromotionSchema(+Create/Update), PromotionAvailabilitySchema(+Create), AppliedPromotionSchema"
      contains: "export const PromotionSchema"
    - path: "src/entities/promotion/model/queries.ts"
      provides: "promotionKeys + usePromotions/useMutationCreatePromotion/useMutationUpdatePromotion/useMutationDeletePromotion/usePromotionAvailabilityWindows/usePromotionActive hooks"
      exports: ["usePromotions", "useMutationCreatePromotion"]
    - path: "src/entities/promotion/index.ts"
      provides: "explicit-named barrel for the promotion entity"
    - path: "src/shared/lib/audit-actions.ts"
      provides: "promotion.apply audit action in AuditActionSchema + AuditAction const"
      contains: "promotion.apply"
  key_links:
    - from: "entities/promotion/model/queries.ts"
      to: "promotions / promotion_availability tables"
      via: "supabase (as any) from().select()/insert()/update()"
      pattern: "from\\('promotions'\\)"
    - from: "AppliedPromotionSchema"
      to: "applied_promotions row shape"
      via: "field-for-field mapping (Plan 20-03 authors the table)"
      pattern: "AppliedPromotionSchema"
---

<objective>
Author the TypeScript contract layer for promotions: Zod schemas in `domain.ts` (the single source of truth), a new `promotion.apply` audit action, and the `entities/promotion/` FSD slice (types + TanStack Query hooks + barrels) cloned from `entities/combo/`. This is interface-first: everything downstream (admin UI in 20-04, banner in 20-07) consumes these contracts.

Purpose: Give executors and the admin UI a ready, typed data-access layer before any UI is built, and lock naming so `domain.ts` column mapping matches the migration column names from Plan 20-01/20-03.
Output: Extended `domain.ts` + `audit-actions.ts`, a new `entities/promotion/` slice, and Wave-0 test stubs. Uses `const db = supabase as any` (pre-regen cast) because the new tables are not yet in `supabase.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-promotions-engine/20-CONTEXT.md
@.planning/phases/20-promotions-engine/20-RESEARCH.md
@.planning/phases/20-promotions-engine/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promotion Zod schemas + promotion.apply audit action</name>
  <files>src/shared/lib/domain.ts, src/shared/lib/domain.test.ts, src/shared/lib/audit-actions.ts</files>
  <read_first>
    - src/shared/lib/domain.ts (ComboSlotSchema/ComboAvailabilitySchema block ~lines 1487-1541 for the Schema/CreateSchema/UpdateSchema triad; CategorySchema ~179 and ProductSchema ~226 for primitives; DiscountType/DiscountScope for names to AVOID)
    - src/shared/lib/audit-actions.ts (AuditActionSchema z.enum + AuditAction const object shape)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (domain.ts section — exact triad to follow; Pitfall 5 naming rule)
    - .planning/phases/20-promotions-engine/20-RESEARCH.md (Pitfall 5 — do NOT reuse DiscountType/DiscountScope)
  </read_first>
  <behavior>
    - PromotionDiscountTypeSchema parses only 'percentage' | 'fixed_amount' | 'fixed_price'; rejects 'percent' (the legacy DiscountType value).
    - PromotionTargetTypeSchema parses only 'item' | 'category' | 'pool_billing' | 'pool_grant'.
    - PromotionCreateSchema rejects a negative discountValue and a percentage discountValue > 100.
    - PromotionSchema round-trips a well-formed object; AppliedPromotionSchema accepts null promotionId (audit survives promotion deletion).
    - AuditActionSchema.parse('promotion.apply') succeeds.
  </behavior>
  <action>
    In `domain.ts`, add (near the existing Combo schemas, following their exact `Schema` / `CreateSchema` = `.omit({id,createdAt})` / `UpdateSchema` = `.partial().required({id})` triad):
    - `PromotionDiscountTypeSchema = z.enum(['percentage','fixed_amount','fixed_price'])` (the three discount shapes per D-04) and `PromotionTargetTypeSchema = z.enum(['item','category','pool_billing','pool_grant'])` (both pool-time meanings per D-05). Per Pitfall 5, do NOT import, reuse, or extend `DiscountType`/`DiscountScope`.
    - `PromotionSchema` fields: `id: UuidSchema`, `name: z.string().min(1).max(100)`, `discountType: PromotionDiscountTypeSchema`, `discountValue: z.number().nonnegative()`, `targetType: PromotionTargetTypeSchema`, `targetProductId: UuidSchema.nullable()`, `targetCategoryId: UuidSchema.nullable()`, `priority: z.number().int()`, `isActive: z.boolean()`, `createdAt: TimestampSchema`. Add a `.refine` (or `.check`) enforcing `discountType !== 'percentage' || discountValue <= 100`. Emit `PromotionCreateSchema` (omit id+createdAt) and `PromotionUpdateSchema` (partial + required id).
    - `PromotionAvailabilitySchema` mirroring `ComboAvailabilitySchema` exactly but with `promotionId: UuidSchema` instead of `comboProductId`; emit `PromotionAvailabilityCreateSchema` (omit id+createdAt).
    - `AppliedPromotionSchema` (immutable — no UpdateSchema): `id`, `promotionId: UuidSchema.nullable()`, `promotionNameSnapshot: z.string()`, `targetType: PromotionTargetTypeSchema`, `discountType: PromotionDiscountTypeSchema.nullable()`, `discountValue: z.number().nullable()`, `tabId: UuidSchema.nullable()`, `orderItemId: UuidSchema.nullable()`, `poolSessionId: UuidSchema.nullable()`, `originalAmount: z.number().nullable()`, `discountedAmount: z.number().nullable()`, `poolMinutesGranted: z.number().int().nullable()`, `consumedAt: TimestampSchema.nullable()`, `createdAt: TimestampSchema`. Export inferred types for all (`export type Promotion = z.infer<typeof PromotionSchema>`, etc.). Honor `exactOptionalPropertyTypes` (use `.nullable()`, not `?`, for these fields).

    In `audit-actions.ts`, add the literal `'promotion.apply'` to the `AuditActionSchema` z.enum and add `PROMOTION_APPLY: 'promotion.apply'` to the `AuditAction` const object.

    In `domain.test.ts`, add the RED tests described in `<behavior>` (enum acceptance/rejection, negative/over-100 rejection, nullable promotionId acceptance).
  </action>
  <verify>
    <automated>npx vitest run src/shared/lib/domain.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/lib/domain.test.ts` passes including the 5 new behavior cases.
    - `npm run typecheck` exits 0.
    - `grep -q "PromotionDiscountTypeSchema" src/shared/lib/domain.ts` and the file contains `AppliedPromotionSchema`; no new usage of `DiscountType`/`DiscountScope` for promotion code.
    - `audit-actions.ts` contains `'promotion.apply'` inside `AuditActionSchema`.
  </acceptance_criteria>
  <done>All five promotion-related schemas + inferred types exist, distinct from legacy discount enums, and `promotion.apply` is a registered audit action; domain tests + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: entities/promotion slice (types + queries + barrels) + hook stubs</name>
  <files>src/entities/promotion/model/types.ts, src/entities/promotion/model/queries.ts, src/entities/promotion/model/index.ts, src/entities/promotion/index.ts, src/entities/promotion/model/queries.test.ts</files>
  <read_first>
    - src/entities/combo/model/queries.ts (full file — comboKeys factory, useCombos, useComboAvailabilityWindows manual row-mapping, useComboAvailability RPC-caller fail-open pattern, `const db = supabase as any`)
    - src/entities/combo/model/types.ts and src/entities/combo/model/index.ts and src/entities/combo/index.ts (barrel shapes; explicit named exports only — `export *` is banned by ESLint)
    - src/entities/audit-log/model/queries.test.ts (unit test style for entity query hooks)
    - .planning/phases/20-promotions-engine/20-PATTERNS.md (entities/promotion/model/queries.ts section — exact clone excerpts)
  </read_first>
  <action>
    Create the `entities/promotion/` FSD slice cloning `entities/combo/` structure. Use a file-level `/* eslint-disable */` + `const db = supabase as any` cast in `queries.ts` (tables not yet in `supabase.types.ts`; remove the cast in Plan 20-06 after regen).

    `model/types.ts`: re-export `Promotion`, `PromotionCreate`, `PromotionUpdate`, `PromotionAvailability`, `PromotionAvailabilityCreate`, `AppliedPromotion` from `@shared/lib/domain` (mirror `entities/combo/model/types.ts`).

    `model/queries.ts`:
    - `promotionKeys` factory mirroring `comboKeys` (`all`, `lists()`, `detail(id)`, `availability(id)`).
    - `usePromotions()` — `db.from('promotions').select('*').order('priority', { ascending: true })`, map rows through `PromotionSchema.parse(...)` (snake_case → camelCase field mapping exactly like `useComboAvailabilityWindows`).
    - `usePromotion(id)` — single promotion by id (enabled when id present).
    - `useMutationCreatePromotion()` — insert a draft row `{ name: 'New Promotion', discount_type: 'percentage', discount_value: 0, target_type: 'item', priority: 0, is_active: false }` and `.select('id').single()`; return the new id; `onSuccess` invalidate `promotionKeys.lists()`. Default `is_active: false` (draft is safe-to-leave-incomplete, per UI-SPEC §1).
    - `useMutationUpdatePromotion()` — update by id with a camelCase→snake_case payload; invalidate lists + detail.
    - `useMutationDeletePromotion()` — delete by id; invalidate lists.
    - `usePromotionAvailabilityWindows(promotionId)` — clone `useComboAvailabilityWindows`, table `promotion_availability`, column `promotion_id`, map via `PromotionAvailabilitySchema.parse`.
    - `usePromotionActive(promotionId)` — clone `useComboAvailability` fail-open RPC caller, calling `supabase.rpc('is_promotion_available' as any, { p_promotion_id, p_ts })`; on error `return true` (fail-open) and `staleTime: 30_000`.

    `model/index.ts` and `index.ts`: explicit named-export barrels (no `export *`).

    `model/queries.test.ts` (Wave 0): stub unit tests (may be `it.todo` or minimal mock-based) asserting `promotionKeys.lists()` shape and that `usePromotions` maps a snake_case row to a parsed `Promotion`. Keep runnable and green.
  </action>
  <verify>
    <automated>npx vitest run src/entities/promotion/model/queries.test.ts && npm run typecheck && npm run lint -- src/entities/promotion</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/entities/promotion/model/queries.test.ts` exits 0.
    - `npm run typecheck` exits 0; `npm run lint` reports 0 errors for `src/entities/promotion`.
    - `queries.ts` exports `usePromotions`, `usePromotion`, `useMutationCreatePromotion`, `useMutationUpdatePromotion`, `useMutationDeletePromotion`, `usePromotionAvailabilityWindows`, `usePromotionActive`, `promotionKeys`.
    - Barrels use explicit named exports (no `export *`).
    - `useMutationCreatePromotion` insert payload sets `is_active: false`.
  </acceptance_criteria>
  <done>entities/promotion mirrors entities/combo with 7 hooks + key factory + barrels; unit stubs, typecheck, and lint green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols this plan creates (exclude from source-drift verification):
- Zod schemas/types in domain.ts: `PromotionDiscountTypeSchema`, `PromotionTargetTypeSchema`, `PromotionSchema`, `PromotionCreateSchema`, `PromotionUpdateSchema`, `PromotionAvailabilitySchema`, `PromotionAvailabilityCreateSchema`, `AppliedPromotionSchema` + inferred types `Promotion`, `PromotionCreate`, `PromotionUpdate`, `PromotionAvailability`, `PromotionAvailabilityCreate`, `AppliedPromotion`
- audit-actions.ts: `'promotion.apply'` enum member, `AuditAction.PROMOTION_APPLY`
- entity hooks/keys: `promotionKeys`, `usePromotions`, `usePromotion`, `useMutationCreatePromotion`, `useMutationUpdatePromotion`, `useMutationDeletePromotion`, `usePromotionAvailabilityWindows`, `usePromotionActive`
- Files: `src/entities/promotion/**`, `src/shared/lib/domain.ts` additions, `src/shared/lib/audit-actions.ts` additions
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form input → Zod → DB | Client-entered promotion values validated by Zod before reaching the DB CHECK constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | promotion discount/target enum reuse | mitigate | Distinct `PromotionDiscountType`/`PromotionTargetType` enums prevent conflation with manual-payment `DiscountType`/`DiscountScope` (Pitfall 5) |
| T-20-05b | Tampering | client-side discount value | mitigate (defense-in-depth) | Zod `.nonnegative()` + percentage ≤ 100 refine mirrors the DB CHECK; DB remains authoritative |
| T-20-SC | Tampering | npm installs | mitigate | Zero new packages this plan; slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run` for the two new test files, `npm run typecheck`, and `npm run lint -- src/entities/promotion` all green.
- Contract naming (column ↔ schema field) is authoritative for Plans 20-03/20-04/20-06/20-07.
</verification>

<success_criteria>
- All promotion Zod schemas + inferred types exist in domain.ts, distinct from legacy discount enums.
- `promotion.apply` audit action registered.
- entities/promotion slice complete with 7 hooks + barrels; tests + typecheck + lint green.
</success_criteria>

<output>
Create `.planning/phases/20-promotions-engine/20-02-SUMMARY.md` when done.
</output>
