---
phase: 33-payment-critical-page-sweep-isolated
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/PaymentModal/ui/PaymentForm.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "Process Payment / Process split payment button carries focusEmphasis=high (thicker ring); its dual label logic, disabled expression, --pos-accent background, and onClick are unchanged"
    - "Each 'Remove payment N' split-leg button carries touchSize=xl focusEmphasis=high; aria-label 'Remove payment N', disabled, and REMOVE_ROW onClick are unchanged"
    - "'Reset to computed' is a POSButton with data-testid=card-override-reset preserved and its link appearance intact (no solid bg-primary)"
    - "PaymentForm.test.tsx stays green (card-override-reset testid + Remove payment N accessible names still resolve)"
  artifacts:
    - path: "src/widgets/PaymentModal/ui/PaymentForm.tsx"
      provides: "PaymentForm with 3 in-scope elements standardized per D-03/D-04/D-05"
      contains: "focusEmphasis=\"high\""
  key_links:
    - from: "PaymentForm Process Payment button"
      to: "handlePrimary / split submit"
      via: "onClick (unchanged) + disabled expression (unchanged)"
      pattern: "focusEmphasis=\"high\""
    - from: "PaymentForm Remove payment N button"
      to: "dispatchSplitRows REMOVE_ROW"
      via: "onClick (unchanged)"
      pattern: "Remove payment"
---

<objective>
Apply the D-03/D-04/D-05 tier rules to the three in-scope elements in `src/widgets/PaymentModal/ui/PaymentForm.tsx`, satisfying COMPONENT-04 with markup/class-only diffs. Zero prop/handler/validation behavior change (Success Criterion 1). This file hosts two of the six `focusEmphasis="high"` (72px) targets plus one raw-`<button>` link conversion.

Purpose: This is the primary money-commit surface. Per D-03 items 1+4 (Process Payment / split-final-confirm, one dual-label element) and D-03 item 5 (Remove payment N — the leg-remove button that `41-split-payment.spec.ts` T3 exercises), plus the raw `<button>` "Reset to computed" from the Phase 29 drift audit.
Output: One-file isolated PR/commit; all E2E gate specs + the co-located PaymentForm.test.tsx green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/widgets/PaymentModal/ui/PaymentForm.tsx
@src/widgets/PaymentModal/ui/PaymentForm.test.tsx
@src/shared/ui/POSButton.tsx
@src/shared/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire focusEmphasis/touchSize on Process-Payment + Remove-payment-N, convert Reset-to-computed to POSButton</name>
  <files>src/widgets/PaymentModal/ui/PaymentForm.tsx</files>
  <read_first>
    - `src/widgets/PaymentModal/ui/PaymentForm.tsx`: the Process Payment / split button (~L998-1021, currently `POSButton touchSize="xl"` at ~L1002, `disabled={isProcessing || (isSplitMode ? !canSubmitSplit : !canSubmit)}`, `className="w-full bg-[var(--pos-accent)] text-black hover:bg-[var(--pos-accent)]/90"`, label = computed `primaryLabel`); the "Remove payment N" button (~L718-733, `POSButton variant="ghost"`, `aria-label={\`Remove payment ${String(index + 1)}\`}`, `className="px-2 text-destructive"`, `Trash2` child, currently NO `touchSize`); the raw `<button>` "Reset to computed" (~L955-963, `data-testid="card-override-reset"`, `className="text-xs text-muted-foreground underline"`); the Cancel button (~L1026, `touchSize="large"` — leave untouched). `POSButton` is ALREADY imported (~L27).
    - `src/widgets/PaymentModal/ui/PaymentForm.test.tsx` — asserts `getByTestId('card-override-reset')` (L270-299) and `getByRole('button', { name: 'Remove payment N' })` (L399-414). These must keep resolving after the swap; do NOT change the testid or aria-label text.
    - `33-PATTERNS.md` § "src/widgets/PaymentModal/ui/PaymentForm.tsx" — target markup for all three (Pattern A/B/C-not-applicable, Pattern D none here).
    - `33-RESEARCH.md` § "3. src/widgets/PaymentModal/ui/PaymentForm.tsx" — full data-testid + accessible-name inventory that must survive verbatim; note that the Remove-payment button has NO explicit `size="icon-*"` so its width is default padding (no width fix needed — unlike SplitTabSheet's Remove check).
    - `33-UI-SPEC.md` Component Inventory rows for PaymentForm + "Focus Ring Contract" (targets 1 and 2) + "Variant Decisions Required" (Reset-to-computed).
    - `src/shared/ui/button.tsx` — confirms `focusEmphasis="high"` → `focus-visible:ring-4 focus-visible:ring-ring`, and that `variant="ghost"` avoids the `variant="default"` solid `bg-primary` background.
  </read_first>
  <action>
    Three edits, nothing else in the file changes:
    (1) Process Payment / split-final-confirm (~L1002, D-03 items 1+4 + D-05): ADD the single prop `focusEmphasis="high"` to the existing `POSButton`. Do NOT touch `touchSize="xl"`, the `disabled` expression, the `className` (keep `bg-[var(--pos-accent)]` verbatim), the `onClick`, or the computed `primaryLabel` child.
    (2) Remove payment N (~L718-733, D-03 item 5 + D-05 — BOTH remove-leg targets are in scope per CONTEXT.md D-03 correction): ADD `touchSize="xl"` and `focusEmphasis="high"` to the existing `POSButton variant="ghost"`. Keep `variant="ghost"`, the `aria-label` template, `disabled={isProcessing}`, the `className="px-2 text-destructive"`, the `onClick` dispatching `REMOVE_ROW`, and the `Trash2` child verbatim. (No width fix needed — this button has no `size="icon-*"`; its width comes from default padding around the icon.)
    (3) Reset to computed (~L955-963, D-01 raw-button conversion, floor tier): change `<button>`/`</button>` to `<POSButton>`/`</POSButton>`, add `variant="ghost"` (per D-03b — avoids the solid `bg-primary` default on this transparent text-link) and `touchSize="default"`. Preserve `type="button"`, `data-testid="card-override-reset"`, the `className="text-xs text-muted-foreground underline"`, the `onClick` calling `setCardChargeOverride(null)`, and the `Reset to computed (${runningTotal.toFixed(2)})` child text verbatim.
    Do NOT touch: Cancel (~L1026, `touchSize="large"`), tip-preset buttons, discount scope/type selectors, or payment-method selectors — none are named in D-03/D-04 for this phase.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx && npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/05-payments.spec.ts e2e/41-split-payment.spec.ts e2e/42-tip-distribution.spec.ts` (live .env.local creds). All green. Visually confirm the Process Payment button shows a thicker (ring-4) focus ring on keyboard focus and the accent background is unchanged.</human-check>
  </verify>
  <acceptance_criteria>
    - Process Payment `POSButton` now has both `touchSize="xl"` and `focusEmphasis="high"`; its `disabled` expression, `bg-[var(--pos-accent)]` className, `onClick`, and `primaryLabel` child are byte-identical to before.
    - Every "Remove payment N" `POSButton` now has `touchSize="xl" focusEmphasis="high"` while keeping `variant="ghost"`, the `Remove payment ${index+1}` aria-label, `disabled={isProcessing}`, and the `REMOVE_ROW` onClick unchanged.
    - "Reset to computed" is a `POSButton variant="ghost" touchSize="default"` with `data-testid="card-override-reset"` preserved (grep `card-override-reset` still matches); `src/widgets/PaymentModal/ui/PaymentForm.tsx` contains no raw `<button` tag (grep `<button` returns 0).
    - `npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx` passes (card-override-reset + Remove payment N assertions green).
    - Cancel button still reads `touchSize="large"` with no `focusEmphasis` (unchanged); no tip/discount/method selector line changed in the diff.
    - `npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Process Payment + Remove-payment-N carry `focusEmphasis="high"` (and `touchSize="xl"` on remove), Reset-to-computed is a ghost POSButton with testid intact, all handlers/labels/disabled preserved verbatim, unit test + typecheck + lint green, 3 gate E2E specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → payment submit / RBAC | The Process Payment button's `disabled` expression enforces submit-validation (canSubmit/canSubmitSplit); onClick triggers the payment mutation. className/prop changes must not alter this gate. |
| client → split-leg state | Remove-payment-N mutates local split-row reducer state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | PaymentForm Process Payment `disabled`/`onClick` (submit gate) | mitigate | Only `focusEmphasis` prop added; `disabled` expression and `onClick` preserved verbatim; `npm run typecheck` catches prop-shape drift; `05/41/42` E2E confirm submit-gate behavior unchanged |
| T-33-03-02 | Tampering | PaymentForm Remove-payment-N `aria-label`/`onClick` | mitigate | `aria-label` + `REMOVE_ROW` onClick preserved verbatim (PaymentForm.test.tsx + `41-split-payment` T3 assert on aria-label) |
| T-33-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Prop-only additions on two existing `POSButton`s + one raw `<button>` → `POSButton` swap. No new files, types, exports, functions, or CSS tokens.

<verification>
- `npx vitest run src/widgets/PaymentModal/ui/PaymentForm.test.tsx` passes.
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- No raw `<button` remains in `PaymentForm.tsx`.
- E2E gate (live creds): `e2e/05-payments.spec.ts`, `e2e/41-split-payment.spec.ts`, `e2e/42-tip-distribution.spec.ts` pass unchanged (Success Criterion 3).
</verification>

<success_criteria>
- Process Payment / split-confirm + Remove-payment-N carry the 72px/high-ring tier (D-03/D-05); Reset-to-computed standardized to `POSButton` (D-01) — all with zero prop/handler/validation change (COMPONENT-04).
- Lands as its own isolated commit/PR (Success Criterion 2).
- Unit test + typecheck + lint green; 3 payment gate E2E specs unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-03-SUMMARY.md` when done.
</output>
