---
phase: 33-payment-critical-page-sweep-isolated
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/split-tab/ui/SplitTabSheet.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "'Confirm Split' carries touchSize=xl focusEmphasis=high; its disabled={!isValid || isMutating} and handleConfirm onClick are unchanged"
    - "'Remove check N' is a POSButton touchSize=xl focusEmphasis=high rendered as a ~72px square (not a 28px sliver); its aria-label 'Remove check N' and removeAmountRow onClick are unchanged"
    - "'Add check' (item mode) and 'Add person' are POSButton variant=outline touchSize=large per D-01b; their onClick/aria-label/children unchanged"
    - "The Evenly-mode number picker, 'Keep tab open', and the Amount-mode row-add 'Add check' (sm) remain unchanged"
  artifacts:
    - path: "src/features/split-tab/ui/SplitTabSheet.tsx"
      provides: "SplitTabSheet buttons standardized per D-01b/D-03/D-04/D-05"
      contains: "focusEmphasis=\"high\""
  key_links:
    - from: "SplitTabSheet Confirm Split"
      to: "handleConfirm"
      via: "onClick (unchanged)"
      pattern: "handleConfirm"
    - from: "SplitTabSheet Remove check"
      to: "removeAmountRow"
      via: "onClick (unchanged)"
      pattern: "removeAmountRow"
---

<objective>
Standardize four in-scope buttons in `src/features/split-tab/ui/SplitTabSheet.tsx` per the locked decisions, satisfying COMPONENT-04 with markup/class-only diffs. Zero prop/handler/validation behavior change (Success Criterion 1).

Purpose: Per D-03/D-05, "Confirm Split" (split-final-confirm for the split-TAB feature) and "Remove check" (remove-leg) get the 72px + high-ring tier. Per D-01b/D-04 (locked correction to the stale Phase-31 state), the item-mode "Add check" and person-mode "Add person" dashed-tile buttons convert from plain shadcn `Button` to `POSButton touchSize="large"`.
IMPORTANT SCOPE NOTE: `33-UI-SPEC.md`'s Component Inventory marks "Add check"/"Add person" as "No change" — that row is STALE. CONTEXT.md D-01b (the authoritative locked decision, dated 2026-07-13) and `33-PATTERNS.md` both direct converting them. Follow CONTEXT.md D-01b.
Output: One-file isolated PR/commit; secondary `34-split-bill` behavior unchanged; typecheck+lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/features/split-tab/ui/SplitTabSheet.tsx
@src/shared/ui/POSButton.tsx
@src/shared/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Split + Remove check → xl/high; Add check + Add person → POSButton large</name>
  <files>src/features/split-tab/ui/SplitTabSheet.tsx</files>
  <read_first>
    - `src/features/split-tab/ui/SplitTabSheet.tsx` at these exact sites (line numbers approximate; match on the described markup, not the number):
      • "Add check" ITEM mode (~L597-606): `<Button variant="outline" type="button" className="min-w-[140px] ... border-dashed ..." onClick={addItemColumn} aria-label="Add sub-check column">` with `<Plus/>` + `<span>Add check</span>` children → CONVERT to `POSButton variant="outline" touchSize="large"` (D-01b/D-04).
      • "Add person" PERSON mode (~L694-703): same dashed-tile shape, `onClick={addPersonColumn}`, `aria-label="Add person"` → CONVERT to `POSButton variant="outline" touchSize="large"` (D-01b/D-04).
      • "Remove check" AMOUNT mode (~L737-747): `<Button variant="ghost" size="icon-sm" type="button" onClick={() => { removeAmountRow(row.id); }} aria-label={\`Remove check ${String(i + 1)}\`}>` with `<X/>` child → CONVERT to `POSButton` at the 72px tier + WIDTH FIX (see action; D-03 item 5 + D-05).
      • "Confirm Split" (~L792-800): `<POSButton touchSize="large" className="flex-1" disabled={!isValid || isMutating} onClick={() => { void handleConfirm(); }}>Confirm Split</POSButton>` → PROP-ONLY bump to `touchSize="xl" focusEmphasis="high"` (D-03 item 4 + D-05).
      • LEAVE UNTOUCHED: Evenly-mode number picker (~L487, already `POSButton touchSize="xl"`, not named), "Keep tab open" cancel (~L789, plain `Button` — not named in D-01b), and the Amount-mode row-add "Add check" (`Button variant="outline" size="sm" onClick={addAmountRow}`, ~L751 — NOT named in D-01b; this is a DIFFERENT button from the item-mode "Add check").
    - `src/shared/ui/button.tsx` lines 23-35 — `size="icon-sm"` → `size-7` (fixes BOTH width AND height to 28px). This is why the width fix is required (Pitfall 1).
    - `src/shared/ui/POSButton.tsx` — `touchSize` sets `min-height` ONLY, never width.
    - `33-PATTERNS.md` § "src/features/split-tab/ui/SplitTabSheet.tsx" — target markup incl. the D-01b addition and the width-fix example.
    - `33-RESEARCH.md` § "6." + Pitfall 1 (icon-sm + touchSize=xl sliver) + Pitfall 2 (these are plain `Button`, NOT `POSButton` — a prop-only edit would fail typecheck).
    - `33-UI-SPEC.md` "Sizing Fix Required" + "Focus Ring Contract" (targets 5 and 6).
    - `33-CONTEXT.md` D-01b + D-03 item 5 + D-04 (authoritative — overrides UI-SPEC's stale "No change" rows for Add check/Add person).
  </read_first>
  <action>
    Both `Button` and `POSButton` are already imported in this file — keep both imports (some plain-`Button` sites remain in scope-excluded buttons). Four edits:
    (1) "Add check" (item mode, ~L597): change `<Button ...>`/`</Button>` to `<POSButton variant="outline" touchSize="large" ...>`/`</POSButton>`. Keep `type="button"`, the dashed-tile `className`, `onClick={addItemColumn}`, `aria-label="Add sub-check column"`, and the `<Plus/>`+`<span>Add check</span>` children verbatim.
    (2) "Add person" (~L694): same swap to `<POSButton variant="outline" touchSize="large">`, preserving `onClick={addPersonColumn}`, `aria-label="Add person"`, className, and children verbatim.
    (3) "Remove check" (~L737): change `<Button variant="ghost" size="icon-sm" ...>`/`</Button>` to `<POSButton variant="ghost" touchSize="xl" focusEmphasis="high" type="button" className="w-[72px]" ...>`/`</POSButton>`. DROP `size="icon-sm"` and ADD `className="w-[72px]"` so the target is a true ~72x72 square (Pitfall 1 — `touchSize="xl"` sets `min-h-[72px]` only; without the width fix, the retained `size-7` would leave a 28px-wide, 72px-tall sliver). Keep `onClick={() => { removeAmountRow(row.id); }}`, the `aria-label={\`Remove check ${String(i + 1)}\`}`, and the `<X className="h-4 w-4 text-muted-foreground hover:text-destructive" />` child verbatim. (Document in the commit body that the 72x72 square was chosen per D-03/D-05.)
    (4) "Confirm Split" (~L792): change ONLY `touchSize="large"` → `touchSize="xl"` and ADD `focusEmphasis="high"`. Keep `className="flex-1"`, `disabled={!isValid || isMutating}`, the `void handleConfirm()` onClick, and the `Confirm Split` text verbatim.
    Do NOT touch the Evenly picker, "Keep tab open", or the Amount-mode row-add "Add check" (sm).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/34-split-bill.spec.ts` (live .env.local creds; secondary spec). Green. Visually confirm: "Remove check" X button renders as a ~72px square (not a tall narrow sliver); "Confirm Split" shows the thicker (ring-4) focus ring; "Add check"/"Add person" dashed tiles look unchanged apart from the touch-height bump.</human-check>
  </verify>
  <acceptance_criteria>
    - "Confirm Split" `POSButton` reads `touchSize="xl" focusEmphasis="high"`; its `disabled={!isValid || isMutating}` and `handleConfirm` onClick are byte-identical to before.
    - "Remove check" is `<POSButton variant="ghost" touchSize="xl" focusEmphasis="high"` with `className="w-[72px]"` and NO `size="icon-sm"` (grep `size="icon-sm"` returns 0 for this button); `aria-label={\`Remove check ${String(i + 1)}\`}` and `removeAmountRow` onClick unchanged.
    - "Add check" (item mode) and "Add person" are `<POSButton variant="outline" touchSize="large"` with their `addItemColumn`/`addPersonColumn` onClick, aria-labels, and `<Plus/>+<span>` children unchanged.
    - The Evenly picker, "Keep tab open", and the Amount-mode sm "Add check" (`addAmountRow`) are unchanged in the diff (grep confirms they remain plain `Button`/existing `POSButton`).
    - `npm run typecheck` exits 0 (proves the `Button`→`POSButton` swap is complete for the 3 converted sites — Pitfall 2: `touchSize` on a plain `Button` would fail here), `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Confirm Split + Remove check at xl/high (Remove check as a 72px square), Add check + Add person as `POSButton large`, all handlers/aria/children preserved verbatim, out-of-scope buttons untouched, typecheck+lint green, split-bill E2E unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → split-tab confirm | "Confirm Split" `disabled` gate enforces validity before committing the tab split; onClick triggers `handleConfirm`. className/prop changes must not alter this gate. |
| client → local split-row state | "Remove check"/"Add check"/"Add person" mutate local split-column state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06-01 | Tampering | SplitTabSheet "Confirm Split" `disabled`/`onClick` (split-commit gate) | mitigate | Only `touchSize`/`focusEmphasis` change; `disabled` + `handleConfirm` onClick preserved verbatim; `npm run typecheck` + `34-split-bill` E2E confirm no drift |
| T-33-06-02 | Tampering | Incomplete `Button`→`POSButton` swap leaving a stale `Button` at a converted site | mitigate | `npm run typecheck` fails if `touchSize` is applied to a plain `Button` (Pitfall 2); grep-confirm each converted site |
| T-33-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Three `Button`→`POSButton` swaps + one prop-only bump on an existing `POSButton`. No new files, types, exports, functions, or CSS tokens.

<verification>
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- Converted sites are `POSButton`; out-of-scope buttons unchanged; "Remove check" is a 72px square.
- E2E (live creds): secondary `e2e/34-split-bill.spec.ts` passes unchanged; no required gate spec exercises this file directly, but zero-behavior-change still holds (Success Criterion 1).
</verification>

<success_criteria>
- SplitTabSheet standardized per D-01b/D-03/D-04/D-05 with zero prop/handler/validation change (COMPONENT-04).
- Lands as its own isolated commit/PR (Success Criterion 2).
- typecheck + lint green; split-bill E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-06-SUMMARY.md` when done.
</output>
