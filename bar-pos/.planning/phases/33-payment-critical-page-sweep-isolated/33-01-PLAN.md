---
phase: 33-payment-critical-page-sweep-isolated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/pos/index.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "The POS order-panel toggle is a POSButton, still toggles the order panel, still exposes data-testid=pos-order-panel-toggle and the same dynamic aria-label/aria-pressed"
    - "The toggle meets the 44px touch floor as a square target (not a 36px non-square button)"
    - "The toggle keeps its transparent/bordered visual style (no solid bg-primary background introduced)"
  artifacts:
    - path: "src/pages/pos/index.tsx"
      provides: "POS page with panel toggle converted to POSButton (variant=ghost, touchSize=default)"
      contains: "POSButton"
  key_links:
    - from: "src/pages/pos/index.tsx panel toggle"
      to: "setOrderPanelCollapsed"
      via: "onClick handler (unchanged)"
      pattern: "setOrderPanelCollapsed"
---

<objective>
Convert the single raw `<button>` panel toggle in `src/pages/pos/index.tsx` (currently lines ~57-72, `data-testid="pos-order-panel-toggle"`) to `POSButton` per D-01, satisfying COMPONENT-04's markup/class-only swap on this payment-critical page. Zero prop/handler/validation behavior change (Success Criterion 1).

Purpose: This is the POS page's only remaining raw `<button>` from the Phase 29 drift audit. It is NOT one of D-03's 5 critical money actions — it is an icon-only panel toggle, so it gets the 44px floor (`touchSize="default"`), not `large`/`xl`.
Output: One-file isolated PR/commit converting the toggle to `POSButton`, all E2E specs green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/pages/pos/index.tsx
@src/shared/ui/POSButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert POS panel toggle to POSButton (variant=ghost, touchSize=default, width fix)</name>
  <files>src/pages/pos/index.tsx</files>
  <read_first>
    - `src/pages/pos/index.tsx` lines 54-98 — the raw `<button>` panel toggle (~57-72) AND the already-converted `Close Tab / Pay` POSButton (~87) which is the in-file precedent for import/usage. `POSButton` is ALREADY imported at line 18 (`import { PageContainer, POSButton, ProtectedAction } from '@shared/ui';`) — do NOT add a duplicate import.
    - `33-PATTERNS.md` § "src/pages/pos/index.tsx" (Pattern A + B, width-fix note) — the exact target markup.
    - `33-RESEARCH.md` § "1. src/pages/pos/index.tsx" — the h-9 w-9 below-floor note and E2E dependency (`e2e/29-panel-toggle.spec.ts` uses `getByTestId('pos-order-panel-toggle')`).
    - `33-UI-SPEC.md` Component Inventory row for pos/index.tsx + "Variant Decisions Required" + "Sizing Fix Required" rows.
    - `src/shared/ui/POSButton.tsx` — confirms `touchSize="default"` → `min-h-[44px]` (height only, never width) and that all other props forward through to `Button`.
  </read_first>
  <action>
    Change the `<button>` opening/closing tags (~L57/L72) to `<POSButton>`/`</POSButton>`. Add `variant="ghost"` explicitly (per D-03b/UI-SPEC "Variant Decisions Required" — the raw button has no CVA styling, so omitting variant would silently apply `variant="default"`'s solid `bg-primary` background, a forbidden visual change). Add `touchSize="default"` (D-04 floor tier — this is NOT a D-03 critical action, so no `focusEmphasis`). In the existing `className`, replace the fixed `h-9 w-9` with `w-11` (44px) so the target is a true 44x44 square (per D-03a: `touchSize` sets `min-h-[44px]` for height, but width must be fixed explicitly — leaving `w-9`/36px yields a non-square target). Keep the rest of the className byte-identical: `absolute right-3 top-3 z-10 flex items-center justify-center rounded-md border bg-background/80 shadow-sm hover:bg-accent`. Preserve VERBATIM: `type="button"`, `data-testid="pos-order-panel-toggle"`, the dynamic `aria-label`, `aria-pressed={orderPanelCollapsed}`, the `onClick` calling `setOrderPanelCollapsed`, and the `PanelRightOpen`/`PanelRightClose` icon children. Touch nothing else in the file (the `Close Tab / Pay` POSButton at ~L87 stays as-is).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/29-panel-toggle.spec.ts` and `npx playwright test e2e/05-payments.spec.ts` (requires live .env.local Supabase creds). Both green. Visually confirm the toggle is a ~44px square with its transparent/bordered look intact (no solid colored fill).</human-check>
  </verify>
  <acceptance_criteria>
    - `src/pages/pos/index.tsx` contains no raw `<button` tag (grep `<button` returns 0 matches in this file).
    - The toggle renders as `<POSButton variant="ghost" touchSize="default"` with `data-testid="pos-order-panel-toggle"` still present (grep `pos-order-panel-toggle` still matches).
    - The className no longer contains `h-9 w-9`; it contains `w-11` (grep `w-11` matches, grep `h-9 w-9` returns 0).
    - `aria-pressed={orderPanelCollapsed}`, the dynamic `aria-label`, and the `onClick` calling `setOrderPanelCollapsed` are byte-identical to before (unchanged in diff).
    - `POSButton` import at line 18 is unchanged (no duplicate import added).
    - `npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Panel toggle is a `POSButton variant="ghost" touchSize="default"` 44x44 square, testid/aria/onClick preserved verbatim, typecheck+lint green, panel-toggle + payments E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → order-panel UI state | Toggle mutates local `orderPanelCollapsed` state only; no server/RBAC/validation logic involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | pos/index.tsx toggle `onClick`/`aria-*`/`data-testid` | mitigate | Only tag name + `variant`/`touchSize`/`className` change; `onClick`/`aria-label`/`aria-pressed`/`data-testid` preserved verbatim; `npm run typecheck` + `29-panel-toggle`/`05-payments` E2E confirm no drift |
| T-33-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring; RESEARCH.md §Package Legitimacy Audit confirms zero new packages |
</threat_model>

## Artifacts this phase produces

No new symbols. Pure primitive swap: raw `<button>` → existing `POSButton` (shipped Phase 30-32). No new files, types, exports, functions, or CSS tokens.

<verification>
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- No raw `<button` remains in `src/pages/pos/index.tsx`.
- E2E gate (live creds): `e2e/05-payments.spec.ts` and secondary `e2e/29-panel-toggle.spec.ts` pass unchanged (Success Criterion 3).
</verification>

<success_criteria>
- POS panel toggle standardized to `POSButton` (shell/token/component/touch/focus) with zero prop/handler/validation change (COMPONENT-04, D-01).
- Lands as its own isolated commit/PR (Success Criterion 2).
- typecheck + lint green; payments/panel-toggle E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-01-SUMMARY.md` when done.
</output>
