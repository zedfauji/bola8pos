---
phase: 33-payment-critical-page-sweep-isolated
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/void-order/ui/VoidOrderDialog.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "The Void order confirm button carries the 72px/high-ring treatment via ConfirmDialog's confirmClassName; its confirmLabel 'Void order', onConfirm, title, and reason input are all unchanged"
    - "The confirmClassName literal matches StopSessionConfirm/StopAndMoveDialog verbatim"
    - "VoidOrderDialog.test.tsx stays green"
  artifacts:
    - path: "src/features/void-order/ui/VoidOrderDialog.tsx"
      provides: "VoidOrderDialog with confirmClassName applied per D-03 item 3 / D-05"
      contains: "confirmClassName"
  key_links:
    - from: "VoidOrderDialog ConfirmDialog"
      to: "onConfirm (void mutation)"
      via: "unchanged — only confirmClassName added"
      pattern: "onConfirm"
---

<objective>
Add the verbatim `confirmClassName` 72px/high-ring literal to the `ConfirmDialog` in `src/features/void-order/ui/VoidOrderDialog.tsx`, satisfying D-03 item 3 ("Void order confirm") + D-05 and COMPONENT-04 with a single prop addition. Zero prop/handler/validation behavior change (Success Criterion 1).

Purpose: D-03 names "Void order confirm" as one of the critical 72px+ring actions, but no file in the original 6-file scope contained it — CONTEXT.md D-01 added `VoidOrderDialog.tsx` as the 7th in-scope file for exactly this. It already uses `ConfirmDialog`; the `confirmClassName` passthrough (added in Phase 32, used by `StopSessionConfirm.tsx`/`StopAndMoveDialog.tsx`) makes this a 1-line, fully-isolated, zero-risk addition. This file is exercised by the `09-rbac` gate spec (void-order flow) by accessible name only, so `confirmClassName` cannot affect it.
Output: One-file isolated PR/commit; `09-rbac` gate + co-located VoidOrderDialog.test.tsx green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/features/void-order/ui/VoidOrderDialog.tsx
@src/features/void-order/ui/VoidOrderDialog.test.tsx
@src/shared/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verbatim confirmClassName to VoidOrderDialog's ConfirmDialog</name>
  <files>src/features/void-order/ui/VoidOrderDialog.tsx</files>
  <read_first>
    - `src/features/void-order/ui/VoidOrderDialog.tsx` — the `<ConfirmDialog>` call (~L55-126): `title="Void order?"`, `confirmLabel="Void order"`, `onConfirm={async () => {...}}`, plus the reason `Input`. Confirm it currently has NO `confirmClassName` prop.
    - `src/shared/ui/ConfirmDialog.tsx` — confirms `confirmClassName?: string` is an opt-in prop merged via `cn()` onto the `AlertDialogAction` (confirm button) only; no default behavior change.
    - `src/features/stop-pool-timer/ui/StopSessionConfirm.tsx` AND `src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx` — the two Phase-32 call sites carrying the exact `confirmClassName` literal to copy verbatim (do not paraphrase).
    - `33-PATTERNS.md` § "src/features/void-order/ui/VoidOrderDialog.tsx" (Pattern C) — the EXACT literal.
    - `33-RESEARCH.md` § "Don't Hand-Roll" + Pitfall 4 — `09-rbac.spec.ts` asserts `getByRole('button', { name: /void order/i })` (accessible name, unaffected by className).
    - `src/features/void-order/ui/VoidOrderDialog.test.tsx` — co-located unit test that must stay green.
  </read_first>
  <action>
    Add exactly one prop to the existing `<ConfirmDialog>` element: `confirmClassName="min-h-[72px] text-lg font-semibold focus-visible:ring-4 focus-visible:ring-ring"` — copied VERBATIM from `src/features/stop-pool-timer/ui/StopSessionConfirm.tsx` / `src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx` (D-03 item 3 + D-05). Change nothing else: `title`, `confirmLabel="Void order"`, `onConfirm`, `open`/`onOpenChange`, the reason `Input`, and all other props/children stay byte-identical. No new import is needed (`ConfirmDialog` is already imported at ~L5).
  </action>
  <verify>
    <automated>npx vitest run src/features/void-order/ui/VoidOrderDialog.test.tsx && npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/09-rbac.spec.ts` (live .env.local creds). Green. Confirm the Void order confirm button is a 72px tall button with a thicker (ring-4) focus ring; the confirm label still reads "Void order" and voiding still works.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/features/void-order/ui/VoidOrderDialog.tsx` contains `confirmClassName="min-h-[72px] text-lg font-semibold focus-visible:ring-4 focus-visible:ring-ring"` on the `ConfirmDialog` (grep matches the exact literal).
    - `confirmLabel="Void order"`, `title`, and `onConfirm` are byte-identical to before (unchanged in diff).
    - The literal matches the one in `src/features/stop-pool-timer/ui/StopSessionConfirm.tsx` and `src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx` character-for-character.
    - `npx vitest run src/features/void-order/ui/VoidOrderDialog.test.tsx` passes.
    - `npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>VoidOrderDialog's ConfirmDialog carries the verbatim 72px/high-ring `confirmClassName`, all other props preserved verbatim, unit test + typecheck + lint green, rbac E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → void-order (RBAC-gated) | Void order is an RBAC-gated destructive action; its `onConfirm`/`confirmLabel`/gating must not change — only the confirm button's className is added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07-01 | Tampering | VoidOrderDialog `onConfirm`/`confirmLabel` (void action) | mitigate | Only `confirmClassName` prop added; `onConfirm`/`confirmLabel`/`title` preserved verbatim; `VoidOrderDialog.test.tsx` + `09-rbac` E2E (accessible-name assertion) confirm no drift |
| T-33-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Single prop addition (`confirmClassName`) on an existing `ConfirmDialog` call. No new files, types, exports, functions, or CSS tokens.

<verification>
- `npx vitest run src/features/void-order/ui/VoidOrderDialog.test.tsx` passes.
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- `confirmClassName` literal present and matches the two Phase-32 call sites verbatim.
- E2E gate (live creds): `e2e/09-rbac.spec.ts` passes unchanged (Success Criterion 3).
</verification>

<success_criteria>
- Void order confirm carries the 72px/high-ring tier (D-03 item 3 / D-05) with zero prop/handler/validation change (COMPONENT-04).
- Lands as its own isolated commit/PR (Success Criterion 2).
- Unit test + typecheck + lint green; rbac E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-07-SUMMARY.md` when done.
</output>
