---
phase: 33-payment-critical-page-sweep-isolated
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/process-refund/ui/RefundSheet.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "'Request approval' is a POSButton touchSize=xl focusEmphasis=high; it still opens the ManagerPinDialog (setPinOpen(true)), still respects disabled={!isValid || mutation.isPending}, still reads 'Request approval'"
    - "'Close refund' is a POSButton variant=outline touchSize=large; still closes the sheet, still reads 'Close refund'"
    - "The dead hand-rolled min-h-[56px] on Request approval is removed (touchSize=xl supersedes it)"
    - "The file no longer imports shadcn Button (fully swapped to POSButton)"
  artifacts:
    - path: "src/features/process-refund/ui/RefundSheet.tsx"
      provides: "RefundSheet footer converted to POSButton per D-03/D-04/D-05"
      contains: "POSButton"
  key_links:
    - from: "RefundSheet Request approval"
      to: "setPinOpen(true)"
      via: "onClick (unchanged) — opens PIN gate, does NOT submit refund"
      pattern: "setPinOpen"
---

<objective>
Swap both footer `Button`s in `src/features/process-refund/ui/RefundSheet.tsx` from raw shadcn `Button` to `POSButton`, applying the D-03/D-04/D-05 tiers, satisfying COMPONENT-04 with markup/class-only diffs. Zero prop/handler/validation behavior change (Success Criterion 1). This is the one in-scope file needing a full primitive swap (import change), not just prop additions.

Purpose: "Request approval" is D-03 item 2 (Refund confirm) → 72px + high ring; "Close refund" is its cancel pair → D-04 `large`. Note: "Request approval" opens the ManagerPinDialog PIN gate — it does NOT itself submit the refund (submission happens in `handleSubmitRefund` from the PIN dialog's onSuccess, out of scope).
Output: One-file isolated PR/commit; `09-rbac` gate + secondary `35-refund` green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/features/process-refund/ui/RefundSheet.tsx
@src/shared/ui/POSButton.tsx
@src/shared/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap RefundSheet footer Button→POSButton (Close refund=large, Request approval=xl+high, drop dead min-h)</name>
  <files>src/features/process-refund/ui/RefundSheet.tsx</files>
  <read_first>
    - `src/features/process-refund/ui/RefundSheet.tsx`: the import line `import { Button, MoneyDisplay, QuantityControl } from "@shared/ui";` (~L14) and the `SheetFooter` block (~L307-320): "Close refund" (`Button variant="outline" className="flex-1"`, onClick `onOpenChange(false)`) and "Request approval" (`Button className="flex-1 min-h-[56px]"`, `disabled={!isValid || mutation.isPending}`, onClick `setPinOpen(true)`). CONFIRM via grep that `Button` is used ONLY in these two places (RESEARCH.md confirms no other `Button` usage in this file) before removing it from the import.
    - `33-PATTERNS.md` § "src/features/process-refund/ui/RefundSheet.tsx" — exact before/after import + both buttons (Pattern A/B + Pattern D delete dead `min-h-[56px]`).
    - `33-RESEARCH.md` § "5. src/features/process-refund/ui/RefundSheet.tsx" — the "opens PIN gate, does not submit" note and `35-refund.spec.ts` accessible-name assertions (`/request approval/i`).
    - `33-UI-SPEC.md` Component Inventory rows for RefundSheet + Focus Ring Contract target 3.
    - `src/shared/ui/button.tsx` — `focusEmphasis="high"` string; confirms "Request approval" was already unstyled `variant="default"` so no variant ambiguity (default matches).
  </read_first>
  <action>
    Change the import to `import { MoneyDisplay, POSButton, QuantityControl } from "@shared/ui";` (alphabetical; drop `Button`, add `POSButton`) — only valid because `Button` is used nowhere else in the file.
    "Close refund" (~L307): change `<Button variant="outline" className="flex-1" onClick={...}>` to `<POSButton variant="outline" touchSize="large" className="flex-1" onClick={...}>` (D-04 cancel/back tier). Keep `variant="outline"`, `className="flex-1"`, the `onOpenChange(false)` onClick, and the text `Close refund` verbatim.
    "Request approval" (~L314): change `<Button className="flex-1 min-h-[56px]" disabled={...} onClick={...}>` to `<POSButton touchSize="xl" focusEmphasis="high" className="flex-1" disabled={...} onClick={...}>` (D-03 item 2 + D-05). DELETE the hand-rolled `min-h-[56px]` from the className (Pattern D — `touchSize="xl"` → `min-h-[72px]` supersedes it; leaving both is dead conflicting weight). Keep `disabled={!isValid || mutation.isPending}`, the `setPinOpen(true)` onClick, and the text `Request approval` byte-identical. Do NOT change either button's onClick/disabled/text.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/09-rbac.spec.ts` and `npx playwright test e2e/35-refund.spec.ts` (live .env.local creds). Both green. Confirm "Request approval" opens the manager PIN dialog (does not submit the refund directly) and shows the thicker (ring-4) focus ring; "Close refund" closes the sheet.</human-check>
  </verify>
  <acceptance_criteria>
    - The import no longer includes `Button`; it imports `MoneyDisplay, POSButton, QuantityControl` (grep the import line; grep `<Button` returns 0 matches in the file).
    - "Close refund" is `<POSButton variant="outline" touchSize="large"` with text `Close refund` and `onOpenChange(false)` onClick unchanged.
    - "Request approval" is `<POSButton touchSize="xl" focusEmphasis="high"` with NO `min-h-[56px]` in its className (grep `min-h-[56px]` returns 0), and `disabled={!isValid || mutation.isPending}` + `setPinOpen(true)` onClick + text `Request approval` byte-identical.
    - `npm run typecheck` exits 0 (proves `touchSize`/`focusEmphasis` are valid on the swapped element — catches the Pitfall-2 "touchSize doesn't exist on Button" error class), `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Both footer buttons are POSButtons (Close refund=outline/large, Request approval=xl/high), dead `min-h-[56px]` deleted, `Button` import removed, handlers/labels/disabled preserved verbatim, typecheck+lint green, rbac + refund E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → manager PIN gate (RBAC) | "Request approval" opens the ManagerPinDialog which gates the refund behind a manager PIN; its `disabled`/`onClick` must not change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Elevation of Privilege | RefundSheet "Request approval" `onClick`/`disabled` (PIN-gate trigger) | mitigate | Only `variant`/`touchSize`/`focusEmphasis`/className change; `setPinOpen(true)` onClick + `disabled` gate preserved verbatim; `09-rbac`/`35-refund` E2E confirm the PIN gate still blocks unauthorized refunds |
| T-33-05-02 | Tampering | Import swap removes `Button` while it is still referenced | mitigate | Grep-confirm `Button` used only in the two footer buttons before dropping from import; `npm run typecheck`/`lint` fail loudly if a stray `Button` reference remains |
| T-33-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Full primitive swap: shadcn `Button` → existing `POSButton` for two footer buttons (+ import change). No new files, types, exports, functions, or CSS tokens.

<verification>
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- No `<Button` and no `min-h-[56px]` remain in `RefundSheet.tsx`.
- E2E gate (live creds): `e2e/09-rbac.spec.ts` + secondary `e2e/35-refund.spec.ts` pass unchanged (Success Criterion 3).
</verification>

<success_criteria>
- Refund footer standardized to `POSButton` with Request approval at the 72px/high-ring tier (D-03/D-05) and Close refund at `large` (D-04), zero prop/handler/validation change (COMPONENT-04); dead `min-h-[56px]` removed.
- Lands as its own isolated commit/PR (Success Criterion 2).
- typecheck + lint green; rbac + refund E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-05-SUMMARY.md` when done.
</output>
