---
phase: 33-payment-critical-page-sweep-isolated
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/PaymentPane/ui/TabPaymentCard.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "The tab-selector card is a POSButton (touchSize=large), still selects the tab on click, still exposes aria-label 'tab {customerName}' and aria-pressed"
    - "The card renders visually identical: full-width, left-aligned content, visible border when unselected, bg-card, hover:bg-accent, primary ring when selected, focus-visible ring on keyboard focus"
    - "The hand-rolled focus-visible:ring-2 duplicate ring is removed in favor of the CVA base ring"
  artifacts:
    - path: "src/widgets/PaymentPane/ui/TabPaymentCard.tsx"
      provides: "TabPaymentCard root converted to POSButton with visual parity preserved"
      contains: "POSButton"
  key_links:
    - from: "TabPaymentCard root button"
      to: "onClick prop"
      via: "onClick passthrough (unchanged)"
      pattern: "onClick={onClick}"
---

<objective>
Convert the root raw `<button>` (the whole selectable card) in `src/widgets/PaymentPane/ui/TabPaymentCard.tsx` to `POSButton touchSize="large"` per D-01/D-04, satisfying COMPONENT-04. Zero prop/handler/validation AND zero visual change (Success Criterion 1).

Purpose: Tab selection is a frequent, non-destructive, non-final action → D-04 `large` (56px) tier. This is the highest-visual-risk swap in the phase because the raw `<button>` is a full-width LAYOUT container, and the base `Button` CVA (`inline-flex items-center justify-center`, `border-transparent`, and `variant="default"`'s `text-primary-foreground`) will fight the card's `text-left`/`bg-card`/visible-border layout unless overridden explicitly. This plan bakes those overrides into the task.
Output: One-file isolated PR/commit; card renders pixel-identical; secondary `17-payment-pane` spec green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/widgets/PaymentPane/ui/TabPaymentCard.tsx
@src/widgets/PaymentPane/ui/PaymentPane.test.tsx
@src/shared/ui/POSButton.tsx
@src/shared/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap TabPaymentCard root to POSButton (variant=ghost, touchSize=large) with visual-parity overrides</name>
  <files>src/widgets/PaymentPane/ui/TabPaymentCard.tsx</files>
  <read_first>
    - `src/widgets/PaymentPane/ui/TabPaymentCard.tsx` (full — ~66 lines) — the root `<button>` (L27-36) with `className={cn('w-full rounded-lg border bg-card p-3 text-left transition-colors hover:bg-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring', selected && 'border-primary ring-1 ring-primary bg-accent')}` and its inner content tree (name/item-count/MoneyDisplay/badges). Note the file imports `MoneyDisplay` from `@shared/ui` and `Badge` from `@shared/ui/badge` — `POSButton` is NOT yet imported.
    - `src/shared/ui/button.tsx` lines 7-47 — CRITICAL: the CVA base string sets `inline-flex items-center justify-center` (centers content — fights `text-left`), `border border-transparent` (makes the border color transparent — fights the card's visible `border`), `focus-visible:ring-3 focus-visible:ring-ring/50` (its own ring), and `size="default"` → `h-8 ... px-2.5`. `variant="default"` adds `bg-primary text-primary-foreground`; `variant="ghost"` adds only `hover:bg-muted hover:text-foreground` (transparent base, no forced text color).
    - `src/shared/ui/POSButton.tsx` — `touchSize="large"` → `min-h-[56px] text-base` (+ `active:scale-95`), forwards all other props to `Button`.
    - `33-PATTERNS.md` § "src/widgets/PaymentPane/ui/TabPaymentCard.tsx" + Pattern D (drop the hand-rolled ring).
    - `33-RESEARCH.md` § "4. src/widgets/PaymentPane/ui/TabPaymentCard.tsx" — the duplicate/conflicting focus-ring note and the `17-payment-pane.spec.ts` aria-label contract.
    - `33-UI-SPEC.md` Component Inventory row for TabPaymentCard + "Variant Decisions Required" (this row: confirm 1:1 screenshot parity).
    - `src/widgets/PaymentPane/ui/PaymentPane.test.tsx` — the co-located unit test that must stay green.
  </read_first>
  <action>
    Add `import { POSButton } from '@shared/ui/POSButton';` (match the existing `@shared/ui`-barrel import style — a dedicated POSButton import path is used elsewhere in the codebase; either the barrel `@shared/ui` or `@shared/ui/POSButton` is acceptable as long as lint passes). Change the root `<button>`/`</button>` to `<POSButton>`/`</POSButton>` with `variant="ghost"` (per D-03b — `default` would add `text-primary-foreground`, changing the card text color) and `touchSize="large"` (D-04 frequent tier). Preserve `type="button"`, `aria-label={\`tab ${tab.customerName}\`}`, `aria-pressed={selected}`, `onClick={onClick}`, and the entire inner content tree VERBATIM.
    Rework the `className` to achieve pixel parity against the base CVA (this is the visual-parity crux — the raw button had NO CVA styling, `POSButton` brings the base CVA):
    - DROP the hand-rolled `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` (Pattern D — the CVA base supplies `focus-visible:ring-3 focus-visible:ring-ring/50`).
    - ADD `justify-start text-left` (override the CVA base's `justify-center`/`items-center` centering so the content stays left-aligned and top-aligned as before — verify the inner content still lays out full-width; add `w-full` to the inner wrapper div if the single child otherwise shrink-wraps under the flex container).
    - ADD `border-border` (override the CVA base's `border-transparent` so the unselected card keeps its visible border; `border-primary` in the `selected` branch already overrides this when selected).
    - KEEP `w-full rounded-lg border bg-card p-3 transition-colors hover:bg-accent` and the `selected && 'border-primary ring-1 ring-primary bg-accent'` branch byte-identical.
    Do NOT alter any child, `MoneyDisplay`, `Badge`, `aria-*`, or `onClick`.
  </action>
  <verify>
    <automated>npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx && npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/17-payment-pane.spec.ts` and `npx playwright test e2e/05-payments.spec.ts` (live .env.local creds). Green. MANDATORY visual parity: side-by-side before/after — card is full-width, content left-aligned (NOT centered), border visible when unselected, bg-card, hover:bg-accent on hover, primary ring when selected, ring on keyboard focus, text color unchanged (not primary-foreground).</human-check>
  </verify>
  <acceptance_criteria>
    - `src/widgets/PaymentPane/ui/TabPaymentCard.tsx` contains no raw `<button` tag; root element is `<POSButton variant="ghost" touchSize="large"` (grep `<button` returns 0, grep `POSButton` matches).
    - `aria-label={\`tab ${tab.customerName}\`}`, `aria-pressed={selected}`, and `onClick={onClick}` are byte-identical to before (unchanged in diff); `17-payment-pane.spec.ts` `getByRole('button', {name: /tab .../i})` still resolves.
    - The className no longer contains `focus-visible:ring-2` (grep returns 0); it contains `border-border` and `justify-start` (grep matches) to preserve visible border + left alignment against the CVA base.
    - `npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx` passes.
    - Visual parity confirmed (human-check): full-width, left-aligned content, visible unselected border, unchanged text color, focus ring present.
    - `npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>TabPaymentCard root is a `POSButton variant="ghost" touchSize="large"` with visual parity (left-aligned, visible border, correct text color, CVA ring), aria/onClick preserved verbatim, hand-rolled ring removed, unit test + typecheck + lint green, payment-pane E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tab selection state | Card click sets the selected tab in the PaymentPane; no server/RBAC/validation logic involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Tampering | TabPaymentCard `onClick`/`aria-label`/`aria-pressed` | mitigate | Only tag name + `variant`/`touchSize`/`className` change; `onClick`/`aria-*` preserved verbatim; `PaymentPane.test.tsx` + `17-payment-pane` E2E confirm selection contract intact |
| T-33-04-02 | Tampering | Accidental visual regression (CVA centering/border/text-color fight) | mitigate | Explicit `variant="ghost"` + `justify-start text-left` + `border-border` overrides + drop dead ring; mandatory before/after visual-parity human-check |
| T-33-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Pure primitive swap: raw `<button>` → existing `POSButton` (+ one import line). No new files, types, exports, functions, or CSS tokens.

<verification>
- `npx vitest run src/widgets/PaymentPane/ui/PaymentPane.test.tsx` passes.
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- No raw `<button` remains in `TabPaymentCard.tsx`; visual parity confirmed.
- E2E gate (live creds): `e2e/05-payments.spec.ts` + secondary `e2e/17-payment-pane.spec.ts` pass unchanged (Success Criterion 3).
</verification>

<success_criteria>
- TabPaymentCard standardized to `POSButton touchSize="large"` (D-01/D-04) with zero prop/handler/validation AND zero visual change (COMPONENT-04).
- Lands as its own isolated commit/PR (Success Criterion 2).
- Unit test + typecheck + lint green; payment-pane E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-04-SUMMARY.md` when done.
</output>
