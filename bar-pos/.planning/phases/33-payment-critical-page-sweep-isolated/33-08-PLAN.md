---
phase: 33-payment-critical-page-sweep-isolated
plan: 08
type: execute
wave: 2
depends_on: [33-01, 33-02, 33-03, 33-04, 33-05, 33-06, 33-07]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "After all 7 payment-surface swaps, npm run typecheck + npm run lint + npm run test (unit) are all green with no new failures vs the pre-phase baseline"
    - "All 5 required gate E2E specs (05-payments, 41-split-payment, 42-tip-distribution, 06-transfer, 09-rbac) pass unchanged"
    - "The D-01a out-of-scope files (TabDrawer, TipDistributionPanel, TipDistributionSettingsTab) are confirmed untouched"
    - "CLAUDE.md Implemented Features records the Phase 33 payment-critical standardization"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Implemented Features entry for Phase 33"
      contains: "Phase 33"
  key_links:
    - from: "all 7 payment-surface commits"
      to: "5 required gate E2E specs"
      via: "zero-behavior-change proof (accessible-name/testid preserved)"
      pattern: "COMPONENT-04"
---

<objective>
Phase gate: prove that the 7 isolated payment-surface swaps (33-01..33-07) introduced zero behavior change, per COMPONENT-04 and Success Criterion 3. Run the full static + unit regression suite and all 5 required gate E2E specs, then record the Phase 33 entry in CLAUDE.md.

Per D-06, the 7 surfaces each landed as their own isolated commit/PR (one page/widget per PR, sequencing at planner discretion — realized here as one plan per file, all parallel in Wave 1). This plan is the aggregate gate after all 7 land — the single place that confirms the whole phase is behavior-neutral, and that the D-01a out-of-scope files (`src/widgets/TabDrawer/`, `src/widgets/TipDistributionPanel/`, `src/widgets/SettingsTabsPanel/tabs/TipDistributionSettingsTab.tsx`) were correctly left untouched.

Purpose: Success Criterion 3 requires `05-payments`, `41-split-payment`, `42-tip-distribution`, `06-transfer`, and `09-rbac` to pass unchanged after every payment-surface commit. This plan is the aggregate gate after all 7 land.
Output: Green regression suite + 5 gate specs; CLAUDE.md updated; phase ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-VALIDATION.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-01-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-02-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-03-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-04-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-05-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-06-SUMMARY.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full static + unit regression gate + D-01a untouched check + CLAUDE.md Implemented Features entry</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `.planning/phases/33-payment-critical-page-sweep-isolated/33-VALIDATION.md` — the sampling map + gate-spec list.
    - `.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md` D-01a — the three out-of-scope files that must remain unmodified by this phase.
    - `.planning/STATE.md` Phase 32 session-log entry — the known pre-existing baseline (2 pre-existing typecheck errors in `tab/model/queries.ts` + `agent/rag.ts`; 1 pre-existing `useCloseTab.test.ts:95` unit failure since Phase 15). New failures beyond this baseline are regressions; the baseline itself is NOT a Phase-33 regression.
    - `CLAUDE.md` "Implemented Features" section (latest entry is the Phase 32 touch/focus sweep) — append the Phase 33 entry in the same style.
  </read_first>
  <action>
    Run `npm run typecheck`, `npm run lint`, and `npm run test` (unit). Compare against the documented pre-phase baseline (STATE.md Phase 32 entry): typecheck must show only the 2 pre-existing unrelated errors (no NEW errors from the 7 files); lint must exit 0; unit suite must show only the 1 pre-existing `useCloseTab.test.ts:95` failure (no NEW failures). If any NEW failure appears, STOP and fix it in the owning file's scope before proceeding (do not paper over). Confirm via `git diff --stat` that the only `src/` files changed across the phase are the 7 in-scope files and that the D-01a out-of-scope files (`TabDrawer/`, `TipDistributionPanel/`, `TipDistributionSettingsTab.tsx`) were NOT modified. Then append one bullet to CLAUDE.md's "Implemented Features (as of ...)" list summarizing Phase 33: the 7 payment-critical surfaces (`pos/index.tsx` panel toggle, `CartPanel` Clear Cart, `PaymentForm` Process Payment + Remove-payment-N + Reset-to-computed, `TabPaymentCard`, `RefundSheet`, `SplitTabSheet`, `VoidOrderDialog`) standardized to `POSButton`/`touchSize`/`focusEmphasis`/`confirmClassName` with zero behavior change (COMPONENT-04). Do NOT add any new E2E spec to the "E2E Test Suite" list — this phase adds no new spec files.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npm run typecheck` shows only the 2 documented pre-existing errors (no new errors referencing any of the 7 in-scope files).
    - `npm run test` shows no new unit failures beyond the pre-existing `useCloseTab.test.ts:95` (PaymentForm.test.tsx, PaymentPane.test.tsx, VoidOrderDialog.test.tsx all green).
    - `git diff --stat` for the phase shows changes confined to the 7 in-scope `src/` files (+ CLAUDE.md); the D-01a out-of-scope files are absent from the diff.
    - CLAUDE.md "Implemented Features" contains a Phase 33 bullet naming the 7 standardized surfaces; the "E2E Test Suite" list is unchanged (no new spec added).
  </acceptance_criteria>
  <done>Static + unit regression green vs baseline; D-01a files confirmed untouched; CLAUDE.md records Phase 33; no new spec added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 7 payment-critical surfaces swapped to standardized primitives across plans 33-01..33-07, plus a green static/unit regression gate (Task 1). E2E gate specs require live `.env.local` Supabase credentials, which are not reliably reachable from the agent sandbox (per Phases 30/32 precedent).</what-built>
  <how-to-verify>
    1. Ensure the dev server + `.env.local` E2E creds are available (`requireIntegrationEnv()`), then run each required gate spec and confirm PASS:
       - `npx playwright test e2e/05-payments.spec.ts`
       - `npx playwright test e2e/41-split-payment.spec.ts`
       - `npx playwright test e2e/42-tip-distribution.spec.ts`
       - `npx playwright test e2e/06-transfer.spec.ts`
       - `npx playwright test e2e/09-rbac.spec.ts`
    2. Run the 3 secondary specs and confirm PASS (should be unaffected): `npx playwright test e2e/29-panel-toggle.spec.ts e2e/35-refund.spec.ts e2e/17-payment-pane.spec.ts`.
    3. Spot-check visuals on the payment surfaces: Process Payment / Refund confirm / Void confirm / Confirm Split render at 72px with the thicker (ring-4) focus ring; the tab-selector card and Clear Cart/Reset links look identical to before (no solid-primary fill, left-aligned card content, visible card border).
    All 5 required gate specs must be green (Success Criterion 3). If any is red, capture the failing assertion, trace it to the owning file's swap, fix in that file's isolated scope, and re-run.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 required gate specs pass (and secondaries + visual spot-check look correct), or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregate client change → payment/refund/void/split/tip flows | The phase gate confirms none of the 7 className/prop swaps altered any RBAC-gated or validation-gated payment behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08-01 | Tampering | Aggregate behavior drift across the 7 swapped surfaces | mitigate | Full typecheck + lint + unit regression vs documented baseline + all 5 required gate E2E specs green (blocking human-verify checkpoint) |
| T-33-08-02 | Tampering | Unintended edits to D-01a out-of-scope files | mitigate | `git diff --stat` confirms the phase touched only the 7 in-scope files + CLAUDE.md |
| T-33-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. This plan is verification + a docs entry only (CLAUDE.md Implemented Features). No new files, types, exports, functions, or CSS tokens.

<verification>
- `npm run typecheck` + `npm run lint` + `npm run test` green vs the documented pre-phase baseline.
- All 5 required gate E2E specs (`05-payments`, `41-split-payment`, `42-tip-distribution`, `06-transfer`, `09-rbac`) pass unchanged (blocking human-verify).
- `git diff --stat` confirms only the 7 in-scope files (+ CLAUDE.md) changed (D-01a files untouched).
- CLAUDE.md Implemented Features records Phase 33.
</verification>

<success_criteria>
- Payment-critical surfaces visually standardized with zero prop/handler/validation change, proven by the full regression suite + 5 gate specs (COMPONENT-04, Success Criteria 1 + 3).
- Each surface landed as its own isolated commit/PR across 33-01..33-07 (D-06, Success Criterion 2).
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-08-SUMMARY.md` when done.
</output>
