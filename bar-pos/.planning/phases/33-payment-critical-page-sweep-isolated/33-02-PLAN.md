---
phase: 33-payment-critical-page-sweep-isolated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/OrderPanel/CartPanel.tsx
autonomous: true
requirements: [COMPONENT-04]
must_haves:
  truths:
    - "The Clear Cart control is a POSButton, still clears the cart on click, still reads 'Clear Cart'"
    - "Clear Cart keeps its text-link appearance (underlined muted text, no solid bg-primary background)"
    - "The Place Order button is untouched (still POSButton touchSize=xl, no focusEmphasis added)"
  artifacts:
    - path: "src/widgets/OrderPanel/CartPanel.tsx"
      provides: "OrderPanel cart with Clear Cart converted to POSButton (variant=ghost, touchSize=default)"
      contains: "Clear Cart"
  key_links:
    - from: "src/widgets/OrderPanel/CartPanel.tsx Clear Cart"
      to: "clearCart"
      via: "onClick handler (unchanged)"
      pattern: "clearCart"
---

<objective>
Convert the raw `<button>` "Clear Cart" text-link in `src/widgets/OrderPanel/CartPanel.tsx` (~L188-193) to `POSButton` per D-01, satisfying COMPONENT-04's markup/class-only swap. Zero prop/handler/validation behavior change (Success Criterion 1).

Purpose: The only raw `<button>` flagged in this widget by the Phase 29 drift audit. "Clear Cart" is a low-stakes/reversible action (clears an unsubmitted cart, not money) — NOT a D-03 critical action — so it gets the 44px floor (`touchSize="default"`), not `large`/`xl`.
Output: One-file isolated PR/commit converting Clear Cart to `POSButton`, all E2E specs green unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-CONTEXT.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-RESEARCH.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-PATTERNS.md
@.planning/phases/33-payment-critical-page-sweep-isolated/33-UI-SPEC.md
@src/widgets/OrderPanel/CartPanel.tsx
@src/shared/ui/POSButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Clear Cart to POSButton (variant=ghost, touchSize=default)</name>
  <files>src/widgets/OrderPanel/CartPanel.tsx</files>
  <read_first>
    - `src/widgets/OrderPanel/CartPanel.tsx` — the raw `<button>` "Clear Cart" (~L188-193, text at L192) AND the already-converted "Place Order" POSButton (~L171-181, `touchSize="xl"`). `POSButton` is ALREADY imported (`import { POSButton } from '@shared/ui/POSButton';`, ~L15) — do NOT add a duplicate import.
    - `33-PATTERNS.md` § "src/widgets/OrderPanel/CartPanel.tsx" — the exact before/after target markup (Pattern A + B).
    - `33-RESEARCH.md` § "2. src/widgets/OrderPanel/CartPanel.tsx" — the "leave Place Order untouched" instruction and the variant-default visual-regression note.
    - `33-UI-SPEC.md` Component Inventory rows for CartPanel (Clear Cart target + Place Order "No change") + "Variant Decisions Required".
    - `src/shared/ui/POSButton.tsx` — confirms prop forwarding + `touchSize` class map.
  </read_first>
  <action>
    Change the `<button>`/`</button>` tags for "Clear Cart" to `<POSButton>`/`</POSButton>`. Add `variant="ghost"` explicitly (per D-03b — the raw button is a transparent text-link with no CVA styling; omitting `variant` would silently apply `variant="default"`'s solid `bg-primary` background, a forbidden visual change; `ghost` keeps the transparent look). Add `touchSize="default"` (D-04 floor tier). Preserve the existing `className` byte-identical (`text-sm text-muted-foreground underline decoration-muted-foreground/60 underline-offset-2 hover:text-foreground`), `type="button"`, the `onClick` calling `clearCart()`, and the literal child text `Clear Cart`. Do NOT touch the "Place Order" button (~L171) — it is not named in D-03/D-04 for this phase; leave its `touchSize="xl"` as-is and do NOT add `focusEmphasis`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <human-check>Run `npx playwright test e2e/05-payments.spec.ts` (live .env.local creds). Green. Visually confirm Clear Cart still renders as an underlined muted-text link (no solid colored button fill) and clears the cart.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/widgets/OrderPanel/CartPanel.tsx` contains no raw `<button` tag (grep `<button` returns 0 matches).
    - Clear Cart renders as `<POSButton variant="ghost" touchSize="default"` with child text `Clear Cart` unchanged (grep `Clear Cart` still matches).
    - The `onClick` calling `clearCart()` and the link-style `className` are byte-identical to before (unchanged in diff).
    - "Place Order" still reads `touchSize="xl"` with NO `focusEmphasis` added (grep confirms Place Order line unchanged).
    - `npm run typecheck` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Clear Cart is a `POSButton variant="ghost" touchSize="default"` text-link, onClick/text/className preserved verbatim, Place Order untouched, typecheck+lint green, payments E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → cart local state | Clear Cart mutates client-side cart store only; no server/RBAC/validation logic involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Tampering | CartPanel.tsx Clear Cart `onClick`/label | mitigate | Only tag name + `variant`/`touchSize` change; `onClick`/text/`className` preserved verbatim; `npm run typecheck` + `05-payments` E2E confirm no drift |
| T-33-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure existing-component prop wiring (RESEARCH.md §Package Legitimacy Audit) |
</threat_model>

## Artifacts this phase produces

No new symbols. Pure primitive swap: raw `<button>` → existing `POSButton`. No new files, types, exports, functions, or CSS tokens.

<verification>
- `npm run typecheck` exits 0; `npm run lint` exits 0.
- No raw `<button` remains in `src/widgets/OrderPanel/CartPanel.tsx`.
- E2E gate (live creds): `e2e/05-payments.spec.ts` passes unchanged (Success Criterion 3).
</verification>

<success_criteria>
- Clear Cart standardized to `POSButton` with zero prop/handler/validation change (COMPONENT-04, D-01); Place Order left untouched.
- Lands as its own isolated commit/PR (Success Criterion 2).
- typecheck + lint green; payments E2E unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-payment-critical-page-sweep-isolated/33-02-SUMMARY.md` when done.
</output>
