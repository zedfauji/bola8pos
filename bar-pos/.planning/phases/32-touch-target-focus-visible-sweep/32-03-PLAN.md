---
phase: 32-touch-target-focus-visible-sweep
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src/features/stop-pool-timer/ui/StopSessionConfirm.tsx
  - src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx
  - e2e/44-focus-tab-order.spec.ts
  - CLAUDE.md
autonomous: true
requirements: [TOUCH-02, FOCUS-02, FOCUS-03]
must_haves:
  truths:
    - "The Stop-pool-session confirm button and the Stop-and-Move confirm button render at 72px with an emphasized (ring-4/ring-ring) focus ring"
    - "Keyboard Tab order through ManagerPinDialog (via TableStatusPanel), the inventory DataTable search/filter row, and inventory/kitchen-prep form inputs matches visual order"
    - "e2e/44-focus-tab-order.spec.ts exists and is part of the permanent suite"
  artifacts:
    - path: "src/features/stop-pool-timer/ui/StopSessionConfirm.tsx"
      provides: "confirmClassName wiring for xl + focusEmphasis (D-03/D-10 target 1)"
      contains: "min-h-[72px]"
    - path: "src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx"
      provides: "confirmClassName wiring for xl + focusEmphasis (D-03/D-10 target 2)"
      contains: "min-h-[72px]"
    - path: "e2e/44-focus-tab-order.spec.ts"
      provides: "automated Tab-order assertions for the 3 named surfaces (FOCUS-03/D-12/D-13)"
    - path: "CLAUDE.md"
      provides: "E2E suite list updated to 23 specs incl. 44-focus-tab-order"
  key_links:
    - from: "StopSessionConfirm/StopAndMoveDialog confirmClassName"
      to: "ConfirmDialog confirm button (72px + ring-4)"
      via: "confirmClassName passthrough added in Plan 32-01"
      pattern: "min-h-\\[72px\\].*ring-4"
---

<objective>
Finalize the phase: escalate the two real destructive confirm actions to 72px + emphasized focus ring using the shared pieces from Plan 32-01, add the automated Tab-order spec (FOCUS-03), and update the docs.

Purpose: D-03/D-10 reserve the 72px size AND the emphasized focus ring for exactly two genuinely destructive/irreversible controls — the pool-session "Stop & finalize" confirm and the "Stop & Move" confirm. Both render via ConfirmDialog/AlertDialogAction (not POSButton), so they are wired through the confirmClassName passthrough added in 32-01 (RESEARCH Pattern 3 / Pitfall 2). ("Delete inventory batch" was dropped — it is not a shipped feature; the target set is 2, per corrected D-03/D-10.) FOCUS-03 is verified by a new Playwright spec asserting keyboard Tab order on the three named surfaces (D-12), numbered 44 to avoid the existing 43-promotions collision (D-13/Pitfall 3).
Output: 2 destructive controls at 72px/ring-4, a passing tab-order spec, updated CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-PATTERNS.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-UI-SPEC.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire confirmClassName (72px + emphasized ring) onto the 2 destructive confirm dialogs</name>
  <files>src/features/stop-pool-timer/ui/StopSessionConfirm.tsx, src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx</files>
  <read_first>
    - src/features/stop-pool-timer/ui/StopSessionConfirm.tsx (the ConfirmDialog usage at lines 107-153 — variant="destructive", confirmLabel="Stop & finalize")
    - src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx (the ConfirmDialog usage at lines 66-98 — variant="destructive", confirmLabel="Stop & Move")
    - src/shared/ui/ConfirmDialog.tsx (the confirmClassName prop added in Plan 32-01 — the passthrough this task consumes)
    - src/shared/ui/POSButton.tsx (lines 39-43 — the exact xl class string min-h-[72px] text-lg font-semibold to reuse literally)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-PATTERNS.md (ConfirmDialog section — the literal confirmClassName string)
  </read_first>
  <action>
    On BOTH ConfirmDialog call sites, pass a new `confirmClassName` prop with the literal string `min-h-[72px] text-lg font-semibold focus-visible:ring-4 focus-visible:ring-ring`. The first three tokens reuse POSButton's exact `xl` class string (72px — D-03 destructive tier); the last two are the `focusEmphasis="high"` class string (D-09/D-10 emphasized ring, full-opacity --ring, +1px width vs baseline). Do not change confirmLabel, variant, onConfirm, onCancel, isLoading, confirmDisabled, the dialog copy, or any handler — this is sizing + focus-ring only (per UI-SPEC Copywriting Contract: no copy change). This is the ONLY place `focusEmphasis`/72px is applied in this phase; do not add it to any other confirm button (RESEARCH Anti-Pattern: keep the emphasized ring reserved for these two).
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "min-h-\[72px\].*focus-visible:ring-4" src/features/stop-pool-timer/ui/StopSessionConfirm.tsx && grep -q "min-h-\[72px\].*focus-visible:ring-4" src/features/stop-and-move-table/ui/StopAndMoveDialog.tsx && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Both files pass confirmClassName="min-h-[72px] text-lg font-semibold focus-visible:ring-4 focus-visible:ring-ring"
    - confirmLabel ("Stop & finalize" / "Stop & Move"), variant="destructive", and all handlers unchanged (behavior assertion — copy/handlers preserved)
    - Existing tests unaffected: `npm run test` green, including StopSessionConfirm.test.tsx and StopAndMoveDialog.test.tsx
    - No other ConfirmDialog call site receives confirmClassName (grep the repo — exactly 2 usages)
    - typecheck exit 0; lint exit 0
  </acceptance_criteria>
  <done>Both destructive confirm buttons render at 72px with the ring-4/ring-ring emphasized focus ring; no copy or handler changed; no other dialog affected.</done>
</task>

<task type="auto">
  <name>Task 2: Add e2e/44-focus-tab-order.spec.ts (FOCUS-03 Tab-order assertions)</name>
  <files>e2e/44-focus-tab-order.spec.ts</files>
  <read_first>
    - e2e/16-table-status.spec.ts (spec boilerplate: import test/expect from ./fixtures, loginAs/logout from ./helpers/auth; seed helpers getOccupiedPoolTableIds/openCaja/resetTestState from ./helpers/supabase; how it opens a tab + starts a pool session + reaches TableStatusPanel and its PIN-gated actions)
    - e2e/helpers/auth.ts (loginAs, loginAsNamed, gotoAuthed, logout, enterPin conventions; admin PIN via env)
    - src/widgets/TableStatusPanel/index.tsx (which action opens ManagerPinDialog — e.g. Edit Start Time / remove-item PIN gate at lines 361-373 / 237-249; the ManagerPinDialog mounts at lines 420/460)
    - src/features/manager-pin-gate/ui/ManagerPinDialog.tsx (PIN entry field/keypad DOM — the focusable order to assert)
    - src/shared/ui/DataTable.tsx + src/shared/ui/SearchInput.tsx (inventory search/filter row focus order)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md (D-11, D-12, D-13 — the exact 3 surfaces and the automated-only method)
  </read_first>
  <action>
    Create e2e/44-focus-tab-order.spec.ts following 16-table-status.spec.ts conventions (header comment listing the source files read; import test/expect from ./fixtures and loginAs/logout from ./helpers/auth; requireIntegrationEnv guard; reuse the supabase seed helpers). Write Tab-order assertions for the three D-12 surfaces using `page.keyboard.press('Tab')` then `expect(locator).toBeFocused()` in visual order: (a) ManagerPinDialog PIN-entry flow reached from TableStatusPanel — open a PIN-gated action, then assert focus lands on / advances through the PIN input(s) in order; (b) the inventory DataTable search/filter row — navigate to /inventory (loginAs admin), assert Tab moves from the search input through the filter control(s) in visual order; (c) another inventory or kitchen-prep form input group (e.g. the Physical Count form fields or a kitchen-prep batch qty / reason field). Derive the exact selectors at execute time from the real rendered DOM (roles/labels/testids), not from assumptions. IMPORTANT: this spec only READS and asserts — it must NOT modify ManagerPinDialog, PINKeypad, DataTable, or SearchInput (those shared components are consumed by out-of-scope pages like clock-in-staff/PINLoginForm; touching them would ripple). If a surface cannot be deterministically reached under the seed harness, assert the reachable subset and record the gap in the SUMMARY rather than writing a flaky assertion. Add the spec so a full `page.keyboard.press('Tab')` sequence proves order matches layout (no jumps backward past a skipped focusable).
  </action>
  <verify>
    <automated>cd bar-pos && test -f e2e/44-focus-tab-order.spec.ts && npx playwright test e2e/44-focus-tab-order.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e/44-focus-tab-order.spec.ts exists, numbered 44 (not 43 — 43-promotions.spec.ts already exists; verify with `ls e2e/*.spec.ts | sort` at execute time)
    - Spec asserts Tab-key focus order via page.keyboard.press('Tab') + toBeFocused() on the ManagerPinDialog surface, the inventory DataTable search/filter row, and an inventory/kitchen-prep form input group (D-12)
    - Spec imports from ./fixtures and ./helpers/auth (project convention); does not modify any shared component
    - `npx playwright test e2e/44-focus-tab-order.spec.ts` passes against a running dev server (requires .env.local E2E creds); if any surface is unreachable under the harness, the reachable subset passes and the gap is documented in SUMMARY
  </acceptance_criteria>
  <done>A passing (or documented-subset) automated tab-order spec exists at e2e/44-focus-tab-order.spec.ts, reading-only, covering the three D-12 surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md E2E suite list + Implemented Features note</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the "## E2E Test Suite (`bar-pos/e2e/`)" section — currently "22 spec files" with the enumerated list; and the "## Implemented Features" section)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md (D-13 — add 44-focus-tab-order to the list once created)
  </read_first>
  <action>
    In CLAUDE.md, update the "## E2E Test Suite" section: change the count from 22 to 23 spec files and append `44-focus-tab-order` to the enumerated list of must-pass specs. Add a one-line entry under "## Implemented Features" recording the Phase 32 touch-target/focus-visible sweep: the new `focusEmphasis` Button prop, POSButton touchSize rollout across the 6 operational pages, the ConfirmDialog `confirmClassName` passthrough powering the 2 destructive 72px/ring-4 confirms, and the e2e/44-focus-tab-order spec. Keep the note factual and terse — no marketing language.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "44-focus-tab-order" CLAUDE.md && grep -q "23 spec files" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md E2E section says "23 spec files" and lists `44-focus-tab-order`
    - CLAUDE.md Implemented Features has a Phase 32 line naming focusEmphasis + touchSize rollout + confirmClassName
    - No unrelated CLAUDE.md sections altered
  </acceptance_criteria>
  <done>CLAUDE.md reflects the new spec (23 total) and the Phase 32 sweep.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-03)

- New file `e2e/44-focus-tab-order.spec.ts` — permanent FOCUS-03 tab-order regression spec (23rd must-pass spec).
- `confirmClassName` wiring on `StopSessionConfirm` and `StopAndMoveDialog` (consumes the prop created in Plan 32-01) — the two 72px + emphasized-ring destructive confirm controls.
- CLAUDE.md updated: E2E suite = 23 specs; Phase 32 Implemented-Features line.
- No new runtime symbols beyond the wired call-site props.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> destructive pool-session confirm | High-stakes irreversible action (stop/finalize, stop-and-move); confirmation gate must remain intact |
| keyboard/barcode -> PIN entry & forms | Tab order affects keyboard/scanner usability on PIN and form surfaces (verified, not modified) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Tampering | destructive ConfirmDialog copy/gating | mitigate | Only confirmClassName (sizing + focus ring) is added; confirmLabel, variant="destructive", confirmDisabled, and all handlers are unchanged — no destructive-action confirmation behavior/copy change; verified by unchanged StopSessionConfirm/StopAndMoveDialog tests |
| T-32-03-02 | Information Disclosure | ManagerPinDialog / PINKeypad | mitigate | The tab-order spec is read-only and does not modify ManagerPinDialog or PINKeypad; PIN-entry access-control logic is untouched, so no PIN security surface is weakened (RESEARCH Anti-Pattern: do not convert PINKeypad) |
| T-32-03-SC | Tampering | package installs | accept | No npm/pip/cargo installs — zero new dependencies |
</threat_model>

<verification>
- Source assertions: both destructive files carry `min-h-[72px] ... focus-visible:ring-4 focus-visible:ring-ring`; exactly 2 confirmClassName usages repo-wide
- `npm run typecheck` exit 0; `npm run lint` exit 0; `npm run test` green (destructive dialog tests unaffected)
- `npx playwright test e2e/44-focus-tab-order.spec.ts` passes (or documented reachable subset) against a running dev server
- CLAUDE.md updated to 23 specs incl. 44-focus-tab-order
</verification>

<success_criteria>
- The 2 destructive confirm controls render at 72px with the emphasized ring-4/ring-ring focus ring (TOUCH-02/FOCUS-02, D-03/D-09/D-10)
- Tab order verified sane on the 3 named surfaces via an automated Playwright spec (FOCUS-03, D-11/D-12/D-13)
- e2e/44-focus-tab-order.spec.ts added to the permanent suite; CLAUDE.md updated
- No destructive-action copy/behavior change; no shared PIN component modified
</success_criteria>

<output>
Create `.planning/phases/32-touch-target-focus-visible-sweep/32-03-SUMMARY.md` when done
</output>
