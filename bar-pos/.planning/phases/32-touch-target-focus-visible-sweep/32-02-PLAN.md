---
phase: 32-touch-target-focus-visible-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/PoolTableGrid/index.tsx
  - src/widgets/KdsBoard/index.tsx
  - src/pages/inventory/index.tsx
  - src/widgets/TableStatusPanel/index.tsx
  - src/entities/inventory/ui/InventoryRow.tsx
autonomous: true
requirements: [TOUCH-01, TOUCH-02, TOUCH-03]
must_haves:
  truths:
    - "No raw shadcn Button or icon-button below the 44px floor remains on pool-tables, pool-table-status, inventory, kds, or kds-bar"
    - "KDS/kds-bar per-card bump buttons render at 56px (frequent action)"
    - "The pool-table-status remove-item icon button is a 44px square"
    - "The inventory table's sort-header buttons meet the 44px floor"
    - "Every tappable-card/button grid on the 6 in-scope pages keeps an 8px+ gap"
  artifacts:
    - path: "src/widgets/PoolTableGrid/index.tsx"
      provides: "filters-toggle converted to POSButton touchSize=default (D-02)"
      contains: "POSButton"
    - path: "src/widgets/KdsBoard/index.tsx"
      provides: "3 raw Buttons converted (2 bump -> large, Retry -> default)"
      contains: "POSButton"
    - path: "src/pages/inventory/index.tsx"
      provides: "Physical Count converted to POSButton touchSize=default"
      contains: "POSButton"
    - path: "src/widgets/TableStatusPanel/index.tsx"
      provides: "remove-item icon button raised to 44px square"
      contains: "h-11 w-11"
    - path: "src/entities/inventory/ui/InventoryRow.tsx"
      provides: "SortHeader native button raised to min-h-[44px]"
      contains: "min-h-[44px]"
  key_links:
    - from: "src/widgets/KdsBoard/index.tsx bump buttons"
      to: "POSButton touchSize='large'"
      via: "raw Button -> POSButton conversion"
      pattern: "touchSize=\"large\""
---

<objective>
Roll out the 44/56px touch floor across the raw-Button and native-button sites on the operational pages (pool-tables, pool-table-status, inventory, kds, kds-bar), and verify the grid-gap floor. This is a mechanical conversion using the existing POSButton touchSize scale — no new component work. No fixed-layout exceptions are taken (D-04) — every in-scope control grows to its assigned touch size without a layout override.

Purpose: TOUCH-01 (44px floor for every interactive element), TOUCH-02 (56px for frequent actions), TOUCH-03 (8px grid gap). Research folded in raw-Button sites beyond CONTEXT.md's original list (KdsBoard's 3 buttons, inventory's Physical Count, TableStatusPanel's remove-item icon, InventoryRow's SortHeader native button — Pitfall 1). The two 72px destructive confirm targets are handled separately in Plan 32-03 because they route through ConfirmDialog, not POSButton.
Output: all in-scope raw/native buttons at or above their assigned touch size; grid gaps verified compliant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-PATTERNS.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert text-button sites (PoolTableGrid, KdsBoard, inventory) to POSButton</name>
  <files>src/widgets/PoolTableGrid/index.tsx, src/widgets/KdsBoard/index.tsx, src/pages/inventory/index.tsx</files>
  <read_first>
    - src/widgets/PoolTableGrid/index.tsx (line 120 raw Button filters-toggle; the correct sibling pattern is already at lines 104-114 / 140-150 as POSButton touchSize="default"; import line 20 lists both Button and POSButton)
    - src/widgets/KdsBoard/index.tsx (import line 8; the 3 raw Buttons: KdsCard bump at lines 66-74, ComboKdsCard bump at lines 143-153, Retry at line 179)
    - src/pages/inventory/index.tsx (import line 8 `{ Button, PageContainer }`; the Physical Count Button at lines 63-72)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-PATTERNS.md (PoolTableGrid + KdsBoard conversion sections; D-03 tier table)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    PoolTableGrid (D-02): convert the line-120 filters-toggle raw Button to POSButton with touchSize="default" (44px — it is neither frequent/primary nor destructive per the D-03 tier), preserving every other prop and the exact className verbatim. POSButton is already imported at line 20; remove Button from that import (it is the only raw-Button usage in the file — lint max-warnings 0 will fail on an unused import).
    KdsBoard: convert all 3 raw Button instances. The two per-card bump buttons (KdsCard near line 66, ComboKdsCard near line 143, currently size="sm") become POSButton touchSize="large" (56px — "bump KDS order" is the frequent/primary action per D-03), dropping size="sm" and keeping variant, disabled, onClick, and className="w-full" (full-width stacked satisfies D-07 — one button per card, no adjacent sibling). The Retry button (near line 179, variant="outline") becomes POSButton touchSize="default" (44px — not a frequent action). Replace the deep import of Button from @shared/ui/button with an import of POSButton from @shared/ui/POSButton.
    inventory/index.tsx: convert the Physical Count Button (line 63) to POSButton touchSize="default" (44px — a secondary admin action, not in the D-03 frequent list), keeping variant="outline", data-testid="physical-count-btn", and onClick. Update the import at line 8 from { Button, PageContainer } to { PageContainer, POSButton } (Button has no other usage in the file).
    Do not alter any labels, handlers, or data-testids (E2E specs depend on them). This is sizing-only.
  </action>
  <verify>
    <automated>cd bar-pos && ! grep -nE "<Button[ >]" src/widgets/PoolTableGrid/index.tsx src/widgets/KdsBoard/index.tsx src/pages/inventory/index.tsx && grep -c "touchSize=\"large\"" src/widgets/KdsBoard/index.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no `<Button` tag remains in PoolTableGrid/index.tsx, KdsBoard/index.tsx, or pages/inventory/index.tsx (all converted to POSButton)
    - KdsBoard has exactly 2 `touchSize="large"` (the two bump buttons) and 1 `touchSize="default"` (Retry)
    - data-testid="physical-count-btn" and data-testid="pool-filters-toggle" still present (behavior preserved)
    - typecheck exit 0; lint exit 0 (no unused Button import)
  </acceptance_criteria>
  <done>All text-button sites on PoolTableGrid, KdsBoard, and inventory page use POSButton at their assigned touch size; imports cleaned; labels/testids unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Raise the icon and native-button sites (TableStatusPanel remove-item, InventoryRow SortHeader) to 44px</name>
  <files>src/widgets/TableStatusPanel/index.tsx, src/entities/inventory/ui/InventoryRow.tsx</files>
  <read_first>
    - src/widgets/TableStatusPanel/index.tsx (the remove-item raw Button at lines 237-249, currently size="icon-sm" / 28px; POSButton is already imported and used at lines 276+; Button appears only at this one site)
    - src/entities/inventory/ui/InventoryRow.tsx (the SortHeader native button at lines 20-29, px-2 py-1 ~28px tall, local to this file, used by inventoryRowColumns)
    - src/shared/ui/QuantityControl.tsx (lines 80-90 — the 44px icon-button reference shape h-11 w-11 touch-manipulation)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md (Open Question 2 — SortHeader scope decision; Pitfall 1)
  </read_first>
  <action>
    TableStatusPanel remove-item button: convert the Button size="icon-sm" at line 237 to POSButton with size="icon" touchSize="default" and change className so the tap target is a 44px square — add h-11 w-11 and keep touch-manipulation (match QuantityControl), preserving type="button", variant="ghost", title="Remove item", and the onClick that sets setSelectedItemForRemoval(item) plus setShowPinForRemoval(true). Keep the inner X icon at size-4. POSButton is already imported; remove Button from the import (its only usage is this site).
    InventoryRow SortHeader: this is a native button (not a shadcn Button), so it falls under TOUCH-01's "all interactive elements" rather than a POSButton conversion (resolving Open Question 2). Add min-h-[44px] to its existing className (keep it a native button — converting a tanstack-table header cell to POSButton would fight the header render for no benefit). Result className keeps `-ml-2 inline-flex items-center gap-1 rounded px-2 py-1 text-left font-medium hover:bg-muted` plus min-h-[44px], plus touch-manipulation. No behavior change to the sort toggle.
    Neither change reorders or renames anything — sizing only.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "h-11 w-11" src/widgets/TableStatusPanel/index.tsx && grep -q "min-h-\[44px\]" src/entities/inventory/ui/InventoryRow.tsx && ! grep -nE "size=\"icon-sm\"" src/widgets/TableStatusPanel/index.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - TableStatusPanel remove-item button is POSButton with h-11 w-11 (44px square); no `size="icon-sm"` remains at that site; Button import removed
    - InventoryRow SortHeader native button carries min-h-[44px]; sort toggle onClick unchanged
    - Source assertion: `grep -c "min-h-\[44px\]" src/entities/inventory/ui/InventoryRow.tsx` >= 1
    - typecheck exit 0; lint exit 0
  </acceptance_criteria>
  <done>The remove-item icon button is a 44px square POSButton and the inventory sort-header meets the 44px floor; no labels/handlers changed.</done>
</task>

<task type="auto">
  <name>Task 3: Grid-gap audit (TOUCH-03) — verify 8px floor across in-scope grids</name>
  <files>src/widgets/PoolTableGrid/index.tsx, src/widgets/KdsBoard/index.tsx, src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx, src/features/produce-prep-batch/ui/PrepBatchPreview.tsx</files>
  <read_first>
    - src/widgets/PoolTableGrid/index.tsx (grid at lines 159 and 173 — gap-4)
    - src/widgets/KdsBoard/index.tsx (board grid `grid gap-6` near line 238; card lists space-y-4; per-card bump is a full-width single button — D-07)
    - src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx (on-hand card grid gap-4 near line 108, space-y-8, gap-2)
    - src/features/produce-prep-batch/ui/PrepBatchPreview.tsx (rows `grid grid-cols-2 gap-2` — 8px)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-UI-SPEC.md (Grid-gap audit contract; D-05/D-06/D-07)
  </read_first>
  <action>
    Verify-only pass (D-05/D-06/D-07 — research already confirmed all in-scope grids are compliant; QuantityControl's stepper is already h-11 w-11 / gap-2). Do NOT change gap classes that already meet the 8px floor. Confirm each in-scope tappable-card/button grid uses gap-2 (8px) or greater: PoolTableGrid gap-4, KdsBoard board gap-6 plus card lists space-y-4, KitchenPrepDashboard gap-4, PrepBatchPreview gap-2. If — and only if — any in-scope grid is found at gap-0/gap-1 (below 8px), bump it to gap-2 (exact Tailwind class, no arbitrary value). Record the audit result (each grid's gap class plus PASS) in the plan SUMMARY. The KDS/kds-bar per-card bump buttons are single full-width buttons per card (no adjacent sibling), so D-07 is satisfied by stacking, not a gap class.
  </action>
  <verify>
    <automated>cd bar-pos && ! grep -rnE "\bgap-(0|1)\b" src/widgets/PoolTableGrid/index.tsx src/widgets/KdsBoard/index.tsx src/widgets/KitchenPrepDashboard/ui/KitchenPrepDashboard.tsx src/features/produce-prep-batch/ui/PrepBatchPreview.tsx && grep -q "gap-4" src/widgets/PoolTableGrid/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: every in-scope tappable grid uses gap-2 or greater; no gap-0/gap-1 present in the four audited files
    - PoolTableGrid still uses gap-4 (16px) at its two grid sites (regression guard — D-06)
    - SUMMARY records the per-grid gap class and PASS for PoolTableGrid, KdsBoard, KitchenPrepDashboard, PrepBatchPreview
  </acceptance_criteria>
  <done>All in-scope grids confirmed at or above the 8px gap floor; no regressions; audit result recorded.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-02)

No new symbols — this plan is a rollout/conversion pass. It changes existing call sites only:
- `src/widgets/PoolTableGrid/index.tsx` — filters-toggle now `POSButton touchSize="default"`; `Button` import removed.
- `src/widgets/KdsBoard/index.tsx` — 2 bump buttons now `POSButton touchSize="large"`, Retry now `POSButton touchSize="default"`; import switched to POSButton.
- `src/pages/inventory/index.tsx` — Physical Count now `POSButton touchSize="default"`; import switched to POSButton.
- `src/widgets/TableStatusPanel/index.tsx` — remove-item now `POSButton size="icon"` with `h-11 w-11`; `Button` import removed.
- `src/entities/inventory/ui/InventoryRow.tsx` — SortHeader native button gains `min-h-[44px]`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/touch -> operational pages | User interaction with pool/kds/inventory controls; no network or data trust boundary crossed by a sizing change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Tampering | remove-item / bump / sort handlers | mitigate | Conversions preserve onClick handlers, data-testids, labels, and disabled logic verbatim — sizing/class only, zero behavior change; verified by typecheck/lint and unchanged E2E testids |
| T-32-02-02 | Denial of Service | KDS realtime board render | accept | Class-only swaps do not alter the realtime data path or render loop; no DoS surface introduced |
| T-32-02-SC | Tampering | package installs | accept | No npm/pip/cargo installs — zero new dependencies |
</threat_model>

<verification>
- Source assertions: no `<Button` tag remains in the three text-button files; no `size="icon-sm"` at the TableStatusPanel remove site; `h-11 w-11` present in TableStatusPanel; `min-h-[44px]` present in InventoryRow; no `gap-0`/`gap-1` in the four audited grids
- `npm run typecheck` exit 0; `npm run lint` exit 0
- Wave/phase gate (manual, dev server required): existing E2E regression specs still pass — `npx playwright test e2e/16-table-status.spec.ts e2e/28-kds.spec.ts e2e/40-kds-bar.spec.ts e2e/10-inventory.spec.ts`
</verification>

<success_criteria>
- Every raw shadcn Button on the 6 in-scope pages is a POSButton at its assigned touch size (TOUCH-01/TOUCH-02, D-01/D-02/D-03)
- KDS bump buttons are 56px; secondary buttons at the 44px floor
- Icon/native buttons (remove-item, sort header) meet the 44px floor
- Grid-gap floor of 8px verified across all in-scope grids (TOUCH-03, D-05/D-06/D-07)
</success_criteria>

<output>
Create `.planning/phases/32-touch-target-focus-visible-sweep/32-02-SUMMARY.md` when done
</output>
