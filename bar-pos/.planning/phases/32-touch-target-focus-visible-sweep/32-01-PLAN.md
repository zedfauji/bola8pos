---
phase: 32-touch-target-focus-visible-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/button.tsx
  - src/shared/ui/button.test.tsx
  - src/shared/ui/POSButton.test.tsx
  - src/shared/ui/ConfirmDialog.tsx
  - src/shared/ui/ConfirmDialog.test.tsx
  - src/shared/ui/SearchInput.tsx
autonomous: true
requirements: [FOCUS-01, FOCUS-02, TOUCH-01, TOUCH-02]
must_haves:
  truths:
    - "focusEmphasis='high' on the shared Button renders focus-visible:ring-4 focus-visible:ring-ring; focusEmphasis='default' keeps the baseline focus-visible:ring-3 focus-visible:ring-ring/50"
    - "The baseline focus-visible ring classes survive when POSButton merges touchSize classes over the base Button className"
    - "ConfirmDialog accepts an opt-in confirmClassName string that is merged onto the AlertDialogAction confirm button"
    - "SearchInput's clear (X) button presents a 44px touch target"
  artifacts:
    - path: "src/shared/ui/button.tsx"
      provides: "focusEmphasis CVA variant (default|high) on buttonVariants, threaded through Button()"
      contains: "focusEmphasis"
    - path: "src/shared/ui/button.test.tsx"
      provides: "unit assertions for baseline + emphasized focus-ring class output (FOCUS-01/FOCUS-02, closes Assumption A1)"
    - path: "src/shared/ui/POSButton.test.tsx"
      provides: "unit assertions for touchSize -> min-h class output (TOUCH-02 regression coverage)"
    - path: "src/shared/ui/ConfirmDialog.tsx"
      provides: "confirmClassName passthrough prop on ConfirmDialogProps"
      contains: "confirmClassName"
    - path: "src/shared/ui/ConfirmDialog.test.tsx"
      provides: "unit assertion that confirmClassName reaches the confirm button (closes Pitfall 2)"
    - path: "src/shared/ui/SearchInput.tsx"
      provides: "44px clear-button touch target"
  key_links:
    - from: "src/shared/ui/button.tsx focusEmphasis variant"
      to: "buttonVariants({ focusEmphasis })"
      via: "CVA variant slot threaded through Button() signature"
      pattern: "focusEmphasis"
    - from: "src/shared/ui/ConfirmDialog.tsx confirmClassName"
      to: "AlertDialogAction className"
      via: "cn(... , confirmClassName)"
      pattern: "confirmClassName"
---

<objective>
Add the two shared-primitive extensions this phase needs and lock them with unit tests, plus fix the one shared-primitive touch-target violation (SearchInput's clear button). This plan produces the reusable pieces that Plan 32-03 wires onto the two destructive confirm dialogs.

Purpose: FOCUS-02 requires a higher-contrast focus ring for high-stakes actions; the two real destructive targets render via ConfirmDialog/AlertDialogAction (NOT POSButton), so both a `focusEmphasis` variant on the base `buttonVariants` (D-09) and an opt-in `confirmClassName` passthrough on ConfirmDialog (RESEARCH Pattern 3 / Pitfall 2) are prerequisites. TOUCH-01 also covers SearchInput's 28px clear button, a shared primitive reached from the inventory DataTable.
Output: `focusEmphasis` CVA variant, `confirmClassName` prop, a 44px SearchInput clear button, and three new unit-test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-PATTERNS.md
@.planning/phases/32-touch-target-focus-visible-sweep/32-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focusEmphasis CVA variant to base Button + unit tests</name>
  <files>src/shared/ui/button.tsx, src/shared/ui/button.test.tsx, src/shared/ui/POSButton.test.tsx</files>
  <read_first>
    - src/shared/ui/button.tsx (the buttonVariants CVA at lines 7-42 and the Button() signature at lines 44-65 — the variant map and defaultVariants you extend)
    - src/shared/ui/POSButton.tsx (touchSizeClasses map at lines 39-43 — the min-h scale you assert)
    - src/shared/ui/PageContainer.test.tsx (existing shared/ui RTL test conventions: render + screen.getByRole + vitest describe/it/expect imports)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md (Architecture Pattern 2 + Assumption A1 — twMerge last-occurrence-wins for the ring swap)
  </read_first>
  <behavior>
    - Test: a Button with focusEmphasis="high" has className containing "focus-visible:ring-4" and "focus-visible:ring-ring" (not "ring-ring/50")
    - Test: a Button with focusEmphasis="default" (and a plain Button with no focusEmphasis) keeps the baseline "focus-visible:ring-3" and "focus-visible:ring-ring/50"
    - Test: a POSButton touchSize="default" className contains "min-h-[44px]" AND still contains the baseline "focus-visible:ring-3" (baseline ring survives the touchSize merge — FOCUS-01)
    - Test: POSButton touchSize="large" contains "min-h-[56px]" and "text-base"; touchSize="xl" contains "min-h-[72px]", "text-lg", "font-semibold"
  </behavior>
  <action>
    In src/shared/ui/button.tsx, add a new `focusEmphasis` slot to the `buttonVariants` CVA `variants` object with two keys: `default: ''` and `high: 'focus-visible:ring-4 focus-visible:ring-ring'` (D-09 — reuse the existing --ring token at full opacity, no new color). Add `focusEmphasis: 'default'` to `defaultVariants`. Destructure `focusEmphasis = 'default'` in the `Button()` function signature alongside `variant`/`size`, and pass it into the `cn(buttonVariants({ variant, size, focusEmphasis, className }))` call — mirror exactly how `variant` and `size` are threaded today. VariantProps<typeof buttonVariants> auto-picks up the new prop, so ButtonProps and POSButtonProps need no manual edit. Do NOT touch the base focus-visible ring classes in the CVA base string (D-08 — baseline stays ring-3/ring-ring/50). Then create src/shared/ui/button.test.tsx and src/shared/ui/POSButton.test.tsx as plain Vitest + React Testing Library files (import render/screen from @testing-library/react, describe/it/expect from vitest, following PageContainer.test.tsx). Render each button, read the rendered element's className via screen.getByRole('button'), and assert the class substrings named in the behavior block. These tests close Assumption A1 (verify twMerge actually resolves the ring swap) rather than trusting it.
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/ui/button.test.tsx src/shared/ui/POSButton.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focusEmphasis" src/shared/ui/button.tsx` >= 3 (variant slot, defaultVariants, Button signature)
    - button.test.tsx asserts both the ring-4/ring-ring (high) and ring-3/ring-ring/50 (default) class outputs
    - POSButton.test.tsx asserts min-h-[44px]/min-h-[56px]/min-h-[72px] for default/large/xl
    - `npx vitest run src/shared/ui/button.test.tsx src/shared/ui/POSButton.test.tsx` passes; typecheck exit 0; lint exit 0 (max-warnings 0)
  </acceptance_criteria>
  <done>focusEmphasis variant exists on buttonVariants and is threaded through Button(); baseline ring is untouched; both new unit test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add confirmClassName passthrough to ConfirmDialog + unit test</name>
  <files>src/shared/ui/ConfirmDialog.tsx, src/shared/ui/ConfirmDialog.test.tsx</files>
  <read_first>
    - src/shared/ui/ConfirmDialog.tsx (ConfirmDialogProps interface at lines 24-47 and the AlertDialogAction className merge at lines 131-149 — the exact cn() call you extend)
    - src/shared/ui/alert-dialog.tsx (AlertDialogAction — confirms it styles via buttonVariants() directly and already accepts+merges className, so a class string is sufficient; no focusEmphasis prop threading needed)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-RESEARCH.md (Architecture Pattern 3 — minimal-diff confirmClassName approach, chosen per Assumption A2)
  </read_first>
  <action>
    In src/shared/ui/ConfirmDialog.tsx, add an optional `confirmClassName?: string` field to the `ConfirmDialogProps` interface (mirror the shape/JSDoc style of the existing `confirmLabel`/`confirmDisabled` props). Destructure `confirmClassName` in the component function params. Merge it into the EXISTING `cn(...)` call on the `AlertDialogAction` (currently `cn(variant === 'destructive' && '...')`) by appending `confirmClassName` as the last argument, so it wins last per twMerge. This is opt-in and additive — do NOT change any default, since ConfirmDialog has ~20 call sites app-wide (RESEARCH Anti-Pattern: never make it a default; it would silently resize payment-critical dialogs reserved for Phase 33). Then create src/shared/ui/ConfirmDialog.test.tsx (Vitest + RTL) that renders ConfirmDialog open with confirmClassName="min-h-[72px] focus-visible:ring-4" and asserts the confirm button (getByRole('button', { name: confirmLabel })) carries both classes — this closes Pitfall 2 (proves the class actually reaches AlertDialogAction, since it is not POSButton-based).
  </action>
  <verify>
    <automated>cd bar-pos && npx vitest run src/shared/ui/ConfirmDialog.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "confirmClassName" src/shared/ui/ConfirmDialog.tsx` >= 2 (prop declaration + merge site)
    - The confirm button (not cancel) receives confirmClassName; cancel button is unchanged
    - ConfirmDialog.test.tsx asserts the passed classes land on the confirm button
    - No default behavior change: rendering ConfirmDialog without confirmClassName produces identical markup to before (existing 20 call sites unaffected)
    - typecheck exit 0; lint exit 0
  </acceptance_criteria>
  <done>ConfirmDialog exposes an opt-in confirmClassName that merges onto the confirm button, verified by a unit test; no default is altered.</done>
</task>

<task type="auto">
  <name>Task 3: Raise SearchInput clear button to a 44px touch target (shared-primitive, ripple-checked)</name>
  <files>src/shared/ui/SearchInput.tsx</files>
  <read_first>
    - src/shared/ui/SearchInput.tsx (the clear Button at lines 116-127 — currently h-7 w-7 / 28px, absolutely positioned inside the input)
    - src/shared/ui/input.tsx (Input is h-10 / 40px — the container height the clear button sits inside)
    - src/shared/ui/QuantityControl.tsx (lines 80-90 — the canonical 44px icon-button shape h-11 w-11, the pattern to match for icon-only buttons)
    - .planning/phases/32-touch-target-focus-visible-sweep/32-CONTEXT.md (Integration Points — SearchInput ripple warning)
  </read_first>
  <action>
    In src/shared/ui/SearchInput.tsx, change the clear (X) Button's size classes from `h-7 w-7` to `h-11 w-11` (44px square, matching QuantityControl's icon-button shape — TOUCH-01, D-01). Keep it as a shadcn Button with `size="icon"`, keep `variant="ghost"`, keep `absolute right-1 top-1/2 -translate-y-1/2` so the 44px square stays vertically centered on the 40px input (it extends ~2px above/below symmetrically — acceptable, no layout break). Add `touch-manipulation` to the className (matches QuantityControl). Keep the inner `<X className="h-4 w-4" />` icon size unchanged. This is a class-only change, zero behavior change. SearchInput is a shared primitive imported ONLY by src/shared/ui/DataTable.tsx (verified) — the change ripples to every DataTable search box app-wide but is markup/class-level with no handler/prop change, consistent with Phase 31's shared-primitive sweeps and Phase 33's COMPONENT-04 "markup/class-level swaps only" allowance.
  </action>
  <verify>
    <automated>cd bar-pos && grep -q "h-11 w-11" src/shared/ui/SearchInput.tsx && ! grep -q "h-7 w-7" src/shared/ui/SearchInput.tsx && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Clear button uses h-11 w-11 (44px); no h-7 w-7 remains in SearchInput.tsx
    - `grep -rl "SearchInput" src --include=*.tsx --include=*.ts | grep -v -E "SearchInput.tsx|index.ts"` returns only src/shared/ui/DataTable.tsx (ripple surface confirmed = DataTable only)
    - Full unit suite (`npm run test`) green — no DataTable-consuming component test regresses
    - typecheck exit 0; lint exit 0
  </acceptance_criteria>
  <done>SearchInput's clear button is a 44px touch target; the only importer is DataTable; full unit suite passes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 32-01)

- `focusEmphasis` — new CVA variant on `buttonVariants` in `src/shared/ui/button.tsx`; values `'default' | 'high'`; `high` => `focus-visible:ring-4 focus-visible:ring-ring`. Auto-exposed as a `Button`/`POSButton` prop via `VariantProps`.
- `confirmClassName?: string` — new opt-in field on `ConfirmDialogProps` (`src/shared/ui/ConfirmDialog.tsx`); merged last onto the confirm `AlertDialogAction`.
- New file `src/shared/ui/button.test.tsx` — focus-ring class assertions.
- New file `src/shared/ui/POSButton.test.tsx` — touchSize class assertions.
- New file `src/shared/ui/ConfirmDialog.test.tsx` — confirmClassName passthrough assertion.
- Modified `src/shared/ui/SearchInput.tsx` — 44px clear-button touch target (no new symbol).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/touch -> shared UI primitives | User interaction with Button/ConfirmDialog/SearchInput; no data crosses a network or trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Tampering | ConfirmDialog default behavior | mitigate | confirmClassName is opt-in only; no default changed, so no destructive-confirmation copy or gating behavior on any of the ~20 existing call sites (incl. payment flows) is altered — verified by ConfirmDialog.test.tsx asserting default render is unchanged |
| T-32-01-02 | Information Disclosure | Focus-ring on PIN-entry surfaces | accept | This plan changes only button/searchinput sizing and a new focus-ring variant; PINKeypad/ManagerPinDialog are untouched here, so no PIN-entry security surface is affected |
| T-32-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this phase — zero new dependencies (RESEARCH: Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `npx vitest run src/shared/ui/button.test.tsx src/shared/ui/POSButton.test.tsx src/shared/ui/ConfirmDialog.test.tsx` all green
- Full unit suite `npm run test` green (SearchInput ripple regression check)
- `npm run typecheck` exit 0, `npm run lint` exit 0
- `grep -c "focusEmphasis" src/shared/ui/button.tsx` >= 3; `grep -c "confirmClassName" src/shared/ui/ConfirmDialog.tsx` >= 2
</verification>

<success_criteria>
- focusEmphasis variant added to base Button, threaded through Button(), baseline ring untouched (D-08/D-09)
- confirmClassName opt-in passthrough added to ConfirmDialog, no default changed (RESEARCH Pattern 3)
- SearchInput clear button is 44px, ripple confirmed limited to DataTable (TOUCH-01/D-01)
- Three new unit-test files pass, closing Assumption A1 and Pitfall 2
</success_criteria>

<output>
Create `.planning/phases/32-touch-target-focus-visible-sweep/32-01-SUMMARY.md` when done
</output>
