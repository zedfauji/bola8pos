---
phase: 26-floating-tables-is-temp
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/entities/resource/index.ts
  - src/entities/resource/model/index.ts
  - src/entities/resource/model/types.ts
  - src/entities/resource/model/queries.ts
  - src/entities/resource/model/queries.test.ts
  - src/entities/resource/model/store.ts
  - src/entities/resource/model/usePoolTimer.ts
  - src/entities/resource/model/usePoolTimer.test.ts
  - src/entities/resource/ui/ResourceCard.tsx
  - src/entities/resource/ui/ResourceCard.stories.tsx
  - src/entities/resource/ui/ResourceIllustration.tsx
  - src/shared/lib/domain.ts
  - src/shared/lib/supabase.ts
  - src/shared/lib/supabase-realtime.ts
  - src/shared/lib/agent/tools/posTools.ts
  - src/shared/lib/agent/tools/guardTools.ts
  - src/shared/lib/agent/tools/index.ts
  - src/app/PoolRealtimeListener.tsx
  - src/app/WaitlistRealtimeListener.tsx
  - src/entities/tab/model/queries.ts
  - src/entities/promotion/model/pool-promotions-rpc.integration.test.ts
  - src/features/close-tab/index.ts
  - src/features/close-tab/tests/useCloseTab.test.ts
  - src/features/seat-waitlist-party/ui/SeatPartySheet.tsx
  - src/features/stop-and-move-table/useStopAndMoveSession.ts
  - src/widgets/PoolTableOccupancyPanel/ui/PoolTableOccupancyPanel.tsx
  - src/widgets/WaitlistQueue/ui/WaitlistQueue.tsx
  - src/widgets/SettingsTabsPanel/tabs/PoolTablesSettingsTab.tsx
autonomous: true
requirements: [SC-1, SC-4]
user_setup: []

must_haves:
  truths:
    - "The application typechecks, lints, and passes its unit suite against the renamed schema (SC-1)."
    - "A staff member can start and stop a pool timer and be billed correctly, exactly as before the rename (SC-4)."
    - "Realtime updates to the renamed relation still invalidate the client cache and refresh the table grid."
    - "Tab transfer and stop-and-move still resolve their target table."
    - "The unrelated settings-row key is untouched and settings backup/restore behavior is unchanged (Pitfall 2)."
  artifacts:
    - src/entities/resource/model/queries.ts
    - src/entities/resource/ui/ResourceCard.tsx
    - src/shared/lib/domain.ts
  key_links:
    - "PostgREST embed hint `fk_resources_current_session` in the entity query must match the constraint renamed by Plan 01 Task 2, or the current-session embed silently returns null."
    - "PostgREST embed resource names in tab queries and close-tab follow the relation name, so `pool_tables!pool_sessions_table_id_fkey` becomes `resources!pool_sessions_table_id_fkey`."
    - "Realtime channel table filters in the app listeners must name the renamed relation or realtime silently stops firing."
    - "TanStack Query cache keys must agree across the entity layer and the three inline hand-rolled queries, or the grid and the waitlist sheet desynchronize."
  prohibitions:
    - "The settings-row key literal in SettingsKeySchema and SETTINGS_KEYS must not be renamed (Pitfall 2)."
    - "No user-facing copy, route path, nav label, or i18n translation value may change (D-02)."
    - "No behavior change to billing math, session start/stop logic, or RLS-relevant query filters — this plan is a rename only."
    - "No new Postgres enum type may be introduced."
    - "The `.max(30)` bound must not be replaced with another fixed numeric ceiling."
---

<!-- planner-discipline-allow: pool_tables -->
<!-- planner-discipline-allow: PoolTable -->

<objective>
Sweep every source reference from the old relation and entity naming to `resources` / `Resource`, move the entity folder, and restore the application to green. This is the source-side half of D-01's full rename — Plan 01 covered the database half, and D-01's discretion note left the schema-first / call-sites-after split to planning, which is the split used here.

Purpose: Plan 01 renamed the database and regenerated the types, which left the application knowingly red. This plan is the restoration gate. Because the regenerated types now make every stale reference a compile error, `npm run typecheck` is an exhaustive, self-maintaining worklist — drive it to zero rather than working from a hand-copied file list.

Two categories escape the compiler and need explicit attention: files using the `supabase as any` escape hatch (where stale names are runtime-only bugs), and PostgREST embed strings (where the resource name follows the relation name inside a select string the compiler does not parse).

Output: entity folder renamed to `src/entities/resource/`, domain schemas renamed, all call sites swept, and `typecheck` + `lint` + unit tests + the pool-timer E2E spec green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-floating-tables-is-temp/26-CONTEXT.md
@.planning/phases/26-floating-tables-is-temp/26-RESEARCH.md
@.planning/phases/26-floating-tables-is-temp/26-PATTERNS.md
@.planning/phases/26-floating-tables-is-temp/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the domain schemas and move the entity folder</name>
  <files>src/shared/lib/domain.ts, src/entities/resource/**</files>
  <read_first>
    - src/shared/lib/domain.ts — lines 98-101 (`PoolTableStatusSchema`, `PoolTableTypeSchema`, `PoolTableType`), lines 567-585 (`PoolTableSchema`, `PoolTableCreateSchema`, `PoolTableUpdateSchema` and their inferred types), lines 1283-1284 and 1404 (the schema-registry / inferred-type map entries), and line 833 (the settings-key literal that must NOT change)
    - src/entities/pool-table/model/queries.ts — all of it; this is the largest file in the move and contains the PostgREST embed hints
    - src/entities/pool-table/model/store.ts — the realtime-apply reducer whose `table` discriminant is a string union of relation names
    - src/entities/pool-table/index.ts and src/entities/pool-table/model/index.ts — the public barrel exports every importer depends on
    - src/entities/pool-table/ui/PoolTableCard.tsx — the `TABLE_TYPE_LABEL_KEY` and `TABLE_TYPE_VARIANT` lookup tables, extended by Plan 04
    - .planning/phases/26-floating-tables-is-temp/26-PATTERNS.md — the per-file rename mapping
  </read_first>
  <action>
Rename in `src/shared/lib/domain.ts`, editing in place: `PoolTableStatusSchema` to `ResourceStatusSchema`, `PoolTableTypeSchema` to `ResourceTypeSchema`, `PoolTableType` to `ResourceType`, `PoolTableSchema` to `ResourceSchema`, `PoolTableCreateSchema` to `ResourceCreateSchema`, `PoolTableUpdateSchema` to `ResourceUpdateSchema`, and the inferred types `PoolTable` / `PoolTableCreate` / `PoolTableUpdate` to `Resource` / `ResourceCreate` / `ResourceUpdate`. Update the schema-registry entries near lines 1283-1284 and the inferred-type map near line 1404 to the new key and symbol names.

Remove the `.max(30)` bound from the `number` field, keeping `.min(1)`. Per Pitfall 5 the database constraint is only `number > 0` with no ceiling, and because the SELECT policy hides soft-deleted rows while the UNIQUE constraint still counts them, floating-table numbers are consumed permanently and only ever climb. Any replacement ceiling would eventually be hit and would fail as a client-side validation error rather than a database error, which is hard to spot in testing against a fresh database. Do not substitute a larger fixed bound.

Do not add an `isTemp` field or a `'floating'` type value here — those belong to Plan 03, after the column and CHECK constraint exist in the database.

Leave line 833 exactly as it stands. That literal is a `settings` row key for an unrelated config blob and collides with the old relation name only by coincidence (Pitfall 2); renaming it would require a `settings` data migration and is outside this phase.

Move the entity folder with `git mv src/entities/pool-table src/entities/resource` so history is preserved, then rename the three UI files to `ResourceCard.tsx`, `ResourceCard.stories.tsx`, and `ResourceIllustration.tsx`. Keep `usePoolTimer.ts` and `usePoolTimer.test.ts` under their current names — the timer is genuinely about pool sessions, `pool_sessions` was not renamed, and D-02 keeps the pool concept user-visible.

Inside the moved folder rename the exported symbols: `poolTableKeys` to `resourceKeys` (its `all` cache key becomes `['resources']`), `PoolTableRow` to `ResourceRow`, `mapPoolTableRow` to `mapResourceRow`, `usePoolTables` to `useResources`, `usePoolTable` to `useResource`, `useMutationAddPoolTable` to `useMutationAddResource`, `useMutationUpdatePoolTable` to `useMutationUpdateResource`, `useMutationDeletePoolTable` to `useMutationDeleteResource`, `useMutationReleasePoolTable` to `useMutationReleaseResource`, `usePoolTableStore` to `useResourceStore`, and `PoolTableCard` / `PoolTableCardProps` to `ResourceCard` / `ResourceCardProps`. Leave session-oriented exports alone: `useMutationStartSession`, `useMutationStopSession`, `usePoolSessionsByTab`, `useMutationLinkPoolSessionToTab`, `useMutationUpdateSessionStartTime`, `usePoolTimer`, `selectActiveSessionForTable`, and `selectSessionsByTabId` all describe sessions, not the renamed relation.

Update the `.from(...)` targets, the `Tables<...>` / `TablesInsert<...>` / `TablesUpdate<...>` generic arguments, and the structured logger event prefixes to the new relation name.

Two embed hints inside the select strings need care because the compiler does not check them: the current-session embed must use `fk_resources_current_session`, matching the constraint Plan 01 renamed, and the previous-table embed keeps `pool_sessions_previous_table_id_fkey` because that FK lives on the untouched `pool_sessions` relation. Getting the first wrong makes the embed silently return null rather than erroring.

In `store.ts`, update the realtime-apply reducer's `table` discriminant union and the branch that compares against it, so realtime INSERT/UPDATE/DELETE events for the renamed relation still reach the store.

Keep the existing `i18next/no-literal-string` disable comments around query-builder chains and cache keys. Those literals are wire identifiers, not user copy, and the rule is enforced repo-wide.

Change no billing math, no session lifecycle logic, and no query filter predicates. This is a rename.
  </action>
  <verify>
    <automated>cd bar-pos && test ! -d src/entities/pool-table && npx vitest run src/entities/resource/model/usePoolTimer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/entities/pool-table/` no longer exists and `src/entities/resource/` contains the moved files.
    - `git log --follow src/entities/resource/model/queries.ts` shows history predating this phase, confirming `git mv` was used.
    - `grep -n 'max(30)' src/shared/lib/domain.ts` returns nothing, and the `number` field still carries `.min(1)`.
    - `src/shared/lib/domain.ts` exports `ResourceSchema`, `ResourceCreateSchema`, `ResourceUpdateSchema`, `ResourceStatusSchema`, `ResourceTypeSchema`, and the types `Resource`, `ResourceCreate`, `ResourceUpdate`, `ResourceType`.
    - The settings-key literal near line 833 of `src/shared/lib/domain.ts` is byte-identical to its pre-task state (`git diff` shows no change on that line).
    - `src/entities/resource/model/queries.ts` contains `fk_resources_current_session` and still contains `pool_sessions_previous_table_id_fkey`.
    - `src/entities/resource/model/queries.ts` contains no `.max(` bound on a table number and no new query filter predicates versus its pre-move state (`git diff -M` shows only rename-shaped edits).
    - `npx vitest run src/entities/resource/model/usePoolTimer.test.ts` passes with the test file unmodified apart from its import paths — billing math is unchanged (SC-4).
    - `src/entities/resource/index.ts` exports `ResourceCard` and `ResourceCardProps`.
  </acceptance_criteria>
  <done>Domain schemas renamed with the number ceiling removed, entity folder moved with history intact, and the pre-existing pool-timer billing test passes untouched.</done>
  <reversibility rating="costly">A folder move plus a repo-wide symbol rename; reverting is mechanical but touches every importer again.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Sweep remaining call sites to zero, including the compiler-invisible ones</name>
  <files>src/app/PoolRealtimeListener.tsx, src/app/WaitlistRealtimeListener.tsx, src/entities/tab/model/queries.ts, src/entities/promotion/model/pool-promotions-rpc.integration.test.ts, src/features/close-tab/index.ts, src/features/close-tab/tests/useCloseTab.test.ts, src/features/seat-waitlist-party/ui/SeatPartySheet.tsx, src/features/stop-and-move-table/useStopAndMoveSession.ts, src/shared/lib/supabase.ts, src/shared/lib/supabase-realtime.ts, src/shared/lib/agent/tools/posTools.ts, src/shared/lib/agent/tools/guardTools.ts, src/shared/lib/agent/tools/index.ts, src/widgets/PoolTableOccupancyPanel/ui/PoolTableOccupancyPanel.tsx, src/widgets/WaitlistQueue/ui/WaitlistQueue.tsx, src/widgets/SettingsTabsPanel/tabs/PoolTablesSettingsTab.tsx</files>
  <read_first>
    - src/entities/tab/model/queries.ts — lines 86, 144, and 278; the embed alias appears as a select-string resource name AND as the destructured response property, so both sides must move together
    - src/features/close-tab/index.ts — lines 17, 29, and 39; same embed-alias-plus-destructure pairing, and line 29's select string is the guard that makes Pitfall 1 true
    - src/features/seat-waitlist-party/ui/SeatPartySheet.tsx — lines 33-53, a hand-rolled inline query using the `supabase as any` escape hatch; stale names here are runtime bugs the compiler cannot see
    - src/widgets/PoolTableOccupancyPanel/ui/PoolTableOccupancyPanel.tsx — lines 21-24, same escape-hatch pattern
    - src/widgets/WaitlistQueue/ui/WaitlistQueue.tsx — lines 32-35, same escape-hatch pattern
    - src/shared/lib/agent/tools/posTools.ts — lines 92, 103, 466, 474, 477, 487, 505, 544, 581, 602; a mix of `.from(...)` targets, `assertExists` relation arguments, and AI-tool NAMES and descriptions
    - src/shared/lib/agent/tools/guardTools.ts — line 159
    - src/shared/lib/agent/tools/index.ts — line 80, the dispatch switch whose case label must stay in lockstep with the tool name in posTools.ts
    - src/shared/lib/supabase-realtime.ts — lines 108-116, the channel table filter
    - src/app/PoolRealtimeListener.tsx line 22 and src/app/WaitlistRealtimeListener.tsx lines 12, 28-29, 42 — channel table filters plus a hardcoded inline cache key that must match `resourceKeys.all` from Task 1
    - src/shared/lib/supabase.ts — line 88, the exported `PoolTableRow` row-type alias
    - src/shared/lib/rbac.test.ts — line 76 asserts that a deliberately bogus action string is not a staff action; it is NOT a reference to the renamed relation and must stay as-is
  </read_first>
  <action>
Run `npm run typecheck` first and treat the error list as the primary worklist. Drive it to zero. The regenerated types make most stale references compile errors, so this is exhaustive for everything the compiler can see.

Then handle the three categories the compiler cannot see:

First, PostgREST embed resource names. In a select string, the embedded resource is named after the relation, so the old name becomes `resources` while the FK-hint suffix after the exclamation mark stays as-is because those constraints live on `pool_sessions`. In `src/entities/tab/model/queries.ts` and `src/features/close-tab/index.ts` the response property produced by the embed is destructured under that same name, so the TypeScript property in the embed-shape type, the destructure site, and the select string must all move together. Missing one produces an undefined lookup at runtime with no compile error: in the close-tab case the tab number silently falls back to its default, and in the tab-queries case the table label disappears from the tab view.

Second, escape-hatch queries. `SeatPartySheet.tsx`, `PoolTableOccupancyPanel.tsx`, and `WaitlistQueue.tsx` each hand-roll an inline TanStack Query with `supabase as any`. Update both the `.from(...)` target and the TanStack cache key in each, and make the cache key match `resourceKeys.all` from Task 1 so these three inline consumers invalidate together with the entity layer. The same applies to the hardcoded inline cache key in `WaitlistRealtimeListener.tsx`, whose own comment notes it was inlined because the entity key was unavailable — point it at the renamed key value.

Third, realtime channel filters. `PoolRealtimeListener.tsx`, `WaitlistRealtimeListener.tsx`, and `supabase-realtime.ts` each pass the relation name as a postgres_changes table filter. These fail silently: a filter naming a relation that no longer exists simply never fires, so the table grid stops live-updating with no error anywhere.

For the AI agent tools, update the `.from(...)` targets and the `assertExists` relation arguments in `posTools.ts` and `guardTools.ts`. The tool NAME `list_pool_tables` and its human-readable descriptions are a model-facing contract describing a user-facing concept that D-02 keeps unchanged, so leave the tool name and description text alone; if you do rename the tool name, the dispatch case label in `agent/tools/index.ts` line 80 must change in the same edit or the tool becomes unreachable. Prefer leaving it.

Rename the exported row-type alias in `src/shared/lib/supabase.ts` line 88 to `ResourceRow` and update its `Database['public']['Tables'][...]` lookup.

Update the two test files that seed real rows (`pool-promotions-rpc.integration.test.ts`, `useCloseTab.test.ts`) to the new relation name so they keep working against the renamed live schema.

In `PoolTablesSettingsTab.tsx`, update only the imported hook names from Task 1. Leave the component name, the file path, the route, the tab label, and every translated string untouched — D-02 keeps the user-facing surface as it is.

Leave `src/shared/lib/rbac.test.ts` line 76 alone; it asserts a deliberately invalid action string is rejected and is not a reference to the renamed relation.

Leave the settings-key entry in `src/entities/settings/model/queries.ts` near line 117 alone (Pitfall 2).

Change no user-facing copy and no i18n translation values anywhere in this sweep.
  </action>
  <verify>
    <automated>cd bar-pos && test "$(grep -rn 'pool_tables' src --include='*.ts' --include='*.tsx' | grep -v 'manage_pool_tables' | grep -vE "^src/(entities/settings/model/queries|shared/lib/domain)\.ts:[0-9]+: *'pool_tables',$" | wc -l)" -eq 0 && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - The residual-reference gate above reports zero hits; the only tolerated survivors are the two settings-key array entries and the bogus RBAC action string, each excluded by an explicit pattern rather than by a blanket file exclusion.
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0 at max-warnings 0.
    - `npm run test` exits 0.
    - `git diff` on `src/entities/settings/model/queries.ts` is empty.
    - `git diff` on `src/shared/lib/rbac.test.ts` is empty.
    - Every `.from(` call in the three escape-hatch components names the renamed relation, and each of their TanStack cache keys equals the value of `resourceKeys.all`.
    - Every postgres_changes table filter across the two app listeners and `supabase-realtime.ts` names the renamed relation.
    - No select string in `src/entities/tab/model/queries.ts` or `src/features/close-tab/index.ts` pairs a renamed embed resource with an un-renamed destructured property, or the reverse.
    - `git diff` across all i18n locale JSON files under `src/shared/lib/i18n/locales/` is empty (D-02).
  </acceptance_criteria>
  <done>Zero residual references outside the two documented collisions; typecheck, lint, and the unit suite are green; realtime filters, embed aliases, and escape-hatch cache keys all name the renamed relation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated staff client to Postgres | Every swept `.from(...)` call crosses RLS; a changed filter predicate changes what a role can read |
| Realtime websocket to client cache | Channel table filters decide what live data reaches the store |
| AI agent tool layer to Postgres | `posTools` / `guardTools` issue queries on behalf of a model-driven caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-08 | Information Disclosure | Escape-hatch queries in `SeatPartySheet`, `PoolTableOccupancyPanel`, `WaitlistQueue` | high | mitigate | These use `supabase as any`, so a mistyped filter is invisible to the compiler and could widen the result set. Task 2 restricts edits to the relation name and cache key only; acceptance requires `git diff -M` to show rename-shaped edits with no added or removed query filters. RLS remains the authoritative boundary regardless. |
| T-26-09 | Tampering | Settings-row key collision (Pitfall 2) | medium | mitigate | A blanket find-and-replace would corrupt an unrelated config-blob key and break settings backup/restore. Acceptance requires an empty `git diff` on the settings queries file, and the residual-reference gate excludes that exact line shape rather than the whole file. |
| T-26-10 | Denial of Service | Realtime channel table filters | medium | mitigate | A filter naming a nonexistent relation fails silently and stops live updates with no error. Acceptance asserts every postgres_changes filter names the renamed relation. |
| T-26-11 | Spoofing | AI agent tool name and dispatch case label | medium | mitigate | Renaming the tool name without the matching dispatch case makes the tool unreachable, and a mismatched pair could dispatch to the wrong handler. Task 2 directs leaving both unchanged, and requires them to move together if changed. |
| T-26-12 | Tampering | Integration tests that seed and delete real rows | medium | accept | These tests create and clean up their own rows against a live project and are already env-guarded via `describe.skipIf`. Renaming their relation target introduces no new privilege; no additional control needed. |
| T-26-13 | Elevation of Privilege | RBAC action-string test | low | accept | The bogus action string at `rbac.test.ts:76` is a negative assertion, not a relation reference. Left untouched; acceptance asserts an empty diff. |
</threat_model>

<verification>
1. Residual-reference gate: zero hits outside the two documented collisions.
2. `npm run typecheck` exits 0.
3. `npm run lint` exits 0 at max-warnings 0.
4. `npm run test` exits 0.
5. `npx playwright test e2e/04-pool-timer.spec.ts` passes — the end-to-end proof that start, stop, and billing survive the rename (SC-1, SC-4). Per CLAUDE.md this needs a desktop display session and `google-chrome-stable`; if the environment cannot provide them, record the spec as deferred to the phase gate rather than marking it green.
6. `git diff` empty for `src/entities/settings/model/queries.ts`, `src/shared/lib/rbac.test.ts`, and all files under `src/shared/lib/i18n/locales/`.
</verification>

<success_criteria>
- Application restored to green after Plan 01's declared transient red (SC-1).
- Pool timer start, stop, and billing behave exactly as before, proven by the pre-existing unit test passing unmodified and by the pool-timer E2E spec (SC-4).
- Realtime, embed aliases, and escape-hatch cache keys all name the renamed relation.
- The settings-key collision and all user-facing copy are untouched (Pitfall 2, D-02).
</success_criteria>

## Artifacts this phase produces

Symbols and paths created or renamed by Plan 02:

**New file paths** (moved from `src/entities/pool-table/`)
- `src/entities/resource/index.ts`, `src/entities/resource/model/index.ts`, `src/entities/resource/model/types.ts`, `src/entities/resource/model/queries.ts`, `src/entities/resource/model/queries.test.ts`, `src/entities/resource/model/store.ts`, `src/entities/resource/model/usePoolTimer.ts`, `src/entities/resource/model/usePoolTimer.test.ts`
- `src/entities/resource/ui/ResourceCard.tsx`, `src/entities/resource/ui/ResourceCard.stories.tsx`, `src/entities/resource/ui/ResourceIllustration.tsx`

**Zod schemas and inferred types** (`src/shared/lib/domain.ts`)
- `ResourceSchema`, `ResourceCreateSchema`, `ResourceUpdateSchema`, `ResourceStatusSchema`, `ResourceTypeSchema`
- `Resource`, `ResourceCreate`, `ResourceUpdate`, `ResourceType`

**Entity-layer exports** (`src/entities/resource/`)
- `resourceKeys`, `ResourceRow`, `mapResourceRow`
- `useResources`, `useResource`
- `useMutationAddResource`, `useMutationUpdateResource`, `useMutationDeleteResource`, `useMutationReleaseResource`
- `useResourceStore`
- `ResourceCard`, `ResourceCardProps`

**Shared-layer exports**
- `ResourceRow` row-type alias in `src/shared/lib/supabase.ts`

Unchanged and deliberately NOT renamed: `usePoolTimer`, `UsePoolTimerOptions`, `useMutationStartSession`, `useMutationStopSession`, `usePoolSessionsByTab`, `useMutationLinkPoolSessionToTab`, `useMutationUpdateSessionStartTime`, `selectTableById`, `selectActiveSessionForTable`, `selectAvailableTableCount`, `selectSessionsByTabId`, `PoolSessionSchema`, `PoolTablesSettingsTab`, and the `list_pool_tables` agent tool name.

<output>
Create `.planning/phases/26-floating-tables-is-temp/26-02-SUMMARY.md` when done. Record the final `resourceKeys.all` cache-key value, since Plan 04 adds a fourth consumer that must match it.
</output>
