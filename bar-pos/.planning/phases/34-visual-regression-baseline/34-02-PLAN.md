---
phase: 34-visual-regression-baseline
plan: 02
type: execute
wave: 1
depends_on: ["34-01"]
files_modified:
  - e2e/visual/45-visual-baseline.spec.ts
autonomous: false
requirements: [VISUAL-02, VISUAL-03]

must_haves:
  truths:
    - "Baseline PNGs exist for all 17 admin routes (D-04), the bartender accessible subset (11 routes) and the manager accessible subset (14 routes) per the code-verified route×role matrix (D-05/D-13)"
    - "Every dynamic region is masked so re-runs are deterministic: pool timers, KDS board + live clock, /pos promo banner, and the toast container on every route (D-08/D-09/D-10/D-16/D-17)"
    - "Denied role-gated routes are captured per D-15: pixel-identical redirects assert URL === /home (no screenshot); /audit's denial renders a distinguishing sonner toast and gets an actual screenshot"
    - "Two consecutive `npm run test:e2e:visual` runs (2nd WITHOUT --update-snapshots) both exit 0 with zero screenshot diffs (ROADMAP success criterion 4 / VISUAL-03)"
    - "Baseline PNGs are gitignored — `git check-ignore` confirms the snapshots dir is untracked (D-12)"
  artifacts:
    - "e2e/visual/45-visual-baseline.spec.ts (new — the baseline capture spec)"
    - "e2e/visual/45-visual-baseline.spec.ts-snapshots/*.png (generated by --update-snapshots, gitignored, local-only)"
  key_links:
    - "mask locators (getByTestId('live-time-display'), getByTestId('kds-board'), getByTestId('elapsed-minutes'), getByTestId('active-promotions-banner'), locator('[data-sonner-toaster]')) ↔ the attributes added in Plan 01 + already-existing hooks"
    - "visual config testDir ./e2e/visual ↔ this spec's location (isolated run via test:e2e:visual)"
    - "role accessible-route arrays ↔ RESEARCH Pattern 3 route×role matrix (NOT CONTEXT D-05's illustrative list)"
---

<objective>
Write the visual-baseline spec that photographs the post-Phase-33.1 UI, seed the local baseline, and prove it is stable across two consecutive runs. This is the phase's actual deliverable: `e2e/visual/45-visual-baseline.spec.ts` capturing all 17 admin routes plus the bartender/manager accessible subsets, with every dynamic region masked, then a two-run zero-diff gate and a one-time human eyeball of the seeded PNGs.

Purpose: A deterministic screenshot baseline that freezes the now-standardized UI (Phases 29-33.1) so future drift is caught. Captures happen only AFTER the UI-standardization fixes shipped (VISUAL-02 — a pre-fix baseline would freeze the old inconsistencies).

Output: `e2e/visual/45-visual-baseline.spec.ts`, its gitignored local baseline PNGs, and a proven two-consecutive-run zero-diff gate.
</objective>

<artifacts>
## Artifacts this phase produces (Plan 02 portion)

New files:
- `e2e/visual/45-visual-baseline.spec.ts` — the baseline capture spec (`45` is the next free spec number after `44-focus-tab-order`; lives in the new `e2e/visual/` subfolder per D-11)

Generated (gitignored, local-only per D-12 — NOT committed):
- `e2e/visual/45-visual-baseline.spec.ts-snapshots/*.png` — one baseline PNG per route×role capture (role-prefixed filenames, e.g. `admin-home.png`, `bartender-pos.png`, `manager-reports.png`, `bartender-audit-denied.png`)

(Plan 01 produced `playwright.visual.config.ts`, the `testIgnore` isolation, the `test:e2e:visual` script, the `.gitignore` snapshot pattern, and the `live-time-display` / `kds-board` mask hooks.)
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-visual-regression-baseline/34-CONTEXT.md
@.planning/phases/34-visual-regression-baseline/34-RESEARCH.md
@.planning/phases/34-visual-regression-baseline/34-PATTERNS.md

# Precondition (runtime, same as the functional E2E suite already requires per CLAUDE.md):
# - `npm run dev` (Vite dev server on :1420) running or auto-spawned by webServer
# - `.env.local` with VITE_SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / E2E_*_PIN present
# - live Supabase reachable (seeding + loginAs use it)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the visual-baseline spec (all routes, roles, masks, denied assertions)</name>
  <files>e2e/visual/45-visual-baseline.spec.ts</files>
  <read_first>
    - `e2e/16-table-status.spec.ts` — the seeded-table-id lookup + navigation pattern to reuse (`getOccupiedPoolTableIds`, `seedOccupiedTableDirect`); note the new spec is one dir deeper so imports become `'../fixtures'`, `'../helpers/auth'`, `'../helpers/supabase'`
    - `e2e/helpers/auth.ts` — `loginAs(page, role)` (line 70), `logout(page)` (line 101); admin PIN `0000`; loginAs lands on /home or /pos after PIN + optional opening-cash dialog
    - `e2e/helpers/supabase.ts` — reuse as-is: `getServiceClient()` (line 22), `resetTestState()` (line 54), `openCaja()` (line 130), `getOccupiedPoolTableIds()` (line 281), `seedKdsFoodOrder(tabId, routing)` (line 612); do NOT depend on `npm run setup:dev` (broken per D-14) — seed via these helpers
    - `e2e/fixtures.ts` — `test`/`expect` re-exports (console/pageerror tailing)
    - `.planning/phases/34-visual-regression-baseline/34-RESEARCH.md` § Pattern 3 (route×role matrix — the source of truth for accessible/denied per role, NOT CONTEXT D-05's illustrative list), Pitfalls 2-8, and Code Examples (masking + fullPage + document.fonts.ready)
    - `.planning/phases/34-visual-regression-baseline/34-PATTERNS.md` § `e2e/visual/45-visual-baseline.spec.ts` — import shape, seeding pattern, mask+fullPage+font-ready snippet, denied-route URL-assertion snippet
  </read_first>
  <action>
    Create `e2e/visual/45-visual-baseline.spec.ts`. Import `test`/`expect` from `'../fixtures'`, `loginAs`/`logout` from `'../helpers/auth'`, and the seed helpers from `'../helpers/supabase'`. Reuse all helpers unmodified — write NO new seed scripts (D-14).

    Test isolation: call `resetTestState()` in `beforeAll` (also clears shifts, so `/staff` has no live shift-duration tick per D-17), and again in `afterAll`. Before capturing KDS and the occupied pool detail, seed once (reused across all roles): create/open a tab and call `seedKdsFoodOrder(tabId, 'KITCHEN')` + `seedKdsFoodOrder(tabId, 'BAR')` so both KDS boards are non-empty (D-16 — an empty board hides card layout/spacing drift); and `seedOccupiedTableDirect()` then `getOccupiedPoolTableIds()` to obtain the occupied table id for `/pool-tables/:tableId` (D-06/D-09). Open a caja via `openCaja()` if a captured route (POS/payments) requires it and loginAs's opening-cash dialog did not already handle it.

    Grouping/block granularity is implementer discretion (D-11 fixes only the file path/number): recommended — one `test.describe.serial` per role with a single `beforeAll` login, and either one `test()` per route or a single looped `test()` using `expect.soft(page).toHaveScreenshot(...)` so one failing capture does not abort the rest. Whichever you choose, use role-prefixed snapshot filenames (`admin-<route>.png`, `bartender-<route>.png`, `manager-<route>.png`; use `home`, `pos`, `pool-tables`, `pool-table-status`, etc. as the slug).

    Before EVERY `toHaveScreenshot()` call: `await page.evaluate(() => document.fonts.ready)` (Pitfall 8 — self-hosted Geist variable font). Use `{ fullPage: true, mask: [...] }` on every capture.

    Per-route mask array (D-08/D-10/D-17):
    - `/pos` → `page.getByTestId('active-promotions-banner')` + `page.locator('[data-sonner-toaster]')`
    - `/pool-tables/:tableId` (occupied) → `page.getByTestId('elapsed-minutes')` + `page.locator('[data-sonner-toaster]')`
    - `/kds` and `/kds-bar` → `page.getByTestId('kds-board')` + `page.getByTestId('live-time-display')` + `page.locator('[data-sonner-toaster]')`
    - every other route → `page.locator('[data-sonner-toaster]')` (defensive toast mask, D-10)

    Capture-order constraints (RESEARCH Anti-Patterns / Pitfalls 2, 7):
    - Capture `/login` FIRST, before any `loginAs()` — either in a fresh unauthenticated context or immediately after `logout(page)` (Pitfall 7: `/login` redirects to `/home` when already authenticated).
    - Capture `/pool-tables` (idle grid) BEFORE `seedOccupiedTableDirect()` runs, so the grid stays idle-state and needs no per-card timer mask (Pitfall 2 / Anti-Pattern). Capture the occupied `/pool-tables/:tableId` AFTER seeding the session.

    ADMIN role (D-04) — capture all 17 routes: `/login`, `/home`, `/pos`, `/pool-tables`, `/pool-tables/:tableId`, `/inventory`, `/staff`, `/reports`, `/settings`, `/rappi`, `/payments`, `/kds`, `/kds-bar`, `/kitchen-prep`, `/waitlist`, `/rbac`, `/audit`.

    BARTENDER role (D-05/D-13, RESEARCH Pattern 3) — capture the 11 accessible routes: `/home`, `/pos`, `/pool-tables`, `/pool-tables/:tableId`, `/payments`, `/rappi`, `/kds-bar`, `/inventory`, `/staff`, `/settings`, `/kitchen-prep`. Denied routes `/reports`, `/waitlist`, `/kds`, `/rbac` → assert redirect only: `await page.goto(route); await expect(page).toHaveURL(/\/home$/)` (D-15 — no screenshot, these are pixel-identical to `/home`). Denied route `/audit` → DOES get a screenshot: `await page.goto('/audit')`, wait for the denial toast (`await expect(page.locator('[data-sonner-toast]')).toBeVisible()`), then `toHaveScreenshot('bartender-audit-denied.png', { fullPage: true })` WITHOUT masking the toast (the toast text is the distinguishing subject per D-15; the underlying /home dashboard is static so no other mask is needed).

    MANAGER role (RESEARCH Pattern 3) — capture the 14 accessible routes: the bartender 11 PLUS `/reports`, `/waitlist`, `/audit`. Denied routes `/kds` and `/rbac` → URL assertion only (redirect to `/home`, D-15). Manager is NOT denied `/audit` (it is accessible) — capture it as a normal screenshot.

    Do NOT snapshot `/waitlist` under bartender (bartender is redirected — CONTEXT D-05's example is wrong here, corrected by D-13). Do NOT snapshot `/kds` under bartender OR manager (both lack `view_kds`).
  </action>
  <acceptance_criteria>
    - `e2e/visual/45-visual-baseline.spec.ts` exists and imports from `'../fixtures'`, `'../helpers/auth'`, `'../helpers/supabase'` (one dir deeper — relative paths)
    - The spec is discoverable and parses under the visual config: `npx playwright test --config=playwright.visual.config.ts --list` exits 0 and lists the baseline test block(s)
    - Spec references the correct mask hooks: `grep -q "getByTestId('live-time-display')" e2e/visual/45-visual-baseline.spec.ts` AND `grep -q "getByTestId('kds-board')" e2e/visual/45-visual-baseline.spec.ts` AND `grep -q "data-sonner-toaster" e2e/visual/45-visual-baseline.spec.ts` all succeed
    - Spec includes the font-ready wait: `grep -q "document.fonts.ready" e2e/visual/45-visual-baseline.spec.ts` succeeds
    - Spec includes the denied-route URL assertion and does NOT snapshot `/waitlist` for bartender: `grep -q "toHaveURL" e2e/visual/45-visual-baseline.spec.ts` succeeds
    - Spec seeds directly (no broken setup:dev dependency): `grep -q "seedKdsFoodOrder" e2e/visual/45-visual-baseline.spec.ts` succeeds AND `grep -q "setup:dev" e2e/visual/45-visual-baseline.spec.ts` FAILS (exit 1)
    - `npm run typecheck` exits 0 (no NEW errors beyond the 2 pre-existing baseline errors); `npm run lint` exits 0 on the new spec
    - Functional suite still excludes it: `npx playwright test --list 2>&1` shows no `e2e/visual/` entry (Plan 01 testIgnore holds)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npx playwright test --config=playwright.visual.config.ts --list >/dev/null 2>&1 && grep -q "getByTestId('live-time-display')" e2e/visual/45-visual-baseline.spec.ts && grep -q "getByTestId('kds-board')" e2e/visual/45-visual-baseline.spec.ts && grep -q "data-sonner-toaster" e2e/visual/45-visual-baseline.spec.ts && grep -q "document.fonts.ready" e2e/visual/45-visual-baseline.spec.ts && grep -q "toHaveURL" e2e/visual/45-visual-baseline.spec.ts && ! grep -q "setup:dev" e2e/visual/45-visual-baseline.spec.ts && npm run lint && echo SPEC_OK</automated>
  </verify>
  <done>`e2e/visual/45-visual-baseline.spec.ts` exists, lists under the visual config, captures the 17 admin routes + bartender/manager accessible subsets with correct per-route masks, asserts denied-route redirects via URL (screenshots only `/audit`'s toast), waits for fonts before each capture, and seeds via existing helpers (no setup:dev).</done>
</task>

<task type="auto">
  <name>Task 2: Seed the local baseline and prove the two-run zero-diff gate</name>
  <files>e2e/visual/45-visual-baseline.spec.ts-snapshots/ (generated, gitignored)</files>
  <read_first>
    - `.planning/phases/34-visual-regression-baseline/34-VALIDATION.md` § "Sampling Rate" / "Phase gate" — the two-run sequence (seed with --update-snapshots, then two clean runs) IS success criterion 4
    - `.planning/phases/34-visual-regression-baseline/34-RESEARCH.md` § "Validation Architecture" — full/quick run commands
    - `playwright.visual.config.ts` (created in Plan 01) — confirm `test:e2e:visual` maps to it
  </read_first>
  <action>
    Ensure the precondition holds: Vite dev server on :1420 (running or auto-spawned by the config's `webServer`), `.env.local` E2E credentials present, live Supabase reachable. If the environment cannot reach live Supabase / the dev server (the known execution-sandbox latency noted in STATE.md blockers), do NOT fake a pass — record the blocker in the SUMMARY and surface it; the gate must be run locally.

    Seed the baseline (first run): `npm run test:e2e:visual -- --update-snapshots`. This creates `e2e/visual/45-visual-baseline.spec.ts-snapshots/` populated with one PNG per capture. `--update-snapshots` always "passes" by definition — it does not prove correctness (that is Task 3's human eyeball).

    Prove stability (VISUAL-03 / success criterion 4): run `npm run test:e2e:visual` (WITHOUT `--update-snapshots`) TWICE back-to-back. Both runs must exit 0 with zero failing screenshot assertions. Any diff means a dynamic region was left unmasked — fix the mask in Task 1's spec, re-seed, and re-run until two consecutive clean runs pass.

    Confirm the baselines are gitignored (D-12): `git check-ignore e2e/visual/45-visual-baseline.spec.ts-snapshots` must return the path (exit 0), and `git status --porcelain` must show no `.png` under `e2e/visual/`. Do NOT `git add` any snapshot PNG.
  </action>
  <acceptance_criteria>
    - After `npm run test:e2e:visual -- --update-snapshots`, `e2e/visual/45-visual-baseline.spec.ts-snapshots/` exists and contains at least one `*.png`
    - Two consecutive `npm run test:e2e:visual` runs (no --update-snapshots) BOTH exit 0 with zero failing screenshot assertions
    - `git check-ignore -q e2e/visual/45-visual-baseline.spec.ts-snapshots` exits 0 (snapshots dir is ignored)
    - `git status --porcelain e2e/visual/ | grep -q '\.png'` FAILS (exit 1) — no PNG is staged/tracked
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && npm run test:e2e:visual && npm run test:e2e:visual && git check-ignore -q e2e/visual/45-visual-baseline.spec.ts-snapshots && ! (git status --porcelain e2e/visual/ | grep -q '\.png') && echo ZERO_DIFF_GATE_OK</automated>
  </verify>
  <done>The local baseline is seeded, two consecutive `npm run test:e2e:visual` runs exit 0 with zero diffs (proving all dynamic regions are masked, VISUAL-03), and the PNGs are confirmed gitignored (D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human eyeball of the seeded baseline PNGs</name>
  <files>e2e/visual/45-visual-baseline.spec.ts-snapshots/ (generated, gitignored)</files>
  <action>Blocking human-verify checkpoint. Because `--update-snapshots` always "passes" by definition, the initial baseline PNGs must be eyeballed once by a human before they are trusted as the canonical baseline (VALIDATION.md Manual-Only). Do not auto-approve; wait for the human resume-signal.</action>
  <what-built>The visual-regression baseline: `e2e/visual/45-visual-baseline.spec.ts` plus the seeded local baseline PNGs under `e2e/visual/45-visual-baseline.spec.ts-snapshots/` (admin 17 routes + bartender/manager accessible subsets + `/audit` denied-toast). Two consecutive `npm run test:e2e:visual` runs already exit 0 with zero diffs (Task 2).</what-built>
  <how-to-verify>
    Because `--update-snapshots` always "passes" by definition (VALIDATION.md Manual-Only), a human must eyeball the initial baseline once to confirm it captured the correct, unbroken post-Phase-33.1 UI (not a broken/half-loaded render frozen as "correct").

    1. Open the snapshot directory: `bar-pos/e2e/visual/45-visual-baseline.spec.ts-snapshots/`
    2. Review each PNG (role-prefixed: `admin-*.png`, `bartender-*.png`, `manager-*.png`, plus `bartender-audit-denied.png`). Confirm for each: the page rendered fully (no blank/spinner-only frame, no missing content), layout matches the standardized UI shipped in Phases 29-33.1, and the magenta mask boxes cover ONLY the intended dynamic regions (pool timer, KDS board, live clock, promo banner, toast container) — not real content.
    3. Confirm `bartender-audit-denied.png` shows the sonner permission-denied toast over the `/home` dashboard (D-15).
    4. Spot-check that no baseline is an accidental duplicate `/home` (e.g. a `/login` shot that silently redirected, or a role route that was actually denied).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the baseline, or describe which screenshots look wrong (broken layout / wrong mask / accidental /home duplicate) so the spec/masks can be corrected and the baseline re-seeded.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → local filesystem | Playwright writes baseline PNG snapshots (containing seeded, PII-looking data) to the local repo tree |
| local repo → git remote | `.gitignore` (added in Plan 01) is the only gate keeping those screenshots out of version control |
| seed helper (service-role) → Supabase | Test seeding uses `getServiceClient()` / `SUPABASE_SERVICE_ROLE_KEY` — same trust boundary the existing functional E2E suite already crosses, unchanged |

## STRIDE Threat Register

This phase adds no new authentication, authorization, session, or cryptography logic — the spec observes already-implemented, already-RBAC-gated screens and reuses the existing `loginAs()`/service-role seed helpers unchanged (RESEARCH § Security Domain: no new ASVS surface). Residual points are data-hygiene, low severity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Information Disclosure | baseline PNG snapshots created here (seeded customer names, in-progress PINs, dollar amounts) | low | mitigate | Task 2 confirms `git check-ignore` on the snapshots dir and asserts no `.png` is staged; PNGs stay local-only (D-12); no capture is ever committed |
| T-34-03 | Information Disclosure | service-role key used for seeding | low | accept | Reuses the existing `e2e/helpers/supabase.ts` `getServiceClient()` path already used by the functional suite; no new key handling, no key logged to snapshots |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this phase — `@playwright/test@1.59.1` already present; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `e2e/visual/45-visual-baseline.spec.ts` lists under the visual config and covers admin 17 routes + bartender 11 + manager 14 accessible routes with correct per-route masks and denied-route URL assertions (D-15)
- `npm run test:e2e:visual -- --update-snapshots` seeds the local baseline; two subsequent `npm run test:e2e:visual` runs both exit 0 with zero diffs (VISUAL-03 / success criterion 4)
- Baseline PNGs confirmed gitignored via `git check-ignore` (D-12)
- Human eyeball of the seeded PNGs confirms correct, unbroken post-Phase-33.1 UI with masks over dynamic regions only
</verification>

<success_criteria>
VISUAL-02 + VISUAL-03 satisfied: screenshot baselines captured for all 17 routes (admin) plus the bartender/manager accessible subsets — captured only after the Phase 29-33.1 fixes shipped; all dynamic/live regions (timers, KDS boards, live clock, promo banner, toasts) masked; two consecutive local `npm run test:e2e:visual` runs both exit 0 with zero diffs; baseline PNGs gitignored; human-confirmed correct on first seed.
</success_criteria>

<output>
Create `.planning/phases/34-visual-regression-baseline/34-02-SUMMARY.md` when done.
</output>
