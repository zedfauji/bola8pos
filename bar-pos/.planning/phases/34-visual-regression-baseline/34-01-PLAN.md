---
phase: 34-visual-regression-baseline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - playwright.visual.config.ts
  - playwright.config.ts
  - package.json
  - .gitignore
  - src/shared/ui/LiveTimeDisplay.tsx
  - src/widgets/KdsBoard/index.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-03]

must_haves:
  truths:
    - "Running `npm run test:e2e` (functional suite) does NOT pick up `e2e/visual/45-visual-baseline.spec.ts` (testIgnore excludes it)"
    - "`npm run test:e2e:visual` resolves to the isolated visual config (headless, bundled Chromium, no slowMo, no channel:'chrome')"
    - "The two mask target attributes `data-testid=\"live-time-display\"` and `data-testid=\"kds-board\"` exist on the live-clock span and the KDS board root, with zero className/behavior change"
  artifacts:
    - "playwright.visual.config.ts (new, from-scratch defineConfig, does NOT import playwright.config.ts)"
    - "testIgnore: /visual\\// line inside playwright.config.ts defineConfig"
    - "test:e2e:visual script in package.json"
    - "e2e/visual/**/*-snapshots/ entry in .gitignore"
    - "data-testid=\"live-time-display\" on src/shared/ui/LiveTimeDisplay.tsx root span"
    - "data-testid=\"kds-board\" on src/widgets/KdsBoard/index.tsx root grid div"
  key_links:
    - "playwright.config.ts testIgnore ↔ e2e/visual/ spec path (config-collision prevention, RESEARCH Pitfall 1)"
    - "data-testid mask hooks ↔ the mask locators the Plan 02 spec will target (RESEARCH Pitfalls 3 & 4)"
---

<objective>
Stand up the visual-regression harness so Plan 02's baseline spec has a fully isolated environment to run in and stable selectors to mask. This plan creates the second Playwright config, isolates the functional config from the new `e2e/visual/` subfolder, wires the `test:e2e:visual` npm script, gitignores the local-only baseline PNGs, and adds the two `data-testid` mask hooks the baseline spec needs.

Purpose: A second Playwright config that diverges from the functional one on every field D-01 requires (headless, bundled Chromium, no slowMo, no on-failure trace/video/screenshot, no functional-suite globalTeardown) — plus the config-collision fix (D-01/Pitfall 1) and the two mask test-hooks (D-17/Pitfalls 3 & 4). No baseline is captured here; that is Plan 02.

Output: `playwright.visual.config.ts`, a one-line `testIgnore` addition to `playwright.config.ts`, a `test:e2e:visual` script, a `.gitignore` entry, and two attribute-only `data-testid` additions.
</objective>

<artifacts>
## Artifacts this phase produces (Plan 01 portion)

New files:
- `playwright.visual.config.ts` — isolated visual-regression Playwright config

New attribute values (mask hooks, pure test hooks — attribute-only, zero style/behavior change):
- `data-testid="live-time-display"` — on `src/shared/ui/LiveTimeDisplay.tsx` root `<span>` (rendered in both `/kds` and `/kds-bar` headers)
- `data-testid="kds-board"` — on `src/widgets/KdsBoard/index.tsx` root grid `<div>`

New npm script:
- `test:e2e:visual` → `playwright test --config=playwright.visual.config.ts`

New `.gitignore` pattern:
- `e2e/visual/**/*-snapshots/` (matches Playwright's default snapshot dir `e2e/visual/45-visual-baseline.spec.ts-snapshots/`)

Config modification (not a new symbol):
- `testIgnore: /visual\//` added inside `playwright.config.ts` `defineConfig({...})`

(Plan 02 produces `e2e/visual/45-visual-baseline.spec.ts` and the gitignored baseline PNGs.)

Already-existing test hooks the Plan 02 spec reuses (no addition needed here):
- `data-testid="elapsed-minutes"` (`/pool-tables/:tableId`), `data-testid="active-promotions-banner"` (`/pos`), `[data-sonner-toaster]` / `[data-sonner-toast]` (library-provided, all routes)
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-visual-regression-baseline/34-CONTEXT.md
@.planning/phases/34-visual-regression-baseline/34-RESEARCH.md
@.planning/phases/34-visual-regression-baseline/34-PATTERNS.md
@.planning/phases/34-visual-regression-baseline/34-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated visual config + isolate functional config + npm script + gitignore</name>
  <files>playwright.visual.config.ts, playwright.config.ts, package.json, .gitignore</files>
  <read_first>
    - `playwright.config.ts` (the functional config being diverged from — copy only its dotenv/__dirname boilerplate and its `webServer` block verbatim; do NOT import or spread it)
    - `.planning/phases/34-visual-regression-baseline/34-RESEARCH.md` § "Pattern 1: Config isolation" — contains the complete drafted `playwright.visual.config.ts` to use as the implementation starting point, and the exact `testIgnore` rationale (Pitfall 1)
    - `.planning/phases/34-visual-regression-baseline/34-PATTERNS.md` § `playwright.visual.config.ts` / `package.json` / `.gitignore` sections — exact divergent-field list and the existing script/gitignore blocks to append to
    - `package.json` scripts block (locate the existing `test:e2e` and `test:e2e:report` entries to append alongside)
    - `.gitignore` "Test / CI outputs" block (locate the existing `e2e-results/` / `e2e-blob-reports/` lines to append alongside)
  </read_first>
  <action>
    Create `playwright.visual.config.ts` at the repo root (`bar-pos/`) as a from-scratch `defineConfig()` — do NOT import from or spread `playwright.config.ts` (they diverge on nearly every `use` field per D-01; a shared base risks one config's field leaking into the other). Copy verbatim only: the `dotenv` + `__dirname`/`fileURLToPath` boilerplate (so `.env.local` loads identically) and the `webServer` block (`command: 'npm run dev'`, `url: 'http://localhost:1420'`, `reuseExistingServer: true`, `timeout: 120_000`). Set these divergent fields exactly per D-01 and RESEARCH Pattern 1's drafted config: `testDir: './e2e/visual'`; `outputDir: './e2e-results-visual'`; `fullyParallel: true`; `workers: undefined`; `retries: 0` (a visual diff must never silently pass on retry); `timeout: 30_000`; `expect: { timeout: 10_000, toHaveScreenshot: { animations: 'disabled', maxDiffPixelRatio: 0.01 } }`; `reporter: [['list']]` only (manual/local per D-02 — no blob/json/CI reporter); `use: { baseURL: 'http://localhost:1420', headless: true, viewport: { width: 1280, height: 800 }, trace: 'off', video: 'off', screenshot: 'off' }`; `projects: [{ name: 'chromium', use: {} }]` (bundled Chromium — NO `channel: 'chrome'`, NO `slowMo`, NO `launchOptions.headless: false`). Do NOT set `globalTeardown` — the functional `e2e/global-teardown.ts` writes a report keyed to functional SUITE_MAP regexes that will never match `e2e/visual/*`.

    In `playwright.config.ts`, add exactly one line inside the `defineConfig({...})` object, alongside `testDir`/`outputDir`: `testIgnore: /visual\//,`. Without this, the functional suite's recursive glob under `testDir: './e2e'` also runs the visual spec headed with slowMo/channel:'chrome' against a baseline never seeded under those settings (D-01, Pitfall 1). This is the ONLY change to `playwright.config.ts`.

    In `package.json`, add `"test:e2e:visual": "playwright test --config=playwright.visual.config.ts"` immediately after the existing `"test:e2e:report"` entry, matching the existing `test:e2e` naming convention (D-03).

    In `.gitignore`, add `e2e/visual/**/*-snapshots/` inside the existing "Test / CI outputs" block (alongside `e2e-results/`). This keeps the local-only baseline PNGs untracked (D-12) — Playwright's default snapshot path is `{testFilePath}-snapshots/`, i.e. `e2e/visual/45-visual-baseline.spec.ts-snapshots/`. Do NOT relax or narrow this pattern in any later work: baseline PNGs contain seeded PII-looking data (customer names, in-progress PINs, dollar amounts) and must never be committed (threat T-34-01).
  </action>
  <acceptance_criteria>
    - `playwright.visual.config.ts` exists at `bar-pos/` root and `grep -q "testDir: './e2e/visual'" playwright.visual.config.ts` succeeds
    - `grep -q "headless: true" playwright.visual.config.ts` succeeds AND `grep -q "animations: 'disabled'" playwright.visual.config.ts` succeeds AND `grep -q "maxDiffPixelRatio" playwright.visual.config.ts` succeeds
    - The visual config does NOT carry functional-only settings: `grep -q "channel:" playwright.visual.config.ts` FAILS (exit 1), `grep -q "slowMo" playwright.visual.config.ts` FAILS, and `grep -q "globalTeardown" playwright.visual.config.ts` FAILS
    - The visual config does NOT import the functional config: `grep -q "playwright.config" playwright.visual.config.ts` FAILS (exit 1)
    - `grep -q "testIgnore: /visual" playwright.config.ts` succeeds
    - `grep -q "test:e2e:visual" package.json` succeeds
    - `grep -q "e2e/visual/\*\*/\*-snapshots/" .gitignore` succeeds (pattern present)
    - Functional-suite isolation smoke: `npx playwright test --list 2>&1` output contains no line under `e2e/visual/` (the functional config never enumerates the visual spec; trivially true now since the spec does not exist yet, and remains true after Plan 02 lands the spec)
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && grep -q "testDir: './e2e/visual'" playwright.visual.config.ts && grep -q "headless: true" playwright.visual.config.ts && grep -q "animations: 'disabled'" playwright.visual.config.ts && ! grep -q "channel:" playwright.visual.config.ts && ! grep -q "slowMo" playwright.visual.config.ts && ! grep -q "playwright.config" playwright.visual.config.ts && grep -q "testIgnore: /visual" playwright.config.ts && grep -q "test:e2e:visual" package.json && grep -q "snapshots/" .gitignore && echo VISUAL_CONFIG_OK</automated>
  </verify>
  <done>Isolated `playwright.visual.config.ts` exists and diverges from the functional config on headless/channel/slowMo/trace/video/screenshot/globalTeardown; `playwright.config.ts` excludes `e2e/visual/` via `testIgnore`; `test:e2e:visual` script and the `.gitignore` snapshot entry are present.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two mask test-hooks (attribute-only)</name>
  <files>src/shared/ui/LiveTimeDisplay.tsx, src/widgets/KdsBoard/index.tsx</files>
  <read_first>
    - `src/shared/ui/LiveTimeDisplay.tsx` (full file, 43 lines) — root element is the `<span className={cn('text-muted-foreground text-sm tabular-nums', className)} suppressHydrationWarning>` at lines 36-39
    - `src/widgets/KdsBoard/index.tsx` — root return element is `<div className="grid gap-6 p-6 md:grid-cols-2">` at line 238 (the non-empty-board branch; the empty-state `<div>` at line 208 is NOT the target)
    - `.planning/phases/34-visual-regression-baseline/34-UI-SPEC.md` § "Test-Hook Contract" — the hard constraint: these additions MUST NOT alter className, DOM structure, copy, or event handlers on the target elements (attribute-only)
    - `.planning/phases/34-visual-regression-baseline/34-RESEARCH.md` Pitfalls 3 (KDS board root) & 4 (LiveTimeDisplay in both KDS headers) — why each hook is needed
  </read_first>
  <action>
    Add `data-testid="live-time-display"` to the root `<span>` in `src/shared/ui/LiveTimeDisplay.tsx` (the element currently opening `<span className={cn('text-muted-foreground text-sm tabular-nums', className)} suppressHydrationWarning>`). This component is rendered in both the `/kds` and `/kds-bar` page headers as a `setInterval`-driven wall-clock; one edit covers both routes' mask (Pitfall 4). Change nothing else — keep `className`, `suppressHydrationWarning`, the `{label}` child, and all hooks exactly as-is.

    Add `data-testid="kds-board"` to the root grid `<div className="grid gap-6 p-6 md:grid-cols-2">` in `src/widgets/KdsBoard/index.tsx` (line 238, the non-empty-board return). This lets the Plan 02 spec mask the whole board region as one box, since per-card relative-age labels (`"2 min"`) have no stable selector and drift with wall-clock time (Pitfall 3, needed because D-16 seeds 1-2 pending orders so the board is non-empty at capture). Change nothing else — keep the `className` and all children exactly as-is. Do NOT add a testid to the empty-state `<div>`.

    Both additions are pure HTML-attribute additions with zero styling/behavior/DOM-structure change, per the 34-UI-SPEC Test-Hook Contract hard constraint.
  </action>
  <acceptance_criteria>
    - `grep -q 'data-testid="live-time-display"' src/shared/ui/LiveTimeDisplay.tsx` succeeds
    - `grep -q 'data-testid="kds-board"' src/widgets/KdsBoard/index.tsx` succeeds
    - The clock span's className is unchanged: `grep -q "text-muted-foreground text-sm tabular-nums" src/shared/ui/LiveTimeDisplay.tsx` still succeeds
    - The KDS board root className is unchanged: `grep -q "grid gap-6 p-6 md:grid-cols-2" src/widgets/KdsBoard/index.tsx` still succeeds
    - `git diff --stat src/shared/ui/LiveTimeDisplay.tsx src/widgets/KdsBoard/index.tsx` shows only these two files touched (no collateral edits)
    - `npm run typecheck` exits 0 (attribute additions do not introduce type errors) — note: 2 pre-existing unrelated typecheck errors (tab/model/queries.ts, agent/rag.ts) are the accepted baseline; no NEW errors introduced
    - `npm run lint` exits 0 on the two touched files
  </acceptance_criteria>
  <verify>
    <automated>cd bar-pos && grep -q 'data-testid="live-time-display"' src/shared/ui/LiveTimeDisplay.tsx && grep -q 'data-testid="kds-board"' src/widgets/KdsBoard/index.tsx && grep -q "text-muted-foreground text-sm tabular-nums" src/shared/ui/LiveTimeDisplay.tsx && grep -q "grid gap-6 p-6 md:grid-cols-2" src/widgets/KdsBoard/index.tsx && npm run lint && echo TESTIDS_OK</automated>
  </verify>
  <done>Both mask hooks (`live-time-display`, `kds-board`) are present, each an attribute-only addition with unchanged className/children, and lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner → local filesystem | Playwright writes baseline PNG snapshots to the local repo tree |
| local repo → git remote | `.gitignore` is the only gate keeping seeded-data screenshots out of version control |

## STRIDE Threat Register

This phase adds no new authentication, authorization, session, or cryptography logic — it captures already-implemented, already-RBAC-gated screens (RESEARCH § Security Domain: no new ASVS surface). Residual points below are test-integrity and data-hygiene, both low severity.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Information Disclosure | baseline PNG snapshots (seeded customer names, in-progress PINs, dollar amounts) | low | mitigate | `.gitignore` entry `e2e/visual/**/*-snapshots/` keeps baselines local-only/untracked (D-12); Task 1 forbids relaxing this pattern in any later work |
| T-34-02 | Tampering | config isolation between functional and visual suites | low | mitigate | `testIgnore: /visual\//` on `playwright.config.ts` prevents the functional suite from running the visual spec against a wrong-environment baseline (Pitfall 1) |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed this phase — `@playwright/test@1.59.1` already present; RESEARCH Package Legitimacy Audit = N/A. No install surface introduced |
</threat_model>

<verification>
- `playwright.visual.config.ts` loads its own isolated settings (headless, bundled Chromium, no slowMo/channel/globalTeardown) and does not import the functional config
- Functional `playwright.config.ts` excludes `e2e/visual/` via `testIgnore`
- `test:e2e:visual` npm script and `.gitignore` snapshot entry present
- Both `data-testid` mask hooks present, attribute-only, `npm run typecheck` and `npm run lint` clean (no NEW errors beyond the 2 pre-existing baseline typecheck errors)
</verification>

<success_criteria>
VISUAL-01 harness is in place: an isolated visual Playwright config exists (headless, bundled Chromium, no slowMo), the functional suite is isolated from `e2e/visual/`, the `test:e2e:visual` script runs the visual config, baseline PNGs are gitignored, and the two mask hooks (`live-time-display`, `kds-board`) exist so Plan 02's spec can mask the live clock and KDS board (VISUAL-03 prerequisite).
</success_criteria>

<output>
Create `.planning/phases/34-visual-regression-baseline/34-01-SUMMARY.md` when done.
</output>
