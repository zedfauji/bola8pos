---
phase: 03-ingredient-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - "03-05"
files_modified:
  - bar-pos/src/widgets/SettingsTabsPanel/index.tsx
autonomous: true
requirements:
  - S3a-07

must_haves:
  truths:
    - "SettingsTabsPanel has an Ingredients tab after the Combos tab"
    - "Ingredients tab renders ManageIngredientsTab"
    - "Ingredients tab is gated by canManageProducts (manager+)"
    - "import { ManageIngredientsTab } from '@widgets/ManageIngredientsTab' at top of file"
  artifacts:
    - path: "bar-pos/src/widgets/SettingsTabsPanel/index.tsx"
      provides: "Updated SettingsTabsPanel with Ingredients tab"
      contains: "ManageIngredientsTab"
  key_links:
    - from: "SettingsTabsPanel"
      to: "ManageIngredientsTab"
      via: "import { ManageIngredientsTab } from '@widgets/ManageIngredientsTab'"
      pattern: "@widgets/ManageIngredientsTab"
---

<objective>
Wire `ManageIngredientsTab` into `SettingsTabsPanel` as the Ingredients tab, gated by `canManageProducts`. This is a single-file modification that makes the Ingredients UI reachable from Settings in the running app.

Purpose: ManageIngredientsTab exists but is unreachable until it appears in the Settings tab list. This plan closes that gap.

Output: Settings → Ingredients tab visible to manager+ users, rendering the full ingredient management UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingredient-foundation/03-RESEARCH.md
@.planning/phases/03-ingredient-foundation/03-PATTERNS.md
@.planning/phases/03-ingredient-foundation/03-05-SUMMARY.md
</context>

<interfaces>
<!-- Direct insertion point from PATTERNS.md (verified from bar-pos/src/widgets/SettingsTabsPanel/index.tsx): -->

Insertion point — after the combos entry, inside the canManageProducts block:
```typescript
        {
          key: 'combos',
          label: 'Combos',
          render: () => <ManageCombosTab />,
        }
        // ← INSERT AFTER HERE:
        {
          key: 'ingredients',
          label: 'Ingredients',
          render: () => <ManageIngredientsTab />,
        }
```

Import to add (after ManageCombosTab import):
```typescript
import { ManageIngredientsTab } from '@widgets/ManageIngredientsTab';
```

RBAC gate: canManageProducts — same gate as the Combos tab entry (manager+).
SettingsTabsPanel uses `usePermissions()` from the rbac system — confirm the hook and `can('manage_products')` call in the file before editing.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Ingredients tab to SettingsTabsPanel</name>
  <files>bar-pos/src/widgets/SettingsTabsPanel/index.tsx</files>
  <read_first>
    - bar-pos/src/widgets/SettingsTabsPanel/index.tsx (full file — find the exact insertion point after the 'combos' key entry; confirm the canManageProducts block structure; confirm existing imports so you add at the correct line)
    - bar-pos/src/widgets/ManageIngredientsTab/index.tsx (confirm ManageIngredientsTab is exported — created in Plan 03-05)
    - .planning/phases/03-ingredient-foundation/03-PATTERNS.md (§ SettingsTabsPanel modification — exact lines for insertion and import)
  </read_first>
  <action>
1. Open `bar-pos/src/widgets/SettingsTabsPanel/index.tsx` and read the full file.

2. Add the import after the existing ManageCombosTab import line:
   ```typescript
   import { ManageIngredientsTab } from '@widgets/ManageIngredientsTab';
   ```

3. Find the entry with `key: 'combos'` inside the `canManageProducts` block. After the closing `}` of that entry (and before the closing `);` or `}` of the canManageProducts block), add:
   ```typescript
         {
           key: 'ingredients',
           label: 'Ingredients',
           render: () => <ManageIngredientsTab />,
         }
   ```

4. Do NOT add the entry outside the `canManageProducts` block — this tab must be gated to manager+.

5. Do NOT change any other part of the file — only add the import and the tab entry.
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ManageIngredientsTab" bar-pos/src/widgets/SettingsTabsPanel/index.tsx` returns 2 matches: 1 import + 1 render usage
    - `grep "key: 'ingredients'" bar-pos/src/widgets/SettingsTabsPanel/index.tsx` returns 1 match
    - `grep "label: 'Ingredients'" bar-pos/src/widgets/SettingsTabsPanel/index.tsx` returns 1 match
    - `grep "@widgets/ManageIngredientsTab" bar-pos/src/widgets/SettingsTabsPanel/index.tsx` returns 1 match (import line)
    - `cd bar-pos && npm run typecheck` exits 0
    - `cd bar-pos && npm run lint` exits 0
  </acceptance_criteria>
  <done>SettingsTabsPanel updated with Ingredients tab (gated by canManageProducts). ManageIngredientsTab renders when tab is selected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsTabsPanel RBAC gate → canManageProducts | UI tab visibility controlled by manager+ permission check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Elevation of privilege | Ingredients tab visibility | mitigate | Tab only added inside `canManageProducts` block — bartenders see no Ingredients tab; DB RLS is the defense-in-depth layer |
</threat_model>

<verification>
```bash
cd bar-pos
npm run typecheck
npm run lint
```
Both must exit 0.

Manual smoke (Tauri dev):
1. Log in as bartender (role: bartender) → Settings → confirm NO Ingredients tab visible
2. Log in as manager or admin → Settings → confirm Ingredients tab IS visible
3. Click Ingredients tab → ManageIngredientsTab renders with ingredients list (empty or seeded)
</verification>

<success_criteria>
1. Settings → Ingredients tab visible for manager+ users
2. Ingredients tab hidden for bartender users (canManageProducts = false)
3. `npm run typecheck && npm run lint` exit 0
4. No other SettingsTabsPanel changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ingredient-foundation/03-06-SUMMARY.md`
</output>
