---
phase: 13-scopes-full-rbac-from-scratch
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - bar-pos/src/shared/lib/supabase.types.ts
autonomous: false
requirements:
  - RBAC13-01
  - RBAC13-02

must_haves:
  truths:
    - "supabase db push succeeds — both migration files applied to remote DB"
    - "role_permissions table type block exists in supabase.types.ts"
    - "Insert.role is required (not optional) in the role_permissions type block"
  artifacts:
    - path: "bar-pos/src/shared/lib/supabase.types.ts"
      provides: "role_permissions table Row/Insert/Update types"
      contains: "role_permissions"
  key_links:
    - from: "supabase.types.ts role_permissions"
      to: "entities/rbac/model/queries.ts"
      via: "TypeScript type inference (used in Plan 03)"
      pattern: "role_permissions"
---

<objective>
[BLOCKING] Push both Phase 13 migration files to the remote Supabase DB and manually transcribe the `role_permissions` table type into `supabase.types.ts`.

Purpose: The DB schema must be live before the frontend entity layer (Plan 03) can be written. The supabase.types.ts transcription is the bridge — without it, TypeScript compilation fails on any code that imports from `@shared/lib/supabase.types`.
Output: DB with role_permissions table + 52 seed rows, supabase.types.ts with role_permissions type block.
</objective>

<execution_context>
@/c/Users/giris/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/giris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@bar-pos/src/shared/lib/supabase.types.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Run supabase db push</name>
  <read_first>
    - bar-pos/supabase/migrations/20260510000001_rls_rewrite_phase13.sql (verify it exists before pushing)
    - bar-pos/supabase/migrations/20260510000002_rpc_role_guards.sql (verify it exists before pushing)
  </read_first>
  <what-built>Migration files 20260510000001 and 20260510000002 written in Plan 01.</what-built>
  <how-to-verify>
Run from the bar-pos/ directory:

```bash
cd bar-pos
supabase db push
```

Expected output: Migration applied successfully. Both files should appear in the output:
- `20260510000001_rls_rewrite_phase13`
- `20260510000002_rpc_role_guards`

If supabase CLI is not installed: Install it first with `npm install -g supabase` or use the Supabase dashboard SQL editor to manually apply both migration files in order.

After push succeeds, verify the role_permissions table has 52 rows:
```sql
SELECT role, COUNT(*) FROM role_permissions GROUP BY role ORDER BY role;
-- Expected:
-- admin    | 22
-- bartender|  9
-- kitchen  |  4
-- manager  | 17
```

Run this verification query in the Supabase dashboard SQL editor.
  </how-to-verify>
  <resume-signal>Type "pushed" when supabase db push succeeds and the 52-row count query returns the correct counts. Type "dashboard" if you used the SQL editor instead of CLI.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Transcribe role_permissions type into supabase.types.ts</name>
  <read_first>
    - bar-pos/src/shared/lib/supabase.types.ts (find the Tables section; locate alphabetically correct insertion point after "rappi_orders" entry)
  </read_first>
  <files>bar-pos/src/shared/lib/supabase.types.ts</files>
  <action>
Read supabase.types.ts. Find the `Tables` section (it has entries like `audit_log: { Row: {...}; Insert: {...}; Update: {...}; Relationships: [...]; };`).

Locate the insertion point: alphabetically, `role_permissions` comes after `rappi_orders` and before `settings` (or `shifts`, whichever comes next alphabetically).

Insert the following type block at the correct alphabetical position inside the `Tables` object:

```typescript
role_permissions: {
  Row: {
    id: string;
    role: Database['public']['Enums']['user_role'];
    action: string;
    created_at: string;
  };
  Insert: {
    id?: string;
    role: Database['public']['Enums']['user_role'];
    action: string;
    created_at?: string;
  };
  Update: {
    id?: string;
    role?: Database['public']['Enums']['user_role'];
    action?: string;
    created_at?: string;
  };
  Relationships: [];
};
```

CRITICAL: `Insert.role` must be `role: Database['public']['Enums']['user_role']` (NOT `role?:`) — `exactOptionalPropertyTypes: true` is enabled.
  </action>
  <verify>
    <automated>cd bar-pos && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "role_permissions" bar-pos/src/shared/lib/supabase.types.ts returns at least 4 lines (Row, Insert, Update blocks)
    - grep "Insert:" -A5 supabase.types.ts shows "role: Database\['public'\]\['Enums'\]\['user_role'\]" WITHOUT "?" before the colon
    - npm run typecheck passes (0 errors) — confirms the type block is syntactically valid
    - The role_permissions block is positioned alphabetically (after rappi_orders or equivalent, before settings/shifts)
  </acceptance_criteria>
  <done>supabase.types.ts contains the role_permissions table type with Row/Insert/Update shapes. Insert.role is required (not optional). typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→Remote Supabase | supabase db push applies migrations to production DB — irreversible without DOWN script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | supabase db push — migration ordering | mitigate | Both files have timestamp 20260510 > all existing migrations; applied in lexicographic order |
| T-13-08 | Denial of Service | DROP ALL policies mid-transaction failure | mitigate | Both migrations use BEGIN/COMMIT; any failure rolls back atomically |
</threat_model>

<verification>
After completion:
1. `cd bar-pos && npm run typecheck` passes (0 errors)
2. `grep -c "role_permissions" bar-pos/src/shared/lib/supabase.types.ts` returns >= 4
3. Supabase dashboard shows `role_permissions` table with 52 rows and columns: id, role, action, created_at
</verification>

<success_criteria>
- Both migrations applied to remote Supabase DB
- role_permissions table has exactly 52 rows (bartender=9, manager=17, admin=22, kitchen=4)
- supabase.types.ts has role_permissions type block with required Insert.role
- npm run typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-scopes-full-rbac-from-scratch/13-02-SUMMARY.md`
</output>
